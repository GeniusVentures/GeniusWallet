---
sketch: 002
name: top-navbar
question: "How should the top navbar (brand, tabs, active-tab mark) read as the app's primary chrome?"
winner: "B and C (consumed by shipped quick-tasks)"
tags: [navbar, chrome, nav-tab, underline, dashboard]
---

# Sketch 002: Top Navbar

## Purpose

Explore the top navbar's structure and its active-tab mark (`index.html`) plus a nav-tab
alignment study (`nav-tab-alignment.html`).

## Winner (consumed by shipped quick-tasks)

- **B** — navbar redesign: 3px `brandCta` gradient underline with `brandPrimaryStrong` glow,
  icons 16→23, `appBarHeight` 60→68, idle "Connect" ghost outline. Shipped by quick-task **`1nk`**.
- **C** — nav-tab inset box shrunk 52→44px, underline regrouped into a centered column so it rides
  ~4px under the label, active tab shows no hover box. Shipped by quick-task **`bxr`**.

Winner locked in code. See the STATE quick-task rows **`1nk`** and **`bxr`** for the exact edits.
