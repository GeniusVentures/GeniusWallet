# Sketch Manifest

## Design Direction

Dark-first fintech dashboard ported to a light theme, built on the existing GeniusWallet token set
(Inter, `#14C8FF` cyan brand, mint `#2BF5B4` secondary, 15px card radius, subtle sheen gradient +
hairline border + soft card elevation). Sketches reuse the real tokens verbatim so a winner ports
directly; any deviation from a shipped token is a deliberate finding, recorded in the sketch README.

Guiding principle for this round: **a wallet with no funds is not a broken wallet.** Zero-state
surfaces should read as ready, not as failed loads.

## Reference Points

The app's own Markets and Transactions panels (existing dashboard chrome), plus the design reference
on `ui-redesign-3.514`.

## Sketches

> **Known number collisions: 020, 030, 031, 065, 066** (each number was reused for two unrelated
> sketches; directories are NOT renamed to preserve cross-refs — the pairs are disambiguated
> here by a parenthetical name and an ⚠ note on each row).
>
> **065 and 066 collided on 2026-07-27/28**, two parallel sessions on the same day, and this is the
> third and fourth time the shared counter has done it. `065 token-row-chain` + `066
> big-number-rendering` (swap picker, Phase 8) against `065 kicker-component` + `066 drawer-language`
> (component/drawer lane). All four directories keep their names, per the rule above. The reserved
> ranges in CLAUDE.md exist precisely for this and were not consulted by either session.

| # | Name | Design Question | Winner | Tags |
|---|------|----------------|--------|------|
| 001 | holdings-panel | How should the dashboard assets panel read before the wallet has any balance? | **A2 · Market-forward + Stacked header + Center gap** | dashboard, empty-state, assets, coins |
| 002 | top-navbar | How should the top navbar (brand, tabs, active-tab mark) read as the app's primary chrome? | **B and C** — consumed by shipped quick-tasks. **B** = navbar redesign (3px `brandCta` gradient underline + glow, icons 16→23, `appBarHeight` 68) via quick-task `1nk`; **C** = nav-tab inset box 52→44px + regrouped centered underline, active tab shows no hover box, via quick-task `bxr`. Winner locked in code (see STATE quick-task rows `1nk`, `bxr`). | navbar, chrome, nav-tab, underline, dashboard |
| 003 | markets-panel | How should the dashboard Markets panel header read, and what replaces the dead "Top {n}·24h" filler? | **A and D** — consumed by shipped quick-tasks. **A** = real Markets header row + shared `CryptoSparkLineChart` Assets-twin rows via quick-task `1nk`; **D** = editorial "View all" link (`GWViewAllLink`) replacing the filler via quick-task `ch1`. Winner locked in code (see STATE quick-task rows `1nk`, `ch1`). | markets, panel, header, view-all, sparkline |
| 004 | section-title | Should all dashboard panel section-titles share one title→content geometry? | **Unified section-title variant** — `GWSectionTitle` Row wrapped in `ConstrainedBox(minHeight:44)` so Assets/Markets/Transactions/Bitcoin-Chart share one title geometry — consumed by shipped quick-task `bxr`. Winner locked in code (see STATE quick-task row `bxr`). | section-title, header, dashboard, consistency |
| 005 | navbar-right-cluster | How should the top-bar's right side (chain/wallet selectors, Connect, Buy GNUS) read as one cohesive cluster, on par with the branded left? | **B · Normalized quiet chips** (chosen 2026-07-21; GSD implementation pending). **Superseded by 039** (2026-07-26): its 40px normalisation did ship via `nav_chip_style.dart`, but its quiet-fill + hairline styling was designed against the gradient sheen flattened 2026-07-25, so the cluster's final form is 039-B. | navbar, chrome, dropdowns, connect, cta |
| 006 | bitcoin-chart-section | How should the dashboard Bitcoin Chart card read — coin identity, price hierarchy, chart treatment, controls — using the mockup's chart language? | **A family** (2026-07-21): base **A · Mockup hero** (pure re-skin) + hover crosshair/tooltip; refining toward **A→ / A↓** which add a timeframe selector and drop the USD delta (% pill only). Final A-vs-A→-vs-A↓ pick pending. | dashboard, chart, price, coin, mockup-faithful, hover, timeframe |
| 007 | transaction-filters | How should the Sent/Received/Escrow/Mint transaction filter read — esp. in the narrow dashboard panel where it's a tiny scaled-down SegmentedButton — across the slim view + wider transactions area? | _pending pick_ (recommending **C · Compact icon-only segmented**: fits the title row, kills the FittedBox overflow, expands active chip's label; full labels on wide) | transactions, filters, segmented, chips, dashboard, chrome |
| 008 | hover-language | What single hover treatment makes nav tabs, timeframe tabs, and VIEW ALL feel like one interactive system (without VIEW ALL implying a persistent selected state)? | **D · Lift chip** (chosen 2026-07-21; now the design-system hover standard for all interactive chrome; timeframe done, nav+VIEW ALL handed to session 2) | hover, interaction, chrome, navbar, timeframe, consistency |
| 009 | transactions-area ~~rejected~~ | How should the transactions area read as one system — row, filter and empty states together — in both the narrow dashboard panel and the wide transactions page? | _pending pick_ (4 variants: A Ledger / B Statement / C Timeline / D Expand-in-place; supersedes the 007 filter pick — decide together) | transactions, rows, filters, empty-state, loading, dashboard |
| 010 | transaction-row-anatomy | With every transaction type sharing one row skeleton, what should the headline be — token, action, or value? | **A · Token-first** (chosen 2026-07-22; 009 rejected — it discarded the coin-icon+direction-badge identity that already works) | transactions, rows, anatomy, hierarchy, formatting |
| 011 | transaction-badges-filters | What colour should each type/status badge be, and how do filters cover all 7 TransactionTypes? | **Scheme 1 · Restrained** (chosen 2026-07-22); filters rec = **F1 two-tier**; coverage gap found: swap/purchase/process unreachable by any filter | transactions, badges, color, filters, tokens |
| 012 | icons-and-send-colour | Which glyph for Mint and Processing job, and what colour gives Sent presence without stealing Pending-amber or Failed-red? | **Sent = Slate #64748B**; **Mint icon = Pickaxe**; **Job icon = Server** (chosen 2026-07-22) | transactions, icons, color, badges |
| 013 | mint-job-badge-colour | What colour should the Mint and Processing-job badges be, given green/red/amber/slate are already taken? | **Mint = brandTertiary #C28FFF**, **Job = brandPrimaryStrong #0AAEE6** (chosen 2026-07-22) | transactions, badges, color, contrast |
| 014 | transactions-final | Consolidated spec: all locked badge/icon/colour decisions + the one open question (filter structure F1/F2/F3) | _filters pending_ (rec **F1 two-tier**) | transactions, spec, filters, badges |
| 015 | boot-loading-sequence | What does the boot screen look like during the ~9.6 s in which nothing can animate — and how does one gate replace the per-section dashboard loaders? | **C · Signal Edge** (approved 2026-07-22): mesh + centred logo + bottom-left `STATUS` line in solid grey + gradient bottom-edge rail; 3-status closing run over ~1.5 s. B withdrawn after measurement — SDK stalls at 52.5% forever, dashboard data ready at 141 ms | boot, splash, loading, progress, gate, dashboard, mode-invariant |
| 016 | compute-panel-anatomy | How does the first dashboard section split into Balance + Compute, in the 276px it actually has? | **B2 · Twin tiles** (chosen 2026-07-22). Fits ONLY because the centred why-row merged into the status sub-line (282px → 261px, +15px headroom — tightest of the 4). Carries B4's 2px brand cap on the balance tile while a job runs. `earned` line excluded. Affordance audit in README: 2 build items (public `AccountDrawer.show`, `RetryProcessingStatus` event). | dashboard, compute, balance, overview, sgnus, anatomy |
| 017 | compute-status-states | What single status component tells the truth across all nine states the compute node actually enters? | **A · Dot + label** (chosen 2026-07-22): the only treatment that stays honest in all 9 states; ring kept for the 56px `GWAiFab` only. Requires 3 code changes — stall detector, restartable processing timer, linked-wallet reason reaching the button. | compute, status, states, sgnus, processing, honesty, telemetry |
| 018 | job-request-flow | What does requesting a processing job look like as a flow, instead of one flat form and a toast? | **A · Drawer, vertical steps** (chosen 2026-07-22): `ResponsiveDrawer` = 420px right-edge panel on desktop / bottom sheet on mobile (NOT a centred pop-up); completed steps collapse to a summary that stays on screen; `/submit_job` kept as the full-screen host for deep links. Locked into Phase 14. | compute, job, flow, drawer, stepper, submit-job, bridge |
| 019 | dashboard-separators | Should the header rule Phase 12 gave Transactions exist, and if so on which panels? | **B** | dashboard, separators, consistency, chrome |
| 020 (transactions-tab) | transactions-tab | The Transactions tab is a full page rendering the dashboard *panel* — what should the page be, and where should the empty state sit (page + panel)? | **B · Filter rail** + **E2 · Anchored** (chosen 2026-07-22; E3's action buttons rejected). Superseded by 021. | transactions, page-layout, filters, empty-state, tab · ⚠ number 020 reused — see both rows (also `020 (boot-mesh-depth)`) |
| 021 | transactions-tab-final | Consolidated: rail state marks traced to precedent, the E2 anchor height, the empty-state icon, and how Amount/Value/Status should read for jobs and failed transactions | Chosen 2026-07-22: anchor **c** (480px bounded centre), icon **4 · Two-way arrows**, amounts **print the real number + status carries the truth** (S1's sign-dropping overruled). Active-mark question moved to 022. | transactions, tab, rail, empty-state, amounts, edge-cases |
| 022 | rail-active-state | How is the ACTIVE filter-rail row marked — leading bar, underline (with or without recoloured label), gradient count, both, or a gradient wash? | **B2 · Underline only** (chosen 2026-07-22) — the navbar's active-tab mark copied outright: w700 label never recoloured, 2px gradient rule beneath, glyph untouched. C and E rejected on measured grounds. | transactions, rail, active-state, gradient, navbar, contrast |
| 020 (boot-mesh-depth) | boot-mesh-depth | The boot canvas walked too bright against the dashboard — how dark, and by which knob? | _pending pick_ (rec **B · Deep**; key finding: `intensity` scales blob alpha AND the black dim overlay together, so lowering it makes the canvas paler, not deeper — the two need separating) | boot, splash, mesh, color, depth, handover · ⚠ number 020 reused — see both rows (also `020 (transactions-tab)`) |
| 023 | transactions-page-proportions | The page reads badly at fullscreen and the rail's All row sits oddly — what is design and what is a slip? | _pending pick_ (rec **V3** hug-floored-to-rail · **W1** keep 1280 · **R4** All becomes a summary, or **R1** for the one-Divider fix). Both complaints traced to implementation slips: `CrossAxisAlignment.stretch` at `:346`, and the `All → rule → Type` divider 022 specified being absent while the Status one is present. | transactions, page, proportions, rail, vertical-fit |
| 024 | transactions-fullscreen | How should the transactions page read at fullscreen width? | _no winner recorded — `index.html` only, no README_ | transactions, page, fullscreen, proportions |
| 031 (news-time-and-refresh) | news-time-and-refresh | How should news-card timestamps and the feed's refresh affordance read? | _no winner recorded — `index.html` only, no README_ | news, timestamp, refresh, feed · ⚠ number 031 reused — see both rows (also `031 (receipt)`) |
| 100 | news-page-layout | What is the Crypto News tab for — fast scanning, visual browsing, or a filterable feed? | **B · Magazine** (chosen on identity; density then refined by 101/102) | news, page-layout, feed, rail, cointelegraph |
| 101 | news-magazine-tuning | B won — how does the magazine layout carry 30 items without becoming eight screens of scrolling? | **B2** (Next up band; refines 100; further tuned by 102) | news, magazine, density, components, hover |
| 102 | news-hero-band | How do two improved B2s fix both the first impression and the page length? | **B2 (reference)** — chosen 2026-07-23 (plain B2: hero + Next up + full 3-col grid; the shorter-page B2-I/B2-II were not taken) | news, magazine, next-up, hero, density |
| 103 | markets-page | What structure should the full Markets page have, and how much of `CoinGeckoMarketData` should it surface — reusing shipped components? | _pending pick_ (4 variants: A Faithful grid+ / B Ranked table / C Featured+grid / D Segmented; all add rank+mktcap per Jakub). Current page is a flat card grid with a dead centre gap, surfacing 4 of ~26 model fields. | markets, page-layout, grid, table, sparkline, coingecko, data-density |
| 104 | coin-detail-page | What structure should the `/token-info` coin page have so identity + price + chart + actions + stats + convert read as one page, reusing today's data/widgets? | _pending pick_ (4 variants: A Faithful cleanup / B Exchange header / C Identity rail / D Unified stack = mobile). Findings baked in: %-pill color/sign bug fixed (`crypto_live_chart.dart:234` vs `:342`), identity moved beside price, dead chart space removed. Open: timeframe tabs (visual only — needs range fetch, per sketch 006) vs zoom/pan. | coin-detail, token-info, chart, layout, actions, convert, market-data |
| 025 | transactions-header | A lone "Transactions" word floating top-left over a 1600px page reads as unfinished — where should the title sit and what fills the header row? | _pending pick_ (rec **B · Title + toolbar actions**, the Markets tab shape using GWPageHeader's existing trailing slot; optionally **C · subtitle**). D/E diverge from the sibling tabs. | transactions, page, header, title, toolbar |
| 026 | page-structure | How should the page title relate to the app's shared chrome? The navbar already names the page (active tab) and the page repeats it 24px below — a floating redundant title. | _pending direction_ (rec **S1** drop the repeat + slim toolbar, page-local; **S3** left sidebar is the real fullscreen-emptiness fix but a milestone; **S2** full-width header band is the middle path) | transactions, page, structure, navbar, shell, sidebar |
| 030 (transactions-status-placement) | transactions-status-placement | Design A (Status only) — where does the Status pill sit in the wide row? Three placements. | _pending pick_ (rec **A2 · beside the amount** — pills form a clean column next to the value, amount stays flush right; **A1** hugs the description; **A3** puts status after the amount but un-flushes the amount, reversing the just-approved fix) | transactions, rows, status, placement, wide, ledger · ⚠ number 030 reused — see both rows (also `030 (drawer-shell)`) |
| 029 | transactions-wide-columns | With the amount now flush-right, a fullscreen row has a cavern between description and amount. Which real per-transaction columns should fill it? | _pending pick_ (rec **B · Status + Fee** — both grounded in `transaction.dart` and scanned often; **A** Status-only is lightest, **C** adds Reference/hash for an explorer-style ledger; Network/chain excluded — no such field. Wide-only, ≥`large` 1024; narrow panel unchanged) | transactions, rows, columns, status, fee, wide, ledger |
| 028 | transactions-empty-state | On a never-transacted wallet (`scoped.isEmpty`) the rail is hidden, so the list card stretches full-width and a 72px icon floats in ~1280×460 of empty card — reads as a failed load. How should the empty state sit? | _pending pick_ (rec **E-a · narrow centred card ~520px** — `Expanded`→`Center(ConstrainedBox(maxWidth:520))`, smallest diff, keeps the card rhythm; **E-b** no-card is the airier fallback; **E-c** keep-rail-with-zeros shown only to prove it reverses 021/022 and reads worse) | transactions, empty-state, page, card, zero-state, first-run |
| 027 | navbar-title-gap | The 28px page title is glued to the navbar (8px gap). How much air between the 68px navbar and the title line? | _pending pick_ (rec **32px `space16`** — the 4-pt "new region" step — applied to Transactions/Markets/News together for one rhythm; 24px `space12` is the safe alternative) | transactions, spacing, header, navbar, 4pt-grid, app-wide |
| 030 (drawer-shell) | drawer-shell | Every drawer routes through one `ResponsiveDrawer` (420px right panel / bottom sheet). What is the shared skeleton — header, body padding, footer, edge relationship — given callers pass bodies with no shared convention (the tx detail panel has ZERO horizontal padding → values slam the window edge)? | **B · Header band → round-2 B1 "Quiet band"** (chosen 2026-07-23; left-aligned title, close top-right, faint gradient hairline, full-height, 20px body padding, footer top border). Round-2 DECIDED = **B1** (see `drawers-final/README.md`). | drawers, shell, responsive-drawer, chrome, padding, header, footer · ⚠ number 030 reused — see both rows (also `030 (transactions-status-placement)`) |
| 031 (receipt) | receipt | How should a transaction/result receipt read — hierarchy of identity, amount, status, detail rows? Hero = the broken "Escrow released" panel. | **B · Status-led → round-2 B1 "Pill + sections"** (chosen 2026-07-23; status leads via icon+pill+status-row, amount stays neutral; all **4** `TransactionStatus` states incl. Cancelled=slate). Round-2 DECIDED = **B1** (see `drawers-final/README.md`). | drawers, receipt, transaction-detail, amount, status, rows · ⚠ number 031 reused — see both rows (also `031 (news-time-and-refresh)`) |
| 032 | list-picker | How should a searchable, selectable list drawer read — row anatomy, search, selection state? Hero = Select Network. | **A · Plain list → round-2 A1 "Comfortable"** (chosen 2026-07-23; tappable rows, rounded gradient-tint selection + gradient check, no accent bar). Round-2 DECIDED = **A1** (see `drawers-final/README.md`). | drawers, list, picker, network, search, selection |
| 033 | confirm-action | How should an Approve/Reject request drawer present WHO is asking and WHAT is being signed? Hero = dApp Transaction Request. | **B · Sign-emphasis → round-2 B1 "Static caution"** (chosen 2026-07-23; static caution, no unbacked "new address" claim; fiat `*` = price feed not yet wired). Round-2 DECIDED = **B1** (see `drawers-final/README.md`). | drawers, confirm, approve, reject, dapp, reown, security |
| 034 | receive-qr | How should a receive drawer balance QR, full address, copy action, and the network warning? Hero = Receive. | **A · QR hero → round-2 A2 "Grouped address"** (chosen 2026-07-23; gap above QR, network chip above QR, 4-char address chunks, copy only — no Share/set-default). Round-2 DECIDED = **A2** (see `drawers-final/README.md`). | drawers, receive, qr, address, copy, network-warning |
| drawers-final | drawers-final | The five chosen drawer designs, consolidated into one `index.html` with round-2 refinements applied. | **030 B1 · 031 B1 · 032 A1 · 033 B1 · 034 A2** — the decided round-2 winners (see `drawers-final/README.md`). | drawers, final, consolidated, responsive-drawer, gradient |
| 035 | web-address-bar | The Web tab's address bar (`_buildSearchBar`) is a full-width unstyled strip bolted under the redesigned navbar — how should the browser chrome read as part of the app? | **B · Scalony toolbar** (chosen 2026-07-24) — unified omnibox: back/forward nested into the field's left edge, favicon + green lock + host, refresh at the right edge, ⋯ menu alongside. Kills the "second bar under the bar". A (re-skin) and C (Arc-minimal) not taken. Consolidated in 037. | web, browser, address-bar, chrome, omnibar, url |
| 036 | web-tab-strip | Tabs are hidden behind a count button that opens a full-screen manager (with upside-down thumbnails) — how should switching + adding tabs work on desktop? | **A · Permanent tab strip** (chosen 2026-07-24) — always-visible horizontal strip: favicon + title + ×, active = surface-elevated + 2px brand underline (navbar's mark), `+` adds a DuckDuckGo tab. Replaces the full-screen `_buildTabManager` + `1` counter. B (overlay grid) kept as overflow idea; C (side rail) not taken. Consolidated in 037. | web, browser, tabs, tab-strip, tab-manager, new-tab |
| 037 | web-chrome-combined | 035 + 036 assembled — how do the chosen address bar and tab strip read as one full Web-tab chrome? | **B · 035-B + 036-A** (chosen 2026-07-24) — navbar + horizontal tab strip (036-A) + unified omnibox toolbar (035-B); ≈178px chrome. The implementable spec for Phase 18. | web, browser, chrome, combined, synthesis, phase-18 |
| 150 | feedback-tab | The Feedback tab (`/logs` → "Send Feedback") is a bare Material form while Transactions/Markets/News are full pages — what makes it a first-class sibling tab without changing the real `Sentry.captureFeedback` + auto-attached-SDK-logs mechanic? | _pending pick_ (rec **A · Composer card**, smallest diff; **B · Context rail** reuses the Transactions two-column shape and makes the payload fully legible; **C · Guided/typed** adds a Bug/Idea/Question chooser — needs a `scope.setTag` wire). Design-lane B. Every variant promotes the buried "logs attach automatically" line to real UI and keeps all 5 states honest (Ready/Sending/Success/No-SDK/Failed). | feedback, logs, submit-logs, page-layout, tab, sentry, states |
| 152 | token-detail-responsive | How does the token-detail page adapt across breakpoints, using only real data? Resolves the open pick on **104**. | **A @ ≥768 (faithful two-panel) + D @ <768 (unified stack, Convert below graph)** — chosen 2026-07-24. Timeframe tabs dropped (no per-range fetch), Convert price read-only, only real fields. | coin-detail, token-info, responsive, layout, convert, data-honesty, resolves-104 |
| 151 | news-search-refresh | News search + refresh are both **real & wired** (`crypto_news_screen.dart`: debounced `GWSearchField` filter + `_UpdatedStamp` re-fetch). Sketch 031 shipped them **split** (search full-width, refresh top-right) and never composed the pair. How should the pair read together? Re-skin, not rebuild. | _pending pick_ (rec **B · One toolbar line**, search grows + refresh pill on the same row, near-zero new code; **A · Polished split** = today's layout + live dot, fallback; **C · Docked** = refresh+green age inside the field, tightest; **D · Command bar** = elevated pill + live pulsing status, boldest/loudest — borrow its dot). Design-lane B. | news, search, refresh, header, freshness, re-skin |
| 105 | swap-tab | How should the Swap tab read as a whole — layout of the two fields, the flip, route details, and the CTA states — reusing the live Squid data the code already has? | **A1 · Focused (bigger)** — chosen 2026-07-24. R1 picked A · Faithful, R2 refined to A1: 560px column, 38px amounts, brand sheen, subtitle, flip in the seam (kills the `-170` hack). | swap, squid, cross-chain, layout, fields, route, cta, states, presence, drawer |
| 120 | phase-8-flow | What are the missing designs that complete the whole Phase 8 (Swap & bridge) flow — beyond the swap tab (105 A1) — so the phase is design-complete? | **B1 · Swap-twin** for the bridge screen — chosen 2026-07-25 (mirrors the Swap tab exactly; instant recognition; lowest-churn reskin of `bridge_screen.dart`; bridge is 1:1 burn→mint so the swap layout carries over). Result/receipt = **reuse the shipped 031-B** for BOTH swap and bridge (resolves the two competing drawer impls, covers findings 21 + 28). Swap route-fetch error (finding 22) designed: receive → `—` + red "not current" notice + Retry. | phase-8, swap, bridge, squid, gnus, states, error, result, receipt, drawer, flow |
| 121 | phase-8-storyboard | What does the whole Phase 8 experience look like page-by-page, from the user's perspective — Swap and Bridge as ordered journeys? | _walkthrough, no winner_ — a map, not a decision sketch. | phase-8, swap, bridge, storyboard, flow, walkthrough, user-journey |
| 038 | dashboard-column-widths | How much width should the Transactions column get, and what do the panels on the left pay for it? | **A1** (chosen 2026-07-25) | dashboard, layout, widths, columns, transactions, chart, three-column |
| 039 | navbar-right-cluster-v2 | How should the navbar's right cluster read in today's flat language - and survive Connect's 4 states plus the vanishing SDK button? | **B · one track** (chosen 2026-07-26) - control track holds the three context selectors; Connect and Buy GNUS stay outside as actions. Two refinements decided by Jakub: Buy GNUS stays 40px, and all `ReownConnectButton` states unify to transparent-fill + 1px state-coloured outline. **Supersedes 005**. | navbar, chrome, cluster, dropdowns, connect, cta, reown, sdk, states |
| 040 | swap-page-header | The page title should sit top-left like the other tabs, but the swap column is centred at 560px - how do you reconcile the two axes? Finding: the shipped code is a **regression of 105 A1**, where the header sat INSIDE the 560px column. | _pending pick_ (rec **A · Header bar** - the title stays on the page frame's X, the row gets a full-bleed 1px line plus a labelled `tune`; **B · One axis** = everything on the left edge, the most consistent but an empty right half; **C · Companion** ⚠ needs data the screen does not have; **D · Bar + one axis** = an A+B hybrid, plus an “A|B side by side” view). A change in `gw_page_header.dart` = the answer to **025** and **026** as well (S2 recommended there = variant A). | swap, page-header, title, layout, gw-page-header, chrome, resolves-025, resolves-026 |
| 041 | swap-page-buildout | What should the Swap page be built out with, so a left-aligned title actually has something to govern - using only data the app already has? Continues 040. | **3 · Ledger** - chosen 2026-07-26 (UI in English; slippage = presets plus a manual field validated 0<x≤50, warnings >5% and <0.05%, because the real drawer only has `double.tryParse` with no range). It was the rec - the only variant 100% on data this screen writes itself: `swap_screen.dart:247` creates `Transaction(type: swap)`; runner-up **4 · Route** on `SquidRouteResponse.route/gasCosts/feeCosts`; rejected **1 · Desk** - its left rail stands on `mockSquidBalances`, `squid_token_service.dart:36`. Also **2 · Pair** (CoinGeckoMarketData, still to be wired) and **5 · Workshop** (modular)). **Refutes 105-A2 and 040-C**: the swap history IS real. | swap, page-layout, buildout, left-align, data-honesty, squid, transactions, coingecko |
| 060 | swap-companion-rail | What should fill the Swap page so a left-aligned title has something to govern - five whole page structures, each judged in the EMPTY state, not the filled one. Continues 040; parallel to 041 (same thesis, different variants). | _pending pick_ (rec **A · Rail** - a 560 form under the title plus a 440 rail: Route / Recent swaps / Balances, 1024 block; **D · Workshop** = 6 tiles, the least emptiness, but 6 new components; **C · Ribbon** keeps 105 A1's centred column at the cost of an empty band before the quote; **B · Portfolio-first** reverses the reading order; rejected **E · Checkout** - it takes the CTA out of the form, against D-09/D-11). Data audit: the swap history is **real** (`swap_screen.dart:247` + `transactions_slim_view.dart:92`), the route and balances are **mocked** (`squid_token_service.dart:36,59`) - the whole screen stands on mocks. Renumbered from 042 (collision with `042-navbar-no-cta`). | swap, page-layout, companion, rail, left-align, data-honesty, squid, route, balances, recent-swaps, empty-state |
| 061 | coin-page-in-app-language | How should the coin page read like the rest of the app - given it is the only one with no navbar (`/token-info` sits OUTSIDE `ShellRoute`, `router.dart:242`), has a centred 1200 frame instead of xxl + a 12 gutter, and an `AppBar` with a breadcrumb instead of `GWPageHeader`? Chrome level, not content - the internal layout was settled by 152. | _pending pick_ (rec **A · Inside the app frame** - the route returns to `ShellRoute`, a “← Markets” chip, the coin identity as `GWPageHeader` plus the price in `trailing`, 1536 frame; **B · Stat bar** adds KPI tiles from fields the model HAS and the screen ignores (`marketCapRank`, `high24h/low24h`, `athChangePercentage`, `fullyDilutedValuation`); **C · Band** = a hero band like News/102, at the cost of a third layer of chrome; **D · My position** = 3 columns around `Coin.balance`, needs ≥1400px; **E · Detail with no navbar** = zero router changes, but it does not cure the missing navigation). Upholds 152: Convert read-only, no 1H/1D/1W tabs, Send/Swap disabled (D-01/D-02). | coin-detail, token-info, page-layout, gw-page-header, page-frame, shell-route, navbar, consistency, data-honesty, follows-152 |
| 062 | ledger-variants | Since 041/3 Ledger won (title on the left plus a build-out on the right), what shape does that build-out take - and which of the data under it REALLY exists? Every data point carries a `HAVE` / `EASY` / `BUILD` marker. Continues 041; consolidates 040/060. | _pending pick_ (rec **A · Rail** - a 560 form plus a flexible `Your swaps` rail plus `Summary` 300; the only variant whose right-hand side promises nothing that does not exist today: one `BlocBuilder<TransactionsCubit>` and one `fold`. Runner-up **D · Ledger** - a full-width table, the best use of 1536, but the `Tx` and `Status` columns are dead today. **B · Ribbon** = a stats band under the title; on an empty wallet, four dashes. **C · Receipt** = a dual-purpose rail (route costs / history), extracting the most `EASY` out of `gasCosts[]`/`feeCosts[]`. Rejected **E · Console** - left rail on `mockSquidBalances`, the same fault as 041/1 Desk). **Corrects 041**: the history is real, BUT `swap_screen.dart:227` is a `// TODO: invoke Squid API`, `:238` writes `hash: ""`, and `:246` hardcodes `TransactionStatus.completed` - the three-state badges have no producer. **New bug:** `:243` writes `fees: fromAmount`, and `transaction_displays.dart:474` renders it as “Network Fee” - the swap drawer **already shows a wrong number today** (todo `2026-07-26-swap-writes-the-swap-amount-into-the-fees-field.md`). The sketch carries its own **“Backend - what is missing”** section: 7 items with weight/file/line, highlighted per variant. Summary without `Total swaps`/`Settled` (Jakub, 26-07). | swap, page-layout, ledger, left-align, data-provenance, data-honesty, squid, transactions, empty-state, backend-gaps, follows-041 |
| 063 | settings-drawer-archetype | How does a drawer that is a FORM read? An audit found the gap: 030-034 covered the shell, the receipt, the list, the confirmation and QR receive, but **there is no form archetype** - and `swap_settings` (1 field), `sdk_account_manager` (7) and `token_info` (2) all need one. The shell is NOT redesigned here: every variant sits inside the approved **030 B1 “Quiet band”**. | **CLOSED WITH NO CHANGE (Jakub, 2026-07-31): what is in the app today stays.** None of the variants goes in - `swap_settings`, `sdk_account_manager` and `token_info` keep their current look, and the sixth archetype is NOT created. Do not ask about it again without a new reason. The variants and their analysis stay in `index.html` in case this topic ever comes back: the rec at the time was **A · Presets + custom value** (presets as the main road, the field as the escape hatch, validation under the field; it degrades into variant C's rows at 7 settings, so A and C were one system rather than rivals), runner-up **C · Rows**, rejected **B · Hero** and **D · Sections**, and **E · No Apply** was a system question, because the footer belongs to 030 B1. It carries `slipState()` from sketch 041 with a runnable check (11 cases) - the real drawer today has `double.tryParse` with no range and accepts both 0 and 900. | drawers, settings, form, archetype, slippage, validation, scale, follows-030, follows-041 |
| 153 | feedback-page | 150 decided the Feedback *card*; this decides the *page* around it - a 560 column on a 1536 frame leaves ~430px dead on each side. What shape, using only the shipped pantry (type chooser, message, two log probes, SDK/platform, status, send, reference number)? | **B · Focused frame** - chosen by Jakub 2026-07-26, executed as Phase 20. (rec was **B** - page frame capped at 1040 so leftover reads as margin, title top-left like the other tabs, composer 640 + receipt rail 380; runner-up **A · Sheet** = smallest diff, keeps `centered:true`; **C · Split card** = same frame, one surface; **D · Steps** = one axis at 640, numbered procedure; rejected **E · Wide composer** - full-bleed textarea, line length breaks every other card). ⚠ B/C/D/E revert the uncommitted `centered:true`. Finding: `_candidateLogNames` is a 2-element const, so the receipt can never exceed two log chips. | feedback, logs, submit-logs, page-layout, page-frame, gw-page-header, data-honesty, follows-150 |
| 064 | feedback-controls | The type control on Feedback has NO hover at all, and its active state is FLAT mint. Both break a decision the project already made (044 - one hover recipe for the whole app; `drawers-final` - the accent is a gradient, never a flat brand). How does it look when it carries the app's own language - and while we are here: the Message field's border and the rail title? | **B · Gradient underline** - chosen by Jakub 2026-07-27, shipped in phase 20 (3px `brandCta`, rounded on top, `brandPrimaryStrong` glow at 50%/blur 10, 200ms, `GWDecorations.hover` on the inactive ones). This is exactly the active-tab language from the nav bar - the argument being that the app ALREADY has a word for “this one is selected”. Runner-up **C · Tint + hairline edge** (the 044 recipe promoted to the active state) - rejected only because hover and active blur into one picture. Rejected **A · Gradient pill** - a filled gradient is CTA language, and this is not a CTA. Also **0 · Today** (reference) and **D · Text only** (too quiet on a page whose entire job is a form). Tab 2 - the Message field gets a gradient focus ring (`BorderSide` takes a single `Color`, so it CANNOT be an `InputBorder`) and a label ABOVE the field, matching `GWTextField` across 7 files. Tab 3 - “What gets sent” → **“Attached automatically”**. **Findings:** at the time the sketch was built, the 044 hover recipe was used in EXACTLY one place in the app; a `BoxDecoration` with a border INSETS its child, so a none→1px animation jitters by 1px (**the nav tabs carry the same latent bug**); a `Stack` gives unpositioned children LOOSE constraints, so `textAlign: center` centres nothing. **Open:** whether B spreads to the transaction filters / the Markets timeframe tabs / the Web tab strip. NOT to selection lists - 032-A1 settled that differently. | feedback, logs, submit-logs, segment-control, tabs, hover, gradient, focus-ring, follows-044, follows-030 |
| 154 | transaction-details-drawer | Sketch 031 already settled how a transaction receipt should read, and it was never carried over. What does the details drawer look like in the drawer language the app shipped - using only the fields `txRowContent` already computes? | _pending pick_ - lane B (design session). Continues **031 · receipt** (winner **B1 · Pill + sections**) and **030 · drawer-shell** (winner **B1 · Quiet band**); both decisions live in `drawers-final/README.md` and neither reached this drawer. **Finding:** `showTransactionDetails` (`transaction_displays.dart:430`) is a bare `ListView`, and `_buildDetailsCard` (`:421`) has vertical padding ONLY - labels touch the panel's left edge and values hit the right. The header has a 20px title inset and the footer has 20px padding; the body is the one zone that forgot. Exactly the defect sketch 031's design question named a year of sketches ago. | drawers, transaction-detail, receipt, responsive-drawer, status, copy, data-honesty, follows-031 |
| 065 ⚠ | token-row-chain | The swap token picker lists the same symbol on several chains — **ETH on 1/137/80001**, **USDT on 1/137** — and the shipped row (032-A1) names the chain nowhere, so three rows read identically and mean three different assets. How does the chain become visible without breaking the list-picker archetype the network picker, account switcher and Search Coins all share? | **C · Grouped by chain** — chosen by Braian 2026-07-27 at the 08-07 walk: the only variant where the chain is still on screen after the row that named it scrolled away, and the only one where two identically-named tokens can never sit adjacent. Rows keep 032-A1 byte-for-byte; the chain becomes structure. A and B not taken. **Real cost, invisible in the HTML:** sticky headers need `SliverPersistentHeader` in a `CustomScrollView`, not the drawer's current `ListView.builder`. **Still owes:** search-vs-grouping (a 2-token filter leaves 2 headers + 2 rows), the thin-wallet all-headers case (MORE likely now the pay side is holdings-filtered), and whether the unfiltered receive side wants the same treatment. Rejected: (**A · Chain crest** = 17px chain logo on the token avatar, zero vertical cost, one `Stack`+`Positioned`, the pattern every multi-chain wallet already teaches — but 17px says *that* there is a chain, not *which*; **B · Chain on the meta line** = `symbol · chain` with a 12px icon, the only variant where the chain is READ rather than recognised, at the cost of the row's last free text line; **C · Grouped by chain** = sticky per-chain sections, the only variant where the chain survives scrolling, at the highest vertical cost). Raised by Braian at the 08-07 walk, 2026-07-27. **Hard constraint:** `networks.json` knows 10 chains with real icons, but the catalogue also carries **42161 / 250 / 1284 / 80001**, which it does not — `getNetworkById` answers `'Unknown'`, so every variant must render an honest unknown state rather than a hole. Content is the real pay-side list after `heldTokens()`. | drawers, list, picker, token, chain, swap, phase-08, follows-032 |
| 066 ⚠ | big-number-rendering | Two slots must survive the same ladder — the picker's trailing balance (420px row, shared with a name and symbol) and the swap card's 38px hero amount (which MAX fills verbatim). Today both are one unwrapped mono string: `1000000000000` is 13 digits and `0.000000000000001` is 17 characters in a font sized for `1.5`. | **B · Grouped + exact second line** — chosen by Braian 2026-07-27 at the 08-07 walk: never hides a digit, which is the right default for a balance you are about to spend. Second line renders ONLY when the grouped form differs, so ordinary balances keep today height. **⚠ compounds with 065-C:** both winners spend vertical space in the SAME list (C adds a header per chain, B adds a line per row) — 6 tokens over 3 chains roughly doubles the drawer height, and neither sketch was evaluated with the other applied. **Still owes:** which slot gets which treatment (row = scanning, hero = editing), and that thousands separators are locale-shaped while the app has no locale formatting today. Rejected: (**A · Compact suffix** = `1T`/`1.23B`, bounded and fastest to scan, but abbreviating money you are about to spend hides 1.0 vs 1.49 trillion — arguably disqualifying; **B · Grouped + exact second line** = nothing ever hidden, at a vertical cost paid by the ~90% of balances that are ordinary; **C · Fit-to-width** = always exact, type stepping 38→28→20→15px, closest to today). Raised by Braian at the 08-07 walk, 2026-07-27, once the magnitude fixtures put it on screen. **Hard constraint on C:** commit `37639d5` bans deriving a dimension CONTINUOUSLY from constraints (it froze the macOS app); C must be a small fixed set of steps keyed to string length, never a measured per-frame fit — the same rule `GWEmptyState` follows. Open question: the row is a scanning surface and the hero an editing surface, so they may deserve different answers. | numbers, typography, swap, picker, phase-08, magnitude, data-honesty |
| 065 ⚠ | kicker-component | The uppercase section label exists in 5 places and was hand-written in every one; they agree on weight (w600) and colour (textSecondary) and disagree on size (10/11/13) and tracking (0.4-0.88). What single value serves all of them plus receipt 154-A? | **C · Two steps** (Jakub 2026-07-27, after a transient pick of D) - 13/0.5 by default, 11/0.6 when `dense`, no rule; declared the foundation of the whole drawer language, see 066. **BUILT** as `lib/components/cards/gw_kicker.dart` + `GWKicker.style()` for the 2 interactive consumers; all 5 call sites migrated, quick 260727-w58. Rejected A/B (force a dimension change on 2 Markets surfaces), E (reverses a convention working in 4 places), D (drawn, unused - no `rule` flag in the code). Found: textSecondary is `static const`, so the kicker measures 3.23:1 in light mode - breaking AA in all 5 places at once. | typography, kicker, section-label, uppercase, components, consistency, drawers, follows-154, follows-030 |
| 067 | section-grammar-real-components | 160-1 was picked but specifies three components of its own that do not exist: a kicker at 10/w700/0.8/textPrimary54 (a sixth value, contradicting 065-C), a card at radiusMd+surfaceSheen (not GWCard) and a row at 14/14 (not _buildRow). What does the same shape look like built ONLY from what the code already has - and what groups the content, given a card on a drawer panel measures 1.11:1? | **A · Kicker only** (Jakub 2026-07-27), together with the **156-A** canvas. **BUILT 2026-07-28, quick 260728-q7c** - kicker plus one changed number, the gap under it 4 → 12, because with no container the gap IS the grouping.; kicker at the default 13 step; body padding moved to the shell. Rejected B (kicker with a rule) and C (card at a white-36% border) - both only earn their keep at two sections, so **B returns as a real candidate for receipt 154-A**. **Key finding: a card on a drawer panel fails on arithmetic, not taste** - GWCard measures 1.11:1 today and **1.00:1 after 156-A** (the same colour), the 12% hairline is 1.42:1, and the first edge to pass 1.4.11 is white **36%** (3.32:1), a visibly grey line. Variants A and B introduce NOT ONE new widget. | drawers, sections, components, contrast, wcag, swap-settings, follows-160, follows-065, follows-156 |
| 066 ⚠ | drawer-language | The shell change reached 19 drawers: 2 shipped, 2 slated for deletion, 1 decided separately (154-A). What language do the remaining 14 share on the 065 kicker foundation, given 7 of them still ship raw OutlinedButtons in Colors.greenAccent? | **Five archetypes accepted** (Jakub 2026-07-27) - A Consent (reown x2), B Result (Banxa x2 + reown swap result, sharing 154-A's hero), C List picker (4 drawers, 032-A1 row + dense kicker grouping), D Receive/QR (3 drawers), E Action menu (2 drawers, kicker fences the irreversible action). Adjusted from 065-D to 065-C in the same review; `index-065d.html` kept as the comparison. **Findings: Receive is implemented THREE times and More Options TWICE** - 5 files, 2 designs, so D and E reduce 5 implementations to 2; **7 of the 14 have footers entirely outside the design system** (raw OutlinedButton in Colors.greenAccent/grey/black, radius 4, height 40) with Approve and Reject reading as equally important; `lib/banxa/` is fenced by CLAUDE.md so its 2 drawers cannot be implemented without lifting it. Body padding had to land in the shell first, or one fix would have become fourteen. Execution not started. | drawers, archetypes, consistency, reown, banxa, receive, menu, follows-065, follows-030, follows-032, follows-154 |
| 156 | drawer-canvas-colour | The drawer is the lightest large surface in the app (`surfaceMenu` #171A21, ~2 steps lighter than anything it opens over) and its input field is painted the same colour as the panel it sits on (1.00:1 - the field is a hairline, not a box). What colour is a drawer? | **A · Card canvas** (Jakub 2026-07-27, taken together with 067-A). **BUILT 2026-07-28, quick 260728-q7c**, with two corrections to this sketch. (1) **Its own 30% edge is wrong**: it reads 2.63:1, not the 3.0:1 claimed - 30% clears 3:1 against the FIELD, but `GWFocusRing` paints its edge as an outer `DecoratedBox`, so the visible ring composites over the PANEL. Sketch 067's table had white **36% = 3.30:1** independently; that shipped as the new `GeniusWalletColors.borderControl`. (2) **Its finding 4 applies to the panel too** - #0C0E14 against the `black54`-scrimmed page is **1.05:1** and the drawer loses its silhouette, so the panel takes a card's whole recipe, fill AND `borderStrong` hairline (2.01:1). **The knock-on list was incomplete**: the three it named were no-ops; the one that mattered was unlisted - anything in a drawer already painted `surfaceElevated` drops to 1.00:1 (`account_dropdown_selector` row `tileColor`, `sdk_account_manager` card `background`, 2 `MenuAnchor` popups), all moved UP to `surfaceMenu`. Fixes 3 things nobody had filed (receipt action badge, receipt avatar punch-out ring, bridge network rows - all painted for a card, on a drawer that was not one). **Its own recommendation to ride the ~19-drawer walk was NOT followed**; reading all 19 call sites substituted for it and is what found the fourth knock-on. The walk is still owed. | drawers, colour, surfaces, tokens, contrast, wcag, responsive-drawer, swap-settings, follows-030, follows-063 |
| 068 | list-picker-drawers | Select Network, SDK Accounts and Your Accounts all do one job - pick one from a list - and ship 3 different rows, 2 different headers and 2 different footers, none looked at since the shell changed. What does one archetype look like on today's canvas, and where does it stop being one? | **A · Loose rows, CHOSEN + BUILT 2026-07-28** (quick 260728-p2m): `_TokenRow` promoted to `GWSelectRow`, all 4 pickers on it, `BottomDrawer` removed from SDK Accounts, Your Accounts footer to GWButton. Build also found a 5th defect the sketch missed - SDK's GWCard row grew a **2px** border when selected, moving the row's geometry by a pixel on every state change; runner-up C · Grouped (B + dense kicker; rejected as default because 2 of the 3 lists are ~3 items and a group header over one row promises structure the data lacks); **B · Ruled well REJECTED on a mechanic worth remembering - a scrolling list inside a bordered box either scrolls its frame away or scrolls under its own bottom edge, so `GWDetailGrid` does not generalise off the receipt**. **Findings: Select Network's selected row is INVISIBLE** (`ListTile(selected:)` with no theme, title colour commented out); **Your Accounts paints selection as a flat brand fill**, the one thing the global accent rule forbids and which quick 260721-0ze swept everywhere else; **SDK Accounts renders a SECOND header** (`BottomDrawer` inside the shell) which 156-A turned from a stand-off into a visible defect - a `surfaceMenu` block inside a `surfaceElevated` panel; **066-C's "no footer" is wrong for 2 of the 3**. Contrast: selection tint 1.39:1 and brand edge 1.60:1 both fail 1.4.11 for a STATE - only the check glyph (6.81:1) carries it, so all three treatments stay. | drawers, list-picker, network, accounts, wallets, components, contrast, wcag, follows-032, follows-066, follows-156, follows-154 |
| 069 | sdk-accounts-section | The SDK section is 1 navbar chip, 1 drawer and 5 dialogs; its footer CTAs truncate to "Add with mn…" at the panel's real width, its row actions are split across two affordances by row state, and one dialog never went through GWDialog. What is the shape of the whole section on today's components? | **A · One door, CHOSEN + BUILT 2026-07-28** (quick 260728-s9k). Verification before approval produced a SEVENTH finding the sketch lacked: the delete snackbar claimed success unconditionally while the bloc emits only on OK with no else - a refusal was silent at both layers. Also corrected: menu items are gated on `hasMnemonic` as well as selection (a private-key import has no phrase), and the drawer's empty state is unreachable. Original recommendation was - one gradient `Add account` CTA, method chosen inside the dialog via the control-track segmented recipe, so both add paths share one dialog/warning/validation; runner-up B · Stacked (honest minimum, nothing truncates, but 2 equal-weight CTAs and 88px of footer); rejected C · Menu-first (two clicks before you can type to answer a binary question). **All three unify the row menu - not a variant choice**: one ⋮ on every row, same 4 items, same order, inapplicable ones DISABLED not absent. **Findings: the footer CTAs cannot fit** (186px each vs ~210 needed - Flutter ellipsizes silently); **"Copy mnemonic" writes a seed phrase to the clipboard with no warning and no confirmation**, where every other copy in the app confirms; **row actions split by state** so Delete is never in the menu and the code's own "cannot delete the active account" guard can never fire from the UI; **the payout result is read one frame too early** (`add()` then immediate `state.…` on a bloc - reports the PREVIOUS attempt, or `null` on first run) - a real bug, independent of the variants; the QR dialog is the only one outside `GWDialog`; the payout field has no validation. | drawers, sdk-accounts, dialogs, cta, menus, security, flow, follows-068, follows-156 |
| 070 | coin-page-components | **WINNER A (Jakub 2026-07-28)**, glyph sub-question answered the same day: **ONE ACCENT** - all six glyphs on `brandPrimaryOnSurface` (10.25:1 dark / 4.23:1 light, zero new tokens), accepted cost being a quieter card. Sketch 061 owns the coin page's chrome and its A leaves the internal layout alone. One layer down: what do the Convert and Info cards look like when every element maps to a component the app already ships - given 2 of their defects were fixed elsewhere in the app the same day? | **Recommended A · On components** - `GWTextField(label above, fill, focusRing)`, `GWDetailGrid` for Info, the receipt's whole-row copy affordance, Total as a one-row grid; **zero new components**. Runner-up C · Minimum (fix only the 2 field defects, leave Info hand-written). **Rejected B · One well** - best-looking, but an input inside the grid well measures **2.85:1 in light** against 1.4.11's 3.0, so it would need a fourth border token for one card. **Findings: both Convert fields use the notched Material floating label** (the only such pattern besides `custom_drop_down.dart`); **Token Amount lights a FLAT brandPrimaryStrong on focus** - structural, since a `BorderSide` takes one Color so no `InputBorder` can be a gradient, which is why `GWFocusRing` and the new `focusRing` flag exist; **`_MarketDataInfo.statRow` is GWDetailGrid hand-written** (11/12 padding, 22px glyph slot) days after the component shipped; **copy-address exists twice one day apart** (15px inline IconButton here vs the receipt's whole-row target with the glyph at rest); Total is a bare bodyLarge. **OPEN QUESTION drawn both ways, not decided: the rainbow info glyphs collapse in light mode** - statusSuccess 1.25:1, brandTertiary 1.63:1, brandPrimaryStrong 1.72:1, statusError 2.19:1 on the light well; only `brandPrimaryOnSurface` (Network) survives at 4.23:1 because it is the only appearance-aware one. So: 5 new light-mode tokens, or 1 accent that is already correct. Not a WCAG failure (labels identify every row) but red is spent on Total Supply where nothing is wrong. Also recorded: **key text on the light well is 4.23:1 vs 4.5 - inherited from the receipt, applies to GWDetailGrid everywhere**, deferred to the light pass. | coin-detail, token-info, convert, detail-grid, text-field, focus-ring, base-components, light-mode, contrast, follows-061, follows-152, follows-154 |
| 071 | coin-page-whole-screen | **WINNER B (Jakub 2026-07-28)**, null-marketData answered the same day: an explicit no-data message, not a silent collapse. Confirmed before approval: **zero backend work** - same endpoint, model, Hive box; only formatting logic is touched. The coin page reads weak. Why, measurably - and what is the whole screen, navbar included, when re-composed on components the app already ships? | **Recommended B · Stat rail** - the A fixes plus a KPI row (rank / 24h change / 24h volume / market cap / from ATH / 24h range) built from fields ALREADY in Hive, chart full width beneath. Runner-up A · In the frame (navbar + one frame + a viewport-driven chart; a strict SUBSET of B so never wasted). Rejected C · Your position (answers "how much do I hold" on a page reached from Markets, where the question is "what is it worth" - and degrades to nothing for coins you do not own, i.e. most of them). **Findings, and the first is the whole complaint: `SizedBox(height: 480)` at token_info_screen.dart:311** - with a ~530px side column, content ENDS near 620px on a 1335px window, so roughly half the page is empty; nothing overflows, the layout just never asks for the room. **It is the only screen in the app outside the ShellRoute** (`router.dart:243`, shell closes at 242) so it is the only one with no navbar - from a coin you cannot reach News without going back. **It uses a different page frame** (1200 centred + space10 vs the 1536 + 12px gutter every tab uses), so its title lands ~170px further in than "Markets" does, one click away. **The model carries 24 fields and the page shows 6** - rank, 24h high/low, FDV, ATH+%, ATL, max supply, price change 24h are all in Hive already, so filling the void costs ZERO new requests. All three variants share the same three fixes because those are defects, not design. **Supersedes 061** (same chrome question, still winner:null, but without the 480 measurement that explains the complaint). To argue when B is planned: its KPI tile would be the THIRD hand-rolled "small labelled number in a box" (markets_hero_card has one, the dashboard another), which is exactly the promotion bar GWKicker/GWSelectRow/GWWarningNote each cleared. | coin-detail, token-info, page-layout, navbar, shell-route, gw-page-header, page-frame, data-honesty, supersedes-061, follows-070, follows-152 |
| 072 | coin-actions-only-what-exists | What does the coin page's action row look like when it holds ONLY actions the code can actually perform - given the live count changes with which of the 4 routes you took to reach the page? | **Recommended A · Only what works** - `GWButton`s at natural width, left-aligned, containing exactly the live actions; no disabled state to design, so finding 3 is dissolved rather than solved. Runner-up B · One CTA + menu (069-A's SDK-row discipline: one menu, same items, inapplicable ones DISABLED not absent - right if the list is expected to grow, and where Buy would slot in; rejected today because a menu is heavy for one item and the growth is speculative, which is what the rule forbids). Rejected C · Into the header (reclaims ~74px but `GWPageHeader.trailing` already holds the price + pill from 071-B, so actions become unlabelled icons and on a narrow window the page's ONLY live action is a 16px glyph). **Findings: SEND does not exist at any layer a user can reach** - no screen, no route, `send_transaction_details.dart` is the WalletConnect REQUEST view, and `GeniusApi.transferTokens()` is real with **zero callers in the repository**; it is a FEATURE not a wiring job, so the button leaves the design. **MORE is dead on THREE of the four routes in** - `isGnusWalletConnected` is hardcoded `false` at markets_screen.dart:58 and dashboard_markets.dart:83 and never passed at markets_search_bar.dart:93, so **arriving from Markets exactly ONE of four buttons does anything and the bar is 75% inert on its primary path**; that is a bug in the CALLERS and rides with any variant. **SWAP is built and never wired** (`/swap` + SwapScreen work) but `SwapScreen` takes NO parameters, so preselection is not in the code and no variant draws it - the honest cost is a `fromToken` param plus mapping `Coin`→`SquidTokenInfo` by symbol+chain. **BUY is reachable** (`BanxaBuyScreen` takes `initialCryptoCode`) but deliberately NOT proposed - two todos already say the existing Buy CTA opens the order history and the Banxa fetch uses a placeholder customer id. No token-address explorer link exists (`getExplorerUrl` takes a tx hash). `_ActVariant.disabled` currently collapses three different truths into one grey box. | coin-detail, token-info, actions, cta, data-honesty, dead-affordances, routing, follows-071, follows-069 |
| 073 | coin-icon-actions | Jakub likes the small icon actions from 072's rejected C. Where do they go - given `GWPageHeader.trailing` already holds the price, the live count changes by route, and "small" has a floor this codebase already set? | **Recommended A · Under the price** - inside `GWPageHeader.trailing` beneath the price + pill, costing **ZERO new vertical height** (that column is already as tall as the title block and the price does not fill it), so the whole 74px action bar leaves the page. Runner-up B · By the identity (under the name; safest at ONE action, costs ~12px, slightly miscategorises actions as identity). Rejected D · In the chart card (prettiest, but **the no-data page has no chart card** per 071-B, so actions would move depending on whether CoinGecko covers the token). Also drawn: C · chart toolbar (~44px, puts wallet actions where zoom controls live), E · grouped control-track pill (~50px, right for 3 items, looks like a bug at 1). **The finding that constrains all five: "small" applies to the GLYPH, not the target.** `GWButton`'s `sm` returns 44 with the comment *"was 36 — touch floor (iOS 44)"* - someone already tried 36 and reversed it - so every variant is a 44x44 target with a 20px glyph; WCAG 2.5.8 AA asks 24x24 and 2.5.5 AAA asks 44x44, so the app is already at the stricter bar. **Icon-only buttons need `tooltip:` + `semanticLabel:`** (both already on GWButton) or they announce nothing; the Labels switch shows that the always-visible caption costs 12px and is **the only version that works on touch**, where no hover exists. Contrast: the glyph carries 1.4.11 at 10.9:1 so the 1.32:1 button edge is acceptable - the same argument `GWSelectRow` recorded for its check glyph. Action set is 072's: Receive + Swap + Bridge, **no Send**, and from Markets the live count is ONE - set the switch to 1 and check every placement, because that state has never been drawn. | coin-detail, token-info, actions, icon-buttons, touch-target, accessibility, follows-072, follows-071 |
| 074 | coin-actions-on-the-chart | **WINNER C2 (Jakub 2026-07-28), IMPLEMENTED same day as quick task `260728-v6c` - uncommitted per CLAUDE.md, analyze 59 = baseline, tests 371/1, WALK PENDING.** Shipped with one change to the sketch's default: **Bridge is ABSENT when the coin is not GNUS, not disabled** - a permanently-grey button says "unavailable" when the truth is "does not apply"; zero balance keeps the disabled state because the user can change it. Glyphs came out as Material icons rather than the sketch-152 SVGs, because `GWButton` tints through an `IconTheme` that `SketchIcon` cannot read, so an SVG would be the one glyph in the row that does not dim. **Three defects no sketch panel could show:** `_pushBridgeScreen` opened with a `Navigator.pop()` that would have popped the PAGE once the drawer was gone; the no-data empty state sat ABOVE the actions, so the row stood at two different heights by coverage - the exact failure C2 was chosen to avoid, fixed not by choosing C2 but by moving the empty state into the chart card's slot; and `_kChromeAboveChart` was 38px stale. Original decision record follows. - component audit before committing found **zero new components and zero new parameters**, but three sketch/code disagreements (icon buttons are CIRCLES not rounded squares per gw_button.dart:270; the tinted "primary" Receive does not exist - `gradient` is a full fill, so either Receive is loud or all three are equal; `GWButton.icon` defaults to `ghost` not `icon`) and one uncounted deletion (**TokenActionBar has exactly 1 consumer so it goes dead**, with its private _ActButton/_ActVariant; the More drawer goes too if Bridge becomes an icon; SlidingDrawerButton survives on 3 other consumers). Default taken: Bridge as an icon with the zero-balance guard on `onPressed: null`. Jakub wants the icon actions on the chart, as suggested by 073's C and D. Drawn precisely: what does each version actually collide with? | **Recommended C2 · Above the card** - icons on a section line OUTSIDE the chart card, opposite a `GWKicker`, so they read as "at the chart" without being in the box that already holds four icons with a different job; **the only one of the four that does not move on the no-data page**. Runner-up D1 · floating translucent top-right on the plot (best-looking, zero height, but shares the card with the zoom row, has nothing to float over on the no-data route, and its glass fill would be a new surface treatment). Rejected D2 · shared bottom row (looks tidy, is worst: *Receive* ends up 6px from *zoom out* at the same size with the same affordance). **THE FINDING THAT DECIDES IT: the chart card already owns an icon row** - `crypto_live_chart.dart:506` renders a centred Row of 4 IconButtons (zoom in/out, back, forward) in the same card, so any in-card placement means two icon rows in one box with categorically different consequences - one changes the view, the other moves money, and undo exists for only one of them. Those 4 icons are also hardcoded `Colors.white` (filed todo), so anything beside them inherits a white-on-light neighbour. **And the no-data page has no chart card** (071-B), so C1/D1/D2 all vanish or must be re-homed there. **Honest note recorded against the recommendation: 073-A (under the price) still wins on cost - zero new height vs C2's ~44px** - so this sketch does not overturn 073, it makes the chart idea comparable. | coin-detail, token-info, actions, icon-buttons, chart, follows-073, follows-072, follows-071 |
| 075 | coin-actions-placement | Jakub wants the Receive/Swap icons somewhere other than the PRICE line - above the stat rail, or beside the price top-right. Six placements against the page as it stands TODAY. | **Recommended A · Beside the price** - inside `GWPageHeader.trailing`, icons left of the price block on the same line: **zero new height AND no movement on the no-data page** (the trailing slot just holds the icons instead of the price), which no other placement manages in both directions; against F (074-C2, shipped) it hands back the ~44px the PRICE line spends on nothing else. Runner-up E · beside the identity (the only placement that depends on NO market data, so it is pixel-identical on every route and state, and the best of the six at a single action; rejected because actions beside a title read as being about the title). Rejected D · seventh rail tile (fits beautifully at 3 actions and costs 0px, which is why it is worth naming - the rail is six FACTS, so a seventh cell of buttons says the buttons are a seventh fact; it also vanishes on the no-data page and the rail re-wraps 6→3→2 by width, so the cell would land somewhere different at every breakpoint). **THE FINDING THAT ELIMINATES TWO OF SIX: the stat rail does not exist on the no-data page**, so C (above the rail) and D (in the rail) both vanish or must be re-homed - the same trap that decided 074 against every in-card placement, drawn in red rather than worked around. **This is NOT a re-run of 073**: that sketch's page no longer exists - 071-B added the rail and moved identity+price into GWPageHeader, 074-C2 added the PRICE line - so three of 073's five placements were drawn against changed geometry and two of these six could not have been drawn at all. Honest note recorded: **A is the tightest of the six on a narrow window** (900px puts a 40px logo, a 30px name, 2-3 44px circles and a 28px price on one row); E and F have more room, and all three live in the same header so switching is one Row apart. Still open and it decides the count: the three hardcoded `isGnusWalletConnected: false` call sites mean the row is TWO icons from Markets. | coin-detail, token-info, actions, icon-buttons, page-header, follows-074, follows-073 |
| 076 | compute-on-base-components | Sketches 016/017/018 were drawn before the base components existed. What survives, what becomes a component, and what did the code read prove wrong? | **Re-cut ACCEPTED 2026-07-29.** 016-B2 / 017-A / 018-A all stand; what they are MADE OF changes (9 hand-built elements become components). Re-measured every roadmap pointer: all 5 bugs alive, 2 pointers stale, `genius_balance_display.dart` is under `lib/wallets/view/` not `lib/components/`. Four new code deltas: burned-tokens-no-hash terminal state, toast-only hash unrecoverable by construction, `isPurchaseable` off-by-one + false accusation, stale percentage survives `isProcessing:false`. | compute, sgnus, submit-job, base-components, phase-14, re-cut |
| 077 | compute-interactive | Three panel shapes and three flow shapes for compute — which one, and what would each cost to build? | **P1 · Twin tiles + F1 · Drawer, vertical steps** (chosen 2026-07-29). Interactive: live in-browser height measurement against the real 276px budget. Inventory 10 exist / 5 to build / 6 need non-UI code. Two spillovers into Phase 23: `GWCopyRow` gains its THIRD consumer (the extraction Phase 23 refused at 2), and F3 was rejected partly because the deferred `GWScreen` sweep makes it not-cheap. F2 rejected: hides the cost table at the moment money is committed. | compute, sgnus, submit-job, interactive, variants, inventory, phase-14 |
| 168 | coin-identity-header | Where does the price belong on the coin page, and does the page still need its own action row? Jakub's annotation from 2026-07-31: the Swap/Receive row crossed out and an arrow dragging the price from the right edge to just after the coin name. Today the price sits in the header's `trailing` slot, so on a 1536px page roughly 1200px of empty row separates it from the name, and the actions take a third row - about 124px in total before the first statistic. | _pending pick_ (rec **D · Price after the name, one fill stays** - Receive and Bridge disappear, Swap stays with its label; 3 rows collapse to 1 (~52px) and the CTA weight rule becomes literally true, because there is no longer a pair of outlines to adjudicate. Runner-up **A · Price on the title line, no actions** - the cleanest header and the most faithful reading of the annotation, but it turns the coin page into a quote sheet with a calculator, which is a product decision rather than a cosmetic one. Rejected **B · Icons instead of labels** - sketch 165's change 3 already settled that this page uses labels, not bare glyphs; 44px instead of 32px answers the accessibility objection and nothing else. **C · Price under the name** is a different question: the only variant where the price keeps its full 28px without competing with the title for the same baseline). All blocks rendered 1:1, measurements taken from Dart at HEAD. | coin-page, header, price, cta, density, follows-165 |
| 169 | banxa-your-orders-header | How should the “Your orders” panel header read on the Buy GNUS screen - the All / Pending / Done filter plus “View all” - built purely from components the repo already has? Jakub's requirement from 2026-07-31: every element must cite the file and line where it already exists and is used, so it can be checked. | _pending pick_ (rec **A · Adopt the shipped filter bar** - control track + 32px icons + an overflow menu for statuses with no chip, `View all` on `GWViewAllLink`; both controls fit on the kicker row, so the separate chip row disappears and the header block drops from 106px to 60px. Runner-up **C · Stop filtering a four-row preview** - the panel renders `visible.take(4)` anyway, and the filter choice is lost at `View all`; the chips become a `GWStatusDot` readout, **zero new components and minus 43 lines**, the only variant that promotes no component. Rejected **B · same tracks, words instead of icons** - not wrong, just the weakest trade: it pays the full width of labels and still takes its own row, so it buys 12px where A buys 46px. Also drawn: **D · one trigger + menu** (007-E transplanted; the only one that absorbs a sixth status without a redesign, but the counts stop being readable at a glance). **THE FINDING THAT SHAPED ALL FOUR: the app has already built this control TWICE, and Buy GNUS built a third by hand.** `CONVENTIONS.md:358` documents the “Control track” recipe with fixed values and says outright that a partial implementation “reads as a different design language on the same screen”; the conforming implementations are `gw_timeframe_segment.dart:36` and `_TransactionFilterBar` in `transactions_slim_view.dart:583` - and the second is **structurally the same control**, with a map of live counts and an overflow menu for statuses with no room for a chip. `_CountChip` (`banxa_buy_screen.dart:720-762`) ignores all of it: a raised `surfaceMenu` instead of a sunken track, no track at all, and **a `brandCta` gradient as the selection fill - identical visual weight to `Get quote`** (`:345-351`), the one committing action on that screen. Second: `getOrderStatuses()` returns five results, `declined` and `expired` map to the `error` tone, and **there is no chip for the error tone** - the count is already in `state.statusCounts`, nothing draws it, so a declined order is invisible under every filter except All. Same with `View all`: the app has `GWViewAllLink` (`gw_view_all_link.dart:19`) built for exactly this slot and used by the Markets panel (`dashboard_markets.dart:67`), and Buy GNUS puts a boxy `GWButton(tertiary, sm)` there instead. The one thing in this sketch that is not a component yet: `_TransactionFilterBar` is **private** to its file - A, B and D require lifting it into `lib/components/`, which is promoting working code rather than a new design, and exactly what the Rule of Three quoted in `gw_timeframe_segment.dart:9-13` asks for. | banxa, buy, orders, filters, chips, control-track, view-all, component-provenance, follows-007 |
| 170 | balance-unit-toggle | The balance in the Compute panel toggles between GNUS and Minions, but nothing on screen says so. Jakub's request from 2026-07-31: make it so a person knows it can be tapped - some arrows suggesting it can be flipped. What should signal that it is clickable - and is a hint even the right answer, or should the control simply show both options? | _pending pick_ (rec **B · Two-segment control track** - both options always visible, the active one raised, in the documented `CONVENTIONS.md:358` recipe, geometrically identical to the timeframe segment on the chart and the transaction filter on the dashboard. **The argument is not decorative: it is the only variant that REMOVES the question instead of answering it** - A, C and D answer “how do we suggest this is tappable”, B dissolves the problem by showing both options, and it is also the only one where you can learn that the other unit is called Minions without pressing anything. The cost is honest and it is about copy, not pixels: the full word MINIONS pushes the track out to ~132px and starts squeezing long balances, so the segment reads `MIN`. Runner-up **A · swap glyph beside the unit** - a 14px `swap_vert`, ~18px wide, zero height, a universal exchange mark; if `MIN` turns out to be an unacceptable abbreviation then A is the answer and B becomes unbuildable at this width. Rejected **D · under the number, as a link** - the most linguistically legible of all of them (“Showing GNUS - switch”) and the only one no balance length can squeeze, but it spends ~18px of height in a panel whose height was disputed this week and raised to 340px. Also drawn: **C · bordered chip** (keeps the full MINIONS, but the risk is that the same panel uses pills for statuses, so a bordered unit may read as a reporting badge rather than a button that changes something). **FINDING: what already works does not need fixing and must not be broken** - `_UnitToggle` (`compute_panel.dart:331-367`) already has a 24x24 target (WCAG 2.5.8 AA), `Semantics(button: true, value: unitLabel)` announcing the active unit, and an `InkWell` rather than a bare `GestureDetector` precisely so it is keyboard-focusable and Enter-operable. **The gap is purely visual and affects only sighted pointer/touch users** - a screen reader knows it is a button; Jakub did not. It survived review because on desktop there is a weak `InkWell` hover and the reviewer had a mouse; on touch there is no signal at all, ever. No variant needs a new component: B needs `GWControlTrack`, which is being created the same day in task `260731-jx5`, so it costs zero if that lands first. | compute, balance, toggle, affordance, control-track, discoverability, wcag-2-5-8, follows-169 |
| 171 | mobile-home-and-nav | How should the home screen and the navigation read on iOS, when the bar has 8 items and three labels do not fit? | _pending pick_ (recommendation **A · Curated Five** plus the control-track shrink from C) | mobile, ios, navigation, bottom-nav, header, home, contrast |
| 172 | dock-icon-and-top-bar | Which specific icon belongs in the centre dock instead of the plus - and what is that dock actually for? | _pending pick_ (rec **A · Swap**, conditionally: if Send beats Swap on frequency, **B · Move** wins) | mobile, ios, bottom-nav, dock, icon, top-bar, total-balance, follows-171 |
| 173 | variant-a-interactive | What happens when you tap the wallet name - and does variant A hold up once you can click through it? | _pending pick_ (a prototype of variant A; the wallet as a sheet, not a dropdown) | mobile, ios, prototype, interactive, wallet-switcher, follows-171, follows-172 |
| 174 | accounts-sdk-vs-private | How should the accounts sheet tell SDK accounts apart from private wallets - and what constraints does the code impose? | _pending pick_ (two sections; finding: picking a wallet and the node's active account are TWO independent axes) | mobile, ios, accounts, sdk, wallets, sheet, follows-173 |
| 175 | more-overflow-and-unit-toggle | What replaces the sheet behind the three dots - and why can you not see which of GNUS/MIN is selected? | _pending pick_ (rec **C · drop the overflow**, runner-up A · a Wallet tab, rejected B · tiles; toggle: rec **U3 · brand tint**, runner-up U1 · gradient - which reverses the recorded CTA weight decision) | mobile, ios, bottom-nav, more-overflow, settings, control-track, unit-toggle, wcag, follows-174 |
| 176 | overflow-interactive | A clickable prototype of four overflow schemes with a tap counter (absorbed into 175, which is now fully interactive) | _superseded by 175_ | mobile, ios, prototype, interactive, more-overflow |
| 177 | assets-full-page | What should the separate Assets page behind View all be - and how should it look when the wallet is empty? | **DECIDED: D simplified** (a search field plus sorting by value only, tapping toggles asc/desc). The recommendation was C; Jakub picked D and trimmed it himself. 7 components used as-is, 0 adapted, 2 new | mobile, ios, assets, full-page, search, sort, empty-state, follows-175 |
| 178 | assets-header-total-vs-view-all | The total amount and View all are fighting over the same Assets header row - which of them should dominate? | _pending pick_ (rec **C · “Assets” as a kicker, the total as the headline** - zero height cost and it does not touch the approved rhythm of 30; runner-up A · the total as a band under the title (+52px); rejected D · a summary well (+64px on the very panel we had just capped); B is Jakub's own idea - the total leaves home, and it appears nowhere else) | mobile, ios, dashboard, assets, section-header, hierarchy, view-all, follows-177 |
| 179 | transaction-row-action-tag | Jakub on his iPhone: all the action tags (Processing, Send, Receive, Purchase) are cut off - fix the truncation, and is the action tag even needed at all, now that the badge icon already sits on the coin logo? | **Recommended ★ C · the action word leads the second line** (`flex: 0 0 auto`, it never shrinks; the context gets `Expanded` and is what shortens; status pinned right). The only scheme in which the action word CANNOT be truncated in any state, and the only one that keeps the type on pending/failed rows. Runner-up **A · layout fix only** (the chip stops being `Flexible`) - correct, but it does not create width, it only picks a different victim: with SHORT token names the job/escrow chips (84-93px) reduce a four-character `GNUS` to an ellipsis. Rejected **B · badge only** (Jakub's proposal) and **D · the action as the headline** (which reverses closed decision 010-A on token-first and cuts 11/11 subtitles). **Findings: (1) the truncation is a layout defect, not proof the tag is surplus** - `transaction_displays.dart:366-405` has TWO `Flexible` children on the title line, so the chip gets half the free space regardless of the word's length: 342 content → 113 for the middle column → 52.5 for the chip → **44.5px for the word**; measured live, **6 of the 8 `_actionFor` strings are truncated**, only `Sent` (31) and `Minted` (46) fit. **(2) the badge cannot carry this alone** - 9 kinds but only **6 fills** (`#64748B` = sent+escrow+swap, `#0AD89C` = received+purchase), so colour does not distinguish them and the whole signal is an **11px** glyph; `escrowRelease` has no badge of its own at all. **(3) decisive: the badge is chosen by STATUS before type** - pending wins, then failed, only then the type, so on every row off the happy path the type is drawn nowhere (pending mint = a clock, failed swap = a cross). **(4) the chip duplicates the line 4px below it** - 5 of 8 types (mint/escrow/escrowRelease/purchase/process) repeat what the subtitle says in full. **(5) a second quiet loss**: the amount column is also `Expanded` flex 1 and takes 113px for content needing ~90. **(6) the chip's `surfaceMenu` fill on `surfaceBase` = 1.13:1** - a container you cannot see; the label WITHOUT the chip improves from 5.30 to 6.01. **(7) C fixes a truncation nobody reported**: `Minted to wallet · Pending` needs ~172px on a 113px line, so today we lose the word “Pending”. WCAG: 1.4.1 is NOT the right criterion here (the badge distinguishes by glyph, not colour), and 1.1.1 is already satisfied by `Semantics(label:)` - B is conformant and still wrong | mobile, ios, transactions, row, action-tag, badge, truncation, layout, flexible, wcag-1.1.1, wcag-1.4.1, contrast, follows-177, follows-178 |
| 180 | mobile-header-brand-and-wallet | Jakub: the Genius logo plus “Genius AI” in the top-left corner, the wallet moved right, and **one** piece of wallet information instead of today's two controls (network chooser + wallet chooser), everything expanding on tap. Four whole schemes plus the current state, differing in WHAT that one control on the right is, not in pixels. | **Recommended ★ B · Wallet pill** - the avatar with a 16px network badge, the name and a caret; one control, one tap opening a combined sheet (a Network section above the two sections from 174). The only scheme meeting all three parts of the request at once and keeping the wallet name in the chrome; **it half-fixes the double-address defect for free** (the name without the address line). Runner-up **A · Avatar only** (44x44, zero text - structurally immune to every text problem; better for a user with a single wallet). Rejected **C · Segmented pill** (one border, but **still two choosers** - exactly what Jakub is complaining about - plus two 48px targets in the worst corner for a thumb). **D · the network leaves the header** (a named chip beside the Assets total) does not win, because it is 3 places instead of one and **3 taps from News/Markets**, but it is the only one that names the network in TEXT and that fix should be carried into the winner. **Findings: (1) there is NO logo asset in the repo** - `assets/images/crypto/` holds coin icons only, so the mark is either a new asset from Jakub, a misused GNUS mark, or a gradient drawn in code; the “Genius AI” wordmark (~66px) is NOT blocked and every scheme has a WORDMARK ONLY mode, so the decision does not wait on the logo. **(2) the app bar has no visible edge** - surfaceElevated vs surfaceBase is **1.004:1**, and the borderSubtle hairline is **1.34:1**; irrelevant today, but a brand block turns the bar into a *band*, which floats without an edge. **(3) the network is identified by picture alone** - a 20x20 `Image.asset` plus a caret, and the Tooltip says “Select network” (the action, not the value), so nowhere is there text saying which chain you are on (1.1.1 + 1.4.11); the network glyphs come out at 3.35:1 (Base) and 3.66:1 (Polygon) - passing 3:1 by a hair. **(4) height discrepancy**: the brief says 56px, the code has `toolbarHeight: 60` AND `preferredSize = 60`. The double-address defect is documented per scheme rather than rediscovered: `import_security_screen.dart:236` writes `walletNameController.text`, `app_bloc.dart:616` names the SDK ones. B has only **4px of slack** at 390pt, so the 236px cap plus ellipsis is a condition, not an option. | mobile, ios, app-bar, branding, logo, wallet-selector, network-selector, one-control, bottom-sheet, touch-targets, wcag-1.4.11, wcag-1.1.1, double-address-defect, blocked-on-asset, follows-174, follows-171 |
| 182 | bottom-nav-destinations | Jakub: propose which buttons belong on the bottom navigation bar (...) he would potentially change More into Menu, and he is not sure he would keep markets and activity - and later the same day: a wallet icon in the top-right corner in a shorter form with the hamburger menu next to it, and then instead of 'more' at the bottom we could put 'news'. WHICH FOUR destinations belong on the bar? The shape (171 B x 172 A: 4 tabs + the Swap dock) is closed and is not what this sketch is about. | **Recommended ★ S7 · Home · Assets · [Swap] · Activity · News**, with the menu moved into a hamburger in the header - the only set that answers BOTH messages at once: the fourth slot stops being a lid, the holdings page from phase 25 finally reaches the bar, and a new user with an empty wallet has one tab with content in it. **Zero new routes** - `/assets` and `/news` both exist in the shell. Runner-up **S2 · Home · Assets · [Swap] · Activity · Menu** (Jakub's own leaning) - the same set with Menu in the fourth slot, ZERO dependencies, one entry in `_mobileDestinations` plus a rename; the shippable fallback if the header is not ready, and a strict prefix of S7. Rejected **S6 · Home · Markets · [Swap] · Activity · News** (the literal reading of Jakub's second message) - two of the four tabs are content belonging to nobody and overlapping each other (Markets = what BTC costs, News = why), and **Assets is left with EXACTLY one entrance in the whole app**; S6 becomes S7 after one swap. Also rejected **S5** (Activity leaves - on the day the Transactions page took over a bar of ten filters). **Findings: (1) the overflow is DERIVED, not written** - `_moreDestinations` = all 8 desktop destinations minus Swap minus whatever is already on the bar, so taking Markets off the bar puts it in the sheet by itself, with no second edit. **(2) `/assets` is NOT a member of `_allDestinations`** (phase 25 added it straight to the router), so it cannot appear in that sheet - today its only entrance is `View all` on the dashboard panel. **(3) `/compute` and `/explore` DO NOT EXIST** - S3 and S4 are construction, not editing. **(4) the brief's assumption does not survive measurement: “Transactions” FITS** - the slot is (390-84)/4 = 76.5px and the word is 63.34px at w600 (advance widths straight out of `assets/fonts/Inter-SemiBold.ttf`, upm 2048, `labelMd` with no letterSpacing); the truncation at 48.7px applied to the eight-tab bar thrown out on 2026-08-06. Every candidate word fits, so label length CANNOT be an argument for any scheme - the real limit is Dynamic Type: “Transactions” breaks at 1.21x, “Activity” at 2.06x, “News” at 2.79x. **(5) the hamburger is not free**: the same tap count (1), but 1936 vs 4590 pt2 of target and the corner furthest from the thumb. **Dependency: S3/S4/S6/S7 only live if the header takes the hamburger - that is sketch 181-wallet-control-compact, in flight; if 181 lands elsewhere, the answer is S2.** Contrast computed: inactive label 5.97:1, gradient stops 10.39/7.54:1 on `#0C0E14` (the bar is FLAT - `surfaceSheen` has the same stop twice). | mobile, ios, navigation, bottom-nav, destinations, information-architecture, overflow, empty-state, taps, label-fit, follows-171, follows-172, follows-175, depends-on-181 |
| 181 | wallet-control-compact | Jakub: the wallet control on the right **takes up half the screen** (maybe a wallet icon and then everything expands, or some shorter form), and later the same day a new direction: **a wallet icon plus the hamburger menu beside it**, with `More` on the bottom bar replaced by something like `News`. Six whole schemes (Now + A-F), differing in WHAT YOU SEE BEFORE tapping; the first axis of comparison is **width in px at 390pt**, printed under each phone and drawn as a ruler on the bar. | **Recommended ★ F · icon + hamburger, with E's monogram in the icon** - `44 + space6 12 + 44` = **100.00 px, 25.6% of the bar** (today 223.94 px = **57.4%**). Honest accounting: the wallet control itself is 44 px as in A/E, and the extra 56 px buys **a slot on the bottom bar**, not information about the wallet. Conditions: (1) the bottom bar gives up `More` - what goes into the freed slot is settled by **182-bottom-nav-destinations**, not by this sketch; (2) the **Accounts** row leaves `_MoreSheetBody`, otherwise two icons 12 px apart lead to the same drawer; (3) both icons stay in `title`, NOT in `actions` - in `actions` the title Row drops from **342.00** to **246.00** and it is the brand that starts shrinking under Dynamic Type. Runner-up **E · monogram** (44 px, 11.3%) - the only scheme under 100 px that says BOTH which wallet AND which network; build E first, it is the condition that makes F make sense. Rejected **B · short pill** (138 px): it pays 94 px more than E for the name, and an 84 px column renders `Super Genius Wallet 1` and `2` identically as **“Super Ge...”** - it fails exactly where the name was supposed to help; widening the column until they separate takes it back to 224 px. **Findings: (1) the `kWalletPillMaxWidth` = 236 cap NO LONGER DOES ANYTHING** - after the lockup grew to mark 28/wordmark 18 there is 223.94 left, so the pill takes the remainder, not the cap. **(2) the avatar is a CURRENCY icon, not a wallet identity** - `AccountAvatar` paints `brandPrimaryStrong` + `crypto/{currencySymbol}.png`, so two ETH wallets are **pixel for pixel identical**; that refutes “just show the avatar” and it is the whole difference between A and E. **(3) the double-icon defect**: for an ETH wallet on Ethereum the 32 px circle and the 16 px badge draw **the same file** `crypto/eth.png`, 3 px apart. **(4) naming the network in text costs the same as naming the wallet** - `Super Genius - TestNet` = 147.28 px, i.e. a 223 px control. **(5)** `account_drawer.dart` compares the selection by `walletName`, so two wallets with the same name both highlight - names are nowhere in this app an identity. The monogram rule: **order is critical** - the address test MUST come first, because an address ends in hex and `0x7a3f...6e21` comes out as **21** instead of **7A**. **D · expand in place** is rejected as the only one not worth building even for free: 342 px (87.7%) and the return of the name+address pair. | mobile, ios, app-bar, wallet-selector, width-budget, icon-only, monogram, hamburger, bottom-nav, touch-targets, wcag-1.4.11, double-address-defect, double-icon-defect, blocked-on-182, follows-180, follows-174 |
| 183 | wallet-hamburger-treatments | Jakub, right after picking scheme F from 181: he would like several other options prepared and shown - with the border, without the border, a gradient border, and so on, so prepare 5-6 such versions and we will see. The composition from F is **closed** (two 44 px controls, `space6` 12, in `title`, **100.00 px** together) - this sketch changes **the skin only**: fill, outline, gradient. Six treatments (A-F) plus today's shipping pill for scale, all on pixel-identical geometry. The second axis: whether the wallet icon and the hamburger get **the same** treatment or **different** ones. | **Recommended ★ F · the split - the wallet keeps its hairline, the hamburger goes bare.** The only variant that **settles** the same-or-different question instead of taking an answer by default, and settles it on WHAT these objects are: the wallet icon carries a **value** (which wallet, which network) and changes, so it gets the existing chip recipe (`surfaceMenu` + `borderControl`, **3.30:1**); the hamburger carries nothing and opens a fixed sheet, so it gets the idiom every phone header draws (**19.29:1**). **Neither of the two identifiers depends on the 1.11:1 fill**, and it is the cheapest diff here - the wallet's decoration **does not move at all**, only the name column and the caret disappear; zero new tokens, zero new components. Runner-up **C · both bare** (7.54 / 19.29:1) - if it is to be quieter than F, the next stop is C, **not B**; C is F with the chip subtracted, so they are one edit apart. Rejected **D · gradient outline**: it wins on contrast outright (**10.39 / 7.54:1**), but (1) it is a **lone secondary action in the brand signature**, which is exactly the case the standing CTA rule refuses in so many words (“gradientOutline **only** for a secondary action standing beside or under a filled gradient CTA (...) **never for a lone secondary action**”); (2) the app is going the other way - `View all` went back this week from a gradient to flat grey at Jakub's request, and with D there would be **five gradient objects** on home; (3) at **1.5 px** (4.5 px physical at 3x) a two-stop ramp does not read as a gradient, it reads as a bright turquoise ring - **full system cost, negligible visual gain**. **Findings: (1) `GWGradientBorderCard` is NOT reusable** even though it does that geometry correctly - its default gradient is `brandBorder` **#14C8FF→#5BFFD0**, not `brandCta`; it is a card, not a button (`radius2xl` 16, `padding` space8); and above all its `onTap` path is `Material(transparent) > InkWell > card`, so **the ripple paints UNDER an opaque container and is never seen** - the opposite of the shipping `WalletPill`. **(2) B (fill with no outline) is internally inconsistent**: the container is **1.11:1 FAIL**, and the contents identify it anyway (7.54 / 17.41), so the fill is decoration you can barely see - either the container means something (A, F) or it should not be painted (C, F). **(3) The variant with LESS paint measures BETTER**: C with no container is 7.54 / 19.29, the shipping outline is 3.30. **(4) E (`borderSubtle` 12%, 1.36:1) is worse than B** - B has no boundary, E paints a boundary below threshold, and a boundary you can almost see lies about the control being identified. **(5) The treatment changes the APPARENT gap by 2.6x with `space6` 12 unchanged**: both outlined = 12.00 px of ink (segmented-control geometry), both bare = **31.00**, the split = **25.00**. **(6) the `surfaceSunken` well** measured and rejected: **1.04:1**, worse than `surfaceMenu`, so it needs `borderControl` anyway, and a control starting in a dip contradicts the “lift chip” language. | mobile, ios, header, wallet-control, hamburger, border, gradient, wcag-1.4.11, contrast, treatments, follows-181 |
| 184 | menu-page | Jakub: build sketches off your feedback, but with nice sections and so on, ideally using our components - and mid-build, a **hard condition**: this menu is to contain only what we currently have and what there is logic and code built for (...) everything must be done off the code. S7 (182) moves the menu into the header hamburger (181-F) - what is inside it, and is it a page or a sheet? Five variants differing in **structure**: A a flat list in one card, B a card per section, C kickers and hairlines only, D a grid of tiles, E the sheet as the control. | **Recommended ★ C · full page, kickers + hairlines, no boxes.** The only variant honest about three facts at once: the menu has **four rows**, two of the four sections have **one row each**, and `GWCard` on this page is a **1.01:1 fill carried by a 1.36:1 hairline**. A kicker over a single row is a label and costs nothing; a box over a single row is a container with no contents. `GWPageHeader` + `GWKicker` + `GWSelectRow` exactly as shipped, **zero new components**, and the only variant where turning on `GW_DEV_TOOLS` adds a **named** group without restyling anything above it. Runner-up **A · flat list** - `_MoreSheetBody` stood up on a page, the smallest diff in the sketch; A is **strictly contained in C** (add kickers and you have C), and they differ on one point: whether the DEVELOPER group can live behind a line instead of behind a name. Rejected **D · grid of tiles** - **two new components** and a second visual language for “a place you can go”, to fix the fact that a page with four rows has empty space; the menu is the least-visited surface in the app, and D dresses it in the weight of the dashboard. **E · the sheet is NOT rejected** - at today's four rows it is defensible and cheapest, because two of the brief's three arguments for a page are weak at this size. **Findings: (1) the menu has FOUR rows** - `_allDestinations` minus the S7 bar = **Markets, Web, Feedback, Settings** (+3 under `GW_DEV_TOOLS`); the list **maintains itself**, because `_moreDestinations` is derived. **(2) `SettingsScreen` is NOT a preferences screen** - it holds Log Config (spdlog levels), Network Config (PubSub port, bind address, UPnP, high/low water) and CRDT Config (backup interval, keep count, auto restore); **appearance, security, currency, language and notifications DO NOT EXIST anywhere in `lib/`** - four items from the original brief deleted, not deferred. **(3) there is NO version and NO About** - `pubspec.yaml` says `1.0.0+1` and nothing in `lib/` renders it, so Support is one row. **(4) Appearance is the one genuine grey area**: the logic is complete and durable (`GWAppearance` -> Hive `preferences`, `gw_appearance.dart:40`), but the only control sits in the dev bubble, and the file's own comment (`gw_appearance.dart:12`) describes a **“Preferences sheet (Appearance row)” - a sheet that never existed**. **(5) the “a sheet has a height ceiling” argument is WEAK** - `GWBottomSheet` passes `isScrollControlled: true`, so the ceiling is nearly the full screen; four rows do not touch it, seven (dev) do. That leaves **one** strong argument for a page: every row opens a full screen, and a sheet must destroy itself to do so (`Navigator.pop` + `context.go`, `responsive_overlay.dart:523`). **(6) the Accounts row must go** from `_MoreSheetBody` (181-F), and that is why the menu has four rows and not five. Every row in the README carries **a file and line citation**; the candidate “push frequently used rows to the top” was **killed by the inventory** - with four rows there is no top. | menu, hamburger, navigation, sections, dev-tools, components, contrast, wcag, code-grounded, follows-181, follows-182, follows-175 |
| 185 | wallet-icon-next-to-menu | Jakub on device, straight after walking the S7 header: one change he wants to make - just show a wallet icon next to the menu icon there, and design a few treatments of those icons and send over 4-5 designs in HTML. The header's left control stops being the account avatar and becomes a **generic wallet icon** beside the hamburger. Five whole takes on the **pair** (not on one icon), differing in: which wallet glyph, whether the two match in optical weight, whether either keeps a container, and whether the network badge survives. | **Recommended ★ A · the pair with no containers, on the app's own outlined billfold** (`Icons.account_balance_wallet_outlined` @18 + `Icons.menu` @18, both bare). **This change kicks the leg out from under 183-F**: F won on the argument that the wallet holds a VALUE and the hamburger holds nothing, and a generic glyph identifies no wallet at all - both controls are now the same thing: value-free doors to a sheet, so by F's own logic they should match (= 183C, the fallback Jakub reserved himself). The two remaining grounds for a chip were **examined and both fall**: anchoring (the wallet is 12.00 from the hamburger, they read as one anchored group) and hierarchy (`borderControl` is an IDENTIFICATION token, not an emphasis one - a bare glyph already identifies at **19.29:1** against the edge's 3.30:1). Runner-up **C · keep the split** - not because the argument holds, but because it is the **cheapest diff (3 lines)** and the only real home for the network badge. Rejected **B · `walleticon.svg`** - it looks like the “reuse a shipping asset” winner, but it has **0 references in Dart**, and it falls on measurement: **88.77%** ink coverage against `Icons.menu`'s 18.75%, so it is a solid beside a stroke - matchable only by bbox, and an area match lands at an absurd **8.27px**. **D · a pair of chips** comes fourth (183's segmented-control defect plus 10 lines undoing F), and **E · person mark** is the best-weighted pair on the page (5.1% ink difference, stroke-matched, unique in the shell) and loses **on meaning, not on measurement** - it promises a profile this app does not have. **Findings: (1) seven wallet assets in `lib/assets/images/` have ZERO references in Dart** (`walleticon.svg`, `navwalletbuttoncustom.*`, `navwalletbuttonactive.png`, `copywalletsymbol.svg`, `profileicon.png`) - “reuse” here means adopting artwork nobody has ever seen; on top of that `copywalletsymbol.svg` is **two documents, i.e. COPY**, and `navwalletbuttoncustom2.png` is **a list**, so the filenames lie. **(2) `Icons.wallet_outlined` is NOT outlined** - extracting from `MaterialIcons-Regular.otf` shows the same 330-character path as `Icons.wallet`, both solid at 40.85%. **(3) A duplicates the Assets tab glyph** (`nav_destinations.dart`:153), breaking that file's own comment that “the two are never adjacent” - a cost, not a defect, but a named one. **(4) the network badge survives exactly where there is a container** - on a bare glyph it hangs in an empty bar 10-14px from any ink and reads as a third object. **(5) the optical weight rule: match the CLASS, then the bbox, then the area** - two Material glyphs at one size have an identical stroke **by construction** (1.50px @18), and a solid beside a stroke can never be equalised by area. **(6) `surfaceMenu` on `surfaceElevated` is still 1.11:1**, so removing the chip costs nothing in contrast. **Lost and named: the header can now never say which wallet is live** - that is already almost true today (the avatar identifies the CURRENCY, two ETH wallets render identically), but 181-E's monogram could have recovered it for 0px, and a generic glyph closes it off; a table of recovery paths with costs is in the README. | mobile, ios, header, wallet-icon, hamburger, icons, assets-inventory, optical-weight, contrast, wcag, touch-targets, code-grounded, follows-181, follows-183, follows-184 |
| 079 | toast-two-densities | Braian: redesign the toast we have and make sure it is actually used across the app - then, on shape: the best user-friendly way **for mobile specifically**. Position (top), safe-area awareness and swipe-up dismissal were settled before the sketch; this asks what the widget itself becomes. | **Recommended ★ A + B together - one `ToastWidget`, a `ToastDensity` enum: card for alerts, compact for confirmations.** Neither works alone: the case for the pill is that today's card is wrong for "Link copied", and the case for keeping the card is that a pill is wrong for "Verification failed". **Findings: (1) the app ships TWO notification systems** - `ToastManager` (26 sites, on-brand, 23-03 contrast work) and Material `SnackBar` (32 sites: 26 behind the `showAppSnackBar` helper, 6 direct), and **`snackBarTheme` is NOT defined in `theme.dart`** (0 refs), so those 32 are Material's grey Roboto pill at the bottom, under the dock. **(2) the migration is ONE function** - `showAppSnackBar` is 18 lines serving 26 callers; repoint its body and 26 sites convert with no call-site churn, leaving 6 by hand. **(3) zero callers pass an `action`** - the parameter is dead, so the toast needs no action slot and that whole design question disappears; 4 callers pass `backgroundColor`, hand-rolling what `ToastType` already means. **(4) there is NO safe-area awareness anywhere in `toast_manager.dart`** - `top: 100.0` is a literal and the file has zero references to `MediaQuery.padding`/`viewPadding`/`SafeArea`, so on a 14 Pro (44-59pt inset + 60pt header) the number is wrong by construction; the fix derives it as `padding.top + kHeaderHeight + space4` = 112. **(5) the toast is FULL-BLEED on mobile** (`left: 0, right: 0`, `maxWidth: infinity`) - it touches both screen edges, which is precisely why it needs a 2px ring to look contained; inset by `pageGutter` 6 and the ring becomes a 4px leading edge. **(6) it is NEVER announced** - no `Semantics` at all, so VoiceOver/TalkBack users do not receive "Verification failed"; an accessibility failure, not a nicety. **(7) the close target is ~28pt** (icon 20 + padding 4) against the 44pt adopted for chips in phase 25, and it sits at the top of the screen where a thumb cannot reach - which is why swipe-up becomes primary and the button the fallback. **(8) the stack is uncapped** at `100 + n × 85`: 4th toast at 355px, 6th off screen. **(9) one 5s duration for everything** - too long for "Link copied", too short for a two-line error. Bottom placement was considered and rejected: `kMobileBarHeight` 60 + dock overhang + bottom inset means it either covers the nav or floats ~90px up reading as detached, and top is where iOS puts system banners. Colours are NOT redesigned - every value is the shipping 23-03 token in both modes. **WALK 2026-08-08 (Braian, Windows desktop) changed the card twice from what is drawn here:** (1) the 4px leading status edge is **dropped** - the icon is the only thing carrying status, since the three glyphs differ by SHAPE and 1.4.1 is satisfied by the glyph rather than the colour, so a second coloured object was saying nothing new; (2) the card is **two rows, not two columns** - icon and title share the first line and the message spans the full width beneath, because a 20px icon beside a two-line block left dead space under itself and cost the message ~32px of indent it needs more on a phone. Also found on the walk and NOT visible in this sketch: the toast is inserted into the root Overlay, which has no Material ancestor, so every Text inherited Flutter's fallback debug style - a yellow double underline under the title and message, in BOTH branches, and the new 44pt IconButton had nothing to paint its ink into. One `Material(type: transparency)` fixes both; a regression test pins the decoration. Open for the walk: the compact pill is centred/auto-width and the card is full-width inset, so two densities on one stack do not share a left edge (variant C shows it) | mobile, ios, toast, snackbar, notifications, safe-area, swipe-dismiss, density, a11y, wcag-4.1.3, live-region, touch-targets, tokens, code-grounded |
