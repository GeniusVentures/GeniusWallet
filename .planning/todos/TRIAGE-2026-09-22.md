# Todo triage — 2026-09-22, against HEAD `3afe7578`

76 pending todos, each claim checked against the tree at `3afe7578` (PR #235 merged).

## Closed (33)

27 FIXED — the defect is demonstrably gone (code, commit or CI evidence cited in each file).
6 STALE — the item is no longer meaningful (self-retracted, superseded, or never had a concrete
ask). Each closed file carries its own "## Closed 2026-09-22 (backlog triage)" section with the
specific evidence; see `.planning/todos/completed/`.

## Held back (4) — not touched by this pass

Three are FIXED/STALE items deliberately not closed. The fourth is a LIVE-SMALL being
closed on another branch, so it counts against the 23 below, not against this pass.

- `2026-07-18-no-user-facing-appearance-toggle.md` — being `git mv`-ed by the concurrent
  `appearance-toggle` branch session.
- `2026-07-18-const-widgets-do-not-re-skin-on-live-appearance-toggle.md` — being appended to by
  that same session; its triage verdict was disputed.
- `2026-07-24-dashboard-bitcoin-chart-renderflex-overflow.md` — a FIXED verdict exists but is
  low-confidence (no widget test asserts the dashboard's `Expanded` constraint); needs a manual
  walk before closing.
- `2026-07-22-light-mode-verification-backlog.md` — records a standing project policy, not a
  defect; there is nothing to close.

## Still pending (43)

The full triage classified all 76 as 29 FIXED, 7 STALE, 23 LIVE-SMALL, 17 LIVE-BIG. This pass
closed 33 of the 36 FIXED/STALE and held 3 (see above), which reconciles:

    33 closed + 3 held (FIXED/STALE) + 23 LIVE-SMALL + 17 LIVE-BIG = 76

LIVE-SMALL means the fix is contained to roughly one or two files with no schema change, no new
dependency and no cross-cutting sweep. LIVE-BIG means it needs a phase: a sweep, a model change,
or a product decision. One LIVE-SMALL — the user-facing appearance toggle — is being closed on
the `feat/appearance-control` branch, so it will leave this count when that merges.

Two items deserve attention out of band rather than waiting their turn:

- `2026-07-23-no-screenshot-or-recording-protection-on-seed-screens.md` carries no `severity:`
  field at all, so it reads as accepted. A desktop-first wallet that renders 12 words in the clear
  by default with no capture mitigation on any platform is a fund-loss path. It needs a real
  severity assigned.
- `2026-07-21-restore-local-wallet-storage-test-pin-coverage.md` asked that a failing test not
  simply be deleted. It was deleted anyway, and the promised repair never happened, so PIN and
  secure-storage coverage is currently zero.
