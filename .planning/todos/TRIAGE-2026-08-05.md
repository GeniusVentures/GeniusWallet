# Todo triage — 2026-08-05, against merged `develop`

79 pending todos, each claim checked against the tree at `a765411` (PR #221 merged).
Baseline at time of triage: `flutter analyze` 0 issues, `flutter test` 1000 passing.

A todo is only moved to CLOSE if the tree disagrees with its claim. Everything else stays.

---

## A. Resolved — safe to delete (6)

| Todo | Evidence in tree |
|---|---|
| `2026-07-28-token-detail-hero-is-now-dead-code` | `TokenDetailHero` has zero references in `lib/` and `test/`; the file is gone. |
| `2026-07-28-five-private-segmented-controls` | `lib/components/gw_control_track.dart` exists, 5 consumers. |
| `2026-07-21-chart-zoom-pan-icons-still-raw-colors-white` | Zero `Colors.*` / `Color(0x` remain in `lib/chart/`. |
| `2026-07-24-phase-19-feedback-tab-redesign` | Phase 19 complete — verification 6/6, walk 4/4. |
| `2026-07-25-dev-fault-injector-for-feedback-sentry-failure-paths` | `lib/dev/dev_fault_injector.dart:25` — the class exists. |

> **Correction (same session).** `2026-07-24-unify-timeframe-segment-component` was first bucketed
> here as resolved because `lib/components/gw_timeframe_segment.dart` exists. It is **not** resolved.
> `markets_hero_card.dart:680` still declares its own second `_TimeframeTab`, and the shared
> component's own doc comment (`gw_timeframe_segment.dart:29-32`) says so. Two classes named
> `_TimeframeTab` now exist — which is also the unexplained entry in Phase 23's
> `verify_additive_boundary.sh` shadow-baseline failure. Moved to bucket C.

## B. Confirmed open — functional or security defects (9)

Ranked by what a user loses.

1. **`banxa-orders-fetch-uses-a-hardcoded-placeholder-customer-id`** — `fetchOrders('your-cust-id')`
   at `banxa_orders_history.dart:39`, `:184`, `:218`. Three production call sites, a literal string.
   **The order list can never show a real user their orders.** The Buy GNUS rebuild put a good UI
   on top of a fetch that asks for the wrong customer.
2. **`send-is-not-built-at-any-layer`** — no `SendScreen`, no `/send` route. Confirmed absent.
   The only `send`-named file is `lib/reown/send_transaction_details.dart`, a dApp display.
3. **`import-screen-controllers-per-build-never-disposed`** — `TextEditingController`s constructed
   inside `build()` at `import_security_screen.dart:37-46`, never disposed. These hold the seed
   phrase and private key. AGENTS.md: prefer `Uint8List`, never leave key material in a `String`.
4. **`seed-phrase-clipboard-has-no-expiry-or-confirm`** — `sdk_account_manager.dart:332`,
   `Clipboard.setData(ClipboardData(text: mnemonic))`. No timed clear, no confirm.
5. **`no-screenshot-or-recording-protection-on-seed-screens`** — no `FLAG_SECURE` or equivalent
   anywhere in `lib/`.
6. **`banxa-fetchallorders-fetches-only-the-first-100`** — `banxa_api_services.dart:251`,
   `int limit = 100`, silently.
7. **`dead-backup-phrase-route-and-unreachable-backupphrasescreen`** — route `/backup_phrase` and
   `BackupPhraseScreen` both still present and unreachable.
8. **`com-example-scaffold-namespace`** — `macos/Runner/Configs/AppInfo.xcconfig:11`,
   `PRODUCT_BUNDLE_IDENTIFIER = com.example.geniusWallet`. Ships as the secure-storage service name.
9. **`second-app-instance-shows-a-silent-black-window`** — no single-instance guard in `main.dart`
   or the Windows runner.

## C. Confirmed open — accessibility (3)

AGENTS.md names accessibility as something this repo is explicitly not lazy about.

- **`track-chips-are-not-keyboard-operable`** (WCAG 2.1.1 **Level A**) — `gw_control_track.dart` has
  no `Focus`, no `FocusableActionDetector`, no `Shortcuts`, no `Semantics`.
- **`track-chip-unselected-text-under-aa-on-surfacesunken-light`** — `gw_control_track.dart:45`,
  `color: gw.surfaceSunken`, unfixed.
- **`status-pill-success-error-fail-aa-in-light-mode`** — 23-03 fixed the *warning* label only.

## D. Open, and understated — the number in the title is now wrong (3)

- **`eight-routes-sit-outside-shellroute`** — it is **13**. `lib/navigation/router.dart` has 11
  `GoRoute`s before the `ShellRoute` and 2 after. The count grew while the todo sat.
- **`track-chips-are-not-keyboard-operable`** — written against 2 consumers, now **5**. The
  extraction that closed two other todos multiplied this defect.
- **`no-user-facing-appearance-toggle`** — the mechanism is real and persisted
  (`gw_appearance.dart`, Hive `preferences` box), but **every `setMode` call site is under
  `lib/dev/`** — design gallery, dev bubble, token probe, all behind `kDebugMode && kShowDevTools`.
  A release user cannot switch appearance. Worse: `gw_appearance.dart`'s own doc comment claims it
  is "toggled from the Preferences sheet (Appearance row)" — a UI that does not exist.

## E. Rewrite, don't action (4)

- **`sdk-init-percentage-is-0-100-but-read-as-0-1`** — the file already retracts itself. `init=37.0`
  was `DevMockSgnus.armInitPercentage(37)`, not an FFI read. **The filename is now actively
  dangerous** — acting on it would normalize a scale never shown to be wrong. Only "does the feed
  stop at 0.525?" survives. Rename to that question.
- **`lib-reown-has-zero-tests`** — false as written; `test/reown/approve_drawer_contract_test.dart`
  exists. Retitle to the uncovered remainder.
- **`ci-quality-job-pins-flutter-3-38-10`** — the pin half closed in `c9831a0`. The live half is
  that the job has never executed.
- **`markets-1h-7d-change-columns`** — `markets_table.dart:191` already carries a `ponytail:` marker.
  This is a recorded simplification, not an untracked defect.

## F. Cannot be settled by reading code — one light-mode walk answers all of these (~14)

`design-system-has-no-light-mode-treatment`, `light-mode-verification-backlog`,
`const-widgets-do-not-re-skin-on-live-appearance-toggle`, `appscreenview-blank-in-dark`,
`dark-mode-disabled-state-visibility`, `gwbutton-secondary-variant-light-mode-text-aa`,
`sketch-theme-gradient-mismatch`, `splash-does-not-cover-full-window`,
`boot-shows-an-empty-window-for-about-4s`, `dashboard-bitcoin-chart-renderflex-overflow`,
`chart-zoom-pan-row-overflows-34px`, `bitcoin-chart-card-height`,
`systemic-mobile-gutter-missing-on-onboarding`, `side-by-side-walk-dashboard-vs-release-exe`.

**This is the single biggest lever in the backlog.** A fifth of the pending list is one walk.

## G. Design judgement — no code answer, needs a decision not an investigation (~13)

`account-row-ux-polish`, `drawer-dialog-padding-spacing-polish`, `mobile-nav-ia-curate-bottom-nav`,
`sdk-account-manager-ux-polish`, `cta-hover-state-is-over-rounded`,
`unify-dashboard-section-card-titles`, `token-info-reuses-coincardrow`,
`responsive-type-scale-and-component-consolidation`, `banxa-screens-need-a-real-design`,
`checkout-options-sheet-visual-contract-was-derived`, `drawer-footer-cta-weight-contradicts-shell`,
`surface-the-bridge-entry-as-a-first-class-action`, `balance-unit-track-clips-a-large-balance`.

## H. Planning hygiene, not code (5)

`sketch-020-number-collision`, `gwappbar-extraction-lost-its-destination-phase` (confirmed: no
`GWAppBar` class exists — the extraction never happened), `gwscreen-sweep-lost-its-destination-phase`
(confirmed: `Scaffold(` still used directly in `lib/banxa/`), `phase-4-criterion-1-may-be-claimed-
without-its-fix`, `restore-local-wallet-storage-test-pin-coverage` (its cited test file is gone).

## Remainder (~22)

Open, plausible, and not individually re-verified here: the drawer/component consolidation items
(`drawer-input-fields-are-hand-assembled`, `drawer-bodies-have-no-horizontal-padding`,
`four-list-rows-still-outside-gw-hover-row`), the compute/job questions
(`can-requestgeniussdkprocess-be-recalled`, `stall-detector-needs-a-traced-processing-feed`),
`banxa-orders-never-enter-the-transaction-store`, `receive-from-markets-shows-wrong-network`,
`receive-drawer-breaks-when-network-has-no-name`, `swap-fab-clears-a-bottom-nav`,
`the-global-swap-fab-floats-over-every-drawer`, `four-independent-wallet-persistence-layers`,
`five-material-text-slots-are-unmapped` (verified accurate — `toMaterialTextTheme()` maps 10 of 15;
`displaySmall`, `headlineSmall`, `titleSmall`, `labelLarge`, `labelSmall` are absent), and the rest.
