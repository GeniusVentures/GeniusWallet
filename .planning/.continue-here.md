---
context: sketch+quick-tasks
phase: 05-dashboard
status: paused
last_updated: 2026-07-21T12:34:06+0200
platform: macOS (darwin)
baseline_commit: 7a95e68
branch: ui-redesign-port
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] **NO COMMITS without Jakub's explicit go.** CLAUDE.md hard-gates commits. All 7 approved tasks are uncommitted by design.
- [ ] **NEVER `git add -A` / `git add .` / `git commit -a`.** The working tree is SHARED with session-2 work (Bitcoin chart, hover language) AND holds 6 skip-worktree signing files (Apple signing `9UJNVD92ZW`, bundle `ai.gnus.GeniusWallet.jakub`). Committing them breaks the build for everyone. Stage **only explicit paths**. Verify hidden files: `git ls-files -v | grep '^S'`.
- [ ] **Do NOT edit `theme.dart`'s `textButtonTheme`.** It is shared by every bare `TextButton` app-wide; a global edit regresses unrelated screens. Use local per-control `ButtonStyle` overrides (see `lib/theme/nav_chip_style.dart`).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---|---|---|---|
| `git add -A` | Sweeps in session-2 in-progress work + 6 skip-worktree signing files | blocking | Stage explicit paths only; run the signing-leak guard before commit |
| Global `textButtonTheme` edit | The "crooked heights" trap — regresses every bare TextButton | blocking | Local `ButtonStyle` overrides (`nav_chip_style.dart`) |
| Windows walk recipes | Phase plans say `flutter run -d windows` | advisory | macOS: `flutter run -d macos --dart-define=GW_DEV_TOOLS=true` (Flutter 3.41.9) |
| Stale app instance | Holds Hive lock → next launch = silent black window | advisory | Kill app before relaunch |

<current_state>
End of day. **Homepage/dashboard redesign is COMPLETE and WALKED & APPROVED (dark)** by Jakub across all 7 uncommitted quick tasks. **Navbar is final.** Session 2 has stopped. Everything is uncommitted in the working tree on `ui-redesign-port` (baseline `7a95e68`): 25 modified + 6 new files (+1228/-418). No PR yet — deferred, awaiting Jakub's explicit authorization.
</current_state>

<completed_work>
Approved on the homepage (dark), all uncommitted:
- `260720-uhe` — section spacing + `space3` token + chart overflow fix
- `260720-vwj` — Assets panel A2 (killed "No balance yet")
- `260721-0ze` — app-wide brand sweep (flat `#14C8FF` → gradient CTA / `brandPrimaryStrong` highlights)
- `260721-1nk` — Navbar "B" + Markets "A" + secondary CTA
- `260721-bxr` — Nav tab "C" + unified `GWSectionTitle` header height
- `260721-ch1` — Markets "View All" editorial link
- `260721-ed9` — top-bar right cluster (sketch 005-B quiet chips, all controls 40px)

Session 2 (folded in): Bitcoin/coin chart (sketch 006) approved on homepage.
</completed_work>

<remaining_work>
- **PR** for the 7 approved tasks — needs Jakub's go; stage per-path (never `-A`), against `ui-redesign-port` (not `main`).
- **Sketch 007** transaction filters — PAUSED; 5 variants built; recommendation **C · icon-only compact**; implement via `/gsd-quick --validate` once picked.
- **Non-homepage pages** — a few redesign items remain (Jakub's note).
- **Light-mode AA pass** — deferred (dark-first policy).
- Session 2: hover-language 008 (nav + VIEW ALL) still in progress — theirs, not this session's.
</remaining_work>

<decisions_made>
- Navbar (tabs + right cluster) is FINAL (Jakub, 2026-07-21).
- 40px control normalization via local `ButtonStyle` overrides, not `theme.dart`.
- `nav_chip_style` pins `VisualDensity.standard` (desktop's ambient compact was collapsing chips to ~32px).
- Connect outline/label appearance-aware (`0xFF0B6E8F` in light) for WCAG AA on white.
</decisions_made>

## Required Reading (in order)
1. `.planning/STATE.md` — Quick Tasks table + the two 2026-07-21 approval notes
2. `.planning/HANDOFF.json` — machine-readable state for `/gsd-resume-work`
3. `.planning/sketches/MANIFEST.md` — sketch winners (001→A2, 005→B, 006 A-family, 007 pending, 008 D)
4. `CLAUDE.md` — lazy-dev rules + the no-commit gate

<next_action>
Confirm with Jakub which to do first: (a) open the PR for the 7 approved tasks (needs his go; per-path staging), (b) implement sketch 007 filters (rec C) via `/gsd-quick --validate`, or (c) start the non-homepage pages. Do NOT commit without authorization.
</next_action>
