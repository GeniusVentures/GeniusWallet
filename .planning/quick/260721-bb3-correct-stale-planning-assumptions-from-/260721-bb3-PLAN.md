---
quick_id: 260721-bb3
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-dashboard/05-UI-SPEC.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/2026-07-21-chart-zoom-pan-icons-still-raw-colors-white.md
  - .planning/todos/pending/2026-07-21-decision-dashboard-error-branch-retry.md
  - .planning/todos/pending/2026-07-21-side-by-side-walk-dashboard-vs-release-exe.md
  - .planning/todos/pending/2026-07-21-wire-real-timeframe-ranges-in-crypto-live-chart.md
autonomous: true
requirements:
  - verification:05-VERIFICATION-new_findings-zoom-pan-icons
  - verification:05-VERIFICATION-gap-1-dashboard-retry-decision
  - verification:05-VERIFICATION-human_verification-release-exe-walk-NOW_POSSIBLE
  - verification:05-VERIFICATION-deferred-chart-reskin-superseded
  - state:open-decision-2-ui-spec-3.1-carries-the-1.96-pairing

must_haves:
  truths:
    - "UI-SPEC §3.1's GNUS/Minions toggle row names `textOnBrand` as the selected-label token, and carries the two measured ratios (1.96:1 rejected, 10.12:1 shipped) inline so the next reader cannot re-introduce the failing pairing."
    - "The §3.1 table still has exactly its original 5 data rows, its header, its separator, and the `**Binding rule:**` paragraph after it — the correction is one cell plus a note, not a rewrite."
    - "ROADMAP Phase 7 carries an explicit inheritance note stating that `crypto_live_chart.dart` was already re-skinned inside Phase 5 by quick task 260721-dws, and that the only residue is 4 raw `Colors.white` values on the zoom/pan IconButton row at lines 455/463/471/480."
    - "Phase 7's four success criteria are textually unchanged — the note is additive context for a future planner, not a scope edit."
    - "Three new todo files exist under `.planning/todos/pending/`, each matching the existing convention (date-prefixed filename, `created`/`title`/`area`/`files` frontmatter, `## Problem` + `## Solution` body)."
    - "The zoom/pan-icons todo and the existing timeframe-ranges todo cross-reference each other BOTH ways, so whoever picks up either one sees that deleting zoom/pan kills the icon fix."
    - "The retry todo is framed as a DECISION with two named options and their consequences — it proposes no code and picks no winner."
    - "Nothing under `lib/`, `test/` or `packages/` is modified. `git status --porcelain` shows only `.planning/` paths."
  artifacts:
    - .planning/phases/05-dashboard/05-UI-SPEC.md
    - .planning/ROADMAP.md
    - .planning/todos/pending/2026-07-21-chart-zoom-pan-icons-still-raw-colors-white.md
    - .planning/todos/pending/2026-07-21-decision-dashboard-error-branch-retry.md
    - .planning/todos/pending/2026-07-21-side-by-side-walk-dashboard-vs-release-exe.md
  key_links:
    - "UI-SPEC §3.1 is the CONTRACT; `lib/components/wallet_overview.dart` is already correct at HEAD (`fillColor: brandPrimary` + `selectedColor: textOnBrand`). This task makes the contract match the shipped code — the reverse direction (editing lib/) would be a regression."
    - "The §3.1 defect has now escaped twice by two different routes (05-02's toggle, then k81's transaction badge). The inline ratios are the mechanism that stops a third escape; a bare token swap with no WHY does not."
    - "ROADMAP Phase 7's note must name the four line numbers. Without them a Phase 7 planner either re-budgets work 260721-dws already did, or loses the residue entirely — the two opposite failure modes the note exists to prevent."
    - "The zoom/pan-icons todo is CONDITIONAL on the timeframe-ranges todo's step 3 (whether zoom/pan survives). One-way linking leaves the dead-todo trap in place."
---

<objective>
Correct two stale planning assumptions that the Phase 05 re-verification surfaced, and capture its three loose follow-ups as todos.

Purpose: `05-VERIFICATION.md` names UI-SPEC §3.1's failing toggle pairing as "the one finding that should outlive this report" — 05-02 measured it, worked around it in code, and asked TWICE for the source to be corrected; it was not, and the identical white-on-bright-brand-fill defect then re-escaped through the transaction badge (quick task k81). Separately, the report's own `deferred` block hands Phase 7 a chart re-skin that quick task 260721-dws already delivered inside Phase 5. Both documents currently mislead their next reader.
Output: one corrected contract cell (with the measured ratios inline), one additive Phase 7 inheritance note, three new todos, one existing todo back-linked. `.planning/` only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-dashboard/05-VERIFICATION.md
@.planning/phases/05-dashboard/05-UI-SPEC.md
@.planning/ROADMAP.md
@.planning/todos/pending/2026-07-21-wire-real-timeframe-ranges-in-crypto-live-chart.md
@.planning/todos/pending/2026-07-20-cta-hover-state-is-over-rounded.md
</context>

<scope_fence>
**HARD FENCE — `.planning/` ONLY.**

No file under `lib/`, `test/`, or `packages/` may be created, modified or deleted by this task.
Every item here is a documentation/contract correction or a captured follow-up.

The live code is ALREADY CORRECT: `lib/components/wallet_overview.dart` uses
`fillColor: brandPrimary` with `selectedColor: textOnBrand` at HEAD, and
`lib/chart/crypto_live_chart.dart`'s four raw values are being RECORDED, not fixed.
If you conclude a code change is required, STOP and report it — do not make it.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Correct UI-SPEC §3.1's toggle row to the shipped, AA-passing token</name>
  <files>.planning/phases/05-dashboard/05-UI-SPEC.md</files>
  <action>
Edit ONE table cell in section 3.1, plus add one short note under the table.

Read lines 150-170 first to see the exact table. The row to change is the one whose first
cell is `| GNUS/Minions toggle |` (currently line 164). Its "Re-skin to" cell currently
prescribes three tokens; the middle one — the token applied to the SELECTED label sitting on
top of the `brandPrimary` fill — is wrong.

Make these surgical changes to that cell, using Edit (not Write):

1. Replace the selected-label token with `GeniusWalletColors.textOnBrand`. Leave the fill
   token (`brandPrimary`) and the border token (`borderSubtle`) exactly as they are — only
   the selected-label token moves.
2. Keep the cell's existing prose shape (it currently reads "keep `ToggleButtons`, re-skin its
   `fillColor`/`selectedColor`/`borderColor` to ..."). Do not restructure the sentence.
3. The cell's existing parenthetical credits the shadow file's own token choices at lines
   148-152. That credit is now misleading, because this is a DEVIATION from the shadow, not a
   copy of it. Reword the parenthetical to say the shadow's own choice was measured and
   rejected — cite the measured dark-mode ratio of the rejected pairing (1.96:1, a WCAG AA
   failure) and the measured ratio of the substitute (10.12:1) — and note that this correction
   was applied 2026-07-21 after 05-02 shipped the substitute in code.

Then, immediately BELOW the §3.1 table and ABOVE the existing `**Binding rule:**` paragraph,
add one short blockquote note (2-4 lines) that states, in plain prose:
  - the general rule the ratios encode: a label placed on a saturated brand fill takes
    `textOnBrand`, never the appearance-aware body-text token;
  - that this exact defect escaped twice (05-02's toggle, then the transaction badge via quick
    task 260721-k81, which `05-VERIFICATION.md` re-closed) — so the rule is load-bearing, not
    cosmetic.

Preserve every other row, the table header, the separator row, the `**Binding rule:**`
paragraph, and all surrounding section text byte-for-byte. Do NOT touch §3.2 or any other
section. Do NOT touch `lib/`.
  </action>
  <verify>
    <automated>ROW=$(grep -F 'GNUS/Minions toggle' .planning/phases/05-dashboard/05-UI-SPEC.md) && echo "$ROW" | grep -q 'textOnBrand' && echo "$ROW" | grep -q 'brandPrimary' && echo "$ROW" | grep -q 'borderSubtle' && echo "$ROW" | grep -q '1\.96:1' && echo "$ROW" | grep -q '10\.12:1' && [ "$(awk '/^### 3\.1 /,/^\*\*Binding rule:/' .planning/phases/05-dashboard/05-UI-SPEC.md | grep -c '^|')" = "7" ] && grep -q '^\*\*Binding rule:' .planning/phases/05-dashboard/05-UI-SPEC.md && echo PASS</automated>
  </verify>
  <done>
§3.1's toggle row names `textOnBrand` as the selected-label token alongside the unchanged
`brandPrimary` fill and `borderSubtle` border, and carries both measured ratios (1.96:1
rejected / 10.12:1 shipped) inline. The table still has exactly 7 pipe-rows (header +
separator + 5 data rows) and the `**Binding rule:**` paragraph survives. A short rule note
sits between them. No file outside `.planning/` changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add a Phase 7 inheritance note recording the chart re-skin already delivered in Phase 5</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Read the `### Phase 7: Token screens` section (around line 260) before editing.

Its four success criteria are CORRECT and stay textually unchanged — in particular criterion 1
("Token info, send, receive, address book and market data render in the redesign skin and match
the Release exe reference"). Do not reword any criterion.

Using Edit (not Write), add a short note to the Phase 7 block, placed after the existing
`**Findings**:` line (the section's trailing metadata area, immediately before the
`### Phase 8:` heading). Give it a bolded label in the same style as the neighbouring metadata
lines — e.g. `**Inherits from Phase 5**:` — and state:

  - `lib/chart/crypto_live_chart.dart` was ALREADY re-skinned inside Phase 5, by quick task
    260721-dws (sketch 006 variant A→), which landed in `0bcf3df` via PR #210. Phase 7
    inherits a largely re-skinned chart — do NOT budget for a full re-skin of this file.
  - The superseded assumption, named so it is not resurrected: the 2026-07-20 Phase 5
    verification had deferred the WHOLE chart re-skin to Phase 7, on the grounds that no 05-*
    plan touched the file and 8 raw `Colors.white`/`Colors.grey[400]` values survived. Both
    grounds are now false; `05-VERIFICATION.md`'s `deferred` block marks that item
    `status: superseded`.
  - The residue Phase 7 DOES inherit, re-counted at HEAD 2026-07-21: exactly 4 raw values
    survive, all `Colors.white`, all on the zoom/pan `IconButton` row — lines 455, 463, 471
    and 480. Every `Colors.grey[400]` is gone.
  - Point at the tracking todo
    `.planning/todos/pending/2026-07-21-chart-zoom-pan-icons-still-raw-colors-white.md`
    (created by Task 3 of this plan) and note the residue is expected to be closed by the
    deferred light-mode AA pass, and may be deleted outright if the zoom/pan controls are
    removed when real timeframe ranges are wired.

Keep it to a compact block. Do not touch any other phase section. Do not touch `lib/`.
  </action>
  <verify>
    <automated>SEC=$(awk '/^### Phase 7: /,/^### Phase 8: /' .planning/ROADMAP.md) && echo "$SEC" | grep -q 'crypto_live_chart' && echo "$SEC" | grep -q '260721-dws' && echo "$SEC" | grep -q '455' && echo "$SEC" | grep -q '480' && echo "$SEC" | grep -q 'chart-zoom-pan-icons-still-raw-colors-white' && [ "$(echo "$SEC" | grep -c '^  [0-9]\. ')" = "4" ] && echo "$SEC" | grep -q 'match the Release exe reference' && echo PASS</automated>
  </verify>
  <done>
Phase 7 in ROADMAP.md carries an additive inheritance note naming quick task 260721-dws, the
superseded whole-file deferral, the 4-value `Colors.white` residue at lines 455/463/471/480,
and the tracking todo path. All four Phase 7 success criteria are byte-unchanged (criterion 1
still ends "match the Release exe reference"), and no other phase section moved.
  </done>
</task>

<task type="auto">
  <name>Task 3: File the three re-verification follow-ups as todos and back-link the timeframe todo</name>
  <files>.planning/todos/pending/2026-07-21-chart-zoom-pan-icons-still-raw-colors-white.md, .planning/todos/pending/2026-07-21-decision-dashboard-error-branch-retry.md, .planning/todos/pending/2026-07-21-side-by-side-walk-dashboard-vs-release-exe.md, .planning/todos/pending/2026-07-21-wire-real-timeframe-ranges-in-crypto-live-chart.md</files>
  <action>
Read `.planning/todos/pending/2026-07-21-wire-real-timeframe-ranges-in-crypto-live-chart.md` and
`.planning/todos/pending/2026-07-20-cta-hover-state-is-over-rounded.md` FIRST and match their
convention exactly: date-prefixed kebab filename, YAML frontmatter with `created` (ISO-8601 Z),
`title`, `area`, and a `files:` list, then a `## Problem` section and a `## Solution` section.
Use `created: 2026-07-21T00:00:00.000Z`. Use `area: ui` for (a) and (c); use `area: ui` for (b)
as well since it concerns a dashboard surface.

Write three NEW files:

(a) `2026-07-21-chart-zoom-pan-icons-still-raw-colors-white.md`
    Title names the defect: the chart's zoom/pan icons are still raw `Colors.white`.
    `files:` lists `lib/chart/crypto_live_chart.dart:455,463,471,480`.
    Problem: quick task 260721-dws deliberately KEPT zoom/pan (re-skin-never-restructure) and
    re-skinned around it, leaving four `const Icon(..., color: Colors.white)` on the zoom-in /
    zoom-out / pan-left / pan-right buttons. Keeping the BEHAVIOR did not require keeping the
    raw color. State explicitly that these are NOT the documented always-dark scrim exception —
    that exception covers text over a dark image scrim (e.g. `crypto_news_screen.dart:173,224`)
    — whereas these are plain icons on the card surface, so they will wash out in light mode.
    Note it is not a regression (the file was worse before) and has not been walked in light.
    Solution: migrate the four to an appearance-aware token (`gw.textSecondary` or equivalent)
    during the deferred light-mode AA pass. Then, as an explicit sequencing constraint, link
    `.planning/todos/pending/2026-07-21-wire-real-timeframe-ranges-in-crypto-live-chart.md` and
    state that its step 3 asks whether zoom/pan is redundant once real timeframe ranges are
    wired — if zoom/pan is deleted, THIS todo dies with it, so settle that question first and
    do not spend the light-pass budget on controls that may not survive.

(b) `2026-07-21-decision-dashboard-error-branch-retry.md`
    Title must start with `DECISION NEEDED —` so it reads as a decision at a glance (follow the
    precedent of the mobile-nav IA todo, which flags itself as a product decision in its body).
    `files:` lists `lib/dashboard/home/view/dashboard_screen.dart:70`,
    `.planning/ROADMAP.md:228`, `.planning/phases/05-dashboard/05-UI-SPEC.md` (§6).
    Problem: this is the SINGLE open gap in `05-VERIFICATION.md` and it BLOCKS Phase 05
    sign-off. Two project documents are in direct conflict and no implementation resolves that
    without a ruling:
      - ROADMAP Phase 5 success criterion 3 (line 228) requires the failure branch to show an
        error "with a working retry";
      - UI-SPEC §6 explicitly locks develop's error string and forbids reconciling it to
        `GWErrorState`;
      - the phase goal ("the dashboard wears the redesign and keeps every behavior develop
        shipped") sides with §6.
    Record the current state: `dashboard_screen.dart:70` is a bare
    `Center(child: Text('Something went wrong!'))` — the error branch returns INSTEAD OF, not
    alongside, `OneColumnDashBoardView`/`ResponsiveDashboardView`, so a user whose account load
    fails has no in-app recovery short of restarting. Note the separate, still-unexercised
    retry that DOES exist on the markets leg (finding 8).
    Solution: present exactly TWO options with their consequences, and pick NEITHER —
      Option 1: add a retry affordance to the dashboard failure branch. Closes ROADMAP
      criterion 3; requires overriding UI-SPEC §6's lock, which must be recorded as a
      deliberate override, not done silently.
      Option 2: record an override stating §6 intentionally descoped the retry, and reword
      ROADMAP criterion 3 to match. Closes the spec conflict with no code; requires a roadmap
      edit and an explicit acceptance that the dashboard has no in-app recovery.
    State plainly that this todo proposes NO code and that either path needs a human ruling
    first. Also carry forward the report's second `missing` item: one forced-failure
    observation proving a retry press actually re-issues the fetch (markets leg, finding 8).

(c) `2026-07-21-side-by-side-walk-dashboard-vs-release-exe.md`
    Title: side-by-side walk of the five dashboard surfaces against the reference Release exe —
    now UNBLOCKED on the Windows box.
    `files:` may list the five surfaces' owning files or simply the phase dir; keep it honest —
    this is a walk task, not a code task.
    Problem: ROADMAP Phase 5 criterion 1's "match the Release exe at GeniusWallet-3514" clause
    was recorded as NEVER TESTED, attributed to the reference being absent. That attribution was
    true of the macOS session that wrote the report and is WRONG for this Windows machine, where
    the built reference is on disk at
    `C:\Users\User\Documents\Projects\GNUS-compare\GeniusWallet-3514\build\windows\x64\runner\Release\genius_wallet.exe`.
    `05-VERIFICATION.md` already marks this item `status: NOW_POSSIBLE`. The five dashboard
    surfaces — balances, holdings, transactions, markets, news — have never been compared
    against it.
    Solution: run both binaries side by side on the Windows box and compare the five surfaces
    one at a time, recording per-surface pass/deviation. Note that the redesign is intentionally
    NOT pixel-identical to the reference in places (approved sketches 001/002/003/005/006/008
    changed the dashboard deliberately), so the walk records *deliberate deviation* vs
    *unintended regression* rather than demanding a pixel match. Note the light-mode AA pass is
    still deferred (dark-first), so light-mode differences are expected and out of scope here.

Finally, EDIT the existing
`.planning/todos/pending/2026-07-21-wire-real-timeframe-ranges-in-crypto-live-chart.md` to
close the cross-reference loop: extend its step 3 (the zoom/pan-redundancy question) with a
sentence pointing at
`.planning/todos/pending/2026-07-21-chart-zoom-pan-icons-still-raw-colors-white.md`, stating
that a second todo depends on this answer and dies if zoom/pan is removed. Change nothing else
in that file.

Do NOT touch `lib/`, `test/` or `packages/`.
  </action>
  <verify>
    <automated>T=.planning/todos/pending && A=$T/2026-07-21-chart-zoom-pan-icons-still-raw-colors-white.md && B=$T/2026-07-21-decision-dashboard-error-branch-retry.md && C=$T/2026-07-21-side-by-side-walk-dashboard-vs-release-exe.md && D=$T/2026-07-21-wire-real-timeframe-ranges-in-crypto-live-chart.md && for f in $A $B $C; do grep -q '^created: 2026-07-21' $f && grep -q '^title: ' $f && grep -q '^area: ' $f && grep -q '^files:' $f && grep -q '^## Problem' $f && grep -q '^## Solution' $f || { echo "FRONTMATTER FAIL $f"; exit 1; }; done && grep -q '455' $A && grep -q '480' $A && grep -qi 'scrim' $A && grep -q 'wire-real-timeframe-ranges-in-crypto-live-chart' $A && grep -q 'chart-zoom-pan-icons-still-raw-colors-white' $D && grep -q '^title: DECISION NEEDED' $B && grep -q 'dashboard_screen.dart' $B && grep -qi 'option' $B && grep -q 'GeniusWallet-3514' $C && [ -z "$(git status --porcelain | grep -vE '^.. \.planning/')" ] && echo PASS</automated>
  </verify>
  <done>
Three new todo files exist under `.planning/todos/pending/`, each with the project's
`created`/`title`/`area`/`files` frontmatter and `## Problem` + `## Solution` body. The
zoom/pan todo names lines 455-480, distinguishes itself from the scrim exception, and links the
timeframe todo — which now links back. The retry todo is titled `DECISION NEEDED —`, names both
options and their consequences, and proposes no code. The walk todo names the on-disk reference
exe path. `git status --porcelain` shows only `.planning/` paths.
  </done>
</task>

</tasks>

<verification>
1. `git status --porcelain` lists ONLY paths under `.planning/` — zero entries under `lib/`,
   `test/` or `packages/`. This is the plan's hardest gate; a hit here means the scope fence
   was breached and the change must be reverted, not patched.
2. `git diff --stat` shows exactly 6 files: `05-UI-SPEC.md`, `ROADMAP.md`, and 4 todo files
   (3 new + 1 back-linked).
3. UI-SPEC §3.1's toggle row and `lib/components/wallet_overview.dart` now agree: both name
   `brandPrimary` as the fill and `textOnBrand` as the selected label. Confirm by reading the
   lib file — READ ONLY, no edit; it is already correct and must stay untouched.
4. Phase 7's four success criteria in `ROADMAP.md` are unchanged under `git diff` (the diff for
   that section shows only added lines, zero removed lines inside the criteria block).
</verification>

<success_criteria>
- The §3.1 contract no longer prescribes a pairing that measures 1.96:1, and carries the two
  measured ratios plus the escape history inline so a future reader cannot re-introduce it.
- A Phase 7 planner reading ROADMAP.md learns, without opening `05-VERIFICATION.md`, that the
  chart is already largely re-skinned and that exactly 4 named lines remain.
- The three loose follow-ups from the re-verification are captured in the project's standing
  todo format, with the zoom/pan ↔ timeframe dependency linked in both directions.
- Zero code files changed.
</success_criteria>

<output>
Create `.planning/quick/260721-bb3-correct-stale-planning-assumptions-from-/260721-bb3-SUMMARY.md` when done.

Commit note: GSD atomic commits ARE authorized on branch `ui-redesign-port` despite AGENTS.md's
"Do not create commits" line (recorded standing exception). Commit per task, `.planning/` paths
only, staged per-path — never `git add -A` (the shared tree carries 6 skip-worktree signing
files).
</output>
