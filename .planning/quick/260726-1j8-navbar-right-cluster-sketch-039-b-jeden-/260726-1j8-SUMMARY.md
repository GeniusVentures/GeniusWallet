---
quick_id: 260726-1j8
subsystem: ui
tags: [navbar, chrome, control-track, reown, wallet-connect, sdk, dark-mode]

provides:
  - "Navbar right cluster on sketch 039-B: one surfaceSunken control track holding the three context selectors, Connect and Buy GNUS as external actions, all sharing a 40px baseline"
  - "ReownConnectButton: all five state branches (idle/connected/connecting/timedOut/error) unified onto transparent-fill + 1px state-coloured outline"
  - "Sketch 039 recorded as won (B), MANIFEST rows added for 038 and 039, sketch 005 annotated as superseded"
affects: [navbar, top-bar, reown-connect, mobile-app-bar, sketch-manifest]

key-files:
  modified:
    - lib/theme/nav_chip_style.dart
    - lib/components/overlay/responsive_overlay.dart
    - lib/reown/reown_connect_button.dart
    - test/theme/nav_chip_style_test.dart
    - .planning/sketches/039-navbar-right-cluster-v2/README.md
    - .planning/sketches/MANIFEST.md

key-decisions:
  - "navContextChipStyle redefined in place (not a new function) — 32px, transparent-at-rest, borderless, pill chip with surfaceElevated hover; the hairline border moved from the chip to the track"
  - "Control track Container copied verbatim from the _TimeframeSegment recipe (surfaceSunken + borderSubtle hairline + radiusPill + 3px padding) into _buildActionRowWidgets, which feeds both the desktop bar and the MobileOverlay AppBar"
  - "Connect border weight unified to 1px across all five states (idle was previously 1.5px) per Jakub's locked refinement"
  - "Buy GNUS stays 40px — untouched, per Jakub's lock"
  - "2px phantom gap accepted when SDKAccountManagerButton self-hides (ponytail comment left in responsive_overlay.dart naming the ceiling and upgrade path)"

requirements-completed: []

duration: ~35min
completed: 2026-07-26
status: complete
---

# Quick Task 260726-1j8: Navbar right cluster — sketch 039-B + Connect outline Summary

**Three navbar context selectors (chain/SDK/wallet) moved into one `surfaceSunken` control track; all five `ReownConnectButton` states unified onto transparent-fill + 1px state-coloured outline; sketch 039 recorded as won.**

## Performance

- **Duration:** ~35 min
- **Tasks:** 4/4 completed
- **Files modified:** 6 (3 lib files, 1 test file, 2 planning docs)

## Accomplishments

- `navContextChipStyle` (`lib/theme/nav_chip_style.dart`) redefined in place: 32px height (`minimumSize`/`maximumSize`), transparent background at rest with a `surfaceElevated` hover lift, `BorderSide.none` (border moved to the track), pill shape (`radiusPill`), `space4` horizontal padding. Docstring rewritten to describe the in-track chip and point at `CONVENTIONS.md` → Control track.
- `_buildActionRowWidgets` (`lib/components/overlay/responsive_overlay.dart`) now returns **two** entries: a `Container` control track (padding 3, `surfaceSunken` fill, `borderSubtle` hairline, `radiusPill`) wrapping `Row(spacing: 2)` of `NetworkDropdownSelector` / `SDKAccountManagerButton` / `AccountDropdownSelector`, followed by the unchanged `ReownConnectButton`. Desktop bar gap changed `space4` → `space6` (12px) between track/Connect/Buy GNUS. MobileOverlay's `AppBar.actions` `Row` gained the same `space6` spacing (previously none).
- `ReownConnectButton.build` (`lib/reown/reown_connect_button.dart`) collapsed from 4 distinct 18%-alpha fills + 1 outlined idle state into one shape: every branch now sets only `icon`, `text`, and a single `stateColor`; after the if/else chain, `backgroundColor = Colors.transparent`, `iconColor = textColor = stateColor`, `border = BorderSide(color: stateColor, width: 1)` for all five. `BorderSide? border` became non-nullable, collapsing the `side` ternary. Both stale comments (mode-invariant fixed fills / idle-only ghost border) rewritten to describe the new one-language behavior.
- Sketch 039 README: `winner: "B"` + `shipped: 2026-07-26` set in frontmatter; a `## Decyzja (2026-07-26)` section added recording Jakub's two refinements (Buy GNUS stays 40px; all Connect states outlined) plus a note correcting the README's own Diagnoza table (selectors were already 40px via `nav_chip_style.dart` before this sketch was drawn — table left unmodified, correction only noted in prose).
- `MANIFEST.md`: appended rows for `038 dashboard-column-widths` (winner A1) and `039 navbar-right-cluster-v2` (winner B · jeden tor, noting both refinements and the supersede of 005). Row `005` annotated in place — not deleted — with a "Superseded by 039" note explaining its 40px normalisation shipped but its quiet-fill styling was designed against a gradient sheen since flattened.

## Task Commits

No commits were created — CLAUDE.md: "Do not create commits." All changes are left in the working tree for review.

1. Task 1: `navContextChipStyle` in-track chip redefinition — uncommitted
2. Task 2: control track wrapping the three selectors — uncommitted
3. Task 3: Connect button all-outlined states — uncommitted
4. Task 4: sketch 039 + MANIFEST bookkeeping — uncommitted

## Files Created/Modified

- `lib/theme/nav_chip_style.dart` - `navContextChipStyle` rewritten to the 32px transparent/borderless in-track chip; docstring updated
- `lib/components/overlay/responsive_overlay.dart` - `_buildActionRowWidgets` builds the control track; desktop + mobile action-row spacing changed to `space6`
- `lib/reown/reown_connect_button.dart` - all five Connect states unified to transparent-fill + 1px state-coloured outline; stale comments corrected
- `test/theme/nav_chip_style_test.dart` - updated the `navContextChipStyle` test to assert the new transparent/borderless/32px/pill contract (see Deviations)
- `.planning/sketches/039-navbar-right-cluster-v2/README.md` - `winner`/`shipped` frontmatter set; Decyzja section added
- `.planning/sketches/MANIFEST.md` - rows added for 038 and 039; row 005 annotated as superseded

## Decisions Made

- Kept `navContextChipStyle` as the single style function (no new function, no call-site edits) — its 3 consumers (the three selectors) all move into the track, so redefining what it returns is the entire change.
- Matched the plan's exact code shape for the selector children (`Row(..., children: const [...])` rather than a fully `const Row`), to stay literal with the plan's specified snippet even though Dart would implicitly const the whole subtree either way.
- Border weight on all five Connect states unified to 1px (idle was 1.5px before). Left the named fallback (restore 1.5 on idle only) undone since no visual regression was reported — human look will confirm.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated stale `navContextChipStyle` unit test to match the new in-track chip contract**
- **Found during:** Post-Task-1 verification (`flutter test` on files touched, per the plan's verification instructions)
- **Issue:** `test/theme/nav_chip_style_test.dart` had an existing test, `navContextChipStyle: surfaceMenu fill + borderSubtle->borderStrong hover`, asserting the *old* chip behavior (surfaceMenu fill, borderSubtle/borderStrong hover border). That test necessarily broke once Task 1 redefined the style to transparent-at-rest/borderless/surfaceElevated-hover/32px/pill — this is not a bug in the new code, but a test written against the design this plan explicitly supersedes.
- **Fix:** Rewrote the test to assert the new contract: transparent background at rest, `surfaceElevated` background on hover, `BorderSide.none` at rest (border now lives on the track, not the chip), 32px min/max height, and `radiusPill` shape.
- **Files modified:** `test/theme/nav_chip_style_test.dart`
- **Verification:** `flutter test test/theme/nav_chip_style_test.dart test/theme/theme_contrast_test.dart` → all 16 tests pass.
- **Committed in:** Not committed (per hard constraint — no commits this task).

---

**Total deviations:** 1 auto-fixed (Rule 1 - stale test correction)
**Impact on plan:** The plan explicitly scoped "no new test file" and no logic changes, but did not anticipate an *existing* test asserting the exact old-chip contract this plan replaces. Updating it was necessary to keep `flutter test` green for files touched, per the plan's own verification section ("Run any test whose files you touched"). No scope creep — the test was updated, not deleted, and still exercises `navContextChipStyle`'s actual public contract.

## Issues Encountered

None beyond the deviation above. `flutter analyze lib` held at 59 issues (the baseline) after every task — no regressions introduced.

## What The Human Should Look At (hot reload `r`, never `R`)

The app is already running under `flutter run -d macos`. Hot reload (`r`) picks up all changes below. Both the **desktop bar** and the **mobile AppBar** are affected (same `_buildActionRowWidgets` feeds both).

**Desktop bar:**
1. Bottom edges of the control track, Connect, and Buy GNUS should form one unbroken 40px line.
2. The three context selectors (chain / SDK account / wallet) should read as one recessed `surfaceSunken` well with a single hairline border — no individual chip should carry its own border.
3. Hover each of the three selectors — it should lift onto `surfaceElevated` inside the track (no border change, just the fill).
4. Each selector should still open its own existing drawer/menu unchanged.
5. 12px gaps: track → Connect → Buy GNUS.

**No-SDK-account case** (temporarily short-circuit the `accounts.isEmpty` guard at `lib/account/sdk_account_manager.dart:40-41` under hot reload to force it true, look, then revert):
- The track should keep its shape and hairline with only two chips inside (chain + wallet); the row should not restyle or resize awkwardly. Only a ~2px phantom gap (from the hidden chip's reserved `Row(spacing: 2)` slot) may differ — this is an accepted, documented ceiling, not a bug.

**Mobile AppBar** (narrow the window past `GeniusBreakpoints.small`, or check `MobileOverlay`):
- The control track should appear in the AppBar's horizontally-scrolling actions row, not overflow, and the icon-only 32px chips should still fit and be tappable.

**ReownConnectButton — all 5 states** (internal booleans `_session`/`_isConnecting`/`_timedOut`/`_hasError` are hard to reach naturally; temporarily force one branch true at the top of `build()` under hot reload, look, move to the next, then revert all forcing):
- **idle** → brand-coloured outline, "Connect" label + link icon, both in the brand colour.
- **connected** → red outline, red "Disconnect" label, red link-off icon (Jakub's stated target for this branch).
- **connecting** → amber outline, "Connecting" label, sync icon still spinning (via `AnimatedRotation`, unchanged).
- **timed out** → amber outline, "Timed Out" label, timer-off icon.
- **error** → red outline, "Retry Connect" label, error-outline icon.
- For every state: transparent fill (the bar's background should show through — no tinted plate behind the button), 1px border in the state colour, label + icon both in that same colour, and the button should still measure 40px tall, level with the control track and Buy GNUS.
- After checking all 5, revert every forced branch and confirm the real idle state renders correctly (this is the button's actual default state when no wallet is connected).

## Next Phase Readiness

- Sketch 039 is fully recorded as shipped (winner B, both refinements); MANIFEST.md and sketch 005 are consistent with what's now in the tree.
- No blockers. The only deferred item is the accepted 2px phantom-gap ceiling on the control track when the SDK account list is empty — documented with a `ponytail:` comment and upgrade path in `responsive_overlay.dart`, not a task for this or the next plan unless it becomes visually objectionable.
- Light mode was explicitly out of scope per this plan's own verification section (deferred to the dedicated light-mode pass) and was not touched or verified here.

---
*Quick task: 260726-1j8*
*Completed: 2026-07-26*
