---
quick_id: 260727-w58
status: complete
date: 2026-07-27
commit: none - CLAUDE.md forbids commits, see Deviations
files_created:
  - lib/components/cards/gw_kicker.dart
  - test/components/gw_kicker_test.dart
files_modified:
  - lib/dashboard/home/widgets/transactions_slim_view.dart
  - lib/dashboard/chart/markets_hero_card.dart
  - lib/dashboard/chart/markets_table.dart
  - lib/tokens/token_info_screen.dart
  - lib/components/cards/gw_view_all_link.dart
  - .planning/sketches/065-kicker-component/README.md
  - .planning/sketches/066-drawer-language/index.html
gates:
  analyze_lib: 59
  analyze_baseline: 59
  tests_pass: 328
  tests_fail: 1
  tests_baseline: "323 pass / 1 fail"
---

# Quick 260727-w58 - SUMMARY

Five hand-written values for one typographic role, replaced by one component with two steps.
Sketch 065, variant **C**.

## What was built

**`lib/components/cards/gw_kicker.dart`** - `GWKicker(label, {dense, trailing})` plus
`GWKicker.style(gw, {dense})`. Two steps: 13/w600/0.5/lineHeight 18 by default, 11/w600/0.6/lineHeight
16 when `dense`. Upper-casing belongs to the component.

**`test/components/gw_kicker_test.dart`** - 5 tests, all green.

## All five call sites migrated

| File | How | Visible change |
|---|---|---|
| `transactions_slim_view.dart` (TODAY/YESTERDAY) | widget, default | tracking 0.7 -> 0.5 |
| `token_info_screen.dart` (INFO/CONVERT) | widget, default | tracking 0.4 -> 0.5 |
| `markets_hero_card.dart` (stat captions) | widget, `dense` | **10px -> 11px** |
| `markets_table.dart` (column headers) | `GWKicker.style(dense)` | **none** - 11/w600/0.6 is exactly what the file already carried |
| `gw_view_all_link.dart` (VIEW ALL) | `GWKicker.style(dense)` + keeps `height:1.0`/white for its ShaderMask | tracking 0.88 -> 0.6 |

## Two changes nobody asked for - deliberate, not silent

Written down explicitly, because `quick-tasks-regress-plan-decisions` records that previous quick
tasks reversed decisions **silently**. Both are one `copyWith` away from being undone:

1. **The Markets hero grows from 10px to 11px.** That is the price of the app carrying two steps
   instead of five. Three captions sit side by side in the hero card - worth a live look for
   wrapping.
2. **`VIEW ALL` loses its 0.88 tracking for 0.6.** That value carried its own in-code comment
   (*"~0.08em at 11px"*), so this overrides a documented decision. The link's underline is drawn to
   the measured label width, so it follows automatically. Undo:
   `.copyWith(letterSpacing: 0.88)` in `gw_view_all_link.dart`.

## Deviations from the gsd-quick workflow

- **No commit.** `CLAUDE.md` says *"Do not create commits"*, and Jakub repeated it in the request.
  Workflow steps 5.6 and 8 (pre-dispatch plan commit, docs commit) were skipped deliberately.
- **No worktree planner/executor.** The task was fully specified by sketch 065, and a worktree
  executor commits - walking straight into the prohibition above. Run inline with the GSD structure
  preserved (directory, PLAN, SUMMARY, STATE.md row).

## Gates

- `flutter analyze lib` = **59**, baseline 59. Zero new.
- `flutter test` = **328 pass / 1 fail**. Baseline 323/1, so +5 is exactly this task's tests. The
  single failure is the inherited `test/local_wallet_storage_test.dart` - *"Missing definition of
  `main` method"*, untouched, confirmed by an isolated run.

## What this unblocks

Receipt **154-A** now has something to write `TRANSACTION` / `NETWORK` with, and sketch **066** has
its foundation for the other fourteen drawers. Both were blocked on the label not existing as a
component.

## What stays open

- **Light mode breaks AA in all five places.** `textSecondary` is `static const`, so it measures
  3.23:1 on a light card. Inherited, not introduced here - but the component is now the only place
  where it can be fixed once. Deferred to the light pass.
- Still unanswered: whether the receipt takes tap-to-copy rows (154-D).
