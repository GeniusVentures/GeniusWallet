---
phase: quick-260721-0ze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/components/buttons/gw_button.dart
  - lib/components/overlay/responsive_overlay.dart
  - lib/theme/theme.dart
  - lib/dashboard/home/widgets/transactions_slim_view.dart
  - lib/components/wallet_overview.dart
  - lib/account/account_dropdown_selector.dart
  - lib/account/sdk_account_manager.dart
  - lib/components/inputs/gw_switch.dart
  - lib/components/inputs/gw_text_field.dart
  - lib/components/inputs/gw_select.dart
  - lib/components/inputs/gw_checkbox.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every filled primary CTA app-wide renders the brandCta green→blue gradient, not the flat cyan brandPrimary fill."
    - "Primary CTAs get the same white brighten hover/press wash and pointer cursor that the gradient variant already has."
    - "The header 'Buy GNUS' button is a filled gradient button (was outline), matching the coins-screen Buy GNUS."
    - "The desktop top-nav active underline is the brandCta gradient; the active tab text+icon are solid brandPrimaryStrong (#0AAEE6)."
    - "The mobile bottom-nav selected item/icon/label are solid brandPrimaryStrong."
    - "Every other highlight/selection site (segmented button, toggle, selected tile/avatar, focused input borders, checkbox fill, switch active, tab indicator, nav rail selected, date-picker header+selected) uses solid brandPrimaryStrong."
    - "The flat neon #14C8FF (brandPrimary) no longer appears as a CTA fill or as an active/highlight color at any of the enumerated sites."
  artifacts:
    - lib/components/buttons/gw_button.dart
    - lib/components/overlay/responsive_overlay.dart
    - lib/theme/theme.dart
  key_links:
    - "GWButtonVariant.primary palette → GeniusWalletGradient.brandCta (single central edit propagates to all primary CTAs)."
    - "_overlay() switch → includes primary so its hover wash matches gradient."
    - "brandPrimaryStrong (existing #0AAEE6 token) reused directly at every solid-highlight site — no new hex, no new alias."

deferred:
  - "Light-mode WCAG AA: brandPrimaryStrong (#0AAEE6) fails AA (~2.9:1) for text/small elements on the white canvas. DARK-FIRST — deferred to the dedicated light-mode contrast pass. Do NOT solve here."
---

<objective>
App-wide brand-consistency sweep: kill the flat neon cyan brandPrimary (#14C8FF) as both a CTA fill and an active/highlight color. Filled primary CTAs become the brandCta green→blue gradient (one central edit in GWButton). Nav active state becomes a gradient underline + solid deep-cyan text/icon. Every other highlight/selection site swaps to the existing solid brandPrimaryStrong (#0AAEE6) token.

Purpose: One coherent brand identity — gradient for CTAs and the nav underline, solid deep-cyan for text/selection accents — instead of the mismatched flat neon.
Output: Edited GWButton primary palette + hover, nav (desktop + mobile), theme.dart highlight tokens, and per-widget highlight swaps.

Decisions honored (locked, do NOT re-open):
- Highlight treatment = gradient nav underline + solid deep-cyan (brandPrimaryStrong) for text/other highlights.
- Scope = full app-wide sweep. CTAs = gradient fill.
- Reuse existing brandPrimaryStrong; NO new hex.
- No `brandAccent` alias: an alias would require editing every site anyway (same churn) — referencing brandPrimaryStrong directly is lazier. Zero new tokens.

Out of scope (do NOT touch): gw_swap_fab, gw_spinner, qr_scanner, sgnus_connection_widget, reown_connect_button, gw_mesh_background, genius_wallet_elevation, coins_screen.dart, coin_card_row.dart, /banxa, /squidrouter, any *.g.dart. Cursor colors (gw_text_field:100, theme textSelectionTheme:289) and date-picker enabled/today-border/range tints are NOT in the enumerated swap list — leave them.
</objective>

<execution_context>
LAZY senior-dev rules apply. CLAUDE.md FORBIDS commits — NO git commit / git add in any task. Verify via `flutter analyze` and `grep`, never via commit. This is almost entirely token/decoration swaps: no new abstractions, no new deps, no test scaffold (a hollow test would add noise — flutter analyze is the runnable check).
</execution_context>

<context>
@lib/theme/genius_wallet_colors.dart
@lib/theme/genius_wallet_gradient.dart
@lib/components/buttons/gw_button.dart
@lib/components/overlay/responsive_overlay.dart
@lib/theme/theme.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Primary CTA → brandCta gradient (central) + header Buy GNUS fill</name>
  <files>lib/components/buttons/gw_button.dart, lib/components/overlay/responsive_overlay.dart</files>
  <action>
In gw_button.dart, change the `GWButtonVariant.primary` case of `_palette()` (currently around :110-117) so it paints the brand CTA gradient exactly like the existing `.gradient` case (:151-158): set `background: GeniusWalletColors.gradientBlue`, keep `foreground: GeniusWalletColors.textOnBrand` (near-black — required for WCAG AA on the bright fill), `border: null`, and add `gradient: GeniusWalletGradient.brandCta`. This single edit propagates to every primary CTA app-wide (main.dart "Go to Dashboard", settings_screen, custom_future_builder, gw_error_state, submit_job_dashboard_button, sdk_account_manager, account_dropdown_selector).

Still in gw_button.dart, in `_overlay()` (the switch around :321-335), add `case GWButtonVariant.primary:` sharing the same body as `case GWButtonVariant.gradient:` (hover = 0.12, press = 0.18) so primary now gets the same white brighten wash on hover/press instead of deferring to the default InkWell ripple (the bright gradient swallows the ripple). The pointer cursor already covers primary via the variant-agnostic `mouseCursor` at :268-269 — no cursor change needed.

In responsive_overlay.dart, the header "Buy GNUS" button (:277-284) is currently `variant: GWButtonVariant.secondary` (outline). Change it to `variant: GWButtonVariant.gradient` (filled), matching the coins-screen Buy GNUS. Leave size/label/onPressed unchanged.
  </action>
  <verify>
    <automated>flutter analyze lib/components/buttons/gw_button.dart lib/components/overlay/responsive_overlay.dart</automated>
    Also: `grep -n "GWButtonVariant.primary" lib/components/buttons/gw_button.dart` shows the primary case now sets `gradient: GeniusWalletGradient.brandCta`; `grep -n "GWButtonVariant.secondary" lib/components/overlay/responsive_overlay.dart` returns no match on the Buy GNUS button (now .gradient).
  </verify>
  <done>Primary variant renders the brandCta gradient with textOnBrand foreground and the same hover/press white wash as gradient; header Buy GNUS is a filled gradient button; flutter analyze clean (no new warnings/errors).</done>
</task>

<task type="auto">
  <name>Task 2: Nav active state — gradient underline + brandPrimaryStrong text/icon</name>
  <files>lib/components/overlay/responsive_overlay.dart</files>
  <action>
Desktop top nav (`_DesktopTopBar`):
- Active underline AnimatedContainer (:250-260): replace the solid `color:` in its BoxDecoration with a gradient. When `isSelected`, set `gradient: GeniusWalletGradient.brandCta` and leave `color` unset (null); when not selected, keep it flat transparent (`color: Colors.transparent`, no gradient). Keep height 1, the existing `width: hideLabels ? 20 : 60` logic, and `borderRadius: BorderRadius.circular(4)`. (A BoxDecoration must not set both a non-null color and a gradient — use one or the other per selected state.)
- Active text + icon color (:211-213, consumed by the icon at :239 and label at :245): change the `isSelected ? GeniusWalletColors.brandPrimary : gw.textSecondary` to use `GeniusWalletColors.brandPrimaryStrong` for the selected branch.

Mobile bottom nav (`_MobileTabBar` :148-156): change `selectedItemColor`, `selectedIconTheme.color`, and `selectedLabelStyle`'s color from `GeniusWalletColors.brandPrimary` to `GeniusWalletColors.brandPrimaryStrong`. Bottom nav has no underline — color only. Leave unselected colors and the surfaceSheen background untouched.

Ensure `GeniusWalletGradient` is imported (add `import 'package:genius_wallet/theme/genius_wallet_gradient.dart';` if not already present).
  </action>
  <verify>
    <automated>flutter analyze lib/components/overlay/responsive_overlay.dart</automated>
    Also: `grep -n "brandPrimaryStrong" lib/components/overlay/responsive_overlay.dart` shows the desktop active color and the three mobile selected tokens; `grep -n "GeniusWalletGradient.brandCta" lib/components/overlay/responsive_overlay.dart` shows the underline gradient.
  </verify>
  <done>Desktop active underline is the brandCta gradient (transparent when unselected, no both-color-and-gradient error); desktop active text/icon and all three mobile selected tokens are brandPrimaryStrong; flutter analyze clean.</done>
</task>

<task type="auto">
  <name>Task 3: theme.dart + per-widget highlight sites → solid brandPrimaryStrong</name>
  <files>lib/theme/theme.dart, lib/dashboard/home/widgets/transactions_slim_view.dart, lib/components/wallet_overview.dart, lib/account/account_dropdown_selector.dart, lib/account/sdk_account_manager.dart, lib/components/inputs/gw_switch.dart, lib/components/inputs/gw_text_field.dart, lib/components/inputs/gw_select.dart, lib/components/inputs/gw_checkbox.dart</files>
  <action>
Swap `GeniusWalletColors.brandPrimary` → `GeniusWalletColors.brandPrimaryStrong` at each enumerated highlight/selection site below. These are the ONLY sites — do not blanket-replace every brandPrimary in these files (leave cursor colors, enabled/plain borders, and date-picker range/today-border tints as-is).

theme.dart:
- :84 tabBarTheme `indicatorColor`.
- :102 datePickerTheme `headerBackgroundColor`; :110 the selected-state return inside `todayBackgroundColor`; :125 the selected-state return inside `dayBackgroundColor`. (Header + selected only — NOT the enabled/today borders or range tints.)
- :227 inputDecorationTheme `focusedBorder` borderSide color.
- :304 dropdownMenuTheme.inputDecorationTheme `focusedBorder` borderSide color.
- :317-325 navigationRailTheme `selectedLabelTextStyle.color` and `selectedIconTheme.color`.
- :339-343 bottomNavigationBarTheme `selectedItemColor` and `selectedIconTheme.color`.
- :361 checkboxTheme `fillColor` selected-state return.

Per-widget:
- transactions_slim_view.dart:84 SegmentedButton `selectedBackgroundColor`.
- wallet_overview.dart:140 ToggleButtons `fillColor`.
- account_dropdown_selector.dart:239 `selectedTileColor`; :390 avatar `backgroundColor`.
- sdk_account_manager.dart:180 the `accentColor` selected branch (drives selected text AND the :207 check_circle icon) and :191 the selected border color. Both swaps together cover the :180/:191/:207 sites the design lists.
- gw_switch.dart:36 `activeColor` and :37 `activeTrackColor` (`brandPrimaryStrong.withAlpha(140)`).
- gw_text_field.dart:117 `focusedBorder` (`_border(GeniusWalletColors.brandPrimaryStrong, width: 2)`). Leave :100 cursorColor.
- gw_select.dart:100 focused `_border(GeniusWalletColors.brandPrimaryStrong, width: 2)`.
- gw_checkbox.dart:43 border color selected branch and :55 checked fill selected-state return.

Reuse the existing `brandPrimaryStrong` token — no new hex, no alias.
  </action>
  <verify>
    <automated>flutter analyze lib/theme/theme.dart lib/dashboard/home/widgets/transactions_slim_view.dart lib/components/wallet_overview.dart lib/account/account_dropdown_selector.dart lib/account/sdk_account_manager.dart lib/components/inputs/gw_switch.dart lib/components/inputs/gw_text_field.dart lib/components/inputs/gw_select.dart lib/components/inputs/gw_checkbox.dart</automated>
    Also spot-check: `grep -n "brandPrimaryStrong" lib/theme/theme.dart` shows tabBar indicator, datePicker header+selected, both focused borders, nav rail selected, bottom nav selected, and checkbox fill.
  </verify>
  <done>All enumerated highlight/selection sites reference brandPrimaryStrong; non-enumerated brandPrimary usages (cursors, enabled/plain borders, date-picker range/today-border tints) untouched; flutter analyze clean across all nine files.</done>
</task>

</tasks>

<verification>
- `flutter analyze` clean across all edited files (no new warnings/errors).
- Grep audit: no `brandPrimary` (the flat #14C8FF neon) remains as a CTA fill or at any enumerated active/highlight site. Remaining brandPrimary references are only the deliberately-excluded ones (cursors, plain/enabled borders, secondary-outline, date-picker range/today-border tints, decorative widgets out of scope).
- No git commit performed (CLAUDE.md forbids it).
</verification>

<success_criteria>
- Every filled primary CTA app-wide + header Buy GNUS renders the brandCta gradient.
- Primary CTA hover/press shows the white brighten wash (matches gradient variant).
- Desktop nav active = gradient underline + brandPrimaryStrong text/icon; mobile nav selected = brandPrimaryStrong.
- All other enumerated highlight/selection sites = solid brandPrimaryStrong.
- Zero new tokens/deps/files; only token + decoration swaps.
- Light-mode contrast deferred (recorded), not addressed.
</success_criteria>

<output>
This is a quick-mode plan — no SUMMARY file required. Execution edits the files above and verifies with `flutter analyze`.
</output>
