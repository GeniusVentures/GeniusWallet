---
status: complete
phase: quick-260726-0z4
plan: 01
one_liner: Filed the control-track (surfaceSunken) recipe as a CONVENTIONS.md standard and moved Feedback/Swap titles to the page's left edge to match Transactions/Markets/News.
files_modified:
  - .planning/codebase/CONVENTIONS.md
  - lib/dashboard/home/view/dashboard_screen.dart
  - lib/dashboard/home/widgets/transactions_slim_view.dart
  - lib/logs/submit_logs_screen.dart
  - lib/squid_router/swap_screen.dart
commit: none (CLAUDE.md forbids commits; working tree left uncommitted)
duration: ~25 min
---

# Quick Task 260726-0z4 Summary

## A. Control-track standard filed

Added a new `### Control track (segmented control / filter bar)` subsection to
`.planning/codebase/CONVENTIONS.md`, inside the existing `## Theme & Design Tokens` section
(right before the closing `---` / date footer). It carries a `<!-- HAND-WRITTEN 2026-07-26 -->`
marker so a future `/gsd-map-codebase` regeneration knows to preserve it. Content: the recipe
table (surfaceSunken fill, hairline `borderSubtle` border, `radiusPill`, 3px track padding, 2px
chip gap), the measured dark-mode rationale (surfaceElevated `#0C0E14` / surfaceMenu `#171A21`
~1.11:1 above it / surfaceSunken `#06080C` ~1.10:1 below it - the hairline border, not the fill
step, is what makes it visible), the light-mode note (deferred to the app-wide light pass), and
the pairing rule naming both files by path.

Both in-code comments were de-trialed:
- `lib/dashboard/home/view/dashboard_screen.dart` (`_TimeframeSegment`, in the `BoxDecoration`)
  now cites `.planning/codebase/CONVENTIONS.md` ("Control track") instead of the dated
  `TRIAL 2026-07-26` marker. The hairline-border comment directly below was left untouched, as
  instructed.
- `lib/dashboard/home/widgets/transactions_slim_view.dart` (`_TransactionFilterBar`, in the
  `BoxDecoration`) got the matching treatment. The geometry-history comment at 601-606 was left
  untouched.

No token/color/geometry value changed in either file - comments only.

## B. Feedback and Swap titles moved to the page's left edge

### Feedback (`lib/logs/submit_logs_screen.dart`, `build()`)

Before: `Align(topCenter) > SingleChildScrollView(pad 16/32/16/16) >
ConstrainedBox(small=640) > Column(start, min)[GWPageHeader, GWCard]` - the whole 640 box was
centred, so the title rode to the window's middle.

After: `Align(topCenter) > SingleChildScrollView(pad 12/32/12/8) > ConstrainedBox(xxl=1536) >
Column(stretch, min)[GWPageHeader, Center(ConstrainedBox(small=640, child: GWCard(...)))]`. The
header now spans the page frame (left edge, matching Transactions/Markets/News); the card is
re-centred inside its own 640 cap by construction, so it did not move or resize. `_buildComposer`
and `_buildSuccess` were untouched. The no-AppBar comment above `build()` was updated from
"left-aligned in-body GWPageHeader" to "page-frame GWPageHeader (left edge, same X as the other
tabs)" to keep it accurate - the only touch to that comment's wording.

### Swap (`lib/squid_router/swap_screen.dart`, `_buildSwapContent()`)

Before: `Stack[sheen, Align(topCenter) > SingleChildScrollView > ConstrainedBox(560) >
Padding(h:10) > Column[SizedBox(32), GWPageHeader, cards-Stack, RouteDetailsCard?, error?,
SizedBox(12), CTA]]`.

After: `Stack[sheen, Align(topCenter) > SingleChildScrollView > Padding(12/32/12/8) >
ConstrainedBox(xxl=1536) > Column(stretch)[GWPageHeader, Center(ConstrainedBox(560,
Padding(h:10, Column[cards-Stack, RouteDetailsCard?, error?, SizedBox(12), CTA])))]]`. The old
`SizedBox(height: space32)` was deleted (replaced by the new Padding's top `space32`, so the
navbar-to-title gap is not doubled). The existing 560 `ConstrainedBox` and its `space10`
horizontal `Padding` were reused verbatim around the cards, not recreated - the cards' internal
geometry is unchanged. Added the missing import
`package:genius_wallet/utils/breakpoints.dart` (confirmed absent beforehand). The stale "header
and cards share one left edge" comment on the reused 560 Padding was rewritten to say the header
deliberately no longer shares that edge.

**Settings-icon decision:** kept at the page's top-right (the default/expected consequence named
in the plan), not the named fallback. `GWPageHeader`'s trailing `IconButton` (with its
`SwapSettingsDrawer.show(...)` `onPressed`, including the `slippage` `setState` callback) moved
up verbatim as part of the header - it was not touched or rebuilt.

## What a human should look at on hot reload (`r`)

- **Feedback tab:** "Send Feedback" title is hard against the page's left gutter, same X as
  "Transactions". Switch Feedback ↔ Transactions and watch the title - it should not jump
  sideways. The card below stays 640-capped, same position, composer still works.
- **Swap tab:** "Swap" title at the page's left gutter, same X as "Transactions", with its "Trade
  any token across chains" subtitle directly under it. Mesh/sheen background intact. The two
  amount cards + flip control unchanged position/width, still centred in the window (flip
  between Swap and Transactions to confirm). The tune icon is now at the page's top-right -
  click it to confirm the settings drawer opens and slippage still works. Scroll the page -
  nothing should clip top or bottom.
- **Dashboard timeframe segment / Transactions filter bar:** comment-only change, nothing should
  look different; both control tracks should still look identical to each other (this was already
  true before this task).

## Deviations from Plan

None - plan executed exactly as written. The only literal-text change beyond what the plan
specified was `dart format` re-wrapping the two restructured files' long lines/argument lists
(e.g. `SizedBox(height: GeniusWalletConsts.space8)` and the `emptyPlaceholder` ternary got
reformatted across multiple lines); this is automated formatting only, no logic or value changed,
and `flutter analyze lib` was re-checked afterward and still holds at 59.

## Verification

- `grep -c 'Control track' .planning/codebase/CONVENTIONS.md` → 1
- `grep -c 'CONVENTIONS.md' lib/dashboard/home/view/dashboard_screen.dart
  lib/dashboard/home/widgets/transactions_slim_view.dart` → 1 each
- `grep -rn 'TRIAL 2026' lib/` → no matches
- `flutter analyze lib` → **59 issues** (unchanged from baseline, checked after each task)
- `flutter test test/logs/submit_logs_feedback_test.dart test/squid_router/route_details_card_test.dart`
  → both pass (6 tests total)
- `git status --short` confirms no commit was created; only the five plan-listed files plus this
  SUMMARY are new/modified by this task. (The working tree also carries unrelated pre-existing
  changes from prior sessions - `lib/chart/crypto_simple_chart.dart`,
  `lib/theme/genius_wallet_decorations.dart`, `lib/web/web_view_mobile.dart`, the cmake files, new
  test files, and sketch/todo directories - none of these were touched by this task and are
  out of scope per the plan's file list.)

## Known Stubs

None introduced.

## Threat Flags

None - no new network endpoints, auth paths, file access, or schema changes. Matches the plan's
threat model (all dispositions `mitigate`/`accept`, no new trust boundary).

## Self-Check: PASSED

- FOUND: .planning/codebase/CONVENTIONS.md (Control track subsection present)
- FOUND: lib/dashboard/home/view/dashboard_screen.dart (de-trialed comment present)
- FOUND: lib/dashboard/home/widgets/transactions_slim_view.dart (de-trialed comment present)
- FOUND: lib/logs/submit_logs_screen.dart (restructured page frame present)
- FOUND: lib/squid_router/swap_screen.dart (restructured page frame + breakpoints import present)
- flutter analyze lib: 59 issues (confirmed)
- flutter test (both files): all passed (confirmed)
- No git commit created (confirmed via `git status --short`, working tree only)
