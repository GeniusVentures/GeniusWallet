---
phase: quick-260726-0z4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/CONVENTIONS.md
  - lib/dashboard/home/view/dashboard_screen.dart
  - lib/dashboard/home/widgets/transactions_slim_view.dart
  - lib/logs/submit_logs_screen.dart
  - lib/squid_router/swap_screen.dart
autonomous: true
requirements: [QUICK-260726-0z4]

must_haves:
  truths:
    - The control-track recipe (surfaceSunken + hairline border + radiusPill + 3px track padding + 2px chip gap) is written down in exactly ONE findable file, and both live tracks point at it by path.
    - Neither live track still carries a temporary/dated trial marker.
    - Feedback's "Send Feedback" title sits at the page's left edge, at the same X as the Transactions / Markets / News titles.
    - Swap's "Swap" title sits at the page's left edge; its subtitle still renders under it and the settings icon still opens SwapSettingsDrawer.
    - The Swap cards stay a 560-capped column centred in the window (D-07 / sketch 105 A1), and the Feedback card stays 640-capped. Neither widens, neither left-aligns.
    - flutter analyze lib stays at 59 issues.
  artifacts:
    - .planning/codebase/CONVENTIONS.md (new "Control track" subsection under "Theme & Design Tokens")
    - lib/logs/submit_logs_screen.dart (page frame restructured)
    - lib/squid_router/swap_screen.dart (page frame restructured)
  key_links:
    - GWPageHeader ends its title Row with a `Spacer()`, so `trailing:` is pinned to the RIGHT edge of whatever width the header is given. Moving the header out of Swap's 560 box moves the settings icon to the page's right edge. This is the one deliberate visual consequence of the change and it is what Task 3's human check is for.
    - The page Padding must sit OUTSIDE the ConstrainedBox, never inside it. A max-width only binds when the viewport is wider than it, so at phone width the cap does nothing and the gutter has to come from the Padding (the 06-01 walk finding on wallet_creation_screen.dart).
    - Wrapping the existing ConstrainedBox(560) in a `Center` hands it the same loose 0..560 constraints it gets today from `Align(topCenter) > SingleChildScrollView`. That is why the Swap cards' internal geometry is preserved by construction, not by luck.
---

<objective>
Two small, unrelated-but-adjacent jobs, both approved live this session:

**A.** Turn the just-approved `surfaceSunken` control-track treatment from two temporary
in-code trial comments into a written project standard with one findable home.

**B.** Move the page titles on Feedback and Swap to the page's left edge, matching
Transactions / Markets / News. The bug is not `GWPageHeader` - both screens already use it.
The header simply sits INSIDE a horizontally centred narrow column, so the title rides to
the middle of the screen.

Purpose: A stops the recipe being rediscovered (it already drifted once). B closes the last
two pages whose title does not land at the shared X.
Output: one new CONVENTIONS.md subsection, two de-trialled code comments, two restructured
page frames. No new files, no new widgets, no new tokens.
</objective>

<execution_context>
Working tree only. **Do NOT create commits** - CLAUDE.md forbids it and the user will stage
this themselves.

The macOS app is ALREADY RUNNING under `flutter run -d macos`. Verification is hot reload
(`r`, not `R` - see the hot-restart note in project memory) plus a human look, plus
`flutter analyze lib`. Do not plan or trigger a rebuild.

Baseline to hold: `flutter analyze lib` = **59 issues**. Any increase is a regression.
</execution_context>

<context>
@.planning/STATE.md

Reference page-frame implementations - READ THESE FIRST, they are the shape to copy:
@lib/dashboard/transactions/transactions_screen.dart
@lib/dashboard/chart/markets_screen.dart

Component being placed (note its trailing `Spacer()` and its own bottom gap):
@lib/components/scaffold/gw_page_header.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the control-track standard, and point both live tracks at it</name>
  <files>.planning/codebase/CONVENTIONS.md, lib/dashboard/home/view/dashboard_screen.dart, lib/dashboard/home/widgets/transactions_slim_view.dart</files>
  <action>
**Where the standard lives, and why.** Write it into `.planning/codebase/CONVENTIONS.md`, as a
new `### Control track (segmented control / filter bar)` subsection appended inside the
existing `## Theme & Design Tokens` section (currently ends at line 356, before the closing
`---` / date footer at 358-360). Justification, since the brief asked for one:

- There is NO `DESIGN_SYSTEM.md` in this repo. It exists only on the read-only reference
  branch `ui-redesign-3.514`, and an earlier decision already recorded its section 5.2 as
  stale. It is not a home.
- `CONVENTIONS.md` is already this project's single written answer to "how do we build
  things here", it is git-tracked, and it already owns a `## Theme & Design Tokens` section.
- It is the file GSD's own planner auto-loads for UI / frontend / component phases. A
  standard put here reaches future planning without anyone having to remember a path.
  A file under `.planning/reference/` would be durable but nothing ever opens it.
- Zero new files, which is the CLAUDE.md rule.

Known risk, stated rather than ignored: `/gsd-map-codebase` regenerates this file. Mitigate
with a hand-written marker comment immediately under the new heading, e.g.
`<!-- HAND-WRITTEN 2026-07-26. Not produced by /gsd-map-codebase. Preserve on regeneration. -->`
The two in-code comments remain the real enforcement point.

**Content of the new subsection.** A control track is the pill-shaped container holding a row
of small chips. Two exist today. State the recipe as a table so partial adoption is visibly
wrong: fill `gw.surfaceSunken`; border `Border.all(color: gw.borderSubtle)` (hairline);
radius `GeniusWalletConsts.radiusPill`; track padding `EdgeInsets.all(3)`; gap between chips
`SizedBox(width: 2)`.

Then the rationale, measured this session in dark mode, with the real hex values (all three
verified in `lib/theme/genius_wallet_colors.dart`): panel fill is `surfaceElevated` `#0C0E14`;
`surfaceMenu` `#171A21` sits 1.11:1 above it; `surfaceSunken` `#06080C` sits about 1.10:1
below it. The point to record is that the FILL is not what makes the control visible - the
hairline border is. Sunken was chosen so the panel stays the brightest plane and the controls
read as recessed wells rather than raised chips. Add one line that light mode's sunken step
(`#FFFFFF` panel vs `#CFD4DB` track) is a larger, clearly visible step, and that any
light-only follow-up belongs to the deferred app-wide light pass, not here.

Add the enabling context: this only became legible after `GWDecorations._surfaceSheenDark`
was flattened earlier this session from a `#181B24` to `#0C0E14` gradient into a flat
`#0C0E14`, matching the Swap tab's boxes.

Close with the pairing rule, which is the part that actually has teeth. Name both files by
path - `lib/dashboard/home/view/dashboard_screen.dart` (`_TimeframeSegment`) and
`lib/dashboard/home/widgets/transactions_slim_view.dart` (`_TransactionFilterBar`) - and state
that they are deliberately identical in geometry and CHANGE TOGETHER. They already drifted
once (pill vs radiusMd, 3 vs 4 padding) and read as two design languages on one screen;
timeframe is the approved shape (sketch 006/008), so the filter bar follows it, never the
reverse. That history is already written at transactions_slim_view.dart:601-606 - do not
duplicate it into the doc, summarise it in one sentence and leave the long version in the code.

**Then de-trial the two code comments.** Both currently open with a dated temporary marker
that must become a permanent standard note.
<!-- planner-discipline-allow: TRIAL -->

- `lib/dashboard/home/view/dashboard_screen.dart` lines 691-693, inside `_TimeframeSegment`'s
  `BoxDecoration`: replace the three trial-marker lines with a note that this is the
  control-track standard - surfaceSunken (recessed well), not surfaceMenu (raised chip) -
  citing `.planning/codebase/CONVENTIONS.md` "Control track" for the recipe and rationale, and
  keeping the existing "keep in sync with the filter track in transactions_slim_view.dart,
  the two are deliberately identical" sentence. Leave the following hairline-border comment
  (lines 695-696) untouched, it is still correct and still earns its place.
- `lib/dashboard/home/widgets/transactions_slim_view.dart` lines 610-611, inside
  `_TransactionFilterBar`'s `BoxDecoration`: same treatment - cite the CONVENTIONS.md section
  by path, keep the "see the matching note on _TimeframeSegment in dashboard_screen.dart,
  these two tracks change together" pointer. Do NOT touch the geometry comment at 601-606.

Comments only in both dart files. Do not change a single value: `gw.surfaceSunken`,
`Border.all(color: gw.borderSubtle)`, `radiusPill`, `EdgeInsets.all(3)` and the `SizedBox(width: 2)`
gaps all stay exactly as they are. Keep lines within the 80-char page width the formatter
enforces. Write " - " (space hyphen space), not an em dash, in any new prose.
  </action>
  <verify>
    <automated>grep -c 'Control track' .planning/codebase/CONVENTIONS.md</automated>
    <automated>grep -c 'CONVENTIONS.md' lib/dashboard/home/view/dashboard_screen.dart lib/dashboard/home/widgets/transactions_slim_view.dart</automated>
    <automated>flutter analyze lib 2>&1 | tail -3</automated>
  </verify>
  <done>
`grep -c 'Control track' .planning/codebase/CONVENTIONS.md` returns at least 1.
Both dart files return a count of at least 1 for `CONVENTIONS.md` (each comment cites the
standard's path).
`grep -rn 'TRIAL 2026' lib/dashboard/home/view/dashboard_screen.dart lib/dashboard/home/widgets/transactions_slim_view.dart`
returns nothing.
`flutter analyze lib` still reports 59 issues (comment-only dart edits cannot move it - if it
moved, something else was touched).
  </done>
</task>

<task type="auto">
  <name>Task 2: Feedback - lift the page header out of the 640 column</name>
  <files>lib/logs/submit_logs_screen.dart</files>
  <action>
Restructure the page frame in `build()` (lines 392-427) so the header sits at the page's left
edge while the card keeps its 640 cap.

Today: `Scaffold > Align(topCenter) > SingleChildScrollView(padding LTRB 16, space32, 16, 16)
> ConstrainedBox(maxWidth: GeniusBreakpoints.small) > Column(start, min)[ GWPageHeader,
GWCard ]`. Because the 640-cap box is centred in the window, the title is centred with it.

Target, copying `transactions_screen.dart:50-95` and `markets_screen.dart:71-87`:

- `SingleChildScrollView`'s `padding` becomes `EdgeInsets.fromLTRB(12, GeniusWalletConsts.space32, 12, 8)`.
  The 16 to 12 side change is not cosmetic drift - it is what puts this title on the SAME X as
  Transactions / Markets / News, which is a recorded Jakub decision (see the comment at
  transactions_screen.dart:68-77). The bottom 16 to 8 follows the same unified frame.
- The `ConstrainedBox` cap changes from `GeniusBreakpoints.small` to `GeniusBreakpoints.xxl`.
  This is now the PAGE frame, not the card frame. `breakpoints.dart` is already imported
  (line 14), so no new import.
- The `Column` keeps `mainAxisSize: MainAxisSize.min` but its `crossAxisAlignment` changes from
  `start` to `CrossAxisAlignment.stretch`, so the header takes the full capped width instead of
  shrink-wrapping and getting centred. This is exactly what transactions_screen.dart:96 does
  and its comment there explains why.
- `GWPageHeader(title: 'Send Feedback')` stays the FIRST child, unchanged, and still owns its
  own bottom gap - do not add a SizedBox after it.
- The `GWCard` gets wrapped: `Center(child: ConstrainedBox(constraints: const BoxConstraints(maxWidth: GeniusBreakpoints.small), child: GWCard(...)))`.
  The GWCard's own `padding` and `child` (the `isSuccess ? _buildSuccess(gw) : _buildComposer(gw, sdkReady)`
  ternary) are untouched.

Keep the existing no-AppBar comment at 393-395; update only the word "left-aligned" if it now
reads wrong. Do not touch `_buildComposer`, `_buildSuccess`, or anything below line 428.

Named fallback if the human check fails: if the card visibly SHIFTS horizontally (today it is
left-aligned inside a centred 640 box, so it could move if it shrink-wraps below 640), restore
the old inner box exactly by putting a `Column(crossAxisAlignment: CrossAxisAlignment.start,
mainAxisSize: MainAxisSize.min, children: [GWCard(...)])` inside the new
`Center > ConstrainedBox(640)`. Do that only if the shift is actually observed.
  </action>
  <verify>
    <automated>flutter analyze lib 2>&1 | tail -3</automated>
    <automated>flutter test test/logs/submit_logs_feedback_test.dart</automated>
    <human-check>Hot reload (`r`) in the running macOS app, open the Feedback tab. The "Send Feedback" title is hard against the page's left gutter, at the same X as the "Transactions" title on the neighbouring tab (switch back and forth and watch the title, it should not jump sideways). The vertical navbar-to-title gap is unchanged. The card below is still 640-wide max, still where it was, nothing clipped, and the composer still works.</human-check>
  </verify>
  <done>
`flutter analyze lib` = 59 issues.
`flutter test test/logs/submit_logs_feedback_test.dart` passes - it imports the screen file, so
it is also the cheap compile guard for this edit (it asserts pure logic, so a layout change
cannot legitimately break it; if it fails, the edit broke the file).
Human confirms the title is at the page's left edge and the card did not move.
  </done>
</task>

<task type="auto">
  <name>Task 3: Swap - lift the page header out of the 560 column, cards untouched</name>
  <files>lib/squid_router/swap_screen.dart</files>
  <action>
Same move as Task 2, applied to `_buildSwapContent()` (lines 515-668), with three things to
protect.

Today: `Scaffold > Stack[ Positioned.fill(_buildSheen()), Align(topCenter) >
SingleChildScrollView > ConstrainedBox(maxWidth: 560) > Padding(horizontal: space10) >
Column[ SizedBox(space32), GWPageHeader, Stack(cards + TokenFlipButton), RouteDetailsCard?,
route-error notice?, SizedBox(space12), _buildSwapCta ] ]`.

Target:

- `Stack` and `Positioned.fill(child: _buildSheen())` stay exactly as they are. Do not touch
  the sheen.
- Inside `Align(topCenter) > SingleChildScrollView`, insert a
  `Padding(padding: const EdgeInsets.fromLTRB(12, GeniusWalletConsts.space32, 12, 8))` as the
  scroll view's direct child, then a `ConstrainedBox(constraints: const BoxConstraints(maxWidth: GeniusBreakpoints.xxl))`,
  then a `Column(crossAxisAlignment: CrossAxisAlignment.stretch)`. This mirrors
  transactions_screen.dart:72-96. **Add the import** `package:genius_wallet/utils/breakpoints.dart` -
  this file does not have it yet (checked: not in the import block at lines 1-30).
- The `SizedBox(height: GeniusWalletConsts.space32)` at line 543 is DELETED. The new Padding's
  top `space32` replaces it, which is what unifies the navbar-to-title gap with the other tabs.
  Do not keep both or the title drops 64px.
- `GWPageHeader(title: "Swap", subtitle: ..., trailing: IconButton(...))` (lines 544-565) moves
  up to become the first child of the new outer Column, VERBATIM. The whole `trailing:`
  IconButton including its `SwapSettingsDrawer.show(...)` onPressed and its `setState` slippage
  callback must survive byte-identical. `gw` is already in scope at line 522, above the return.
- Everything from the cards `Stack` (line 573) down to `_buildSwapCta(gw)` (line 658) becomes:
  `Center(child: ConstrainedBox(constraints: const BoxConstraints(maxWidth: 560), child: Padding(padding: const EdgeInsets.symmetric(horizontal: GeniusWalletConsts.space10), child: Column(children: [ ...those children unchanged... ]))))`.
  The existing `ConstrainedBox(560)` and its `Padding(horizontal: space10)` are REUSED here, not
  recreated with different numbers, and the inner `Column` keeps its default
  `crossAxisAlignment` - do not add `stretch` to it.
- Keep the existing D-07 comment on the 560 constraint (line 532) with the box it belongs to.
  The "Page horizontal padding applied once at the column level so the header and the cards
  share one left edge" comment at 535-536 is now FALSE and must be rewritten: the header
  deliberately no longer shares the cards' left edge.

Why the cards are safe: today the 560 box receives loose `0..windowWidth` constraints from
`Align(topCenter) > SingleChildScrollView`. Under the new `Center` it receives loose
`0..(windowWidth - 24)`. In both cases the box resolves to the same `0..560` and its centre
lands on the same point (a 12px gutter on each side does not move the midpoint). The cards'
internal geometry is preserved by construction, not by inspection. Do NOT widen the 560, do
NOT left-align the cards, do NOT change `space10` - that column is approved design (sketch
105 A1 / D-07).

Expected and deliberate consequence: `GWPageHeader` ends its title Row with a `Spacer()`, so
the settings (tune) IconButton now rides to the PAGE's right edge instead of sitting beside the
560 column. That is the cheapest correct outcome - a page-level action at the page's top-right -
and it needs zero extra widgets. Named fallback if Jakub dislikes it on the walk: wrap only the
`GWPageHeader` in `ConstrainedBox(constraints: const BoxConstraints(maxWidth: 560))` inside an
`Align(alignment: Alignment.centerLeft, ...)`, which keeps the title at the page's left edge and
pulls the icon back to 560px in. Apply that only if asked.

Do not touch `_buildSwapCta`, `_buildRouteErrorNotice`, `_buildSheen`, `_flipTokens`,
`_debouncedFetchRoute`, or any route/quote logic.
  </action>
  <verify>
    <automated>flutter analyze lib 2>&1 | tail -3</automated>
    <automated>flutter test test/squid_router/route_details_card_test.dart</automated>
    <human-check>Hot reload (`r`), open the Swap tab. (1) The "Swap" title is at the page's left gutter, same X as the Transactions title, with its "Trade any token across chains" subtitle still directly under it. (2) The mesh/sheen background is intact behind everything. (3) The two amount cards and the flip control between them are exactly where they were, same width, still centred in the window - flip between Swap and Transactions and back to be sure. (4) Click the tune icon (now at the page's top-right): the swap settings drawer opens and the slippage control still works. (5) Scroll the page - nothing clips at the top or bottom. Judge point 4's icon POSITION explicitly: if the icon being at the far right reads wrong, say so, the fallback is already named in the plan.</human-check>
  </verify>
  <done>
`flutter analyze lib` = 59 issues.
`flutter test test/squid_router/route_details_card_test.dart` still passes (it golden-locks the
route card's four derived figures - proof this layout edit did not reach the quote path).
Human confirms all five points, in particular that the settings drawer still opens and the 560
card column did not move or resize.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds no input parsing, no network call, no storage write, no dependency, and no package-manager install. It edits one markdown doc, two code comments, and two widget trees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0z4-01 | Tampering | swap_screen.dart route/quote path | low | mitigate | Task 3 restricts edits to layout widgets and names every helper that must not be touched; `route_details_card_test.dart` re-run as the automated guard on the quote figures. |
| T-0z4-02 | Denial of Service | Swap settings drawer reachability | low | mitigate | The `trailing:` IconButton moves widget-tree position; Task 3 requires it verbatim and the human check clicks it to confirm the drawer still opens. |
| T-0z4-03 | Information Disclosure | submit_logs_screen.dart attachment/send path | low | accept | Only `build()`'s frame is touched; `_buildComposer` / `_buildSuccess` / the Sentry send path are explicitly out of scope and the existing logic test re-runs. |
| T-0z4-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages are installed by this plan. No legitimacy gate applies. |
</threat_model>

<verification>
1. `flutter analyze lib` reports **59 issues** after all three tasks. Not 60, not 58 with a
   different set - same 59.
2. `flutter test test/logs/submit_logs_feedback_test.dart test/squid_router/route_details_card_test.dart`
   both pass.
3. `grep -rn 'TRIAL 2026' lib/` returns nothing.
4. Human walk on the running macOS app after a hot reload: Transactions, Markets, News,
   Feedback, Swap. All five page titles land at the same X. Nothing else on any of the five
   pages moved.
5. No commit was created.

No new automated test is added. These are layout and documentation edits with no logic, and
the two existing tests already compile-guard both touched screens - CLAUDE.md asks for one
runnable check behind non-trivial logic, and there is none here.
</verification>

<success_criteria>
- The control-track recipe and its measured rationale are written in `.planning/codebase/CONVENTIONS.md`,
  once, with both live tracks named by path and the "they change together" rule stated.
- No dated trial marker remains anywhere in lib/.
- Feedback and Swap page titles sit at the page's left gutter, at the same X as Transactions /
  Markets / News.
- Swap's cards are still a 560-capped centred column, its sheen is intact, and its settings
  drawer still opens. Feedback's card is still 640-capped.
- `flutter analyze lib` = 59. Working tree left uncommitted.
</success_criteria>

<output>
Create `.planning/quick/260726-0z4-standard-toru-kontrolek-surfacesunken-ty/260726-0z4-SUMMARY.md`
when done. Record: where the standard was filed and why, the exact before/after page-frame
shape for both screens, the settings-icon position decision (kept at page right, or fallback
applied), and the analyze count.
</output>
