---
quick_id: 260728-q7c
date: 2026-07-28
sketches: [067-A, 156-A]
---

# Quick 260728-q7c - PLAN

Two sketch decisions that Jakub took together on 2026-07-27 and that only make sense together.
Sketch 067's own decision note says so: *"The two picks interlock, and not by accident: once the
panel becomes #0C0E14, a GWCard inside it measures 1.00:1 and is invisible. The no-card variant is
the only one that makes sense after 156-A."*

## 067-A - section grammar, kicker only

`swap_settings_drawer.dart`'s `'Slippage tolerance'` is a `Text` in `labelMd`/w600/`textPrimary`,
sentence case. It becomes `GWKicker('Slippage tolerance')` at the **default 13 step** (not `dense`).
No container, no rule: the gap does the grouping.

The label stays English. Sketch 067's HTML shows the description line in Polish; that is mockup
copy, not a spec for the app.

## 156-A - card canvas

| | Before | After |
|---|---|---|
| panel (3 paints in `responsive_drawer.dart`) | `surfaceMenu` #171A21 | `surfaceElevated` #0C0E14 |
| an input's fill | `surfaceMenu` (= its own panel, 1.00:1) | `surfaceMenu` (now the LIGHTER object) |
| an input's edge | `borderSubtle` 12%, 1.36:1 | a new `borderControl` 36%, **3.30:1** |

The edge value comes from sketch 067's contrast table, not from 156's text - 156 proposed white 30%,
which measures 2.63:1 on the new panel and does not clear 1.4.11.

## Knock-ons to check before calling it done

Sketch 156 named three. All four drawers below were read, not assumed:

1. `token_selector_drawer.dart` - selection tint AND the search field's own fill.
2. `GWFocusRing`'s four call sites - which are in drawers and which are not.
3. the `brandPrimarySubtle` header hairline, tuned against #171A21.
4. **anything else in a drawer painted `surfaceElevated`**, which is the case 156 did not enumerate
   and the one that actually bites: a row painted the panel's own new colour disappears.

## Gates

`flutter analyze lib` at baseline 59; `flutter test` at baseline 332/1 plus whatever this adds.
**No commit** - `CLAUDE.md`.
