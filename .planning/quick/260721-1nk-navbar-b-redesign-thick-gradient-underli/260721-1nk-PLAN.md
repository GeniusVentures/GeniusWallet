---
phase: quick-260721-1nk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/components/overlay/responsive_overlay.dart
  - lib/theme/genius_wallet_consts.dart
  - lib/reown/reown_connect_button.dart
  - lib/dashboard/chart/dashboard_markets.dart
  - lib/chart/crypto_simple_chart.dart
  - lib/components/buttons/gw_button.dart
autonomous: true
requirements: [NAV-B, MKT-A, CTA-SECONDARY]
user_setup: []

must_haves:
  truths:
    - "The active desktop nav item shows a 3px gradient bar (brandCta) with a soft brandPrimaryStrong glow flush at the item's bottom edge — the gradient is actually visible, unlike the old 1px line"
    - "Desktop nav icons render at 23px and the top bar breathes at ~68px tall"
    - "The 'Connect' button in the top bar is a ghost/outline treatment (transparent fill + brandPrimaryStrong border + brandPrimaryStrong text), NOT flat neon brandPrimary — and its connect/disconnect onTap logic is unchanged"
    - "The Markets panel shows a real 'Markets' header row (titleLg / textPrimary) above the rows, not a title crammed into the first list item"
    - "Each Markets row mirrors the Assets CoinCardRow rhythm: 34px icon, coin NAME as primary (titleMd/textPrimary), price as secondary (bodySm/textSecondary), and a filled % chip (status tint + status fg) beside the sparkline"
    - "Markets up/down colors and the sparkline stroke use gw.statusSuccess/gw.statusError — the legacy mutedGreen/Colors.red/Colors.grey are gone from this row"
    - "Every GWButtonVariant.secondary CTA app-wide (settings, add-wallet/account, transaction displays, empty states) renders a deep-cyan brandPrimaryStrong outline + text, not brandPrimary neon — and stays an outline, not a gradient"
  artifacts:
    - lib/components/overlay/responsive_overlay.dart
    - lib/reown/reown_connect_button.dart
    - lib/dashboard/chart/dashboard_markets.dart
    - lib/chart/crypto_simple_chart.dart
    - lib/components/buttons/gw_button.dart
  key_links:
    - "_DesktopTopBar active indicator AnimatedContainer to GeniusWalletGradient.brandCta plus BoxShadow(brandPrimaryStrong at 0.5, blur 10)"
    - "CryptoSparkLineChart priceColor and sparkline stroke to gw.statusSuccess/gw.statusError (read from Theme extension)"
    - "gw_button.dart GWButtonVariant.secondary palette to GeniusWalletColors.brandPrimaryStrong (one edit, propagates app-wide)"
    - "DashboardMarkets header rendered inside FutureStateWidget.onData (visibleCoins.length available there); existing bounded ListView.separated stays bounded via Column plus Expanded"

light_mode_deferral: "Dark-first per locked design. brandPrimaryStrong (#0AAEE6) outline/text and status-tint chips may not hit WCAG AA on a light base; the status tokens are already appearance-aware (gw.statusSuccess/Error) so they adapt, but the ghost-Connect border/text and the 3px gradient bar are dark-tuned. Any light-mode contrast is DEFERRED to the light pass — note, do not solve here."
---

<objective>
Implement three approved, locked designs on the desktop wallet chrome:

- **A — Navbar "variant B"** (sketch 002-B): thick 3px gradient underline with glow, bigger 23px icons, roomier ~68px bar, and a ghost/outline "Connect" button consistent with the new accent.
- **B — Markets "variant A"** (sketch 003-A): a real "Markets" panel header plus rows that mirror the Assets `CoinCardRow` (34px icon, name-over-price, sparkline + filled % chip, status green/red).
- **C — Fix `GWButtonVariant.secondary`**: one central palette edit swapping brandPrimary to brandPrimaryStrong so every outline CTA app-wide reads as the calmer deep-cyan accent.

Purpose: kill the last flat-neon `brandPrimary` outline (C) and land the two approved chrome redesigns (A, B) so the top bar and Markets panel match the sketched design language and the already-shipped Assets panel.

Output: 6 edited files; no new deps; `flutter analyze` clean. Note: `CryptoSparkLineChart` (edited in Task B) is shared by TWO live surfaces — the dashboard Markets panel (`dashboard_markets.dart`) AND the full-page `/markets` route (`markets_screen.dart:186`, each row wrapped in a `Clip.hardEdge` `GWDecorations.surface(radiusMd)` card, passing `iconSize: 32`). The row redesign SHOULD propagate to `/markets` too (a consistent Markets row everywhere is desired), so this change is NOT confined to the dashboard panel — `/markets` is a required walk surface (see verification). Dark-first — light-mode contrast for the new accent is deferred to the light pass (recorded in front-matter).
</objective>

<context>
@.planning/STATE.md
@.planning/sketches/002-top-navbar/index.html
@.planning/sketches/003-markets-panel/index.html
@lib/components/coins/view/coin_card_row.dart
@lib/components/coins/view/coins_screen.dart
@lib/components/overlay/responsive_overlay.dart
@lib/chart/crypto_simple_chart.dart
@lib/dashboard/chart/dashboard_markets.dart

Tokens (already defined, reuse — do NOT add): `GeniusWalletColors.brandPrimaryStrong` (#0AAEE6), `GeniusWalletColors.gradientBlue`, `GeniusWalletGradient.brandCta`, `gw.statusSuccess`/`gw.statusError` (appearance-aware via GWColors extension), `GeniusWalletTypography.titleLg`/`titleMd`/`bodySm`/`labelMd`, `gw.textPrimary`/`textSecondary`/`borderSubtle`/`surfaceElevated`/`surfaceMenu`.

Constraints (CLAUDE.md): LAZY — reuse GWButton, existing tokens, `buildTokenIcon`, the fl_chart sparkline already in `crypto_simple_chart.dart`. No new deps. Central edits over per-site (C = one edit). NO git commits (CLAUDE.md gate) — verify is `flutter analyze` + greps only. Do NOT add a hollow test; these are visual edits and analyze is the check.

Do NOT touch: the Assets panel (`coins_screen.dart` CoinCardRow rows, `coin_card_row.dart` — already done), `/banxa`, `/squidrouter`, `*.g.dart`. Do NOT regress the 0ze brand sweep (gradient CTAs, brandPrimaryStrong highlights) or the vwj Assets work.
</context>

<tasks>

<task type="auto">
  <name>Task A: Navbar "variant B" — thick gradient underline, bigger icons, roomier bar, ghost Connect</name>
  <files>lib/components/overlay/responsive_overlay.dart, lib/theme/genius_wallet_consts.dart, lib/reown/reown_connect_button.dart</files>
  <action>
In `responsive_overlay.dart` `_DesktopTopBar`:
- Bump the nav icon size: change `_kIconSize` (currently 16.0 at :112) to 23.0 to match sketch 002-B (23px icons).
- Give the bar room: raise `GeniusWalletConsts.appBarHeight` (genius_wallet_consts.dart:14) from 60 to 68. This is the single shared token both `_DesktopTopBar` (preferredSize + SizedBox) and `dev_tools_bubble.dart` `_headerHeight` read; the dev bubble anchor follows the taller bar, which is correct. Only these two desktop consumers read it (verified; the mobile top bar does not).
- Replace the invisible active underline (the `AnimatedContainer` at ~:251-267 that today is `height: 1`) with a 3px gradient bar per 002-B: `height: 3`, `borderRadius: const BorderRadius.vertical(top: Radius.circular(3))` (rounded top corners, flush at bottom), selected uses `gradient: GeniusWalletGradient.brandCta` with `boxShadow: [BoxShadow(color: GeniusWalletColors.brandPrimaryStrong.withValues(alpha: 0.5), blurRadius: 10)]` for the soft glow; unselected uses `color: Colors.transparent`, no gradient, no shadow (a BoxDecoration cannot carry both color and gradient — keep the existing one-per-state guard). Keep the existing `AnimatedContainer` duration and the `hideLabels ? 20 : 60` width.
- Active label/icon color: keep `GeniusWalletColors.brandPrimaryStrong` as-is (already set at :212-214) — reads well against the new bar and matches the 0ze sweep; leave idle = `gw.textSecondary`. (002-B's mockup uses textPrimary for the label; brandPrimaryStrong is the approved equivalent under the locked design — do not regress the 0ze active-accent choice.)

In `reown_connect_button.dart` (the idle "Connect" `else` branch, :537-543): restyle ONLY the idle visual branch to the ghost/outline treatment from 002-B: set `iconColor` and `textColor` to `GeniusWalletColors.brandPrimaryStrong`, set `backgroundColor` to `Colors.transparent`, and add a `brandPrimaryStrong` 1.5px outline via the button style `side` at :546 (`TextButton.styleFrom(backgroundColor: backgroundColor, side: const BorderSide(color: GeniusWalletColors.brandPrimaryStrong, width: 1.5))`). Leave the connected/connecting/timedOut/error branches (their status colors + backgrounds) untouched, and DO NOT change `onPressed`, `_connect`, `_disconnect`, or any connection logic — visual only.

Leave "Buy GNUS" (already `GWButtonVariant.gradient`) and the network/account chips as-is — the chips already read `gw.borderSubtle`/`surfaceMenu` per the eu9 sweep; do not change their behavior.
  </action>
  <verify>
    <automated>flutter analyze lib/components/overlay/responsive_overlay.dart lib/theme/genius_wallet_consts.dart lib/reown/reown_connect_button.dart 2>&1 | tail -5 && grep -q "blurRadius: 10" lib/components/overlay/responsive_overlay.dart && grep -q "Radius.circular(3)" lib/components/overlay/responsive_overlay.dart && grep -q "_kIconSize = 23" lib/components/overlay/responsive_overlay.dart && grep -q "appBarHeight = 68" lib/theme/genius_wallet_consts.dart && grep -q "brandPrimaryStrong" lib/reown/reown_connect_button.dart</automated>
  </verify>
  <done>flutter analyze clean for the three files; active nav item paints a 3px brandCta gradient bar with glow; icons at 23px; bar at 68px; Connect renders as a brandPrimaryStrong ghost outline with its connection logic intact.</done>
</task>

<task type="auto">
  <name>Task B: Markets "variant A" — real panel header + Assets-mirror rows (status colors, % chip, 34px icon)</name>
  <files>lib/dashboard/chart/dashboard_markets.dart, lib/chart/crypto_simple_chart.dart</files>
  <action>
In `crypto_simple_chart.dart` `CryptoSparkLineChart` — mirror the Assets `CoinCardRow` rhythm (reference: lib/components/coins/view/coin_card_row.dart):
- Retire the legacy color statics: replace `priceColor` (built from `_mutedGreen`/`Colors.red`/`Colors.grey[500]`) with a Theme-driven `changeColor` computed in `build()` from the `gw` extension: `priceChangePercent >= 0 ? gw.statusSuccess : gw.statusError`. Remove the `_mutedGreen`/`_mutedRed` statics and the `priceColor` getter (delete the legacy imports/refs; keep `GeniusWalletColors` import only if still used).
- Default `iconSize` to 34 (change the constructor default `this.iconSize = 28` to `34`) so `buildTokenIcon` renders the Assets-matching 34px icon. Keep `buildTokenIcon`.
- Swap the text hierarchy so NAME is primary: `title` (coin name) becomes `GeniusWalletTypography.titleMd.copyWith(color: gw.textPrimary)` (was 16px gw.textSecondary); `subtitle` (price) becomes `GeniusWalletTypography.bodySm.copyWith(color: gw.textSecondary)` (was 14px gw.textPrimary). Keep the `AutoSizeText`/`Text` widgets and the existing `formattedPrice`.
- Trailing: keep the sparkline + %, but make the % a FILLED CHIP like Assets — wrap the % `Text` in a `Container(padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 2), decoration: BoxDecoration(color: changeColor.withValues(alpha: 0.15), borderRadius: BorderRadius.circular(8)), child: Text(..., style: GeniusWalletTypography.labelMd.copyWith(color: changeColor, fontWeight: FontWeight.w600)))`. Keep the `+`/sign formatting. Resize the sparkline `SizedBox` to `width: 56, height: 20` (was 80x15) per 003-A, and set both the `LineChartBarData.color` to `changeColor`. MANDATORY — bump the trailing `SizedBox(height: 44)` to `height: 50`: the new filled % chip (labelMd 13/18 + 2+2 vertical pad ≈ 22px) + `spacing: 5` + the 20px sparkline ≈ 47px, which RenderFlex-overflows the old 44px trailing box by ~3px (there is a pending todo about overflow in exactly this chart area — 2026-07-20-dashboard-live-chart-overflows-by-6px). 50px provably fits (22 + 5 + 20 = 47 ≤ 50). Do not leave the height at 44 and do not defer this to a guess.
- Keep the fl_chart `LineChart` config, `getSparklineChartData()`, `onTap`, and `ListTile` structure otherwise intact. No new deps.

In `dashboard_markets.dart` — promote the title to a REAL header row (matching the Assets header treatment in coins_screen.dart :252-301) instead of cramming it into the first list item:
- Delete the `if (index == 0 && widget.title != null)` special-case Column wrapper (:82-97); `itemBuilder` returns just `item` for every row.
- Inside `onData`, render the header ABOVE the list: return a `Column(children: [ header, Expanded(ListView.separated(...)) ])`. The header is a `Padding(padding: const EdgeInsets.fromLTRB(GeniusWalletConsts.space4, 0, GeniusWalletConsts.space4, GeniusWalletConsts.space8), child: Row(mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [Text(widget.title ?? 'Markets', style: GeniusWalletTypography.titleLg.copyWith(color: gw.textPrimary)), Text('Top ${visibleCoins.length} · 24h', style: GeniusWalletTypography.bodySm.copyWith(color: gw.textSecondary))]))` — the quiet "Top {n} · 24h" subtitle uses data already on hand (`visibleCoins.length`), no new fetch. Wrapping the existing `ListView.separated` in `Expanded` preserves its current bounded-height contract (the panel already gives it bounded height today). Add the `GeniusWalletConsts` import if missing.
- Keep the `Container(height: 1, color: gw.borderSubtle)` separator (already an Assets-matching 1px borderSubtle divider) and the market-data source, tap/navigation, and fl_chart sparkline unchanged.

SECOND CONSUMER (awareness — no proactive edit needed if the row change is self-contained): `lib/dashboard/chart/markets_screen.dart:186` renders the SAME `CryptoSparkLineChart` for the full-page `/markets` route, wrapping each row in `Container(clipBehavior: Clip.hardEdge, decoration: GWDecorations.surface(radius: radiusMd))` and passing `iconSize: 32` (so it keeps a 32px icon; the new default 34 only affects the dashboard panel). The taller trailing box (50px) + resized sparkline + filled chip inherit here automatically — which is the intended, consistent Markets row. You do NOT need to touch `markets_screen.dart` UNLESS the redesigned row clips or RenderFlex-overflows inside that per-row `Clip.hardEdge` `GWDecorations.surface(radiusMd)` card (see the `/markets` walk item in verification). If it does clip there, fix it at the source — bump the card's internal padding or the row trailing height — rather than leaving it clipped.
  </action>
  <verify>
    <automated>flutter analyze lib/dashboard/chart/dashboard_markets.dart lib/chart/crypto_simple_chart.dart 2>&1 | tail -5 && grep -q "statusSuccess" lib/chart/crypto_simple_chart.dart && grep -q "titleMd" lib/chart/crypto_simple_chart.dart && ! grep -qE "mutedGreen|Colors.red|priceColor" lib/chart/crypto_simple_chart.dart && grep -q "titleLg" lib/dashboard/chart/dashboard_markets.dart && grep -q "Expanded" lib/dashboard/chart/dashboard_markets.dart && ! grep -q "index == 0 && widget.title" lib/dashboard/chart/dashboard_markets.dart</automated>
  </verify>
  <done>flutter analyze clean for both files; Markets panel shows a "Markets" header (titleLg) with a "Top {n} · 24h" subline; rows read as Assets twins (34px icon, name-over-price, filled % chip + sparkline) using status green/red; legacy mutedGreen/red/grey are gone.</done>
</task>

<task type="auto">
  <name>Task C: Fix GWButtonVariant.secondary — one central palette edit to brandPrimaryStrong</name>
  <files>lib/components/buttons/gw_button.dart</files>
  <action>
In `gw_button.dart` `_palette()` `case GWButtonVariant.secondary:` (:120-128): change BOTH the `foreground` and the `border` color from `GeniusWalletColors.brandPrimary` to `GeniusWalletColors.brandPrimaryStrong`. Keep `background: Colors.transparent`, keep the `BorderSide(width: 1.5)`. Do NOT convert to a gradient — it stays a clean deep-cyan outline. This single edit fixes every secondary/outline CTA app-wide (settings, sdk_account_manager Add-Wallet/Account, transaction_displays, empty states).
  </action>
  <verify>
    <automated>flutter analyze lib/components/buttons/gw_button.dart 2>&1 | tail -5 && sed -n '120,128p' lib/components/buttons/gw_button.dart | grep -q "brandPrimaryStrong" && ! sed -n '120,128p' lib/components/buttons/gw_button.dart | grep -qE "brandPrimary[^S]"</automated>
  </verify>
  <done>flutter analyze clean; the secondary palette's foreground and border are brandPrimaryStrong; it remains a transparent outline (no gradient); no lingering bare brandPrimary in that case.</done>
</task>

</tasks>

<threat_model>
Not applicable — visual-only redesign of existing widgets. No new trust boundaries, no user input handling, no new dependencies, no package installs. Connect's connection logic is explicitly left untouched (only its idle visual style changes), so no auth/session surface is modified.
</threat_model>

<verification>
- `flutter analyze` is clean across all 6 touched files (no new warnings/errors; 2 pre-existing info deprecations from the 0ze sweep are acceptable and unrelated).
- No git commits (CLAUDE.md gate). No new dependencies added to pubspec.yaml.
- Grep gates in each task's `<automated>` confirm the token swaps and structural changes landed.
- Human dark-mode walk (blocking, performed by the user on a `GW_DEV_TOOLS=true` build): (1) active nav item shows a visible 3px gradient bar with glow; icons bigger; bar roomier; Connect is a cyan ghost outline and still connects/disconnects. (2) Markets panel has a real "Markets" header and Assets-twin rows with status-colored chips + sparklines. (3) A `secondary` CTA (e.g. settings or Add Wallet) reads deep-cyan outline, not neon. (4) `/markets` FULL PAGE (second consumer of the shared `CryptoSparkLineChart`): open the full-page Markets route and confirm the new taller %-chip + resized sparkline do NOT clip or RenderFlex-overflow inside each per-row `Clip.hardEdge` `GWDecorations.surface(radiusMd)` card. If any row clips there, the executor must adjust the card's internal padding or the row trailing height at the source (do not leave it clipped).
- Light-mode contrast for the new brandPrimaryStrong accent is DEFERRED to the light pass (front-matter `light_mode_deferral`), not verified here.
</verification>

<success_criteria>
All three locked designs land dark-first: navbar variant B (3px gradient underline + glow, 23px icons, 68px bar, ghost Connect), Markets variant A (real header + Assets-mirror rows with status colors and filled % chip), and the central `secondary` CTA fix to brandPrimaryStrong. `flutter analyze` clean, no new deps, no commits. Connect's connection logic unchanged. Assets panel, 0ze sweep, and vwj work not regressed.
</success_criteria>

<output>
No SUMMARY required (quick task). On completion, update `.planning/STATE.md` Quick Tasks Completed with a 260721-1nk row (WALK PENDING — dark walk covers navbar + Markets panel + the `/markets` full page, since the shared row change propagates there; uncommitted per CLAUDE.md gate) and record the light-mode deferral.
</output>