---
quick_id: 260720-eu9
type: execute
autonomous: false
files_modified:
  - lib/theme/genius_wallet_colors.dart
  - lib/network/network_dropdown_selector.dart
  - lib/account/account_dropdown_selector.dart
  - lib/components/sgnus/sgnus_connection_widget.dart
  - lib/reown/reown_connect_button.dart
  - lib/components/job/submit_job_dashboard_button.dart
  - lib/components/overlay/responsive_overlay.dart
must_haves:
  truths:
    - Every bare TextButton in the app renders a legible fill + ink in light mode and keeps its ~#132135 dark chip in dark mode
    - Header controls (network selector, wallet/account triggers, GNUS/SGNUS connection, Submit-Job) read clearly in BOTH modes
    - No control listed in this plan ships a raw Colors.* or greenAccent that fails to flip with appearance
    - SubmitJobDashboardButton and "Buy GNUS" are branded GWButtons with identical onPressed behavior
  artifacts:
    - lib/theme/genius_wallet_colors.dart (btnFilter appearance-aware)
  key_links:
    - genius_wallet_colors.btnFilter getter → theme.dart textButtonTheme.backgroundColor (line 258) → every bare TextButton app-wide
---

<objective>
Fix light-mode legibility of the header + current-balance controls. One shared root cause (theme.dart's global `textButtonTheme.backgroundColor = GeniusWalletColors.btnFilter`, a fixed dark-navy const that never flips) plus a set of per-widget hardcoded `Colors.*` / `greenAccent` values that never adopt the appearance-aware token system. Also convert two CTAs to branded GWButtons.

Purpose: In light mode every bare TextButton currently paints a dark-navy fill with near-black ink = illegible; the header action widgets stay dark-on-light and some run off-screen. This is a re-skin regression tracked in `.planning/todos/pending/2026-07-20-top-bar-action-widgets-render-black-in-light-mode.md`.

Output: btnFilter made appearance-aware (dark preserved, light chip added) so ALL btnFilter consumers flip app-wide; five per-widget token migrations; two branded CTAs. Behavior/structure of every widget unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
Re-skin only — keep every widget's behavior, structure, callbacks, and layout. This project enforces WCAG AA in BOTH modes / all states.

Token facts (verified in source):
- `GeniusWalletColors.btnFilter` (genius_wallet_colors.dart:27) is the ONLY consumer-facing value; its single read is theme.dart:258 inside a `resolveWith` closure (NOT a const context), so converting it from `static const` to a `static Color get` getter is safe and compiles. No other `btnFilter` reference exists in lib/.
- Appearance-aware getter pattern already present in this file: `static bool get _isLight => GWAppearance.isLight;` (line 41); `textPrimary`, `surfaceElevated`, `surfaceMenu`, `borderSubtle` etc. all follow `_isLight ? light : dark`. MATCH this pattern.
- `textPrimary` (the TextButton foreground, theme.dart:260-265) ALREADY flips (ink #10131A light / white dark) — confirmed. Only the background is broken.
- `GWColors` ThemeExtension exposes appearance-aware `surfaceElevated`, `textPrimary`, `textSecondary`, `borderSubtle` via `Theme.of(context).extension<GWColors>() ?? GWColors.dark()`. Mode-invariant status tokens: `GeniusWalletColors.statusError` (#FF4D4D), `statusWarning` (#FFC42E).
- `GWButton` variants: primary (brand fill, textOnBrand fg, WCAG-safe), secondary (outlined brand), gradient (hero CTA), plus `.leading` slot and `GWButtonSize` sm/md/lg.

Do NOT touch: the GNUS/Minions ToggleButtons (already §3.1-safe), the QrImageView `backgroundColor: Colors.white` (QR codes require a white quiet zone — mode-invariant by design).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root theme fix — make btnFilter appearance-aware</name>
  <files>lib/theme/genius_wallet_colors.dart</files>
  <action>
Convert `static const Color btnFilter = Color.fromARGB(255, 19, 33, 53);` (line 27) into an appearance-aware static getter matching the existing `_isLight` getter pattern in this file. The DARK branch MUST return the exact current value `const Color.fromARGB(255, 19, 33, 53)` (preserve the dark look precisely — do not substitute a different dark token). The LIGHT branch returns a legible light chip consistent with this file's light surface ladder — use the surfaceMenu light value `const Color(0xFFEFF2F6)` so a filled chip reads subtly on the light page and the already-flipping `textPrimary` ink (#10131A) sits on it at ~15:1 (WCAG AA pass). Result shape: `static Color get btnFilter => _isLight ? const Color(0xFFEFF2F6) : const Color.fromARGB(255, 19, 33, 53);`. Add a short comment noting this is the app-wide root fix for bare-TextButton light-mode legibility (theme.dart:258 is the sole consumer). Do NOT alter `btnFilterSelected` or any other token. This single change flips every bare TextButton app-wide because theme.dart re-runs `getThemeData()` per appearance toggle.
  </action>
  <verify>
    <automated>flutter analyze lib/theme/genius_wallet_colors.dart lib/theme/theme.dart</automated>
  </verify>
  <done>btnFilter is a static getter; dark returns ARGB(255,19,33,53) unchanged, light returns 0xFFEFF2F6; analyze reports 0 errors on both files.</done>
</task>

<task type="auto">
  <name>Task 2: Header controls — migrate hardcoded colors to appearance-aware / status tokens</name>
  <files>lib/network/network_dropdown_selector.dart, lib/account/account_dropdown_selector.dart, lib/components/sgnus/sgnus_connection_widget.dart, lib/reown/reown_connect_button.dart</files>
  <action>
Re-skin only; keep all callbacks, structure, and state logic. Each control below sits in the desktop top bar / header and must read in both modes.

network_dropdown_selector.dart: the empty-state text at ~line 124 uses `TextStyle(color: Colors.white70)`. Read `Theme.of(context).extension<GWColors>() ?? GWColors.dark()` in `build()` and set the empty-state color to `gw.textSecondary`. (The visible dropdown TextButton itself now inherits the fixed btnFilter chip from Task 1 — no change needed there.)

account_dropdown_selector.dart: the Delete menu item at ~lines 366-375 hardcodes `Colors.redAccent` twice (leadingIcon color and the label `TextStyle`). Replace BOTH with `GeniusWalletColors.statusError` (already imported; mode-invariant, WCAG-safe on the surfaceMenu bg in both modes). Leave the adjacent Rename/Copy items (they already use `gw.textPrimary`).

sgnus_connection_widget.dart: (a) the `SGNUSConnectionStatusWidget` AutoSizeText at ~line 137-141 hardcodes `color: isProcessing ? Colors.white : Colors.white70` — invisible on the light hero. Read `gw` from the GWColors extension in that `build()` and change to `isProcessing ? gw.textPrimary : gw.textSecondary`. (b) the CircularProgressIndicator spinner at ~line 83-87 uses `color: Colors.greenAccent` — change to `GeniusWalletColors.brandPrimary` (matches the app's progressIndicatorTheme; mode-invariant brand accent). Add the `gw_colors.dart` and `genius_wallet_colors.dart` imports as needed.

reown_connect_button.dart: this is the biggest offender. In `build()` (lines ~500-560), the idle branch (line ~530-536) sets `iconColor = Colors.greenAccent; textColor = Colors.white; backgroundColor = GeniusWalletColors.deepBlueCardColor` — none flip. Read `gw` from the GWColors extension at the top of `build()` and migrate: idle `backgroundColor → gw.surfaceElevated`, idle `textColor → gw.textPrimary`, idle `iconColor → GeniusWalletColors.brandPrimary`. For the status states (connected/connecting/timedOut/hasError) replace the raw `Colors.redAccent / Colors.amber / Colors.orange`: use `GeniusWalletColors.statusError` for connected/disconnect + error states and `GeniusWalletColors.statusWarning` for connecting + timed-out; set `iconColor` and `textColor` to that status token, and the `backgroundColor` tint to the SAME token at a STRONGER alpha than the current 0.1 so it reads on a light surface — use `.withValues(alpha: 0.18)`. Also migrate the surviving raw values in the dialog/snackbars: the AlertDialog `backgroundColor: GeniusWalletColors.deepBlueTertiary` (line ~249) → `gw.surfaceElevated`; the two dialog accent icons `Icons.paste` (line ~302) and `Icons.link` (line ~360) currently `GeniusWalletColors.lightGreenPrimary` (= greenAccent, illegible once the dialog bg flips to white) → `GeniusWalletColors.brandPrimary`; every `showAppSnackBar(..., backgroundColor: Colors.red)` (lines ~159, ~230, ~434) → `GeniusWalletColors.statusError`. Leave the `QrImageView` white background as-is. Keep the `_didManualPair`/`_isConnecting` logic untouched.
  </action>
  <verify>
    <automated>flutter analyze lib/network/network_dropdown_selector.dart lib/account/account_dropdown_selector.dart lib/components/sgnus/sgnus_connection_widget.dart lib/reown/reown_connect_button.dart</automated>
  </verify>
  <done>No raw `Colors.white`, `Colors.white70`, `Colors.red*`, `Colors.amber`, `Colors.orange`, or `greenAccent` survives on these four controls (except the intentional QR white); status states use statusError/statusWarning with a ≥0.18 tint; idle/text/surface read from GWColors; analyze reports 0 errors.</done>
</task>

<task type="auto">
  <name>Task 3: Balance-area branded CTAs — GWButton conversions</name>
  <files>lib/components/job/submit_job_dashboard_button.dart, lib/components/overlay/responsive_overlay.dart</files>
  <action>
submit_job_dashboard_button.dart: replace the `TextButton.icon` (lines ~28-36) with `GWButton(variant: GWButtonVariant.primary, size: GWButtonSize.md, label: 'Create Processing Job', leading: const Icon(Icons.create), onPressed: () async { onPressed?.call(); await context.push('/submit_job'); walletDetailsCubit.getCoins(); })`. Drop the `greenAccent` icon color entirely — GWButton.primary supplies the WCAG-safe `textOnBrand` foreground for both the label and the leading icon via its IconTheme. Keep the `isSelectedWalletLinkedToSGNUS` early-return guard and the exact onPressed body. Add the `import 'package:genius_wallet/components/buttons/gw_button.dart';` import.

responsive_overlay.dart: replace the "Buy GNUS" `ElevatedButton` (lines ~276-284) with `GWButton(variant: GWButtonVariant.secondary, size: GWButtonSize.md, label: 'Buy GNUS', onPressed: () async { context.push('/buy'); })`. Use `secondary` (outlined brand) — it is one action widget among several in the top-bar Row, not the page's hero CTA, so secondary is correct per the decision. Keep the exact `context.push('/buy')` behavior and the "Buy GNUS" label. Add the `gw_button.dart` import (the file does not yet import it; `GeniusWalletColors`/`GWColors`/typography are already imported). Do not touch any other button or the GNUS/Minions toggle in this file.
  </action>
  <verify>
    <automated>flutter analyze lib/components/job/submit_job_dashboard_button.dart lib/components/overlay/responsive_overlay.dart</automated>
  </verify>
  <done>SubmitJobDashboardButton is a GWButton(primary) with Icons.create leading and identical onPressed; "Buy GNUS" is a GWButton(secondary) pushing '/buy'; no greenAccent icon remains; analyze reports 0 errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Root theme fix (btnFilter now appearance-aware — flips every bare TextButton app-wide) plus per-widget token migrations on the header controls and two branded CTAs. A debug build is ALREADY RUNNING; use the dev-tools bubble's in-place light/dark toggle (GW_DEV_TOOLS=true) to flip appearance over any screen without navigating. Do NOT run flutter build/run.
  </what-built>
  <how-to-verify>
BLAST RADIUS WARNING: the btnFilter change touches EVERY bare TextButton in the app, not just the header. Walk BOTH light and dark.

1. Dashboard header/top bar (light AND dark): network selector, wallet/account trigger, GNUS/SGNUS connection widget, Submit-Job button all read clearly — no dark-navy chip with black ink in light, no illegible text. Confirm the dark mode still shows the familiar ~#132135 chip (unchanged).
2. Current-balance area: "Create Processing Job" (only visible when the selected wallet is SGNUS-linked) is a branded primary GWButton with a legible icon+label; "Buy GNUS" is an outlined secondary GWButton and still navigates to /buy.
3. Reown Connect button: idle state (Connect) reads on light; trigger Connecting/Timed-Out/Error and confirm the amber/red status tints are visible on the light surface (not washed out); open the WalletConnect dialog — its background flips (not dark-navy on light) and the paste/link icons are legible.
4. SGNUS status text + spinner read in light mode (previously white-on-white).
5. Account drawer "..." menu → Delete item is legibly red in both modes.
6. APP-WIDE TextButton regression spot-check (BOTH modes) — confirm no regression on these notable bare-TextButton sites: onboarding (recovery_phrase / backup_phrase / verify_recovery_phrase / legal / import_security screens), pin_screen, disclaimer_dialogue, swap_screen + swap_settings_drawer, submit_job_screen, sdk_account_manager, bridge_screen, banxa buy/order screens, action_button, copy_button, sliding_drawer_button, desktop_container dialogs. Each bare TextButton should show the light chip in light mode and the ~#132135 chip in dark mode with legible ink.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the illegible/regressed controls with mode + screen.</resume-signal>
</task>

</tasks>

<verification>
- `flutter analyze` on all seven modified files reports 0 errors (the only automated gate — `flutter test` does not compile in this project; a debug build is already running so no build/run step is performed here).
- Human walk (Task 4) confirms legibility in BOTH modes across header, balance CTAs, and the app-wide TextButton spot-check, with no dark-mode regression.
</verification>

<success_criteria>
- btnFilter is appearance-aware: dark preserved exactly, light legible; every bare TextButton flips app-wide.
- No raw `Colors.*` / `greenAccent` survives on the five migrated controls (except the intentional QR white).
- SubmitJobDashboardButton and "Buy GNUS" are branded GWButtons with unchanged behavior.
- WCAG AA holds for each control in both modes (verified in the walk).
- Windows/PowerShell: stage only the seven listed files by explicit path (README.md is dirty — never `git add -A`). Do NOT commit docs or update ROADMAP.md.
</success_criteria>

<output>
This is a quick task — no SUMMARY file required beyond the standard quick-task record. Stage the seven modified files by explicit path only.
</output>
