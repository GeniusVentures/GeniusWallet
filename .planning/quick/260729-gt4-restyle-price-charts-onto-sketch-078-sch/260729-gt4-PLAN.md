---
quick_id: 260729-gt4
phase: quick-260729-gt4
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [078-S1, 078-S2, 078-S3, 078-S4, 078-S5, 078-S6]
files_modified:
  - lib/chart/chart_axis.dart
  - lib/chart/crypto_live_chart.dart
  - lib/components/gw_timeframe_segment.dart
  - lib/dashboard/home/view/dashboard_screen.dart
  - lib/dashboard/chart/markets_hero_card.dart
  - test/chart/chart_axis_test.dart
  - test/dashboard/markets_hero_height_test.dart

must_haves:
  truths:
    - "A rising window draws the line green and a falling window draws it red, on the coin chart, the dashboard chart AND the markets hero - one rule, three surfaces (078-S2, 078-S6)."
    - "Given a plot box 220px tall or taller, the chart shows nice-rounded price labels down the RIGHT edge, horizontal-only gridlines sitting on those same values, and time labels along the bottom (078-S1)."
    - "Given a plot box under 220px, the same widget falls back to the axis-free layout, and the H/L labels sit in reserved 17px strips the line is drawn outside of (078-S4, 078-S5)."
    - "Hovering paints a full-height crosshair that clears 3:1 in both themes and updates a header readout; no floating box is drawn over the data (078-S1, 078-S3)."
    - "A fetch that returns nothing says 'No price history' instead of pulsing a skeleton for ever."
    - "The markets hero card is NO taller on the wide layout after the chart grows 180 -> 253, proven by a measurement rather than by arithmetic (078-S6)."
    - "flutter analyze 0, tool/check_brace_style.sh --count 0, flutter test 0 failures - all three still green."
  artifacts:
    - lib/chart/chart_axis.dart
    - test/chart/chart_axis_test.dart
    - lib/components/gw_timeframe_segment.dart
    - test/dashboard/markets_hero_height_test.dart
  key_links:
    - "chartUsesFrame(h) reads the box handed to the PLOT area (a LayoutBuilder below the header row), never the card - the dashboard's two-column call site has no minimum height at all, so nothing else can answer 'does B fit here'."
    - "FlGridData.horizontalInterval and the right SideTitles.interval both take the SAME step returned by chartTickStep, so every label sits on a gridline by construction instead of by coincidence."
    - "chartYBounds stays the single source of the Y window; scheme A's banded window is a pure transform layered on top of it, never a replacement."
    - "The area-fill gradient's 0.62 stop is measured over fl_chart's belowBarLargestRect (top of the highest spot -> plot floor), which is the same box the sketch's SVG objectBoundingBox gradient used - the two are equivalent, so 0.62 ports 1:1."
    - "GWTimeframeSegment is the dashboard's labels + the dashboard's track (CONVENTIONS.md 'Control track': surfaceSunken well). The markets copy keeps its own labels and is NOT touched here."
---

<objective>
Restyle the price charts onto sketch 078 scheme B ("Trading frame"), with scheme A as the runtime
fallback, exactly as `.planning/sketches/078-chart-restyle/README.md` decided on 2026-07-29.

The six locked scope items, referenced by ID from every task below:

| ID | Item |
|---|---|
| 078-S1 | Scheme B in `CryptoLiveChart`: right Y axis with nice-rounded ticks, horizontal-only gridlines, bottom time labels, full-height crosshair, header readout replacing the floating tooltip, timeframe segment replacing the four zoom/pan buttons |
| 078-S2 | Trend colour on the line, its gradient and the touched dot - wiring the EXISTING `trendColor` through, not computing anything new |
| 078-S3 | Touched-spot indicator moves `borderStrong` (2.10:1) -> `borderControl` (3.30:1), WCAG 1.4.11 |
| 078-S4 | A-versus-B is a RUNTIME rule measured off the plot box (>=220px of plot = B), never a per-surface flag |
| 078-S5 | Scheme A's high/low labels get reserved 17px gutters top and bottom, on opaque plates; the line is drawn only between them |
| 078-S6 | Markets hero: `SizedBox(height: 180)` -> 253, and `_HeroChart` drops its fixed gradientGreen -> gradientBlue for the same trend colour |

Purpose: "clunky" is four separate problems. Three of them (a green line on a red day, magnifier
buttons where a timeframe belongs, a bubble parked over the data) are fixed by every scheme in the
sketch. The fourth - you cannot read a value off the card - is fixed only by B.

Output: one shared axis-math file with tests, a restyled `CryptoLiveChart` that carries both schemes
and picks between them from the box it was handed, an extracted `GWTimeframeSegment`, and a markets
hero on the same colour rule with a widget test that MEASURES the height claim instead of trusting
the arithmetic.

**Task count: 4 implementation tasks + 1 human-verify checkpoint.** That is above the 1-3 quick-mode
norm, and it is stated here rather than crammed. The reason is that scope items S1/S4/S5 are three
separate renderers (frame, fallback, label bands) that share one pure-math substrate, and S6 lives
in a different file with a different verification. Splitting them into one task each is what keeps
each task inside its context budget; merging them is what produced the "quick tasks silently regress
plan decisions" pattern already recorded in this project.
</objective>

<hard_constraints>
Read these before the first edit. They override the normal GSD execute-plan workflow.

1. **NO COMMITS. AT ALL.** `AGENTS.md:23` (`CLAUDE.md` is a symlink to it) says *"Do not create
   commits."* Do not `git commit`, do not `git add`, do not `git stash`, do not push, do not create
   a branch. Every change stays in the working tree for Jakub to review. If the execute-plan
   workflow tells you to make an atomic commit per task, **that instruction is overridden here.**
2. **No new dependencies.** `fl_chart` 1.2.0 is already installed and already has every capability
   this plan needs - verified against `~/.pub-cache/hosted/pub.dev/fl_chart-1.2.0/` while planning.
   Two package-install proposals have already been declined in this project. Do not edit
   `pubspec.yaml`.
3. **No golden tests, no snapshot/pixel/visual-regression tooling, no `integration_test`, no
   `patrol`, no Playwright.** Cancelled twice in project instructions. Ordinary `flutter test`
   widget tests only.
4. **Do NOT touch `_ChartSectionHeader`'s `space24` bottom padding** at
   `lib/dashboard/home/view/dashboard_screen.dart:579-596`. That reclaim is a separate, deliberately
   deferred decision belonging to Jakub. Out of scope.
5. **Files under `/banxa` and `/squidrouter` are auto-generated.** Do not change them.
6. **`chartYBounds` (`crypto_live_chart.dart:66-97`) must not change.** It is the sketch 070/071 fix:
   it reduces over the VISIBLE slice and pads 8% each side, and `test/chart/chart_y_bounds_test.dart`
   has 5 tests on it including the GNUS -98.53% case. Layer on top of it; never edit it.
7. **Do not simplify.** No "v1", no "static for now", no "wired later". If something in this plan
   cannot be built as written, stop and report it rather than shipping a reduced version.
</hard_constraints>

<context>
@.planning/sketches/078-chart-restyle/README.md
@.planning/sketches/078-chart-restyle/index.html
@AGENTS.md
@lib/chart/crypto_live_chart.dart
@lib/dashboard/chart/markets_hero_card.dart
@lib/dashboard/home/view/dashboard_screen.dart
@lib/tokens/token_info_screen.dart
@lib/theme/gw_colors.dart

**The sketch's JavaScript is the reference implementation, not decoration.** Port the LOGIC, never
the SVG:
- `ticks()` at `index.html:553` - the nice-number ladder, which deliberately includes 2.5
- `axisMoney()` at `:534` - precision from the tick STEP, not from the value's magnitude
- `plot()` at `:823` - the scheme A and scheme B branches, the 17px label bands at `:853-855`, the
  x-label clamp at `:952`, the 0.62 fill stop at `:892`
- `chartYBounds()` at `:513` - already shipped in Dart; do not port it again

**Facts established while planning, so you do not have to re-derive them:**
- `fl_chart` grid lines and side titles BOTH iterate from the axis baseline (`baselineY`, default 0)
  by `interval` - confirmed in `axis_chart_helper.dart` `iterateThroughAxis` and
  `side_titles_widget.dart:161`. So handing the same `step` to `FlGridData.horizontalInterval` and
  to the right `SideTitles.interval` makes labels land exactly on gridlines, with no manual drawing.
- `SideTitles` has `minIncluded` / `maxIncluded`, both defaulting to **true**, which would print the
  raw padded `minY` / `maxY` (e.g. `$63,412.77`) alongside the nice ladder. Both must be set false.
- `SideTitleWidget` in 1.2.0 is `SideTitleWidget({required child, required meta, space = 8.0, ...})`.
- `GetLineTooltipItems` returns `List<LineTooltipItem?>`; `line_chart_painter.dart` skips null items
  (`if (tooltipItem == null) { continue; }`), so returning all-nulls draws no text.
- The below-bar gradient shader rect is `belowBarLargestRect` =
  `Rect.fromLTRB(mostLeftSpotX, mostTopSpotY, mostRightSpotX, viewSize.height)` -
  `line_chart_painter.dart:797`. It spans the top of the highest spot to the plot floor, which is
  the SAME box an SVG `objectBoundingBox` gradient on the area path spans. `stops: [0.0, 0.62]`
  therefore reproduces the sketch exactly.
- `MarketsHeroCard({required coin, required data, onTap})` takes plain data and fires no request
  (`markets_hero_card.dart:18-31`), so it is safe to pump in a widget test with a hand-built fixture.
- Flutter resolves on this machine at `/Users/jakub/development/flutter/bin/flutter`. If `flutter`
  is not on `PATH` in your shell, prefix commands with that path.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The axis math, as pure functions with a test</name>
  <files>lib/chart/chart_axis.dart, test/chart/chart_axis_test.dart</files>
  <behavior>
    Every one of these is a rule the browser sweep of the sketch caught as a real defect. Write the
    test first; each case below is a named test.

    - `chartTickStep(63000, 64274, 6)` returns more than 2 tick values. (A plain 1-2-5-10 ladder
      collapsed a 6-tick request to 2 labels on this real BTC window. The 2.5 rung is why the ladder
      has 8 entries, not 4.)
    - `chartTickStep` returns values that are all exact multiples of the returned step, all inside
      [lo, hi], and at least 2 of them.
    - `chartTickStep(lo, lo, 6)` (zero span) returns a single value and a step of 1, and does not
      divide by zero or loop.
    - `axisMoneyLabel(1.0004, 0.0002)` and `axisMoneyLabel(1.0006, 0.0002)` return DIFFERENT strings.
      (This is the stablecoin bug: four different prices printed as `$1.00` four times because the
      precision came from the value's magnitude.)
    - `axisMoneyLabel(64250, 250)` returns `$64,250` - step >= 1 means zero decimals.
    - `chartXLabelCount(plotWidth: 900, sampleCount: 4)` returns 4, not 6. (With 4 samples, 6 label
      slots printed "6:35 PM" twice.)
    - `chartXLabelCount(plotWidth: 200, sampleCount: 50)` returns 2 - the floor is 2, never fewer.
    - `chartXLabelCount(plotWidth: 900, sampleCount: 50)` returns 6 - the ceiling is 6.
    - `chartUsesFrame(220)` is true, `chartUsesFrame(219.9)` is false. Boundary pinned, because that
      threshold is the whole of scope item 078-S4.
    - `chartYTickCount` is clamped to [3, 6].
    - `chartBandedBounds` on a 140px plot with 17px bands returns a window strictly wider than the
      input, and mapping the input lo/hi through the returned window lands them exactly `band` px
      inside each edge (assert with a closeTo tolerance of 0.01 on the derived pixel positions).
    - `visibleExtremes` over a series whose minimum is OUTSIDE the [viewMinX, viewMaxX] window
      returns the minimum that is INSIDE the window. (The chart's window is the last 50 points while
      the fetched series is longer, so a naive whole-series scan would label a price that is not on
      screen.)
    - `visibleExtremes` returns null for an empty list and for a flat series (high == low), so the
      caller can skip drawing the labels.
  </behavior>
  <action>
Create `lib/chart/chart_axis.dart` - a new file next to `crypto_live_chart.dart` holding the chart's
pure geometry rules. Top-level and pure so each rule can be tested; a chart-axis bug looks like a
working chart in every screenshot, which is exactly how the y-window bug shipped in the first place.

Constants (named, so no literal comes back unnoticed - the same reasoning as `coinChartHeight`):
- `kChartFrameMinHeight = 220.0` - at or above this many pixels of plot the frame earns its gutters;
  below it the ticks crowd. Per 078-S4 this is a RUNTIME measurement, never a per-surface setting:
  `ChartDashboardView` has three call sites under three height regimes and the two-column one has no
  minimum height at all, so no configuration value can express "does the frame fit here".
- `kChartAxisGutter = 62.0` - the right-hand price gutter.
- `kChartTimeRowHeight = 22.0` - the bottom time-label row.
- `kChartLabelBand = 17.0` - scheme A's reserved label strip, top and bottom (078-S5).
- `kChartFillFadeStop = 0.62` - where the area fill reaches zero alpha.

Functions, each ported from the named sketch function:

`(List<double> values, double step) chartTickStep(double lo, double hi, int want)` - port of
`ticks()` at `index.html:553`. `mag = pow(10, (log(span / want) / ln10).floor())`; try multipliers
`[1, 1.5, 2, 2.5, 3, 4, 5, 10]`; for each, values run from `(lo / step).ceil() * step` up to
`hi + step * 1e-9`; reject candidates with fewer than 2 values; keep the candidate whose count is
closest to `want`. Zero or negative span returns `([lo], 1)`. Keep the 2.5 rung and say in a comment
why it is there.

`String axisMoneyLabel(double value, double step)` - port of `axisMoney()` at `:534`. Decimals come
from the STEP: `step >= 1` -> 0, otherwise `min(8, (-log10(step)).ceil() + 1)`. Format through
`NumberFormat.currency(symbol: '\$', decimalDigits: d)` so it matches the rest of the app. The
comment must record that deriving precision from the value's magnitude is a real bug this rule
exists to prevent, not a style choice.

`int chartXLabelCount({required double plotWidth, required int sampleCount})` - port of the clamp at
`:952`: `max(2, min(min(6, (plotWidth / 110).floor()), sampleCount))`. Clamped to the sample count as
well as the width.

`int chartYTickCount(double plotHeight)` - `max(3, min(6, (plotHeight / 76).round()))`, per `:938`.

`bool chartUsesFrame(double plotHeight)` - `plotHeight >= kChartFrameMinHeight`.

`(double, double) chartBandedBounds((double, double) bounds, double plotHeight, {double band = kChartLabelBand})` -
scheme A's reserved gutters expressed as a Y-window transform, because `fl_chart` has no notion of a
reserved label strip and `HorizontalLineLabel` takes a text style but no background. Given the plot
is `plotHeight` tall and the data must occupy only the middle `plotHeight - 2 * band`, expand the
window so `lo` lands `band` px above the floor and `hi` lands `band` px below the ceiling:
`unitsPerPx = (hi - lo) / max(1, plotHeight - 2 * band)`, then return
`(lo - band * unitsPerPx, hi + band * unitsPerPx)`. Guard a zero/negative `plotHeight - 2 * band` by
returning the input untouched. The doc comment records WHY this exists: a plate behind the label was
tried and rejected by Jakub, because the line still ran through the plate and the plate merely hid
that stretch of the series. Hiding data behind a caption is not a fix.

`(FlSpot high, FlSpot low)? visibleExtremes(List<FlSpot> data, {double? viewMinX, double? viewMaxX})` -
filter to the window the same way `chartYBounds` does (fall back to the whole series if the window
selects nothing), then return the highest and lowest spots. Return null when the list is empty or
when high.y == low.y, so a flat series draws no labels at all.

Write `test/chart/chart_axis_test.dart` covering every case in the behaviour block above. No
fixtures, no mocks - these are pure functions. Follow `test/chart/chart_y_bounds_test.dart` for
style.

Brace every `if` with the body on its own line (`AGENTS.md`), and run `dart format` before verifying.
  </action>
  <verify>
    <automated>flutter test test/chart/chart_axis_test.dart</automated>
    <automated>flutter analyze lib/chart/chart_axis.dart test/chart/chart_axis_test.dart</automated>
    <automated>tool/check_brace_style.sh --count</automated>
  </verify>
  <done>
    All new tests pass; `flutter analyze` reports no issues for the two new files; the brace-style
    count is still 0. `chartYBounds` in `crypto_live_chart.dart` is byte-for-byte unchanged and its
    5 existing tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: CryptoLiveChart - trend colour, the contrast fix, the crosshair, and the chrome swap</name>
  <files>lib/chart/crypto_live_chart.dart, lib/components/gw_timeframe_segment.dart, lib/dashboard/home/view/dashboard_screen.dart</files>
  <action>
This task does everything in `CryptoLiveChart` EXCEPT the frame itself. Task 3 adds the frame on top.

**(a) Trend colour reaches the graphic (078-S2).** `trendColor` already exists at line 319 and today
tints only the % pill at `:430` and `:440`. Delete the `mintColor` local entirely and pass
`trendColor` to the line, to its below-bar gradient and to the touched-spot dot. This is wiring an
existing value through, not computing a new one. Update the stale comment above it - it currently
states that the chart is deliberately decoupled from up/down, and that intent is now reversed. Record
the reversal rather than overwriting it: always-green was proposed because the % pill already states
direction, and it was reversed once the consequence was seen, because the Markets sparklines colour
by sign on purpose (`crypto_simple_chart.dart:53-55`, "Assets-mirror up/down") and an always-green
hero would have sat directly above red sparklines reporting the same fact. One rule now covers every
surface. The `GeniusWalletColors` import may become unused - remove it if so.

**(b) The touched-spot indicator clears 3:1 (078-S3).** Line 504 uses `gw.borderStrong`, white 24%,
which composites to 2.10:1 on `surfaceElevated` and 1.70:1 on a white card. It is the thing that
tells you which point you are reading, so WCAG 1.4.11 wants 3:1. Change it to `gw.borderControl`
(3.30:1 dark / 3.10:1 light). The token exists and its doc comment at
`genius_wallet_colors.dart:179-203` already derives 36% as "the first step that clears 3:1" for
exactly this case.

**(c) The indicator becomes a full-height crosshair.** On the same `FlLine`, set the touch line to
span the whole plot using `LineTouchData.getTouchLineStart` / `getTouchLineEnd` - return the chart's
`minY` and `maxY` so the line runs floor to ceiling instead of stopping at the spot. Keep
`strokeWidth: 1`. The dot stays, on `trendColor`, with the ring alpha it has today.

**(d) The floating tooltip goes away (078-S1).** It works, but it sits under the cursor at the exact
point you are trying to read, and `fl_chart` cannot give it a shadow. Make `getTooltipItems` return
one `null` per touched spot - the painter skips nulls - and neutralise the box that would otherwise
still be stroked: `getTooltipColor` returns `Colors.transparent`, `tooltipBorder: BorderSide.none`,
`tooltipPadding: EdgeInsets.zero`. Delete the now-dead per-point tooltip styling, but KEEP
`_percentAt` - the readout in (f) uses it. Leave the `ponytail:` note about the missing shadow
deleted along with the bubble it described.

**(e) The area fill stops lying about its baseline.** Today the below-bar gradient runs edge to edge
at 30% alpha (`:466-476`). `chartYBounds` is deliberately NOT zero-based, and a fill that touches the
floor on a non-zero axis is the documented misleading case. Change the gradient to
`colors: [trendColor.withValues(alpha: 0.18), trendColor.withValues(alpha: 0.0)]` with
`stops: const [0.0, kChartFillFadeStop]`, `begin: Alignment.topCenter`, `end: Alignment.bottomCenter`.
`fl_chart` shades this over `belowBarLargestRect` (top of the highest spot -> plot floor), which is
the same box the sketch's SVG gradient used, so 0.62 ports exactly. Add a comment naming that
equivalence so nobody "fixes" the stop later.

**(f) The zoom/pan row becomes the chart's own header (078-S1).** Delete the four-button `Row` at
`:579-617` and the four methods `_zoomIn`, `_zoomOut`, `_panLeft`, `_panRight` that feed it. That
closes three filed todos at once: the 34px RenderFlex overflow (Flutter's default icon button is
48x48, and that arithmetic IS the overflow), the four hardcoded `Colors.white` icons, and "is
zoom/pan redundant once ranges are wired". Keep `_viewMinX` / `_viewMaxX` and everything that sets
them - they are still the window `chartYBounds`, `minX` and `maxX` read, and after this deletion the
window is simply "the last 50 points", which is what it already initialises to.

Replace it with a compact header row ABOVE the plot, as a `StatelessWidget` (never a `_buildFoo()`
helper - `AGENTS.md`), rendered **exactly when `showPriceHeader == false`**. Do not add a new
parameter for this. The rule is one sentence and it holds at both call sites today: *either the host
owns the chart's chrome, or the chart does.* The coin page passes `showPriceHeader: false` and has no
chrome of its own (`token_info_screen.dart:416-423`); the dashboard leaves it true and its
`_ChartSectionHeader` already carries both the identity and a timeframe segment, so it must NOT get a
second one. Write that rule into the doc comment on `showPriceHeader`.

The header row contains, left to right: the price at 17px bold with
`FontFeature.tabularFigures()` on `gw.textPrimary`; the signed percentage at 12px bold on
`trendColor`; the hovered sample's time - or the word `Latest` when nothing is hovered - at 11px on
`gw.textSecondary`; a `Spacer`; and `GWTimeframeSegment`. Because `_displayPrice` already resolves to
the hovered price when hovering (`:218-219`), the readout tracks the crosshair with no new state -
only the timestamp is new, so hold the hovered spot's x alongside `_hoveredPrice` in `_onHover`.

When `showPriceHeader == true` (the dashboard), leave today's big price + % pill exactly as they are,
and append the hovered timestamp - or `Latest` - INSIDE the existing pill row at `:423-443`, as a
sibling in the same `Row`, at 11px on `gw.textSecondary` with an 8px gap. It must add zero height:
that card is height-starved, files RenderFlex overflows at boot, and the `isCompact` assert at
`:341-345` depends on the header's budget. Do not add a line, do not change `priceHeight`.

**(g) `GWTimeframeSegment`, extracted.** The dashboard's private `_TimeframeSegment` +
`_TimeframeTab` (`dashboard_screen.dart:674-...`) already ARE the canonical version on both axes:
the labels `['1H','1D','1W','1M','1Y']` follow the "1X" grammar Robinhood publishes, and the track is
`surfaceSunken` per `CONVENTIONS.md` ("Control track": recessed well = sunken, raised chip = menu).
Move both classes verbatim into a new `lib/components/gw_timeframe_segment.dart` as public
`GWTimeframeSegment` (keep the tab class private to that file), have `dashboard_screen.dart` import
and use it, and delete both private copies from `dashboard_screen.dart`. Preserve every existing
comment, including the two `ponytail:` notes and the CONVENTIONS.md quote - they are the reason the
track is what it is. Signature: `const GWTimeframeSegment({super.key, this.initialIndex = 1})`,
holding its own selection. Add NO `onChanged` callback: the tabs are visual-only on every surface
today and nothing would consume it (`AGENTS.md` rung 1). This is now the third occurrence in the
codebase, which is what makes the extraction correct under the Rule of Three.

**Leave `markets_hero_card.dart`'s own `_TimeframeSegment` alone.** It carries a DIFFERENT label set
(`24H/7D/30D/1Y`) and folding it in changes what the markets card says - that is the separate filed
todo `.planning/todos/pending/2026-07-24-unify-timeframe-segment-component.md`, not this task. Add
one line to `GWTimeframeSegment`'s doc comment pointing at that todo.

**(h) The empty state stops lying.** `_fetchHistoricalData` only calls `setState` on a non-empty
response (`:159`), so an empty or failed fetch pulses a `PulsingSkeleton` for ever with no error and
no retry (`:621-624`). Add a `bool _loadAttempted = false`; wrap the fetch body in try/catch and set
the flag in a `finally` via `setState` (guarded by `mounted`). Render: not attempted -> today's
`PulsingSkeleton`; attempted and still empty -> `GWEmptyState(icon: Icons.show_chart, title: 'No
price history', message: 'The market data provider returned no series for this range.')`, matching
the honest state the sketch proposes at `index.html:828-832`. Reuse the existing `GWEmptyState`
component (see `token_info_screen.dart:403-414` for the call shape); do not write a new one.
Swallowing the caught error silently is not acceptable - keep it out of any log that could carry
wallet data, but the user-visible state must distinguish "loading" from "nothing came back".

Run `dart format` on all three files. Brace every `if` with the body on its own line.

<!-- planner-discipline-allow: mintColor -->
<!-- planner-discipline-allow: IconButton -->
  </action>
  <verify>
    <automated>flutter analyze</automated>
    <automated>tool/check_brace_style.sh --count</automated>
    <automated>grep -v '^\s*//' lib/chart/crypto_live_chart.dart | grep -c 'mintColor'</automated>
    <automated>grep -v '^\s*//' lib/chart/crypto_live_chart.dart | grep -c 'IconButton'</automated>
    <automated>flutter test</automated>
  </verify>
  <done>
    `flutter analyze` is 0 issues (unchanged baseline). `tool/check_brace_style.sh --count` is 0.
    Both greps report 0 - the always-mint constant and the zoom/pan buttons are gone from live code.
    `flutter test` shows 0 failures and at least the 517 pre-existing passes plus Task 1's new tests.
    `chartYBounds` and its 5 tests are untouched. `dashboard_screen.dart` no longer declares a
    private timeframe segment and `_ChartSectionHeader`'s `space24` padding is byte-identical.
  </done>
</task>

<task type="auto">
  <name>Task 3: The runtime A/B switch - the trading frame, and scheme A's reserved label bands</name>
  <files>lib/chart/crypto_live_chart.dart</files>
  <action>
**(a) Measure, then choose (078-S4).** Wrap the plot area - everything below the header row from
Task 2 - in a `LayoutBuilder`, and branch on `chartUsesFrame(constraints.maxHeight)`. The box this
measures is the PLOT box, not the card: the card's padding and its header row are already spent by
the time this builder runs, and the threshold in the sketch is quoted against the plot. Add a comment
recording why this is a measurement and not a parameter: `ChartDashboardView` is rendered at
`dashboard_screen.dart:223` (two-column), `:273` (three-column) and `:303` (one-column, capped at
350), the two-column one is `(viewport - 324) / 2` with no floor at all, and a parameter would
silently re-break the moment a layout moved.

**(b) Scheme B, the trading frame (078-S1).** When `chartUsesFrame` is true, build a `Column` of the
`LineChart` (as `Expanded`) over a fixed `kChartTimeRowHeight` time-label row:

- Compute the window once: `final (lo, hi) = _yBounds();` then
  `final (tickValues, step) = chartTickStep(lo, hi, chartYTickCount(constraints.maxHeight - kChartTimeRowHeight));`
  `tickValues` is only used to choose the step - do not draw them yourself.
- `gridData: FlGridData(show: true, drawVerticalLine: false, horizontalInterval: step,
  getDrawingHorizontalLine: (_) => FlLine(color: gw.borderSubtle.withValues(alpha: 0.06), strokeWidth: 1))`.
  Horizontal only. 6% is a legitimate choice, not a shortcut: WCAG 1.4.11's own Understanding
  document works a line chart explicitly and exempts the graduated lines, so gridlines are not gated
  - and no design system publishes a numeric dark-mode gridline opacity, so there is nothing to copy.
  Derive it from the token rather than writing a colour literal; `borderSubtle` is white-on-dark and
  ink-on-light, so one expression is correct in both modes.
- `rightTitles: AxisTitles(sideTitles: SideTitles(showTitles: true, reservedSize: kChartAxisGutter,
  interval: step, minIncluded: false, maxIncluded: false, getTitlesWidget: ...))`. The widget is
  `SideTitleWidget(meta: meta, space: 8, child: Text(axisMoneyLabel(value, step), ...))` at 11px on
  `gw.textSecondary` with `FontFeature.tabularFigures()`. **`minIncluded` and `maxIncluded` MUST be
  false** or `fl_chart` also prints the raw 8%-padded `minY`/`maxY`, which are not on the ladder.
  Left, top and bottom titles all stay hidden.
  The axis is on the RIGHT because on a time series the newest value sits at the right edge and would
  otherwise be furthest from its own scale. Note in the comment that this is a strong de-facto
  convention, NOT a documented standard - Highcharts still defaults its y-axis to the left.
- Because grid lines and side titles both iterate from `baselineY` (0) by `interval`, handing the
  same `step` to both makes every label land on a gridline by construction. Say so in a comment; it
  is the reason no tick values are drawn by hand.
- The time row is a plain `Row` of `Text`s, NOT `fl_chart`'s bottom titles - `fl_chart` anchors its
  x intervals to `baselineX`, which cannot be made to line up with epoch-second sample positions
  without fighting the library. Wrap the row in `Padding(padding: EdgeInsets.only(right:
  kChartAxisGutter))` so it spans exactly the plot's width, and use
  `MainAxisAlignment.spaceBetween`: the first label then sits at the left edge, the last at the right
  edge and the middles centre, which is precisely the `text-anchor` behaviour of the sketch at
  `index.html:955`. Label count is `chartXLabelCount(plotWidth: constraints.maxWidth -
  kChartAxisGutter, sampleCount: visible.length)`; the sample for slot `b` is
  `((b / (count - 1)) * (visible.length - 1)).round()`. `visible` is the slice inside
  `[_viewMinX, _viewMaxX]` - the window, not the whole fetched series. Reuse `_formatTime`. 11px,
  `gw.textSecondary`, tabular figures.

**(c) Scheme A, the fallback with reserved label bands (078-S4, 078-S5).** When `chartUsesFrame` is
false, render the axis-free plot: grid off, all four titles off, no time row - exactly today's
geometry, plus the label bands.

Set the chart's Y window to `chartBandedBounds(_yBounds(), constraints.maxHeight)` so the series is
confined to the middle strip and can never enter a label. This is the SECOND attempt at this defect
and the first one was wrong: a plate alone was tried and rejected by Jakub, because the line still
ran through the plate and the plate merely hid that stretch of the series. Write that history into
the comment - it is the reason the bands exist rather than just the plates.

Overlay the two labels in a `Stack` over the `LineChart`:
- `Positioned(top: 0, left: 0, right: 0, height: kChartLabelBand, ...)` for the high,
  `Positioned(bottom: 0, ...)` for the low, each wrapped in `IgnorePointer` so they never eat a hover.
- Inside each, `Align(alignment: Alignment(fx, 0), child: plate)` where
  `fx = (((spot.x - viewMinX) / (viewMaxX - viewMinX)) * 2 - 1).clamp(-1.0, 1.0)`. `Align` positions
  the child proportionally AND keeps it inside the box, which is the sketch's clamp at
  `index.html:1086` for free and with no text measurement.
- The plate is a `StatelessWidget`: `Container(padding: EdgeInsets.symmetric(horizontal: 5, vertical:
  2), decoration: BoxDecoration(color: gw.surfaceElevated.withValues(alpha: 0.92), borderRadius:
  BorderRadius.circular(3)), child: Text('H ${axisMoneyLabel(...)}', ...))` at 10px on
  `gw.textSecondary`, `height: 1`, letterSpacing 0.6, tabular figures. The plates STAY even though
  the bands now prevent overlap, because the area fill still runs under the bottom band and bare text
  over a chart has no defined background - its contrast ratio cannot even be computed.
- Source the two spots from `visibleExtremes(_priceData, viewMinX: _viewMinX, viewMaxX: _viewMaxX)`
  and the label precision from `axisMoneyLabel(value, (hi - lo) / 4)` - same window-derived rule as
  the axis, per `index.html:862`. When it returns null (empty or flat), draw no labels at all.

**Both branches keep everything Task 2 established** - trend colour, `borderControl` crosshair
running the full plot height, suppressed tooltip, the 0.62 fill ramp, `clipData: FlClipData.all()`,
and `minY`/`maxY` still ultimately derived from `chartYBounds`. Scheme A's banded window is a
transform layered ON TOP of `chartYBounds`, never a replacement for it.

Keep the two branches in two small `StatelessWidget`s rather than one branching build method, so the
DevTools inspector can tell them apart and neither rebuilds the other.

Run `dart format`. Brace every `if` with the body on its own line.
  </action>
  <verify>
    <automated>flutter analyze</automated>
    <automated>tool/check_brace_style.sh --count</automated>
    <automated>flutter test test/chart/chart_axis_test.dart test/chart/chart_y_bounds_test.dart test/chart/compact_price_font_size_test.dart</automated>
    <automated>flutter test</automated>
  </verify>
  <done>
    `flutter analyze` 0, brace count 0, `flutter test` 0 failures with the full pre-existing suite
    still passing. `crypto_live_chart.dart` contains exactly one `chartUsesFrame` call site, sited
    inside a `LayoutBuilder` that wraps the plot area rather than the card. `chartYBounds` is still
    the only producer of the base Y window, and its 5 tests pass untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: The markets hero - measure the height claim, then grow the chart and colour it by trend</name>
  <files>test/dashboard/markets_hero_height_test.dart, lib/dashboard/chart/markets_hero_card.dart</files>
  <behavior>
    The claim under test is that growing the hero chart from 180 to 253 costs the card NO height on
    the wide layout, because the right column sits in an `IntrinsicHeight` row whose height comes
    from the taller LEFT column and the `Spacer` at `markets_hero_card.dart:167` is holding 73px of
    slack. **That number is DERIVED from the widget tree at `8ed02e78`, not measured in a running
    app**, and 253 consumes the slack exactly (left 301 = 32 segment + 16 gap + 253 chart). A
    derivation that lands exactly on the boundary is precisely the kind that must be measured.

    So the test is written and RUN BEFORE the change, against the shipped 180:
    - Test: "wide markets hero: growing the chart to 253 costs the card no height".
    - Pump `MarketsHeroCard` with a hand-built `CoinGeckoCoin` + `CoinGeckoMarketData` fixture
      (30-point rising sparkline) inside a `MaterialApp` carrying the app's dark theme extension, in
      a 1200px-wide, height-unbounded `SingleChildScrollView` - the same shape as its only real
      consumer, `markets_screen.dart:197`.
    - Record `tester.getSize(find.byType(MarketsHeroCard)).height` as a named constant in the test
      with a comment stating it was measured on this machine, on this date, against the 180 chart.
    - Assert the card height equals that constant.
    - Assert `tester.takeException()` is null - a right column taller than the row would overflow.
    - Assert the chart's own box is the expected height, so a squeezed chart cannot pass by shrinking.
    - Then flip 180 -> 253 and re-run. The SAME assertions must still pass.
  </behavior>
  <action>
**Write and run the test first, against the current code, before changing a single pixel.** If the
recorded card height changes when the chart grows, STOP and report it in your summary rather than
accepting a taller card - the narrow/stacked layout has no `Spacer` and genuinely grows by 73px,
which is accepted and expected, but the WIDE layout growing means the derivation was wrong.

`MarketsHeroCard({required coin, required data, onTap})` takes plain data and fires no request, so
the fixture is straightforward - no mocks, no network, no `integration_test`.

Then, in `lib/dashboard/chart/markets_hero_card.dart`:

**(a) `SizedBox(height: 180, ...)` at `:168` becomes 253 (078-S6).** Give it a named top-level
constant with a comment carrying the arithmetic and its provenance: left column 301 (46 icon + 20 +
48 price + 16 + 24 pill + 24 + 1 rule + 24 + 39 stat + 20 + 39 stat), right column 48 + chart, so 253
is the exact height at which the right column stops being the shorter one. Derived from the tree, and
now MEASURED by the test beside it. Note that the narrow layout has no `Spacer`, so there the +73 is
paid in full and that is expected.

**(b) `_HeroChart` drops its fixed gradient for the trend colour (078-S6).** The hero is currently a
THIRD colour rule: `markets_hero_card.dart:312-315` paints the line on a fixed
`gradientGreen -> gradientBlue` regardless of direction, while `CryptoLiveChart` was always mint and
the two sparklines colour by sign. After this there is one rule. Compute
`final up = data.last >= data.first;` and
`final Color trend = up ? gw.statusSuccess : gw.statusError;`, then:
- the line takes `color: trend` (drop the `gradient:` argument entirely),
- the below-bar gradient becomes `[trend.withValues(alpha: 0.18), trend.withValues(alpha: 0.0)]`
  with `stops: const [0.0, 0.62]` - the same non-zero-baseline honesty as the main chart; the hero
  sets no `minY`/`maxY`, so its fill would otherwise run to a floor the scale never claims,
- the touched dot takes `trend` for both fill and its 26%-alpha ring.
Use the local `up`, not `data.priceChangePercentage24h`: the plotted series is the 7d sparkline, so
the colour must describe what is drawn. Say so in a comment - a 24h-signed colour on a 7d line is the
same class of lie this whole task is removing.

**(c) The hero's touched-spot indicator gets the same contrast fix as the main chart (078-S3).**
`:368` uses `gw.borderStrong` (2.10:1). Move it to `gw.borderControl` (3.30:1). Same token, same
reason - it is the mark that tells you which point you are reading.

Leave the hero's floating tooltip in place: it is a decorative backdrop with no header readout to
move the values into, and replacing it is not in this task's scope. Leave `_TimeframeSegment` in this
file alone (see Task 2). The `GeniusWalletColors` import is still needed for `textOnBrand` - check
before removing it.

Run `dart format`. Brace every `if` with the body on its own line.

<!-- planner-discipline-allow: gradientGreen -->
  </action>
  <verify>
    <automated>flutter test test/dashboard/markets_hero_height_test.dart</automated>
    <automated>flutter analyze</automated>
    <automated>tool/check_brace_style.sh --count</automated>
    <automated>grep -v '^\s*//' lib/dashboard/chart/markets_hero_card.dart | grep -c 'gradientGreen'</automated>
    <automated>flutter test</automated>
  </verify>
  <done>
    The height test passes against BOTH 180 and 253 with the same recorded card height, proving the
    growth is absorbed on the wide layout. The grep reports 0. `flutter analyze` 0, brace count 0,
    full `flutter test` 0 failures.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Jakub walks the three surfaces</name>
  <files>(none - no code changes in this task)</files>
  <action>
    Stop. Run the three gates one final time, quote their real output, then hand the running app to
    Jakub with the walk script below and WAIT. Do not commit, do not open a PR, do not start any
    follow-up work while waiting. If the walk turns up a defect, fix it in the working tree and ask
    for the walk again.
  </action>
  <what-built>
    Scheme B on the coin page (wide and stacked) and anywhere the plot clears 220px, scheme A below
    it, one trend-colour rule across the coin chart / dashboard chart / markets hero, the crosshair
    moved off a 2.10:1 token onto a 3.30:1 one, the zoom/pan row replaced by the timeframe segment,
    the markets hero chart grown to 253, and an honest empty state.

    **Nothing is committed.** Every change is in the working tree, per `AGENTS.md:23`.
  </what-built>
  <how-to-verify>
    1. `flutter run -d macos --dart-define=GW_DEV_TOOLS=true` (only ONE instance - a second dies on
       the Hive lock).
    2. **Coin page, wide window.** Open any covered token. The chart should carry price labels down
       the right edge on nice round numbers, faint horizontal gridlines under them, times along the
       bottom, and a header row with price + % + `Latest` on the left and the 1H·1D·1W·1M·1Y segment
       on the right. Hover: a full-height vertical line, the header numbers follow the cursor, and NO
       box appears over the data.
    3. **Coin page, narrow the window until it is mobile-shaped.** The frame should drop away and the
       chart become axis-free with `H $x` pinned in a strip at the top and `L $y` in a strip at the
       bottom. **The line must never touch either label.** This is the exact defect you flagged
       (*"te oznaczenia HIGH/LOW overlapują z wykresem"*) and it is the second attempt at it.
    4. **Drag-resize the window slowly through the switch point.** The chart should flip between the
       two layouts cleanly, with no overflow stripes and no freeze.
    5. **Dashboard.** The Bitcoin chart card must have exactly ONE timeframe segment (in the section
       header), the price + % + a small time next to the pill, no magnifier buttons, and no 34px
       overflow stripe at boot. Resize the window down: the frame should reduce to the axis-free
       layout rather than crowding.
    6. **Markets page.** The hero chart is taller and its line is green or red with the 24h move, not
       green-to-blue. On a wide window the CARD must be the same height as before - the chart should
       have eaten the empty space above it, not pushed the card down.
    7. **Find a red day.** Every line on screen - hero, coin chart, dashboard chart, sparklines - must
       agree on the colour.
    8. **Toggle light mode** and re-check the axis labels, the gridlines, the crosshair and the H/L
       plates. Light mode is where this repo has historically broken.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what is wrong and on which surface.</resume-signal>
  <verify>
    <human-check>Jakub walks steps 1-8 above on a running macOS build and replies.</human-check>
    <automated>git log -1 --format=%H</automated>
  </verify>
  <done>
    Jakub has typed "approved", or every defect he named has been fixed in the working tree and
    re-walked. `git log -1` prints the SAME commit hash the session started on - no commit was
    created at any point in this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CoinGecko HTTP -> `CryptoLiveChart` | Untrusted remote series already crosses here today; this task changes only how it is DRAWN, not how it is fetched or parsed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-gt4-01 | Denial of Service | `chartTickStep` loop over an attacker-shaped price range | low | mitigate | The loop is bounded by `hi + step * 1e-9` with `step` derived from the span, and the zero/negative-span case returns early. Task 1 pins the zero-span case in a test so a division by zero or an unbounded loop cannot ship. |
| T-gt4-02 | Information Disclosure | the caught fetch error in `_fetchHistoricalData` | low | mitigate | The user-visible state is a fixed string; the caught error is not rendered and must not be sent anywhere that could carry wallet data. No key, mnemonic or seed-derived value is anywhere near this code path. |
| T-gt4-03 | Tampering | package supply chain | n/a | accept | **No package installs in this plan.** `pubspec.yaml` is not touched and `fl_chart` 1.2.0 is already resolved and pinned in `pubspec.lock`. No Package Legitimacy Gate applies. |

No new trust boundary is created. This is a rendering change inside an existing widget, with no new
network call, no new persistence, no new platform channel and no new dependency.
</threat_model>

<verification>
Run all three project gates from the repo root after the last implementation task:

```
flutter analyze                      # baseline 0 issues - must still be 0
tool/check_brace_style.sh --count    # baseline 0 - must still be 0
flutter test                         # baseline 517 pass / 0 fail - must be >= 517 pass / 0 fail
```

Quote the real output; never claim a baseline you did not run. If `flutter` is not on `PATH`, use
`/Users/jakub/development/flutter/bin/flutter`.

Then confirm the plan's own invariants:
- `git diff --stat` lists ONLY the seven files in `files_modified`. Nothing under `/banxa` or
  `/squidrouter`. `_ChartSectionHeader`'s `space24` padding is unchanged.
- `git log -1` shows the SAME commit as before you started. **No commit was created.**
- `git diff lib/chart/crypto_live_chart.dart` shows no change inside `chartYBounds` (lines 66-97).
</verification>

<success_criteria>
- Above 220px of plot the chart draws a right-hand price axis on rounded values, horizontal-only
  gridlines on those same values, and bottom time labels; below it the axis-free layout with banded
  H/L labels. One widget, two modes, chosen from a measurement.
- The line, its fill and its touched dot are green rising and red falling on all three full charts.
- The touched-spot indicator is `borderControl` and runs the full plot height; no tooltip box paints.
- The area fill reaches zero alpha at 62% and never touches the floor of a non-zero axis.
- The four zoom/pan buttons and the always-mint constant are gone from live code.
- An empty fetch renders "No price history"; only an unfinished fetch renders the skeleton.
- The markets hero chart is 253 tall and the wide card's height is unchanged, PROVEN by
  `test/dashboard/markets_hero_height_test.dart`, not by arithmetic.
- `flutter analyze` 0, `tool/check_brace_style.sh --count` 0, `flutter test` 0 failures.
- The working tree holds every change and git history is untouched.
</success_criteria>

<not_in_scope>
Recorded so nobody re-derives them mid-task. None of these needs a new todo file - each is either
already filed or explicitly deferred:

- `_ChartSectionHeader`'s `space24` bottom padding - Jakub's deferred decision.
- A floor height for the dashboard's two-column chart slot - already filed twice
  (`2026-07-21-bitcoin-chart-card-height-dashboard-vertical-budget.md`,
  `2026-07-24-dashboard-bitcoin-chart-renderflex-overflow.md`). Both conclude the SLOT is broken, not
  the chart. Once it gets its floor, the runtime rule in Task 3 gives that card the frame for free.
- Folding `markets_hero_card.dart`'s `24H/7D/30D/1Y` segment into `GWTimeframeSegment` -
  `2026-07-24-unify-timeframe-segment-component.md`.
- Wiring the timeframe tabs to real ranges - `2026-07-21-wire-real-timeframe-ranges-in-crypto-live-chart.md`.
  They stay visual-only on every surface, exactly as today.
- Scheme C on the markets hero (the sketch's runner-up, and its strongest candidate) - not chosen.
- The zh/ja/ko/tw inverted red-up convention - recorded in the sketch, a localisation decision.
- The empty state IS in scope and is built in Task 2, so no todo is filed for it.
</not_in_scope>

<output>
No SUMMARY file and no commit. Report back in the session with:
- the three gate outputs, quoted verbatim,
- the card height the markets hero test recorded, and whether it held at 253,
- anything you could not build as written.
</output>
</content>
</invoke>
