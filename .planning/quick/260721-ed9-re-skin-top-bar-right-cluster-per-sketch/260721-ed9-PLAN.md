---
phase: quick-260721-ed9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/theme/nav_chip_style.dart          # NEW — shared chip ButtonStyle builder
  - test/theme/nav_chip_style_test.dart    # NEW — runnable check (height/radius/fill + AA contrast)
  - lib/network/network_dropdown_selector.dart
  - lib/account/sdk_account_manager.dart
  - lib/account/account_dropdown_selector.dart
  - lib/reown/reown_connect_button.dart
  - lib/components/buttons/gw_button.dart
  - lib/components/overlay/responsive_overlay.dart
autonomous: true
requirements: [SKETCH-005-B]

must_haves:
  truths:
    - "All five top-bar right-cluster controls render at ONE explicit height: 40px."
    - "Context chips 1-3 (chain / SDK / wallet) are quiet: gw.surfaceMenu fill + 1px gw.borderSubtle, hover -> gw.borderStrong, radiusMd(12), padding space6(12), internal gap space4(8)."
    - "Connect is the outlined secondary (brand border + brand label/icon), same 40px shell; its label/icon/border clear WCAG AA in BOTH modes (light surfaceElevated is pure white — raw brandPrimaryStrong ~2:1 there, so an appearance-aware darker brand is used in light)."
    - "Buy GNUS is the ONLY gradient (single primary CTA), dropped 48 -> 40 via a LOCAL height override — GWButtonSize.md stays 48 app-wide (no global regression)."
    - "The action Row has even gaps: spacing space4(8) (it is flush today)."
    - "SDKAccountManagerButton still returns SizedBox.shrink() when no SDK accounts — the Row(spacing:) collapses the gap, no hole."
    - "ReownConnectButton keeps its 5-state machine (idle/connected/connecting/timed-out/retry) and status colors — only shell (shape+height) normalized."
    - "theme.dart textButtonTheme is UNTOUCHED (the trap)."
  artifacts:
    - lib/theme/nav_chip_style.dart
    - test/theme/nav_chip_style_test.dart
  key_links:
    - "lib/components/overlay/responsive_overlay.dart _DesktopTopBar right Row (the one wrapping _buildActionRowWidgets + Buy GNUS) gains spacing: GeniusWalletConsts.space4."
    - "network_dropdown_selector.dart / sdk_account_manager.dart / account_dropdown_selector.dart each apply navContextChipStyle(context) to their TextButton."
    - "reown_connect_button.dart merges navChipShell(context) with its per-state backgroundColor + side."
    - "gw_button.dart optional height param consumed at the Buy GNUS call site in responsive_overlay.dart."
---

<objective>
Re-skin (NOT restructure) the desktop top-bar right cluster per approved sketch 005 winner B —
"Normalized quiet chips". Make all five controls read as one 40px family: three quiet context chips,
one outlined Connect (the secondary), one gradient Buy GNUS (the single primary CTA), with even
space4 gaps.

Purpose: today the four bare TextButtons inherit theme.dart's textButtonTheme vertical padding
(space10 = 20) and render ~56-60px tall, while Buy GNUS is 48 — four heights, three fill languages,
zero gaps. Normalizing them removes the mismatch without touching any shared theme.

Output: a small shared chip ButtonStyle builder + its runnable check, applied locally to each of the
five controls. Behaviour, dropdowns, the SDK-absent collapse, the 5-state Connect machine, and the
responsive label drops are all PRESERVED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/sketches/005-navbar-right-cluster/IMPLEMENTATION-BRIEF.md
@lib/theme/gw_colors.dart
@lib/theme/genius_wallet_consts.dart
</context>

<constraints>
- NO COMMITS. CLAUDE.md hard-forbids commits and Jakub reiterated it. The executor makes edits +
  writes SUMMARY.md ONLY. Do NOT run `git add`, `git commit`, or create a commit of any kind. All
  changes stay uncommitted in the working tree for Jakub to walk. There is NO atomic-commit step in
  this plan.
- Do NOT edit theme.dart's textButtonTheme (line ~247, padding vertical: space10). It is shared by
  every bare TextButton app-wide; editing it regresses unrelated screens. This is THE trap a
  plan-checker verifies. Local styleFrom overrides ONLY.
- Do NOT change GWButtonSize.md (48). It is used app-wide; changing it to 40 is the same trap class.
  Buy GNUS gets 40 via a LOCAL, additive, optional height override (default preserves 48 everywhere
  else).
- Do NOT revert eu9's appearance-aware btnFilter or any other in-flight session work. There are
  ~210 uncommitted lines already in responsive_overlay.dart + edits in reown_connect_button.dart /
  theme.dart this session — reconcile with the current on-disk file, do not rewrite from scratch.
- This is a re-skin: do not delete, reorder, or merge any of the five features. Only chrome changes.
- Stage nothing; if any incidental `git` command is needed it is read-only (status/diff) only.
</constraints>

<tokens>
Read via `Theme.of(context).extension<GWColors>() ?? GWColors.dark()` (fail-soft, registers the
InheritedWidget dependency for live appearance flips):
- gw.surfaceMenu (chip fill), gw.borderSubtle (rest border), gw.borderStrong (hover border),
  gw.textPrimary / gw.textSecondary (chip content).
Sizing/shape from GeniusWalletConsts: space4 = 8, space6 = 12, radiusMd = 12.
Brand: GeniusWalletColors.brandPrimaryStrong = 0xFF0AAEE6 (Connect in DARK).
Light surfaceElevated = pure white (0xFFFFFFFF) — brandPrimaryStrong on white is ~2.1:1 (fails AA
text AND 3:1 non-text). Use an appearance-aware darker brand in LIGHT: Color(0xFF0B6E8F)
(≈5.8:1 on white — verified in the test). Dark keeps brandPrimaryStrong.
</tokens>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared nav-chip ButtonStyle builder + runnable check</name>
  <files>lib/theme/nav_chip_style.dart, test/theme/nav_chip_style_test.dart</files>
  <behavior>
    - navChipShell(context) returns a ButtonStyle whose min height AND max height both resolve to 40
      (so a bare TextButton stops inheriting the theme's ~56-60px). radius resolves to radiusMd (12).
      horizontal padding space6 (12). Height 40 must actually RENDER: set tapTargetSize
      shrinkWrap + a compact visualDensity so Material's 48 tap floor does not re-inflate it, and
      pin height with minimumSize AND maximumSize (do NOT use fixedSize with Size.fromHeight — that
      is infinite width and blows up an unbounded Row).
    - navContextChipStyle(context) = the shell PLUS quiet fill: backgroundColor gw.surfaceMenu,
      foregroundColor gw.textPrimary, and a state-aware side (rest = gw.borderSubtle 1px,
      hovered = gw.borderStrong 1px). The hover->borderStrong wiring MUST be a
      WidgetStateProperty.resolveWith on `side` (styleFrom's plain `side` arg is not state-aware).
    - Light-mode Connect brand color Color(0xFF0B6E8F) clears 4.5:1 on white; dark-mode Connect
      brandPrimaryStrong clears 4.5:1 on the dark surfaceElevated (0xFF0C0E14).
  </behavior>
  <action>
    Create lib/theme/nav_chip_style.dart. Export two top-level functions (plain ButtonStyle builders,
    NOT a new widget — no public widget/abstraction): navChipShell(BuildContext) and
    navContextChipStyle(BuildContext). Read gw via Theme.of(context).extension GWColors ?? GWColors.dark().
    Build the shell with TextButton.styleFrom giving symmetric horizontal padding space6, shape
    RoundedRectangleBorder radiusMd, minimumSize Size(0, 40), maximumSize Size(double.infinity, 40),
    tapTargetSize MaterialTapTargetSize.shrinkWrap, visualDensity VisualDensity.compact; then .copyWith
    is fine but keep the height pinned via the min/max. navContextChipStyle takes the shell and merges
    backgroundColor WidgetStatePropertyAll(gw.surfaceMenu), foregroundColor WidgetStatePropertyAll(
    gw.textPrimary), and side WidgetStateProperty.resolveWith((states) => BorderSide(color:
    states.contains(WidgetState.hovered) ? gw.borderStrong : gw.borderSubtle, width: 1)). Also expose
    a tiny appearance-aware helper connectBrandColor(BuildContext) returning brandPrimaryStrong in dark
    and Color(0xFF0B6E8F) in light (gate on GWAppearance.isLight or on whether gw resolves the light
    surface — reuse the existing appearance signal, do not invent a new one). Add a small pure-Dart
    contrastRatio(Color a, Color b) function ONLY if the test needs it — otherwise put the contrast
    math in the test file. Then create test/theme/nav_chip_style_test.dart (flutter_test, no running
    app): build both styles under a MaterialApp/Builder or by calling with a stub context via
    tester.pumpWidget, resolve and assert min+max height == 40 and shape radius == 12; assert the
    context style backgroundColor resolves to a surfaceMenu value and the side resolves borderSubtle
    at rest and borderStrong when {WidgetState.hovered}; and assert contrastRatio(Color(0xFF0B6E8F),
    Color(0xFFFFFFFF)) >= 4.5 and contrastRatio(brandPrimaryStrong, Color(0xFF0C0E14)) >= 4.5. This
    single test is the runnable check for all the non-trivial styling + AA claims.
  </action>
  <verify>
    <automated>flutter test test/theme/nav_chip_style_test.dart</automated>
  </verify>
  <done>nav_chip_style.dart compiles; the test passes, proving height-40, radiusMd, surfaceMenu fill, borderSubtle->borderStrong hover, and both Connect AA contrasts.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the quiet chip style to the three context controls</name>
  <files>lib/network/network_dropdown_selector.dart, lib/account/sdk_account_manager.dart, lib/account/account_dropdown_selector.dart</files>
  <action>
    In each of the three files, import lib/theme/nav_chip_style.dart and genius_wallet_consts.dart if
    not already imported. On each control's existing TextButton (network build() ~line 138, sdk
    build() ~line 45, account build() ~line 426) add style: navContextChipStyle(context). Change each
    control's internal Row spacing from 6.0 to GeniusWalletConsts.space4 (8) for the consistent
    icon/label/chevron gap. Do NOT touch onPressed, the drawers, the responsive label-drop guards
    (MediaQuery >= GeniusBreakpoints.small in sdk/account), or the SizedBox.shrink() early-return in
    sdk_account_manager when accounts.isEmpty — those are PRESERVED behaviour. Leave the chain-icon
    Image untinted (foregroundColor only affects the chevron/label). Do not add any new widget.
  </action>
  <verify>
    <automated>flutter analyze lib/network/network_dropdown_selector.dart lib/account/sdk_account_manager.dart lib/account/account_dropdown_selector.dart</automated>
  </verify>
  <done>All three chips carry navContextChipStyle (quiet surfaceMenu fill + borderSubtle, hover borderStrong, 40px, radiusMd, padding space6, internal gap space4); analyze is clean; behaviour/dropdowns/SDK-absent/label-drops unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize Connect + Buy GNUS to the 40px shell and add even gaps</name>
  <files>lib/reown/reown_connect_button.dart, lib/components/buttons/gw_button.dart, lib/components/overlay/responsive_overlay.dart</files>
  <action>
    (a) reown_connect_button.dart: import nav_chip_style.dart. In build(), read gw
    (Theme.of(context).extension GWColors ?? GWColors.dark()) — the old "no appearance-aware read
    needed" comment is now wrong because light surfaceElevated is white. For the IDLE Connect branch,
    replace the hard brandPrimaryStrong icon/text/border color with connectBrandColor(context)
    (dark = brandPrimaryStrong, light = 0xFF0B6E8F) so the outline+label clear AA in both modes; keep
    the 1.5px border width. Leave the four status branches (Disconnect/Connecting/Timed Out/Retry) and
    their statusError/statusWarning fills exactly as-is. Change the button's style from the bare
    TextButton.styleFrom(backgroundColor, side) to navChipShell(context).copyWith(
    backgroundColor: WidgetStatePropertyAll(backgroundColor), side: border == null ? null :
    WidgetStatePropertyAll(border)) so it adopts the 40px/radiusMd/padding shell while keeping its
    per-state fill+border. Optionally set the inner Row spacing to GeniusWalletConsts.space4 for family
    consistency. Do NOT change the isMobile Tooltip wrap, the connect/disconnect logic, or the state
    machine.
    (b) gw_button.dart: add an optional `final double? height;` field to the default GWButton
    constructor (default null; GWButton.icon can leave it null). Change `double get _height` to return
    `height ?? <existing size switch value>`. This is additive and preserves 48 for every existing md
    call site — do NOT change the GWButtonSize.md value. One-line ponytail comment: local height
    override so a single call site (top-bar Buy GNUS) can be 40 without a global size regression.
    (c) responsive_overlay.dart: in _DesktopTopBar, on the right Row that wraps
    `..._buildActionRowWidgets(context)` + the Buy GNUS GWButton (~line 372), add
    `spacing: GeniusWalletConsts.space4` for even gaps across all five controls. On that Buy GNUS
    GWButton, pass `height: 40` so it matches the 40px family (keep variant gradient / size md — its
    gradient identity and radiusLg stay; only the height drops). Do NOT add spacing to the MobileOverlay
    Row (mobile is out of scope for this sketch). Do NOT edit theme.dart.
  </action>
  <verify>
    <automated>flutter analyze lib/reown/reown_connect_button.dart lib/components/buttons/gw_button.dart lib/components/overlay/responsive_overlay.dart</automated>
  </verify>
  <done>Connect uses the 40px shell with appearance-aware AA-safe brand outline/label and keeps all 5 states; Buy GNUS renders 40 via the local height override (md stays 48 elsewhere); the right Row has spacing space4; analyze is clean; theme.dart unchanged.</done>
</task>

</tasks>

<threat_model>
Pure presentational re-skin: no new trust boundaries, no new inputs parsed, no packages installed,
no auth/data paths touched. The only cross-cutting risk is UI regression from editing a SHARED
surface — mitigated by the two hard constraints (no theme.dart textButtonTheme edit; no GWButtonSize.md
change), both enforced by local-override-only styling and verified in the walk.
</threat_model>

<verification>
- flutter test test/theme/nav_chip_style_test.dart passes (height 40, radiusMd, surfaceMenu fill,
  hover borderStrong, both Connect AA contrasts).
- flutter analyze is clean across all touched files.
- Grep proves the trap is intact: `git diff lib/theme/theme.dart` shows NO change to textButtonTheme
  padding, and GWButtonSize.md still returns 48 in gw_button.dart.
- No commit created: `git status` still shows the files as modified/untracked (working tree only).
- Walk (Jakub, macOS): `flutter run -d macos --dart-define=GW_DEV_TOOLS=true` —
  * all five controls read as one 40px family with even space4 gaps;
  * context chips are quiet (surfaceMenu fill, subtle border) with a consistent hover (borderStrong);
  * Connect is the outlined secondary and its 5 states still fire (dev bubble / disconnect), legible in BOTH light and dark;
  * SDK-absent case leaves no hole;
  * Buy GNUS is the ONLY gradient;
  * check BOTH light AND dark.
</verification>

<success_criteria>
The desktop top-bar right cluster reads as one normalized 40px family (winner B): three quiet chips,
an outlined Connect, one gradient Buy GNUS, even gaps — with every feature/behaviour preserved,
WCAG AA in both modes, the shared theme untouched, and no commit made.
</success_criteria>

<output>
Create `.planning/quick/260721-ed9-re-skin-top-bar-right-cluster-per-sketch/260721-ed9-SUMMARY.md`
when done. Do NOT commit — leave all changes in the working tree for Jakub to walk.
</output>
