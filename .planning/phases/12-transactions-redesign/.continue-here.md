---
context: phase
phase: 12-transactions-redesign
plan: 12-06
total_plans: 6
status: parked
last_updated: 2026-07-22T13:01:05Z
---

# Continue here — Phase 12, Transactions redesign

**Parked:** 2026-07-22 · **Branch:** `ui-redesign-port` · **Committed** in `8cb4222`
(the "everything uncommitted" line in `handoffs/HANDOFF-phase12-transactions.md` is stale —
superseded by `79dc29c`, and PR #211 merged it).

Parked because Phase 06 (Onboarding) is the active phase. Nothing here is blocked technically.

## Position

**5 of 6 plans done. Implementation is complete.** The only thing left is `12-06`, a human walk.

## To resume

Open `12-06-PLAN.md` and walk it. Three blocking checkpoints: dark rows and badges, filters and
empty states, and recording the light-mode deferral.

**DARK MODE ONLY.** Light mode is backlog per
`.planning/todos/pending/2026-07-22-light-mode-verification-backlog.md`; a light-mode finding is a
note in that file, never a blocker.

```
flutter run -d macos --dart-define=GW_DEV_TOOLS=true
```

Kill any running instance first — a stale one holds the Hive container lock and the next launch is a
silent black window. **Transactions render empty until you inject fixtures** — the bug icon in the
dev panel, "Mock txns" (11 fixtures covering all 7 types and all 4 statuses).

## One decision waiting on you, not on an executor

The active filter chip's gradient **fill** measures **1.65:1 / 2.28:1** against the light bar
surface, where WCAG 1.4.11 wants 3:1 for UI components. The executor deliberately did not touch it:
"active = `brandCta` gradient" is a locked decision of yours, and the ink glyph carries the state at
10.66:1. Changing it is your call.

## Do not report these as defects

- **Sketch 014's `Job #4192` and `Banxa · card` are fiction.** The model has no job id, no purchase
  provider and no failure reason; the implementation derives from real fields (truncated hash,
  status name).
- **`responsive_overlay.dart:199` overflows 82px right.** Pre-existing, predates Phase 12.
- **`local_wallet_storage_test.dart` fails.** Entirely commented out, fails at load. Pre-existing.

## Known-but-unfixed

Markets draws its row line with `Container(height: 1)` (`dashboard_markets.dart:73`) while Assets
and Transactions use `Divider`. Same pixel, two widgets. One-line change, not made.

## Freeze rule — binding for anyone touching layout here

**No dimension may be derived continuously from constraints** — no `AutoSizeText`, no `FittedBox`,
no `textScaler`-derived `fontSize`. `test/freeze_rule_test.dart` fails with a file:line if either
widget returns to a dashboard-reachable file. Full chain in
`.planning/handoffs/HANDOFF-2026-07-22-session-end.md`.
