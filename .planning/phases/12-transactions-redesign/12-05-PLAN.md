---
phase: 12-transactions-redesign
plan: 05
type: execute
wave: 5
depends_on: ["12-02", "12-03", "12-04"]
files_modified:
  - lib/dashboard/home/widgets/transactions_slim_view.dart
  - lib/dev/dev_mock_transactions.dart
  - test/dashboard/transaction_filters_test.dart
autonomous: true
requirements: [TX-08, TX-10, TX-11]

must_haves:
  truths:
    - "The panel shows a day separator per calendar day instead of eight identical relative-time strings."
    - "A wallet that has never transacted and a filter that matched nothing say visibly different things."
    - "The filtered-empty state names the filter, states how many transactions DO exist, and offers Show all — not Buy GNUS."
    - "The panel derives no font size, scale or fractional width from its constraints."
    - "The dev mock batch exercises all seven transaction types across three calendar days, so the walk can see every branch."
  artifacts:
    - "transactions_slim_view.dart body rebuilt around day groups"
    - "filteredEmptyTitle / filteredEmptyMessage"
    - "lib/dev/dev_mock_transactions.dart (extended batch)"
  key_links:
    - "groupTransactionsByDay() -> the flat entry list the ListView renders; day headers and dividers come from the same pass."
    - "scoped list (SGNUS-applied, filter-unapplied) -> both the overflow-menu counts and the filtered-empty 'you have N' number, so the two can never disagree."
    - "GWEmptyState(actionLabel/onAction) -> Show all; the component already supports an action, so no new widget is needed."
---

<objective>
Assemble the panel: day separators, one surface with hairlines, two genuinely different empty
states, an honest footer — and remove the last of the constraint-derived sizing from this file.

Purpose: two of sketch 010's symptoms are still live after 12-03 and 12-04. Eight rows reading
`a day ago` carry no information, and the app currently shows the SAME copy for "this wallet has
never transacted" and "this filter matched nothing" — the second of which reads as a broken
load. Sketch 014 makes the second name the filter, state how many transactions DO exist, and
offer Show all instead of Buy GNUS. This plan is also where the file's `AutoSizeText` and its
`textScale`-derived font size go, which matters because this panel sits in the same
height-starved column whose constraint-derived font size froze the app in `37639d5`.

Output: a slim view whose body is day-grouped, two distinct empty branches, a fixed-size footer,
and a dev fixture that can actually exercise all seven types for the walk.

Requirements covered: TX-08 (day separators + timestamps), TX-10 (one surface, hairlines),
TX-11 (two empty states).

NOTE: per `./CLAUDE.md` this repo's commit gate is held by the user. Do NOT create commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/ROADMAP.md
@.planning/sketches/010-transaction-row-anatomy/README.md
@.planning/sketches/014-transactions-final/README.md
@.planning/sketches/014-transactions-final/index.html

@lib/dashboard/home/widgets/transactions_slim_view.dart
@lib/dashboard/home/widgets/transaction_utils.dart
@lib/components/feedback/gw_empty_state.dart
@lib/components/coins/view/coins_screen.dart
@lib/dev/dev_mock_transactions.dart
@lib/dev/dev_tools_bubble.dart
</context>

<interface_context>
Verified at HEAD:

- `GWEmptyState({icon, required title, message, actionLabel, onAction})` ALREADY supports an
  action button — `actionLabel` + `onAction` render a `GWButton(variant: secondary)`. Do not
  build a new empty-state widget for "Show all". It also has a shape-aware compact mode that
  already accounts for the action block's height in its threshold, so an action-bearing empty
  state in the short dashboard panel is a case it handles.
- `_TransactionsSlimViewState` currently declares `with WidgetsBindingObserver` and overrides
  `didChangeMetrics`, but there is NO `initState` and `WidgetsBinding.instance.addObserver` is
  never called — the observer is never registered and `didChangeMetrics` never fires. It is dead
  code; delete the mixin and the override rather than wiring it up.
- The footer currently uses `AutoSizeText` with
  `fontSize: MediaQuery.textScalerOf(context).scale(labelMd.fontSize!)`. Both go.
- `DashboardScrollContainer` (`dashboard_screen.dart:308`) gives this panel a bounded height
  with `space6` padding and NO scroll view of its own, so the `Expanded` list is legal and the
  panel height really is the scarce resource.
- The dev fixture is `DevMockTransactions.instance.batch(isSgnus:)`, driven by the
  "Mock txns" button in `lib/dev/dev_tools_bubble.dart`'s MOCK section. Its current batch is
  8 transactions all within 8 minutes of a fixed `_base`, and it contains NO `process`
  transaction and no successful `purchase`.
- 12-02 supplies `groupTransactionsByDay`, `TxDay`, `txDayLabel`, `txTimeLabel`.
  12-04 supplies the 10-value `Filters`, its groupings and `filterCounts`.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Day-grouped body, two empty states, honest footer, no derived sizes</name>
  <files>lib/dashboard/home/widgets/transactions_slim_view.dart, test/dashboard/transaction_filters_test.dart</files>
  <action>
Rebuild the body of `_TransactionsSlimViewState.build` below the title row.

(a) NAMES. You already factored the SGNUS-scoped list out in 12-04. Use it consistently:
`scoped` = the SGNUS-scoped list with NO filter applied; `txs` = `scoped` with
`selectedFilter.matches` applied. `scoped.length` is the number the filtered-empty state quotes
and the number the overflow counts describe — sourcing both from one getter is what stops them
disagreeing.

(b) HEADER RULE. Immediately after `GWSectionTitle` and before the body, emit
`Divider(height: 1, thickness: 1, color: gw.borderSubtle)` — the same hairline the rows use, per
sketch 014's panel. Flag it in the summary: the Assets panel has no such rule, so 12-06's walk
must judge whether the two panels still read as one system with it there.

(c) THREE BRANCHES inside the `Expanded`:

  BRANCH 1 — `scoped.isEmpty` (the wallet has never transacted, within this scope):
  `GWEmptyState(icon: Icons.receipt_long_outlined, title: 'No transactions yet', message: 'Your
  sends, receives and swaps will appear here.')`. Copy unchanged from today; no action button —
  this branch is the one that is allowed to be a dead end.

  BRANCH 2 — `scoped.isNotEmpty && txs.isEmpty` (the filter matched nothing):
  `GWEmptyState(icon: Icons.filter_alt_outlined, title: filteredEmptyTitle(selectedFilter),
  message: filteredEmptyMessage(scoped.length), actionLabel: 'Show all', onAction: () =>
  setState(() => selectedFilter = Filters.all))`.
  Add two top-level pure helpers so this copy is testable:
    - `String filteredEmptyTitle(Filters f)` -> "No " + the label lowercased + " transactions",
      e.g. "No swapped transactions".
    - `String filteredEmptyMessage(int total)` -> "You have " + total + " transaction" +
      (total == 1 ? "" : "s") + ", but none match this filter."
  The whole point is that this must NOT read like branch 1 and must NOT offer "Buy GNUS" — a
  user whose filter matched nothing does not have an empty wallet, and telling them to buy
  tokens is how the current shared copy reads as a broken load.

  BRANCH 3 — the list. Call `groupTransactionsByDay(txs)` once, then flatten it into a single
  `List<Widget>`-shaped entry list before handing it to `ListView.builder` (build the flat list
  in the `build` body, not inside `itemBuilder`, so no index arithmetic runs per frame):
    - for each `TxDay`, first a day header:
      `Padding(padding: EdgeInsets.fromLTRB(space6, <top>, space6, space2), child:
      Text(day.label.toUpperCase(), style: GeniusWalletTypography.labelMd.copyWith(color:
      gw.textSecondary, fontWeight: FontWeight.w600, letterSpacing: 0.7)))`, where `<top>` is
      `space2` for the FIRST day and `space8` for every later one — the first header must not
      double up against `GWSectionTitle`'s own `space8` bottom gap.
    - then each transaction as `TransactionRow(tx: ..., onTap: () =>
      showTransactionDetails(context, ...))`, with `Divider(height: 1, thickness: 1, color:
      gw.borderSubtle)` after every row EXCEPT the last one of its day — the day header is the
      separator at a day boundary, so a divider there would double the rule.
    - `ListView.builder` with `padding: EdgeInsets.zero` so the hairlines stay full-bleed.
    The sketch sets day labels at 11px; use the 13px `labelMd` step instead, for the same reason
    12-03 did — `genius_wallet_typography.dart` records the floor was deliberately raised from
    12 to 13 because 12px read too small on a touchscreen. Record the deviation.

(d) FOOTER. Keep it only in branch 3 (both empty branches render no footer, matching the
sketch). Replace the `AutoSizeText` with a plain
`Text('${txs.length} transaction${txs.length == 1 ? '' : 's'}', style:
GeniusWalletTypography.labelMd.copyWith(color: gw.textSecondary))` inside the existing
`Align(alignment: centerRight)` and the existing `SizedBox(height: space8)` gap. The count
phrasing changes from "Transactions: 8" to "8 transactions" per sketch 014.

(e) FREEZE SWEEP — this file must end with NONE of the following: the `auto_size_text` import,
any `AutoSizeText`, any `MediaQuery.textScalerOf(...).scale(...)` feeding a `fontSize`, any
`FittedBox`. Also delete the dead `with WidgetsBindingObserver` mixin and the `didChangeMetrics`
override — the observer is never registered, so the callback never fires and removing it changes
no behaviour. The only value in this file derived from layout stays the `compact` boolean from
12-04.

(f) TESTS — append a group to `test/dashboard/transaction_filters_test.dart` covering the two
pure copy helpers: `filteredEmptyTitle(Filters.swap)` names the filter;
`filteredEmptyMessage(1)` is singular and `filteredEmptyMessage(8)` is plural; and — the point
of the requirement — assert `filteredEmptyTitle`/`filteredEmptyMessage` output differs from the
never-transacted title/message strings, so the two states can never be collapsed back into one
by a future edit.
  </action>
  <verify>
    <automated>flutter test test/dashboard/transaction_filters_test.dart</automated>
    <automated>flutter analyze lib/dashboard/home/widgets</automated>
    <automated>grep -v '^ *//' lib/dashboard/home/widgets/transactions_slim_view.dart | grep -c AutoSizeText</automated>
  </verify>
  <done>
The code-line grep returns 0 (comment lines are excluded, so a note explaining the removal does
not self-invalidate the gate). Analyze is clean. The copy-helper tests pass, including the
two-states-must-differ assertion. The list renders day headers, rows, and within-day hairlines,
with a right-aligned "N transactions" footer only when rows exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend the dev mock batch so all seven types and three days are walkable</name>
  <files>lib/dev/dev_mock_transactions.dart</files>
  <action>
The existing 8-transaction fixture cannot exercise this phase's work: it has NO `process`
transaction (the "Completed job" case sketch 010 named as the worst offender), no SUCCESSFUL
purchase (only a failed one), no `cancelled` status, and every timestamp sits within 8 minutes
of one instant, so no day separator can ever appear. 12-06's walk needs all of it.

Extend `batch({required bool isSgnus})` — keep it deterministic in ORDER and CONTENT, keep every
existing transaction, keep the icon URLs null so the batch stays fully offline:

  - Change `_base` from the frozen `DateTime(2026, 7, 20, 12, 0)` to `DateTime.now()`. Document
    why in the existing doc comment: the day separators are relative to today, so a frozen base
    would render every row under one stale date and the Today / Yesterday branches would never
    be seen. Content and ordering stay deterministic; only the absolute instants float.
  - Spread the timestamps across three calendar days: roughly the first four transactions a few
    hours back (Today), the next four about a day back (Yesterday), the rest about three days
    back (a dated header). Use `_base.subtract(Duration(hours: ...))` so the buckets land on
    real distinct calendar days rather than minutes apart.
  - ADD a `TransactionType.process` transaction: GNUS, `completed`, direction `sent`, a non-zero
    `fees` (for example `0.42`) since the fee is the only economic content a job row has, and a
    recipients list with a token amount. This is the row that renders as a bare "Completed job"
    string today.
  - ADD a SUCCESSFUL `TransactionType.purchase`: GNUS, `completed`, direction `received`, amount
    around `500`. The batch currently only has the failed purchase, so the success branch of the
    purchase row has never been seen.
  - ADD a `TransactionStatus.cancelled` transaction (any type, `transfer` is fine) so the
    `failed`-filter-spans-cancelled rule and the cancelled badge are both walkable.
  - Keep transaction 8 (`123456789.123456789123456789` ETH) exactly as it is — it is the case
    that proves the amount clamp, and the walk checks it explicitly.
  - Update the doc comment's "~8 transactions" wording and its list of exercised branches to
    match the new batch.

Do NOT touch `dev_tools_bubble.dart` — the existing "Mock txns" button already calls this batch
and needs no change.
  </action>
  <verify>
    <automated>flutter analyze lib/dev/dev_mock_transactions.dart</automated>
    <automated>grep -c 'TransactionType.process' lib/dev/dev_mock_transactions.dart</automated>
  </verify>
  <done>
Analyze clean; the batch contains at least one transaction of every `TransactionType` value, a
completed purchase, a cancelled transaction, and timestamps spanning three calendar days
relative to now.
  </done>
</task>

</tasks>

<risk>
**Freeze rule (phase-wide, from `37639d5`) — this plan is where it is enforced.** The removed
`AutoSizeText` + `textScale` footer is the closest thing in this file to the pattern that caused
the freeze: a font size computed from ambient layout state, re-evaluated on every metrics change,
inside the height-starved dashboard column. Replace it with a fixed style; do not replace it with
a different auto-sizing widget.

**Panel height is the scarce resource.** Do not spend it on chrome. The day header is one 13px
line with `space2`/`space8` padding — resist adding a per-day net total (sketch 010 offered one
and it was not carried into the locked 014 spec).
</risk>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filtered vs unfiltered list -> the count the user reads | If the footer or the "you have N" number describes the wrong list, the user is told something false about their own history |
| Dev fixture -> the walked UI | A dev-only injector that reaches a release build would show fabricated transactions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-14 | Repudiation | filtered-empty copy and footer count | medium | mitigate | The "you have N" number and the overflow counts both read the single `scoped` getter; the footer counts `txs`. One getter, one meaning — asserted by the copy-helper tests and confirmed live in 12-06 |
| T-12-15 | Spoofing | dev mock transactions | medium | mitigate | Unchanged from HEAD: `DevMockTransactions` is only reachable from `dev_tools_bubble.dart` behind `kDebugMode && kShowDevTools`, both compile-time const bools, so the path constant-folds out of release. This plan adds fixture rows only and must not add a new call site |
| T-12-02 | Denial of service | footer / day header text sizing | high | mitigate | The `AutoSizeText` + `textScale`-derived font size is deleted outright, which is the specific pattern behind the `37639d5` freeze |
| T-12-SC | Tampering | package installs | low | accept | No install task; this plan REMOVES a dependency usage (`auto_size_text` in this file) rather than adding one |
</threat_model>

<verification>
- `flutter analyze lib/dashboard/home/widgets lib/dev/dev_mock_transactions.dart` reports no
  issues.
- `flutter test` shows no new failures.
- `grep -c 'auto_size_text' lib/dashboard/home/widgets/transactions_slim_view.dart` returns 0.
- `grep -c 'TransactionType.process' lib/dev/dev_mock_transactions.dart` returns at least 1.
</verification>

<success_criteria>
- The panel renders a day separator per calendar day, with a real clock time on every row.
- The never-transacted and filter-matched-nothing states have different icons, different titles,
  different messages, and only the second one offers "Show all".
- The footer reads "N transactions" and appears only when there are rows.
- No `AutoSizeText`, no `FittedBox`, no `textScale`-derived font size, and no dead
  `WidgetsBindingObserver` remain in the file.
- The dev batch covers all seven types across three calendar days.
- No commit created.
</success_criteria>

<output>
Create `.planning/phases/12-transactions-redesign/12-05-SUMMARY.md` when done.
Record: whether the header rule under `GWSectionTitle` reads consistently against the Assets
panel (which has none), the exact empty-state copy shipped, and the composition of the extended
dev batch so 12-06's walk recipe can name specific rows.
</output>
</content>
</invoke>
