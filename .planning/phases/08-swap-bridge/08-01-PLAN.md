---
phase: 08-swap-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/components/scaffold/gw_page_header.dart
  - lib/squid_router/token_flip_button.dart
  - lib/squid_router/swap_field.dart
  - lib/squid_router/route_details_card.dart
  - lib/squid_router/token_selector_drawer.dart
  - test/squid_router/route_details_card_test.dart
autonomous: true
requirements: [SCR-04]
user_setup: []

must_haves:
  truths:
    - "GWPageHeader accepts an OPTIONAL subtitle (default null) so Swap can pass D-07's one-liner; Transactions/Markets/News/Swap-today pass no subtitle and render byte-identically. (D-07; verified: analyze lib clean + full test suite unchanged)"
    - "SwapField wears the redesign: surfaceElevated card, labelMd/textSecondary label, the D-07-locked 38px hero amount, a surfaceMenu+borderSubtle token pill, and a MAX affordance that writes the selected token's formatted balance through the EXISTING onChanged pipeline. (D-07; verified: grep gates + 08-07 walk)"
    - "SwapField exposes an optional `emptyPlaceholder` (default null) so a caller can render an em dash instead of a stale amount — the hook D-09's route-error state needs, owned by the field rather than hand-rolled in the screen. (D-09 enabler; verified: grep gate)"
    - "The USD line under an amount is ABSENT when no price is known — never a fabricated $0.00. It reuses fiatValue()/livePricesBySymbol(); no new price source is introduced. (verified: grep for fiatValue, no literal $0.00 fallback)"
    - "TokenFlipButton is a 44px brandCta-gradient rounded square with a textOnBrand glyph and a surfaceElevated border wide enough to punch the card seam; AnimatedRotation and the onFlip callback are untouched. (D-07; verified: grep brandCta + walk)"
    - "RouteDetailsCard's FOUR derived strings are byte-identical before and after the re-skin — only its paint changed. Rendered against the real mockSquidRoute constant: Price Impact = `0.51%`, Fees = `$0.30`. (D-08 / criterion 1; verified: test/squid_router/route_details_card_test.dart)"
    - "TokenSelectorDrawer's list rows read in GWColors vocabulary and still route through ResponsiveDrawer.show. (verified: grep ResponsiveDrawer retained + analyze)"
  artifacts:
    - "lib/components/scaffold/gw_page_header.dart — additive optional `subtitle` parameter"
    - "lib/squid_router/swap_field.dart — re-skinned field: GW surface, 38px amount, MAX, optional USD line, GW token pill"
    - "lib/squid_router/token_flip_button.dart — seam-ready 44px brand flip control"
    - "lib/squid_router/route_details_card.dart — paint-only re-skin, derivations untouched"
    - "lib/squid_router/token_selector_drawer.dart — GW-vocabulary list rows"
    - "test/squid_router/route_details_card_test.dart — NEW golden gate on the four derived strings (criterion 1)"
  key_links:
    - "GWPageHeader.subtitle default null ↔ every existing caller renders unchanged (additive-only contract)"
    - "MAX affordance ↔ the existing onChanged/_debouncedFetchRoute pipeline (MAX must not open a second quote path)"
    - "RouteDetailsCard derivations ↔ mockSquidRoute constant (criterion 1's whole meaning — D-18b)"
  prohibitions:
    - "Do NOT change RouteDetailsCard's four derivations (pricing / slippage passthrough / priceImpact / totalFeesUsd→fees). D-08: the figures are mechanics; only paint changes."
    - "Do NOT write a test that asserts the quote VARIES by token pair or amount. `SquidTokenService.getRoute()` returns one hardcoded constant regardless of input (D-18b) — such a test asserts something this app has never done."
    - "Do NOT touch lib/squid_router/squid_token_service.dart. The mocking predates the redesign and is out of scope."
    - "Do NOT add an input formatter to SwapField's TextField. The missing formatter is a PRE-EXISTING gap (flag, do not fix) — re-skin never restructures."
    - "Do NOT make GWPageHeader's subtitle required, and do NOT add a subtitle to any other screen's header in this plan."
    - "Do NOT use the accent gradient for field borders or the token pill. Accent is reserved for the CTA, the flip control, and MAX (UI-SPEC Color)."
    - "Do NOT reference the token `brandFill` or `statusErrorFill` — NEITHER EXISTS in this codebase (UI-SPEC names them speculatively). Use GeniusWalletColors.brandPrimarySubtle / brandPrimaryOnSurface, and statusError.withValues(alpha: 0.12)."
    - "Do NOT hardcode a sketch hex, and do NOT bind any colour to a mode-invariant constant where an appearance-aware GWColors getter exists. D-15 requires WCAG AA in BOTH modes and ALL states; light-mode statusSuccess/statusError are deliberately darkened relative to their dark-mode values, so the getters are the only correct source. Contrast is judged at the 08-07 walk, but binding to the wrong token here guarantees a failure there."
---

<objective>
Re-skin the swap tab's component family — the shared page header (additive subtitle), the amount field,
the flip control, the route-details card and the token picker — so plan 08-03 can assemble sketch 105 A1
out of parts that already wear the redesign. Lock ROADMAP criterion 1's real meaning with a golden test:
the route/fee/slippage figures a re-skinned RouteDetailsCard prints are byte-identical to develop's.

Purpose: interface-first. 08-03 (swap tab) and 08-04 (bridge, the "swap-twin") both consume this
vocabulary; establishing it once here is what keeps the two screens from diverging.

Output: five re-skinned/extended widgets plus a new regression test that is the automated half of
criterion 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-swap-bridge/08-CONTEXT.md
@.planning/phases/08-swap-bridge/08-RESEARCH.md
@.planning/phases/08-swap-bridge/08-UI-SPEC.md
@.planning/sketches/105-swap-tab/README.md

# Re-skin targets
@lib/components/scaffold/gw_page_header.dart
@lib/squid_router/swap_field.dart
@lib/squid_router/token_flip_button.dart
@lib/squid_router/route_details_card.dart
@lib/squid_router/token_selector_drawer.dart

# Design vocabulary
@lib/theme/gw_colors.dart
@lib/theme/genius_wallet_typography.dart
@lib/theme/genius_wallet_consts.dart
@lib/components/cards/gw_card.dart
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add GWPageHeader's optional subtitle and re-skin the flip control for the seam</name>
  <files>lib/components/scaffold/gw_page_header.dart, lib/squid_router/token_flip_button.dart</files>
  <read_first>
    - lib/components/scaffold/gw_page_header.dart (whole file — 41 lines; note it OWNS the space8 gap below itself).
    - lib/squid_router/token_flip_button.dart (whole file — 41 lines).
    - 08-UI-SPEC.md § Typography (Body 14px/bodySm for the subtitle) and § Screen Contracts → "Swap tab" (the subtitle bullet), § Radius (the flip control's 44px + radiusMd + 4–5px seam border).
    - lib/theme/genius_wallet_gradient.dart (brandCta) and lib/theme/genius_wallet_colors.dart (textOnBrand).
  </read_first>
  <action>
    (a) GWPageHeader: add a third, OPTIONAL `final String? subtitle;` constructor parameter defaulting to
    null. When non-null, render it directly under the title Row as `GeniusWalletTypography.bodySm` in
    `gw.textSecondary`, separated by space2, BEFORE the header's existing trailing space8 gap. When null the
    widget must produce exactly the widget tree it produces today — this is additive-only; every existing
    caller (Transactions, Markets, News, and Swap as it stands right now) passes no subtitle and must render
    unchanged. Keep the fail-soft `Theme.of(context).extension&lt;GWColors&gt;() ?? GWColors.dark()` read and
    the existing headlineLg title style untouched.

    (b) TokenFlipButton: replace the mini FloatingActionButton with a 44×44 tap target built from
    InkWell + Container so it can sit in the card seam without Material's own margin/elevation. Decoration:
    `GeniusWalletGradient.brandCta` fill, `BorderRadius.circular(GeniusWalletConsts.radiusMd)`, and a 5px
    solid border in the appearance-aware `gw.surfaceElevated` so the control visually punches a hole in the
    seam between the two cards (UI-SPEC § Radius). Glyph: `Icons.swap_vert` in
    `GeniusWalletColors.textOnBrand`, size 20. PRESERVE the AnimatedRotation wrapper, the 400ms
    easeInOut duration, the `_rotationTurns += 1` increment, and the `widget.onFlip()` call verbatim — the
    flip is mechanics. Drop the Material green-accent fill and the deepBlueTertiary glyph colour; every
    colour resolves through GWColors / the brand tokens, never a raw Material palette entry.
    Add a Semantics label ("Flip tokens") since the FAB's built-in semantics go away with it.
  </action>
  <verify>
    <automated>flutter analyze lib/components/scaffold/gw_page_header.dart lib/squid_router/token_flip_button.dart</automated>
    <automated>rg -q "this.subtitle" lib/components/scaffold/gw_page_header.dart &amp;&amp; rg -q "String\? subtitle" lib/components/scaffold/gw_page_header.dart &amp;&amp; rg -q "brandCta" lib/squid_router/token_flip_button.dart &amp;&amp; rg -q "AnimatedRotation" lib/squid_router/token_flip_button.dart &amp;&amp; echo PASS</automated>
    <automated>flutter analyze lib</automated>
  </verify>
  <acceptance_criteria>
    `subtitle` is present, nullable, and defaults to null; no existing GWPageHeader call site was edited.
    TokenFlipButton renders a 44px brand-gradient rounded square with a surfaceElevated seam border, keeps
    AnimatedRotation + onFlip verbatim, and carries no Material-palette colour literal.
    `flutter analyze lib` reports no new issue over the 61-issue baseline.
  </acceptance_criteria>
  <done>Both widgets compile; analyze holds at baseline; the flip control's rotate animation and callback are unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin SwapField — 38px hero amount, MAX affordance, optional USD line</name>
  <files>lib/squid_router/swap_field.dart</files>
  <read_first>
    - lib/squid_router/swap_field.dart (whole file — 153 lines; note the raw Card + Material white-alpha text colours and the balance line at :132-143).
    - 08-UI-SPEC.md § Typography (the LOCKED 38px display one-off and its `numericDisplay.copyWith(fontSize: 38, height: 1.0)` implementation note), § Color (MAX = brand wash + brand-on-surface text; token pill stays surfaceMenu/borderSubtle), § Spacing (space8 horizontal / space10 vertical card padding), § Component Inventory → "Swap field".
    - lib/squid_router/models/squid_balance.dart (the `formattedBalance` extension — the MAX source value).
    - lib/dashboard/home/widgets/transaction_utils.dart lines 111-135 (`fiatValue` + `livePricesBySymbol` — the ONLY price source this plan may use).
    - lib/components/cards/gw_card.dart (GWCard's padding/radius/background API).
  </read_first>
  <action>
    Re-skin SwapField in place; it stays a StatelessWidget with the same seven constructor parameters plus
    the additions below. Behaviour stays develop's.

    (a) SURFACE: swap the raw `Card` + legacy deep-blue fill for `GWCard` (or GWDecorations.surface) on
    `gw.surfaceElevated`, `GeniusWalletConsts.radiusLg`, padding space8 horizontal / space10 vertical, with
    a `gw.borderSubtle` hairline. Keep the existing horizontal margin rhythm to whatever 08-03's column
    supplies — do not hardcode a page margin inside the field.

    (b) LABEL: "You Pay" / "You Receive" as `GeniusWalletTypography.labelMd` in `gw.textSecondary`.

    (c) HERO AMOUNT: the TextField's style becomes
    `GeniusWalletTypography.numericDisplay.copyWith(fontSize: 38, height: 1.0)` in `gw.textPrimary`, hint
    "0.0" in `gw.textPrimary38`, border still `InputBorder.none`. This 38px is a D-07-locked one-off — do
    not propagate it anywhere else. Keep `keyboardType: numberWithOptions(decimal: true)`, the passed-in
    controller, and `onChanged: onChanged` exactly as they are.

    (d) TOKEN PILL: fill `gw.surfaceMenu` with a `gw.borderSubtle` hairline (NOT a brand fill), symbol in
    `GeniusWalletTypography.titleMd`/`gw.textPrimary`, chevron in `gw.textSecondary`, and keep the 32px
    ClipOval logo with its existing errorBuilder fallback. The InkWell still opens
    `TokenSelectorDrawer.show(...)` with the same three arguments.

    (e) BALANCE LINE: `labelMd` in `gw.textSecondary`, same "&lt;formattedBalance&gt; &lt;symbol&gt;" content, same
    "" when no balance is known.

    (f) NEW — MAX affordance: render a compact "MAX" button next to the balance line ONLY when
    `isSelectingFrom` is true AND `selectedToken?.balance != null`. Fill = a low-alpha brand wash
    (`GeniusWalletColors.brandPrimarySubtle`), label = `labelMd` in
    `GeniusWalletColors.brandPrimaryOnSurface`, radius `radiusSm`. On tap it must set
    `controller.text = selectedToken!.balance!.formattedBalance` AND call
    `onChanged(controller.text)` so the screen's EXISTING debounce/quote pipeline fires. It must NOT call
    any service directly and must NOT introduce a second quote path.

    (g) NEW — USD line: under the amount row, render `GeniusWalletTypography.bodySm` /
    `gw.textSecondary` fiat text derived from `fiatValue(symbol: selectedToken!.symbol, amount:
    double.tryParse(controller.text) ?? 0, pricesBySymbol: livePricesBySymbol())`. When that returns null
    (no price known for the symbol) render NOTHING — omit the line entirely. Never substitute a zero
    placeholder; an invented fiat figure is the exact defect the shipped transaction rows already refuse.
    Because the field is stateless and reads `controller.text`, the line updates on the parent's setState
    (which already fires on every onChanged) — do not add a listener or convert the widget to stateful.

    (h) NEW — `final String? emptyPlaceholder;` optional parameter, default null. When non-null AND
    `controller.text` is empty, the amount slot renders that string in the 38px hero style coloured
    `gw.textPrimary38` INSTEAD of the TextField's hint. This is the hook plan 08-03 needs for D-09's
    route-error state, where the "You Receive" field must show an em dash rather than a stale number — it
    exists here so the field stays the single owner of its own amount presentation. Default null keeps every
    current call site rendering exactly as it does today; do not use it anywhere in this plan.

    Every colour resolves through GWColors or the brand tokens; no Material-palette or legacy deep-blue
    literal survives in this file.
  </action>
  <verify>
    <automated>flutter analyze lib/squid_router/swap_field.dart</automated>
    <automated>rg -q "fontSize: 38" lib/squid_router/swap_field.dart &amp;&amp; rg -q "MAX" lib/squid_router/swap_field.dart &amp;&amp; rg -q "fiatValue" lib/squid_router/swap_field.dart &amp;&amp; rg -q "formattedBalance" lib/squid_router/swap_field.dart &amp;&amp; rg -q "TokenSelectorDrawer.show" lib/squid_router/swap_field.dart &amp;&amp; rg -q "String\? emptyPlaceholder" lib/squid_router/swap_field.dart &amp;&amp; echo PASS</automated>
    <automated>rg -v '^\s*//' lib/squid_router/swap_field.dart | rg -c "deepBlueCardColor|Colors\.white70|Colors\.white54|Colors\.white30"; test $? -ne 0 || echo "LEGACY COLOUR LITERAL STILL PRESENT"</automated>
    <automated>flutter analyze lib</automated>
  </verify>
  <acceptance_criteria>
    38px hero amount present via numericDisplay.copyWith; MAX renders only for the pay side with a known
    balance and drives the existing onChanged; the USD line is omitted (not zeroed) when no price is known;
    token pill uses surfaceMenu + borderSubtle, not brand; no legacy deep-blue / white-alpha colour literal
    remains outside comments; `flutter analyze lib` stays at the 61 baseline.
  </acceptance_criteria>
  <done>SwapField compiles and renders the redesigned field; MAX and the fiat line behave as specified; visual/contrast fidelity is proven at the 08-07 walk, not here.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Golden-lock RouteDetailsCard's figures (criterion 1), then paint it; re-skin the token picker rows</name>
  <files>test/squid_router/route_details_card_test.dart, lib/squid_router/route_details_card.dart, lib/squid_router/token_selector_drawer.dart</files>
  <read_first>
    - lib/squid_router/route_details_card.dart (whole file — 80 lines; the four derivations live at :26-33).
    - lib/squid_router/squid_token_service.dart lines 92-148 (the `mockSquidRoute` constant this card is always fed: aggregatePriceImpact `0.51`, ONE feeCost with amountUSD `0.3048`).
    - lib/squid_router/token_selector_drawer.dart (whole file — 145 lines; it already routes through ResponsiveDrawer.show at :25).
    - 08-RESEARCH.md § "The Squid quote pipeline" and Pitfall 2 (why this test must be constant-in/constant-out).
    - test/dashboard/ for the project's existing flutter_test widget-test conventions (MaterialApp wrapper + theme extension setup).
  </read_first>
  <action>
    ORDER MATTERS. Write and run the test BEFORE re-skinning the card, so it captures develop's figures and
    then proves the re-skin did not disturb them.

    (a) Create `test/squid_router/route_details_card_test.dart`. Pump `RouteDetailsCard` inside a
    MaterialApp carrying the app theme, fed the REAL `mockSquidRoute` constant imported from
    `squid_token_service.dart` (assert against the actual constant the app uses, not a local copy), with
    `fromAmount: '1'`, `toAmount: '0.995'`, a from/to SquidTokenInfo pair whose symbols are 'ETH'/'USDT',
    and `slippage: '0.5'`. Assert the four rendered strings via `find.text`:
      • Pricing  → `1 ETH ~ 0.995 USDT`
      • Slippage → `0.5` (a straight passthrough of the caller's string)
      • Price Impact → `0.51%`
      • Fees → `$0.30`  (0.3048 through toStringAsFixed(2))
    Also assert the four row LABELS render. Run the test against the UNMODIFIED card first: it must pass.
    If any expectation above disagrees with what the current card actually prints, the CURRENT OUTPUT WINS —
    correct the expectation to develop's real value and note it in the SUMMARY; the point of this test is to
    freeze develop's behaviour, not to impose the planner's arithmetic.

    (b) Now re-skin `route_details_card.dart` — PAINT ONLY. `Card` → `gw.surfaceElevated` container with a
    `gw.borderSubtle` hairline and `GeniusWalletConsts.radiusMd`; row label `labelMd`/`gw.textSecondary`,
    row value `labelMd`/`gw.textPrimary`; the Price Impact VALUE in `gw.statusSuccess`; dividers in
    `gw.borderSubtle`; row padding space10 horizontal / space6 vertical. The four derivation expressions at
    the top of `build()` and the four `_row(...)` labels and their order are untouched. Re-run the test —
    it must still pass with the same expectations.

    (c) TokenSelectorDrawer: bring the list rows into GW vocabulary — row fill `gw.surfaceMenu`, primary
    text `gw.textPrimary`, secondary/balance text `gw.textSecondary`, the search field's fill and hint via
    GWColors instead of the black/white-alpha literals, and the logo fallback container on
    `gw.surfaceMenu`. It must still call `ResponsiveDrawer.show` (do not swap the shell) and its filter /
    onTokenSelected behaviour is unchanged.
  </action>
  <verify>
    <automated>flutter test test/squid_router/route_details_card_test.dart</automated>
    <automated>rg -q "mockSquidRoute" test/squid_router/route_details_card_test.dart &amp;&amp; rg -q "0.51%" test/squid_router/route_details_card_test.dart &amp;&amp; rg -q "statusSuccess" lib/squid_router/route_details_card.dart &amp;&amp; rg -q "ResponsiveDrawer.show" lib/squid_router/token_selector_drawer.dart &amp;&amp; echo PASS</automated>
    <automated>flutter analyze lib/squid_router/route_details_card.dart lib/squid_router/token_selector_drawer.dart</automated>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    The new test passes against the card BOTH before and after the re-skin, asserting the four figures
    derived from the real `mockSquidRoute`; it contains no assertion that the quote varies by input.
    RouteDetailsCard's four derivations are textually unchanged. TokenSelectorDrawer still uses
    ResponsiveDrawer.show. Full `flutter test` is 248 pass / 1 known pre-existing failure — no new red.
  </acceptance_criteria>
  <done>Criterion 1's automated half exists and is green; the route card and token picker wear the redesign with develop's figures intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SquidTokenService (mock constants) → UI | Token symbols / logo URIs reach text layout and `Image.network`. Today the data is a compile-time constant, so it is not attacker-controlled; it becomes untrusted only when the real API is wired (out of scope, D-01) |
| user keyboard → amount field | Free-text numeric input reaches `double.tryParse` and the quote pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | SwapField USD line | low | mitigate | `fiatValue` returns null for an unknown symbol and the line is OMITTED — no fabricated `$0.00` that a user could read as a real valuation |
| T-08-02 | Tampering | SwapField amount TextField (no input formatter) | low | accept | Pre-existing gap on develop (soft-keyboard hint only). All consumers go through `double.tryParse`, which fails closed. Fixing it is a restructure, not a re-skin — flagged, not fixed (RESEARCH "Don't Hand-Roll") |
| T-08-03 | Information Disclosure | RouteDetailsCard figures | low | mitigate | Paint-only change plus a golden test freezing all four derived strings — a re-skin cannot silently alter a displayed financial figure |
| T-08-04 | Tampering | `Image.network(selectedToken.logoURI)` | low | accept | Existing behaviour with an errorBuilder fallback; the URI source is a local constant this phase does not change |
| T-08-SC | Tampering | npm/pip/cargo/pub installs | n/a | accept | This plan adds ZERO packages — every primitive already ships on this branch. RESEARCH records the Package Legitimacy Audit as N/A; gate not applicable |
</threat_model>

<verification>
- `flutter analyze lib` stays at or below the 61-issue baseline (compile gate, never visual evidence).
- `flutter test test/squid_router/route_details_card_test.dart` passes — this is criterion 1's automated half.
- Full `flutter test` remains 248 pass / 1 known pre-existing failure (`local_wallet_storage_test.dart`, entirely commented out).
- Grep gates: subtitle is nullable and additive; 38px hero amount, MAX and `fiatValue` present in SwapField; brandCta + AnimatedRotation present in the flip control; ResponsiveDrawer retained in the token picker; no legacy colour literals outside comments.
- Visual fidelity to sketch 105 A1 and WCAG AA in both modes are NOT asserted here — they are earned at the 08-07 walk.
</verification>

<success_criteria>
The swap tab's component family wears the redesign, GWPageHeader can carry D-07's subtitle without
disturbing any other screen, and criterion 1 has a real automated regression gate proving the route, fee
and slippage figures are byte-identical to develop's.
</success_criteria>

<output>
Create `.planning/phases/08-swap-bridge/08-01-SUMMARY.md` when done.

## Artifacts this plan produces (08-01)
- Modified: `lib/components/scaffold/gw_page_header.dart` (additive optional subtitle)
- Modified: `lib/squid_router/swap_field.dart`, `token_flip_button.dart`, `route_details_card.dart`, `token_selector_drawer.dart`
- Created: `test/squid_router/route_details_card_test.dart`
No new packages. `squid_token_service.dart` untouched. Record in the SUMMARY: the exact four golden
strings the test locked, and whether any differed from this plan's predicted values.
</output>
