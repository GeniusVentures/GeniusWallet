---
phase: 08-swap-bridge
plan: 06
type: execute
wave: 4
depends_on: ["08-04", "08-05"]
files_modified:
  - lib/dashboard/bridge/bridge_receipt.dart
  - test/dashboard/bridge/bridge_receipt_test.dart
  - lib/dashboard/bridge/bridge_screen.dart
autonomous: true
requirements: [SCR-04]
user_setup: []

must_haves:
  truths:
    - "CRITERION 4: a bridge result shows its success/error toast AND the 031-B result receipt — both, in that order, never one instead of the other. The inline AlertDialog is gone. (finding 28 / D-04; verified: grep + 08-07 walk)"
    - "The bridge Transaction is SYNTHESIZED for display by a pure factory and REUSES an existing TransactionType (`mint`). `TransactionType` gained no value and nothing under `packages/` was edited. (D-19; verified: unit test + a git-clean gate on packages/)"
    - "The receipt's headline amount reads as the bridged GNUS amount (coinSymbol is the pay token's symbol and the single recipient entry carries the amount the shared derivation renders), and Status maps directly from the bridgeOut response's isSuccess — bridge has a REAL success/failure branch, unlike swap. (verified: unit test)"
    - "No Network Fee row is claimed on the bridge receipt. The only fee datum bridge has is a gas PRICE in Gwei of the SOURCE chain's native token, which the shared receipt would compose against the GNUS symbol — a doubled unit or a false magnitude either way. The gas figure stays on the bridge screen's own gas card. (verified: unit test asserting blank fees + 08-05's row guard)"
    - "The bridge result is NOT persisted — no TransactionsCubit call, no TransactionStorageService call. Bridge results are not persisted today, and adding that would give the Phase 12/15 Transactions surface a new row type mid-flight. (RESEARCH recommendation, adopted; verified: negative grep)"
    - "Bridge mechanics still untouched: bridgeOut with all seven arguments including shouldMintTokens, the mounted guard, and the ToastManager call are exactly as develop wrote them. (D-11; verified: grep gates)"
    - "The failure branch no longer loses the response's errorMessage: it is surfaced in the toast when present, because the shared receipt has no free-text error slot and the AlertDialog used to show it."
    - "DOCUMENTED BEHAVIOUR CHANGE: dismissing the receipt no longer pops the /bridge route. The user stays on the bridge screen with its amount fields reset. This is recorded, not smuggled, and is put to the human at the 08-07 walk. (RESEARCH Assumptions Log A4)"
  artifacts:
    - "lib/dashboard/bridge/bridge_receipt.dart — NEW pure factory building the display-only Transaction"
    - "test/dashboard/bridge/bridge_receipt_test.dart — NEW: the full field mapping, both outcomes"
    - "lib/dashboard/bridge/bridge_screen.dart — AlertDialog replaced by the shared receipt; dead helpers and imports cleaned"
  key_links:
    - "bridgeReceiptTransaction() ↔ showTransactionDetails (its only consumer; the factory must produce exactly what the shared derivation needs)"
    - "blank fees ↔ 08-05's Network-Fee-row guard (without that guard this receipt renders a bare symbol — which is precisely why this plan depends on 08-05)"
    - "TransactionType.mint ↔ transaction_utils' badge/action/subtitle derivation (mint badge, 'Minted', 'Minted to wallet') — the reuse D-19 chose, with the badge-copy cost knowingly accepted"
  prohibitions:
    - "Do NOT add a value to `TransactionType`, and do NOT edit ANY file under `packages/` (D-19). That enum is Hive-persisted with explicit field indices, so a new value is a persisted-schema change — out of bounds for a re-skin phase. RESEARCH recommends the opposite (its Option C); D-19 is a POST-RESEARCH correction and OVERRIDES it."
    - "Do NOT add a case, badge kind, or copy arm to `transaction_utils.dart` or `transaction_badge.dart`. The entire point of reusing an existing type is that no Phase-12-owned switch has to grow."
    - "Do NOT persist the bridge Transaction. No cubit call, no storage call. That would add a new row type to the Phase 12/15 Transactions surface — a product decision, not a re-skin."
    - "Do NOT design a new receipt or extend showTransactionDetails beyond 08-05's already-landed blank-fee guard. D-03 reuses the shipped 031-B implementation as-is; the sketch's fuller pill/sections/badge treatment is explicitly NOT the target."
    - "Do NOT author pending or confirming copy. bridgeOut returns a settled success or failure; there is no pending bridge state to describe."
    - "Do NOT change bridgeOut's arguments, drop shouldMintTokens, remove the mounted guard, or move the toast call after the receipt call. The toast fires first, exactly as it does today."
    - "Do NOT set exchangeRate on the synthesized Transaction. Bridge is 1:1 and has no rate; leaving it unset is what correctly suppresses the Rate row."
    - "Do NOT pop the /bridge route when the receipt is dismissed, and do NOT try to await showTransactionDetails — it returns void. The navigation change is deliberate; record it."
    - "Do NOT touch lib/reown/swap_result_drawer.dart or lib/reown/handle_dapp_requests.dart (D-05, Phase 10)."
---

<objective>
Close ROADMAP criterion 4: a bridge result shows its toast alongside the result receipt (finding 28). Replace
bridge's inline AlertDialog with the same 031-B receipt the swap tab now uses — which requires SYNTHESIZING a
Transaction, because bridge_screen.dart has never built one.

Purpose: D-04 reads as a drop-in swap and genuinely is for the swap tab. The symmetry breaks for bridge
(RESEARCH Pitfall 6): there is no `transaction` local here, TransactionType has no bridge value, and D-19
forbids adding one. This plan is that real work, done in a pure tested factory rather than inline in a submit
closure.

Output: a tested display-only Transaction factory and a bridge result path that is toast + shared receipt.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-swap-bridge/08-CONTEXT.md
@.planning/phases/08-swap-bridge/08-RESEARCH.md
@.planning/phases/08-swap-bridge/08-UI-SPEC.md
@.planning/phases/08-swap-bridge/08-04-SUMMARY.md
@.planning/phases/08-swap-bridge/08-05-SUMMARY.md

# The receipt, the derivation it feeds, and the screen
@lib/dashboard/home/widgets/transaction_displays.dart
@lib/dashboard/home/widgets/transaction_utils.dart
@lib/dashboard/bridge/bridge_screen.dart
@packages/genius_api/lib/models/transaction.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure factory for the display-only bridge Transaction (D-19)</name>
  <files>lib/dashboard/bridge/bridge_receipt.dart, test/dashboard/bridge/bridge_receipt_test.dart</files>
  <read_first>
    - packages/genius_api/lib/models/transaction.dart lines 25-140 — READ ONLY. The seven TransactionType values each carry an explicit Hive field index, which is exactly why D-19 forbids an eighth. Also note TransferRecipients and the full Transaction field list.
    - lib/dashboard/home/widgets/transaction_utils.dart lines 252-290 (mint maps to the mint badge and the action word "Minted"), lines 302-360 (for a non-swap type the TITLE is the coin symbol and the mint SUBTITLE is "Minted to wallet"), lines 419-430 (for mint the headline amount is composed from the FIRST RECIPIENT'S amount plus the coin symbol, with an incoming tone — this is why the recipients list must carry the bridged amount).
    - lib/dashboard/home/widgets/transaction_displays.dart lines 428-524 — which rows a non-swap type renders: Date, Status, a single From/To address row, Network, Network Fee (now guarded on a non-blank fee by 08-05), and Hash.
    - 08-CONTEXT.md D-19 in full: the decision, its preference for mint, its transfer fallback, and the knowingly accepted cost that the badge will not read "bridge".
    - 08-RESEARCH.md § "Drawer consolidation mechanics" — the field-mapping sketch and the three-option table. Its Option C recommendation is OVERRIDDEN by D-19.
  </read_first>
  <behavior>
    - Success: status is completed; hash is the response data string; coinSymbol is the pay token's symbol; recipients has exactly one entry whose amount is the bridged amount and whose toAddr is the wallet address; fromAddress is the wallet address; type is mint; direction is received; fees is the empty string; exchangeRate is null.
    - Failure: identical shape but status is failed, and hash is the empty string when the response carries no data.
    - A null or blank response hash never becomes the literal text "null".
    - fees is ALWAYS the empty string — the factory neither accepts nor forwards a gas figure.
    - The factory adds nothing to any cubit or storage; it is a pure constructor.
  </behavior>
  <action>
    Create `lib/dashboard/bridge/bridge_receipt.dart` exposing ONE pure function —
    `bridgeReceiptTransaction({required bool isSuccess, required String? txHash, required String
    walletAddress, required String amount, required String coinSymbol, DateTime? timestamp})` returning a
    `Transaction` per the behavior block. It takes no BuildContext, imports no Flutter UI library, and touches
    no cubit or storage. Default the timestamp to the current time but allow injection so the test is
    deterministic.

    Document at the top of the file, in a comment a future reader cannot miss:
      • WHY the type is mint rather than a dedicated bridge value — the destination-chain half of a bridge IS
        a mint and the API call asks for minting on arrival; the type enum is Hive-persisted with explicit
        field indices, so growing it is a persisted-schema change D-19 rules out. The knowingly accepted cost
        is that the badge and title copy read "Minted", not "Bridged".
      • WHY fees is blank — the only fee datum bridge has is a gas PRICE in Gwei of the SOURCE chain's native
        token, while the shared receipt composes the fee against this Transaction's own symbol; any value
        produces either a doubled unit or a false magnitude. The gas figure stays on the bridge screen's gas
        card, where its own label owns its unit.
      • THAT this record is display-only and deliberately not persisted.

    Write the test file FIRST — one expectation per behavior bullet, asserting every field explicitly (type,
    status, hash, coinSymbol, recipients length and amount and toAddr, fromAddress, direction, blank fees,
    null exchangeRate) for BOTH outcomes, with an injected timestamp. Watch it fail, then implement to green.

    If the mint reuse turns out to read badly in situ, D-19's sanctioned fallback is transfer — but that is a
    walk-time observation for 08-07, not a call to pre-empt here. Ship mint.
  </action>
  <verify>
    <automated>flutter test test/dashboard/bridge/bridge_receipt_test.dart</automated>
    <automated>rg -q "bridgeReceiptTransaction" lib/dashboard/bridge/bridge_receipt.dart &amp;&amp; rg -q "TransactionType.mint" lib/dashboard/bridge/bridge_receipt.dart &amp;&amp; rg -q "TransactionStatus.failed" lib/dashboard/bridge/bridge_receipt.dart &amp;&amp; echo PASS</automated>
    <automated>rg -q "material.dart|BuildContext|TransactionsCubit|TransactionStorageService" lib/dashboard/bridge/bridge_receipt.dart; test $? -ne 0 || echo "FACTORY IS NOT PURE OR PERSISTS"</automated>
    <automated>git status --porcelain packages/ | rg -q .; test $? -ne 0 || echo "D-19 VIOLATED: packages/ WAS MODIFIED"</automated>
    <automated>flutter analyze lib/dashboard/bridge/bridge_receipt.dart</automated>
  </verify>
  <acceptance_criteria>
    Every behavior bullet is asserted and green for both outcomes; the factory is pure (no Flutter UI import,
    no BuildContext, no cubit, no storage); fees is blank and exchangeRate is null; the type is an EXISTING
    enum value; `git status --porcelain packages/` is empty.
  </acceptance_criteria>
  <done>The bridge receipt's data mapping is provable without launching the app, and the enum-and-schema fence held.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace the bridge AlertDialog with the shared receipt, keeping the toast alongside it</name>
  <files>lib/dashboard/bridge/bridge_screen.dart</files>
  <read_first>
    - lib/dashboard/bridge/bridge_screen.dart, the submit closure as 08-04 left it: the bridgeOut call with all seven arguments, the mounted guard, the ToastManager call with its success/error titles and messages, and the showDialog/AlertDialog block that follows (including its Close action, which today pops the dialog AND pops the /bridge route).
    - lib/dashboard/bridge/bridge_receipt.dart (Task 1) and lib/dashboard/home/widgets/transaction_displays.dart (`showTransactionDetails(BuildContext, Transaction)` — it returns void and opens a ResponsiveDrawer that pops only itself).
    - 08-RESEARCH.md § "bridge_screen.dart structural map", final bullets — the post-receipt navigation question (Assumptions Log A4), stated there as an explicit planner decision point.
    - 08-UI-SPEC.md interaction locks 7 and 9 (one receipt, no orphans; the toast fires ALONGSIDE the receipt).
  </read_first>
  <action>
    (a) In the submit closure, immediately after the existing ToastManager call, build the receipt Transaction
    with `bridgeReceiptTransaction(...)` — `isSuccess` from the response, `txHash` from the response data,
    `walletAddress` from the wallet state's selected wallet address, `amount` from the pay amount controller's
    text, `coinSymbol` from the pay token's symbol falling back to `'GNUS'` (`Coin.symbol` is nullable but
    `Transaction.coinSymbol` is not, and an empty symbol would strip the unit off the receipt's headline
    amount — bridge is GNUS-only, so that fallback is factual) — and call
    `showTransactionDetails(context, tx)`. Then
    DELETE the entire showDialog/AlertDialog block. Everything above the toast is untouched: the bridgeOut
    call, its seven arguments including the mint flag, and the mounted guard.

    (b) FAILURE MESSAGE: on the failure branch, use the response's errorMessage as the toast message when it is
    non-null and non-empty, otherwise keep develop's existing failure string. The success branch's title and
    message are unchanged. The shared receipt has no free-text error slot, and the AlertDialog used to print
    this string — routing to 031-B without this would silently discard the only diagnostic a user gets from a
    real on-chain failure.

    (c) NAVIGATION — the deliberate, documented change. The old Close action popped the dialog and then popped
    the /bridge route back to the token screen. `showTransactionDetails` returns void and its drawer pops only
    itself, so that chain cannot be reproduced without awaiting a function that returns nothing. Therefore:
    do NOT pop /bridge. On SUCCESS, after showing the receipt, reset the screen — clear both amount
    controllers, null the gas estimate, clear the error flag — so the user lands back on a clean bridge screen
    rather than one still showing a completed amount. On FAILURE, leave the entered amount in place so the
    user can correct and retry. Add a short comment recording that the route pop was intentionally dropped and
    why, and put the question to the human at the 08-07 walk.

    (d) CLEANUP so analyze stays at baseline: remove the now-unused `_cryptoIcon` helper and any import that
    only the deleted dialog needed (the go_router import if `GoRouter` is no longer referenced anywhere in the
    file, the snackbar helper import if its only use was the dialog's copy action, and the breakpoints import
    if 08-04 already removed its other use). KEEP `flutter/services.dart` — DecimalTextInputFormatter needs
    it. KEEP `DecimalTextInputFormatter` itself. Verify with analyze rather than by eye; an unused import or
    private method is a NEW analyze issue and breaks the baseline gate.
  </action>
  <verify>
    <automated>rg -q "showTransactionDetails\(context," lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "bridgeReceiptTransaction" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "ToastManager.instance.showToast" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; echo "PASS: criterion 4 — toast AND receipt both present"</automated>
    <automated>rg -q "shouldMintTokens: true" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "context.mounted" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "DecimalTextInputFormatter\(\)" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "milliseconds: 300" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; echo "PASS: D-11 mechanics intact"</automated>
    <automated>rg -v '^\s*//' lib/dashboard/bridge/bridge_screen.dart | rg -c "AlertDialog|showDialog|_cryptoIcon"; test $? -ne 0 || echo "OLD RESULT DIALOG OR ITS HELPER STILL PRESENT"</automated>
    <automated>rg -q "transactionsCubit|TransactionStorageService" lib/dashboard/bridge/bridge_screen.dart; test $? -ne 0 || echo "BRIDGE RESULT IS BEING PERSISTED — NOT AUTHORISED"</automated>
    <automated>flutter analyze lib</automated>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    The toast fires first and the shared receipt follows it on BOTH outcomes; the AlertDialog, showDialog call
    and `_cryptoIcon` are gone; the failure toast carries the response error message when present; no cubit or
    storage call was added; bridgeOut's arguments, the mint flag, the mounted guard, the 300ms debounce and the
    decimal formatter are all intact; `flutter analyze lib` is at or below the 61-issue baseline (no unused
    import or unused private method); full suite 248 pass / 1 known pre-existing failure.
  </acceptance_criteria>
  <done>Criterion 4 is implemented — toast alongside receipt — with bridge's real on-chain call untouched. Whether it READS right, and whether losing the route pop is acceptable, are 08-07's questions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC bridgeOut response → receipt fields | The tx hash and error message are RPC-supplied strings reaching text layout and an explorer-URL derivation |
| synthesized display record → shared Phase-12 receipt | A Transaction that was never persisted flows through a function whose other callers read persisted records |
| Hive-persisted TransactionType enum | Adding a value would be a schema change across every stored transaction — the fence D-19 draws |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-23 | Tampering | Persisted-schema change via a new TransactionType value | high | mitigate | D-19's fence is enforced by reusing an existing value AND by a `git status --porcelain packages/` gate that fails the task if anything under packages/ was touched |
| T-08-24 | Information Disclosure | A network fee stated in the wrong unit or magnitude | medium | mitigate | fees is blank and 08-05's guard omits the row; the gas figure remains only where its own label owns its unit (the bridge gas card) |
| T-08-25 | Repudiation | Losing the on-chain failure reason | medium | mitigate | The response error message is surfaced in the failure toast, replacing the AlertDialog's error text that consolidation would otherwise discard |
| T-08-26 | Tampering | RPC-supplied hash interpolated into an explorer URL | low | accept | `getExplorerUrl` returns empty for an unmapped symbol (GNUS is unmapped), so the footer is suppressed rather than opening a wrong-chain link; the existing scheme check in the footer's launcher still applies |
| T-08-27 | Repudiation | A display-only bridge record invisible in the Transactions tab | low | accept | KNOWINGLY ACCEPTED: bridge results are not persisted today either, so this is not a regression. Persisting them is a product decision for a future todo, not a re-skin |
| T-08-28 | Denial of Service | Losing the automatic return to the token screen | low | accept | Documented behaviour change (RESEARCH A4) with a screen reset on success and an explicit question at the 08-07 walk; the back arrow still works |
| T-08-SC | Tampering | npm/pip/cargo/pub installs | n/a | accept | Zero packages added. Legitimacy gate not applicable |
</threat_model>

<verification>
- `flutter test test/dashboard/bridge/bridge_receipt_test.dart` green for both outcomes and every mapped field.
- Full `flutter test` stays 248 pass / 1 known pre-existing failure.
- `flutter analyze lib` at or below the 61-issue baseline — this is also the gate that catches the unused helper and imports the dialog's removal leaves behind.
- `git status --porcelain packages/` is EMPTY (D-19's hard fence).
- Gates: toast AND receipt both present; AlertDialog, showDialog and the icon helper gone; the mint flag, mounted guard, 300ms debounce and decimal formatter intact; no persistence call added.
- NOT evidence here: whether the receipt's "Minted" badge/title reads acceptably for a bridge in situ, and whether dropping the route pop is acceptable. Both go to 08-07's human walk, where D-19's transfer fallback stays available.
</verification>

<success_criteria>
A bridge result shows its toast alongside the shipped 031-B receipt (criterion 4), the receipt's data is
synthesized truthfully with no schema change and no fabricated fee, and every argument reaching bridgeOut is
still develop's.
</success_criteria>

<output>
Create `.planning/phases/08-swap-bridge/08-06-SUMMARY.md` when done.

## Artifacts this plan produces (08-06)
- Created: `lib/dashboard/bridge/bridge_receipt.dart`, `test/dashboard/bridge/bridge_receipt_test.dart`
- Modified: `lib/dashboard/bridge/bridge_screen.dart` (AlertDialog replaced by toast + shared receipt; dead helper and imports removed)
- NOT touched: anything under `packages/`; `lib/dashboard/home/widgets/transaction_utils.dart`; `transaction_badge.dart`; the reown path
Record in the SUMMARY: the exact rows the bridge receipt renders in situ, that the route pop was intentionally
dropped and the screen resets on success instead, and whether the "Minted" copy needs D-19's transfer
fallback (a walk decision, not a code change made here).
</output>
