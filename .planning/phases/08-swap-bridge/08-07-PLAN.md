---
phase: 08-swap-bridge
plan: 07
type: execute
wave: 5
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05", "08-06"]
files_modified: []
autonomous: false
requirements: [SCR-04]
user_setup: []

must_haves:
  truths:
    - "CRITERION 1 confirmed by eye: the swap screen renders in the redesign skin (sketch 105 A1) and a quote returns showing the SAME route, fee and slippage figures develop showed — for ANY token pair, because the quote is a hardcoded constant (D-18b). (verified: human walk, both modes)"
    - "CRITERION 2 confirmed: a forced route-fetch failure shows the red notice, the 'You Receive' field shows an em dash rather than the previous number, the route card disappears, and the CTA becomes an enabled Retry. (finding 22 / D-09; verified: human walk)"
    - "CRITERION 3 confirmed: submitting a swap produces develop's outcome — toast plus a receipt plus a persisted transaction — with NO real swap executed, and the deviation is written down in 08-CONTEXT.md D-01 and guarded in code. Recorded as the criterion's SECOND branch, never as a live swap. (D-01; verified: human walk + the decision record)"
    - "CRITERION 4 confirmed: a bridge result shows its success/error toast ALONGSIDE the result receipt. (finding 28; verified: human walk at whichever depth the developer authorises in Task 1)"
    - "CRITERION 5 confirmed: the app cold-starts with NO `!_dirty` red screen, the global swap FAB is visible on the dashboard and on pushed detail screens, and absent on /swap and on onboarding/wallet-creation surfaces. (NAV-02 / D-17; verified: human walk — this bug class is NOT reproducible under pumpWidget)"
    - "D-15 confirmed: every CTA rung, the route-error notice, the price-impact green, the MAX chip and the brand sheen hold WCAG AA in BOTH light and dark, and the disabled rungs stay visibly distinct from the enabled one without vanishing. (verified: human walk in both modes)"
    - "D-16 confirmed: the walk runs on the Windows host and needs no platform-parity split — both screens are ordinary Flutter widgets with no platform views, unlike Phase 18's webview work. One walk, one platform, no macOS re-run required."
    - "08-VERIFICATION.md exists and records each numbered check as PASS or OUTSTANDING per mode, flipping to passed ONLY when every check is earned."
  artifacts:
    - ".planning/phases/08-swap-bridge/08-VERIFICATION.md — the walk record and SCR-04's gate"
  key_links:
    - "walk evidence ↔ 08-VERIFICATION.md (flip to passed ONLY when all five criteria plus D-15 are earned in both modes)"
    - "live-bridge authorisation (Task 1) ↔ how deeply criterion 4 can be verified (a real bridgeOut moves real funds)"
  prohibitions:
    - "Do NOT auto-approve. This is a blocking human checkpoint; ignore any auto-advance setting. Every item here is something only eyes, or the developer's own risk judgement, can settle."
    - "Do NOT record criterion 3 as 'a swap executed'. It is satisfied by the DOCUMENTED-DEVIATION branch (D-01). Recording it as a live swap would be false."
    - "Do NOT invoke a real bridgeOut without the explicit authorisation captured in Task 1. bridgeOut performs a genuine burn on the source chain and a mint on the destination — real funds, real gas (RESEARCH Pitfall 4)."
    - "Do NOT try to prove the quote varies by token pair or amount. It cannot; the route is a compile-time constant (D-18b). Criterion 1 is 'the same figures as develop', not 'a live route'."
    - "Do NOT flip 08-VERIFICATION.md to passed unless every numbered check passes in BOTH appearance modes. Record any failure as OUTSTANDING for a gap-closure round."
    - "Do NOT change any source file in this plan. If the walk finds a defect, record it — fixes belong to a gap-closure plan, not to the walk."
---

<objective>
Walk the finished Phase 8 surfaces on the Windows host and settle everything only a person can: the visual
fidelity of sketch 105 A1 and 120 B1, WCAG AA in both appearance modes across every CTA rung including the
disabled ones (D-15), the route-error notice, the consolidated receipt's copy, and the cold-start crash fix.
Record the verdict to 08-VERIFICATION.md and gate SCR-04 on it.

Purpose: `flutter analyze` and the test suite are gates, never evidence. Criteria 2, 4 and 5 have no
automatable proof (the quote service is static, bridgeOut moves real funds, and the `!_dirty` bug needs
runApp's real mount timing). This plan is where they are earned.

Output: the walk record, and an explicit decision about how deeply a bridge can be exercised.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-swap-bridge/08-CONTEXT.md
@.planning/phases/08-swap-bridge/08-RESEARCH.md
@.planning/phases/08-swap-bridge/08-UI-SPEC.md
@.planning/phases/08-swap-bridge/08-01-SUMMARY.md
@.planning/phases/08-swap-bridge/08-02-SUMMARY.md
@.planning/phases/08-swap-bridge/08-03-SUMMARY.md
@.planning/phases/08-swap-bridge/08-04-SUMMARY.md
@.planning/phases/08-swap-bridge/08-05-SUMMARY.md
@.planning/phases/08-swap-bridge/08-06-SUMMARY.md
@.planning/sketches/105-swap-tab/README.md
@.planning/sketches/120-phase-8-flow/README.md
@.planning/sketches/121-phase-8-storyboard/README.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: How deeply may the bridge be exercised during this walk?</name>
  <decision>
    Whether the walk submits a REAL bridge (a genuine on-chain burn plus mint, spending real GNUS and real
    gas) in order to observe the toast-plus-receipt result path, or stops short of submitting.
  </decision>
  <context>
    Unlike swap — which deliberately executes nothing (D-01) — `bridgeOut(... shouldMintTokens: true)` and
    `getBrigeOutGasCost(...)` are REAL Web3 calls (RESEARCH Pitfall 4). Criterion 4 is about what the UI does
    AFTER a bridge returns, so observing it at full depth requires a real submission. Nothing in CONTEXT.md or
    the UI-SPEC says whether that is expected, and RESEARCH raised it as an open question rather than
    assuming either answer. Bridge is GNUS-only and gated at zero balance, so a submission needs a funded
    GNUS wallet on a configured chain.
  </context>
  <options>
    <option id="live-mainnet">
      <name>Submit a real bridge with a small GNUS amount</name>
      <pros>Criterion 4 is verified at full depth — the real success branch, the real hash on the receipt, the real toast; the failure branch can be provoked separately</pros>
      <cons>Spends real GNUS and real gas; irreversible; requires a funded wallet</cons>
    </option>
    <option id="live-testnet">
      <name>Submit on a configured testnet</name>
      <pros>Full-depth verification with no real value at risk</pros>
      <cons>Only possible if a testnet chain with bridgeable GNUS is actually configured for this wallet — confirm before choosing</cons>
    </option>
    <option id="dry-run">
      <name>Stop at the CTA-ready state; verify the receipt via the dev bubble instead</name>
      <pros>No funds moved. The dev-tools bubble's repointed receipt buttons (08-05) exercise the same shared receipt, and the gas-estimate path still runs for real, so the ladder and the gas card are verified</pros>
      <cons>The bridge-specific receipt mapping (its "Minted" badge, its rows, its hash) is inspected via a synthesized record rather than a real response; criterion 4 is recorded as partially verified</cons>
    </option>
  </options>
  <action>
    Before starting the walk, put the three options above to the developer and WAIT. Do not run the app's
    bridge submit path, and do not tap the bridge CTA in its ready state, until an answer is given. If
    `live-testnet` is chosen, first confirm a testnet chain with bridgeable GNUS is actually configured for
    this wallet — RESEARCH could not establish that one is, and an unconfigured chain would silently push the
    walk onto mainnet. Record the answer verbatim in 08-VERIFICATION.md before item 7 of the walk begins, and
    carry it into criterion 4's verdict so the depth of that verification is never ambiguous later.
  </action>
  <resume-signal>Select: live-mainnet, live-testnet, or dry-run. State the answer before the walk starts; do not submit anything until it is given.</resume-signal>
  <acceptance_criteria>
    The chosen depth is recorded verbatim in 08-VERIFICATION.md, and criterion 4's verdict states which depth
    produced it. No bridgeOut call was made before the answer was given.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Walk all five criteria plus D-15 contrast, in dark AND light</name>
  <what-built>
    Phase 8 re-skinned swap and bridge to the redesign while leaving their mechanics alone. 08-01 re-skinned
    the swap component family (field with a 38px amount, MAX and an optional USD line; seam-ready flip
    control; paint-only route card; token picker) and added an optional subtitle to the shared page header,
    with a golden test freezing the route figures. 08-02 ported `GlobalSwapFabHost` from `7a63b4f` with its
    startup guard verbatim and the nonexistent AI-FAB half stripped, and mounted it in main.dart. 08-03 laid
    the swap tab out as sketch 105 A1 (560px column, brand sheen, subtitle, flip in the card seam, the pixel
    offset hack gone), replaced the CTA with a gradient GWButton driven by a unit-tested state ladder, and
    added D-09's route-error state. 08-04 re-skinned the bridge screen to sketch 120 B1 as the swap tab's twin
    with every on-chain argument preserved. 08-05 pointed the swap result at the shipped 031-B receipt,
    deleted the three superseded drawers, repointed the dev bubble, and re-skinned the Swap Settings drawer.
    08-06 replaced bridge's AlertDialog with the same receipt, fed by a synthesized display-only Transaction.
    Swap still deliberately executes nothing (D-01), and the quote is a hardcoded constant (D-18b).
  </what-built>
  <how-to-verify>
    Build and run the Windows app (Flutter 3.41.9 at C:\Users\User\Documents\Projects\GNUS\flutter\flutter\bin\flutter.bat).
    Walk every numbered item in BOTH dark and light (toggle appearance in Preferences), and record each as
    PASS or OUTSTANDING per mode.

    1. CRITERION 5 — COLD START (do this FIRST, before anything else): fully quit and relaunch the app.
       There must be NO red error screen and no `!_dirty` / `assert(!_dirty)` line in the run console as the
       initial route resolves. Then: the swap FAB is visible bottom-right on the dashboard AND on a pushed
       token-detail screen; it is ABSENT on the /swap tab itself and on the landing/onboarding/wallet-creation
       screens; and it REAPPEARS after backing out of /swap. Confirm it does not cover another screen's
       primary CTA. There must be exactly ONE FAB — no second button bottom-left.

    2. CRITERION 1 — SWAP SKIN + FIGURES (sketch 105 A1): open Swap. Confirm the centred ~560px column, the
       soft brand sheen behind it (it must NOT wash out card contrast — check light mode especially), the
       one-line subtitle under "Swap", the two cards with 38px amounts, and the flip control sitting IN THE
       SEAM between the cards rather than floating above them. Enter an amount and confirm the route-details
       card appears with Pricing / Slippage / Price Impact / Fees. NOTE: the quote is a hardcoded constant —
       every token pair returns the SAME figures (0.51% impact, $0.30 fees). Criterion 1 asks whether those
       are the SAME numbers develop showed, not whether they change. Tap the tune icon: the Swap Settings
       drawer must read as the same design language (its Apply button is a gradient GWButton, not the old
       hand-rolled one) and changing slippage must still update the Slippage row.

    3. MAX AND THE FIAT LINE: tap MAX on the You Pay card — the amount fills with the token's balance and a
       quote refresh follows. If a USD line appears under an amount, it must be a real figure; if no price is
       known for that token the line must be ABSENT, never $0.00.

    4. CRITERION 2 — ROUTE ERROR (finding 22, D-09): force a route fetch to fail (disconnect the network, or
       use the dev fault injector if it covers this path). The "You Receive" field must show an em dash — NOT
       the last number it showed. The route-details card must disappear. A red notice must appear saying the
       quote is not current. The CTA must read "Retry" and be ENABLED. Tap Retry and confirm it re-fetches
       immediately. Check the notice's legibility in BOTH modes.

    5. CTA LADDER + D-15 DISABLED CONTRAST: with the fields empty, the CTA reads "Enter an amount" and is
       disabled but still legible. Enter more than your balance: it reads "Insufficient {SYMBOL} balance" on a
       red-tinted fill, disabled and legible. While a quote is fetching it reads "Finding best route…". With a
       valid quote it is the brand gradient reading "Swap". In BOTH modes, every disabled rung must be
       clearly distinguishable from the enabled one AND readable — not a grey smear.

    6. CRITERION 3 — SWAP SUBMIT (the D-01 deviation): tap Swap. Expect the "Swap Submitted" toast AND the
       031-B receipt drawer, both. On the receipt confirm: no "Network Fee" row (nothing executed, so no fee
       is claimed), no explorer button (the hash is empty), and NO wording anywhere implying an on-chain swap
       settled. Close the receipt and confirm the transaction appears in the Transactions surface. RECORD THIS
       AS THE DOCUMENTED DEVIATION (D-01), never as a live swap.

    7. CRITERION 4 — BRIDGE (sketch 120 B1), at the depth authorised in Task 1: reach bridge via a GNUS token
       → More → "Bridge Tokens" (confirm the entry still requires GNUS and a non-zero balance, and that More
       does nothing on a non-GNUS token). On the bridge screen confirm: the compact back-arrow header matching
       the rest of the app, a ~560px column that reads as the Swap tab's twin, a source→destination network
       route bar whose DESTINATION chip opens a network picker sheet (not a dropdown menu), "You Pay" and
       "You Receive on {network}" cards, a gas card with exactly two rows and an em dash (not a 0) before any
       estimate arrives, NO rate row and NO slippage row, and no flip control and no swap/bridge toggle. Type
       an amount and confirm a real gas estimate arrives and the receive amount mirrors the pay amount 1:1.
       Then, if authorised, submit: the toast AND the receipt must BOTH appear. On the receipt, judge whether
       the "Minted" badge and title read acceptably for a bridge — D-19's sanctioned fallback is the transfer
       type if they do not. Also decide: after dismissing the receipt you now STAY on the bridge screen (with
       fields reset on success) rather than being returned to the token screen as before. Is that acceptable?

    8. NO ORPHANS, NO PHASE-10 DAMAGE: open the dev-tools bubble (with the dev flag on). Its two former swap
       drawer buttons now open the shared receipt. Its "Swap OK" / "Swap fail" buttons must STILL open the
       reown drawer, unchanged — that path belongs to Phase 10 (D-05) and must be untouched.

    9. CONSOLE WATCH throughout: zero new exceptions, zero `setState after dispose`, zero overflow warnings on
       either screen at a narrow window width.
  </how-to-verify>
  <action>
    Hand the running Windows app to the developer and walk the numbered recipe below in BOTH appearance modes —
    do not substitute your own judgement for any item; every one of them is something only eyes, a real cold
    start, or the developer's risk call can settle. Capture a PASS or OUTSTANDING verdict per numbered item PER
    MODE into 08-VERIFICATION.md, together with the bridge depth chosen in Task 1 and the two judgement calls
    in item 7. Flip `status:` to `passed` ONLY if every item passes in both modes; otherwise leave it
    gaps_found and record each failure specifically enough for a gap-closure plan to act on. Record criterion 3
    as D-01's documented deviation, never as a live swap. Change no source file here.
  </action>
  <resume-signal>Type "approved" if every numbered item passes in both modes, or describe exactly what failed — which item, which mode, which screen — so it can be fixed and re-walked.</resume-signal>
  <acceptance_criteria>
    - 08-VERIFICATION.md exists and records each of the nine items as PASS or OUTSTANDING, per appearance mode.
    - All five ROADMAP criteria have an explicit verdict, with criterion 3 recorded as the documented-deviation
      branch and criterion 4 annotated with the verification depth chosen in Task 1.
    - D-15's verdict covers every CTA rung including the disabled ones, the route-error notice, and the sheen,
      in both modes.
    - The two D-19 and A4 judgement calls (the "Minted" copy, the dropped route pop) are recorded with the
      developer's answer.
    - 08-VERIFICATION.md flips to `status: passed` only if every item passes; otherwise it stays gaps_found with
      specific OUTSTANDING entries.
    - No auto-approval occurred and no source file was modified by this plan.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer authorisation → real on-chain burn/mint | The only place in this phase where a genuine fund-moving call may be invoked, and it requires an explicit decision first |
| walk verdict → SCR-04's completion claim | An unearned PASS here is how a phase ships a defect as done |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-29 | Tampering | Unauthorised real bridgeOut during the walk | high | mitigate | Task 1 is a BLOCKING decision checkpoint; no submission may occur before the developer answers, and the chosen depth is recorded alongside criterion 4's verdict |
| T-08-30 | Repudiation | Recording criterion 3 as a live swap | medium | mitigate | The prohibition is explicit and the acceptance criteria require it to be recorded as the documented-deviation branch (D-01) |
| T-08-31 | Repudiation | Auto-approving a walk nobody performed | high | mitigate | `gate="blocking"`, auto-advance explicitly ignored, and per-mode PASS/OUTSTANDING entries required in 08-VERIFICATION.md |
| T-08-32 | Information Disclosure | Shipping a contrast failure as passed | medium | mitigate | D-15 is walked as its own numbered item across every CTA rung and both modes; a failure is recorded OUTSTANDING, not waived |
| T-08-SC | Tampering | npm/pip/cargo/pub installs | n/a | accept | No packages; no source changes at all in this plan |
</threat_model>

<verification>
The human walk IS the gate. Capture each of the nine items as PASS or OUTSTANDING (with the specific failure,
screen and mode) into 08-VERIFICATION.md. Flip `status:` to `passed` only when every item is earned in both
appearance modes. Criterion 3 is recorded as D-01's documented deviation, never as a live swap. Criterion 4's
verdict names the depth authorised in Task 1. Do not auto-approve, and do not fix anything here — defects
become a gap-closure plan.
</verification>

<success_criteria>
All five ROADMAP criteria and D-15's contrast rule are confirmed by a person at the running Windows app in
both appearance modes and recorded honestly — or every failure is written down as OUTSTANDING for a
gap-closure round.
</success_criteria>

<output>
Create `.planning/phases/08-swap-bridge/08-07-SUMMARY.md` when done, and create/update
`.planning/phases/08-swap-bridge/08-VERIFICATION.md` with the per-item, per-mode verdicts.

## Artifacts this plan produces (08-07)
- Created: `.planning/phases/08-swap-bridge/08-VERIFICATION.md` (the walk record; SCR-04's gate)
- No source files. Records the bridge-depth authorisation, the D-19 "Minted"-copy judgement, and the A4
  route-pop judgement as the developer's answers.
</output>
