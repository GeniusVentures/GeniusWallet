---
phase: 08-swap-bridge
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - lib/squid_router/swap_cta_state.dart
  - test/squid_router/swap_cta_state_test.dart
  - lib/squid_router/swap_screen.dart
autonomous: true
requirements: [SCR-04]
user_setup: []

must_haves:
  truths:
    - "The swap tab renders sketch 105 A1: a 560px centred column, a soft brand-sheen wash behind it, the page title carrying D-07's one-line subtitle, and the flip control sitting IN THE SEAM between the two amount cards. (D-07; verified: grep gates + 08-07 walk)"
    - "The hard-coded vertical pixel offset that used to yank the flip button up the tree is GONE — the flip control is positioned by a real seam-overlap layout, not a magic offset. This is D-07's stated signal that the layout landed. (verified: negative grep for a Transform offset in swap_screen.dart)"
    - "The duplicated `if (fetchedRoute != null)` guard is gone (it was dead code, nested inside itself)."
    - "The CTA is a GWButton on the brand gradient and it RENDERS IN EVERY STATE, including its disabled states — the ladder is Enter an amount / Insufficient {symbol} balance / Finding best route… / Swap / Submitting swap… / Retry. (UI-SPEC Copywriting Contract; verified: test/squid_router/swap_cta_state_test.dart)"
    - "`insufficientBalance` EXISTS as newly derived state (amount vs the selected pay token's balance). It did not exist anywhere in this file before — `canSwap` never compared against a balance. (RESEARCH Pitfall 1; verified: unit test)"
    - "D-09 HARD CONTRACT: when a route fetch fails, the 'You Receive' field shows an em dash — never the last-known amount — the route-details card is hidden, a red inline notice appears, and the CTA becomes an ENABLED 'Retry'. The transient snackbar may remain as a supplementary toast but is not the mechanism. (D-09 / criterion 2; verified: unit test on the ladder + 08-07 walk on the visible notice)"
    - "Every figure the route card prints still comes from develop's untouched derivations; the quote pipeline, the 500ms debounce, `canSwap`, `swapParams`, `_flipTokens` and `_loadTokens` are unchanged. (D-08; verified: 08-01's golden test still green + grep)"
  artifacts:
    - "lib/squid_router/swap_cta_state.dart — NEW pure module: the CTA state enum, its precedence resolver, its label copy and its enabled/disabled rule"
    - "test/squid_router/swap_cta_state_test.dart — NEW: one test per ladder rung plus the precedence cases"
    - "lib/squid_router/swap_screen.dart — sketch 105 A1 layout, GWButton CTA on the ladder, D-09 route-error state"
  key_links:
    - "swapCtaState() ↔ swap_screen's build (one source of truth for label + enabled; the screen must not re-derive copy inline)"
    - "routeError flag ↔ three simultaneous effects: em-dash receive field, hidden route card, enabled Retry CTA (all three or the contract is not met)"
    - "Retry CTA ↔ _fetchRoute (direct call, NOT the 500ms debounce — a user-initiated retry should not wait)"
    - "GWPageHeader.subtitle (from 08-01) ↔ the Swap screen being its ONLY caller that passes one"
  prohibitions:
    - "Do NOT implement the swap execution. `// TODO: invoke Squid API` and `// TODO: record transaction...` stay exactly where they are, verbatim (D-01). Wiring real Squid execution is a deferred functional change with its own testing and safety story."
    - "Do NOT add any word, icon, animation or progress affordance implying on-chain settlement (D-02). No 'Confirming on Ethereum…', no explorer-style progress."
    - "Do NOT introduce an artificial delay to make 'Submitting swap…' visible. Set the flag around the REAL await and let it be as brief as it truly is. Faking latency is fabricating a claim."
    - "Do NOT change SquidTokenService, RouteDetailsCard's derivations, the 500ms debounce duration, `canSwap`, `swapParams`, `_flipTokens`'s body, or the BlocListener reset (D-08/D-11 discipline: mechanics do not move)."
    - "Do NOT write a test asserting the quote varies by token pair or amount — it never does (D-18b)."
    - "Do NOT generalise the D-09 notice into a new shared `GWInlineNotice` primitive. No such primitive exists and creating one is scope creep beyond this phase's fence (RESEARCH: keep it minimal and inline)."
    - "Do NOT replace the SwapSuccessDrawer call or touch the transaction construction/persistence in the submit closure — plan 08-05 owns that. This plan only wraps the closure's chrome and its submitting flag."
    - "Do NOT reference `brandFill` or `statusErrorFill`; NEITHER TOKEN EXISTS. Use GeniusWalletColors.brandPrimarySubtle / brandPrimaryOnSurface and `gw.statusError.withValues(alpha: 0.12)`."
    - "Do NOT invent a new disabled tone for the CTA, and do NOT use a sketch hex for the sheen. D-15 requires WCAG AA in BOTH modes and in EVERY state including disabled: reuse the shipped textPrimary38-on-surfaceMenu disabled treatment, resolve every colour through the appearance-aware getters, and scale the sheen's alpha down in light mode so it cannot swallow card contrast."
---

<!-- planner-discipline-allow: Transform.translate -->
<!-- planner-discipline-allow: Colors.greenAccent -->

<objective>
Assemble the swap tab into sketch 105 A1 and give it the two behaviours the redesign owes the user: a CTA
that tells the truth about why it is disabled, and a route-fetch failure that can never leave a stale
"You Receive" number on screen (D-09, ROADMAP criterion 2, finding 22).

Purpose: this plan carries criterion 2 and the visible half of criterion 1. The quote pipeline underneath is
untouched — and note plainly that it is MOCKED end to end: `SquidTokenService.getRoute()` returns one
hardcoded constant regardless of input (D-18b), and per D-01 submitting a swap executes nothing. Criterion 1
therefore means "the re-skin did not disturb a constant", and nothing in this plan implies live behaviour.

Output: a pure, unit-tested CTA state module and a swap screen that wears 105 A1 and honours D-09.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-swap-bridge/08-CONTEXT.md
@.planning/phases/08-swap-bridge/08-RESEARCH.md
@.planning/phases/08-swap-bridge/08-UI-SPEC.md
@.planning/phases/08-swap-bridge/08-01-SUMMARY.md
@.planning/sketches/105-swap-tab/README.md
@.planning/sketches/120-phase-8-flow/README.md

# The screen and the vocabulary 08-01 established
@lib/squid_router/swap_screen.dart
@lib/squid_router/swap_field.dart
@lib/squid_router/token_flip_button.dart
@lib/squid_router/route_details_card.dart
@lib/components/buttons/gw_button.dart
@lib/theme/gw_colors.dart
@lib/theme/genius_wallet_decorations.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the CTA state ladder into a pure, tested module</name>
  <files>lib/squid_router/swap_cta_state.dart, test/squid_router/swap_cta_state_test.dart</files>
  <read_first>
    - lib/squid_router/swap_screen.dart lines 121-146 (`canSwap` and `swapParams` — the existing derivations this module sits alongside, and which it must NOT duplicate or replace).
    - 08-UISPEC § Color → "CTA state → color mapping" table and § Copywriting Contract (the six swap CTA strings — copy them exactly, including the ellipsis characters).
    - 08-RESEARCH.md Pitfall 1 (no "insufficient balance" condition exists in this codebase today; it must be newly derived, mirroring the balance pre-check pattern `bridge_screen.dart` already uses).
    - lib/squid_router/models/squid_balance.dart (`formattedBalance` / `amountAsDouble` — how a balance becomes a number).
  </read_first>
  <behavior>
    - Empty / unparseable amount, tokens selected → `enterAmount`, label "Enter an amount", disabled.
    - No tokens selected → `enterAmount`, disabled.
    - Amount 5, balance 1 → `insufficientBalance`, label "Insufficient ETH balance" (symbol interpolated), disabled.
    - Amount 1, balance 1 (exactly affordable) → NOT insufficient.
    - Balance unknown (null) → NOT insufficient (never accuse the user on missing data).
    - Amount valid, fetch in flight → `findingRoute`, label "Finding best route…", disabled.
    - Amount valid, route present, no error → `ready`, label "Swap", ENABLED.
    - routeError true → `routeError`, label "Retry", ENABLED (the field is in error, the button is not).
    - isSubmitting true → `submitting`, label "Submitting swap…", disabled — and it OUTRANKS every other rung.
    - Precedence, top to bottom: submitting → enterAmount (no tokens / no parseable amount) → routeError → insufficientBalance → findingRoute → ready.
  </behavior>
  <action>
    Create `lib/squid_router/swap_cta_state.dart` containing NO Flutter widget code — pure Dart so it is
    trivially unit-testable:
      • `enum SwapCtaState { enterAmount, insufficientBalance, findingRoute, ready, submitting, routeError }`
      • `SwapCtaState resolveSwapCtaState({required bool hasBothTokens, required String fromAmount,
        required double? fromBalance, required bool isFetchingRoute, required bool hasRoute,
        required bool routeError, required bool isSubmitting})` implementing exactly the precedence in
        `<behavior>`.
      • `String swapCtaLabel(SwapCtaState state, {String? symbol})` returning the UI-SPEC strings verbatim.
        For `insufficientBalance` interpolate the symbol; fall back to "Insufficient balance" when symbol is
        null or empty.
      • `bool swapCtaEnabled(SwapCtaState state)` → true only for `ready` and `routeError`.
    Write the test file FIRST with one expectation per bullet in `<behavior>` (including the exactly-
    affordable boundary, the null-balance case, and the submitting-outranks-everything case), watch it fail,
    then implement until green. Keep the module free of `BuildContext`, colours and widgets — colour mapping
    stays in the screen where GWColors is in scope.
  </action>
  <verify>
    <automated>flutter test test/squid_router/swap_cta_state_test.dart</automated>
    <automated>rg -q "enum SwapCtaState" lib/squid_router/swap_cta_state.dart &amp;&amp; rg -q "resolveSwapCtaState" lib/squid_router/swap_cta_state.dart &amp;&amp; rg -q "Insufficient" lib/squid_router/swap_cta_state.dart &amp;&amp; rg -q "Retry" lib/squid_router/swap_cta_state.dart &amp;&amp; echo PASS</automated>
    <automated>rg -q "material.dart|BuildContext" lib/squid_router/swap_cta_state.dart; test $? -ne 0 || echo "MODULE IS NOT PURE"</automated>
    <automated>flutter analyze lib/squid_router/swap_cta_state.dart</automated>
  </verify>
  <acceptance_criteria>
    All `<behavior>` bullets are asserted and green; the module imports no Flutter UI library and takes no
    BuildContext; the six labels match the UI-SPEC copy exactly; `swapCtaEnabled` is true only for ready and
    routeError.
  </acceptance_criteria>
  <done>The ladder — including the never-before-existing insufficient-balance rung and D-09's enabled Retry — is provable without launching the app.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Lay out the swap tab as sketch 105 A1 and kill the offset hack</name>
  <files>lib/squid_router/swap_screen.dart</files>
  <read_first>
    - lib/squid_router/swap_screen.dart lines 213-330 (`_buildSwapContent`: the 500px ConstrainedBox at :223, the GWPageHeader + tune IconButton at :228-251, the two SwapFields, the offset-wrapped TokenFlipButton at :306-309, the doubled `if (fetchedRoute != null)` at :310-311).
    - 08-UI-SPEC.md § Screen Contracts → "Swap tab — sketch 105 A1" (560px, the two-glow sheen, the subtitle, the vertical order) and § Spacing Scale (space10 page padding, space12 route-card↔CTA gap, space16 focused-column top, space32 navbar→title gap which is ALREADY correct at :227).
    - 08-RESEARCH.md Pitfall 3 (the offset hack — confirmed present, confirmed removable, and what "removed" means: no hardcoded pixel offset may remain for the flip control).
    - lib/theme/genius_wallet_decorations.dart (existing radial/sheen gradients — prefer an existing decoration over a hand-rolled one).
    - 08-01-SUMMARY.md (the SwapField / flip-control / route-card APIs as actually shipped, including `emptyPlaceholder`).
  </read_first>
  <action>
    Re-lay out `_buildSwapContent` to sketch 105 A1. Behaviour and callbacks stay develop's.

    (a) COLUMN: `maxWidth` 500 → 560. Page horizontal padding space10 applied to the column (so the header
    and the cards share one left edge — the same content-width discipline Phase 7 used). Keep the space32
    navbar→title gap and the `Align(topCenter)` + `SingleChildScrollView` shell.

    (b) SHEEN: behind the column, two overlapping low-alpha radial glows (a cyan-ish and a mint-ish wash)
    painted by a `DecoratedBox`/`Container` inside an `IgnorePointer` so it can never intercept taps, sitting
    BELOW the cards in the stack order. Derive the colours from the brand tokens with low alpha via
    `withValues(alpha: ...)` — never a literal hex from the sketch. Scale the alpha DOWN in light mode; the
    wash must never compete with card contrast (checked at the 08-07 walk in both modes).

    (c) HEADER: pass `subtitle: "Trade any token across chains"` to GWPageHeader (the parameter 08-01 added).
    Re-colour the tune `IconButton`'s icon from a raw white to `gw.textSecondary` and keep its
    `SwapSettingsDrawer.show(...)` call with the SAME `initialSlippage` / `onSlippageChanged` arguments —
    that callback updates `slippage`, which feeds `swapParams`, so it is mechanics.

    (d) FLIP CONTROL IN THE SEAM: replace the wrapper that yanked the flip button 170px up the tree with a
    real overlap layout — put the two SwapField cards in a `Stack` (or a Column inside a Stack) and
    `Positioned` the 44px flip control centred on the boundary between them, offset by half its own known
    height rather than a magic number. NO hardcoded pixel offset for the flip control may survive anywhere in
    this file. `onFlip: _flipTokens` and `_flipTokens()`'s body are untouched.

    (e) DEAD CODE: collapse the doubled `if (fetchedRoute != null)` into a single guard.

    (f) ORDER + RHYTHM: You Pay card → (seam flip) → You Receive card → route-details card → CTA, with a
    space12 gap between the route card and the CTA and space6/space8 between the cards, per the spacing
    table. The `isLoading` spinner path is preserved as-is; it is not redesigned.
  </action>
  <verify>
    <automated>rg -q "maxWidth: 560" lib/squid_router/swap_screen.dart &amp;&amp; rg -q "subtitle:" lib/squid_router/swap_screen.dart &amp;&amp; rg -q "IgnorePointer" lib/squid_router/swap_screen.dart &amp;&amp; rg -q "onFlip: _flipTokens" lib/squid_router/swap_screen.dart &amp;&amp; echo PASS</automated>
    <automated>rg -v '^\s*//' lib/squid_router/swap_screen.dart | rg -c "Transform.translate|Offset\(0, -170\)"; test $? -ne 0 || echo "OFFSET HACK STILL PRESENT"</automated>
    <automated>rg -c "if \(fetchedRoute != null\)" lib/squid_router/swap_screen.dart | rg -q '^1$' &amp;&amp; echo "PASS: single route guard"</automated>
    <automated>flutter analyze lib</automated>
    <automated>flutter test test/squid_router/route_details_card_test.dart</automated>
  </verify>
  <acceptance_criteria>
    560px column; subtitle passed; sheen present and wrapped in IgnorePointer; no Transform-based pixel
    offset for the flip control anywhere in the file; exactly one `fetchedRoute != null` guard;
    `_flipTokens` still wired; 08-01's golden route-figure test still green; analyze at the 61 baseline.
  </acceptance_criteria>
  <done>The tab reads as 105 A1 and the offset hack is gone — D-07's own signal that the seam layout landed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire the CTA ladder and D-09's route-error state (criterion 2)</name>
  <files>lib/squid_router/swap_screen.dart</files>
  <read_first>
    - lib/squid_router/swap_screen.dart lines 148-180 (`_fetchRoute`'s try/catch — the catch at :165-172 is snackbar-only today, which is exactly what D-09 says is insufficient) and lines 320-422 (the raw ElevatedButton + its onPressed closure).
    - lib/squid_router/swap_cta_state.dart (Task 1's module) and test/squid_router/swap_cta_state_test.dart.
    - 08-UI-SPEC.md § Screen Contracts → "Swap non-happy states" (all three) and § Color → the CTA state→colour table and the route-error notice spec (statusError @ ~12% alpha fill, radiusMd, space6/space8 padding, icon + text in statusError).
    - 08-UI-SPEC.md § Copywriting Contract → the route-error notice copy, verbatim.
    - lib/components/buttons/gw_button.dart (`GWButtonVariant.gradient`, `GWButtonSize.lg`, `isLoading`, `expand`; disabled = `onPressed: null`).
  </read_first>
  <action>
    (a) NEW STATE FIELDS on `_SwapScreenState`: `bool isFetchingRoute = false;`, `bool routeError = false;`,
    `bool isSubmitting = false;`. Add a `double? get fromBalanceAmount` helper reading the selected pay
    token's balance as a number (null when unknown).

    (b) `_fetchRoute()`: set `isFetchingRoute = true` and `routeError = false` before the call. On SUCCESS
    keep the existing body byte-for-byte (formatTokenAmount, `toAmount`, `toAmountController.text`,
    `fetchedRoute`) and clear `isFetchingRoute`. On FAILURE — this is D-09's hard contract — set
    `routeError = true`, `fetchedRoute = null`, `toAmount = ''` and `toAmountController.clear()`, and clear
    `isFetchingRoute`. Clearing the controller is what makes the field show the em dash instead of a stale
    number; NEVER leave the previous quote on screen. The existing `showAppSnackBar` call may stay as a
    supplementary transient toast, but it is no longer the mechanism.

    (c) RECEIVE FIELD: pass `emptyPlaceholder: routeError ? '—' : null` to the "You Receive" SwapField (the
    parameter 08-01 added). Its `onChanged` stays as it is.

    (d) NOTICE: when `routeError` is true, render a compact inline notice between the receive card and the
    CTA — a `Container` with `gw.statusError.withValues(alpha: 0.12)` fill, `radiusMd`, space6 vertical /
    space8 horizontal padding, a small error icon and text both in `gw.statusError`, carrying the UI-SPEC's
    exact two-part copy (headline + the "the quote above is not current" body). Keep it inline in this file;
    do not extract a shared primitive.

    (e) ROUTE CARD: hide the route-details card entirely while `routeError` is true (it must not display
    figures derived from a route that just failed). Its own derivations are untouched.

    (f) CTA: replace the raw ElevatedButton and its Material green-accent fill with
    `GWButton(variant: GWButtonVariant.gradient, size: GWButtonSize.lg, expand: true)`. The button now
    renders in EVERY state — remove the `if (canSwap)` gate that hid it, because a ladder whose disabled
    rungs never appear is not a ladder. Resolve state via `resolveSwapCtaState(...)` from Task 1, label via
    `swapCtaLabel(state, symbol: fromToken?.symbol)`, and pass `onPressed: null` whenever
    `swapCtaEnabled(state)` is false. Map the two non-gradient fills per the UI-SPEC table: the
    insufficient rung gets a `gw.statusError`-at-~12%-alpha fill with `gw.statusError` label; the
    enterAmount / findingRoute / submitting rungs get `gw.surfaceMenu` with `gw.textPrimary38` (the shipped
    disabled treatment Phase 7 uses — reuse it, do not invent a new disabled tone). Use `isLoading` for the
    submitting rung's spinner.
    ROUTE-ERROR RUNG: `onPressed` calls `_fetchRoute()` DIRECTLY (not `_debouncedFetchRoute()`) — a user
    who taps Retry should not wait out a debounce.
    READY RUNG: `onPressed` runs the EXISTING submit closure unchanged except for setting
    `isSubmitting = true` before its body and clearing it in a `finally`. Everything inside that closure —
    the debugPrint, BOTH `// TODO:` markers, the `Transaction(...)` construction, the ToastManager call, the
    `SwapSuccessDrawer.show(...)` call, `transactionsCubit.addTransaction`, and the
    `TransactionStorageService().addTransaction` await — stays exactly as it is. Plan 08-05 replaces the
    drawer call; this plan does not.
  </action>
  <verify>
    <automated>rg -q "resolveSwapCtaState" lib/squid_router/swap_screen.dart &amp;&amp; rg -q "swapCtaLabel" lib/squid_router/swap_screen.dart &amp;&amp; rg -q "GWButtonVariant.gradient" lib/squid_router/swap_screen.dart &amp;&amp; rg -q "emptyPlaceholder" lib/squid_router/swap_screen.dart &amp;&amp; rg -q "routeError" lib/squid_router/swap_screen.dart &amp;&amp; echo PASS</automated>
    <automated>rg -q "// TODO: invoke Squid API" lib/squid_router/swap_screen.dart &amp;&amp; rg -q "// TODO: record transaction" lib/squid_router/swap_screen.dart &amp;&amp; echo "PASS: D-01 deviation still written down in code"</automated>
    <automated>rg -v '^\s*//' lib/squid_router/swap_screen.dart | rg -c "Colors.greenAccent|ElevatedButton"; test $? -ne 0 || echo "LEGACY CTA STILL PRESENT"</automated>
    <automated>rg -q "if \(canSwap\)" lib/squid_router/swap_screen.dart; test $? -ne 0 || echo "CTA STILL HIDDEN WHEN DISABLED"</automated>
    <automated>flutter analyze lib</automated>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    `resolveSwapCtaState`/`swapCtaLabel` drive the CTA (no inline copy switch in the screen); the CTA renders
    in disabled states; the route-error branch sets routeError, nulls `fetchedRoute`, CLEARS the receive
    controller and passes the em-dash placeholder; the notice and the hidden route card are conditioned on
    routeError; Retry calls `_fetchRoute` directly; both D-01 TODO markers are still present verbatim; no
    ElevatedButton or Material green-accent fill remains outside comments; analyze at baseline; full suite
    251/1.
  </acceptance_criteria>
  <done>Criterion 2 is implemented and its ladder is unit-proven; the visible notice and the never-stale field are confirmed at the 08-07 walk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mocked quote service → displayed financial figures | The route/fee/slippage a user reads comes from a compile-time constant (D-18b); the UI must not imply it is live |
| user amount input → balance comparison | The new insufficient-balance derivation parses untrusted text and compares it to a token balance |
| failed network path → displayed quote | The exact defect D-09 exists to close: a stale number presented as current |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-08 | Information Disclosure | Stale quote after a failed route fetch | high | mitigate | D-09: the catch branch nulls `fetchedRoute`, clears the receive controller, shows an em dash, hides the route card and surfaces a red notice; the ladder's Retry rung is unit-tested |
| T-08-09 | Repudiation | Success UI on a swap that never executed | high | accept | KNOWINGLY ACCEPTED and written down (D-01). The deviation stays; both TODO markers are grep-gated so a future pass cannot silently "fix" it into a real fund-moving call without a decision. D-02 forbids upgrading the copy's claims |
| T-08-10 | Tampering | Amount parsed for the insufficient-balance rung | low | mitigate | `double.tryParse` fails closed to the `enterAmount` rung; a null balance never produces an accusation; the exactly-affordable boundary is unit-tested |
| T-08-11 | Denial of Service | Retry hammering the quote path | low | accept | `getRoute()` is a local constant return — no network to hammer. Revisit if real Squid execution is ever wired (deferred) |
| T-08-SC | Tampering | npm/pip/cargo/pub installs | n/a | accept | Zero packages added; every primitive already ships. Legitimacy gate not applicable |
</threat_model>

<verification>
- `flutter test test/squid_router/swap_cta_state_test.dart` green — the whole ladder, including the insufficient rung that never existed and D-09's enabled Retry.
- `flutter test test/squid_router/route_details_card_test.dart` still green — proof the layout pass did not disturb develop's figures (criterion 1).
- Full `flutter test` stays 251 pass / 1 known pre-existing failure.
- `flutter analyze lib` at or below the 61-issue baseline.
- Grep gates: 560px column, subtitle passed, IgnorePointer sheen, no Transform-based flip offset, single route guard, GWButton gradient CTA, no ElevatedButton/green-accent survivors, no `if (canSwap)` gate, and BOTH D-01 TODO markers still present.
- NOT evidence here: that the sheen reads well, that the notice is legible in light mode, that the disabled rungs stay WCAG-AA and visibly distinct. Those are 08-07's walk.
</verification>

<success_criteria>
The swap tab is sketch 105 A1 with the offset hack gone; the CTA tells the user why it is disabled; and a
failed route fetch can no longer leave a stale "You Receive" figure on screen (criterion 2). The D-01
deviation is preserved and grep-guarded, and nothing on the screen claims an on-chain swap occurred.
</success_criteria>

<output>
Create `.planning/phases/08-swap-bridge/08-03-SUMMARY.md` when done.

## Artifacts this plan produces (08-03)
- Created: `lib/squid_router/swap_cta_state.dart`, `test/squid_router/swap_cta_state_test.dart`
- Modified: `lib/squid_router/swap_screen.dart` (105 A1 layout, GWButton CTA ladder, D-09 route-error state)
Record in the SUMMARY: how the seam overlap was achieved (so 08-04's bridge twin can mirror it), the exact
notice copy shipped, and confirmation that both D-01 TODO markers survive. No packages added; no drawer or
transaction wiring changed (08-05 owns that).
</output>
