---
phase: 08-swap-bridge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/components/overlay/global_swap_fab_host.dart
  - lib/main.dart
  - test/components/global_swap_fab_host_test.dart
autonomous: true
requirements: [SCR-04]
user_setup: []

must_haves:
  truths:
    - "`lib/components/overlay/global_swap_fab_host.dart` EXISTS on this branch, ported from `7a63b4f` (branch ui-redesign-3.514-develop), with the swap half byte-identical to the source. (D-17/D-18; verified: grep + test)"
    - "The `if (!_ready) return;` guard in `_onRouteChanged` AND its full explanatory comment survive VERBATIM — the comment records that the initial mount runs under attachRootWidget where schedulerPhase is `idle`, so the schedulerPhase check does not catch that case. (D-18; verified: grep gate on the guard + on the attachRootWidget sentence)"
    - "The AI-FAB half is ABSENT: no import of a nonexistent AI-FAB component, no second Positioned block, no `/submit_job` visibility condition. The port compiles. (D-18a; verified: analyze + negative grep)"
    - "GlobalSwapFabHost is MOUNTED once at MaterialApp.router's builder in main.dart, composed with DevicePreview.appBuilder as the OUTERMOST wrapper. (D-17; verified: grep + 08-07 cold-start walk)"
    - "On its first frame the host renders ONLY its child (no FAB); after the post-frame callback it renders the FAB on a visible path and no FAB on a hidden path. (D-18 — the _ready gate is what prevents the `!_dirty` red screen; verified: test/components/global_swap_fab_host_test.dart)"
    - "`gw_swap_fab.dart` is CONFIRMED already design-system compliant and is NOT re-painted. (D-14; verified: grep for brandCta/textOnBrand + no edit to the file)"
  artifacts:
    - "lib/components/overlay/global_swap_fab_host.dart — NEW, ported with the AI-FAB half stripped"
    - "lib/main.dart — GlobalSwapFabHost composed into the MaterialApp.router builder"
    - "test/components/global_swap_fab_host_test.dart — NEW: the _ready gate + hidden-path behaviour"
  key_links:
    - "GlobalSwapFabHost._ready ↔ the first-frame `return widget.child` in build() (breaking either half reopens NAV-02)"
    - "MaterialApp.router builder ↔ DevicePreview.appBuilder composition (both must survive; DevicePreview outermost)"
    - "widget.router.routerDelegate listener ↔ FAB reappearing after a back-pop (the delegate is listened to, NOT routeInformationProvider — that distinction is load-bearing and documented in the source comment)"
  prohibitions:
    - "Do NOT create an AI-FAB button component or port any AI FAB. WIRE-02 puts `lib/ai/` and its FAB out of scope for this milestone; ROADMAP's Phase 3 accounting explicitly subtracted that one file. Adding it imports another phase's scope into this one."
    - "Do NOT rewrite, shorten, 'clean up' or reflow the `_onRouteChanged` guard comment or the `_ready` field comment. They are the port's whole value (D-18). Copy them character-for-character."
    - "Do NOT replace the `_ready` gate with a `schedulerPhase` check. The source comment states why that does not catch the startup case — re-deriving this costs a day."
    - "Do NOT remove `DevicePreview.appBuilder` from main.dart. Compose with it; do not displace it."
    - "Do NOT re-paint `lib/components/buttons/gw_swap_fab.dart`. D-14 is satisfied by confirming it stays compliant (UI-SPEC: 'confirm during the walk, don't re-paint')."
    - "Do NOT attempt to reproduce the `!_dirty` startup crash in a widget test. RESEARCH is explicit that this bug class depends on runApp's real mount timing and is not reproducible under pumpWidget; the cold-start walk in 08-07 is its gate."
    - "Do NOT add or remove entries from `_hiddenPaths` — port the set verbatim (see Task 1 note on `/legal`)."
---

<!-- planner-discipline-allow: submit_job -->

<objective>
Land ROADMAP criterion 5 — the `7a63b4f` carry Phase 4 deferred "to the swap-FAB phase". Port
`GlobalSwapFabHost` from branch `ui-redesign-3.514-develop`, mount it at MaterialApp.router's builder, and
prove the `_ready` gate that prevents the NAV-02 `!_dirty` red screen on startup still behaves.

Purpose: this is a CRASH FIX carrying an already-solved subtle finding. It is a PORT, not a build — with
exactly one necessary deviation (D-18a: the source file's AI-FAB half references a component that does not
exist on this branch and will not compile).

Output: the host file, its mount point, and a widget test covering the first-frame gate and hidden paths.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-swap-bridge/08-CONTEXT.md
@.planning/phases/08-swap-bridge/08-RESEARCH.md

# Mount point and the already-shipped button
@lib/main.dart
@lib/components/buttons/gw_swap_fab.dart
@lib/navigation/router.dart
@lib/components/splash.dart
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port GlobalSwapFabHost from 7a63b4f with the AI-FAB half stripped</name>
  <files>lib/components/overlay/global_swap_fab_host.dart</files>
  <read_first>
    - The source, verbatim: `git show 7a63b4f:lib/components/overlay/global_swap_fab_host.dart` (143 lines). Read ALL of it before writing anything.
    - Its own diff, to see what the fix changed: `git diff 7a63b4f^ 7a63b4f -- lib/components/overlay/global_swap_fab_host.dart`.
    - 08-CONTEXT.md D-18 and D-18a (the correction that makes a byte-for-byte port impossible).
    - 08-RESEARCH.md § "The `GlobalSwapFabHost` port" and Pitfall 5 (the compile blocker) — plus its route-path cross-check.
    - lib/components/buttons/gw_swap_fab.dart (the surviving FAB's constructor: `GWSwapFab({required onPressed, size = 56, tooltip = 'Swap'})`).
    - lib/components/splash.dart around line 57 (a live trace of the same mid-mount condition).
  </read_first>
  <action>
    Create `lib/components/overlay/global_swap_fab_host.dart` by copying the `7a63b4f` file and removing
    ONLY the AI-FAB half (~15 lines). Concretely:

    KEEP VERBATIM — copy character-for-character, including comments and blank lines:
      • the `flutter/material.dart`, `flutter/scheduler.dart`, `gw_swap_fab.dart`,
        `genius_wallet_consts.dart` and `go_router.dart` imports;
      • the class doc comment's paragraphs about hidden surfaces, the single mount at the
        `MaterialApp.router` builder level, and why the `router` is taken explicitly rather than read from
        context (the builder's context can sit above the InheritedGoRouter);
      • the `GlobalSwapFabHost` StatefulWidget with its `router` + `child` fields;
      • the `static const Set&lt;String&gt; _hiddenPaths` set and its doc comment — ALL entries exactly as
        written, including `'/'`, the onboarding/wallet-creation paths, `'/swap'`, and the payment/KYC
        block. See the `/legal` note below;
      • the `_ready` field AND its four-line comment explaining why the router must not be read during
        MaterialApp's very first build;
      • `initState`'s delegate-listener comment (why `routerDelegate` and not
        `routeInformationProvider`), `widget.router.routerDelegate.addListener(_onRouteChanged)`, and the
        `addPostFrameCallback` that flips `_ready`;
      • `dispose`'s `removeListener`;
      • `_onRouteChanged` IN FULL — the `if (!mounted) return;`, the whole multi-line comment above
        `if (!_ready) return;`, that guard itself, the second comment about a delegate notifying mid-frame,
        and the `SchedulerPhase.persistentCallbacks` branch with its post-frame deferral;
      • `build()`'s `if (!_ready) return widget.child;` first-frame short-circuit and its comment;
      • the `currentConfiguration.uri.path` read, the `_hiddenPaths.contains(path)` check, the
        `MediaQuery.of(context).viewPadding.bottom` inset, the `Stack` with `widget.child` first, and the
        `Positioned(right: space10, bottom: 80 + bottomInset)` swap FAB with its
        "clears the 60px bottom nav" comment and `widget.router.push('/swap')` callback.

    REMOVE (D-18a — this is the one authorised deviation from "port verbatim"):
      • the import of the AI-FAB button component (source line 3), which does not exist on this branch;
      • the SECOND `Positioned` block in the `Stack` — the bottom-left mirror FAB, its two-line comment,
        and its extra path condition;
      • the two clauses in the class doc comment that describe that bottom-left mirror and its
        live-AI-processing behaviour. Rewrite that doc paragraph so it describes ONLY the swap FAB, and add
        one short sentence recording that the AI-FAB mirror was intentionally dropped at port time because
        WIRE-02 keeps that component out of this milestone. Do not name a file that does not exist.

    `/legal` NOTE: the ported `_hiddenPaths` contains `'/legal'`, which is NOT a registered route path on
    this branch (grep `lib/navigation/` confirms zero matches). Keep the entry — an extra path that is never
    reached is inert and removing it would risk un-hiding the FAB if a Legal route lands later. Record this
    in the SUMMARY as a known-inert entry rather than silently dropping or "fixing" it.

    Do not otherwise reformat, reorder, or modernise the file.
  </action>
  <verify>
    <automated>test -f lib/components/overlay/global_swap_fab_host.dart &amp;&amp; flutter analyze lib/components/overlay/global_swap_fab_host.dart</automated>
    <automated>rg -q "if \(!_ready\) return;" lib/components/overlay/global_swap_fab_host.dart &amp;&amp; rg -q "attachRootWidget" lib/components/overlay/global_swap_fab_host.dart &amp;&amp; rg -q "persistentCallbacks" lib/components/overlay/global_swap_fab_host.dart &amp;&amp; rg -q "routerDelegate.addListener" lib/components/overlay/global_swap_fab_host.dart &amp;&amp; rg -q "_hiddenPaths" lib/components/overlay/global_swap_fab_host.dart &amp;&amp; echo PASS</automated>
    <automated>rg -c "Positioned\(" lib/components/overlay/global_swap_fab_host.dart | rg -q '^1$' &amp;&amp; echo "PASS: exactly one FAB slot"</automated>
    <automated>rg -q "submit_job" lib/components/overlay/global_swap_fab_host.dart; test $? -ne 0 || echo "AI-FAB CONDITION STILL PRESENT"</automated>
    <automated>flutter analyze lib</automated>
  </verify>
  <acceptance_criteria>
    The file exists and compiles. The `_ready` guard, its comment (including the attachRootWidget sentence),
    the delegate-listener comment, the `_hiddenPaths` set and the swap `Positioned` block are all present.
    Exactly ONE `Positioned` remains. No reference to a nonexistent AI-FAB component or to `/submit_job`
    survives. `flutter analyze lib` holds at the 61-issue baseline.
  </acceptance_criteria>
  <done>The host compiles standalone; the fix's guard and its rationale comment are preserved verbatim.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mount the host at MaterialApp.router's builder, composed with DevicePreview</name>
  <files>lib/main.dart</files>
  <read_first>
    - lib/main.dart lines 300-348 (the `MaterialApp.router` inside `ValueListenableBuilder&lt;GWAppearanceMode&gt;`; note `builder: DevicePreview.appBuilder` is ALREADY occupying the builder slot at :338).
    - lib/navigation/router.dart (the `geniusWalletRouter` instance passed to `routerConfig`).
    - 08-RESEARCH.md § "The GlobalSwapFabHost port" → "Mount point" and Assumptions Log A2 (why DevicePreview should be the outermost wrapper).
    - lib/components/buttons/gw_swap_fab.dart (read-only — the D-14 compliance confirmation below).
  </read_first>
  <action>
    (a) Replace the bare `builder: DevicePreview.appBuilder` with a composed builder that keeps DevicePreview
    OUTERMOST and inserts GlobalSwapFabHost between it and the router's Navigator:
    a two-argument closure that returns `DevicePreview.appBuilder(context, GlobalSwapFabHost(router:
    geniusWalletRouter, child: <the builder's own child, with a SizedBox.shrink fallback when null>))`.
    Pass the SAME router instance that `routerConfig` receives — the host takes the router explicitly
    precisely because the builder's context can sit above the InheritedGoRouter, so do NOT switch to
    `GoRouter.of(context)`. Add the import for the new host file. Change nothing else in `main.dart`:
    `debugShowCheckedModeBanner`, `locale`, `title`, `theme: getThemeData()` and `routerConfig` stay as they
    are, and the `ValueListenableBuilder` wrapper stays.

    (b) D-14 CONFIRMATION — read-only, no edit: confirm `lib/components/buttons/gw_swap_fab.dart` still
    resolves its fill through `GeniusWalletGradient.brandCta`, its glyph through
    `GeniusWalletColors.textOnBrand`, and its press feedback through `GeniusWalletMotion`, and that it
    carries no raw Material-palette colour. Do NOT modify the file. Record the confirmation in the SUMMARY;
    D-14 is satisfied by it staying compliant, not by a re-paint.
  </action>
  <verify>
    <automated>rg -q "GlobalSwapFabHost" lib/main.dart &amp;&amp; rg -q "DevicePreview.appBuilder" lib/main.dart &amp;&amp; rg -q "geniusWalletRouter" lib/main.dart &amp;&amp; echo PASS</automated>
    <automated>rg -q "GeniusWalletGradient.brandCta" lib/components/buttons/gw_swap_fab.dart &amp;&amp; rg -q "textOnBrand" lib/components/buttons/gw_swap_fab.dart &amp;&amp; echo "PASS: D-14 confirmed compliant"</automated>
    <automated>git status --porcelain lib/components/buttons/gw_swap_fab.dart | rg -q .; test $? -ne 0 || echo "D-14 VIOLATION: gw_swap_fab.dart was modified"</automated>
    <automated>flutter analyze lib</automated>
  </verify>
  <acceptance_criteria>
    `main.dart` mounts GlobalSwapFabHost exactly once inside the MaterialApp.router builder, with
    DevicePreview.appBuilder still present and outermost, passing the same `geniusWalletRouter` instance.
    `gw_swap_fab.dart` is unmodified in git status and still uses the brand tokens. Analyze holds at baseline.
  </acceptance_criteria>
  <done>The app builds; the FAB host is in the tree. Its cold-start behaviour is proven at the 08-07 walk.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Widget-test the _ready gate and the hidden-path behaviour</name>
  <files>test/components/global_swap_fab_host_test.dart</files>
  <read_first>
    - lib/components/overlay/global_swap_fab_host.dart (the file just ported — the `_ready` gate, `_hiddenPaths`, and the single Positioned).
    - test/components/ (existing widget-test conventions in this repo: MaterialApp wrapper, theme setup, pumpWidget/pump usage).
    - 08-RESEARCH.md § Validation Architecture → the SCR-04 criterion-5 row (why the real `!_dirty` crash is NOT pumpWidget-reproducible and must not be faked here).
  </read_first>
  <action>
    Create `test/components/global_swap_fab_host_test.dart` with a minimal local `GoRouter` carrying two
    routes: one visible path (e.g. `/dashboard`) and one path that appears in `_hiddenPaths` (`/swap`), each
    building a trivial placeholder widget. Mount `MaterialApp.router` with `routerConfig` set to that router
    and `builder` wrapping the Navigator in `GlobalSwapFabHost(router: thatRouter, child: child!)`.

    Three tests:
      1. FIRST-FRAME GATE — after a single `await tester.pumpWidget(app)` and BEFORE any further pump,
         `find.byType(GWSwapFab)` finds NOTHING (the `_ready` short-circuit returned the child alone). Then
         `await tester.pump()` (which lets the post-frame callback's setState land) and the FAB IS found.
         This is the automatable half of the NAV-02 fix: it proves the host does not read the router on its
         first build.
      2. HIDDEN PATH — navigate the router to `/swap`, pump, and assert the FAB is absent.
      3. RETURN FROM HIDDEN PATH — navigate back to `/dashboard`, pump, and assert the FAB is present again
         (this is what the routerDelegate listener buys over routeInformationProvider).
    Also assert `tester.takeException()` is null in each test.

    Do NOT try to reproduce the `assert(!_dirty)` startup crash itself — it needs runApp's real mount
    timing and is the 08-07 cold-start walk's job. If the FAB widget type is awkward to find directly,
    match on its tooltip ('Swap') rather than weakening the assertion to a smoke test.
  </action>
  <verify>
    <automated>flutter test test/components/global_swap_fab_host_test.dart</automated>
    <automated>rg -q "pumpWidget" test/components/global_swap_fab_host_test.dart &amp;&amp; rg -q "takeException" test/components/global_swap_fab_host_test.dart &amp;&amp; rg -q "/swap" test/components/global_swap_fab_host_test.dart &amp;&amp; echo PASS</automated>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    Three tests pass: no FAB on the very first frame, FAB after the post-frame rebuild, FAB absent on a
    hidden path and present again after returning. No exceptions escape. Full `flutter test` stays at
    248 pass / 1 known pre-existing failure.
  </acceptance_criteria>
  <done>The `_ready` gate has a real automated guard, so a future refactor cannot silently delete it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route path string → FAB visibility | `currentConfiguration.uri.path` decides whether a global action floats over the current screen; exact-match only |
| global overlay → onboarding / KYC / checkout surfaces | A floating action over a seed-phrase, identity or payment form could obscure a CTA or invite a mis-tap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Elevation of Privilege | Global swap FAB over pre-auth / onboarding surfaces | medium | mitigate | `_hiddenPaths` is ported VERBATIM and covers splash, landing, all wallet-creation/recovery paths and the KYC/checkout block; the port adds no path and removes none. Test 2 proves hidden-path suppression |
| T-08-06 | Denial of Service | `!_dirty` assertion on initial route resolution (NAV-02) | high | mitigate | The `_ready` gate plus the first-frame `return widget.child` are preserved verbatim and pinned by Test 1; the crash's real repro is the 08-07 cold-start walk |
| T-08-07 | Spoofing | FAB tap target overlapping another screen's primary CTA | low | accept | Placement (right: space10, bottom: 80 + safe-area inset) is develop's ported geometry; the 08-07 walk checks it does not cover a real CTA |
| T-08-SC | Tampering | npm/pip/cargo/pub installs | n/a | accept | Zero packages added. The ported file's only dependencies (`go_router`, `flutter/scheduler`, an already-shipped in-repo button) are existing. Legitimacy gate not applicable |
</threat_model>

<verification>
- `flutter analyze lib` stays at or below the 61-issue baseline; in particular there is NO unresolved-import issue, which is the specific signal that the AI-FAB strip was done correctly (RESEARCH Pitfall 5).
- `flutter test test/components/global_swap_fab_host_test.dart` passes all three tests.
- Full `flutter test` remains 248 pass / 1 known pre-existing failure.
- Grep gates: the `_ready` guard, its attachRootWidget rationale, the persistentCallbacks branch and the delegate listener all survive; exactly one `Positioned` remains; no `/submit_job` condition; `gw_swap_fab.dart` untouched per git status.
- Criterion 5's real evidence — a cold start with NO `!_dirty` red screen, the FAB visible on the dashboard and absent on `/swap` and onboarding — is earned at the 08-07 walk, not here.
</verification>

<success_criteria>
`GlobalSwapFabHost` exists, is mounted once, keeps its hard-won startup guard verbatim, and compiles without
the AI FAB. Criterion 5's automatable half is green and its walk half is queued for 08-07.
</success_criteria>

<output>
Create `.planning/phases/08-swap-bridge/08-02-SUMMARY.md` when done.

## Artifacts this plan produces (08-02)
- Created: `lib/components/overlay/global_swap_fab_host.dart` (ported, AI-FAB half stripped per D-18a)
- Modified: `lib/main.dart` (host composed into the MaterialApp.router builder, DevicePreview outermost)
- Created: `test/components/global_swap_fab_host_test.dart`
- NOT modified: `lib/components/buttons/gw_swap_fab.dart` (D-14 confirmed compliant, no re-paint)
Record in the SUMMARY: the exact lines dropped from the source file, the `/legal` known-inert entry, and the
DevicePreview composition order actually used.
</output>
