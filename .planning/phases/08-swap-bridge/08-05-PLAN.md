---
phase: 08-swap-bridge
plan: 05
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - lib/squid_router/swap_screen.dart
  - lib/dashboard/home/widgets/transaction_displays.dart
  - test/dashboard/transaction_receipt_fee_row_test.dart
  - lib/dev/dev_tools_bubble.dart
  - lib/squid_router/swap_settings_drawer.dart
  - lib/squid_router/swap_success_drawer.dart
  - lib/squid_router/swap_fail_drawer.dart
  - lib/squid_router/swap_drawer_content.dart
autonomous: true
requirements: [SCR-04]
user_setup: []

must_haves:
  truths:
    - "A submitted swap now shows the already-shipped 031-B status-led receipt via `showTransactionDetails(context, transaction)` — ALONGSIDE the existing toast, not instead of it. (D-03/D-04; verified: grep + 08-07 walk)"
    - "The D-01 deviation is intact and grep-guarded: `// TODO: invoke Squid API` and `// TODO: record transaction...` are still in `swap_screen.dart`, the transaction is still constructed, added to TransactionsCubit and persisted to Hive, and NO Squid execution call was added. (D-01 / criterion 3; verified: positive grep on both markers + negative grep for a Squid execute call)"
    - "The receipt gains NO new language implying an on-chain swap occurred — the toast copy and the 031-B rows are reused as shipped. (D-02; verified: grep that the toast strings are unchanged)"
    - "`showTransactionDetails` omits the Network Fee row when `tx.fees` is blank, matching the Rate row's existing skip-empty behaviour. This is a one-line guard honouring the function's own documented contract, not a receipt redesign. (D-03 scope; verified: unit/widget test)"
    - "The swap Transaction no longer reports the PAY AMOUNT as a network fee. Nothing executed (D-01), so no fee is claimed — the row is absent rather than false. (verified: test + grep)"
    - "The three superseded drawers are DELETED, not orphaned: swap_success_drawer.dart, swap_fail_drawer.dart and swap_drawer_content.dart are gone and nothing in lib/ references them. (D-06; verified: file-absence gates + analyze)"
    - "`lib/reown/swap_result_drawer.dart` still EXISTS, is still called from `handle_dapp_requests.dart`, and its two dev-bubble buttons still call it. (D-05 — Phase 10 territory; verified: file-presence + call-site greps)"
    - "The Swap Settings drawer no longer reads as pre-redesign chrome: its hand-rolled gradient ElevatedButton+Ink footer is a GWButton and its TextField wears the shipped input theme. Slippage behaviour is unchanged. (D-13; verified: grep + walk)"
  artifacts:
    - "lib/squid_router/swap_screen.dart — receipt call replaces the success drawer; D-01 markers and persistence intact"
    - "lib/dashboard/home/widgets/transaction_displays.dart — blank-fee row guard (one condition)"
    - "lib/dev/dev_tools_bubble.dart — two buttons repointed to the shared receipt; the two reown buttons untouched"
    - "lib/squid_router/swap_settings_drawer.dart — re-skinned (D-13)"
    - "DELETED: lib/squid_router/swap_success_drawer.dart, swap_fail_drawer.dart, swap_drawer_content.dart"
  key_links:
    - "showTransactionDetails ↔ both flows (this plan wires the swap half; 08-06 wires the bridge half on the same guard)"
    - "blank-fee guard ↔ the bridge receipt in 08-06 (08-06 depends on this guard existing — do not skip it)"
    - "dev_tools_bubble's SwapResultDrawer buttons ↔ reown/swap_result_drawer.dart reachability (the ONLY manual test entry point for a Phase 10 file — deleting them would orphan it)"
  prohibitions:
    - "Do NOT touch `lib/reown/swap_result_drawer.dart` or `lib/reown/handle_dapp_requests.dart` — no import, no reference, no consolidation. D-05: that is Phase 10's territory and it serves the dApp request path."
    - "Do NOT delete the dev bubble's 'Swap OK' / 'Swap fail' buttons. They are the only thing keeping the reown drawer manually testable, and D-05 does not ask for their removal."
    - "Do NOT wire real Squid execution, and do NOT remove or reword either `// TODO:` marker in the submit closure. Criterion 3 is satisfied by the deviation being re-confirmed and written down (D-01), and those markers are its in-code record."
    - "Do NOT add copy to the receipt or the toast implying settlement, confirmation, or on-chain progress (D-02). Do NOT author pending/failed swap copy — the swap is always recorded completed immediately, so there is no such state to describe."
    - "Do NOT redesign `showTransactionDetails`. The ONLY authorised change is guarding the Network Fee row on a non-blank fee. Do not touch its layout, its identity block, its status handling, its explorer footer, or any other row."
    - "Do NOT edit anything under `packages/`, and do NOT add a value to `TransactionType` (D-19). That enum is Hive-persisted with explicit field indices."
    - "Do NOT change the slippage parse/apply behaviour in the settings drawer, its title, or its 'Slippage Tolerance (%)' label. The quick-chip row the sketch shows is OPTIONAL and not contracted — skip it."
    - "Do NOT hardcode colours in the settings drawer. D-15 requires WCAG AA in BOTH modes and ALL states — let the TextField inherit the shipped input theme and let GWButton own its own contrast, rather than pinning either to a mode-invariant literal."
---

<!-- planner-discipline-allow: SwapSuccessDrawer -->
<!-- planner-discipline-allow: SwapFailDrawer -->
<!-- planner-discipline-allow: SwapDrawerContent -->

<objective>
Point the swap result at the receipt the app already ships (031-B), delete the drawers that consolidation
supersedes rather than leaving them orphaned, and bring the Swap Settings drawer in line with the tab that
opens it. Re-confirm ROADMAP criterion 3 the way D-01 requires: the deviation stays, and it stays written
down — in the decision record AND in the code, guarded.

Purpose: D-06 names the Phase 18 trap explicitly — an orphaned helper whose passing tests implied a rule the
app no longer enforced. This plan removes the orphans in the same pass that supersedes them.

Output: one receipt call path for the swap tab, three files deleted, a corrected dev bubble, and a
redesigned settings drawer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-swap-bridge/08-CONTEXT.md
@.planning/phases/08-swap-bridge/08-RESEARCH.md
@.planning/phases/08-swap-bridge/08-UI-SPEC.md
@.planning/phases/08-swap-bridge/08-03-SUMMARY.md

# The receipt, the call sites, and the drawers being removed
@lib/dashboard/home/widgets/transaction_displays.dart
@lib/dashboard/home/widgets/transaction_utils.dart
@lib/squid_router/swap_screen.dart
@lib/squid_router/swap_settings_drawer.dart
@lib/dev/dev_tools_bubble.dart
@lib/components/buttons/gw_button.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route the swap result to 031-B and stop reporting the pay amount as a network fee</name>
  <files>lib/squid_router/swap_screen.dart, lib/dashboard/home/widgets/transaction_displays.dart, test/dashboard/transaction_receipt_fee_row_test.dart</files>
  <read_first>
    - lib/squid_router/swap_screen.dart, the submit closure as 08-03 left it (the `// TODO:` markers, the `Transaction(...)` construction with `fees: fromAmount`, the ToastManager call, the success-drawer call, `transactionsCubit.addTransaction`, and the `TransactionStorageService().addTransaction` await).
    - lib/dashboard/home/widgets/transaction_displays.dart lines 428-524 (`showTransactionDetails` in full — note the local `add()` helper's `if (value.trim().isEmpty) return;` skip, which `add('Rate', tx.exchangeRate ?? '')` already relies on, and the Network Fee line that composes `'${formatTxAmount(tx.fees)} ${tx.coinSymbol}'` so its value is NEVER blank even when `fees` is).
    - 08-RESEARCH.md § "Drawer consolidation mechanics" (what the function needs to serve a swap result, and that `exchangeRate` is never set so no Rate row appears).
    - 08-CONTEXT.md D-01, D-02, D-04.
  </read_first>
  <behavior>
    - A Transaction with a non-blank `fees` still renders a "Network Fee" row (existing behaviour, unchanged).
    - A Transaction with `fees: ''` renders NO "Network Fee" row at all — not a bare symbol, not a zero.
    - The `Rate` row's existing skip-empty behaviour is unaffected.
    - A swap Transaction (type swap, blank fees, empty hash) renders Date, Status, From, To, Network — and no Network Fee row and no explorer footer.
  </behavior>
  <action>
    (a) `transaction_displays.dart` — ONE guard, nothing else. Make the Network Fee row conditional on
    `tx.fees.trim().isNotEmpty` so a blank fee omits the row, exactly as a blank `exchangeRate` already omits
    the Rate row. Today the row's value is composed with the coin symbol appended, so a blank fee renders a
    lone symbol; that is the defect this closes. Add a one-line comment saying a blank fee means "no fee is
    known", so a future reader does not "restore" it. Change NOTHING else in the function.

    (b) Create `test/dashboard/transaction_receipt_fee_row_test.dart` covering the `<behavior>` bullets:
    pump `showTransactionDetails` (or the smallest wrapper that exercises its row assembly) with a
    non-blank-fee Transaction and assert the Network Fee row is found; with a blank-fee Transaction and assert
    it is NOT found; and with a swap-shaped Transaction and assert the From/To rows appear and Network Fee does
    not. Write the blank-fee expectation first and watch it fail before adding the guard.

    (c) `swap_screen.dart` submit closure — the ONLY authorised edits:
      • replace the success-drawer call with `showTransactionDetails(context, transaction)`, placed AFTER
        `transaction` is constructed and AFTER the existing ToastManager call, so the toast and the receipt
        both appear (finding 28's rule applies to swap too: alongside, never instead of);
      • change the Transaction's `fees:` argument from the pay amount to `''`. Nothing executed (D-01), so no
        network fee exists to report; with (a) in place the receipt simply omits the row instead of printing
        the pay amount under a "Network Fee" label. Add a short comment recording WHY it is blank.
      • drop the now-unused import of the deleted success drawer (Task 2 removes the file).
    Everything else in the closure is untouched, verbatim: the debugPrint, BOTH `// TODO:` markers, every
    other Transaction field, `transactionsCubit.addTransaction(transaction)`, and the
    `await TransactionStorageService().addTransaction(walletAddress, transaction)`. Do NOT add a Squid
    execution call. Do NOT set `exchangeRate` (leaving it unset is what correctly suppresses the Rate row).
  </action>
  <verify>
    <automated>flutter test test/dashboard/transaction_receipt_fee_row_test.dart</automated>
    <automated>rg -q "showTransactionDetails\(context, transaction\)" lib/squid_router/swap_screen.dart &amp;&amp; rg -q "ToastManager.instance.showToast" lib/squid_router/swap_screen.dart &amp;&amp; rg -q "transactionsCubit.addTransaction" lib/squid_router/swap_screen.dart &amp;&amp; rg -q "TransactionStorageService\(\)" lib/squid_router/swap_screen.dart &amp;&amp; echo PASS</automated>
    <automated>rg -q "// TODO: invoke Squid API" lib/squid_router/swap_screen.dart &amp;&amp; rg -q "// TODO: record transaction" lib/squid_router/swap_screen.dart &amp;&amp; echo "PASS: criterion 3 deviation still written down in code"</automated>
    <automated>rg -q "fees\.trim\(\)\.isNotEmpty" lib/dashboard/home/widgets/transaction_displays.dart &amp;&amp; echo PASS</automated>
    <automated>rg -q "SquidTokenService.execute|executeRoute|/execute" lib/squid_router/swap_screen.dart; test $? -ne 0 || echo "SQUID EXECUTION WAS WIRED — D-01 VIOLATED"</automated>
    <automated>flutter analyze lib</automated>
  </verify>
  <acceptance_criteria>
    The receipt is reached via `showTransactionDetails(context, transaction)` after the toast; the swap
    Transaction's fees are blank and the Network Fee row is provably omitted for a blank fee and retained for
    a real one; both D-01 markers survive verbatim; persistence and every other Transaction field unchanged;
    no Squid execution call exists; analyze at the 61 baseline.
  </acceptance_criteria>
  <done>Criterion 3 is closed by its second branch — the deviation is preserved, re-confirmed and guarded — and the swap result reuses the shipped receipt without inventing a fee.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete the three superseded drawers and repoint the dev bubble (D-06) without touching the reown path (D-05)</name>
  <files>lib/squid_router/swap_success_drawer.dart, lib/squid_router/swap_fail_drawer.dart, lib/squid_router/swap_drawer_content.dart, lib/dev/dev_tools_bubble.dart</files>
  <read_first>
    - lib/dev/dev_tools_bubble.dart lines 535-620 (the four swap-related dev buttons: 'Swap OK' and 'Swap fail' call the REOWN drawer — LEAVE THEM; 'Succeed' and 'Failed' call the two squid_router drawers — these are the ones to repoint) and lines 17-19 (its three drawer imports).
    - lib/squid_router/swap_success_drawer.dart, swap_fail_drawer.dart, swap_drawer_content.dart (confirm nothing else in the tree consumes them beyond what RESEARCH's call-site inventory lists).
    - 08-RESEARCH.md § "Superseded-drawer call-site inventory" (the authoritative table: which callers are production, which are dev-only, and the explicit instruction to leave the reown dev buttons alone).
    - 08-CONTEXT.md D-05 and D-06.
  </read_first>
  <action>
    (a) Re-verify the inventory before deleting: grep lib/ for each of the three class names and confirm the
    only remaining references are the dev bubble's two buttons and its imports. If any OTHER production caller
    turns up that RESEARCH did not list, STOP and record it in the SUMMARY rather than deleting.

    (b) Repoint the dev bubble's 'Succeed' and 'Failed' buttons to the shared receipt: build a small synthetic
    `Transaction` inline for each (swap type, a plausible from/to symbol+amount pair, a fake hash,
    `TransactionStatus.completed` for one and `TransactionStatus.failed` for the other) and call
    `showTransactionDetails(context, tx)`. This keeps a manual test entry point for the consolidated receipt
    and updates the tooltips to say so. Leave the button labels' positions and the `_devButton` helper alone.

    (c) Delete the three files: `lib/squid_router/swap_success_drawer.dart`,
    `lib/squid_router/swap_fail_drawer.dart`, `lib/squid_router/swap_drawer_content.dart`. Remove their two
    imports from the dev bubble. The fail drawer has had NO production caller at all — it goes too; leaving it
    as dead weight is precisely the Phase 18 trap D-06 names.

    (d) LEAVE UNTOUCHED: `lib/reown/swap_result_drawer.dart`, `lib/reown/handle_dapp_requests.dart`, the dev
    bubble's reown import, and its 'Swap OK' / 'Swap fail' buttons. Those two buttons are the only manual
    entry point to a Phase 10 file (D-05).
  </action>
  <verify>
    <automated>test ! -f lib/squid_router/swap_success_drawer.dart &amp;&amp; test ! -f lib/squid_router/swap_fail_drawer.dart &amp;&amp; test ! -f lib/squid_router/swap_drawer_content.dart &amp;&amp; echo "PASS: three drawers deleted"</automated>
    <automated>test -f lib/reown/swap_result_drawer.dart &amp;&amp; rg -c "SwapResultDrawer.show" lib/reown/handle_dapp_requests.dart | rg -q '^2$' &amp;&amp; rg -c "SwapResultDrawer.show" lib/dev/dev_tools_bubble.dart | rg -q '^2$' &amp;&amp; echo "PASS: D-05 reown path fully intact"</automated>
    <automated>rg -q "showTransactionDetails" lib/dev/dev_tools_bubble.dart &amp;&amp; echo PASS</automated>
    <automated>rg -v '^\s*//' lib/dev/dev_tools_bubble.dart | rg -c "squid_router/swap_success_drawer|squid_router/swap_fail_drawer|squid_router/swap_drawer_content"; test $? -ne 0 || echo "DANGLING IMPORT REMAINS"</automated>
    <automated>flutter analyze lib</automated>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    The three files no longer exist; nothing in lib/ imports them; the dev bubble's two former drawer buttons
    now open the shared receipt; the reown drawer file, its two production call sites and its two dev buttons
    are all still present and unmodified; analyze at baseline with no unresolved import; full suite 251/1.
  </acceptance_criteria>
  <done>Consolidation left no orphans, and the Phase 10 path is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-skin the Swap Settings drawer (D-13)</name>
  <files>lib/squid_router/swap_settings_drawer.dart</files>
  <read_first>
    - lib/squid_router/swap_settings_drawer.dart (whole file — 88 lines; the raw TextField with an OutlineInputBorder at :32-41 and the hand-rolled gradient ElevatedButton+Ink footer at :47-84 that D-13 names as the pre-redesign chrome).
    - 08-UI-SPEC.md § Component Inventory → "Swap settings drawer" and § Copywriting Contract (the title and label are PRESERVED; the quick-chip row is explicitly optional and not contracted).
    - lib/components/buttons/gw_button.dart (GWButtonVariant.gradient + expand).
    - lib/components/bottom_drawer/responsive_drawer.dart (it already provides the shell — do not change it).
  </read_first>
  <action>
    Re-skin in place; the slippage behaviour is untouched.
      • Replace the footer's hand-rolled gradient ElevatedButton + Ink + Container stack with
        `GWButton(variant: GWButtonVariant.gradient, expand: true, label: 'Apply', onPressed: ...)`, keeping
        the SAME callback body: parse `slippageController.text` with `double.tryParse`, call
        `onSlippageChanged(parsed)` only when non-null, then pop. Do not change the parse, the guard, or the pop.
      • Let the TextField inherit the app's shipped input theme instead of hardcoding an OutlineInputBorder;
        colour its text/hint through GWColors. Keep the numeric-with-decimal keyboard type, the controller
        seeded from `initialSlippage`, and the hint text.
      • Bring the "Slippage Tolerance (%)" label to `labelMd`/`gw.textSecondary` and the drawer padding to
        space8/space10 tokens. Keep the `ResponsiveDrawer.show<void>` call and the "Swap Settings" title
        exactly as they are.
      • Do NOT add the sketch's quick-chip row (0.1 / 0.5 / 1.0 / Custom). It is a new interaction pattern and
        D-13 only requires the drawer not read as pre-redesign chrome.
  </action>
  <verify>
    <automated>rg -q "GWButtonVariant.gradient" lib/squid_router/swap_settings_drawer.dart &amp;&amp; rg -q "onSlippageChanged" lib/squid_router/swap_settings_drawer.dart &amp;&amp; rg -q "double.tryParse" lib/squid_router/swap_settings_drawer.dart &amp;&amp; rg -q "Swap Settings" lib/squid_router/swap_settings_drawer.dart &amp;&amp; rg -q "Slippage Tolerance" lib/squid_router/swap_settings_drawer.dart &amp;&amp; echo PASS</automated>
    <automated>rg -v '^\s*//' lib/squid_router/swap_settings_drawer.dart | rg -c "ElevatedButton|Ink\(|greenBlueGreenGradient"; test $? -ne 0 || echo "HAND-ROLLED GRADIENT BUTTON STILL PRESENT"</automated>
    <automated>flutter analyze lib/squid_router/swap_settings_drawer.dart</automated>
  </verify>
  <acceptance_criteria>
    Footer is a gradient GWButton with the identical parse/guard/pop body; no hand-rolled gradient button
    remains; title and label preserved; no quick-chip row added; analyze clean on the file.
  </acceptance_criteria>
  <done>The drawer the tune icon opens reads as the same design language as the redesigned tab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthesized swap record → persisted Hive store | A Transaction describing a swap that did not execute is written to local storage (the D-01 deviation) |
| shared receipt function → two phases' flows | `transaction_displays.dart` is Phase-12-owned and consumed by Phase 12/15 surfaces as well as this phase |
| dev-only bubble → production drawers | The dev bubble is the only reachability path for a Phase 10 drawer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-18 | Repudiation | Persisted swap record for a swap that never executed | high | accept | KNOWINGLY ACCEPTED per D-01 and re-confirmed here; both `// TODO:` markers are grep-gated so the deviation cannot be silently "fixed" into a fund-moving call, and a negative grep asserts no execution call was added |
| T-08-19 | Information Disclosure | Pay amount surfaced as "Network Fee" on the shared receipt | medium | mitigate | The swap Transaction's fee is set blank and the receipt omits the row — routing to 031-B would otherwise newly expose develop's mislabelled value that the old drawer never displayed |
| T-08-20 | Tampering | Editing a Phase-12-owned shared function | medium | mitigate | Exactly ONE condition is added, honouring the function's own skip-empty contract; a new test asserts existing non-blank-fee behaviour is unchanged, and the full suite guards Phase 12/15's consumers |
| T-08-21 | Denial of Service | Deleting a file another surface still imports | medium | mitigate | Task 2(a) re-verifies the call-site inventory before deleting and STOPS on any unlisted production caller; `flutter analyze lib` catches a dangling import; the reown path has explicit presence gates |
| T-08-22 | Tampering | Slippage value applied to `swapParams` | low | accept | The parse/guard/pop body is preserved verbatim; only the button chrome changed |
| T-08-SC | Tampering | npm/pip/cargo/pub installs | n/a | accept | Zero packages added. Legitimacy gate not applicable |
</threat_model>

<verification>
- `flutter test test/dashboard/transaction_receipt_fee_row_test.dart` green (blank fee omits the row, real fee keeps it, swap shape renders From/To).
- Full `flutter test` stays 251 pass / 1 known pre-existing failure — this is the regression gate for Phase 12/15's use of the shared receipt.
- `flutter analyze lib` at or below the 61-issue baseline, with NO unresolved import (the specific signal that the three deletions were completed cleanly).
- Gates: `showTransactionDetails(context, transaction)` after the toast; BOTH D-01 markers present; no Squid execution call; blank-fee guard present; three drawer files absent; reown drawer file present with 2 production + 2 dev call sites; gradient GWButton in the settings drawer with its parse/guard/pop intact.
- NOT evidence here: that the receipt reads correctly for a swap, or that the settings drawer's contrast holds in light mode. 08-07's walk.
</verification>

<success_criteria>
One receipt serves the swap tab, the superseded drawers are gone rather than orphaned, the dApp path is
untouched, the settings drawer matches the tab, and criterion 3 is closed by re-confirming and guarding the
D-01 deviation rather than quietly implementing or dropping it.
</success_criteria>

<output>
Create `.planning/phases/08-swap-bridge/08-05-SUMMARY.md` when done.

## Artifacts this plan produces (08-05)
- Modified: `lib/squid_router/swap_screen.dart`, `lib/dashboard/home/widgets/transaction_displays.dart`, `lib/dev/dev_tools_bubble.dart`, `lib/squid_router/swap_settings_drawer.dart`
- Created: `test/dashboard/transaction_receipt_fee_row_test.dart`
- Deleted: `lib/squid_router/swap_success_drawer.dart`, `swap_fail_drawer.dart`, `swap_drawer_content.dart`
- NOT touched: `lib/reown/swap_result_drawer.dart`, `lib/reown/handle_dapp_requests.dart` (D-05)
Record in the SUMMARY: the re-verified call-site inventory, that the swap Transaction's fee is now blank and
why, and which rows the swap receipt actually renders in situ.
</output>
