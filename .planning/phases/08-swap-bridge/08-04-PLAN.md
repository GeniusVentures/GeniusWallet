---
phase: 08-swap-bridge
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - lib/dashboard/bridge/bridge_cta_state.dart
  - test/dashboard/bridge/bridge_cta_state_test.dart
  - lib/dashboard/bridge/bridge_screen.dart
autonomous: true
requirements: [SCR-04]
user_setup: []

must_haves:
  truths:
    - "The bridge screen reads as the Swap tab's twin (sketch 120 B1): the Phase 7 back-arrow AppBar convention, a 560px centred column, a source→destination network route bar, two amount cards, a gas card, and one gradient CTA. The Material AppBar title, both DropdownButtons and the TextButton CTA are gone. (D-10; verified: grep gates + 08-07 walk)"
    - "The destination network is chosen from a ResponsiveDrawer list fed by the SAME `availableBridgeNetworks` data and the SAME onItemChanged callback — a re-skinned selector, not a new data path. (D-10; verified: grep for ResponsiveDrawer + availableBridgeNetworks retained)"
    - "BRIDGE MECHANICS ARE UNTOUCHED: the 300ms debounce, the balance pre-check that returns BEFORE any API call, the `_isApiCallInProgress` guard, `getBrigeOutGasCost(...)`'s six arguments, the 1:1 echo of the pay amount into the receive field, and `bridgeOut(... shouldMintTokens: true)` all survive verbatim. (D-11; verified: grep gates)"
    - "`DecimalTextInputFormatter` still exists and is still applied to the pay amount field. The re-skin did not drop the bridge's only real input validation. (verified: grep gate)"
    - "Bridge stays a back-arrow sub-screen reached only by pushing `/bridge` — no tab, no swap/bridge toggle, and the You Pay side stays the single passed-in GNUS coin. (D-12; verified: grep for absence of a toggle + walk)"
    - "The gas row shows an em dash when no estimate is known — never a literal `0`, which would read as a free bridge. (honesty fix inside the re-skin; verified: grep + walk)"
    - "The bridge CTA ladder is a pure, unit-tested module with GNUS-denominated copy, and its ready label stays develop's 'Bridge' — NOT 'Review bridge', because no second confirmation step exists. (D-02 discipline extended to bridge; verified: test)"
  artifacts:
    - "lib/dashboard/bridge/bridge_cta_state.dart — NEW pure ladder module (states, precedence, labels, enabled rule)"
    - "test/dashboard/bridge/bridge_cta_state_test.dart — NEW"
    - "lib/dashboard/bridge/bridge_screen.dart — full visual replacement to sketch 120 B1 with mechanics preserved"
  key_links:
    - "onAmountChanged's debounce body ↔ getBrigeOutGasCost (the ONE place a real on-chain estimate is requested; its argument list must not drift)"
    - "availableBridgeNetworks / toNetwork ↔ the new picker drawer (same data, same setter)"
    - "DecimalTextInputFormatter ↔ the pay amount field (the only input validation bridge has)"
    - "transactionCost / isError ↔ the gas card and the CTA ladder (the re-skinned card must keep reading develop's state, not a new one)"
  prohibitions:
    - "Do NOT change bridge mechanics (D-11). The 1:1 rule, the gas-only cost, the 300ms debounce duration, the pre-API balance check, the `_isApiCallInProgress` guard, and every argument passed to `getBrigeOutGasCost` / `bridgeOut` stay exactly as they are. No rate row, no slippage row — bridge has neither."
    - "Do NOT add an 'Est. time' row. NO data source for it exists anywhere in the code; the UI-SPEC lists it speculatively. Rendering an invented duration is a fabricated claim."
    - "Do NOT add a swap/bridge toggle, promote bridge to a nav tab, or change how `/bridge` is reached (D-12)."
    - "Do NOT add an in-screen `isGnusBridgeEnabled` or zero-balance gate. RESEARCH verified that gate lives entirely at the CALLER (`token_info_screen.dart`, Phase 7 territory) and this phase must not touch it — D-12 is satisfied by `/bridge` never being reachable at zero balance, not by a second check here."
    - "Do NOT remove or bypass `DecimalTextInputFormatter`, and do NOT 'harden' the swap field to match it — the swap side's missing formatter is a pre-existing gap this phase flags, not fixes."
    - "Do NOT port the flip control's FUNCTION into bridge. Direction is fixed and chosen via the network picker; a flip affordance would be a dead or wrong control (UI-SPEC: decorative-only or omitted)."
    - "Do NOT replace the result AlertDialog or build a Transaction in this plan — plan 08-06 owns the bridge receipt. Here the submit closure's body is untouched apart from the submitting flag."
    - "Do NOT reuse `TokenSelectorDrawer` for the network picker. It is `SquidTokenInfo`-typed, not `Network`-typed; the picker body is new code on the existing ResponsiveDrawer shell."
    - "Do NOT 'fix' the gas figure itself. `transactionCost` is a gas PRICE string in Gwei that develop labels as a cost — a pre-existing inaccuracy in mechanics. Preserve the value; record the inaccuracy in the SUMMARY."
    - "Do NOT invent a new disabled tone and do NOT hardcode a sketch hex. D-15 requires WCAG AA in BOTH modes and in EVERY state including disabled — reuse the same shipped textPrimary38-on-surfaceMenu treatment the swap CTA uses and resolve every colour through the appearance-aware getters, so the twin screens cannot drift apart in one mode."
---

<objective>
Re-skin the 727-line bridge screen into sketch 120 B1 — the Swap tab's twin — while leaving every one of its
REAL on-chain mechanics exactly where they are. Bridge is the opposite of swap here: `getBrigeOutGasCost` and
`bridgeOut(... shouldMintTokens: true)` are genuine Web3 burn/mint calls, not stubs (RESEARCH Pitfall 4), so
the discipline is absolute — replace the widget tree, touch no argument.

Purpose: bridge is the largest file in the phase and the highest-consequence one. Instant recognition against
the redesigned Swap tab (D-10) is the goal; a changed API argument is the failure mode.

Output: a pure bridge CTA ladder with tests, and a bridge screen that looks like the swap tab and behaves
like develop.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-swap-bridge/08-CONTEXT.md
@.planning/phases/08-swap-bridge/08-RESEARCH.md
@.planning/phases/08-swap-bridge/08-UI-SPEC.md
@.planning/phases/08-swap-bridge/08-01-SUMMARY.md
@.planning/sketches/120-phase-8-flow/README.md
@.planning/sketches/121-phase-8-storyboard/README.md

# The screen, the vocabulary it must mirror, and the shipped conventions it reuses
@lib/dashboard/bridge/bridge_screen.dart
@lib/squid_router/swap_field.dart
@lib/components/bottom_drawer/responsive_drawer.dart
@lib/components/buttons/gw_button.dart
@lib/components/cards/gw_card.dart
@lib/theme/gw_colors.dart
@lib/tokens/token_info_screen.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the bridge CTA ladder into a pure, tested module</name>
  <files>lib/dashboard/bridge/bridge_cta_state.dart, test/dashboard/bridge/bridge_cta_state_test.dart</files>
  <read_first>
    - lib/dashboard/bridge/bridge_screen.dart lines 101-172 (the debounce body: the balance pre-check at :116-119, the `_isApiCallInProgress` guard, and the two setState branches that own `transactionCost` and `isError`) and lines 202-205 (today's whole disabled rule: `fromAmountController.text.isEmpty || isError`).
    - 08-UI-SPEC.md § Copywriting Contract → the bridge CTA rows (and note it leaves the ready label to the planner: this plan LOCKS "Bridge", develop's own word, because no review step exists to justify "Review bridge").
    - 08-CONTEXT.md D-11 and D-12 (what the ladder may and may not know about).
  </read_first>
  <behavior>
    - Empty / unparseable amount → `enterAmount`, "Enter an amount", disabled.
    - Amount 5, balance 1 → `insufficientBalance`, "Insufficient GNUS balance" (symbol interpolated), disabled.
    - Amount 1, balance 1 → NOT insufficient.
    - Balance null → treated as insufficient (bridge's own pre-check already does exactly this — `fromToken?.balance == null` sets isError), and the copy must still read as an insufficient-balance state, not an accusation about a parse.
    - Amount valid, estimate in flight → `estimatingGas`, "Estimating gas…", disabled.
    - Estimate failed (isError true, amount parseable and affordable) → `gasError`, "Couldn't estimate gas", disabled.
    - Amount valid, `transactionCost` present, no error → `ready`, "Bridge", ENABLED.
    - isSubmitting → `submitting`, "Bridging…", disabled — outranks every other rung.
    - Precedence, top to bottom: submitting → enterAmount → insufficientBalance → estimatingGas → gasError → ready.
  </behavior>
  <action>
    Create `lib/dashboard/bridge/bridge_cta_state.dart` as pure Dart (no Flutter UI import, no BuildContext):
      • `enum BridgeCtaState { enterAmount, insufficientBalance, estimatingGas, gasError, ready, submitting }`
      • `BridgeCtaState resolveBridgeCtaState({required String amount, required double? balance,
        required bool isEstimating, required bool hasEstimate, required bool isError,
        required bool isSubmitting})` implementing the `<behavior>` precedence exactly.
      • `String bridgeCtaLabel(BridgeCtaState state, {String? symbol})` — GNUS-denominated copy per
        `<behavior>`; fall back to "Insufficient balance" when symbol is null/empty.
      • `bool bridgeCtaEnabled(BridgeCtaState state)` → true ONLY for `ready`.
    This deliberately does NOT reuse the swap ladder module: bridge has no route fetch and no Retry rung, and
    swap has no gas estimate — a shared enum would have to carry rungs that are unreachable on one screen.
    Write the tests first (one per `<behavior>` bullet, including the exactly-affordable boundary, the null
    balance, and submitting-outranks-all), watch them fail, then implement to green.
  </action>
  <verify>
    <automated>flutter test test/dashboard/bridge/bridge_cta_state_test.dart</automated>
    <automated>rg -q "enum BridgeCtaState" lib/dashboard/bridge/bridge_cta_state.dart &amp;&amp; rg -q "resolveBridgeCtaState" lib/dashboard/bridge/bridge_cta_state.dart &amp;&amp; rg -q "'Bridge'" lib/dashboard/bridge/bridge_cta_state.dart &amp;&amp; echo PASS</automated>
    <automated>rg -q "Review bridge" lib/dashboard/bridge/bridge_cta_state.dart; test $? -ne 0 || echo "READY LABEL IMPLIES A REVIEW STEP THAT DOES NOT EXIST"</automated>
    <automated>rg -q "material.dart|BuildContext" lib/dashboard/bridge/bridge_cta_state.dart; test $? -ne 0 || echo "MODULE IS NOT PURE"</automated>
  </verify>
  <acceptance_criteria>
    All `<behavior>` bullets green; module is pure Dart; ready label is "Bridge"; `bridgeCtaEnabled` is true
    only for ready; no rung borrowed from the swap ladder.
  </acceptance_criteria>
  <done>Bridge's disabled-state copy is provable without a build, and it can never silently claim a review step exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bridge shell — back-arrow AppBar, 560px column, network route bar, destination picker drawer</name>
  <files>lib/dashboard/bridge/bridge_screen.dart</files>
  <read_first>
    - lib/dashboard/bridge/bridge_screen.dart lines 62-100 (the Material `AppBar(title: Text("Bridge"))`, the BlocBuilder, the `GeniusBreakpoints.medium` ConstrainedBox and the outer `Card`) and lines 177-198 (the destination `_buildDropdown&lt;Network&gt;` instantiation — its `availableItems`, `onItemChanged`, `displayText`, `displayIcon` are the data contract the new picker must honour) and lines 54-60 (`_fetchBridgeNetworks` seeding `toNetwork = networks.first`).
    - lib/tokens/token_info_screen.dart lines 92-140 (the SHIPPED Phase 7 back-arrow AppBar convention: 48px toolbarHeight, `gw.surfaceSunken`, 0 elevation, `automaticallyImplyLeading: false`, a 30×30 chevron InkWell, content-width-aligned padding — reuse this, do not invent sketch 120's own 34×34 back button).
    - 08-UI-SPEC.md § Screen Contracts → "Bridge screen — sketch 120 B1" (header treatment, network route bar, the tappable destination chip, column width guidance).
    - lib/components/bottom_drawer/responsive_drawer.dart (the `show<T>` signature — the shell the picker must use).
  </read_first>
  <action>
    (a) HEADER: replace the default Material AppBar with the Phase 7 back-arrow convention read above —
    48px toolbarHeight, `gw.surfaceSunken` background, 0 elevation, `titleSpacing: 0`,
    `automaticallyImplyLeading: false`, a compact 30×30 chevron InkWell calling
    `Navigator.of(context).maybePop()`, and a left-aligned "Bridge" title sharing the page's content padding.
    One back-arrow pattern for the whole app — do not introduce a second.

    (b) COLUMN: replace the `GeniusBreakpoints.medium` ConstrainedBox + outer `Card` with a centred
    `ConstrainedBox(maxWidth: 560)` column matching the Swap tab, page padding space10, on the scaffold's
    `gw.surfaceBase`. Keep the `SingleChildScrollView` and the `BlocBuilder<WalletDetailsCubit, ...>` exactly
    where they are — `state` is read inside the debounce body and the submit closure, so the builder must
    still wrap them.

    (c) NETWORK ROUTE BAR, directly under the header: source-chain chip · arrow · a small "Bridge" pill
    centred on the arrow · destination-chain chip. Source chip shows `state.selectedNetwork` (name + icon);
    destination chip shows `toNetwork`. Chips are `gw.surfaceMenu` + `gw.borderSubtle` pills matching every
    other pill in the app. The DESTINATION chip is tappable and opens the picker in (d); the source chip is
    NOT tappable (the source is the connected network, not a choice).

    (d) DESTINATION PICKER: delete the `_buildDropdown<Network>` instantiation and replace it with a
    `ResponsiveDrawer.show` list — one tappable row per entry of `availableBridgeNetworks`, each row showing
    the same `network.iconPath` asset (with the existing SizedBox errorBuilder fallback) and
    `network.name`, marking the current `toNetwork`, and on tap calling the SAME
    `setState(() => toNetwork = newNetwork)` the dropdown's `onItemChanged` did, then dismissing. Rows in
    `gw.surfaceMenu` / `gw.textPrimary`. Do NOT change `_fetchBridgeNetworks`, the asset read, or the
    `toNetwork = networks.first` seeding.

    (e) DEAD FIELD: remove the unused `int? previousNetwork` field (provably unreferenced — the same dead-code
    cleanup class as the swap screen's doubled guard). Change nothing else in the state class.

    Keep `_buildDropdown<T>` in the file for now only if Task 3 still needs it; Task 3 removes it.
  </action>
  <verify>
    <automated>rg -q "toolbarHeight: 48" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "automaticallyImplyLeading: false" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "maxWidth: 560" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "ResponsiveDrawer" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "availableBridgeNetworks" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; echo PASS</automated>
    <automated>rg -q "previousNetwork" lib/dashboard/bridge/bridge_screen.dart; test $? -ne 0 || echo "DEAD FIELD STILL PRESENT"</automated>
    <automated>rg -q "TokenSelectorDrawer" lib/dashboard/bridge/bridge_screen.dart; test $? -ne 0 || echo "WRONG-TYPED SELECTOR REUSED"</automated>
    <automated>flutter analyze lib</automated>
  </verify>
  <acceptance_criteria>
    Phase 7's back-arrow AppBar convention in place; 560px centred column; a network route bar with a tappable
    destination chip opening a ResponsiveDrawer list fed by `availableBridgeNetworks` and setting `toNetwork`
    through the same setter; no `DropdownButton` for Network remains; dead `previousNetwork` removed; analyze
    at the 61 baseline.
  </acceptance_criteria>
  <done>The bridge frame and its destination selector read as the redesigned app; the network data path is unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Bridge amount cards, gas card and gradient CTA — mechanics preserved verbatim</name>
  <files>lib/dashboard/bridge/bridge_screen.dart</files>
  <read_first>
    - lib/dashboard/bridge/bridge_screen.dart lines 80-198 (both `_buildDropdown` instantiations INCLUDING the whole `onAmountChanged` debounce body at :101-172 — copy that body verbatim into the new field's onChanged), lines 199-232 (the TextButton CTA, its disabled rule, and the `bridgeOut` call with its seven arguments plus the ToastManager call), lines 516-547 (the CTA style and the "Estimated Gas Cost" row), lines 566-698 (`_buildDropdown<T>` — the widget being deleted, note the `inputFormatters: [DecimalTextInputFormatter()]` at :662 and the `readOnly: onAmountChanged == null` at :664), lines 700-726 (`DecimalTextInputFormatter` — KEEP this class).
    - lib/squid_router/swap_field.dart as re-skinned by 08-01 (the vocabulary to mirror: surfaceElevated card, labelMd label, 38px hero amount, surfaceMenu pill, balance line) — bridge must LOOK like this without importing it (SwapField is SquidTokenInfo-typed; build the bridge's equivalent inline).
    - lib/dashboard/bridge/bridge_cta_state.dart (Task 1).
    - 08-UI-SPEC.md § Screen Contracts → "Bridge screen" (the two cards' labels, the gas-only card, the CTA ladder) and § Copywriting Contract → the bridge field labels (`You Pay` / `You Receive on {network}` — keep the "on {network}" qualifier so it never reads as a token conversion).
  </read_first>
  <action>
    (a) TWO CARDS, replacing both `_buildDropdown` instantiations. Each is a `gw.surfaceElevated` card
    (radiusLg, `gw.borderSubtle` hairline, space8/space10 padding) laid out like the re-skinned SwapField:
    label in `labelMd`/`gw.textSecondary`, the amount as
    `GeniusWalletTypography.numericDisplay.copyWith(fontSize: 38, height: 1.0)` in `gw.textPrimary`, and an
    asset-based token/network pill on `gw.surfaceMenu` + `gw.borderSubtle`.
      • "You Pay": the pill shows the passed-in `fromToken` (GNUS) and is NOT tappable — bridge is GNUS-only
        and the coin is fixed (D-12). Below the amount, keep develop's balance line
        ("&lt;balance&gt; &lt;symbol&gt;"). The TextField keeps `controller: fromAmountController`,
        `keyboardType: numberWithOptions(decimal: true)`, and — CRITICALLY —
        `inputFormatters: [DecimalTextInputFormatter()]`. Its `onChanged` receives the EXISTING debounce body
        copied verbatim: cancel the prior timer, a new `Timer(const Duration(milliseconds: 300), ...)`, the
        `_isApiCallInProgress` early return, the try/catch balance pre-check that sets `transactionCost = null`
        / clears `toAmountController` / `isError = true` and RETURNS BEFORE ANY API CALL, then the
        `context.read<GeniusApi>().getBrigeOutGasCost(...)` call with all six arguments unchanged, then the
        success branch (`toAmountController.text = value`, `transactionCost = gasCostResponse.data`,
        `isError = false`) and the failure branch. Do not restructure, reorder, or "tidy" this body.
      • "You Receive on {toNetwork.name}": the pill shows the destination network; the amount field stays
        read-only, driven only by `toAmountController` (bridge is 1:1 — the receive value is literally the
        echo the debounce body writes). No rate row anywhere.
      • Add `isEstimating` state around the gas call (set true when the debounce body reaches the API call,
        false in both outcome branches) so the ladder has a real signal. This is added state, not changed
        mechanics — the call itself and its arguments are untouched.

    (b) GAS CARD: a `gw.surfaceElevated` + `gw.borderSubtle` card at `radiusMd` with rows in the route-card
    idiom (label `labelMd`/`gw.textSecondary`, value `labelMd`/`gw.textPrimary`). Exactly TWO rows, both
    backed by real data: "You receive" (the 1:1 echo, `toAmountController.text` + the GNUS symbol) and
    "Estimated gas cost" (develop's `transactionCost` value, unchanged). When `transactionCost` is null render
    an em dash for that value — NOT the literal `0` develop prints, which reads as a free bridge. NO rate row,
    NO slippage row, and NO "Est. time" row (no data source exists for it).

    (c) CTA: replace the `TextButton` with
    `GWButton(variant: GWButtonVariant.gradient, size: GWButtonSize.lg, expand: true)`, rendered in EVERY
    state. Resolve state via `resolveBridgeCtaState(amount: fromAmountController.text, balance:
    fromToken?.balance, isEstimating: isEstimating, hasEstimate: transactionCost != null, isError: isError,
    isSubmitting: isSubmitting)`; label via `bridgeCtaLabel(state, symbol: fromToken?.symbol)`; pass
    `onPressed: null` whenever `bridgeCtaEnabled(state)` is false; use `isLoading` for the submitting rung.
    Map the disabled fills per the UI-SPEC table exactly as the swap CTA does: `gw.surfaceMenu` +
    `gw.textPrimary38` for enterAmount / estimatingGas / submitting; a `gw.statusError`-at-~12%-alpha fill
    with `gw.statusError` label for insufficientBalance and gasError.
    The ready rung's `onPressed` runs the EXISTING submit closure unchanged — `api.bridgeOut(...)` with all
    seven arguments including `shouldMintTokens: true`, the `if (!context.mounted) return;`, the
    ToastManager call with its existing success/error titles and messages, and the current `showDialog`
    AlertDialog — wrapped only with `isSubmitting = true` before and cleared in a `finally`. Plan 08-06
    replaces that dialog; this plan must not.

    (d) DELETE `_buildDropdown<T>` once nothing calls it. KEEP `DecimalTextInputFormatter` and KEEP
    `_cryptoIcon` (the AlertDialog still uses it until 08-06).
  </action>
  <verify>
    <automated>rg -q "DecimalTextInputFormatter\(\)" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "milliseconds: 300" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "_isApiCallInProgress" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "shouldMintTokens: true" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "getBrigeOutGasCost" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; echo PASS</automated>
    <automated>rg -q "resolveBridgeCtaState" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "GWButtonVariant.gradient" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; rg -q "You Receive on" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; echo PASS</automated>
    <automated>rg -v '^\s*//' lib/dashboard/bridge/bridge_screen.dart | rg -c "DropdownButton|_buildDropdown"; test $? -ne 0 || echo "OLD SELECTOR STILL PRESENT"</automated>
    <automated>rg -q "Est\. time|Estimated time" lib/dashboard/bridge/bridge_screen.dart; test $? -ne 0 || echo "FABRICATED TIME ROW PRESENT"</automated>
    <automated>rg -q "ToastManager.instance.showToast" lib/dashboard/bridge/bridge_screen.dart &amp;&amp; echo "PASS: finding-28 toast half preserved"</automated>
    <automated>flutter analyze lib</automated>
    <automated>flutter test</automated>
  </verify>
  <acceptance_criteria>
    Both amount cards wear the swap vocabulary; the pay field keeps its decimal formatter and the verbatim
    300ms debounce body with the pre-API balance check and all six gas-call arguments; the receive side is a
    read-only 1:1 echo with no rate row; the gas card has exactly two data-backed rows and an em dash for an
    unknown estimate; the CTA is a GWButton driven by the tested ladder; `bridgeOut(... shouldMintTokens:
    true)` and the ToastManager call are untouched; `_buildDropdown` is gone, `DecimalTextInputFormatter`
    stays; analyze at baseline; full suite 251/1.
  </acceptance_criteria>
  <done>The bridge screen is the Swap tab's twin and every real on-chain call still receives exactly the arguments develop gave it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user amount input → REAL on-chain burn | `bridgeOut` executes a genuine burn on the source chain and a mint on the destination; this is the highest-consequence path in the phase |
| local network asset JSON → destination chain id | `readNetworkBridgeAssets()` supplies `toNetwork.chainId`, which is passed straight into the contract call |
| RPC gas response → displayed cost | `transactionCost` is RPC-derived text reaching layout |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-12 | Tampering | Amount submitted to `bridgeOut` exceeding balance | high | mitigate | The pre-API balance check at the top of the debounce body is copied VERBATIM and grep-gated; it still returns before any call, and the ladder additionally disables the CTA on the insufficient rung. Nothing in the re-skin can widen what reaches the burn |
| T-08-13 | Tampering | Free-text amount reaching a contract call | medium | mitigate | `DecimalTextInputFormatter` is preserved and grep-gated on the pay field — the re-skin's specific regression risk (RESEARCH § Security Domain V5) |
| T-08-14 | Information Disclosure | Gas cost displayed as `0` when unknown | medium | mitigate | Unknown estimate renders an em dash, never a zero that reads as a free bridge |
| T-08-15 | Repudiation | Gas figure is a gas PRICE in Gwei labelled as a cost | low | accept | Pre-existing on develop (`genius_api` returns "X.XX Gwei"); computing a true cost is mechanics, out of scope for a re-skin. Recorded in the SUMMARY rather than silently corrected |
| T-08-16 | Elevation of Privilege | Reaching `/bridge` outside the GNUS + non-zero-balance gate | medium | accept | The gate lives at the caller (`token_info_screen.dart`, Phase 7, finding-37-locked). This plan is forbidden from touching it and from adding a second one; 08-07's walk re-confirms the entry path |
| T-08-17 | Information Disclosure | `network.iconPath` / `coin.iconPath` interpolated into an asset path | low | accept | Existing local-asset behaviour with the existing errorBuilder fallback; the data source is a bundled JSON this plan does not change |
| T-08-SC | Tampering | npm/pip/cargo/pub installs | n/a | accept | Zero packages added. Legitimacy gate not applicable |
</threat_model>

<verification>
- `flutter test test/dashboard/bridge/bridge_cta_state_test.dart` green.
- Full `flutter test` stays 251 pass / 1 known pre-existing failure.
- `flutter analyze lib` at or below the 61-issue baseline.
- Grep gates — the mechanics guard rail: `DecimalTextInputFormatter()` applied, `milliseconds: 300` present, `_isApiCallInProgress` present, `getBrigeOutGasCost` present, `shouldMintTokens: true` present, ToastManager call present; and on the visual side: 48px back-arrow AppBar, 560px column, ResponsiveDrawer picker, `You Receive on` label, gradient GWButton, no DropdownButton, no fabricated time row, no dead `previousNetwork`.
- NOT verified here: whether a REAL bridge submission succeeds. `bridgeOut` moves real funds (RESEARCH Pitfall 4) — no automated gate may invoke it. Whether the walk includes a live submission is an explicit question for 08-07's checkpoint.
</verification>

<success_criteria>
The bridge screen is sketch 120 B1 — visually the Swap tab's twin, still a back-arrow GNUS-only sub-screen —
and every argument reaching `getBrigeOutGasCost` and `bridgeOut` is byte-identical to develop's.
</success_criteria>

<output>
Create `.planning/phases/08-swap-bridge/08-04-SUMMARY.md` when done.

## Artifacts this plan produces (08-04)
- Created: `lib/dashboard/bridge/bridge_cta_state.dart`, `test/dashboard/bridge/bridge_cta_state_test.dart`
- Modified: `lib/dashboard/bridge/bridge_screen.dart` (120 B1 re-skin; mechanics preserved)
Record in the SUMMARY: that the gas figure is a Gwei gas PRICE labelled as a cost (pre-existing, not fixed),
that no "Est. time" row was rendered because no data source exists, the ready CTA label chosen ("Bridge") and
why, and confirmation that the AlertDialog result path is still intact for 08-06 to replace.
</output>
