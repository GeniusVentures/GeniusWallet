---
phase: 29-fee-transparency
plan: 04
type: execute
wave: 4
depends_on: [29-03]
files_modified:
  - test/squid_router/route_details_card_test.dart
autonomous: true
requirements: [FEE-02]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A route with no fee costs renders no fee row and no zero line — the normal same-chain case (crit 3, D-06)"
    - "Fee rows and the gas row are separately findable, never one merged string (crit 2)"
    - "The rows render and stay legible in BOTH appearances, asserted rather than assumed (crit 2)"
  artifacts:
    - test/squid_router/route_details_card_test.dart
  key_links:
    - "The both-appearance case flips the global appearance flag, not just the colour constructor"
---
<objective>
Close the rendering half: the empty route (which D-06 calls the NORMAL case), fee-versus-gas
distinctness, the multi-fee render, and both appearances. Resolves V-7 explicitly as parameterised
automated coverage rather than the dark-only coverage this file would otherwise inherit.
</objective>
<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/29-fee-transparency/29-VALIDATION.md
@.planning/phases/29-fee-transparency/29-03-SUMMARY.md
@AGENTS.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: The empty case, and fees visibly distinct from gas</name>
  <files>test/squid_router/route_details_card_test.dart</files>
  <read_first>
    - test/squid_router/route_details_card_test.dart (whole file, post plan 02)
    - test/squid_router/route_fixture.dart (`syntheticRouteWithFees`, added in plan 03)
  </read_first>
  <action>
    Give `_pumpCard` a sibling taking a `SwapQuote` directly so a synthetic quote can be pumped, with
    the existing helper delegating to it so current cases do not change shape.

    Add three cases. First the empty case, which D-06 calls the NORMAL case, not an edge case: pump
    `sameChainRoute` and assert the gas row and its `$0.01` are present while no fee row and no
    zero-dollar line exist — assert on the absence of any rendered text containing the word fee, not
    on one specific missing label, so a future placeholder row cannot slip past. Second, distinctness:
    pump `crossChainRoute` and assert the route fee label and the gas label are two separately
    findable strings and the two amounts likewise, with no single rendered string carrying both
    figures. Third, the render half of the multi-fee case: pump a quote built from the synthetic
    three-entry body and assert all three names and all three amounts render as their own rows, plus
    the gas row.
  </action>
  <verify>
    <automated>C:/Users/User/Documents/Projects/GNUS/flutter/flutter/bin/flutter test test/squid_router/route_details_card_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `sameChainRoute`: `find.textContaining('fee')` findsNothing, `find.text('\$0.00')` findsNothing, `find.text('Network gas')` and `find.text('\$0.01')` each findsOneWidget
    - `crossChainRoute`: `find.text('Gas receiver fee')`, `find.text('Network gas')`, `find.text('\$0.48')`, `find.text('\$0.02')` each findsOneWidget
    - Synthetic quote: all three fee names and all three amounts each findsOneWidget
    - `grep -rn 'toAmount -' lib/` returns nothing (criterion 4)
    - `grep -rn 'supergenius\|gnus.ai-wallet' lib/ test/` returns nothing (D-07: the integrator ID is never committed)
    - `route_details_card_test.dart` passes with 6 tests, 0 failures
  </acceptance_criteria>
  <done>The normal empty route, the multi-fee route and the fee-versus-gas distinction all have running coverage.</done>
</task>
<task type="auto">
  <name>Task 2: Both appearances, with the global flag actually flipped</name>
  <files>test/squid_router/route_details_card_test.dart</files>
  <read_first>
    - test/squid_router/route_details_card_test.dart lines 13-31 (the dark-only `_host`)
    - test/dashboard/transaction_filters_test.dart lines 38-57 (`gwFor` — the flip-plus-teardown shape)
    - test/theme/theme_contrast_test.dart lines 16-36 (`contrastRatio`, `themeFor`)
  </read_first>
  <action>
    This resolves V-7 as automated, not manual: the card's pump helper is dark-only today, so
    "readable in both appearances" has no coverage, and light mode is where this repo has historically
    broken. Parameterise `_host` and both pump helpers with an optional colour extension defaulting to
    dark, so no existing case changes.

    Add one case looping over both appearance modes. Constructing the light colours alone is NOT
    enough — the surface fields read a global appearance singleton, so a light instance built under a
    dark global hands back dark values, which is how a light-sounding case ends up asserting the dark
    branch. Follow the established shape: set the global appearance to the loop's mode, register a
    teardown restoring dark so no later file inherits a flipped flag, then build the matching colour
    extension. Assert the flip actually took before anything else, by checking the elevated surface's
    luminance crosses in the light iteration. Then pump `crossChainRoute` and assert the fee row and
    the gas row both render in each mode, and that the label and value tokens each clear 4.5:1 against
    the card's own surface, importing the existing contrast helper rather than writing a second one.

    If a contrast assertion fails that is a pre-existing token defect, not licence to introduce a
    colour here — this phase adds no colour and reads every token through the theme extension exactly
    as the adjacent rows already do. Report it in the SUMMARY and stop.
  </action>
  <verify>
    <automated>C:/Users/User/Documents/Projects/GNUS/flutter/flutter/bin/flutter test test/squid_router/route_details_card_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The new case runs once per appearance mode; both iterations find the fee row and the gas row
    - The light iteration asserts the elevated surface luminance exceeds 0.5 before its other expectations
    - A teardown restores the dark appearance; the full suite afterwards shows no new failures elsewhere
    - `grep -c 'GWColors.light()' test/squid_router/route_details_card_test.dart` returns at least 1
    - `bash tool/check_brace_style.sh` and `bash tool/check_raw_colors.sh` both exit 0
    - Full `flutter test` shows no regression against the baseline recorded in the plan-02 summary
  </acceptance_criteria>
  <done>Both appearances are covered by a running case, and the one that historically broke is the one now asserted.</done>
</task>
</tasks>
<threat_model>
Trust boundary: the Squid `/v2/route` HTTPS response entering `SquidSwapProvider` — unchanged by this
plan, which adds tests only: no production code, no network call, no user input, no key handling.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Denial of Service | `_feeLine`/`_feeLinesRaw` | medium | mitigate | Verified in plan 03; unchanged here. |
| T-29-02 | Tampering | `RouteDetailsCard` rows | high | mitigate | Task 1 re-runs the `toAmount` grep as an acceptance criterion at phase close — the final gate on D-04. |
| T-29-03 | Information Disclosure | `FeeLine`, `SwapQuote.feeLines` | low | accept | Unchanged: two fields, both from the route response, on a type that is not Bloc/Cubit state. |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | No install task; `pubspec.yaml` untouched (`git diff --stat pubspec.yaml` empty at phase close). |
</threat_model>
<verification>
Quick run per task, then at phase close: full `flutter test`, `flutter analyze`,
`bash tool/check_brace_style.sh`, `bash tool/check_raw_colors.sh`. Manual-only, carried to UAT: fee
rows at phone width in the running app — narrow-window overflow is not caught at the default test
surface size.
</verification>
<success_criteria>
V-1 through V-7 in `29-VALIDATION.md` each have a running automated owner except the phone-width walk,
recorded as manual; light mode is asserted, not assumed.
</success_criteria>
<artifacts>
## Artifacts this phase produces
Authoritative phase manifest is in `29-01-PLAN.md`. This plan adds a quote-taking sibling of `_pumpCard` and
an appearance parameter on `_host` (`test/squid_router/route_details_card_test.dart`), plus coverage
for the empty same-chain case, fee-versus-gas distinctness, the multi-fee render, and a
both-appearance render case with contrast assertions.

**No new files.** No `pubspec.yaml` change. No production code touched.
</artifacts>
<output>
Create `.planning/phases/29-fee-transparency/29-04-SUMMARY.md` when done (≤40 lines). Record the full
`flutter test` count so the next phase has a real baseline.
</output>
