---
phase: 29-fee-transparency
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - lib/swap/swap_quote.dart
  - lib/squid_router/squid_swap_provider.dart
  - lib/squid_router/route_details_card.dart
  - test/squid_router/route_details_card_test.dart
autonomous: false
requirements: [FEE-02]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A cross-chain route's own fee renders as its own row with its own amount, never merged (crit 1, D-03)"
    - "Chain gas renders as a separate row under our own label, distinct from any fee row (crit 2)"
    - "Nothing subtracts a fee from the receive amount; both figures come from one response (crit 4, D-04)"
  artifacts:
    - lib/swap/swap_quote.dart
    - lib/squid_router/squid_swap_provider.dart
    - lib/squid_router/route_details_card.dart
  key_links:
    - "squidQuote and squidQuoteFromJson both fill SwapQuote.feeLines through one shared converter"
    - "RouteDetailsCard iterates quote.feeLines with no name comparison anywhere"
---
<objective>
The tracer: carry ONE real route fee from wire body through adapter, domain type and widget to pixels, proven end to end on the recorded cross-chain fixture. Applies D-03 (render generically), D-04 (no fee arithmetic), D-05 (no hard-coded label). Plans 03-04 only widen coverage.
</objective>
<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/29-fee-transparency/29-CONTEXT.md
@.planning/phases/29-fee-transparency/29-PATTERNS.md
@AGENTS.md
</context>
<tasks>
<task type="checkpoint:decision" gate="blocking">
  <decision>What happens to the merged cost figure once fees are itemized</decision>
  <context>
    D-04 ("never do fee arithmetic") is this phase's core safety property, rated one-way in spirit:
    reintroducing arithmetic later silently reopens the disagreement it exists to prevent. Both options
    honour it — neither computes a net against the receive amount. The choice is whether ANY merged
    figure remains, which is what criterion 1 is about. Today the card shows one `$0.50` row labelled
    `Fees` (`route_details_card.dart:65`), merging a $0.48 service charge with $0.02 of chain gas.
  </context>
  <options>
    <option id="option-a">
      <name>Drop the merged row entirely (recommended)</name>
      <pros>Criterion 1 read literally — no collapsed figure survives. Smallest diff. A same-chain swap still shows its gas, so nothing regresses.</pros>
      <cons>No single "what this costs me" number; the reader adds two small figures.</cons>
    </option>
    <option id="option-b">
      <name>Keep a summed row, relabelled Total cost, below the itemized rows</name>
      <pros>One glanceable total survives with the itemization above it, so nothing is hidden.</pros>
      <cons>A merged figure remains — the shape criterion 1 objects to — and it is a sum computed in the widget.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>
<task type="tracer">
  <name>Task 2: End-to-end — one route fee reaches the screen carrying its own name</name>
  <files>lib/swap/swap_quote.dart, lib/squid_router/squid_swap_provider.dart, lib/squid_router/route_details_card.dart, test/squid_router/route_details_card_test.dart</files>
  <read_first>
    - lib/swap/swap_quote.dart, lib/squid_router/route_details_card.dart, test/squid_router/route_details_card_test.dart (whole files — const ctor, no Equatable, doc-comment budget, pump helpers)
    - lib/squid_router/squid_swap_provider.dart lines 165-205, 237-270 (both call sites, `_sumUsd`/`_sumUsdRaw`)
    - .planning/phases/29-fee-transparency/29-PATTERNS.md sections 1-3
  </read_first>
  <action>
    Wire ONE fee entry from wire body to pixels, on the `crossChainRoute` fixture only — plan 03 widens it.

    `swap_quote.dart`: add a plain `FeeLine` class above `SwapQuote`, `const` constructor, exactly two
    named-required fields `String name` and `double amountUsd`. No Equatable — nothing under `lib/swap/`
    has any. Add `SwapQuote.feeLines` as `List&lt;FeeLine&gt;` defaulting to `const []`; a required param
    breaks the one direct construction at `swap_submit_test.dart:81`. Keep `feesUsd`, `gasUsd` and
    `totalCostUsd` — D-03 says the new field is additive.
    `squid_swap_provider.dart`: add one private converter `_feeLine(String name, String amountUsd)` using
    the tolerant parse `_sumUsd` uses, `double.tryParse(...) ?? 0.0`, never the throwing parse — a cost
    the wire sends unreadably is worth nothing, not a crash mid-swap. Reach it from two thin wrappers
    mirroring the existing `_sumUsd`/`_sumUsdRaw` pair: `_feeLines` over the typed cost collection, and
    `_feeLinesRaw(Object? costs)` returning `const []` for a non-List and skipping any element that is
    not a Map. On the typed path the label MUST come from the serializer proved in plan 01, never the
    bare enum accessor; on the raw path read the `name` key as a string with an empty-string default.
    Fill `feeLines:` at BOTH `SwapQuote(` call sites — typed near line 170, raw near line 237. Add no
    fallback label for an empty name: the schema makes the field required, and inventing one is the
    over-building D-05 forbids.
    `route_details_card.dart`: convert the `_row` helper into a private `StatelessWidget` `_DetailRow` in
    the same file — mechanical move of the existing `Column`/`Padding`/`Row`/`Divider` body, same tokens,
    no visual change, reading the colour extension inside its own `build` rather than as a field, since
    helper methods returning a Widget are forbidden for new code and the fee rows are new rows. Replace
    the single merged row with one `_DetailRow` per entry of `quote.feeLines` — label the entry's name
    verbatim, value the inline `toStringAsFixed(2)` dollar string this repo already uses at four money
    sites, no formatter class — then one gas row labelled `Network gas` carrying `quote.gasUsd`. Iterate
    generically: no comparison of a fee name against any literal anywhere. Brace every new conditional
    with its body on its own line. Apply the Task 1 decision.
    Update the two cases this breaks by design: rename `the four rows survive` to a name true of the new
    row count and swap its merged-label assertion for the itemized labels; in `a second real quote
    renders different figures` replace the merged `$0.50` assertion with the itemized figures. Keep
    `every figure is read off the quote` passing — its `$0.01` assertion is criterion 3 and must survive.
  </action>
  <verify>
    <automated>C:/Users/User/Documents/Projects/GNUS/flutter/flutter/bin/flutter test test/squid_router/route_details_card_test.dart test/swap/squid_quote_mapping_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `crossChainRoute`: `find.text('Gas receiver fee')`, `find.text('\$0.48')`, `find.text('Network gas')`, `find.text('\$0.02')` each findsOneWidget; `find.text('Fees')` findsNothing
    - `sameChainRoute`: `find.text('\$0.01')` findsOneWidget (gas survives the split)
    - `grep -rn 'toAmount -' lib/` returns nothing (D-04) and `grep -rnE '(Integrator|Service|Axelar) fee' lib/` returns nothing (D-05)
    - `grep -rln 'package:squidrouter' lib/` lists only `squid_client.dart` and `squid_swap_provider.dart`
    - `grep -v '^\s*//' lib/squid_router/route_details_card.dart | grep -cE 'Widget +_[a-z]'` returns 0
    - `bash tool/check_brace_style.sh` and `bash tool/check_raw_colors.sh` both exit 0; `route_details_card_test.dart` passes with 3 tests, 0 failures
  </acceptance_criteria>
  <reversibility rating="one-way">D-04's no-arithmetic property: once a net is computed anywhere, the fee lines and the receive figure can disagree — the failure this phase exists to prevent.</reversibility>
  <done>One real route fee reaches the screen with its own name and amount, chain gas on its own row beside it, committed.</done>
</task>
</tasks>
<threat_model>
Trust boundary: the Squid `/v2/route` HTTPS response entering `SquidSwapProvider` — the only untrusted input. No new network call, no user input, no key handling.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Denial of Service | `_feeLine`/`_feeLinesRaw` | medium | mitigate | Tolerant parse only; non-List collection yields `const []`; non-Map element skipped. Written here, exercised in plan 03. |
| T-29-02 | Tampering | `RouteDetailsCard` rows | high | mitigate | No code path computes a net against the receive amount; gated by Task 1's blocking checkpoint and the `toAmount` grep in Task 2. |
| T-29-03 | Information Disclosure | `FeeLine`, `SwapQuote.feeLines` | low | accept | Two fields, both built only from the route response; `SwapQuote` is not Bloc/Cubit state, so this opens no channel for key- or seed-derived values. |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | No install task; `pubspec.yaml` untouched (`git diff --stat pubspec.yaml` empty at plan close). |
</threat_model>
<verification>
Quick run: `flutter test test/squid_router/ test/swap/squid_quote_mapping_test.dart`. Plan close: full `flutter test` (record the count as the phase baseline), `flutter analyze`, both shell gates.
</verification>
<success_criteria>
Route details name the route fee and chain gas as two separate rows with their own figures, on a real recorded response, and no merged figure survives under the old label.
</success_criteria>
<artifacts>
## Artifacts this phase produces
Authoritative phase manifest is in `29-01-PLAN.md`. This plan creates `FeeLine` and
`SwapQuote.feeLines` (`lib/swap/swap_quote.dart`), `_feeLine`/`_feeLines`/`_feeLinesRaw` (private,
`squid_swap_provider.dart`), `_DetailRow` (private `StatelessWidget`, `route_details_card.dart`,
replacing `_row`), the per-fee rows, the `Network gas` row, and the renamed row-count test. **No new
files, no `pubspec.yaml` change, `squidrouter/` untouched.**
</artifacts>
<output>
Create `.planning/phases/29-fee-transparency/29-02-SUMMARY.md` when done (≤40 lines).
</output>
