---
phase: 29-fee-transparency
plan: 03
type: execute
wave: 3
depends_on: [29-02]
files_modified:
  - test/squid_router/route_wrap_drift_test.dart
  - test/squid_router/route_fixture.dart
  - test/swap/squid_quote_mapping_test.dart
autonomous: true
requirements: [FEE-02]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Two or more fee entries map to distinct named lines, and an unrecognised name survives verbatim (crit 1 + 5, D-03, D-05)"
    - "Both adapter paths yield identical fee names and amounts for the same bytes"
    - "A malformed cost entry degrades to nothing, never to a crash mid-swap (T-29-01)"
  artifacts:
    - test/squid_router/route_fixture.dart
    - test/swap/squid_quote_mapping_test.dart
  key_links:
    - "The synthetic multi-fee body reaches both the mapping test and plan 04's render test from one shared helper"
---
<objective>
Prove the mapping layer generic: two paths that cannot disagree, more than one fee entry, a fee name
the wire schema does not list, and a malformed cost that degrades instead of throwing. None of this
is reachable from a recorded fixture, and none of it may be faked as one.
</objective>
<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/29-fee-transparency/29-VALIDATION.md
@.planning/phases/29-fee-transparency/29-02-SUMMARY.md
@AGENTS.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: The two adapter paths must not disagree about a fee name</name>
  <files>test/squid_router/route_wrap_drift_test.dart</files>
  <read_first>
    - test/squid_router/route_wrap_drift_test.dart lines 66-86 (the existing parity loop)
    - lib/squid_router/squid_swap_provider.dart (the converters written in plan 02)
  </read_first>
  <action>
    Extend the existing parity case `the raw mapper agrees with the generated one where both can read`
    to also compare fee lines across the two paths for `sameChainRoute` and `crossChainRoute`. The new
    domain type is a plain class with identity equality, so comparing the lists directly would always
    fail — compare the projected names and the projected amounts as two separate list assertions
    instead. This is the permanent guard against the typed path drifting back to the bare enum
    accessor, which would render a different string to a user than the raw path does. Keep the
    existing `reason: name` argument style on each new expectation.
  </action>
  <verify>
    <automated>C:/Users/User/Documents/Projects/GNUS/flutter/flutter/bin/flutter test test/squid_router/route_wrap_drift_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The parity case asserts the mapped names lists are equal and the mapped amounts lists are equal, for both fixtures
    - `route_wrap_drift_test.dart` passes with 7 tests, 0 failures
    - Temporarily reverting the typed path to the bare enum accessor makes this case fail (confirm once, then restore)
  </acceptance_criteria>
  <done>A fee name rendered through the typed path is provably the same string the raw path renders.</done>
</task>
<task type="auto">
  <name>Task 2: A synthetic multi-fee body, mapped generically</name>
  <files>test/squid_router/route_fixture.dart, test/swap/squid_quote_mapping_test.dart</files>
  <read_first>
    - test/squid_router/route_fixture.dart (whole file — loader conventions and doc-comment style)
    - test/swap/squid_quote_mapping_test.dart (whole file)
    - lib/squid_router/squid_swap_provider.dart lines 204-270 (what the raw mapper requires of a body)
  </read_first>
  <action>
    No recorded response carries two fee entries, and fabricating one under `fixtures/` would break the
    exact honesty property this phase establishes — so the multi-fee case is an inline synthetic body
    from a helper, not a file. Add `syntheticRouteWithFees` to `route_fixture.dart`, taking a list of
    cost maps and returning the smallest body the raw mapper accepts: a `route` map with a `quoteId`
    and an `estimate` map carrying parseable `fromAmount`, `toAmount` and `toAmountMin` strings,
    `fromToken` and `toToken` maps each with a numeric `decimals`, the caller's list under `feeCosts`,
    and one entry under `gasCosts`. Give it a doc comment of at most three lines saying WHY it is
    synthetic — no live route we can fetch carries two fee entries — naming no plan, phase or file.

    In `squid_quote_mapping_test.dart`, add cases driving `squidQuoteFromJson` over that helper. Prove
    three entries map to three lines in order with their own names and amounts, using one legal but
    uncommon name (`Boost fee`) and one the wire schema does not list at all (`Wormhole relayer fee`)
    — the raw path is what production uses precisely because the generated deserializer rejects an
    unknown fee name outright, so this is the path that must stay generic. Then prove the tolerant
    reads: a cost collection that is not a list yields no lines and does not throw; an element that is
    not a map is skipped; an element whose amount string is unreadable still yields its named line at
    zero rather than an exception.
  </action>
  <verify>
    <automated>C:/Users/User/Documents/Projects/GNUS/flutter/flutter/bin/flutter test test/swap/squid_quote_mapping_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Three synthetic entries yield `feeLines.length == 3`, names in order `Gas receiver fee`, `Boost fee`, `Wormhole relayer fee`
    - The unknown name survives verbatim — no substitution, no exception
    - Non-list collection yields empty `feeLines`; non-map element skipped; unreadable amount yields `0.0` for that line
    - `squid_quote_mapping_test.dart` passes with 0 failures, its 5 pre-existing cases still green
    - `ls test/squid_router/fixtures/ | wc -l` still returns 4 — no fabricated fixture was added
  </acceptance_criteria>
  <done>The generic path is proven on input no recorded fixture can supply, without faking a fixture.</done>
</task>
</tasks>
<threat_model>
Trust boundary: the Squid `/v2/route` HTTPS response entering `SquidSwapProvider` — unchanged by this
plan, which adds tests only: no production code, no network call, no user input, no key handling.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Denial of Service | `_feeLine`/`_feeLinesRaw` | medium | mitigate | Task 2's tolerant-read cases ARE the verification: non-list, non-map element and unreadable amount each degrade rather than throw. |
| T-29-02 | Tampering | `RouteDetailsCard` rows | high | mitigate | Unchanged from plan 02; re-gated at phase close in plan 04. |
| T-29-03 | Information Disclosure | `FeeLine`, `SwapQuote.feeLines` | low | accept | Unchanged: two fields, both from the route response, on a type that is not Bloc/Cubit state. |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | No install task; `pubspec.yaml` untouched (`git diff --stat pubspec.yaml` empty at plan close). |
</threat_model>
<verification>
Quick run: `flutter test test/squid_router/route_wrap_drift_test.dart
test/swap/squid_quote_mapping_test.dart`. Plan close: full `flutter test`, no regression against the
baseline recorded in the plan-02 summary.
</verification>
<success_criteria>
The fee list maps generically — more entries, an unknown name and a malformed entry all handled by the
same code path, with no name comparison anywhere and no fabricated fixture on disk.
</success_criteria>
<artifacts>
## Artifacts this phase produces
Authoritative phase manifest is in `29-01-PLAN.md`. This plan adds `syntheticRouteWithFees`
(`test/squid_router/route_fixture.dart`), the fee-line half of the existing typed-vs-raw parity case,
and mapping coverage for multi-entry, unrecognised-name and malformed-cost input.

**No new files.** No `pubspec.yaml` change. No fixture file added — the multi-fee body is synthetic
and inline by design.
</artifacts>
<output>
Create `.planning/phases/29-fee-transparency/29-03-SUMMARY.md` when done (≤40 lines).
</output>
