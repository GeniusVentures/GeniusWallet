---
phase: 29-fee-transparency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/squid_router/route_wrap_drift_test.dart
autonomous: true
requirements: [FEE-02]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The accessor that yields a human-readable fee name is proven by a running assertion, not inferred (D-03)"
  artifacts:
    - test/squid_router/route_wrap_drift_test.dart
  key_links:
    - "The typed adapter path must reach the wire label through the generated serializer, not the bare enum accessor"
---
<objective>
Wave 0. Settle the one unknown that would otherwise be decided by accident inside the tracer: how the
generated enum exposes a fee's human-readable name.

D-01 scopes this phase to FEE-02. D-02 (FEE-01 is server-side, no app lever) and D-07 (ship the ID
already in the gitignored config) need no code; both are guarded by greps in plans 02 and 04.

Output: one assertion. No production code changes here.
</objective>
<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/29-fee-transparency/29-CONTEXT.md
@AGENTS.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Pin how the generated enum exposes a fee name</name>
  <precondition>The Flutter SDK is not on PATH; it resolves at `C:/Users/User/Documents/Projects/GNUS/flutter/flutter/bin/flutter` (the doubled directory is correct). Halt if that binary is absent.</precondition>
  <files>test/squid_router/route_wrap_drift_test.dart</files>
  <read_first>
    - test/squid_router/route_wrap_drift_test.dart (whole file — the existing typed-vs-raw parity home)
    - squidrouter/lib/src/model/fee_type.g.dart lines 1-20 and 72-110
    - squidrouter/lib/src/model/fee_cost.dart lines 28-55 (the name field's declared type)
  </read_first>
  <action>
    Settle the research's one unexecuted assumption before any mapper is written against it. Add a
    test named `the generated fee enum hides the wire name behind its serializer`. Assert that the
    enum constant's `.name` accessor yields the screaming-case Dart identifier `GAS_RECEIVER_FEE`,
    and that the same constant passed through `standardSerializers.serializeWith(FeeType.serializer,
    ...)` yields the human label `Gas receiver fee` the wire actually carries. The file already
    imports the generated package, so no new import is needed. Add a two-line WHY comment — reading
    the accessor directly would put a screaming-case identifier in front of a user — naming no
    artifact, file, plan or phase.
  </action>
  <verify>
    <automated>C:/Users/User/Documents/Projects/GNUS/flutter/flutter/bin/flutter test test/squid_router/route_wrap_drift_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `route_wrap_drift_test.dart` passes with 7 tests (6 existing + 1 new), 0 failures
    - The new case asserts both the `.name` value and the serialized value
    - `grep -c 'GAS_RECEIVER_FEE' test/squid_router/route_wrap_drift_test.dart` returns at least 1
  </acceptance_criteria>
  <done>The wire-name accessor question is answered by a running assertion, not by inference.</done>
</task>
</tasks>
<threat_model>
Trust boundary: the Squid `/v2/route` HTTPS response entering `SquidSwapProvider` — the only
untrusted input in this phase. Phase 29 adds no network call, no new user input and no key handling.
This plan adds a test only.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Denial of Service | fee mapping in `squid_swap_provider.dart` | medium | mitigate | Tolerant parse only (`double.tryParse(...) ?? 0.0`); a non-List cost collection yields an empty list; a non-Map element is skipped. Implemented in plan 02, exercised in plan 03. |
| T-29-02 | Tampering | `RouteDetailsCard` fee and gas rows | high | mitigate | D-04: no code path computes a net against the receive amount. Gated by `grep -rn 'toAmount -' lib/` returning nothing, and by the blocking decision checkpoint opening plan 02. |
| T-29-03 | Information Disclosure | the new fee domain type | low | accept | It carries exactly two fields, both built only from the route response, and `SwapQuote` is not a Bloc/Cubit state class — so no key- or seed-derived value can reach a log through this addition. Enforced by the two-field shape, not by review. |
| T-29-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; `pubspec.yaml` is untouched and `squidrouter` is consumed as generated. Research's Package Legitimacy Audit records this as not applicable. |
</threat_model>
<verification>
`C:/Users/User/Documents/Projects/GNUS/flutter/flutter/bin/flutter test test/squid_router/route_wrap_drift_test.dart`
green. Record the answer in the SUMMARY — plan 02's adapter is written against it.
</verification>
<success_criteria>
The serializer-versus-accessor question has a running answer, so the typed path cannot silently render
a screaming-case identifier to a user.
</success_criteria>
<artifacts>
## Artifacts this phase produces

Authoritative manifest for the whole phase — later plans point here.

**Symbols** — `FeeLine` (`lib/swap/swap_quote.dart`; `String name`, `double amountUsd`) ·
`SwapQuote.feeLines` (`List<FeeLine>`, default `const []`) · `_feeLine`, `_feeLines`, `_feeLinesRaw`
(private, `lib/squid_router/squid_swap_provider.dart`) · `_DetailRow` (private `StatelessWidget`,
`lib/squid_router/route_details_card.dart`, replacing `_row`) · `syntheticRouteWithFees`
(`test/squid_router/route_fixture.dart`) · a quote-taking sibling of `_pumpCard` and an appearance
parameter on `_host` (`test/squid_router/route_details_card_test.dart`). All but the first plan's
test name land in plans 02-04.

**Rendered strings** — one row per `feeLines` entry, label = the fee's own name as the wire sent it ·
`Network gas`, our label for `quote.gasUsd` (chain gas carries no name on the wire, only a type).

**Tests** — `the generated fee enum hides the wire name behind its serializer` (this plan) · the
renamed row-count case, was `the four rows survive` (02) · fee-line parity and generic multi-fee
mapping (03) · empty-case, distinctness and both-appearance cases (04).

**No new files.** No `pubspec.yaml` change. No fixture file added. `squidrouter/` untouched.
</artifacts>
<output>
Create `.planning/phases/29-fee-transparency/29-01-SUMMARY.md` when done (≤40 lines).
</output>
