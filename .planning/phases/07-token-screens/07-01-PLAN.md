---
phase: 07-token-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/components/action_button.dart
  - lib/tokens/token_info_screen.dart
  - lib/components/qr/crypto_address_qr.dart
autonomous: true
requirements: [SCR-03]
user_setup: []

must_haves:
  truths:
    - "The four action buttons (Receive/Send/Swap/More) render in the redesign skin — surfaceElevated fill, brandPrimaryOnSurface icon glyph, textSecondary caption — all appearance-aware in both dark and light. (SCR-03; verified by 07-03 walk + analyze compile)"
    - "FINDING 37: tapping 'More' on a non-GNUS token does NOTHING (the button is disabled); it never opens an empty 'More Options' drawer. onPressed stays `isGnusBridgeEnabled ? (...) : null`. (verified: grep guard for the ternary + 07-03 walk on a non-GNUS token)"
    - "Send and Swap render (re-skinned) but stay INERT — presented as disabled ActionButtons with no onPressed; no enabled-looking button is dead. (D-01; verified: grep no onPressed on Send/Swap + walk)"
    - "FINDING 16/6: the Receive drawer's QR renders on a SOLID LIGHT background in BOTH appearance modes and scans with a phone camera in dark mode. (D-05; verified: grep opaque white + 07-03 camera scan)"
    - "_MarketDataInfo's Info card uses GWCard/appearance-aware surface, and its leading icons use a single restrained brandPrimaryOnSurface accent — the rainbow Material colors (amber/lightBlue/orange/red) are gone. (verified: grep no rainbow literals + walk)"
    - "_ConvertSection's card + TextFields wear the shipped GW theme; the local price×amount calc behavior is preserved unchanged. (verified: walk — enter an amount, Total updates)"
    - "Receive and More stay ResponsiveDrawer (bottom sheet ≤768px / desktop side dialog ≥768px); no new routes or full-screen promotion. (D-03; verified: grep ResponsiveDrawer.show retained)"
  artifacts:
    - "lib/components/action_button.dart — re-skinned, appearance-aware colours, disabled state visibly distinct + WCAG-legible"
    - "lib/tokens/token_info_screen.dart — re-skinned action row + Info card + Convert card + both drawers; finding 37 preserved; Send/Swap inert"
    - "lib/components/qr/crypto_address_qr.dart — opaque light QR background in both modes"
  key_links:
    - "ActionButton disabled state ↔ WCAG legibility in both dark and light (must stay distinct AND visible)"
    - "CryptoAddressQR.backgroundColor ↔ a fixed light surface, NEVER an appearance-aware token (finding 16 regression guard)"
    - "More button onPressed ↔ isGnusBridgeEnabled gate (finding 37)"
  prohibitions:
    - "Do NOT wire a Send flow, and do NOT create any /send route or Send*Screen (D-01 — deferred to its own future phase; Send stays a re-skinned but dead button, exactly as on develop)"
    - "Do NOT build an address book (deferred — no address_book screen exists; build-new is fenced OUT)"
    - "Do NOT wire Swap (Phase 8 owns it)"
    - "Do NOT re-skin or touch lib/chart/crypto_live_chart.dart in this plan (Phase 5 inherited; 07-02 owns only the finding-24 lifecycle guard)"
    - "Do NOT re-skin or edit the Markets tab / markets_screen.dart (Phase 16)"
    - "Do NOT promote the Receive or More drawers to full screens/routes (D-03)"
    - "Do NOT bind the QR background to an appearance-aware token (textPrimary*/surface*) — that is exactly the finding-16 regression"
---

<objective>
Re-skin the token-detail surface (`/token-info`) and its two drawers to the shipped redesign while
keeping develop's behavior. This is the core of SCR-03: the four-action button row, the balance/identity
`_MarketDataInfo` card, the `_ConvertSection` card, the Receive drawer (QR), and the More drawer all wear
`GWColors`/`gw_*` vocabulary. Two evidenced defects are locked in the same pass: finding 37 (More disabled
on non-GNUS tokens — preserve) and finding 16/6 (Receive QR must stay light-on-light for camera scanning).

Purpose: RE-SKIN ONLY — "re-skin, never restructure." No new capability is grown. Send and Swap stay
inert (D-01). ROADMAP criterion 2's "a send completes end to end" is an ACKNOWLEDGED DEFERRAL (D-02): no
send flow exists and none is built here — only the receive-QR half of that criterion is in scope.

Output: three re-skinned files (action button component, token-detail screen, QR widget). The dark+light
visual fidelity, the phone-camera QR scan, and the disabled-state legibility are proven by the 07-03 walk.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-token-screens/07-CONTEXT.md
@.planning/phases/07-token-screens/07-UI-SPEC.md
@.planning/reference/REVIEW_FINDINGS_REDESIGN.md

# Re-skin vocabulary + call sites
@lib/tokens/token_info_screen.dart
@lib/components/action_button.dart
@lib/components/qr/crypto_address_qr.dart
@lib/theme/gw_colors.dart
@lib/components/cards/gw_card.dart
@lib/theme/genius_wallet_decorations.dart
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin the ActionButton component onto appearance-aware GWColors</name>
  <files>lib/components/action_button.dart</files>
  <read_first>
    lib/components/action_button.dart (whole file — the legacy const defaults at :23-25),
    lib/theme/gw_colors.dart (surfaceElevated, textSecondary, textPrimary38 getters),
    lib/theme/theme.dart (GeniusWalletColors.brandPrimaryOnSurface usage — the appearance-aware accent),
    07-UI-SPEC.md "Component Inventory" ActionButton row + "Color" table.
  </read_first>
  <action>
    Migrate ActionButton's three legacy color defaults (backgroundColor=deepBlueCardColor,
    iconColor=lightGreenSecondary, textColor=Colors.grey) to the appearance-aware design system per
    UI-SPEC. Make the three Color fields nullable (default null); in build(), read
    `final gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();` and resolve: fill =
    widget.backgroundColor ?? gw.surfaceElevated; icon glyph = widget.iconColor ??
    GeniusWalletColors.brandPrimaryOnSurface (the appearance-aware accent); caption = widget.textColor ??
    gw.textSecondary. Keep borderRadiusCard (15px), the 13px/w500 caption, the AutoSizeText ellipsis, the
    rotate animation, and the Semantics(button:true, enabled: onPressed != null, label:) wiring exactly.
    DISABLED STATE (onPressed == null — Send, Swap, gated More): the button MUST stay visibly distinct
    from the enabled state AND remain WCAG-legible in BOTH modes (per the project WCAG contrast rule and
    the shipped disabled-state-visibility todo). Set explicit disabled fill/icon/caption via
    ElevatedButton.styleFrom disabledBackgroundColor / disabledForegroundColor (or a lowered-but-visible
    token such as textPrimary38 for the glyph over a dimmed surface) so a disabled button neither reads as
    enabled nor vanishes. Do NOT hardcode raw hex or Material palette colors; every color resolves through
    GWColors or the brandPrimaryOnSurface token.
  </action>
  <verify>
    <automated>rg -n "extension&lt;GWColors&gt;|surfaceElevated|brandPrimaryOnSurface|textSecondary" lib/components/action_button.dart &amp;&amp; rg -n "deepBlueCardColor|lightGreenSecondary|Colors.grey" lib/components/action_button.dart; test $? -ne 0 || echo "LEGACY DEFAULT STILL PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    ActionButton resolves fill/icon/caption from GWColors + brandPrimaryOnSurface (no legacy
    deepBlueCardColor/lightGreenSecondary/Colors.grey defaults remain); disabled state has explicit,
    legible, appearance-aware disabled colors; radius, caption style, Semantics(enabled:) unchanged.
  </acceptance_criteria>
  <done>Component compiles; enabled and disabled variants both render legibly in dark and light (proven at the walk).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin token_info_screen.dart (action row, Info card, Convert card, drawers) + lock finding 37</name>
  <files>lib/tokens/token_info_screen.dart</files>
  <read_first>
    lib/tokens/token_info_screen.dart (whole file — action row :147-203, More guard :179-198,
    _ConvertSection :235-313, _MarketDataInfo :315-455),
    REVIEW_FINDINGS_REDESIGN.md finding [37] (:221-225),
    07-UI-SPEC.md "Spacing Scale", "Color" (rainbow-icon cleanup paragraph), "Component Inventory".
  </read_first>
  <action>
    Re-skin the token-detail screen in place; keep develop's structure and behavior.
    (a) SPACING: replace raw literals with GeniusWalletConsts tokens per UI-SPEC — EdgeInsets.all(20.0)
    page padding and the Row/Column `spacing: 20` → space10; the action Row `spacing: 8` → space4; the
    action-section Column `spacing: 16` → space8; the drawer QR SizedBox(height: 32) stays in the QR
    widget (Task 3). No off-scale literals introduced.
    (b) ACTION ROW: Receive stays wired (opens the Receive ResponsiveDrawer). Send and Swap stay inert —
    keep them as `const ActionButton(text:..., icon:...)` with NO onPressed (D-01; they now render as the
    re-skinned disabled treatment from Task 1). Do NOT add an onPressed to Send or Swap.
    (c) FINDING 37 (LOCK — do not regress): the More button's onPressed MUST remain
    `isGnusBridgeEnabled ? () { ResponsiveDrawer.show(... child: SlidingDrawerButton(...)) } : null`
    (already correct at :179). Do NOT reintroduce the always-enabled + `if (isGnusBridgeEnabled)`
    empty-child pattern the finding flagged. Preserve the inner `selectedCoin?.balance == 0 ? null : ...`
    gate and the `/bridge` push. Give SlidingDrawerButton an appearance-aware color (e.g. gw.textPrimary
    or brandPrimaryOnSurface) instead of relying on the default.
    (d) _MarketDataInfo: swap the outer `Card(color: cs.surface)` for GWCard (or a Container using
    GWDecorations.surface) on surfaceElevated. Migrate every leading-icon color: the rainbow set
    (amber[300]/lightBlue[300]/orange[300]/red[300]) AND the `cs.primary` reads on Network/Address tiles
    → a single restrained brandPrimaryOnSurface glyph; the copy-icon and title/subtitle text →
    appearance-aware GWColors (textSecondary for labels/subtitles). The token-logo CircleAvatar KEEPS its
    raw `Colors.white` backing (§4.4 always-light-chip exception). Do NOT leave the removed rainbow color
    names in comments.
    (e) _ConvertSection: re-skin the `Card` → GWCard/surfaceElevated and let the two TextFields inherit
    the shipped input theme (do not hardcode InputDecoration colors). Preserve the price×amount calc, the
    controllers, and the "Total: $X" readout behavior exactly.
    Keep reusing the A2-redesigned CoinCardRow for the Info-card top slot (D-04) — do NOT build a bespoke
    hero; leave `_buildActionSection`'s `topSlot: CoinCardRow(...)` wiring intact.
    Behavior everywhere is develop's; this is a skin pass only.
  </action>
  <verify>
    <automated>rg -n "onPressed: isGnusBridgeEnabled" lib/tokens/token_info_screen.dart &amp;&amp; rg -n "GWCard|GWDecorations" lib/tokens/token_info_screen.dart &amp;&amp; ! rg -q "Colors\.amber|Colors\.lightBlue|Colors\.orange\[|Colors\.red\[" lib/tokens/token_info_screen.dart &amp;&amp; echo "PASS: finding37 guarded, GW surface used, rainbow removed"</automated>
  </verify>
  <acceptance_criteria>
    finding 37 ternary (`onPressed: isGnusBridgeEnabled ? ... : null`) present and intact; Send/Swap carry
    no onPressed; Info + Convert cards use GWCard/GWDecorations.surface; zero rainbow Material color
    literals remain; raw spacing literals replaced with space* tokens; convert calc behavior unchanged;
    token-logo CircleAvatar keeps raw white.
  </acceptance_criteria>
  <done>Screen compiles; `flutter analyze lib` introduces no new issues over the 61 baseline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Lock the Receive QR to an opaque light background in both modes (finding 16/6)</name>
  <files>lib/components/qr/crypto_address_qr.dart</files>
  <read_first>
    lib/components/qr/crypto_address_qr.dart (whole file — QrImageView backgroundColor at :26),
    07-UI-SPEC.md interaction lock 3 (Receive QR light-background HARD CONTRACT).
  </read_first>
  <action>
    Change the QrImageView `backgroundColor` from the current translucent white to a SOLID light surface
    that stays light over a dark drawer in BOTH appearance modes — use opaque `Colors.white` (or ≥0.9
    alpha). QR modules render default black on that light backing so a phone camera can scan it in dark
    mode. This is the documented §4.4 always-light exception: NEVER bind this background to an
    appearance-aware token (textPrimary*/surface*) — that is the exact finding-16 regression. Migrate the
    two `SizedBox(height: 32)` rhythm literals to GeniusWalletConsts.space16 and the `height: 8` to space4
    while here; leave the copy button and address text behavior unchanged.
  </action>
  <verify>
    <automated>rg -n "backgroundColor: Colors.white" lib/components/qr/crypto_address_qr.dart &amp;&amp; rg -n "backgroundColor:.*(textPrimary|surface)" lib/components/qr/crypto_address_qr.dart; test $? -ne 0 || echo "QR BOUND TO APPEARANCE TOKEN"</automated>
  </verify>
  <acceptance_criteria>
    QrImageView backgroundColor is an opaque/near-opaque light color (Colors.white), never an
    appearance-aware token; QR remains scannable over a dark drawer; spacing literals tokenized.
  </acceptance_criteria>
  <done>Widget compiles; QR proven scannable with a real phone camera in dark mode at the 07-03 walk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device screen → camera | Receive QR encodes the wallet's public receive address (already-public data); no secret crosses here |
| user → token-detail UI | Display-only surface this phase; Send stays INERT so NO transaction-signing / key path is introduced or reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | Receive QR (public address) | low | accept | Only the public receive address is encoded — develop behavior, no change; no private key or seed on this surface |
| T-07-02 | Elevation of Privilege | Send/Swap inert buttons | low | mitigate | Buttons carry NO onPressed and present as disabled — no send/swap/sign path exists to invoke (D-01); re-skin adds no capability |
| T-07-SC | Tampering | package installs | n/a | accept | No npm/pip/cargo/pub package added this phase — pure re-skin of existing files; legitimacy gate not applicable |
</threat_model>

<verification>
- `flutter analyze lib` introduces no new issues beyond the 61 baseline (compile/no-regression gate, not visual evidence).
- Grep guards above pass: finding-37 ternary intact, GW surfaces used, rainbow literals gone, QR background opaque light and not appearance-bound.
- Visual fidelity (dark+light), phone-camera QR scan, and disabled-state legibility are proven by plan 07-03's human walk — NOT asserted here.
</verification>

<success_criteria>
The token-detail screen and its two drawers wear the redesign in both modes; finding 37 preserved; Send/Swap
inert; QR light-on-light and scannable. ROADMAP criterion 2's send-half is recorded out-of-scope (D-02), not passed.
</success_criteria>

<output>
Create `.planning/phases/07-token-screens/07-01-SUMMARY.md` when done.

## Artifacts this phase produces (07-01)
- Modified: `lib/components/action_button.dart` (appearance-aware re-skin)
- Modified: `lib/tokens/token_info_screen.dart` (re-skin + finding 37 lock + inert Send/Swap)
- Modified: `lib/components/qr/crypto_address_qr.dart` (opaque light QR background)
No new routes, screens, or packages. Send flow and address book are DEFERRED (not produced).
</output>
