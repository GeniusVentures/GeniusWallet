---
phase: 07-token-screens
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/components/bottom_drawer/responsive_drawer.dart
autonomous: true
gap_closure: true
requirements: [SCR-03]
user_setup: []

must_haves:
  truths:
    - "The drawer header is a compact quiet band: LEFT-aligned title, close ✕ at TOP-RIGHT, a faint 1px brand-primary-subtle hairline under it. (gap 4, sketch 030-B1)"
    - "The close ✕ is a small top-right control — no oversized 24px-in-a-56px-leading affordance. (gap 4 — 'X too big')"
    - "The header no longer occupies excess vertical space — compact toolbar height. (gap 4 — 'header too tall')"
    - "The change is SAFE for every ResponsiveDrawer caller (~19 drawers): the desktop 420px side panel + mobile bottom sheet, appearance-aware surfaceMenu backgrounds, live-appearance flip, footer, and caller-supplied actions all still work. (D-03 — shared shell)"
  artifacts:
    - "lib/components/bottom_drawer/responsive_drawer.dart — 030-B1 quiet-band header chrome"
  key_links:
    - "AppBar centerTitle:false + left title ↔ 030-B1 left-aligned header"
    - "close IconButton moved from `leading` (leadingWidth 56) into top-right `actions` ↔ 030-B1 close top-right"
    - "AppBar.bottom PreferredSize 1px hairline ↔ brand-primary-subtle appearance-aware token"
  prohibitions:
    - "Do NOT add blanket body padding to the shell — many of the ~19 callers already pad their own bodies (e.g. the Receive drawer wraps its own Padding; SlidingDrawerButton pads 24). Blanket padding would double-pad and regress them. Header chrome only; leave body padding to callers."
    - "Do NOT change the desktop 420px side-panel / mobile bottom-sheet split, the appearance-aware surfaceMenu backgrounds, or the live-flip Theme dependency — preserve them exactly."
    - "Do NOT drop caller-supplied `actions` — the close ✕ must coexist with them at top-right."
    - "Do NOT remove or rename the documented shadow classes or trip tool/verify_additive_boundary.sh."
---

<objective>
Bring the shared `ResponsiveDrawer` chrome to the already-won sketch 030-B1 "Quiet band": a compact
left-aligned header with a small top-right close ✕ and a faint 1px brand hairline — fixing the Receive
drawer's oversized close and too-tall header. Because this is the one shell for ~19 drawers, the change is
header-chrome only and must not regress any caller.

Purpose: close walk gap 4 (Receive drawer close ✕ too big + header too tall) as drift from won sketch 030-B1.
Output: a re-skinned `responsive_drawer.dart` header that lands 030-B1 across all drawers at once, safely.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-token-screens/07-CONTEXT.md
@.planning/phases/07-token-screens/07-UI-SPEC.md
@.planning/sketches/030-drawer-shell/README.md
@.planning/sketches/drawers-final/README.md
@lib/components/bottom_drawer/responsive_drawer.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin the ResponsiveDrawer header to sketch 030-B1 quiet band (compact, left title, small top-right close)</name>
  <files>lib/components/bottom_drawer/responsive_drawer.dart</files>
  <read_first>
    - .planning/sketches/030-drawer-shell/README.md (Round 2, "B1 · Quiet band") and .planning/sketches/drawers-final/README.md ("030 Shell | B1 Quiet band") — the LOCKED shell: header is its own zone with a LEFT-aligned title (18px), close top-right, a faint 1px brand-primary-subtle (gradient) hairline under it, compact. It is the canonical chrome for all ~19 drawers.
    - lib/components/bottom_drawer/responsive_drawer.dart — current `_ResponsiveDrawerScaffold.build()` AppBar: `centerTitle: true`, `leadingWidth: 56`, a big padded `IconButton(Icons.close)` as `leading`, `actions: actions`, no hairline. The desktop 420 panel + mobile bottom sheet branches, the appearance-aware `gw.surfaceMenu` reads, and the live-flip Theme dependency comment must be preserved.
    - lib/theme/gw_colors.dart — the appearance-aware GWColors getters; pick the brand-primary-subtle / hairline token for the 1px underline (appearance-aware, faint).
  </read_first>
  <action>
    Re-skin ONLY the AppBar header chrome in `_ResponsiveDrawerScaffold.build()` to sketch 030-B1:

    1. Left-align the title: set `centerTitle: false` and keep the title text (titleMedium / ~18px per 030-B1).
    2. Move the close ✕ to the TOP-RIGHT and make it SMALL: remove `leading` and `leadingWidth: 56` (set
       `leading: null`, keep `automaticallyImplyLeading: false`); render the close as a compact IconButton
       (tight constraints / smaller iconSize, no 56px well) placed in `actions`, AFTER any caller-supplied
       `actions` (combine: caller actions first, then the close ✕). The close must coexist with caller actions,
       not replace them.
    3. Add a faint 1px brand-primary-subtle hairline UNDER the header via `AppBar.bottom`
       (`PreferredSize` of height 1 with an appearance-aware brand-primary-subtle color) — quiet, not flat blue.
    4. Make the header compact: a tight `toolbarHeight` so it no longer eats vertical space.

    Preserve everything else EXACTLY: the desktop 420px right-panel + mobile bottom-sheet branches, the
    `gw.surfaceMenu` backgrounds, the fail-soft `Theme.of(context).extension<GWColors>() ?? GWColors.dark()`
    live-flip dependency, the footer `bottomNavigationBar`, and the transparent/elevation:0 AppBar. Do NOT add
    any blanket body padding — body padding stays the callers' responsibility (see prohibitions).
  </action>
  <verify>
    <automated>bash -lc 'grep -q "centerTitle: false" lib/components/bottom_drawer/responsive_drawer.dart && ! grep -q "leadingWidth: 56" lib/components/bottom_drawer/responsive_drawer.dart && grep -Eq "bottom:\s*(const )?PreferredSize" lib/components/bottom_drawer/responsive_drawer.dart && grep -q "surfaceMenu" lib/components/bottom_drawer/responsive_drawer.dart && echo PASS'</automated>
  </verify>
  <acceptance_criteria>
    - `centerTitle: false`, title left-aligned; no `leadingWidth: 56` and no big leading close remain.
    - The close ✕ is a small top-right control in `actions`, coexisting with any caller `actions`.
    - A 1px brand-primary-subtle hairline (`AppBar.bottom` PreferredSize) renders under the header; toolbar height is compact.
    - The 420/bottom-sheet split, `surfaceMenu` backgrounds, live-flip, and footer are unchanged; no blanket body padding added.
    - Human re-walk (07-08) confirms the Receive drawer header is compact with a small top-right ✕, and spot-checks another drawer (e.g. More) for no regression.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Grep gate confirms left title, removed oversized leading, the hairline, and the retained surfaceMenu. `flutter
analyze lib` stays at/below the 61-issue baseline with no new issue in `responsive_drawer.dart` (no-regression
gate). `bash tool/verify_additive_boundary.sh` passes, and the Flutter test suite still compiles (shared-shell
safety). Cross-caller no-regression is spot-checked at the 07-08 re-walk.
</verification>

<success_criteria>
The drawer header matches sketch 030-B1 (compact, left title, small top-right ✕, faint hairline) with no
regression to any of the ~19 callers — grep gates pass and the 07-08 re-walk confirms Receive + one other drawer.
</success_criteria>

<output>
Create `.planning/phases/07-token-screens/07-06-SUMMARY.md` when done.

## Artifacts this phase produces (07-06)
- lib/components/bottom_drawer/responsive_drawer.dart — sketch 030-B1 quiet-band header chrome (left title, small top-right close, 1px brand hairline, compact height) applied to the shared shell without regressing callers.
</output>
