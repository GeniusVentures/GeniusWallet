---
phase: 07-token-screens
plan: 08
type: execute
wave: 3
depends_on: ["07-04", "07-05", "07-06", "07-07"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [SCR-03]
user_setup: []

must_haves:
  truths:
    - "GAP 1 confirmed: the token-detail page matches sketch 152 — two-panel at >=768, unified stack with Convert directly below the chart at <768 — in BOTH dark and light. (verified: human walk on a narrow AND a wide window)"
    - "GAP 2 confirmed: the Convert 'Token Price' cannot be edited; only 'Token Amount' is editable and it still drives the Total. (verified: human walk)"
    - "GAP 3 confirmed: the Receive QR is ~60% smaller (sketch 034-A2 — chip above, chunked address) AND still decodes on a REAL phone camera in dark mode. (verified: human walk with a phone)"
    - "GAP 4 confirmed: the Receive drawer header is a compact quiet band with a small top-right close ✕ (sketch 030-B1); a second drawer (e.g. More) shows no regression. (verified: human walk)"
    - "GAP 5 confirmed: More -> Bridge Tokens opens a populated, intentional drawer that routes to /bridge; on a NON-GNUS token More does nothing (finding 37). (verified: human walk on a GNUS and a non-GNUS token)"
    - "FINDING 24 stays clean: leaving the token chart mid-fetch / during a refresh tick throws no `setState after dispose`. (verified: human walk console watch)"
  artifacts: []
  key_links:
    - "walk evidence ↔ 07-VERIFICATION.md (flip to status: passed ONLY when all five gaps + finding-24 are earned)"
  prohibitions:
    - "Do NOT record 'a send completes end to end' (ROADMAP criterion 2, send half) as a PASS — it is the acknowledged D-02 deferral (no send flow exists). Keep it out-of-scope, not a gap closed here."
    - "Do NOT auto-approve — this is a blocking human checkpoint only the user's eyes/phone can satisfy; ignore any auto-advance."
    - "Do NOT flip 07-VERIFICATION.md to passed unless every one of the six checks passes in both modes; record any failure as OUTSTANDING for another gap-closure round."
---

<objective>
Re-walk the Phase 7 token-detail re-skin after gap closure (07-04..07-07) to confirm gaps 1-5 now pass in dark
and light, the Receive QR still scans, non-GNUS "More" is disabled (does nothing), and finding-24 stays clean —
then record the outcome to 07-VERIFICATION.md, flipping to passed only when earned. The D-02 send deferral stays
out-of-scope.

Purpose: close walk gap 6 (re-walk) and gate SCR-03 on observed truth — `flutter analyze` is a gate, never evidence.
Output: the re-walk outcome captured for 07-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-token-screens/07-CONTEXT.md
@.planning/phases/07-token-screens/07-UI-SPEC.md
@.planning/phases/07-token-screens/07-VERIFICATION.md
@.planning/phases/07-token-screens/07-03-SUMMARY.md
@.planning/phases/07-token-screens/07-04-SUMMARY.md
@.planning/phases/07-token-screens/07-05-SUMMARY.md
@.planning/phases/07-token-screens/07-06-SUMMARY.md
@.planning/phases/07-token-screens/07-07-SUMMARY.md
@.planning/sketches/152-token-detail-responsive/README.md
@.planning/sketches/034-receive-qr/README.md
@.planning/sketches/030-drawer-shell/README.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Re-walk gaps 1-5 + finding-24 in dark + light, confirm QR scan and non-GNUS More-disabled</name>
  <read_first>
    - .planning/phases/07-token-screens/07-VERIFICATION.md — the five gaps to flip and the earned passes to preserve.
    - .planning/sketches/152-token-detail-responsive/README.md, 034-receive-qr/README.md, 030-drawer-shell/README.md — the locked designs to check against.
    - 07-04..07-07 SUMMARYs — what each gap-closure plan changed.
  </read_first>
  <action>
    Hand the running Windows app to the human and walk the numbered recipe below in BOTH dark and light — do
    not proceed on your own judgement; every item is something only eyes or a phone camera can settle. Capture
    the verdict per numbered item into 07-VERIFICATION.md. Flip `status:` to `passed` ONLY if all six pass in
    both modes; otherwise record the specific failure as OUTSTANDING for another gap-closure round. Record
    ROADMAP criterion 2's send half as the out-of-scope D-02 deferral, never a pass.
  </action>
  <what-built>
    Gap closure for Phase 7: token-detail responsive layout at 768 with Convert below the chart on mobile and a
    read-only Convert price (07-04); the Receive QR brought to sketch 034-A2 — contained ~60%, network chip
    above, chunked copy-only address, opaque-white backing preserved (07-05); the shared ResponsiveDrawer header
    brought to sketch 030-B1 — compact quiet band, small top-right close, 1px brand hairline (07-06); and the
    More -> Bridge Tokens drawer body populated with real content that still routes to /bridge (07-07). Send and
    the address book remain deferred (D-01/D-02).
  </what-built>
  <how-to-verify>
    Build and run the Windows app (Flutter 3.41.9 at C:\Users\User\Documents\Projects\GNUS\flutter\flutter\bin\flutter.bat).
    Open a token-detail screen (tap a holding on the dashboard) and walk, in BOTH dark and light (toggle appearance):

    1. GAP 1 — LAYOUT (sketch 152): On a WIDE window (>=768) the page is two-panel (chart + actions left,
       Info + Convert right). Resize NARROW (<768): it becomes a single stack with the Convert card directly
       BELOW the chart and ABOVE Info. The switch happens around 768, not 1024. No timeframe tabs; only real data.
    2. GAP 2 — CONVERT READ-ONLY: try to edit the "Token Price" field — it must NOT accept edits. Edit "Token
       Amount" — the "Total: $…" readout updates.
    3. GAP 3 — RECEIVE QR (sketch 034-A2): tap Receive. The QR is clearly smaller (~60%), the coin/network chip
       is ABOVE it, the address shows as chunked mono with a copy affordance (copy-only). Scan the QR with a REAL
       phone camera in DARK mode — it must decode the address.
    4. GAP 4 — RECEIVE DRAWER HEADER (sketch 030-B1): the header is compact (not too tall), the title is
       left-aligned, and the close ✕ is small and top-right (not the old oversized one). Open one OTHER drawer
       (e.g. More on a GNUS token) and confirm its header looks right too (no shared-shell regression).
    5. GAP 5 — MORE / BRIDGE + FINDING 37: on a connected GNUS token, tap "More" — a populated Bridge Tokens
       drawer opens (description + a real action row) and tapping it routes to the bridge screen. On a NON-GNUS
       token, tap "More" — it must do NOTHING (disabled), never open an empty drawer.
    6. FINDING 24 (console watch): with the token chart visible, navigate AWAY mid-load and also leave it open
       across a refresh tick; watch the run console — ZERO `setState after dispose` / `setState() called after
       dispose()` lines.

    NOTE: ROADMAP criterion 2's "a send completes end to end" is NOT testable here — no send flow exists (D-02,
    deferred). Only the receive-QR half (step 3) is in scope; record the send half as out-of-scope, not a pass.
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass in both modes, or describe what failed (which gap, which mode) so it can be fixed and re-walked.</resume-signal>
  <acceptance_criteria>
    - Each of the six checks recorded PASS/OUTSTANDING in 07-VERIFICATION.md for both dark and light.
    - 07-VERIFICATION.md flips to `status: passed` only if all six pass; otherwise it stays gaps_found with the specific OUTSTANDING items.
    - The send half of criterion 2 is recorded as the D-02 out-of-scope deferral, never a pass.
    - No auto-approval occurred.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Human walk is the gate. Capture each of the six checks as PASS or OUTSTANDING (with the specific failure) into
07-VERIFICATION.md. Flip to `status: passed` only when all six are earned in both modes. The send half of
criterion 2 is an acknowledged out-of-scope deferral (D-02), never a pass. Do not auto-approve.
</verification>

<success_criteria>
All five gaps plus finding-24 pass in both dark and light — 07-VERIFICATION.md flips to passed — or every failure
is recorded honestly as OUTSTANDING for another gap-closure round.
</success_criteria>

<output>
Create `.planning/phases/07-token-screens/07-08-SUMMARY.md` when done (capture the re-walk outcome; update 07-VERIFICATION.md).

## Artifacts this phase produces (07-08)
- No source files. Produces the re-walk evidence that flips SCR-03 to passed (when earned) and preserves the D-02 send deferral.
</output>
