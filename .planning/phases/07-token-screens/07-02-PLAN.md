---
phase: 07-token-screens
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chart/crypto_live_chart.dart
autonomous: true
requirements: [SCR-03]
user_setup: []

must_haves:
  truths:
    - "FINDING 24: leaving the token chart mid-fetch, OR while its 1-minute refresh timer fires, throws no `setState after dispose`. Both post-await setState sites — the `_fetchHistoricalData` success path and the timer-driven `_addNewPricePoint` — are guarded with a mounted check. (SCR-03 / ROADMAP criterion 4; verified: grep for the guards + 07-03 walk console watch)"
  artifacts:
    - "lib/chart/crypto_live_chart.dart — mounted guard added ahead of the success-path setState and the _addNewPricePoint setState; the existing error-branch guard is preserved"
  key_links:
    - "post-await setState (_fetchHistoricalData success) ↔ `if (!mounted) return;`"
    - "timer-driven setState (_addNewPricePoint) ↔ `if (!mounted) return;`"
  prohibitions:
    - "Do NOT re-skin the chart visuals — the chart was already re-skinned in Phase 5 (quick task 260721-dws). This plan's ONLY change is the finding-24 lifecycle guard."
    - "Do NOT change chart behavior: zoom/pan, timeframe, data fetching, hover, tooltip, and layout all stay exactly as-is"
    - "Do NOT touch the 4 residual raw Colors.white on the zoom/pan IconButton row — that residue is tracked separately (deferred light-mode AA pass), NOT this plan's work"
---

<objective>
Fix finding 24: the token chart's two post-`await` `setState` calls run without a mounted guard, so
leaving the token screen mid-fetch — or letting the 1-minute refresh timer fire after the widget is
disposed — throws `setState after dispose`. Add `if (!mounted) return;` ahead of both. The chart's
redesign skin is INHERITED from Phase 5 and is NOT touched here.

Purpose: this is a lifecycle correctness fix (ROADMAP criterion 4), not a visual change. It is the ONLY
chart work Phase 7 owns.

Output: one guarded file. Runs in parallel with 07-01 (no file overlap).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-token-screens/07-CONTEXT.md
@.planning/reference/REVIEW_FINDINGS_REDESIGN.md
@lib/chart/crypto_live_chart.dart
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add mounted guards to the two unguarded post-await setState sites</name>
  <files>lib/chart/crypto_live_chart.dart</files>
  <read_first>
    lib/chart/crypto_live_chart.dart (_fetchHistoricalData :86-109 — the success-path setState at :96;
    _startLiveUpdates :111-123 and _addNewPricePoint :125-142 — the setState at :126; dispose :80-84),
    REVIEW_FINDINGS_REDESIGN.md finding [24] (:143-147) — names the two sites and the fix.
  </read_first>
  <action>
    Add `if (!mounted) return;` immediately before the `setState(() { ... })` in `_fetchHistoricalData`'s
    success branch (currently the unguarded setState at :96, inside the `if (historicalPrices.isNotEmpty)`
    block, after the `await fetchHistoricalPrices(...)`), and immediately before the `setState(() { ... })`
    in `_addNewPricePoint` (:126, reached from the periodic timer after `await fetchCoinsMarketData(...)`).
    This matches develop's intended guard and is consistent with the guard already present on the newer
    error branch. Change NOTHING else — no visual edits, no behavior edits, no re-skin. Keep `dispose()`
    cancelling the timer as-is.
  </action>
  <verify>
    <automated>test $(rg -c "if \(!mounted\) return;" lib/chart/crypto_live_chart.dart) -ge 2 &amp;&amp; echo "PASS: both post-await setState sites guarded"</automated>
  </verify>
  <acceptance_criteria>
    A `if (!mounted) return;` guard precedes both the `_fetchHistoricalData` success-path setState and the
    `_addNewPricePoint` setState; no other line of the chart changed; file compiles.
  </acceptance_criteria>
  <done>`flutter analyze lib` introduces no new issues over baseline; runtime absence of the exception is confirmed at the 07-03 walk (leave the chart screen mid-fetch; console stays clean).</done>
</task>

</tasks>

<verification>
- Grep guard confirms ≥2 `if (!mounted) return;` guards in the file (the two required sites; the pre-existing error-branch guard may make it ≥2).
- `flutter analyze lib` introduces no new issues beyond the 61 baseline.
- A reliable automated widget test is NOT available: `_fetchHistoricalData`/`_addNewPricePoint` call top-level CoinGecko network functions directly (not injectable), so a mount-then-dispose test would be network-flaky (same class as the flaky token_info_loader_test). The runtime proof is therefore the 07-03 walk's console watch, recorded honestly — not a synthetic PASS.
</verification>

<success_criteria>
Leaving the token chart mid-fetch or during a timer tick throws no `setState after dispose`. Chart visuals unchanged.
</success_criteria>

<output>
Create `.planning/phases/07-token-screens/07-02-SUMMARY.md` when done.

## Artifacts this phase produces (07-02)
- Modified: `lib/chart/crypto_live_chart.dart` (finding-24 mounted guards only)
No re-skin, no behavior change.
</output>
