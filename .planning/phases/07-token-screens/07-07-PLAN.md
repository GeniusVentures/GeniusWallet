---
phase: 07-token-screens
plan: 07
type: execute
wave: 2
depends_on: ["07-04"]
files_modified:
  - lib/tokens/token_info_screen.dart
  - lib/components/sliding_drawer_button.dart
autonomous: true
gap_closure: true
requirements: [SCR-03]
user_setup: []

must_haves:
  truths:
    - "The More -> Bridge Tokens drawer body reads as real, intentional content — not one lone button on empty space. It has a short description plus a proper Bridge Tokens action row (icon + label + trailing chevron) per the drawer-shell/quiet-band pattern. (gap 5)"
    - "Tapping Bridge Tokens still pops the drawer and pushes /bridge with the walletDetailsCubit, then refreshes coins — behavior preserved. (gap 5, behavior lock)"
    - "The inner balance gate (selectedCoin?.balance == 0 -> disabled) is preserved. (behavior lock)"
    - "FINDING 37 stays LOCKED: More's onPressed remains `isGnusBridgeEnabled ? (...) : null` — on a non-GNUS token More is DISABLED and opens nothing. (SCR-03 criterion 3)"
  artifacts:
    - "lib/tokens/token_info_screen.dart — populated More drawer body (child passed to ResponsiveDrawer.show)"
    - "lib/components/sliding_drawer_button.dart — (if adjusted) the Bridge Tokens row styling"
  key_links:
    - "ResponsiveDrawer.show child ↔ a structured Column (description + Bridge Tokens row), composed inside the 030-B1 shell from 07-06"
    - "Bridge Tokens row onPressed ↔ Navigator.pop + GoRouter.push('/bridge', extra: walletDetailsCubit) + getCoins()"
  prohibitions:
    - "Do NOT make More always-enabled or reintroduce the always-open + empty-child anti-pattern finding 37 flagged — keep `isGnusBridgeEnabled ? (...) : null`."
    - "Do NOT change the /bridge push, the extra: walletDetailsCubit payload, the getCoins() refresh, or the selectedCoin?.balance == 0 disable gate."
    - "Do NOT invent bridge features/data that the /bridge screen does not provide — this is a drawer content pass, not new bridge logic."
    - "Do NOT remove or rename the documented shadow classes or trip tool/verify_additive_boundary.sh."
---

<objective>
Fill the empty "More -> Bridge Tokens" drawer (which currently renders a single `SlidingDrawerButton` on
otherwise blank space) with meaningful, intentional content per the drawer-shell / quiet-band pattern: a short
description of what Bridge Tokens does plus a proper action row that routes to the real /bridge screen — while
keeping finding-37's disabled-More lock and all the existing bridge behavior.

Purpose: close walk gap 5 (Bridge Tokens drawer body reads empty).
Output: a populated More drawer body in `token_info_screen.dart` that composes inside the 030-B1 shell (07-06).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-token-screens/07-CONTEXT.md
@.planning/phases/07-token-screens/07-UI-SPEC.md
@.planning/phases/07-token-screens/07-01-SUMMARY.md
@.planning/phases/07-token-screens/07-04-SUMMARY.md
@.planning/sketches/030-drawer-shell/README.md
@.planning/sketches/drawers-final/README.md
@lib/tokens/token_info_screen.dart
@lib/components/sliding_drawer_button.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate the More -> Bridge Tokens drawer body (description + action row), keep finding-37 + bridge behavior</name>
  <files>lib/tokens/token_info_screen.dart, lib/components/sliding_drawer_button.dart</files>
  <read_first>
    - lib/tokens/token_info_screen.dart:181-205 — the "More" ActionButton: `onPressed: isGnusBridgeEnabled ? () { ResponsiveDrawer.show(title: "More Options", child: SlidingDrawerButton(...)) } : null` (finding-37 lock), the inner `selectedCoin?.balance == 0 ? null : () async { Navigator.pop; GoRouter.push('/bridge', extra: walletDetailsCubit); getCoins(); }`, label "Bridge Tokens", `color: gw.textPrimary`.
    - lib/components/sliding_drawer_button.dart — the current lone row (TextButton, 24px padding, icon + AutoSizeText label). Decide whether to reuse it inside a richer body or render a dedicated list-row.
    - .planning/sketches/030-drawer-shell/README.md + drawers-final ("032 List | A1 Comfortable") — the list/section content pattern: comfortable rows with an icon, a label, and (for a navigating row) a trailing chevron; appearance-aware surfaces, quiet-band chrome. The body composes inside the 030-B1 shell delivered by 07-06.
  </read_first>
  <action>
    Replace the bare `SlidingDrawerButton` child of the More drawer's `ResponsiveDrawer.show(...)` with a
    structured body Column that reads as intentional content:

    1. A short description line explaining the drawer (e.g. a one/two-line "Move your GNUS across chains with
       the bridge." — preserve the existing "More Options" title and the "Bridge Tokens" label copy).
    2. A proper Bridge Tokens action ROW below it — an icon (e.g. bridge/swap glyph) + the "Bridge Tokens"
       label + a trailing chevron, styled per the drawer-shell/quiet-band list pattern, appearance-aware
       (GWColors), spacing on GeniusWalletConsts.space* tokens. You may extend/adjust `SlidingDrawerButton`
       (add an optional trailing chevron) OR render a dedicated ListTile-style row — either is fine as long as
       the result reads as a real list item, not a lone floating button.
    3. Keep the row's onPressed EXACTLY: `selectedCoin?.balance == 0 ? null : () async { Navigator.of(context).pop();
       await GoRouter.of(context).push('/bridge', extra: walletDetailsCubit); walletDetailsCubit.getCoins(); }`
       — same disable-on-zero-balance gate, same /bridge push with the cubit payload, same coin refresh.
    4. Do NOT touch the outer `onPressed: isGnusBridgeEnabled ? (...) : null` on the More button — finding-37
       stays: on a non-GNUS token More is disabled and opens nothing.

    All colors appearance-aware; no hardcoded colors. The body renders inside the 030-B1 shell from 07-06.
  </action>
  <verify>
    <automated>bash -lc 'grep -q "onPressed: isGnusBridgeEnabled" lib/tokens/token_info_screen.dart && grep -q "push(.*/bridge" lib/tokens/token_info_screen.dart && grep -q "selectedCoin?.balance == 0" lib/tokens/token_info_screen.dart && echo PASS'</automated>
  </verify>
  <acceptance_criteria>
    - The More drawer body is a structured Column (description + a Bridge Tokens list-row), not a single bare button on empty space.
    - `onPressed: isGnusBridgeEnabled ? (...) : null` is unchanged (finding 37 locked).
    - The /bridge push (`extra: walletDetailsCubit`), the `getCoins()` refresh, and the `selectedCoin?.balance == 0` disable gate are all preserved.
    - Colors are appearance-aware; the body reads well in the 030-B1 shell in dark+light.
    - Human re-walk (07-08): on a connected GNUS token, More opens a populated Bridge Tokens drawer that routes to /bridge; on a non-GNUS token, More does nothing.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Grep gate confirms the finding-37 lock, the /bridge push, and the balance gate are all intact. `flutter analyze
lib` stays at/below the 61-issue baseline with no new issue in the two files. `bash tool/verify_additive_boundary.sh`
passes. The populated-body and disabled-non-GNUS behavior are confirmed at the 07-08 re-walk.
</verification>

<success_criteria>
The Bridge Tokens drawer reads as real content and still routes to /bridge, with finding-37 and the balance gate
intact — grep gates pass and the 07-08 re-walk confirms the GNUS-open / non-GNUS-disabled behavior.
</success_criteria>

<output>
Create `.planning/phases/07-token-screens/07-07-SUMMARY.md` when done.

## Artifacts this phase produces (07-07)
- lib/tokens/token_info_screen.dart — populated More -> Bridge Tokens drawer body (description + action row) composed in the 030-B1 shell.
- lib/components/sliding_drawer_button.dart — (if adjusted) Bridge Tokens row styling (optional trailing chevron).
</output>
