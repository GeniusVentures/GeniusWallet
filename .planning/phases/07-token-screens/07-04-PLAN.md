---
phase: 07-token-screens
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/tokens/token_info_screen.dart
autonomous: true
gap_closure: true
requirements: [SCR-03]
user_setup: []

must_haves:
  truths:
    - "The desktop/mobile layout switch happens at 768px (GeniusBreakpoints.medium), NOT 1024 — tablet-width windows get the two-panel layout. (gap 1, sketch 152)"
    - "On a <768 window the Convert card renders directly below the chart and ABOVE the Info card (unified stack D: actions → chart → Convert → Info). (gap 1, sketch 152 D)"
    - "On a >=768 window the page is two-panel (chart + actions left, Info + Convert in the right column) per sketch 152 A. (gap 1, sketch 152 A)"
    - "The Convert 'Token Price' field is read-only (display only); only 'Token Amount' is editable; Total = price x amount still computes on amount change. (gap 2)"
    - "Only real data is shown — no timeframe range tabs, no invented fields (rank/ATH/24h high-low). The chart keeps only its existing zoom/pan control. (sketch 152 data-honesty)"
  artifacts:
    - "lib/tokens/token_info_screen.dart — responsive layout at 768 + read-only Convert price"
  key_links:
    - "LayoutBuilder isDesktop condition ↔ GeniusBreakpoints.medium (768)"
    - "_ConvertSection price TextField ↔ readOnly:true (mirrors bridge_screen.dart:664 pattern)"
  prohibitions:
    - "Do NOT make Token Price editable — it is a read-only display of marketData.currentPrice."
    - "Do NOT add 1H/1D/1W/1M/1Y timeframe tabs or any per-range fetch control — the chart has no such fetch; keep only the existing zoom/pan (sketch 152 removed tabs on purpose)."
    - "Do NOT invent data fields (rank, ATH, 24h high/low, holders) — show only fields the code already has."
    - "Do NOT remove or rename the documented shadow classes (Loading, Splash, WalletsOverview) or otherwise trip tool/verify_additive_boundary.sh — this is an in-place re-skin."
---

<objective>
Bring the token-detail page to the locked responsive design (sketch 152): switch the desktop/mobile
layout at 768px instead of 1024, put the Convert card directly below the chart on mobile, and make the
Convert "Token Price" a read-only display so only the amount is editable.

Purpose: close walk gaps 1 (page layout drifts from sketch 152) and 2 (Convert price wrongly editable).
Output: a re-skinned `token_info_screen.dart` whose layout and Convert behavior match sketch 152.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-token-screens/07-CONTEXT.md
@.planning/phases/07-token-screens/07-UI-SPEC.md
@.planning/phases/07-token-screens/07-01-SUMMARY.md
@.planning/sketches/152-token-detail-responsive/README.md
@.planning/sketches/152-token-detail-responsive/index.html
@lib/tokens/token_info_screen.dart
@lib/utils/breakpoints.dart
@lib/dashboard/bridge/bridge_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive layout to sketch 152 — switch at 768, Convert below chart on mobile</name>
  <files>lib/tokens/token_info_screen.dart</files>
  <read_first>
    - .planning/sketches/152-token-detail-responsive/README.md and index.html — the LOCKED layout: A two-panel at >=768, D unified stack at <768 with Convert directly under the graph. Read-only price; NO timeframe tabs (chart keeps only zoom/pan); only real data.
    - lib/tokens/token_info_screen.dart:59-131 — the LayoutBuilder with `isDesktop = constraints.maxWidth > GeniusBreakpoints.large` (1024) and the current desktop Row / mobile Column trees; :211-238 `_buildActionSection` renders `_MarketDataInfo` (Info, carries the CoinCardRow identity top-slot) then `_ConvertSection` (Convert).
    - lib/utils/breakpoints.dart — GeniusBreakpoints.medium = 768 (the code's own `ResponsiveDrawer`/`useDesktopLayout` threshold that sketch 152 matches); large = 1024.
  </read_first>
  <action>
    Change the LayoutBuilder switch from `constraints.maxWidth > GeniusBreakpoints.large` to
    `constraints.maxWidth > GeniusBreakpoints.medium` so the two-panel layout engages at 768 (sketch 152's
    locked switch, matching GeniusBreakpoints.medium). No `> GeniusBreakpoints.large` may remain as the
    layout switch.

    Desktop branch (>=768, sketch 152 A — two-panel): keep the existing shape — chart + the static action
    row in the left Expanded, Info + Convert in the right Expanded. This already matches A; the only change
    here is the breakpoint above.

    Mobile branch (<768, sketch 152 D — unified stack): reorder the Column so Convert sits DIRECTLY below the
    chart and ABOVE Info. Target order top-to-bottom: static actions row -> graph section (chart, which
    carries the hero price/% pill) -> Convert card -> Info card. To do this, stop rendering Convert and Info
    together via a single `_buildActionSection` on mobile; render `_ConvertSection` and `_MarketDataInfo` as
    separate siblings so Convert can be interleaved between the chart and Info. Keep reusing the A2-redesigned
    CoinCardRow as the Info card's top-slot (D-04); do not build a bespoke hero. Match sketch 152 index.html
    as closely as the existing widgets allow — the two non-negotiables are (a) the 768 switch and (b) Convert
    immediately below the chart and above Info on mobile.

    Do not touch the chart widget itself, do not add timeframe tabs, and do not add any invented data fields.
    Keep every color appearance-aware (GWColors) and spacing on the GeniusWalletConsts.space* tokens.
  </action>
  <verify>
    <automated>bash -lc 'grep -q "constraints.maxWidth > GeniusBreakpoints.medium" lib/tokens/token_info_screen.dart && ! grep -q "> GeniusBreakpoints.large" lib/tokens/token_info_screen.dart && echo PASS'</automated>
  </verify>
  <acceptance_criteria>
    - The isDesktop condition reads `constraints.maxWidth > GeniusBreakpoints.medium`; no `> GeniusBreakpoints.large` remains as the layout switch.
    - Mobile (<768) Column renders `_ConvertSection` between the graph section and `_MarketDataInfo` (Convert above Info).
    - Desktop (>=768) keeps chart+actions left, Info+Convert right.
    - No timeframe tabs and no invented data fields were added; chart widget untouched.
    - Human re-walk (07-08) confirms the visual on both a narrow (<768) and wide (>=768) window in dark+light.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Make Convert "Token Price" read-only (only Token Amount editable)</name>
  <files>lib/tokens/token_info_screen.dart</files>
  <read_first>
    - lib/tokens/token_info_screen.dart:241-317 — `_ConvertSection` / `_ConvertSectionState`: `_tokenPriceController` seeded from `widget.tokenPrice` (marketData.currentPrice), the price TextField (:288-295) currently editable with `onChanged: (_) => _calculateTotalValue()`, the amount TextField (:296-303), and the "Total: $X" readout.
    - lib/dashboard/bridge/bridge_screen.dart:660-667 — the read-only pattern to mirror: a `TextField` with `readOnly: ...` that disables editing while still displaying the value.
  </read_first>
  <action>
    Make the "Token Price" TextField read-only so the user cannot edit the price — it is a display of
    marketData.currentPrice. Set `readOnly: true` on the price TextField and remove its `onChanged` price
    edit hook (the price no longer changes, so it need not recompute). Style it as a read-only display row
    mirroring the Bridge screen's read-only field (bridge_screen.dart:664) — appearance-aware, visibly a
    display (not an active input), yet legible in both dark and light (WCAG). Leave the "Token Amount"
    TextField fully editable with its `onChanged: (_) => _calculateTotalValue()`; Total must still update as
    the amount changes (Total = price x amount, price fixed). Keep the controllers and the calc otherwise
    intact.
  </action>
  <verify>
    <automated>bash -lc 'grep -Eq "readOnly:\s*true" lib/tokens/token_info_screen.dart && echo PASS'</automated>
  </verify>
  <acceptance_criteria>
    - The price TextField has `readOnly: true`; typing in it does nothing.
    - The amount TextField stays editable and updating it recomputes the Total.
    - The read-only price is visibly a display yet WCAG-legible in dark and light.
    - Human re-walk (07-08) confirms price cannot be edited and amount still drives Total.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Grep gates above confirm the 768 switch and the read-only price. `flutter analyze lib` must stay at or below
the 61-issue baseline with zero new issues in `token_info_screen.dart` (analyze is a no-regression gate, never
the visual evidence). `bash tool/verify_additive_boundary.sh` must pass (no shadow-class removal). Visual/behavioral
truth is settled by the 07-08 human re-walk.
</verification>

<success_criteria>
Layout switches at 768, Convert sits below the chart on mobile, and the Convert price is read-only — verified by
the grep gates and confirmed visually at the 07-08 re-walk in both appearance modes.
</success_criteria>

<output>
Create `.planning/phases/07-token-screens/07-04-SUMMARY.md` when done.

## Artifacts this phase produces (07-04)
- lib/tokens/token_info_screen.dart — responsive layout at the 768 breakpoint (sketch 152 A/D) with Convert directly below the chart on mobile, and a read-only Convert "Token Price".
</output>
