---
phase: 07-token-screens
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/components/qr/crypto_address_qr.dart
autonomous: true
gap_closure: true
requirements: [SCR-03]
user_setup: []

must_haves:
  truths:
    - "The Receive QR is contained (~60% of its current fill size) instead of filling the drawer — an explicit QR size, not a fill. (gap 3, sketch 034-A2)"
    - "The coin/network chip sits ABOVE the QR (not a 'Your <network> Address' heading below it). (gap 3, sketch 034-A2 / drawers-final)"
    - "The address is shown as a tappable mono block grouped into 4-char chunks with first/last chunks emphasized, copy-only (an inline/welded copy affordance). (gap 3, sketch 034-A2)"
    - "The QR renders black modules on a SOLID light background in BOTH dark and light so a real phone camera can scan it in dark mode. (finding 16/6, D-05 — preserved, not regressed)"
  artifacts:
    - "lib/components/qr/crypto_address_qr.dart — contained QR, network chip above, chunked address, copy-only"
  key_links:
    - "QrImageView size ↔ explicit contained dimension (self-contained, independent of the wrapping SizedBox)"
    - "QrImageView.backgroundColor ↔ raw Colors.white (opaque, appearance-independent) — finding 16/6 guard"
  prohibitions:
    - "Do NOT bind the QR backgroundColor to an appearance-aware token (textPrimary*/surface*) — keep it raw opaque Colors.white in both modes (finding 16/6). This is the documented §4.4 always-light exception."
    - "Do NOT add a Share button or a 'set as default receive address' action — the widget carries only CopyButton; those would need share_plus / logic the code lacks (respected audit gaps)."
    - "Do NOT remove or rename the documented shadow classes or trip tool/verify_additive_boundary.sh — in-place re-skin only."
---

<objective>
Bring the Receive QR widget to the locked sketch 034-A2 ("Grouped address") design: a contained QR (~60% of
the current oversized fill), the coin/network chip moved ABOVE the QR, and the full address shown below as a
tappable 4-char-chunked mono block — copy only. The QR stays black-on-white in both modes so it scans in dark.

Purpose: close walk gap 3 (Receive QR oversized) as drift from the already-won sketch 034-A2.
Output: a re-skinned `crypto_address_qr.dart` matching 034-A2, self-contained so it needs no change to its caller.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-token-screens/07-CONTEXT.md
@.planning/phases/07-token-screens/07-UI-SPEC.md
@.planning/phases/07-token-screens/07-01-SUMMARY.md
@.planning/sketches/034-receive-qr/README.md
@.planning/sketches/drawers-final/README.md
@lib/components/qr/crypto_address_qr.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin CryptoAddressQR to sketch 034-A2 — contained QR, network chip above, chunked address</name>
  <files>lib/components/qr/crypto_address_qr.dart</files>
  <read_first>
    - .planning/sketches/034-receive-qr/README.md (Round 2, "A2 · Grouped address") and .planning/sketches/drawers-final/README.md ("034 Receive | A2") — the LOCKED design: gap above QR, network chip moved ABOVE the QR, QR shrinks slightly, address grouped in 4-char chunks with first/last emphasized (eyeball-verify), copy icon welded in, borderless network chip (avatar + text), a bordered amber warning note, NO "Your <network> address" label, Copy-only (no Share, no set-default), QR stays black-on-white in both themes.
    - lib/components/qr/crypto_address_qr.dart — current widget: `QrImageView` fills its parent (no explicit size) with the caller's SizedBox(width: 320) making it ~304px; opaque `backgroundColor: Colors.white` (finding 16/6 — KEEP); a "Your $network Address" headlineSmall heading BELOW the QR; a "Use this address to receive tokens." line; a `CopyButton` with `WalletUtils.getAddressForDisplay(address)`.
    - lib/theme/gw_colors.dart — the appearance-aware GWColors getters (for the network chip text, the chunked-address emphasis, and the warning note color). Use a status/warning token for the amber note; do NOT use raw Colors.amber.
  </read_first>
  <action>
    Restructure the widget body to sketch 034-A2 while KEEPING the finding-16/6 QR contract:

    1. Give the QR an EXPLICIT contained size (~60% of the current ~304px fill, e.g. roughly 180-200px) via
       `QrImageView(size: ...)` so it is self-contained and no longer depends on the caller's SizedBox to
       shrink it. Keep `backgroundColor: Colors.white` (opaque, raw — finding 16/6), keep the embedded coin
       icon. QR modules render default black on the white tile in BOTH modes.
    2. Move the coin/network identity to a borderless chip ABOVE the QR (avatar/icon + network name text,
       appearance-aware textSecondary/textPrimary). Remove the "Your $network Address" headlineSmall heading
       — 034-A2 has no such label.
    3. Below the QR, show the FULL address as a tappable mono block grouped into 4-character chunks with the
       first and last chunks emphasized (heavier weight / textPrimary) and the middle chunks in
       textSecondary, with an inline/welded copy affordance. Tapping copies the full address (reuse the
       existing copy-to-clipboard behavior / CopyButton). Copy-only — no Share, no set-default.
    4. Keep the network warning as a quiet BORDERED amber note (appearance-aware warning token, WCAG-legible
       in both modes) — replace the plain "Use this address to receive tokens." line with the 034-A2 warning
       note weight.

    All colors appearance-aware via GWColors EXCEPT the QR's raw white backing (the documented §4.4 / finding
    16/6 exception). Spacing on GeniusWalletConsts.space* tokens.
  </action>
  <verify>
    <automated>bash -lc 'grep -Eq "QrImageView\(" lib/components/qr/crypto_address_qr.dart && grep -Eq "size:" lib/components/qr/crypto_address_qr.dart && grep -q "backgroundColor: Colors.white" lib/components/qr/crypto_address_qr.dart && ! grep -q "Your \$network Address" lib/components/qr/crypto_address_qr.dart && echo PASS'</automated>
  </verify>
  <acceptance_criteria>
    - `QrImageView` has an explicit `size:` (contained ~60%), and `backgroundColor: Colors.white` is retained (opaque, not an appearance token).
    - The "Your $network Address" heading is gone; the coin/network chip renders ABOVE the QR.
    - The address renders as a 4-char-chunked, copy-only mono block with first/last emphasis.
    - No Share / set-default action was added; the amber warning note is appearance-aware and legible.
    - Human re-walk (07-08) confirms the QR is ~60% smaller AND still decodes on a real phone camera in dark mode.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Grep gate confirms the contained `size:`, the retained opaque-white backing (finding 16/6), and the removed
heading. `flutter analyze lib` stays at/below the 61-issue baseline with no new issue in `crypto_address_qr.dart`
(no-regression gate, never the scan evidence). `bash tool/verify_additive_boundary.sh` passes. The scannability
and the ~60% size are settled visually at the 07-08 re-walk.
</verification>

<success_criteria>
The Receive QR matches sketch 034-A2 (contained, chip-above, chunked address, copy-only) and still scans in dark
mode — grep gates pass and the 07-08 re-walk confirms the phone-camera scan.
</success_criteria>

<output>
Create `.planning/phases/07-token-screens/07-05-SUMMARY.md` when done.

## Artifacts this phase produces (07-05)
- lib/components/qr/crypto_address_qr.dart — sketch 034-A2 Receive QR: contained QR, network chip above, 4-char-chunked copy-only address, warning note; finding-16/6 opaque-white backing preserved.
</output>
