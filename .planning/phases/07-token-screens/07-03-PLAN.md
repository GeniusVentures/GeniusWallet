---
phase: 07-token-screens
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified: []
autonomous: false
requirements: [SCR-03]
user_setup: []

must_haves:
  truths:
    - "The token-detail screen, action row, Info card, Convert card, and both drawers match the redesign in BOTH dark and light — nothing clipped, no unreadable text, no washed-out surfaces. (SCR-03 criterion 1; verified: human walk)"
    - "The Receive QR scans with a REAL phone camera in dark mode (light-on-light backing holds). (SCR-03 criterion 2, receive half; verified: human walk with a phone)"
    - "FINDING 37 confirmed LIVE: tapping 'More' on a non-GNUS token does nothing (disabled), never opens an empty drawer. (SCR-03 criterion 3; verified: human walk on a non-GNUS token)"
    - "Send and Swap read as clearly DISABLED yet legible in both dark and light (WCAG); neither looks tappable, neither vanishes. (D-01 + WCAG rule; verified: human walk)"
    - "FINDING 24 confirmed LIVE: leaving the token chart mid-fetch / during a refresh tick produces NO `setState after dispose` in the console. (SCR-03 criterion 4; verified: human walk console watch)"
  artifacts: []
  key_links:
    - "walk evidence ↔ 07-VERIFICATION.md (record PASS/OUTSTANDING per this project's no-unearned-PASS doctrine)"
  prohibitions:
    - "Do NOT record 'a send completes end to end' (ROADMAP criterion 2, send half) as a PASS — it is an ACKNOWLEDGED DEFERRAL (D-02): no send flow exists. Record it as out-of-scope, not a gap closed here."
    - "Do NOT auto-approve — this is a blocking human checkpoint that only the user's eyes/phone can satisfy"
---

<objective>
Human verification walk for the Phase 7 re-skin. Visual fidelity (dark+light), the QR phone-camera scan,
finding-37 disabled-More, disabled Send/Swap legibility, and the finding-24 clean-console runtime check are
things only a person at the running Windows app can judge — `flutter analyze` is a gate, never evidence.
This plan gates the phase on that walk.

Purpose: convert the 07-01/07-02 code changes into observed truth. Record results honestly (PASS /
OUTSTANDING) per this project's standing no-unearned-PASS rule; record the send-half of ROADMAP criterion 2
as an out-of-scope deferral (D-02), not a pass.

Output: walk outcome captured for 07-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-token-screens/07-CONTEXT.md
@.planning/phases/07-token-screens/07-UI-SPEC.md
@.planning/phases/07-token-screens/07-01-SUMMARY.md
@.planning/phases/07-token-screens/07-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Walk the token-detail re-skin in dark + light (fidelity, QR scan, finding 37, finding 24)</name>
  <action>
Hand the running Windows app to the human and walk the numbered recipe below in BOTH dark and light —
do not proceed on your own judgement; every item is something only eyes or a phone camera can settle.
Capture the verdict per numbered item into 07-VERIFICATION.md, and record ROADMAP criterion 2's send
half as an out-of-scope deferral (D-02), never a pass.
  </action>
  <what-built>
    The token-detail surface re-skinned to the redesign: appearance-aware ActionButton row (Receive wired;
    Send/Swap inert/disabled; More gated), the _MarketDataInfo Info card (GW surface + single-accent icons),
    the _ConvertSection card, the Receive drawer with a light-backed QR, and the More drawer — plus the
    finding-24 mounted guards on the token chart. Nothing new was wired: Send and the address book remain
    deferred.
  </what-built>
  <how-to-verify>
    Build and run the Windows app (Flutter 3.41.9 at C:\Users\User\Documents\Projects\GNUS\flutter\flutter\bin\flutter.bat).
    Open a token-detail screen (tap a holding on the dashboard) and walk, in BOTH dark and light (toggle appearance):

    1. VISUAL FIDELITY: the action row, Info card, Convert card, and page background wear the redesign —
       nothing clipped, all text legible, surfaces not washed out. Compare against the Release-exe reference.
    2. ACTION BUTTONS: Receive is enabled and clearly primary. Send and Swap read as clearly DISABLED
       (distinct from enabled) yet still legible — not vanished, not looking tappable — in BOTH modes.
    3. FINDING 37: on a NON-GNUS token, tap "More" — it must do NOTHING (button disabled). Confirm no empty
       "More Options" drawer opens. (On a connected GNUS token, More opens and Bridge Tokens is reachable.)
    4. RECEIVE QR (both modes): tap Receive; the QR sits on a solid light background in dark mode too. Scan
       it with a REAL phone camera in DARK mode — it must decode the address.
    5. CONVERT: type a token amount; the "Total: $…" readout updates (behavior preserved).
    6. FINDING 24 (console watch): with the token chart visible, navigate AWAY mid-load (immediately after
       opening) and also leave it open across a refresh tick; watch the run console — there must be ZERO
       `setState after dispose` / `setState() called after dispose()` lines.

    NOTE: ROADMAP criterion 2's "a send completes end to end" is NOT testable here — no send flow exists
    (D-02, deferred). Only the receive-QR half (step 4) is in scope; record the send half as out-of-scope.
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass in both modes, or describe what failed (which step, which mode) so it can be fixed and re-walked.</resume-signal>
</task>

</tasks>

<verification>
Human walk is the gate. Capture each of the six checks as PASS or OUTSTANDING (with the specific failure)
into 07-VERIFICATION.md. The send half of criterion 2 is recorded as an acknowledged out-of-scope deferral
(D-02), never a pass. Do not auto-approve.
</verification>

<success_criteria>
All six checks pass in both dark and light, or every failure is recorded honestly as OUTSTANDING for gap closure.
</success_criteria>

<output>
Create `.planning/phases/07-token-screens/07-03-SUMMARY.md` when done (capture the walk outcome; feed 07-VERIFICATION.md).

## Artifacts this phase produces (07-03)
- No source files. Produces the walk evidence that gates SCR-03 and records the D-02 send deferral.
</output>
