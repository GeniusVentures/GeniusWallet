---
phase: 06-onboarding
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - lib/onboarding/existing_wallet/view/legal_screen.dart
  - lib/onboarding/existing_wallet/view/select_wallet_type_screen.dart
  - lib/onboarding/routes/wallet_routes.dart
autonomous: false
requirements: [SCR-02, GAP-04]
must_haves:
  truths:
    - "The Legal step — ONE file with TWO callers (ExistingWalletFlow's ImportWalletStep.legal and NewWalletFlow's NewWalletStep.agreement, both already correctly wired) — wears the redesign: headlineLg/bodyMd token typography, the two link buttons as GWButton secondary, the CheckboxListTile replaced by GWCheckbox's own label param, and Continue as a GWButton gradient that is still DISABLED until the checkbox is accepted (UI-SPEC §4.3)"
    - "GAP-04 closes as a mechanical 1:1 component swap, not an open design question: select_wallet_type_screen.dart's Card+ListTile row becomes GWWalletCard — the primitive Alex already used for this exact data shape in SupportedExistingWallets — giving GWWalletCard (Phase 3 §2.2, zero callers to date) its first real non-gallery consumer, while the SupportedWallet model, the single hardcoded Ethereum entry, the three commented-out future networks, the ListView.separated shell and the ImportWalletSelected dispatch stay byte-identical (UI-SPEC §2.1, §2.2)"
    - "GAP-04's routing half is satisfied by preservation: wallet_routes.dart keeps every GoRoute path and builder unchanged; only the two identical inline fallback Text widgets get token typography, and the string 'Something went wrong! Please reload the app.' is preserved VERBATIM and deliberately NOT reconciled to GWErrorState — there is no retry handler this file can wire, since loadUserStatus is owned by AppBloc (UI-SPEC §2.3)"
    - "GWCheckbox's disabled and unchecked states stay visibly distinct in BOTH modes on the Legal step — the standing project rule, and the exact defect STATE's open 'dark-mode disabled-state visibility — checkbox + switch' todo already records for this primitive"
    - "The canonical Loading import in wallet_routes.dart (package:genius_wallet/components/loading.dart) is NOT repointed to the lib/components/loading/loading.dart shadow — a repoint the Dart compiler and flutter analyze see as no collision at all (03-SHADOW-NAMES.md)"
  artifacts:
    - "lib/onboarding/existing_wallet/view/legal_screen.dart — headlineLg + gw.textPrimary heading, bodyMd + gw.textSecondary body, two GWButton secondary link buttons, GWCheckbox replacing CheckboxListTile+AutoSizeText, GWButton gradient Continue with the accepted?onContinue:null gate preserved; both URLs and all copy verbatim"
    - "lib/onboarding/existing_wallet/view/select_wallet_type_screen.dart — GWWalletCard row, space10 separator, headlineLg/bodyMd headings; SupportedWallet class, the network list including its commented-out entries, and the ImportWalletSelected dispatch unchanged"
    - "lib/onboarding/routes/wallet_routes.dart — the two fallback Text widgets given bodyMd + gw.textSecondary; every GoRoute path, builder and BlocBuilder gate byte-identical; the canonical Loading import untouched"
  key_links:
    - "ExistingWalletFlow(_buildStep: ImportWalletStep.legal) AND NewWalletFlow(_buildStep: NewWalletStep.agreement) -> the SAME LegalScreen(accepted:, onToggle:, onContinue:) -> one re-skin serves both flows' first step; the two flows' differing onContinue bodies (LegalAccepted vs AgreementAccepted) are untouched"
    - "SelectWalletTypeScreen -> GWWalletCard(onTap:) -> context.read<ExistingWalletBloc>().add(ImportWalletSelected(walletName:, coinType:)) -> ImportWalletStep.importWalletSecurity -> the ImportSecurityScreen 06-04 re-skins"
    - "wallet_routes.dart /import_existing_wallet and /create_wallet -> BlocBuilder<AppBloc>.loadUserStatus gate -> canonical Loading | MultiBlocProvider(flow) | the preserved fallback Text; the gate's third branch is unreachable in practice and gets typography only, never an invented retry"
---

<objective>
Re-skin the two step screens that sit at the FRONT of the onboarding flows — the shared Legal step
(UI-SPEC §4.3, mounted by both flows) and the import flow's wallet-type chooser (§2.2) — plus the two
inline fallback strings in `wallet_routes.dart` (§2.3). Together these two files close GAP-04
completely and give a user their first two redesigned in-flow steps, mounting inside the transparent
step-bar 06-01 just landed.

Purpose: ROADMAP Phase 6 criterion 4 in full (the select-wallet-type step wears the extended design
language and still routes correctly through `wallet_routes.dart`) and criterion 1's legal/import-entry
half. Requirement GAP-04 is satisfied entirely by this plan.

GAP-04's premise, corrected (UI-SPEC §2.1 — do not re-litigate). The ROADMAP calls
`select_wallet_type_screen.dart` "develop-only, no mockup." Verified false in the sense that matters:
Alex's branch has no file of that NAME but has an almost exact structural and behavioral twin,
`SupportedExistingWallets` (consumed by `ImportWalletScreen`) — the same `SupportedWallet {name, image,
coinType}` model with the same three fields, the same hardcoded 1-item Ethereum list with the same three
commented-out entries, the same `ListView.separated` + `NeverScrollableScrollPhysics` + `shrinkWrap`,
the same `ImportWalletSelected` event on the same bloc — with the row widget already ported to
`GWWalletCard`. So "extend the design language" here is NOT an open design question. It is: swap the row
widget for the primitive Alex already used for this exact data shape, and change nothing else. No IA
change, so no product decision is owed under the GAP treatment test.

Re-skin, never restructure (§1): develop's `Center`/`ConstrainedBox(small * 2/3)`/`Column` tree is
locked on both screens. No `LayoutBuilder`, no breakpoint split, no `AppScreenWithHeader*` import.

Preserve, don't reconcile (§2.3, §6): `wallet_routes.dart` is pure `GoRoute` wiring and REQUIREMENTS.md
GAP-04 locks its flow and routing explicitly. Its only visible surface is two identical inline fallback
`Text` widgets on an unreachable-in-practice guard branch. They get typography and nothing else. They
must NOT become `GWErrorState`: `loadUserStatus` is written by `AppBloc`, not by anything this file can
reach, so a retry button here would have no action behind it — worse than plain text. This mirrors
Phase 5 §6's discipline for the dashboard's own "Something went wrong!" string.

Appearance-color access path (04-02/04-04 discipline): appearance-aware text/surface colors read via
`final gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();` so const-instanced subtrees
re-skin LIVE on an in-place toggle; mode-invariant brand tokens stay on the `GeniusWalletColors` static
getters.

Output: a token-correct shared Legal step, a `GWWalletCard`-based wallet-type chooser, and two
token-touched fallback strings — with every route, event and string preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-onboarding/06-UI-SPEC.md
@.planning/phases/03-gw-component-library/03-SHADOW-NAMES.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin legal_screen.dart — the shared first step of BOTH flows (§4.3)</name>
  <files>lib/onboarding/existing_wallet/view/legal_screen.dart</files>
  <read_first>
    - lib/onboarding/existing_wallet/view/legal_screen.dart (69 lines — the WHOLE file): `LegalScreen({accepted, onToggle, onContinue})`; `Center` + `ConstrainedBox(maxWidth: GeniusBreakpoints.small * 2 / 3)` + `Column(mainAxisSize: min, spacing: 20.0)` :20-26; "Legal" via `textTheme.headlineLarge` :27; body copy plain Text :28-30; two `SizedBox(width: double.infinity, child: OutlinedButton(...))` calling `launchWebSite(context, 'https://www.gnus.ai/privacypolicy.html')` and `'https://www.gnus.ai/tos.html'` :31-48; `CheckboxListTile(value: accepted, onChanged: (value) => onToggle(), title: AutoSizeText(...), controlAffinity: leading)` :49-56; `SizedBox(width: double.infinity, child: FilledButton(onPressed: accepted ? onContinue : null, child: Text("Continue")))` :57-63
    - lib/onboarding/existing_wallet/routes/existing_wallet_flow.dart :81-92 and lib/onboarding/new_wallet/routes/new_wallet_flow.dart :54-65 — the TWO call sites, to confirm the constructor signature must not change
    - lib/components/inputs/gw_checkbox.dart (`GWCheckbox({required value, required onChanged, label, description, tristate, enabled})`; `onChanged` is `ValueChanged<bool?>?`; disabled visuals use `gw.borderSubtle` for both side and fill)
    - lib/components/buttons/gw_button.dart (variants + sizes + the `expand` flag)
    - lib/theme/genius_wallet_typography.dart (headlineLg, bodyMd), lib/theme/gw_colors.dart (textPrimary, textSecondary; fail-soft read)
    - 06-UI-SPEC.md §4.3 (the substitution table), §4.1's flagged-not-fixed secondary-variant light-mode defect (inherited here too), §6 (every string on this screen is preserved verbatim, including the safety-relevant agreement label), §1 (locked structure)
    - .planning/todos/pending/2026-07-18-dark-mode-disabled-state-visibility-checkbox-switch.md (the open GWCheckbox disabled-state defect this screen will surface)
  </read_first>
  <action>
    Re-skin `legal_screen.dart` in place. The constructor signature (`accepted`, `onToggle`,
    `onContinue`) MUST NOT change — two flows construct it. Per §4.3:
    - Add `final gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();` at the top of
      `build()` (import `package:genius_wallet/theme/gw_colors.dart`; no bang).
    - "Legal" heading: `textTheme.headlineLarge` -> `GeniusWalletTypography.headlineLg.copyWith(color: gw.textPrimary)`.
    - Body copy: plain default-styled `Text` -> `GeniusWalletTypography.bodyMd.copyWith(color: gw.textSecondary)`.
    - "Privacy Policy" and "Terms of Service": each `SizedBox(width: double.infinity, child: OutlinedButton(...))`
      -> `GWButton(label: ..., variant: GWButtonVariant.secondary, size: GWButtonSize.lg, expand: true, onPressed: ...)`.
      Keep BOTH `launchWebSite` calls and BOTH URLs byte-identical. `expand: true` supersedes the
      `SizedBox(width: double.infinity)` wrapper — drop the wrapper rather than nesting both.
    - Agreement checkbox: `CheckboxListTile` + `AutoSizeText` title ->
      `GWCheckbox(value: accepted, onChanged: (_) => onToggle(), label: "I've read and accept the Terms of Service and Privacy Policy")`.
      Use `GWCheckbox`'s own `label` parameter — dropping the ListTile wrapper is the precedent Phase 4
      §3.1 set for `SwitchListTile` -> `GWSwitch`. Preserve the label string exactly, apostrophe
      included (§6). Remove the `auto_size_text` import if this was its only use in the file.
    - "Continue": `FilledButton` -> `GWButton(label: 'Continue', variant: GWButtonVariant.gradient, size: GWButtonSize.lg, expand: true, onPressed: accepted ? onContinue : null)`.
      The `accepted ? ... : null` gate is BEHAVIOR — preserve it exactly; `GWButton`'s own disabled
      visual state then replaces `FilledButton`'s default disabled look.

    Do NOT fix `GWButtonVariant.secondary`'s light-mode foreground — the same inherited, cross-cutting
    `gw_button.dart` defect §4.1 flagged, present on both link buttons here. Record, do not repair.

    Locked structure (§1): keep the `Center`, the `ConstrainedBox(maxWidth: GeniusBreakpoints.small * 2 / 3)`
    and the `Column`'s `spacing: 20.0` exactly as develop has them. No `LayoutBuilder`, no
    `useDesktopLayout` branch, no `app_screen_with_header_*` import.

    Token discipline (§7): zero raw hex, zero raw px typography, no `Colors.*` introduced.
    <!-- planner-discipline-allow: CheckboxListTile -->
    <!-- planner-discipline-allow: OutlinedButton -->
    <!-- planner-discipline-allow: FilledButton -->
  </action>
  <verify>
    <automated>flutter analyze lib/onboarding/existing_wallet/view/legal_screen.dart && grep -q 'extension<GWColors>()' lib/onboarding/existing_wallet/view/legal_screen.dart && grep -q 'GWCheckbox(' lib/onboarding/existing_wallet/view/legal_screen.dart && [ "$(grep -c 'GWButton(' lib/onboarding/existing_wallet/view/legal_screen.dart)" -ge 3 ] && ! (grep -vE '^\s*//' lib/onboarding/existing_wallet/view/legal_screen.dart | grep -qE 'CheckboxListTile|OutlinedButton|FilledButton|headlineLarge|LayoutBuilder|app_screen_with_header') && grep -q 'accepted ? onContinue : null' lib/onboarding/existing_wallet/view/legal_screen.dart && grep -q 'privacypolicy.html' lib/onboarding/existing_wallet/view/legal_screen.dart && grep -q 'gnus.ai/tos.html' lib/onboarding/existing_wallet/view/legal_screen.dart</automated>
  </verify>
  <done>
    The heading/body use headlineLg/bodyMd with gw.textPrimary/gw.textSecondary; both link buttons and
    Continue are GWButton (secondary/secondary/gradient) with both URLs and the launchWebSite calls
    intact; the checkbox is a GWCheckbox using its own label param with the agreement string verbatim;
    the `accepted ? onContinue : null` disabled gate is preserved; the constructor signature is
    unchanged so both flow call sites still compile; no legacy Material widget or headlineLarge lookup
    survives on a non-comment line; the locked layout arithmetic is unchanged; analyze reports no new
    issues.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: GAP-04 — swap the wallet-type row to GWWalletCard (§2.2) and token-touch wallet_routes.dart's two fallback strings (§2.3)</name>
  <files>lib/onboarding/existing_wallet/view/select_wallet_type_screen.dart, lib/onboarding/routes/wallet_routes.dart</files>
  <read_first>
    - lib/onboarding/existing_wallet/view/select_wallet_type_screen.dart (88 lines — the WHOLE file): the `supportedNetworks` list with ONE `SupportedWallet(name: 'Ethereum', image: 'assets/images/ethereum_icon.png', coinType: TWCoinType.TWCoinTypeEthereum)` plus THREE commented-out entries :12-22; `Center`+`ConstrainedBox(small * 2/3)`+`Column(spacing: 20.0)` :24-30; "Import Wallet" via `textTheme.headlineLarge` :31-34; subtitle plain Text :35; `ListView.separated(itemCount:, separatorBuilder: (c,i) => const SizedBox(height: 20), physics: NeverScrollableScrollPhysics, shrinkWrap: true)` :36-70; the row `Card(clipBehavior: Clip.hardEdge, child: ListTile(contentPadding:, leading: Image.asset(network.image, package: 'genius_wallet', height: 30.0, width: 30.0, fit: BoxFit.contain), title: Text(network.name), trailing: Icon(Icons.chevron_right), onTap: ... ImportWalletSelected(walletName:, coinType:)))` :43-68; the `SupportedWallet` class :78-88
    - lib/components/cards/gw_wallet_card.dart (`GWWalletCard({walletName = 'Ethereum', walletIcon, onTap, showArrow = true})`; it renders `Image.asset(walletIcon!, package: 'genius_wallet', semanticLabel: walletName, height: 30, width: 30, fit: BoxFit.contain)` — numerically identical to develop's leading image — an AutoSizeText name on bodyMd + gw.textPrimary, and its own trailing arrow when `showArrow`; it wraps `GWCard(onTap:, radius: radiusPill)`)
    - lib/onboarding/routes/wallet_routes.dart (131 lines): all seven GoRoutes :22-130; the canonical `import 'package:genius_wallet/components/loading.dart'` :7; the TWO identical fallback widgets `const Center(child: Text('Something went wrong! Please reload the app.'))` at :90-92 (/import_existing_wallet) and :122-124 (/create_wallet); note the file imports `package:flutter/widgets.dart`, NOT material
    - lib/theme/genius_wallet_typography.dart (headlineLg, bodyMd), lib/theme/genius_wallet_consts.dart (space10 = 20.0), lib/theme/gw_colors.dart
    - 06-UI-SPEC.md §2.1 (why this is a mechanical swap, not a design question), §2.2 (the substitution table), §2.3 (routing untouched; the explicit prohibition on swapping to GWErrorState), §6 (both strings verbatim)
    - .planning/phases/03-gw-component-library/03-SHADOW-NAMES.md (the canonical Loading path in wallet_routes.dart must not be repointed to the shadow)
  </read_first>
  <action>
    `select_wallet_type_screen.dart` — a mechanical component swap (§2.2). Add the fail-soft `gw` read
    to `build()`. Then:
    - "Import Wallet" heading: `textTheme.headlineLarge` -> `GeniusWalletTypography.headlineLg.copyWith(color: gw.textPrimary)`.
    - Subtitle: plain `Text` -> `GeniusWalletTypography.bodyMd.copyWith(color: gw.textSecondary)`.
    - Row: replace the entire `Card(clipBehavior: Clip.hardEdge, child: ListTile(...))` with
      `GWWalletCard(walletIcon: network.image, walletName: network.name, onTap: () { ...unchanged ImportWalletSelected dispatch... })`.
      `GWWalletCard`'s own trailing arrow replaces the explicit `Icon(Icons.chevron_right)`; its own
      `Image.asset` replaces the `leading:`; its own padding replaces the `contentPadding`. Keep the tap
      body EXACTLY as develop writes it, including its use of `supportedNetworks[index]` for both
      arguments.
    - Separator: `const SizedBox(height: 20)` -> `const SizedBox(height: GeniusWalletConsts.space10)`
      (20px — numerically identical, now token-backed).
    - Leave the `supportedNetworks` list, ALL THREE commented-out `SupportedWallet` entries, the
      `ListView.separated` shell with its `NeverScrollableScrollPhysics` and `shrinkWrap: true`, the
      `SupportedWallet` class, and the outer `Center`/`ConstrainedBox`/`Column` exactly as they are.
      Do NOT delete the commented-out networks — they are develop's recorded future intent, and the
      before/after comparison GAP-04 is judged by expects the same items in the same order.

    `wallet_routes.dart` — typography only (§2.3). At BOTH fallback sites (:90-92 and :122-124), give
    the `Text` a `style: GeniusWalletTypography.bodyMd.copyWith(color: gw.textSecondary)`, resolving
    `gw` from the enclosing `BlocBuilder`'s context via the same fail-soft read. Keep the string
    'Something went wrong! Please reload the app.' VERBATIM at both sites. The widgets are `const`
    today; adding a non-const style means dropping `const` at those two expressions — that is expected
    and is the only structural consequence permitted in this file.

    In `wallet_routes.dart`, change NOTHING else: no `GoRoute` path, no builder, no `BlocBuilder` gate,
    no `MultiBlocProvider` composition. Do NOT swap either fallback to `GWErrorState` — `loadUserStatus`
    is owned by `AppBloc` and this file has no retry to wire; a button with no action behind it is worse
    than the plain text (§2.3). Do NOT repoint `import 'package:genius_wallet/components/loading.dart'`
    to `lib/components/loading/loading.dart` — the compiler and analyze see NO collision between those
    two `Loading` classes, which is exactly why the additive-boundary guard exists. Do NOT touch the
    `/backup_phrase` route — its screen is scope-fenced dead code (§5.1) and 06-06 files the todo.

    Token discipline (§7): zero raw hex, zero raw px typography in either file.
    <!-- planner-discipline-allow: ListTile -->
    <!-- planner-discipline-allow: chevron_right -->
    <!-- planner-discipline-allow: headlineLarge -->
  </action>
  <verify>
    <automated>flutter analyze lib/onboarding/existing_wallet/view/select_wallet_type_screen.dart lib/onboarding/routes/wallet_routes.dart && bash tool/verify_additive_boundary.sh && grep -q 'GWWalletCard(' lib/onboarding/existing_wallet/view/select_wallet_type_screen.dart && grep -q 'ImportWalletSelected(' lib/onboarding/existing_wallet/view/select_wallet_type_screen.dart && grep -q "name: 'Ethereum'" lib/onboarding/existing_wallet/view/select_wallet_type_screen.dart && [ "$(grep -cE "SupportedWallet\(name: '(XRP|Stellar|Tron)'" lib/onboarding/existing_wallet/view/select_wallet_type_screen.dart)" -eq 3 ] && ! (grep -vE '^\s*//' lib/onboarding/existing_wallet/view/select_wallet_type_screen.dart | grep -qE 'ListTile|chevron_right|headlineLarge') && [ "$(grep -c 'Something went wrong! Please reload the app.' lib/onboarding/routes/wallet_routes.dart)" -eq 2 ] && ! grep -q "components/loading/loading.dart" lib/onboarding/routes/wallet_routes.dart && [ "$(grep -c 'GoRoute(' lib/onboarding/routes/wallet_routes.dart)" -eq 8 ]</automated>
  </verify>
  <done>
    The wallet-type row is a GWWalletCard carrying the same icon asset, name and ImportWalletSelected
    dispatch; the separator is token-backed space10; the live Ethereum entry AND all three commented-out
    future networks (XRP / Stellar / Tron) are still present — the gate counts the three commented forms
    explicitly, so it fails on a deletion rather than passing by coincidence; no ListTile, chevron_right
    or headlineLarge lookup survives on a non-comment line; wallet_routes.dart still has all 8 GoRoutes
    (including the scope-fenced /backup_phrase route, which stays), both fallback strings verbatim, and
    its canonical Loading import unrepointed; the additive-boundary guard passes; analyze reports no new
    issues.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Legal + GAP-04 walk — both flows' first step, the disabled-Continue gate, and the wallet-type route; both modes</name>
  <action>Human verification checkpoint — no code changes. Run the recipe in <how-to-verify> in BOTH appearance modes; record pass/fail per criterion in the SUMMARY.</action>
  <what-built>
    The shared Legal step re-skinned (token typography, two GWButton secondary link buttons, a GWCheckbox
    with its own label, a gradient Continue still gated on acceptance) and GAP-04 closed — the
    wallet-type chooser's row swapped to GWWalletCard with its data, event and list shell untouched, plus
    token typography on wallet_routes.dart's two preserved fallback strings.
  </what-built>
  <how-to-verify>
    Start from a genuinely wallet-less profile (see 06-01 Task 3 step 0 — clear the app's Hive /
    application-support directory; close every other instance first). Cold debug run with the pinned SDK
    (/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin):
    `CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release -DGENIUS_DEPENDENCY_BRANCH=develop -Dc-ares_DIR=..." flutter run -d windows --debug --dart-define=GW_DEV_TOOLS=true`

    1. Legal step, BOTH callers (criterion 1). From `/landing_screen` press "Create new wallet" and
       confirm the Legal step renders redesigned. Then back out and press "I already have a wallet" and
       confirm the SAME redesigned Legal step renders there too. One file, two flows — if only one looks
       right, something call-site-specific was changed that should not have been.
    2. DISABLED STATE (explicit, and a standing project rule): on arrival, with the checkbox UNCHECKED,
       confirm Continue is visibly disabled AND that "disabled" is visually distinguishable from both
       "enabled" and from the surrounding surface — in BOTH modes. Then check the box and confirm
       Continue becomes enabled and advances the flow. Uncheck it again and confirm it re-disables.
    3. CHECKBOX DISABLED/UNCHECKED LEGIBILITY: STATE carries an open todo that a disabled GWCheckbox is
       invisible in dark mode. Confirm the unchecked box is clearly visible against the Legal step's
       background in BOTH modes. If it is not, report it against `gw_checkbox.dart` and that existing
       todo — not as a new onboarding defect.
    4. Links: press "Privacy Policy" and "Terms of Service" and confirm each still opens the correct
       gnus.ai URL.
    5. GAP-04 / criterion 4: continue into the import flow and confirm the wallet-type step renders the
       Ethereum row as a GWWalletCard (rounded pill surface, 30px icon, name, trailing arrow) — and that
       tapping it STILL ROUTES to the import-security step. Routing is half of criterion 4; a
       good-looking card that does not navigate fails it.
    6. ZERO/SPARSE STATE: the list has exactly ONE entry today. Confirm a single-item list looks
       deliberate rather than broken — no stray separator above or below, no empty space suggesting a
       missing second row, no overflow.
    7. WINDOW SIZE: repeat items 1 and 5 with the window dragged narrow and short. The `ConstrainedBox`
       arithmetic is locked, so this is checking that the taller GWCheckbox label and the pill card do
       not overflow where the old ListTile did not.
    8. LIVE FLIP: with the Legal step on screen, toggle appearance IN PLACE via the dev bubble and
       confirm heading, body, checkbox and both buttons re-skin immediately. A subtree that stays in the
       old mode means a static getter is being read where the GWColors extension should be — BLOCKS
       close.
    9. Repeat items 1-7 in the other appearance mode. In LIGHT mode note the two secondary link buttons'
       label contrast — that is the INHERITED `gw_button.dart` defect 06-01 recorded (1.93:1). Report it
       against `gw_button.dart` again if visible; do not mark it as this plan's failure and do not mark
       Dimension 3 as passing in light mode for those two buttons.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per mode)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkbox acceptance -> Continue enablement | The `accepted ? onContinue : null` gate is the only thing stopping a user from skipping the legal agreement; a widget swap that drops it silently removes a consent gate |
| wallet-type tap -> ExistingWalletBloc | `ImportWalletSelected(walletName:, coinType:)` is the sole path into the import-security step; a row swap that loses `onTap` strands the import flow |
| AppBloc.loadUserStatus -> flow mount | wallet_routes.dart's gate decides between Loading, the flow, and the fallback text; a typography edit must not perturb the branch |
| canonical Loading import -> shadow Loading | A silently repointed import swaps one widget for another with zero compiler or analyzer diagnostic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Elevation of Privilege | Legal-agreement consent gate (`accepted ? onContinue : null`) | high | mitigate | The gate expression is preserved verbatim into `GWButton.onPressed` and asserted by a grep gate; Task 3 step 2 walks unchecked-disabled, checked-enabled, and re-unchecked-disabled in both modes |
| T-06-02-02 | Tampering | `ImportWalletSelected` dispatch during the Card+ListTile -> GWWalletCard swap (GAP-04, criterion 4) | high | mitigate | Tap body copied verbatim into `GWWalletCard.onTap`; grep gate asserts the event is still dispatched; Task 3 step 5 confirms the step actually navigates, not just renders |
| T-06-02-03 | Tampering | `wallet_routes.dart` GoRoute wiring during the two-string typography edit | high | mitigate | Only the two `Text` widgets are touched; grep gate asserts all 7 `GoRoute(` remain and both strings are byte-identical; no builder or BlocBuilder gate is edited |
| T-06-02-04 | Tampering | Canonical `Loading` import repointed to the `lib/components/loading/loading.dart` shadow | high | mitigate | `bash tool/verify_additive_boundary.sh` in the task verify, plus an explicit negative grep for the shadow path in `wallet_routes.dart` |
| T-06-02-05 | Information Disclosure | GWCheckbox unchecked/disabled state invisible in dark (open STATE todo) on a consent control | medium | mitigate | Task 3 step 3 makes it an explicit walk item in both modes and routes any failure to `gw_checkbox.dart` and the existing todo rather than mis-filing it as new |
| T-06-02-06 | Tampering | The three commented-out future networks (XRP/Stellar/Tron) treated as dead code and deleted | low | mitigate | Explicit instruction to preserve them plus a count-based grep gate; GAP-04 is judged by a before/after showing the same items in the same order |
| T-06-02-07 | Information Disclosure | Inherited `GWButtonVariant.secondary` light-mode text at 1.93:1 on both link buttons | medium | accept | Same cross-cutting `gw_button.dart` defect 06-01 recorded; deferred to the light pass by standing project decision, re-recorded at Task 3 step 9, filed as a todo in 06-06 |
</threat_model>

<verification>
- `flutter analyze` reports no new issues on all three files; `bash tool/verify_additive_boundary.sh` passes.
- `legal_screen.dart`: GWColors read present, `GWCheckbox(` present, at least three `GWButton(` sites, the `accepted ? onContinue : null` gate present, both gnus.ai URLs present, no legacy Material widget or `headlineLarge` lookup on a non-comment line.
- `select_wallet_type_screen.dart`: `GWWalletCard(` present, `ImportWalletSelected(` present, the three `SupportedWallet(name:` entries still counted, no `ListTile` / `chevron_right` / `headlineLarge` on a non-comment line.
- `wallet_routes.dart`: both fallback strings present verbatim (count 2), all 7 `GoRoute(` present, no shadow Loading path.
- Task 3 walk (both modes): the Legal step rendering identically from BOTH flows, the disabled/enabled Continue gate, checkbox visibility, both URLs, the GWWalletCard row NAVIGATING (criterion 4's routing half), the single-item list reading as deliberate, narrow/short window safety, and the live flip.
</verification>

<success_criteria>
GAP-04 is closed: the wallet-type step wears the extended design language via GWWalletCard and still
routes correctly through `wallet_routes.dart`, with its data, event, list shell and commented-out
networks untouched and both fallback strings preserved verbatim. The shared Legal step renders
identically from both flows in the redesign skin, its consent gate still disables Continue until the
checkbox is accepted, both legal URLs still open, and every surface flips live and holds AA in both
modes — with the inherited `gw_button.dart` secondary-variant light-mode defect recorded, not absorbed.
</success_criteria>

<output>
Create `.planning/phases/06-onboarding/06-02-SUMMARY.md` when done. Record: confirmation that GAP-04 was
closed as a mechanical 1:1 swap with zero IA change (so no product decision is owed under the GAP
treatment test); confirmation that the three commented-out networks survive; confirmation that neither
fallback string was reconciled to `GWErrorState` and why; the checkbox visibility observation from Task
3 step 3; and the re-recorded inherited secondary-button light-mode gap.
</output>
