---
phase: 26-swap-that-actually-swaps
plan: 07
type: execute
wave: 6
depends_on: ["26-06"]
files_modified: [lib/squid_router/swap_messages.dart, lib/squid_router/swap_screen.dart, test/squid_router/swap_messages_test.dart, test/squid_router/swap_execution_test.dart]
autonomous: true
requirements: [SWAP-01]
estimate: {tokens: 45000, raw_tokens: 45000, tasks: 3, confidence: low}

must_haves:
  truths:
    - "Route failure, missing transactionRequest, approval failure, send failure and a non-success terminal status each produce a different user-visible message."
    - "No failure branch produces a generic fallback string, and no two branches produce the same string."
    - "No failure branch leaves a row in Hive."
    - "A build with no integrator ID says the swap is unavailable rather than reporting a network error."
    - "lib/squid_router/ contains no fabricated data and no commented-out HTTP."
  artifacts: ["lib/squid_router/swap_messages.dart", "test/squid_router/swap_messages_test.dart"]
  key_links:
    - "Messages map from SwapOutcome, not from a caught exception's toString(). An RPC error string is not a sentence a user can act on, and it can carry an address."
    - "The route-error inline notice and the error toast already exist on this screen (D-09). This plan reuses both — Phase 8 owns the swap skin, and a state needing a screen that does not exist is a Phase 8 finding, not a redesign here."
---

<objective>
Every way a swap can fail says which way it failed.

Purpose: the phase goal is that a swap either moves funds or says why it could not. 26-06 delivered
the first half and left every failure sharing the shipped route-error copy. This closes the second.
Output: one pure outcome-to-message mapping, surfaced through the error affordances the screen
already has, with no stored row on any failure path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/26-swap-that-actually-swaps/26-ENVIRONMENT.md
@.planning/phases/26-swap-that-actually-swaps/26-CONTEXT.md
@lib/squid_router/swap_execution.dart
@lib/squid_router/swap_cta_state.dart
@lib/squid_router/swap_screen.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One message per way it can fail</name>
  <files>lib/squid_router/swap_messages.dart, test/squid_router/swap_messages_test.dart</files>
  <behavior>
    - Every SwapOutcome shape without a hash maps to a non-empty message.
    - Every hash-bearing outcome whose resolved status is not success also maps to a message.
    - No two shapes map to the same string, asserted by collecting all of them into a set.
    - No message is produced by interpolating a caught error's toString().
    - Adding a new outcome shape without a message fails the switch at compile time.
  </behavior>
  <action>
  Create `lib/squid_router/swap_messages.dart`: a pure top-level `swapFailureMessage(SwapOutcome)`,
  the shape of `swapCtaLabel` in `swap_cta_state.dart`. Exhaustive switch over the outcome, no
  default arm — an unhandled shape must be a compile error, not a generic string at runtime.
  Distinct copy per branch, naming what the user can do about it. The branches are: no route
  available, the route came back unsignable, the allowance could not be read, the approval failed or
  was rejected, the transaction could not be sent, and the swap settled as something other than a
  success. Keep the register of the shipped D-09 copy and do not upgrade any claim.
  A message must never contain an RPC error string, an address, a hash or a raw amount.
  </action>
  <verify>
    <automated>flutter test test/squid_router/swap_messages_test.dart --no-pub</automated>
  </verify>
  <done>Every shape has its own message; the set of messages is the same size as the set of shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface them, and store nothing</name>
  <files>lib/squid_router/swap_screen.dart, test/squid_router/swap_execution_test.dart</files>
  <behavior>
    - A failure clears any stale quote, leaving no figure on screen from the attempt that failed.
    - No failure branch reports a stored row via the side-effect triple.
    - With the integrator ID absent, the unavailable copy shows rather than a network-error message.
  </behavior>
  <action>
  In `_submitSwap`'s failure path, show `swapFailureMessage` through the existing error toast and the
  existing inline notice — the two affordances D-09 already put on this screen. Add no new widget, no
  new colour and no new drawer.
  A failed submit must leave the form usable and the quote cleared, the same contract the route-error
  path already keeps: a stale quote beside a failure message is a figure the user might still act on.
  Extend the execution tests to assert the side-effect triple stays all-false across every failure
  branch. That assertion, with 26-06's, is the standing regression guard for the original bug.
  </action>
  <verify>
    <automated>flutter test test/squid_router/ --no-pub</automated>
    <human-check>Underfund gas on a throwaway **Base mainnet (8453)** wallet and submit: the message names the send failure, the transactions list gains no row, and the form is still usable.</human-check>
  </verify>
  <done>Each failure is distinguishable on screen and leaves no trace in storage.</done>
</task>

<task type="auto">
  <name>Task 3: Phase closeout gates</name>
  <files>test/squid_router/swap_messages_test.dart</files>
  <action>
  Add the greps below to the standing verification run and fix anything they catch. They are the
  phase's success criteria expressed as commands rather than as a review note.
  Confirm by inspection that `lib/squid_router/` holds no fabricated token, balance or route data and
  no commented-out HTTP block, that nothing under `squidrouter/` was modified, and that no source
  file added in this phase cites a plan, phase, sketch, spec or UAT number or names a test file.
  </action>
  <verify>
    <automated>grep -rn "993.72" lib/ ; grep -rniE "^\s*return mock" lib/squid_router/ ; git diff --name-only origin/develop -- squidrouter/ | wc -l</automated>
  </verify>
  <done>The first two greps return nothing and the third is 0.</done>
</task>

</tasks>

<!-- planner-discipline-allow: 993.72, return mock -->

<threat_model>
Trust boundaries: caught exception → user-visible string (an RPC or Dio error can carry addresses,
URLs and node internals); failure path → storage (a failure that writes a row is the original bug in
a new costume).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-20 | Information Disclosure | error copy | medium | mitigate | Messages are written constants selected by outcome shape; no branch interpolates a caught error, asserted in Task 1. |
| T-26-21 | Spoofing | stale quote beside a failure | medium | mitigate | The failure path clears the quote, the same contract the shipped route-error path keeps. |
| T-26-22 | Repudiation | silent failure | high | mitigate | The switch has no default arm, so a shape with no message cannot compile. A silent failure is the state this phase exists to remove. |
</threat_model>

<verification>
1. `flutter test --no-pub` — at or above the measured baseline, zero failures.
2. `flutter analyze --no-pub` in root and in `packages/genius_api` — neither count rose.
3. `bash tool/check_brace_style.sh --count` → 0; `dart format --set-exit-if-changed lib test packages/genius_api/lib` exit 0.
4. Task 3's three gates all pass.
</verification>

<success_criteria>
- A failed or rejected swap leaves no row in Hive and tells the user what happened.
- No two failures look the same to the user.
- The service contains no fabricated return and no commented-out HTTP.
</success_criteria>

<output>
Create `.planning/phases/26-swap-that-actually-swaps/26-07-SUMMARY.md` when done. 40 lines maximum.
</output>
