---
phase: 26-swap-that-actually-swaps
plan: 06
type: execute
wave: 5
depends_on: ["26-05"]
files_modified: [lib/squid_router/swap_screen.dart, test/squid_router/swap_submit_test.dart]
autonomous: true
requirements: [SWAP-01]
estimate: {tokens: 45000, raw_tokens: 45000, tasks: 2, confidence: low}

must_haves:
  truths:
    - "No toast, receipt, or Hive write occurs on any path that did not produce a transaction hash. Asserted at the screen, not only at the pure layer."
    - "A swap that reaches the chain stores a row keyed by its real hash, written as pending before polling resolves, then updated to the status that came back."
    - "The fabricated Transaction, the unconditional success toast and their TODO markers no longer exist."
    - "A real swap on mainnet moved real funds and the receipt's hash resolves on the explorer."
  artifacts: ["test/squid_router/swap_submit_test.dart"]
  key_links:
    - "addTransaction does box.put(tx.hash, tx). Today's fabricated row has hash: '', so every fake swap has overwritten one empty-string key — re-putting with the real hash is how the pending row is later resolved."
    - "The screen must read sideEffectsFor and act on it. A screen that re-derives the decision from the outcome is the original bug with extra steps, and Task 2 is what catches that."
    - "`fees` was deliberately left blank because nothing executed and the receipt's blank-fee guard omits the row. Now something executes, so the figure exists and belongs there."
---

<objective>
Delete the lie and wire the screen to the outcome.

Purpose: `swap_screen.dart:301` fabricates a `completed` Transaction, shows a success toast, opens
the receipt and writes to Hive — in release builds, without calling Squid. This is the change that
removes it. The orchestrator it calls already resolves status, so execution cannot ship without it.
Output: a `_submitSwap` that is a thin adapter, and the first real swap this app has ever made.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/26-swap-that-actually-swaps/26-ENVIRONMENT.md
@.planning/phases/26-swap-that-actually-swaps/26-CONTEXT.md
@lib/squid_router/swap_execution.dart
@lib/squid_router/swap_screen.dart
@lib/hive/services/transaction_storage_service.dart
</context>

<tasks>
<task type="auto">
  <name>Task 1: _submitSwap becomes an adapter</name>
  <files>lib/squid_router/swap_screen.dart</files>
  <action>
  Rewrite `_submitSwap`. Delete the fabricated `Transaction`, the unconditional success toast, the
  receipt call and the Hive write that currently run whether or not anything happened, along with the
  two TODO markers above them.
  New body: call the orchestrator, read `sideEffectsFor`, act on it and nothing else. On the
  hash-bearing outcome store the row keyed by the real hash as pending BEFORE polling resolves — a
  crash mid-poll must not lose a broadcast transaction — then re-store the same key with the resolved
  status. Populate `fees` from the route's gas cost. On every other outcome: no toast, no receipt, no
  row; 26-07 owns what the user is told instead, and until then the shipped route-error toast stands
  in. `isSubmitting` still wraps the whole await and the CTA ladder is untouched.
  Inject the orchestrator through a `SwapScreen` parameter defaulting to the real one, so Task 2 can
  drive every outcome without a network, a key or a wallet.
  </action>
  <verify>
    <automated>flutter test test/squid_router/ --no-pub</automated>
    <human-check>On **Base mainnet (chainId 8453 — not the dead 84531 "Base - Sepolia" entry)** with a throwaway wallet, run one real swap: the receipt's hash resolves on the explorer, the stored row matches chain truth, and the amount received matches the quote within slippage. Record the hash in the SUMMARY.</human-check>
  </verify>
  <done>A real swap moved real funds, and `_submitSwap` contains no TODO marker.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: The screen honours the invariant, not just the module</name>
  <files>test/squid_router/swap_submit_test.dart</files>
  <behavior>
    - Fed an outcome with no hash, submitting shows no success toast and opens no receipt.
    - Fed an outcome with no hash, submitting performs no storage write.
    - Fed a hash-bearing outcome, submitting writes once as pending and once with the resolved status, under the same key.
    - The CTA leaves its submitting rung in both cases.
  </behavior>
  <action>
  Add `test/squid_router/swap_submit_test.dart`, driving `SwapScreen` with a stubbed orchestrator and
  a stubbed storage collaborator. Assert against what the screen DID, not against what the pure
  function returned — 26-05 already covers that, and the gap between the two is where this bug lived.
  Use no real Hive box: real Hive I/O inside `testWidgets` hangs indefinitely in this repo.
  </action>
  <verify>
    <automated>flutter test test/squid_router/swap_submit_test.dart --no-pub</automated>
  </verify>
  <done>All four behaviours asserted, and the suite does not hang.</done>
</task>
</tasks>

<threat_model>
Trust boundary: orchestrator outcome → storage and screen — what the user is told happened to their
money, and what the transaction list will claim tomorrow.
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-15b | Spoofing | screen side effects | critical | mitigate | Task 2 asserts the screen's behaviour, not only the pure function's return. A correct module behind an incorrect screen is the original bug. |
| T-26-17 | Repudiation | lost hash | high | mitigate | The row is written pending, keyed by the real hash, before polling begins; a crash mid-poll leaves an accurate pending row rather than nothing. |
| T-26-23 | Information Disclosure | receipt contents | low | accept | The receipt shows the user their own hash, addresses and amounts — the function of a receipt. |
</threat_model>

<verification>
1. `flutter test --no-pub` at or above baseline, zero failures; `flutter analyze --no-pub` count did not rise.
2. In `swap_screen.dart`, every `showToast` / `showTransactionDetails` / `addTransaction` call inside `_submitSwap` is guarded by the side-effect triple.
3. Brace style 0; `dart format --set-exit-if-changed lib test` exit 0.
4. One mainnet hash recorded in the SUMMARY and resolving on the explorer.
</verification>

<success_criteria>
- A swap on a real network moved real funds and the receipt's hash is on the explorer.
- No success is shown and no row stored for a swap that produced no hash.
- The stored status is the one Squid reported.
</success_criteria>

<output>
Create `.planning/phases/26-swap-that-actually-swaps/26-06-SUMMARY.md` when done. 40 lines maximum.
</output>
