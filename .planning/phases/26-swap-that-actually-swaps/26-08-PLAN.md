---
phase: 26-swap-that-actually-swaps
plan: 08
type: execute
wave: 7
depends_on: ["26-06", "26-07"]
files_modified: [packages/genius_api/lib/models/transaction.dart, lib/dashboard/home/widgets/transaction_utils.dart, lib/dashboard/home/widgets/transaction_displays.dart, lib/dashboard/home/widgets/transactions_slim_view.dart, lib/squid_router/swap_screen.dart, lib/dev/dev_mock_transactions.dart, test/dashboard/transaction_recovery_states_test.dart, test/dashboard/transaction_row_subtitle_test.dart, test/dashboard/transaction_filters_test.dart]
autonomous: true
requirements: [SWAP-01]
estimate: {tokens: 60000, raw_tokens: 60000, tasks: 2, confidence: low}

must_haves:
  truths:
    - "A paused swap offers the link the API returned. A row carrying no link offers no button at all, rather than a composed guess at one."
    - "None of the three states prints the enum's own name, and each says in plain words where the money is and whether anything is asked of the user."
    - "No new colour token. Every new status tone is a pair the theme already proves in both appearance modes."
    - "Each new state is reachable from a filter that already ships; no chip was added to a bar another phase owns."
  artifacts: ["test/dashboard/transaction_recovery_states_test.dart"]
  key_links:
    - "The link must survive a restart, so it is persisted on the row as a new appended field. Existing indices are never renumbered — the same append rule the enum follows. Without the field the action exists only during the session that made the swap, which is the one session the user is not in when they come back to it."
    - "26-07 owns failures with NO stored row and its table lives in `swap_messages.dart`. This owns states WITH a stored row and its table lives in `transaction_utils.dart`, keyed by `TransactionStatus`. Merging them would put swap-attempt copy on a bridge or Banxa row."
    - "The narrow row pins the status word inside a 76px box (`_narrowStatusMaxWidth`); `Cancelled` measures 66.0 there. A longer word ellipsises silently, so these labels are length-constrained and `transaction_row_subtitle_test.dart` is what reddens."
---

<objective>
Three states where the money moved but not where it was asked to go, made legible and — where a fix
exists — actionable.

Purpose: 26-05 extended the enum and carried the recovery data, 26-06 stores the row. A user meeting
one of these states today gets a row labelled with a camel-case enum name and no way to act on it.
Output: plain copy per state, the API's own recovery link in the receipt's existing footer slot, and
all three findable through the filter bar that already ships.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/26-swap-that-actually-swaps/26-ENVIRONMENT.md
@.planning/phases/26-swap-that-actually-swaps/26-CONTEXT.md
@lib/dashboard/home/widgets/transaction_utils.dart
@lib/dashboard/home/widgets/transaction_displays.dart
@lib/components/feedback/gw_warning_note.dart
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: A paused swap, end to end — stored, labelled, and recoverable</name>
  <precondition>26-05's resolved outcome exposes the status response's recovery URL and 26-06 writes that row. If the outcome does not carry it, capture it in the adapter's status mapping before starting — do not proceed by composing a link.</precondition>
  <files>packages/genius_api/lib/models/transaction.dart, lib/squid_router/swap_screen.dart, lib/dashboard/home/widgets/transaction_utils.dart, lib/dashboard/home/widgets/transaction_displays.dart, test/dashboard/transaction_recovery_states_test.dart</files>
  <behavior>
    - A row written with a recovery link and read back through its adapter still carries it, and a row written by an earlier build still deserializes.
    - A paused row renders a real word, never the enum's own name, in the pill, the Status row and the narrow tail.
    - Its receipt carries one sentence saying the transfer is paused, that the funds are held and recoverable, and what the button will do.
    - The button opens exactly the string the row carries — an altered stored link opens the altered one, proving nothing is composed — and a row whose link is absent, unparseable or not `https` shows the sentence and NO button.
  </behavior>
  <action>
  Append one nullable recovery-link field to `Transaction` at the next free `@HiveField` index, never
  renumbering an existing one. Regenerate the adapter with `build_runner` inside `packages/genius_api`
  and populate the field at the swap's store-and-resolve site from what the resolved outcome carries.
  In `transaction_utils.dart` add a public status-word function and a public status-note function.
  The word function replaces BOTH `_statusLabel` here and `_capitalizeStatus` in
  `transaction_displays.dart`: those capitalise `status.name`, which yields a camel-case non-word for
  the new values, and they are already two rules for one fact — the drift `txStatusColors` exists to
  have stopped. Delete both; route the pill, the Status row and the tail through the one function.
  The caller-supplied `statusLabel` override keeps precedence.
  Give the paused state the pending pair in `txStatusColors` and the pending badge in `txRowContent`
  — it is in flight and waiting on an action, which is what that pair and that glyph already mean.
  Add no colour and no badge kind.
  In `showTransactionDetails`, render the note between the status pill and the first kicker through
  the shipped `GWWarningNote`, and give the footer a recovery button ahead of the explorer default
  when the row's link parses and its scheme is `https`. An explicit `footer:` argument still wins and
  every other call site stays untouched; the hash stays copyable in the Network group, which is what
  replacing the explorer button here costs. No host name is assembled anywhere in `lib/`.
  Write for someone who does not know what a bridge is: name what happened, then what the tap does.
  </action>
  <verify>
    <automated>flutter test test/dashboard/transaction_recovery_states_test.dart --no-pub</automated>
  </verify>
  <done>Every behaviour asserted with a pumped row and receipt; no stored fixture loses a field.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: The other two states, and somewhere to find all three</name>
  <files>lib/dashboard/home/widgets/transaction_utils.dart, lib/dashboard/home/widgets/transactions_slim_view.dart, lib/dev/dev_mock_transactions.dart, test/dashboard/transaction_recovery_states_test.dart, test/dashboard/transaction_row_subtitle_test.dart, test/dashboard/transaction_filters_test.dart</files>
  <behavior>
    - The partial state's sentence names the token the user ASKED for and names no other ticker, because the status response carries no symbol for what actually arrived.
    - The refunded state's sentence says the funds return on their own and roughly when, and offers no button.
    - Each of the three returns a tone pair that some already-shipping status also returns — no unproven pair enters the palette.
    - Every status word fits the narrow tail's box with the same margin the existing words keep.
    - Each of the three matches at least one status filter, and the swap type filter still lists them.
  </behavior>
  <action>
  Extend Task 1's two functions with the partial and refunded states.
  Partial: the swap stopped at the destination step, the funds are safe, and they are not the token
  asked for. State the requested symbol, and say the balances or the linked transfer are where to see
  what is actually held. Do NOT name the token that arrived — it differs by route family and the
  status response returns no symbol for it, so naming one would be a fabrication of exactly the kind
  this phase exists to delete. It keeps the type badge: the pill carries the state, and the failed
  glyph would call a partial swap a failure.
  Refunded: it did not go through, the funds return to the sending address by themselves, usually
  within about ten minutes, and nothing is required. Render this one as secondary text rather than
  through the warning note — an amber box saying "nothing to do" contradicts itself. Give it the
  cancelled tone and the failed badge, and add it to `isDead` so it takes the `Not charged` value line
  for the same reason a cancelled row does.
  In `transactions_slim_view.dart` fold all three into the existing failed filter's set: that chip is
  the app's "something went wrong, find it" affordance and already folds cancelled in on the stated
  grounds that a person hunting a transaction does not sort its kinds. Membership of that set is a
  finding aid, not a claim about the money, and each row still says what happened. Add NO chip — that
  bar belongs to another phase. Add one dev mock row per state so all three can be seen without
  producing a real one, and make the status-word and filter tests iterate every enum value rather
  than a written-out list, so the next value added cannot skip them.
  </action>
  <verify>
    <automated>flutter test test/dashboard/ --no-pub</automated>
    <human-check>With the dev mocks on, open each of the three rows in BOTH appearance modes: the sentence reads as English, the word in the pill is not clipped, the paused row's button opens the stored link, and the other two offer none.</human-check>
  </verify>
  <done>All five behaviours asserted, and the status-word and filter tests iterate the enum.</done>
</task>
</tasks>

<threat_model>
Trust boundary: a third-party API's URL string → a browser the wallet opens. The recovery link is attacker-influenceable in exactly the way the tx hash is not.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-24 | Tampering | recovery link | high | mitigate | The button renders only when the stored string parses and its scheme is `https`; anything else shows the sentence with no button, asserted in Task 1. |
| T-26-25 | Spoofing | recovery link host | medium | accept | The host is whatever the route provider returns and is not pinned — pinning would break the moment a non-Axelar route family reports one. Revisit if the provider is replaced. |
| T-26-26 | Repudiation | a state that says nothing | high | mitigate | Task 2's tests iterate every enum value, so a status with no word and no sentence fails rather than printing its own name. |
</threat_model>

<verification>
1. `flutter test --no-pub` at or above the measured baseline, zero failures.
2. `flutter analyze --no-pub` in the root AND in `packages/genius_api` — check `$?`, neither count rose; `bash tool/check_brace_style.sh --count` → 0; `dart format --set-exit-if-changed lib test packages/genius_api/lib` exit 0.
3. Nothing assembled that should have been given: `grep -rniE "axelarscan|/gmp/" lib/` and `grep -rnE "Colors\.|Color\(0x" lib/dashboard/home/widgets/` both return nothing.
</verification>

<!-- planner-discipline-allow: axelarscan, /gmp/, Colors., Color(0x -->

<success_criteria>
- A user meeting any of the three states is told where their money is, in words they can act on.
- The paused state's action opens the link the API returned, or shows no action at all.
- No new colour, no new badge, no new chip, and no new component.
</success_criteria>

<output>
Create `.planning/phases/26-swap-that-actually-swaps/26-08-SUMMARY.md` when done. 40 lines maximum.
</output>
