---
phase: 26-swap-that-actually-swaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pubspec.yaml, pubspec.lock, lib/squid_router/squid_client.dart, lib/squid_router/swap_screen.dart, test/squid_router/squid_client_test.dart]
autonomous: false
requirements: [SWAP-01]
estimate: {tokens: 45000, raw_tokens: 45000, tasks: 3, confidence: low}

must_haves:
  truths:
    - "App code constructs a Squid v2 client and resolves getRoute / getStatus / getSDKInfo, proven without a network call."
    - "The integrator ID comes from the build environment. No literal is checked in."
    - "With no integrator ID the swap screen says it is unavailable, its CTA cannot be tapped, and it attempts no call."
  artifacts: ["lib/squid_router/squid_client.dart", "test/squid_router/squid_client_test.dart"]
  key_links:
    - "setApiKey('IntegratorId', …) is the only injection point — ApiKeyAuthInterceptor reads apiKeys['IntegratorId'] and writes x-integrator-id. Without it every call 401s."
    - "Root analysis_options.yaml already excludes squidrouter/, keeping its ~60 generated-code warnings out of the app gate. Removing that exclusion would raise the baseline by ~60 with no app change."
---

<objective>
Make the vendored Squid v2 client reachable from app code, and make an unconfigured build say so
instead of pretending.

Purpose: every later plan calls through this one file, and nothing here needs a credential — so this
plan is not blocked on the integrator ID application.
Output: a path dependency, a client factory keyed on a build-time ID, and honest degradation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/26-swap-that-actually-swaps/26-ENVIRONMENT.md
@.planning/phases/26-swap-that-actually-swaps/26-CONTEXT.md
@.planning/phases/26-swap-that-actually-swaps/26-RESEARCH.md
@lib/dev/dev_flags.dart
@squidrouter/lib/src/api.dart
@lib/squid_router/swap_screen.dart
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm the transitive packages are real</name>
  <what-built>Nothing yet. 26-RESEARCH.md has no Package Legitimacy Audit, so every package the new
  path dependency pulls in is unaudited. Research resolved 6 new packages on 2026-09-16.</what-built>
  <how-to-verify>
  From the pubspec.lock diff, list every newly resolved package. Open pub.dev for each and confirm
  publisher, a download count consistent with a real package, and a version matching the lock. The
  submodule's own pubspec expects dio, one_of, one_of_serializer, built_value, built_collection and
  their transitives. Anything outside that set is the finding.
  </how-to-verify>
  <resume-signal>Type "approved" with the verified list, or name the package that failed</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Path dependency and the client factory</name>
  <files>pubspec.yaml, pubspec.lock, lib/squid_router/squid_client.dart, test/squid_router/squid_client_test.dart</files>
  <action>
  Add `squidrouter: {path: squidrouter}` to pubspec.yaml dependencies and resolve.

  Create `lib/squid_router/squid_client.dart` holding three things and nothing else: a top-level
  `kSquidIntegratorId` from `String.fromEnvironment('GW_SQUID_INTEGRATOR_ID')` with an empty default,
  following `dev_flags.dart`; a `squidConfigured` predicate true only when it is non-empty; and a
  memoised `squidApi()` returning `DefaultApi`, built from `Squidrouter()` with
  `setApiKey('IntegratorId', kSquidIntegratorId)` applied before `getDefaultApi()`. Do not override
  the base path — the submodule pins the right one. Do not write a comment claiming the ID is hidden;
  it is compiled into the binary in plain text.

  Test: round-trip a `quoteOnly` `RouteRequest` through `standardSerializers` and assert the map
  carries fromChain, fromToken, fromAmount, toChain, toToken and quoteOnly; assert
  `Squidrouter.basePath` is the v2 host and that getRoute and getStatus resolve. No network call.
  </action>
  <verify>
    <automated>flutter test test/squid_router/squid_client_test.dart --no-pub</automated>
  </verify>
  <done>Test passes, analyzer count did not rise, the new deps appear in pubspec.lock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: An unconfigured build reports itself unavailable</name>
  <files>lib/squid_router/swap_screen.dart, test/squid_router/squid_client_test.dart</files>
  <behavior>
    - `squidConfigured` is false for an empty ID, true for a non-empty one.
    - With availability false the screen renders a disabled CTA reading "Swap unavailable".
    - With availability false `_loadTokens` is not called and no quote is shown.
  </behavior>
  <action>
  Give `SwapScreen` a `swapAvailable` constructor parameter defaulting to `squidConfigured`, so a
  widget test can drive both states without a build define. When false: skip `_loadTokens` entirely
  and render the CTA through the already-shipped disabled treatment (`surfaceMenu` background,
  `textPrimary38` foreground) with the label "Swap unavailable".

  Leave `swap_cta_state.dart` untouched — the gate sits above the ladder, not inside it. Add no new
  screen, no new drawer and no new colour: this phase changes no visual design.
  </action>
  <verify>
    <automated>flutter test test/squid_router/ --no-pub</automated>
  </verify>
  <done>Both availability states are asserted. The unavailable state performs no fetch.</done>
</task>

</tasks>

<threat_model>
Trust boundaries: build config → shipped binary (dart-defines are plain text in the artifact);
pub.dev → dependency tree (a path dep drags transitives into a wallet binary).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-01 | Information Disclosure | `kSquidIntegratorId` | medium | accept | A public client identifier; every client app ships one (26-CONTEXT). Void if Squid answers that it must be secret — then the phase needs a proxy. Record their answer before 26-06 ships. |
| T-26-02 | Spoofing | missing/blank ID | low | mitigate | Task 3: an unconfigured build refuses to quote rather than calling unauthenticated and rendering the 401 as a route error. |
| T-26-SC | Tampering | pub package installs | high | mitigate | Task 1 blocking-human legitimacy check on every newly resolved package. Not auto-approvable. |
</threat_model>

<verification>
1. `flutter test --no-pub` — at or above the measured baseline, zero failures.
2. `flutter analyze --no-pub` — count did not rise, zero error-severity items.
3. `bash tool/check_brace_style.sh --count` → 0; `dart format --set-exit-if-changed lib test` exit 0.
4. `grep -rn "GW_SQUID_INTEGRATOR_ID" lib/` returns exactly one hit, in `squid_client.dart`.
</verification>

<success_criteria>
- App code reaches Squid v2 through one file.
- A build with no integrator ID is honest on screen and makes no call.
- Nothing under `squidrouter/` was edited.
</success_criteria>

<output>
Create `.planning/phases/26-swap-that-actually-swaps/26-01-SUMMARY.md` when done. 40 lines maximum.
</output>
