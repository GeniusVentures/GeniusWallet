---
phase: 26-swap-that-actually-swaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [packages/genius_api/lib/web3/web3.dart, packages/genius_api/lib/src/genius_api.dart, lib/squid_router/swap_allowance.dart, test/squid_router/swap_allowance_test.dart, test/squid_router/erc20_abi_test.dart]
autonomous: true
requirements: [SWAP-01]
estimate: {tokens: 50000, raw_tokens: 50000, tasks: 3, confidence: low}

must_haves:
  truths:
    - "An ERC-20 allowance can be read for an owner/spender pair, returned in raw base units."
    - "An approval is requested only when allowance is short, and for exactly the swap amount."
    - "A native token needs no approval and no call is made for one."
    - "A failed approve returns its own error. It does not fall through to a generic string."
    - "A raw token balance can be read as an exact integer, with no floating-point division."
  artifacts: ["lib/squid_router/swap_allowance.dart", "test/squid_router/swap_allowance_test.dart", "test/squid_router/erc20_abi_test.dart"]
  key_links:
    - "executeBridgeOutTransaction (web3.dart:424) is the write pattern to copy — except its catch builds an ApiResponse.error and never returns it, so every failure reads 'Failed to bridge: unknown' (11-FINDINGS F-03). Copy the shape, not the bug."
    - "Allowance and amount must be compared in the SAME units. Squid's fromAmount is raw base units; balanceOf (web3.dart:208) divides by 10^decimals and returns a double. Comparing those is a silent off-by-10^18."
---

<objective>
Give the wallet the two ERC-20 calls it has never had, plus one exact-integer balance read.

Purpose: a Squid route spends the user's token through a router contract, which cannot move anything
until it holds an allowance. Grepping `packages/genius_api/lib/` for approve / allowance / erc20
returns zero hits today.
Output: `allowance`, `approve` and `rawBalanceOf` on Web3, a GeniusApi wrapper resolving the key the
way the dApp path already does, and the pure rule deciding when an approval is needed at all.
Not blocked on the integrator ID; its automated proof costs nothing, and the one live `approve` that
costs gas is folded into 26-06's walk.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/26-swap-that-actually-swaps/26-ENVIRONMENT.md
@.planning/phases/26-swap-that-actually-swaps/26-CONTEXT.md
@.planning/phases/26-swap-that-actually-swaps/26-RESEARCH.md
@packages/genius_api/lib/web3/web3.dart
@packages/genius_api/lib/src/genius_api.dart
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: The rule that decides whether to approve</name>
  <files>lib/squid_router/swap_allowance.dart, test/squid_router/swap_allowance_test.dart</files>
  <behavior>
    - Native address (all-zero, or Squid's 0xEeee… sentinel, case-insensitive) → not required.
    - allowance >= amount → sufficient, no call. allowance < amount → approve.
    - The approve outcome's amount equals the swap amount exactly.
    - Zero allowance with a non-zero amount → approve. Zero amount → sufficient.
  </behavior>
  <action>
  Create `lib/squid_router/swap_allowance.dart` as a pure module in the shape of `swap_cta_state.dart`
  and `held_tokens.dart`: top-level function, no Flutter import, no client.
  `decideApproval({required String tokenAddress, required BigInt allowance, required BigInt amount})`
  returns one of three outcomes — not required, sufficient, or approve-this-exact-amount. The approve
  outcome carries the amount and has no path that can produce any other value, which makes "never
  uint256 max" structural rather than a review note (26-CONTEXT: a standing unlimited allowance to a
  router is how drained-wallet incidents happen).
  Both operands are `BigInt` raw base units. Do not accept a double on either side.
  </action>
  <verify>
    <automated>flutter test test/squid_router/swap_allowance_test.dart --no-pub</automated>
  </verify>
  <done>All four behaviours asserted. The module imports nothing from flutter/ or squidrouter/.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: allowance and rawBalanceOf — the two reads</name>
  <files>packages/genius_api/lib/web3/web3.dart, test/squid_router/erc20_abi_test.dart</files>
  <behavior>
    - The shared ABI resolves `allowance` taking two addresses and returning uint256.
    - The shared ABI resolves `approve` taking an address and a uint256.
    - The four pre-existing ABI entries still resolve.
  </behavior>
  <action>
  Extend the shared ERC-20 ABI at `web3.dart:42` with standard `allowance(address,address)` and
  `approve(address,uint256)` entries, changing nothing about the four already there.
  Add `allowance({required String owner, required String spender, required String contractAddress,
  required String rpcUrl})` returning `BigInt` — the read pattern of `balanceOf` at `:208` minus the
  decimals division. Raw units, because that is the unit Squid's fromAmount arrives in.
  Add `rawBalanceOf({required String address, required String contractAddress, required String
  rpcUrl})` returning `BigInt`: the same call `balanceOf` already makes before it divides. 26-04 needs
  the exact integer — the picker's dust, floor and long-fraction cases were found at a walk and a
  double cannot carry them.
  Both dispose the client in a `finally`.
  </action>
  <verify>
    <automated>flutter test test/squid_router/erc20_abi_test.dart --no-pub</automated>
  </verify>
  <done>ABI test passes; both reads compile and analyze clean in packages/genius_api.</done>
</task>
<task type="auto">
  <name>Task 3: approve — the write, with its error intact</name>
  <files>packages/genius_api/lib/web3/web3.dart, packages/genius_api/lib/src/genius_api.dart</files>
  <action>
  Add `Web3.approve({required String contractAddress, required String rpcUrl, required
  StoredKeyWallet wallet, required String spender, required BigInt amount, required int chainId})`
  returning `ApiResponse&lt;String&gt;` with the tx hash. Build the call from the shared ABI, sign with
  `EthPrivateKey.fromHex(getPrivateKeyStr(wallet))`, send via `client.sendTransaction(..., chainId:)`
  — the shape of `executeBridgeOutTransaction` at `:424`.
  Its `catch (e)` MUST `return ApiResponse.error(...)`. The function it is modelled on omits that
  return and loses every real failure reason; there is no generic trailing error string in this one.
  Add the GeniusApi wrapper beside `signAndSendTransaction` (genius_api.dart:1189), taking an address
  rather than a wallet and resolving the key identically: `_secureStorage.getWallet(address)` then
  `getDevPrivateKey() ?? web3.getPrivateKeyStr(wallet)`.
  Never log, print or `toString()` the key or the wallet. The error returned is the RPC failure only.
  </action>
  <verify>
    <automated>flutter test test/squid_router/ --no-pub</automated>
  </verify>
  <done>approve exists on both layers, returns its own error, and the suite is at or above baseline.</done>
</task>
</tasks>

<threat_model>
Trust boundaries: key store → signing (the key is materialised as a hex String); user → router
contract (an approval grants a third party the right to move a token); RPC node → app (the allowance
figure the decision is made from).
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-03 | Elevation of Privilege | `approve` amount | high | mitigate | `decideApproval` can only emit the swap amount; there is no max-approval code path to reach. Asserted in Task 1. |
| T-26-04 | Information Disclosure | key as String | medium | accept | Inherited from the shipped signAndSendTransaction path; a String cannot be zeroed. Not widened: no new logging, no new field, the key never leaves the function. Converting all three call sites to Uint8List is a separate change. |
| T-26-06 | Repudiation | dropped approve error | medium | mitigate | Task 3's explicit `return` in the catch, so a rejected approval reports why and 26-07 can say it. |
</threat_model>

<verification>
1. `flutter test --no-pub` — at or above the measured baseline, zero failures.
2. `flutter analyze --no-pub` in the root AND in `packages/genius_api` — neither count rose.
3. `bash tool/check_brace_style.sh --count` → 0; `dart format --set-exit-if-changed lib test packages/genius_api/lib` exit 0.
4. `grep -rniE "privatekey|mnemonic|seed" packages/genius_api/lib/web3/web3.dart | grep -iE "print|log"` returns nothing.
</verification>

<success_criteria>
- The wallet can read an allowance and grant one.
- The granted amount is provably the swap amount.
- A failing approve says what failed.
</success_criteria>

<output>
Create `.planning/phases/26-swap-that-actually-swaps/26-02-SUMMARY.md` when done. 40 lines maximum.
</output>
