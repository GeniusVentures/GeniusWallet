---
phase: 26-swap-that-actually-swaps
plan: 03
type: execute
wave: 2
depends_on: ["26-01"]
files_modified: [lib/squid_router/squid_token_service.dart, lib/squid_router/squid_util.dart, lib/squid_router/route_details_card.dart, lib/squid_router/swap_screen.dart, lib/squid_router/models/squid_route_response.dart, lib/squid_router/models/squid_swap_params.dart, lib/squid_router/models/squid_gas_cost.dart, lib/squid_router/models/squid_fee_cost.dart, test/squid_router/squid_util_test.dart, test/squid_router/route_parse_test.dart, test/squid_router/route_details_card_test.dart, test/squid_router/fixtures/route_response.json]
autonomous: true
requirements: [SWAP-01]
estimate: {tokens: 60000, raw_tokens: 60000, tasks: 3, confidence: low}

must_haves:
  truths:
    - "Typing an amount produces a quote from a live /v2/route call, not from a constant."
    - "The amount sent to Squid is the typed amount converted to the pay token's base units."
    - "Rate, price impact and fees on screen are read off the returned estimate, and a recorded real /v2/route body deserializes into RouteResponseData with a non-null estimate."
    - "No quote path can produce a transactionRequest — every call in this plan is quoteOnly."
  artifacts: ["test/squid_router/fixtures/route_response.json", "test/squid_router/route_parse_test.dart", "test/squid_router/squid_util_test.dart"]
  key_links:
    - "fromAmount is RAW BASE UNITS. The screen passes the typed string through unconverted today, so '1.5' would be read as 1.5 wei. The single highest-consequence line in this plan."
    - "The four deleted models are a closed set: gas_cost and fee_cost import squid_token_info, but nothing imports THEM except squid_route_response and the card. Deleting the four does not force the token models out — 26-04 owns those."
---

<objective>
The quote becomes real: the first live call this app has ever made to Squid.

Purpose: the rate on screen is the constant 993.72 whatever the pair or amount, and the models the
card is typed on were fitted to that constant. Both go. `quoteOnly: true` is what keeps this path
harmless — without it Squid returns a signable transactionRequest and the display path carries one.
Output: a live route call returning `RouteResponseData`, a recorded fixture the rest of the phase
tests against, and a route card reading the real estimate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/26-swap-that-actually-swaps/26-ENVIRONMENT.md
@.planning/phases/26-swap-that-actually-swaps/26-CONTEXT.md
@.planning/phases/26-swap-that-actually-swaps/26-RESEARCH.md
@lib/squid_router/squid_client.dart
@lib/squid_router/squid_token_service.dart
@squidrouter/lib/src/model/estimate.dart
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: One typed amount reaches Squid and a real rate comes back</name>
  <files>lib/squid_router/squid_util.dart, lib/squid_router/squid_token_service.dart, test/squid_router/squid_util_test.dart</files>
  <precondition>`GW_SQUID_INTEGRATOR_ID` is defined in the run environment. Without it this task's live half cannot run — halt and report rather than stubbing it.</precondition>
  <behavior>
    - "1.5" at 18 decimals → 1500000000000000000; "1" at 6 decimals → 1000000.
    - "0.000000000000000001" at 18 decimals → 1.
    - A fraction longer than `decimals` truncates rather than rounding up, and never throws.
    - An unparseable string returns null; it does not return zero.
  </behavior>
  <action>
  Add `toBaseUnits(String amount, int decimals)` to `squid_util.dart` returning `BigInt?` — the
  inverse of the `formatTokenAmount` already there. String/BigInt arithmetic only, never `double`: a
  double cannot hold 18 significant digits and this value is the amount of money that moves.
  Replace `SquidTokenService.getRoute` with a live call: build a `RouteRequest` (chains as strings,
  tokens as addresses, fromAmount from `toBaseUnits`, both addresses the wallet's own, slippage,
  `quoteOnly: true`), pass it to `squidApi().getRoute`, return `RouteResponseData`. Let the
  `DioException` propagate — the screen's route-error path already catches, and 26-07 owns its
  wording. Touch only `getRoute` and the dead commented block beneath it, which references a
  `$_baseUrl` that does not exist and would not compile; the other two methods belong to 26-04.
  </action>
  <verify>
    <automated>flutter test test/squid_router/squid_util_test.dart --no-pub</automated>
    <human-check>Run the app with the integrator ID defined, pick a real pair, type an amount: the returned rate is not 993.72 and moves when the amount does.</human-check>
  </verify>
  <done>Base-unit conversion asserted by test; one live quote observed end to end.</done>
</task>
<task type="auto">
  <name>Task 2: Freeze a real response as the phase's fixture</name>
  <files>test/squid_router/fixtures/route_response.json, test/squid_router/route_parse_test.dart</files>
  <action>
  Capture the body of Task 1's live `/v2/route` response and save it verbatim as
  `test/squid_router/fixtures/route_response.json`. Redact the wallet address to an all-zero address
  before committing — nothing else. Do not hand-write this file: a fixture that was invented proves
  only that the invention parses.
  Write `route_parse_test.dart`: load the fixture, deserialize through `standardSerializers` into
  `RouteResponseData`, assert a non-null `route.estimate` with non-empty `exchangeRate`,
  `aggregatePriceImpact` and `toAmount` and at least one entry in `feeCosts` and `gasCosts`, and
  assert `route.quoteId` is present since 26-05 polls with it.
  This is the phase's offline proof: six of 26-RESEARCH.md's seven validation dimensions run against
  it, with no network and no credential.
  </action>
  <verify>
    <automated>flutter test test/squid_router/route_parse_test.dart --no-pub</automated>
  </verify>
  <done>The recorded body round-trips into the generated model. The fixture holds no real address.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: The card prints the estimate, and the old models go</name>
  <files>lib/squid_router/route_details_card.dart, lib/squid_router/swap_screen.dart, lib/squid_router/models/squid_route_response.dart, lib/squid_router/models/squid_swap_params.dart, lib/squid_router/models/squid_gas_cost.dart, lib/squid_router/models/squid_fee_cost.dart, test/squid_router/route_details_card_test.dart</files>
  <behavior>
    - Fed the fixture, Price Impact shows the fixture's aggregatePriceImpact.
    - Fed the fixture, Fees shows the summed USD of every fee cost AND every gas cost.
    - Two different fixture estimates produce two different rendered figures.
  </behavior>
  <action>
  Retype `RouteDetailsCard.route` and `_SwapScreenState.fetchedRoute` as `RouteResponseData`, reading
  `route.estimate` for every figure. Keep the card's four rows and its layout exactly — this phase
  changes no visual design.
  The Fees row today sums `feeCosts` only and ignores gas, understating what a swap costs. Sum
  `gasCosts` into the same total rather than adding a row: more truthful, same layout. Guard the sum
  against an unparseable `amountUsd` instead of bang-unwrapping `double.tryParse` as it does now.
  Delete the four listed models. `swapParams` becomes a `RouteRequest` builder and the `debugPrint`
  in `_submitSwap` that serialised it goes with it — it printed a wallet address; leave the rest of
  `_submitSwap` to 26-06. Rewrite `route_details_card_test.dart` against the fixture: its golden
  values were frozen from the constant and are no longer what the app can produce.
  </action>
  <verify>
    <automated>flutter test test/squid_router/ --no-pub</automated>
  </verify>
  <done>`ls lib/squid_router/models/` shows only the two token models. No figure on the card traces to a literal.</done>
</task>
</tasks>

<!-- planner-discipline-allow: 993.72 -->
<threat_model>
Trust boundaries: Squid API → app (an untrusted third party supplies the numbers a user spends money
on); user input → route request (a typed amount becomes a base-unit integer); recorded fixture →
repository (a captured response can carry wallet-identifying data into git).
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-07 | Tampering | `toBaseUnits` | critical | mitigate | String/BigInt arithmetic only, four asserted cases including sub-unit and over-length fractions. A 10^18 error here is the whole balance. |
| T-26-08 | Information Disclosure | route fixture | medium | mitigate | Address redacted to all-zero before commit; verification greps the fixture for 40-hex addresses. |
| T-26-09 | Tampering | estimate figures | medium | accept | The quote is advisory and is re-fetched non-quoteOnly at submit time in 26-05; slippage protection lives in toAmountMin, which the router enforces on-chain. |
</threat_model>

<verification>
1. `flutter test --no-pub` — at or above the measured baseline, zero failures.
2. `flutter analyze --no-pub` — count did not rise.
3. `grep -rn "993.72" lib/ | grep -vc '^\s*//'` → 0.
4. The fixture holds no 40-hex address other than the all-zero one; brace style 0; `dart format --set-exit-if-changed lib test` exit 0.
</verification>

<success_criteria>
- The quote on screen comes from the network, and the amount sent is the amount meant.
- A real response is frozen in the repo for every later plan to test against.
</success_criteria>

<output>
Create `.planning/phases/26-swap-that-actually-swaps/26-03-SUMMARY.md` when done. 40 lines maximum.
</output>
