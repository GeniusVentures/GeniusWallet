---
phase: 26-swap-that-actually-swaps
plan: 04
type: execute
wave: 3
depends_on: ["26-02", "26-03"]
files_modified: [lib/squid_router/swap_token.dart, lib/squid_router/squid_token_service.dart, lib/squid_router/models/squid_token_info.dart, lib/squid_router/models/squid_balance.dart, lib/squid_router/held_tokens.dart, lib/squid_router/swap_preselection.dart, lib/squid_router/swap_field.dart, lib/squid_router/token_selector_drawer.dart, lib/squid_router/route_details_card.dart, lib/squid_router/swap_screen.dart, test/squid_router/swap_token_test.dart, test/squid_router/squid_balance_test.dart, test/squid_router/held_tokens_test.dart, test/squid_router/token_identity_test.dart, test/squid_router/swap_preselection_test.dart, test/squid_router/token_selector_empty_state_test.dart, test/squid_router/swap_flip_centring_test.dart, test/squid_router/route_details_card_test.dart]
autonomous: true
requirements: [SWAP-01]
estimate: {tokens: 75000, raw_tokens: 75000, tasks: 3, confidence: low}

must_haves:
  truths:
    - "The token list comes from Squid's live catalogue, filtered to the wallet's selected chain."
    - "The pay side offers only tokens the wallet holds, decided from a real on-chain balance."
    - "A balance renders identically to today for dust, the display floor, an 18-digit fraction and a 13-digit whole number."
    - "lib/squid_router/models/ no longer exists, and no fabricated token or balance list survives under lib/."
  artifacts: ["lib/squid_router/swap_token.dart", "test/squid_router/swap_token_test.dart"]
  key_links:
    - "/v2/sdk-info is the catalogue endpoint: getSDKInfo() takes no required parameters and returns BuiltList<Token>. /v2/tokens (getTokenPrice) takes a REQUIRED address and is a price lookup, not a catalogue. Squid v2 exposes no balances endpoint at all, so balances come from the wallet's own machinery."
    - "Token.chainId is a String and Token.decimals is a num; SquidTokenInfo used int for both. Every comparison site is a conversion site."
    - "The four magnitude fixtures in the deleted balance list were added at the 08-07 walk because the picker had never been asked to render a real token magnitude. They move into the test, not out of existence."
---

<objective>
The token catalogue and the balances behind it become real, and the duplicate models go.

Purpose: `fetchTokens` and `fetchBalances` both return fabricated lists on their first line, so the
picker's pay side — which exists so a user cannot try to spend what they do not have — decides that
from invented numbers.
Output: one `SwapToken` view type over the submodule's `Token`, a live catalogue, real balances, and
no `lib/squid_router/models/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/26-swap-that-actually-swaps/26-ENVIRONMENT.md
@.planning/phases/26-swap-that-actually-swaps/26-CONTEXT.md
@lib/squid_router/models/squid_balance.dart
@lib/squid_router/held_tokens.dart
@lib/wallets/cubit/wallet_details_state.dart
@squidrouter/lib/src/model/token.dart
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: SwapToken replaces the two duplicated models</name>
  <files>lib/squid_router/swap_token.dart, lib/squid_router/models/squid_token_info.dart, lib/squid_router/models/squid_balance.dart, lib/squid_router/held_tokens.dart, lib/squid_router/swap_preselection.dart, test/squid_router/swap_token_test.dart, test/squid_router/squid_balance_test.dart, test/squid_router/held_tokens_test.dart, test/squid_router/token_identity_test.dart, test/squid_router/swap_preselection_test.dart</files>
  <behavior>
    - Identity is address AND chain, case-insensitive on the address; one address on two chains is two tokens.
    - A raw balance of 1000 at 18 decimals renders as the below-floor form, never as zero.
    - One raw unit of a 6-decimal token renders as the floor figure itself, not the below-floor form.
    - An 18-digit fraction caps at six places while the exact value stays readable for MAX, and a 13-digit whole number renders in full.
    - Absent and zero balances are both unspendable; dust is spendable.
  </behavior>
  <action>
  Create `lib/squid_router/swap_token.dart`: a `SwapToken` pairing the submodule's `Token` with a
  nullable `BigInt rawBalance`. It is not a re-implementation of a generated model — it is the balance
  pairing the API does not return. Move `sameAs`, `amountAsDouble`, `formattedBalance`,
  `displayBalance` and `display` onto it unchanged, keeping the raw-integer representation so the
  walk's four magnitude cases still render as they do today.
  Delete both files under `lib/squid_router/models/`, retype `held_tokens.dart` and
  `swap_preselection.dart`, migrate their tests. `hasSpendableBalance` keeps its conservative reading:
  absent or unparseable is not spendable, dust is.
  `Token.chainId` is a String and `Token.decimals` a num — convert once at the boundary. Fold
  `squid_balance_test.dart` into `swap_token_test.dart` and delete it; its fixtures come with it.
  </action>
  <verify>
    <automated>flutter test test/squid_router/swap_token_test.dart test/squid_router/held_tokens_test.dart test/squid_router/token_identity_test.dart test/squid_router/swap_preselection_test.dart --no-pub</automated>
  </verify>
  <done>`ls lib/squid_router/models/` errors — the directory is gone. All six behaviours asserted.</done>
</task>
<task type="auto">
  <name>Task 2: A live catalogue and a real balance behind it</name>
  <files>lib/squid_router/squid_token_service.dart, lib/squid_router/swap_screen.dart</files>
  <precondition>`GW_SQUID_INTEGRATOR_ID` is defined in the run environment for this task's live half.</precondition>
  <action>
  Replace `fetchTokens` with `squidApi().getSDKInfo()`, returning its `tokens` filtered to the
  selected network's chain id. Use `getSDKInfo`, not `getTokenPrice` — the latter is a price lookup
  that requires an address.
  Delete `fetchBalances` outright rather than reimplementing it: the generated client has no balances
  operation, and the wallet already knows its own holdings. In `_loadTokens`, build each `SwapToken`
  by intersecting the catalogue with `WalletDetailsState.coins` on address plus chain, then reading
  the exact integer for just those matches through the `rawBalanceOf` added in 26-02. `coins` decides
  WHICH tokens to read. Do not read a balance for every catalogue entry — on a busy chain that is
  dozens of RPC round trips per page load.
  The chain's native token has no ERC-20 contract to call. Take its balance from
  `WalletDetailsState.selectedWalletBalance`, which the rest of the app already displays, converted
  with `toBaseUnits`. Add no new RPC path for it.
  Delete the three fabricated lists and the retired commented block referencing an undefined
  `$_baseUrl` and a `testnet.api.squidrouter.com/v1` host; after this task the file holds network
  calls only. A failing catalogue fetch keeps the shipped behaviour: error toast, empty list, no crash.
  </action>
  <verify>
    <automated>flutter test test/squid_router/ --no-pub</automated>
    <human-check>With the integrator ID defined, open Swap on a funded wallet: the pay picker lists only held tokens with their real balances, the receive picker lists the chain's full catalogue.</human-check>
  </verify>
  <done>Both service methods hit the network or are deleted. No fabricated list remains under lib/.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Migrate the picker and the screen</name>
  <files>lib/squid_router/swap_field.dart, lib/squid_router/token_selector_drawer.dart, lib/squid_router/route_details_card.dart, lib/squid_router/swap_screen.dart, test/squid_router/token_selector_empty_state_test.dart, test/squid_router/swap_flip_centring_test.dart, test/squid_router/route_details_card_test.dart</files>
  <behavior>
    - The picker marks the currently selected token as selected, and still lists it.
    - The empty state renders when the filtered list is empty.
    - The flip control stays centred on the seam when the two cards differ in height.
  </behavior>
  <action>
  Retype every `SquidTokenInfo` parameter, field and callback in the four listed lib files, including
  `tokensForSide`. The MAX affordance and the balance line read through the moved helpers.
  This is a type migration and nothing else: no layout, no colour, no copy, no behaviour change — a
  diff line that is neither a type change nor a helper-access change is out of scope. Update the
  three tests to construct `SwapToken` directly rather than importing a fabricated list.
  </action>
  <verify>
    <automated>flutter test test/squid_router/ --no-pub</automated>
  </verify>
  <done>Suite at or above baseline. `grep -rn "SquidTokenInfo\|SquidBalance" lib/ test/` returns nothing.</done>
</task>
</tasks>

<threat_model>
Trust boundaries: Squid catalogue → picker (a third-party list decides which contract address a swap
targets); RPC node → balance (the figure the spendable decision is made from).
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-11 | Spoofing | catalogue token address | high | mitigate | Only tokens whose address AND chain match an entry the wallet already holds reach the pay side; the receive side's address is never spent from. Catalogue served over TLS from the pinned v2 host. |
| T-26-12 | Tampering | reported balance | medium | mitigate | The balance is read from the chain via `rawBalanceOf`, not taken from the catalogue. An over-reported balance fails at the CTA, not at signing. |
| T-26-13 | Denial of Service | per-token RPC reads | medium | mitigate | Reads bounded to the wallet's own holdings, never the catalogue. |
</threat_model>

<verification>
1. `flutter test --no-pub` at or above baseline, zero failures; `flutter analyze --no-pub` in root and in `packages/genius_api` — neither count rose.
2. `grep -rn "SquidTokenInfo\|SquidBalance\|squid_router/models" lib/ test/` returns nothing.
3. `grep -vE '^\s*(//|///)' lib/squid_router/squid_token_service.dart | grep -ciE "^\s*return mock"` → 0; brace style 0; `dart format --set-exit-if-changed lib test` exit 0.
</verification>

<success_criteria>
- Every token and every balance on the swap screen came from the network or the chain.
- The picker renders the walk's four magnitude cases exactly as today, and `models/` is gone.
</success_criteria>

<output>
Create `.planning/phases/26-swap-that-actually-swaps/26-04-SUMMARY.md` when done. 40 lines maximum.
</output>
