---
phase: 05-dashboard
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - lib/dashboard/news/view/crypto_news_screen.dart
autonomous: false
requirements: [SCR-01]
must_haves:
  truths:
    - "The news feed wears the redesign: each _NewsCard is a GWDecorations.surfaceSheen Container (borderSubtle border, card shadow, radiusMd) matching Alex's NewsCard decoration, the title/date text uses GeniusWalletTypography.titleMd/bodySm with gw.textSecondary replacing Colors.white60, the image placeholder fills gw/GeniusWalletColors.surfaceSunken, and appearance-aware reads go through Theme.of(context).extension<GWColors>() so cards flip LIVE on an in-place toggle (UI-SPEC §4.4)"
    - "Pull-to-refresh + retry on the news feed keep working and reload the data: the ALREADY-CORRECT _retryNews() re-issuing fetchCoinTelegraphNews(), the RefreshIndicator(onRefresh: _retryNews) wrap, and onRetry: _retryNews on FutureStateWidget (finding 18, :27-31/:57-58) are untouched (criterion 2)"
    - "develop's news structure is preserved: the StaggeredGrid.extent masonry layout is kept (NOT swapped for Alex's MasonryGridView.count — a different-but-equal algorithm, no user-visible requirement to change it), and the 'Crypto News' heading (already token-correct via textTheme.displaySmall) is left as-is (UI-SPEC §4.4 / §1)"
    - "The image error icon uses GeniusWalletColors.statusError (mode-invariant static) replacing Colors.red; the photo-scrim gradient (Colors.black54/black) is intentionally kept as-is (a scrim over a photo, not a brand surface); develop's news error/empty strings ('Failed to load news.' / 'No news found.') are unchanged (UI-SPEC §4.4 / §6)"
    - "No hardcoded-dark constant survives, including the Scaffold background (the §7 deepBlue* trap flagged specifically for crypto_news_screen.dart)"
  artifacts:
    - "lib/dashboard/news/view/crypto_news_screen.dart — _NewsCard re-skinned to surfaceSheen decoration, titleMd/bodySm typography, surfaceSunken image placeholder, statusError error icon; StaggeredGrid.extent + _retryNews + RefreshIndicator wiring untouched"
  key_links:
    - "_NewsCard build() -> Theme.of(context).extension<GWColors>() ?? GWColors.dark() -> card surface/text/border flips live; mode-invariant statusError stays static; GWDecorations.surfaceSheen recomputes on the forced rebuild"
    - "RefreshIndicator(onRefresh: _retryNews) + FutureStateWidget(onRetry: _retryNews) -> fetchCoinTelegraphNews() reload (finding 18, criterion 2) — unchanged"
---

<objective>
Re-skin develop's news feed (UI-SPEC §4.4) — CryptoNewsScreen and its _NewsCard — in place, taking Alex's
card-level token treatment (news_card.dart) as the visual reference while keeping develop's masonry layout and
data behaviour.

Purpose: criterion 1 (news renders in the redesign skin) and criterion 2's news clause (pull-to-refresh + retry
reload the feed — finding 18). Verify, bind, re-skin (§2): finding 18's _retryNews + RefreshIndicator wiring is
ALREADY correct; this plan re-skins the card chrome without touching it, and re-reads :27-31/:57-58 to prove it.
Re-skin, never restructure (§1): keep develop's StaggeredGrid.extent — Alex's MasonryGridView.count is a
different-but-equal masonry algorithm with no user-visible requirement forcing the change.

Appearance-color access path (04-02/04-04 discipline): appearance-aware card surface/text/border read via `final
gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();`; mode-invariant statusError stays on the
`GeniusWalletColors` static getter. GWDecorations.surfaceSheen recomputes from live appearance on the forced
rebuild. crypto_news_screen.dart is a Loading importer — its image-placeholder Loading is re-skinned by 05-01.

Output: a token-correct, appearance-aware `crypto_news_screen.dart`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin crypto_news_screen.dart _NewsCard (§4.4)</name>
  <files>lib/dashboard/news/view/crypto_news_screen.dart</files>
  <read_first>
    - lib/dashboard/news/view/crypto_news_screen.dart (203 lines): _retryNews() re-issuing fetchCoinTelegraphNews() and RefreshIndicator(onRefresh: () async => _retryNews()) at :27-31, onRetry: _retryNews on FutureStateWidget at :57-58 (finding 18 — do NOT touch); "Crypto News" heading via textTheme.displaySmall (already token-correct — leave as-is); StaggeredGrid.extent masonry (keep); _NewsCard = plain Card(clipBehavior: antiAlias); image placeholder Container(color: Colors.grey.shade800, child: Loading()); image error Icon(Icons.error, color: Colors.red); gradient text-overlay LinearGradient([Colors.black54, Colors.black]); card title/date raw TextStyle(fontSize: 14/12/16, ...), Colors.white60; the Scaffold background (deepBlue* trap, §7)
    - lib/theme/gw_colors.dart (appearance-aware textPrimary/textSecondary/surface/border fields; fail-soft read)
    - lib/theme/genius_wallet_decorations.dart (GWDecorations.surfaceSheen), lib/theme/genius_wallet_consts.dart (radiusMd)
    - lib/theme/genius_wallet_typography.dart (titleMd, bodySm, bodyMd), lib/theme/genius_wallet_colors.dart (statusError, surfaceSunken, borderSubtle — which are static vs appearance-aware)
    - C:\Users\User\Documents\Projects\GNUS-compare\GeniusWallet-3514\lib\dashboard\news\... news_card.dart (Alex's card decoration — READ-ONLY visual reference for _buildDesktopLayout/_buildMobileLayout)
    - 05-UI-SPEC.md §4.4 (the per-element substitution table), §2 finding-18 (wiring already correct), §6 (news copy unchanged), §7 (Scaffold-background deepBlue* trap + zero-raw discipline)
  </read_first>
  <action>
    Add `final gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();` at the top of _NewsCard's build()
    (and any other build() that reads an appearance-aware color). Follow §4.4's table:
    - "Crypto News" heading: leave as-is — already token-correct via textTheme.displaySmall.
    - Card decoration: plain `Card(clipBehavior: antiAlias)` -> `Container(decoration: BoxDecoration(gradient:
      GWDecorations.surfaceSheen, borderRadius: BorderRadius.circular(GeniusWalletConsts.radiusMd), border:
      Border.all(color: gw.borderSubtle), boxShadow: [...the card shadow]))` — matching Alex's NewsCard decoration
      (keep antiAlias clipping on the Container so the image corners stay rounded).
    - Image placeholder: `Container(color: Colors.grey.shade800, child: Loading())` -> `Container(color:
      GeniusWalletColors.surfaceSunken, child: Loading())` (Loading is re-skinned by 05-01; surfaceSunken — use
      `gw.surfaceSunken` for the live-flip read).
    - Image error icon: `Icon(Icons.error, color: Colors.red)` -> `Icon(Icons.error, color:
      GeniusWalletColors.statusError)` (statusError is MODE-INVARIANT, static).
    - Gradient text-overlay: keep `LinearGradient([Colors.black54, Colors.black])` as-is — a scrim over a photo,
      not a brand surface (§4.4 keeps this).
    - Card title / date text: raw `TextStyle(fontSize: 14/12/16, ...)` -> `GeniusWalletTypography.titleMd` (title)
      / `bodySm` (date); `Colors.white60` -> `gw.textSecondary`. Apply the same typography-token substitution to
      any hover-overlay text.
    - FutureStateWidget error/empty text: plain `Text` -> `GeniusWalletTypography.bodyMd.copyWith(color:
      gw.textSecondary)`.
    - Scaffold background: replace any hardcoded-dark constant (the §7 trap flagged specifically here) with the
      appropriate appearance-aware surface / transparent-over-theme choice — no deepBlue* survives.
    Keep develop's `StaggeredGrid.extent` layout (do NOT swap to MasonryGridView.count, §1). Do NOT touch
    _retryNews, the RefreshIndicator, or the onRetry wiring (:27-31/:57-58, finding 18) — re-read those ranges and
    confirm unchanged. Preserve develop's news copy verbatim (§6). Token discipline: zero raw hex/px; no
    Colors.white60 / Colors.red / Colors.grey.shade800 / deepBlue* survives (the scrim Colors.black54/black is the
    sole intentional exception).
  </action>
  <verify>
    <automated>flutter analyze lib/dashboard/news/view/crypto_news_screen.dart && bash tool/verify_additive_boundary.sh && grep -q 'extension<GWColors>()' lib/dashboard/news/view/crypto_news_screen.dart</automated>
  </verify>
  <done>
    _NewsCard is a surfaceSheen Container with a borderSubtle border, radiusMd, and card shadow; title/date use
    titleMd/bodySm with gw.textSecondary; the image placeholder fills surfaceSunken; the error icon is statusError;
    the photo scrim is intentionally kept; the Scaffold background has no deepBlue* left; StaggeredGrid.extent and
    the finding-18 _retryNews/RefreshIndicator/onRetry wiring are unchanged; news copy verbatim; no
    Colors.white60/red/grey.shade800 survives; analyze 0 errors and additive guard passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: News walk — criterion 1 + criterion 2 (pull-to-refresh); both modes</name>
  <action>Human verification checkpoint — no code changes. Run the recipe in <how-to-verify> in BOTH modes; record pass/fail per criterion in the SUMMARY.</action>
  <what-built>
    A re-skinned news feed — surfaceSheen _NewsCards with borderSubtle borders, titleMd/bodySm typography,
    surfaceSunken image placeholders, statusError error icons — with develop's StaggeredGrid.extent masonry and
    the pull-to-refresh + retry wiring intact.
  </what-built>
  <how-to-verify>
    Cold debug run (pinned SDK on PATH; close any Release exe): `CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release
    -DGENIUS_DEPENDENCY_BRANCH=develop -Dc-ares_DIR=..." flutter run -d windows --debug
    --dart-define=GW_DEV_TOOLS=true`.
    1. Criterion 1: open the news feed; confirm the cards (surfaceSheen decoration, border, shadow, title/date
       typography, image placeholder, scrim) match the Release exe reference.
    2. Criterion 2 (finding 18): pull-to-refresh the news feed and confirm it reloads (fetchCoinTelegraphNews
       re-issues); force a fetch failure and confirm the retry re-issues the fetch.
    3. Image states: confirm a card with a broken image shows the statusError error icon and a card mid-load
       shows the re-skinned Loading over a surfaceSunken placeholder.
    4. LIVE-FLIP: toggle appearance IN PLACE via the Dev header row and confirm the card surfaces, borders, and
       text re-skin immediately.
    5. Repeat in BOTH light and dark mode; confirm WCAG AA contrast for the title/date text over the card sheen
       (and legible over the photo scrim) in each mode.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per mode)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| News fetch -> refresh/retry surface | Pull-to-refresh and retry must reload the feed, not silently no-op (finding 18) |
| remote image -> card render | A broken/missing image must fall through to a token error icon, never crash the card |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05-01 | Denial of Service | _retryNews / RefreshIndicator / onRetry wiring | high | mitigate | Do NOT touch the :27-31/:57-58 wiring; only re-skin card chrome; confirm pull-to-refresh + retry reload at the walk |
| T-05-05-02 | Information Disclosure | Stale appearance color read (card surface/text wrong-mode on a live toggle) | medium | mitigate | Read card surface/text/border via `Theme.of(context).extension<GWColors>() ?? GWColors.dark()`; verified by the walk live-flip step and the grep gate |
| T-05-05-03 | Tampering | shadow-import allowlist (crypto_news_screen is a Loading importer) | low | mitigate | Run `bash tool/verify_additive_boundary.sh`; keep the canonical Loading import, no repoint |
| T-05-05-04 | Tampering | develop's masonry layout (StaggeredGrid.extent) | low | accept | UI-SPEC §1 keeps it; Alex's MasonryGridView.count is not adopted (no user-visible requirement) |
</threat_model>

<verification>
- `flutter analyze lib/dashboard/news/view/crypto_news_screen.dart` 0 errors; `bash tool/verify_additive_boundary.sh`
  passes.
- Card surface/text/border read via `Theme.of(context).extension<GWColors>()` (grep gate); statusError stays
  static; the finding-18 wiring re-read and confirmed unchanged.
- Task 2 walk (both modes): criterion 1 (news skin), criterion 2 (pull-to-refresh + retry reload), image states,
  live flip, WCAG AA all confirmed; news copy verbatim.
</verification>

<success_criteria>
The news feed wears the redesign (surfaceSheen cards, titleMd/bodySm typography, surfaceSunken placeholders,
statusError error icon) and flips LIVE on an in-place toggle; pull-to-refresh + retry reload the feed (finding
18); develop's StaggeredGrid.extent masonry and news copy are unchanged; WCAG AA holds in both modes.
</success_criteria>

<output>
Create `.planning/phases/05-dashboard/05-05-SUMMARY.md` when done. Record that StaggeredGrid.extent was kept
(Alex's MasonryGridView.count not adopted), the finding-18 re-verification, and the Scaffold-background trap fix.
</output>
</content>
