---
phase: 05-dashboard
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - lib/components/wallet_overview.dart
autonomous: false
requirements: [SCR-01, GAP-06]
must_haves:
  truths:
    - "The hero balance / wallet overview (canonical lib/components/wallet_overview.dart, GAP-06) wears the redesign: the 'Current Balance' label is GeniusWalletTypography.headlineMd on gw.textSecondary, the balance figure is GeniusWalletTypography.numericDisplay wrapped in GWAnimatedNumber (the §1-sanctioned reuse) so it counts up on first paint and on value change, and appearance-aware text/surface colors are read via Theme.of(context).extension<GWColors>() so the widget re-skins LIVE on an in-place appearance toggle"
    - "develop's behaviour is kept, never Alex's mock: the SGNUS-wallet-type branch (GeniusBalanceDisplay + GNUS/Minions toggle), the non-SGNUS balance branch ($0.00 / 'No funds available' / formatted balance), and the SGNUSConnectionWidget + SGNUSConnectionStatusWidget + SubmitJobDashboardButton stack are all preserved unchanged (UI-SPEC §3.1) — no 24h delta, no action-pill row, no Assets/NFTs tab is introduced (Alex's disqualified IA, §1)"
    - "'No funds available' uses GeniusWalletColors.statusError (mode-invariant static), replacing the hardcoded Colors.red; the GNUS/Minions ToggleButtons keeps its structure with fillColor/selectedColor/borderColor re-skinned to brandPrimary/textPrimary/borderSubtle (UI-SPEC §3.1 table)"
    - "The hero balance still does not render before the account has loaded — this widget only mounts under DashboardScreenState's loaded gate (finding 9, criterion 3), unchanged by this plan"
    - "dashboard_screen.dart:19's wallet_overview import is NOT repointed to the wallets_overview.g.dart shadow (analyzer-blind, 03-SHADOW-NAMES.md); the shadow is read-only reference"
  artifacts:
    - "lib/components/wallet_overview.dart — re-skinned hero: headlineMd label, numericDisplay balance in GWAnimatedNumber, statusError for 'No funds available', token-re-skinned GNUS/Minions ToggleButtons; appearance-aware reads via GWColors"
  key_links:
    - "wallet_overview.dart build() -> Theme.of(context).extension<GWColors>() ?? GWColors.dark() -> appearance-aware label/balance/surface flips live; mode-invariant statusError/brandPrimary stay static getters"
    - "GWAnimatedNumber(balance) -> count-up on first paint and on value change (§1 sanctioned component reuse, no behavior attached)"
    - "DashboardScreenState loaded gate (dashboard_screen.dart:57-69) -> WalletsOverview mounts only after account load -> hero never renders pre-load (finding 9)"
---

<objective>
Re-skin develop's hero balance / wallet overview (UI-SPEC §4.1 / §3.1, GAP-06) in place — the "Current
Balance" label, the balance figure, the SGNUS/non-SGNUS balance branches, the GNUS/Minions toggle — while
carrying develop's logic intact. This is the first area to land inside 05-01's already-re-skinned container.

Purpose: criterion 1 (balances render in the redesign skin and match the Release exe) and criterion 3's
hero-not-before-account-load clause (re-confirm the gate still holds with the re-skinned hero). Take the
visual, never the behavior (§1): the balance TYPOGRAPHY and the GWAnimatedNumber component are the only
things sanctioned from Alex's disqualified dashboard_screen.dart; his 24h delta (WIRE-9), action-pill row,
and Assets/NFTs tab (WIRE-8) are NOT ported. Re-skin, never restructure: develop's SGNUS branch, GNUS/Minions
toggle, SGNUSConnectionWidget/Status/SubmitJobDashboardButton stack keep their exact structure and behavior.

Reference: the shadow file `lib/components/wallets_overview.g.dart` (READ-ONLY — same author's own token
re-skin of this same widget) is the single best visual reference; it is structurally near-identical to the
canonical file and differs only in token discipline, which is this plan's job. Do NOT import or repoint to it.

Appearance-color access path (04-02/04-04 discipline): appearance-aware label/balance/surface colors read
via `final gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();`; mode-invariant statusError /
brandPrimary stay on the `GeniusWalletColors` static getters.

Output: a token-correct, appearance-aware `wallet_overview.dart`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-UI-SPEC.md
@.planning/phases/03-gw-component-library/03-SHADOW-NAMES.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin wallet_overview.dart (§3.1 / §4.1) in place</name>
  <files>lib/components/wallet_overview.dart</files>
  <read_first>
    - lib/components/wallet_overview.dart (153 lines — WalletsOverview / WalletsOverviewState; "Current Balance" heading via textTheme.headlineMedium; SGNUS branch = GeniusBalanceDisplay + GNUS/Minions ToggleButtons; non-SGNUS branch = $0.00 / "No funds available" (Colors.red) / formatted balance in a raw TextStyle(fontSize: 36, fontWeight: w500); the SGNUSConnectionWidget + SGNUSConnectionStatusWidget + SubmitJobDashboardButton stack)
    - lib/components/wallets_overview.g.dart (SHADOW — READ-ONLY reference; token choices for the GNUS/Minions ToggleButtons fillColor/selectedColor/borderColor at ~:148-152; do NOT import or repoint)
    - lib/components/data/gw_animated_number.dart (GWAnimatedNumber — count-up API; the §1-sanctioned reuse)
    - lib/theme/gw_colors.dart (appearance-aware textPrimary/textSecondary/surface fields; fail-soft read)
    - lib/theme/genius_wallet_typography.dart (headlineMd, numericDisplay, bodyMd), lib/theme/genius_wallet_colors.dart (statusError, brandPrimary, textPrimary, borderSubtle — which are MODE-INVARIANT static vs appearance-aware)
    - 05-UI-SPEC.md §3.1 (the per-element substitution table), §4.1, §1 (what CAN / CANNOT be taken from Alex), §6 (copy: "Current Balance" / "No wallet selected" / "No funds available" unchanged), §7 (token discipline)
  </read_first>
  <action>
    Re-skin per §3.1's table. At the top of the relevant build() add `final gw =
    Theme.of(context).extension<GWColors>() ?? GWColors.dark();` (import `package:genius_wallet/theme/gw_colors.dart`).
    - "Current Balance" heading: `textTheme.headlineMedium` -> `GeniusWalletTypography.headlineMd` on
      `gw.textSecondary` (it is the LABEL above the number, not the number — matches Alex's _HeroBalance label).
    - "No wallet selected" / "No funds available": plain `Text` -> `GeniusWalletTypography.bodyMd`; the
      "No funds available" color `Colors.red` -> `GeniusWalletColors.statusError` (MODE-INVARIANT static — do NOT
      route through GWColors).
    - Balance figure: raw `TextStyle(fontSize: 36, fontWeight: w500)` -> `GeniusWalletTypography.numericDisplay
      .copyWith(color: gw.textPrimary)`, and WRAP the figure in `GWAnimatedNumber` (the §1-sanctioned reuse) so it
      counts up on first paint and on value change — replacing the static `Text`. Attach NO new behavior (no delta,
      no percentage) — GWAnimatedNumber only animates the develop-sourced value.
    - GNUS/Minions toggle: keep the `ToggleButtons` widget and its behavior; re-skin `fillColor` ->
      `GeniusWalletColors.brandPrimary`, `selectedColor` -> `GeniusWalletColors.textPrimary` (or the shadow's
      choice), `borderColor` -> `GeniusWalletColors.borderSubtle` (the shadow's own token choices at ~:148-152).
    Preserve develop's copy verbatim (§6): "Current Balance", "No wallet selected", "No funds available" — no
    rewording. Keep the SGNUS-wallet-type branch, the non-SGNUS branch, and the SGNUSConnectionWidget +
    SGNUSConnectionStatusWidget + SubmitJobDashboardButton stack structurally unchanged (§3.1 / §1). Do NOT add a
    24h delta, an action-pill row, or an Assets/NFTs tab (Alex's disqualified IA, §1). The container is NOT this
    widget's job — DashboardScrollContainer (05-01) owns it. Do NOT import or repoint to wallets_overview.g.dart
    (03-SHADOW-NAMES.md). Token discipline: zero raw hex, zero raw px, no Colors.red/Colors.white survives.
  </action>
  <verify>
    <automated>flutter analyze lib/components/wallet_overview.dart && bash tool/verify_additive_boundary.sh && grep -q 'extension<GWColors>()' lib/components/wallet_overview.dart</automated>
  </verify>
  <done>
    "Current Balance" label is headlineMd on gw.textSecondary; the balance figure is numericDisplay in
    GWAnimatedNumber on gw.textPrimary; "No funds available" is statusError; the GNUS/Minions ToggleButtons is
    re-skinned (brandPrimary/textPrimary/borderSubtle) with its behavior intact; the SGNUS/non-SGNUS branches and
    the connection/submit-job stack are structurally unchanged; no 24h delta / pill row / NFTs tab added; develop
    copy verbatim; no shadow import repointed; no Colors.red/white survives; analyze 0 errors and additive guard
    passes; `extension<GWColors>()` present.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Hero balance walk — criterion 1 + criterion 3 hero-not-before-load; both modes + live flip</name>
  <action>Human verification checkpoint — no code changes. Run the recipe in <how-to-verify> in BOTH modes; record pass/fail per criterion in the SUMMARY.</action>
  <what-built>
    A re-skinned hero balance / wallet overview — headlineMd label, numericDisplay balance in GWAnimatedNumber,
    statusError "No funds available", token-re-skinned GNUS/Minions toggle — carrying develop's SGNUS/non-SGNUS
    branches and connection/submit-job stack intact, mounted inside 05-01's re-skinned container.
  </what-built>
  <how-to-verify>
    Cold debug run (pinned SDK on PATH; close any Release exe): `CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release
    -DGENIUS_DEPENDENCY_BRANCH=develop -Dc-ares_DIR=..." flutter run -d windows --debug
    --dart-define=GW_DEV_TOOLS=true` (GW_DEV_TOOLS=true for the Dev header appearance toggle).
    1. Criterion 1: land on /dashboard, confirm the hero balance area matches the Release exe reference —
       "Current Balance" label above a large numericDisplay figure that counts up on first paint; side-by-side
       with the exe at GeniusWallet-3514 if available.
    2. Confirm develop's behavior is intact: for a non-SGNUS wallet with no funds, "No funds available" reads in
       statusError; for the SGNUS wallet, the GNUS/Minions toggle switches units and the GeniusBalanceDisplay,
       SGNUSConnectionWidget/Status, and Submit Job button are all present and functional — NO 24h delta, NO
       action-pill row, NO Assets/NFTs tab.
    3. Criterion 3 (hero-not-before-load): confirm the hero balance does NOT flash before the account has loaded
       (the DashboardScreenState gate still holds — this plan did not touch it).
    4. LIVE-FLIP: with the hero on screen, toggle appearance IN PLACE via the Dev header row and confirm the
       label, balance figure, and surrounding text re-skin IMMEDIATELY (stale = a static-getter read regression,
       BLOCKS close).
    5. Repeat in BOTH light and dark mode; confirm WCAG AA contrast for the label, the balance figure, and
       "No funds available"/statusError in each mode.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per mode)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shadow allowlist -> canonical mount | wallets_overview.g.dart is an analyzer-blind shadow of this file; dashboard_screen.dart:19 must keep importing the canonical path |
| develop balance logic -> displayed figure | The re-skin must animate develop's real balance, never introduce a fabricated delta or mocked value (WIRE-9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-01 | Tampering | wallets_overview.g.dart shadow-import allowlist (analyzer-blind .g.dart, WalletsOverview/WalletsOverviewState) | high | mitigate | Read the shadow for reference only; never import or repoint dashboard_screen.dart:19; run `bash tool/verify_additive_boundary.sh` in the task verify |
| T-05-02-02 | Spoofing | Fabricated balance/delta (Alex's WIRE-9 mock) sneaking in via the re-skin | medium | mitigate | GWAnimatedNumber animates only develop's sourced value; add no delta/percentage; confirm at the walk that no 24h delta renders |
| T-05-02-03 | Information Disclosure | Stale appearance color read (hero rendered wrong-mode on a live toggle) | medium | mitigate | Read label/balance/surface via `Theme.of(context).extension<GWColors>() ?? GWColors.dark()`; verified by the walk live-flip step and the grep gate |
| T-05-02-04 | Denial of Service | Hero rendering before account load (gate regression) | low | accept | This plan does not touch the finding-9 gate (owned by 05-01/dashboard_screen.dart); re-confirmed at the walk |
</threat_model>

<verification>
- `flutter analyze lib/components/wallet_overview.dart` 0 errors; `bash tool/verify_additive_boundary.sh` passes
  (shadow dead, import not repointed).
- Appearance-aware label/balance/surface read via `Theme.of(context).extension<GWColors>()` (grep gate);
  statusError/brandPrimary stay static.
- Task 2 walk (both modes): criterion 1 (hero skin + count-up match), develop behavior intact (SGNUS/non-SGNUS
  branches, toggle, connection/submit stack, no delta/pill/NFTs), criterion 3 (hero-not-before-load), live flip,
  WCAG AA all confirmed.
</verification>

<success_criteria>
The hero balance wears the redesign (headlineMd label, numericDisplay count-up, statusError "No funds
available", re-skinned GNUS/Minions toggle) and flips LIVE on an in-place appearance toggle; develop's
SGNUS/non-SGNUS branches and connection/submit-job stack are intact with no Alex mock introduced; the hero
does not render before account load; WCAG AA holds in both modes; no shadow import repointed.
</success_criteria>

<output>
Create `.planning/phases/05-dashboard/05-02-SUMMARY.md` when done. Record the GWAnimatedNumber reuse (visual
only, no behavior), confirmation no WIRE-9 delta / WIRE-8 NFTs tab was introduced, and that the shadow stayed
read-only.
</output>
</content>
