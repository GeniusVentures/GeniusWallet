---
phase: 05-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/dashboard/home/view/dashboard_screen.dart
  - lib/components/loading.dart
  - lib/components/custom_future_builder.dart
autonomous: false
requirements: [SCR-01, GAP-06]
must_haves:
  truths:
    - "Every one of develop's five dashboard areas renders inside the re-skinned DashboardScrollContainer (a GWDecorations.surface Container, not a plain Material Card) — the single highest-leverage re-skin, since DashboardScrollContainer wraps all five areas (UI-SPEC §5); the container's appearance-aware surface is resolved via Theme.of(context).extension<GWColors>() so it flips LIVE on an in-place appearance toggle rather than rendering stale"
    - "The shared Loading widget (canonical lib/components/loading.dart, GAP-06) wears the redesign spinner (brandGreen + statusInfo dots, space8 Wrap, headlineLg text) IN PLACE, so all 19 canonical importers — the dashboard, markets, news, and the loading screen the accountStatus gate falls back to — pick up the re-skin with zero call-site changes (UI-SPEC §3.2)"
    - "FutureStateWidget's own default chrome is token-correct (error_outline + statusError icon, a GWButton retry) while its onRetry/error plumbing (finding 8) is untouched (UI-SPEC §4.6)"
    - "develop's dashboard behaviour is preserved unregressed and re-verified by reading the same line ranges: the accountStatus/subscribeToWalletStatus gate + explicit error branch (finding 9, :57-69), the OneColumnDashBoardView pull-to-refresh (finding 17, :221-223), and the wallet/network-guarded getCoins() (finding 29, :38-43)"
    - "The develop IA is unchanged: gridSpacing and every ConstrainedBox/Expanded/flex layout arithmetic in ResponsiveDashboardView/OneColumnDashBoardView stay byte-identical (UI-SPEC §1 locks the 5-area IA); dashboard_screen.dart:19's wallet_overview import is NOT repointed to the wallets_overview.g.dart shadow"
  artifacts:
    - "lib/dashboard/home/view/dashboard_screen.dart — DashboardScrollContainer (:257-267) re-skinned to a GWDecorations.surface Container reading appearance-aware surface via GWColors; layout arithmetic and the three behaviour sites (gate/refresh/getCoins) unchanged"
    - "lib/components/loading.dart — canonical Loading re-skinned in place (brandGreen/statusInfo dots, space8 Wrap, headlineLg), shadow lib/components/loading/loading.dart left untouched"
    - "lib/components/custom_future_builder.dart — FutureStateWidget default error icon + default retry button re-skinned; onRetry/error plumbing untouched"
  key_links:
    - "DashboardScrollContainer.build() -> Theme.of(context).extension<GWColors>() ?? GWColors.dark() -> the GWDecorations.surface fill flips live for all five dashboard areas on an in-place appearance toggle (04-02/04-04 discipline)"
    - "lib/components/loading.dart (canonical) -> its 19 importers (03-SHADOW-NAMES.md) inherit the re-skin; the shadow lib/components/loading/loading.dart stays dead code, no importer repointed (verify_additive_boundary.sh)"
    - "DashboardScreenState.build() accountStatus==loaded && subscribeToWalletStatus==loaded gate (:57-69) -> error branch OR LoadingScreen (which renders the re-skinned Loading) -> hero never renders pre-load (finding 9, criterion 3)"
---

<objective>
Lay the shared dashboard chrome foundation the four following area plans render through, before any
single area is re-skinned: the DashboardScrollContainer that wraps all five areas (UI-SPEC §5), the
shared Loading widget (§3.2, GAP-06), and FutureStateWidget's default chrome (§4.6). Doing these first
means each subsequent area re-skin lands inside an already-correct container and loading treatment,
instead of a walk that shows a re-skinned area sitting in an un-skinned Card.

Purpose: criterion 1 (the shell every area mounts into wears the skin), criterion 2's dashboard
pull-to-refresh (finding 17), and criterion 3's error-with-retry / never-an-endless-spinner /
hero-not-before-account-load gate (findings 9, 29). Re-skin, never restructure (UI-SPEC §1): develop's
5-area IA, gridSpacing, and layout arithmetic are locked — only visual treatment changes. Verify, bind,
re-skin-without-regressing (UI-SPEC §2): findings 9/17/29 are ALREADY correct on develop; this plan
touches their file's visuals without disturbing their logic, and re-reads the same line ranges to prove
it before closing.

Appearance-color access path (04-02/04-04 discipline): appearance-aware surface/text/border colors MUST
be read via `final gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();` so const / no-Theme-
dependency widgets re-skin LIVE on an in-place toggle; mode-invariant brand/status tokens (brandGreen,
statusInfo, statusError) stay on the `GeniusWalletColors` static getters. GWDecorations.surface recomputes
from live appearance on every build() call — it just needs build() to re-run, which the `gw` read forces.

Output: a token-correct, appearance-aware DashboardScrollContainer; a re-skinned canonical Loading; a
token-correct FutureStateWidget default chrome.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-UI-SPEC.md
@.planning/phases/03-gw-component-library/03-SHADOW-NAMES.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin DashboardScrollContainer (§5) — the container wrapping all five areas</name>
  <files>lib/dashboard/home/view/dashboard_screen.dart</files>
  <read_first>
    - lib/dashboard/home/view/dashboard_screen.dart (400 lines): gridSpacing const :21; DashboardScreenState.build() accountStatus gate + error branch :57-69 (finding 9); getCoins() wallet/network guard in the post-frame callback :38-43 (finding 29); ResponsiveDashboardView :78+ (1/2/3-col layout, gridSpacing/2 padding :101/:145); OneColumnDashBoardView :204+ with its RefreshIndicator :221-223 (finding 17); DashboardScrollContainer :257-267 (the plain Card + EdgeInsets.all(gridSpacing) padding); the five area wrappers each wrapping their child in DashboardScrollContainer (:274, :292, :339, :352, :380); import of wallet_overview.dart at :19 (shadow hazard — do NOT repoint)
    - lib/theme/gw_colors.dart (GWColors ThemeExtension — surfaceBase/surfaceElevated/surfaceMenu/surfaceSunken + text/border fields; fail-soft read `Theme.of(context).extension<GWColors>() ?? GWColors.dark()`)
    - lib/theme/genius_wallet_decorations.dart (GWDecorations.surface(radius) / surfaceSheen — recompute from live GWAppearance.isLight on each build())
    - lib/theme/genius_wallet_consts.dart (radiusLg, gridSpacing precedents)
    - 05-UI-SPEC.md §5 (DashboardScrollContainer re-skin target), §1 (IA locked — layout untouched), §7 (the deepBlue* / Colors.white trap + zero-raw discipline)
    - .planning/phases/04-navigation-shell-chrome/04-04-SUMMARY.md (why registering a Theme dependency in a persisting widget's build() also freshens GWDecorations reads)
  </read_first>
  <action>
    Re-skin ONLY DashboardScrollContainer (:257-267). At the top of its build() add the fail-soft read
    `final gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();` (import
    `package:genius_wallet/theme/gw_colors.dart`; no bang). Replace the plain Material `Card` wrapper with a
    `Container(decoration: GWDecorations.surface(radius: GeniusWalletConsts.radiusLg), ...)` per §5, keeping the
    existing `Padding(padding: EdgeInsets.all(gridSpacing), child: child)`. Because DashboardScrollContainer is
    const-constructed at its five call sites, the `gw` read is what registers the Theme dependency that forces
    the const subtree to rebuild and re-skin LIVE on an in-place appearance toggle; GWDecorations.surface then
    recomputes its appearance-aware fill on that rebuild (04-04 mechanism). If GWDecorations.surface needs an
    explicit surface color argument, feed it an appearance-aware `gw.*` surface field (surfaceElevated) — never
    a hardcoded-dark constant (deepBlueCardColor / deepBlueMenu / deepBlueTertiary) and never Colors.white/
    grey[N] (§7 trap). Keep `gridSpacing`'s value (:21) and EVERY ConstrainedBox/Expanded/flex/gridSpacing
    arithmetic in ResponsiveDashboardView and OneColumnDashBoardView exactly as-is — those are layout, and §1
    locks the IA. Do NOT touch the accountStatus gate (:57-69), the RefreshIndicator (:221-223), or the getCoins
    guard (:38-43) — they are behaviour (findings 9/17/29), already correct; re-read those three ranges and
    confirm unchanged before closing. Do NOT repoint the wallet_overview.dart import at :19 to the
    wallets_overview.g.dart shadow (analyzer-blind; 03-SHADOW-NAMES.md binding rule). Token discipline: zero raw
    hex, zero raw px.
  </action>
  <verify>
    <automated>flutter analyze lib/dashboard/home/view/dashboard_screen.dart && bash tool/verify_additive_boundary.sh && grep -q 'extension<GWColors>()' lib/dashboard/home/view/dashboard_screen.dart</automated>
  </verify>
  <done>
    DashboardScrollContainer is a GWDecorations.surface Container reading its appearance-aware surface via
    `Theme.of(context).extension<GWColors>() ?? GWColors.dark()`; no plain Card, no deepBlue*/Colors.white
    survives in it; gridSpacing and all layout arithmetic unchanged; the finding-9 gate, finding-17
    RefreshIndicator, and finding-29 getCoins guard are byte-identical; wallet_overview import not repointed;
    analyze 0 errors and the additive guard passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin the shared Loading (§3.2, GAP-06) + FutureStateWidget default chrome (§4.6)</name>
  <files>lib/components/loading.dart, lib/components/custom_future_builder.dart</files>
  <read_first>
    - lib/components/loading.dart (24 lines — `{String? text}` StatelessWidget; leftDotColor lightGreenPrimary, rightDotColor Colors.blue; Row(spacing: 16, mainAxisSize: min); default/inherited text style)
    - lib/components/loading/loading.dart (the SHADOW — READ-ONLY reference only; its token choices leftDotColor brandGreen / rightDotColor statusInfo, Wrap(space8), headlineLg are exactly the re-skin target; do NOT import or repoint to it)
    - lib/components/custom_future_builder.dart (55 lines — FutureStateWidget; default `loading` = Center(Loading()); default `error` = Icon(Icons.error, color: Colors.red, size: 48); default retry = bare ElevatedButton(child: Text('Retry')); the onRetry/error slots — finding 8 plumbing — must stay untouched)
    - lib/components/buttons/gw_button.dart (GWButton API: label / variant / leading)
    - lib/theme/genius_wallet_colors.dart (brandGreen, statusInfo, statusError — all MODE-INVARIANT static getters)
    - lib/theme/genius_wallet_consts.dart (space8), lib/theme/genius_wallet_typography.dart (headlineLg)
    - 05-UI-SPEC.md §3.2 (loading.dart substitution table), §4.6 (FutureStateWidget default chrome)
    - 03-SHADOW-NAMES.md (Loading shadow: 19 canonical importers; canonical path wins; no importer repointed)
  </read_first>
  <action>
    `loading.dart` (canonical, GAP-06 — edited IN PLACE so all 19 importers pick it up): apply the shadow file's
    token choices per §3.2 — `leftDotColor: GeniusWalletColors.brandGreen`, `rightDotColor:
    GeniusWalletColors.statusInfo` (both MODE-INVARIANT, stay on static getters — do NOT route through GWColors);
    layout `Row(spacing: 16, mainAxisSize: min)` -> `Wrap(spacing: GeniusWalletConsts.space8,
    crossAxisAlignment: WrapCrossAlignment.center)` (functionally equivalent, token-based spacing); text style ->
    `GeniusWalletTypography.headlineLg`. Do NOT import or repoint to the shadow `lib/components/loading/loading.dart`
    — edit the canonical file in place; the shadow stays dead code (03-SHADOW-NAMES.md binding rule). This one
    edit closes the loading-state visual for the dashboard, markets, news, and the loading screen at once — do
    not additionally hand-roll a different loading treatment per screen.

    `custom_future_builder.dart` (§4.6 — chrome only, NOT plumbing): default error icon
    `Icon(Icons.error, color: Colors.red, size: 48)` -> `Icon(Icons.error_outline, color:
    GeniusWalletColors.statusError, size: 48)` (statusError is MODE-INVARIANT, static). Default retry button
    (fired when onRetry is set and no custom error widget is supplied) `ElevatedButton(child: Text('Retry'))` ->
    `GWButton(label: 'Retry', variant: GWButtonVariant.primary, leading: Icon(Icons.refresh))`. Leave the default
    `loading` slot as `Center(Loading())` — it is re-skinned automatically by the loading.dart edit above. Do NOT
    touch the `onRetry` wiring or the `error` slot resolution logic (finding 8, already correct everywhere in
    this phase's scope). Token discipline: zero raw hex, zero raw px; no Colors.red/Colors.blue survives.
  </action>
  <verify>
    <automated>flutter analyze lib/components/loading.dart lib/components/custom_future_builder.dart && bash tool/verify_additive_boundary.sh</automated>
  </verify>
  <done>
    loading.dart spinner dots read brandGreen/statusInfo (static), layout is Wrap(space8), text is headlineLg;
    the shadow loading/loading.dart is untouched and its 19 canonical importers are unchanged (additive guard);
    custom_future_builder.dart default error = Icons.error_outline + statusError, default retry = GWButton
    primary with a refresh leading icon, and the onRetry/error plumbing is byte-identical; no Colors.red/blue
    survives; analyze 0 errors and the additive guard passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Dashboard foundation walk — container, loading, gate, refresh; both modes + live flip</name>
  <action>Human verification checkpoint — no code changes. Run the recipe in <how-to-verify> in BOTH appearance modes and record pass/fail per criterion in the SUMMARY.</action>
  <what-built>
    The DashboardScrollContainer re-skinned to a GWDecorations.surface Container that wraps all five dashboard
    areas, the shared Loading widget re-skinned in place (reaching the whole app's 19 importers), and
    FutureStateWidget's default error/retry chrome — with develop's accountStatus gate, dashboard
    pull-to-refresh, and guarded getCoins() all preserved.
  </what-built>
  <how-to-verify>
    Cold debug run (pinned SDK on PATH: /c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin; close any
    Release exe first): `CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release -DGENIUS_DEPENDENCY_BRANCH=develop
    -Dc-ares_DIR=..." flutter run -d windows --debug --dart-define=GW_DEV_TOOLS=true`. GW_DEV_TOOLS=true so the
    Dev header row's appearance toggle is reachable in place (there is no user-facing toggle yet — STATE todo).
    1. Criterion 1 (container): land on /dashboard and confirm ALL five areas (balances/holdings, markets,
       news, transactions, plus the SGNUS/chart areas) render inside the new GWDecorations.surface card
       treatment — rounded radiusLg surface, no flat legacy Material Card, no hardcoded-dark fill.
    2. Criterion 3 (gate, finding 9): confirm that while the account is loading the dashboard shows the
       re-skinned loading spinner (brandGreen + statusInfo dots) and NEVER a bare hero balance before the
       account has loaded; if you can force a wallet/account load failure, confirm an error message appears
       with a working retry (a GWButton), not an endless spinner.
    3. Criterion 2 (finding 17): on the single-column layout, pull-to-refresh the dashboard and confirm it
       dispatches a reload (wallets + getCoins).
    4. Loading reach (§3.2): confirm the re-skinned spinner also appears on the markets grid and the news feed
       while they load — one edit, four surfaces.
    5. LIVE-FLIP: with the dashboard on screen, toggle appearance IN PLACE via the Dev header row and confirm
       the container surfaces of all five areas re-skin IMMEDIATELY (a card that stays dark after the toggle
       means the container is still reading a static getter instead of GWColors — a stale-const regression,
       BLOCKS close).
    6. Repeat the full walk in BOTH light and dark mode; confirm WCAG AA contrast for text-on-container in each
       mode. A light-mode regression blocks close.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per mode)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shadow allowlist -> canonical mount | The dashboard mounts the canonical Loading and WalletsOverview; the additive guard's shadow-importer counts (Loading=19, WalletsOverview=0-live) must not drift |
| accountStatus/subscribeToWalletStatus -> hero render | The gate must keep the hero from rendering before the account loads (finding 9); a re-skin that alters the gate reintroduces a pre-load render |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Tampering | Loading / WalletsOverview shadow-import allowlist | high | mitigate | Edit canonical loading.dart in place, never repoint an importer to the shadow; do NOT repoint dashboard_screen.dart:19's wallet_overview import; run `bash tool/verify_additive_boundary.sh` in every task verify |
| T-05-01-02 | Information Disclosure | Stale appearance color read (container rendered wrong-mode on a live toggle) | medium | mitigate | Read the container surface via `Theme.of(context).extension<GWColors>() ?? GWColors.dark()`; verified by the Task 3 live-flip step and the `grep -q 'extension<GWColors>()'` gate |
| T-05-01-03 | Denial of Service | accountStatus gate / getCoins guard regressed into an endless spinner or pre-load crash | high | mitigate | Do not touch the :57-69 gate, :221-223 refresh, or :38-43 getCoins guard; re-read the ranges before closing; confirm error-with-retry (never endless spinner) at the walk |
| T-05-01-04 | Tampering | develop's locked 5-area IA (gridSpacing / flex arithmetic) | low | accept | UI-SPEC §1 locks it; only DashboardScrollContainer's decoration changes; layout arithmetic left byte-identical |
</threat_model>

<verification>
- `flutter analyze` 0 errors on all three files; `bash tool/verify_additive_boundary.sh` passes (Loading shadow
  still dead, 19 canonical importers unchanged, wallet_overview import not repointed).
- DashboardScrollContainer reads its surface via `Theme.of(context).extension<GWColors>()` (grep gate); loading.dart
  and custom_future_builder.dart use mode-invariant static tokens for brand/status.
- Task 3 walk (both modes): container skin on all five areas, re-skinned loading on dashboard/markets/news, the
  finding-9 gate (error+retry, hero-not-before-load), finding-17 pull-to-refresh, and the in-place live flip all
  confirmed; WCAG AA holds in both modes.
- Findings 9/17/29 re-read and confirmed unchanged after the edit.
</verification>

<success_criteria>
All five dashboard areas render inside the re-skinned GWDecorations.surface container, flipping LIVE on an
in-place appearance toggle; the shared Loading and FutureStateWidget default chrome wear the redesign; develop's
accountStatus gate (hero-not-before-load, error+retry, no endless spinner), dashboard pull-to-refresh, and
guarded getCoins() are preserved unregressed; WCAG AA holds in both modes; no shadow import repointed; the
locked IA is untouched.
</success_criteria>

<output>
Create `.planning/phases/05-dashboard/05-01-SUMMARY.md` when done. Record the DashboardScrollContainer surface
choice, confirmation the Loading edit reaches all 19 importers with no shadow repoint, and the re-read
confirmation that findings 9/17/29 stayed correct.
</output>
</content>
</invoke>
