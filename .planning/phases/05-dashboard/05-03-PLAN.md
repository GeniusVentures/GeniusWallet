---
phase: 05-dashboard
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - lib/components/coins/view/coins_screen.dart
  - lib/components/coins/view/coin_card_row.dart
autonomous: false
requirements: [SCR-01]
must_haves:
  truths:
    - "The holdings list wears the redesign: the loading state renders inside a GWDecorations.surface container (not a hardcoded-color Card) showing the re-skinned Loading, the empty state is a GWEmptyState ('No coins yet' + explanatory body), and each CoinCardRow's one hardcoded Colors.white value text is read via gw.textPrimary so it re-skins LIVE on an in-place appearance toggle (UI-SPEC §4.2)"
    - "Market data refreshes ONCE A MINUTE, not every 20 seconds: coins_screen.dart's Timer.periodic stays const Duration(minutes: 1) (finding 14, :44, criterion 4) — verified unchanged after the re-skin"
    - "'No Coins Detected' is deliberately re-copied to 'No coins yet' — the ONE recorded copy change this plan makes, following the project's 'No <thing> yet' empty-state pattern (UI-SPEC §4.2 / §6); recorded explicitly in the SUMMARY"
    - "develop's behaviour is preserved: the ListTile row structure is kept (not swapped to GWTokenRow), the gain/loss color logic (cs.primary / cs.error) already flips correctly post-Phase-4 and is unchanged, and the pull-to-refresh / getCoins wiring is untouched"
    - "No overflow or crash on long balance values: every AutoSizeText / overflow guard on the coin rows survives the token substitution unchanged (criterion 5); the null-safe icon fallback (buildTokenIcon -> Icons.image_not_supported) is preserved, and Alex's letter-avatar pattern is NOT introduced (finding 30)"
  artifacts:
    - "lib/components/coins/view/coins_screen.dart — loading state in a GWDecorations.surface container, empty state as GWEmptyState ('No coins yet'), refresh Timer.periodic verified at 1 minute"
    - "lib/components/coins/view/coin_card_row.dart — the one hardcoded Colors.white value text migrated to gw.textPrimary; ListTile structure and gain/loss logic unchanged"
  key_links:
    - "coin_card_row.dart build() -> Theme.of(context).extension<GWColors>() ?? GWColors.dark() -> the value text flips live on toggle; gain/loss cs.primary/cs.error resolves through the wired theme (Phase 4)"
    - "coins_screen.dart Timer.periodic(const Duration(minutes: 1)) (:44) -> CoinGecko refresh cadence (finding 14, criterion 4) — unchanged"
    - "buildTokenIcon() (image_utils.dart) -> Icons.image_not_supported fallback on empty/null iconPath -> no empty-symbol crash (finding 30); no letter-avatar path added"
---

<objective>
Re-skin develop's holdings list (UI-SPEC §4.2) — the ContributionsDashboardView content, i.e. CoinsScreen +
CoinCardRow — in place, and verify the finding-14 market-data refresh interval. All re-skin targets here are
cosmetic; develop's logic already wins.

Purpose: criterion 1 (holdings render in the redesign skin), criterion 4 (market data refreshes once a minute
— the coins_screen Timer, finding 14), and criterion 5's long-value / empty-symbol clauses (overflow-safe rows,
the null-safe icon fallback, no Alex letter-avatar). Re-skin, never restructure (§1): keep develop's ListTile
row and the SGNUS/coins IA; this is not a GWTokenRow migration.

The ONE deliberate copy change this whole phase makes lands here: the coins empty state "No Coins Detected"
becomes "No coins yet" (§4.2 / §6), following the established "No <thing> yet" pattern — recorded explicitly in
the SUMMARY, matching Phase 4's discipline for its own single copy divergence.

Appearance-color access path (04-02/04-04 discipline): appearance-aware text/surface read via `final gw =
Theme.of(context).extension<GWColors>() ?? GWColors.dark();`; mode-invariant tokens stay static. Note coins_screen.dart
is one of the 19 Loading importers — it picks up 05-01's re-skinned Loading automatically; do NOT hand-roll a
different loading treatment here.

Output: a token-correct, appearance-aware `coins_screen.dart` and `coin_card_row.dart`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin coins_screen.dart + coin_card_row.dart (§4.2); verify the 1-minute refresh</name>
  <files>lib/components/coins/view/coins_screen.dart, lib/components/coins/view/coin_card_row.dart</files>
  <read_first>
    - lib/components/coins/view/coins_screen.dart (202 lines): _refreshTimer = Timer.periodic(const Duration(minutes: 1), ...) at :44 (finding 14 — VERIFY unchanged); the loading-state Card(color: deepBlueCardColor, shadowColor: transparent, child: Center(child: Loading())); the empty-state Card(color: deepBlueCardColor) + AutoSizeText("No Coins Detected", color: btnTextDisabled); pull-to-refresh / getCoins wiring
    - lib/components/coins/view/coin_card_row.dart (89 lines): ListTile with buildTokenIcon, AutoSizeText name/balance, the one hardcoded Colors.white value text (~:70), gain/loss color logic (cs.primary / cs.error)
    - lib/theme/gw_colors.dart (appearance-aware textPrimary/surface fields; fail-soft read)
    - lib/components/feedback/gw_empty_state.dart (GWEmptyState — icon / title / message API), lib/theme/genius_wallet_decorations.dart (GWDecorations.surface)
    - lib/utils/image_utils.dart (buildTokenIcon -> Icons.image_not_supported fallback at ~:35-42 — the null-safe path; do NOT add a letter-avatar)
    - 05-UI-SPEC.md §4.2 (the per-element substitution table), §2 finding-30 binding rule (no letter-avatar), §6 (the "No coins yet" recorded copy change), §7 (deepBlueCardColor trap, zero-raw discipline)
  </read_first>
  <action>
    `coins_screen.dart` (§4.2): at the top of the relevant build() add `final gw =
    Theme.of(context).extension<GWColors>() ?? GWColors.dark();` where an appearance-aware surface is needed.
    - Loading state: replace the hardcoded-color `Card(color: deepBlueCardColor, ...)` wrapper with a
      `Container(decoration: GWDecorations.surface(...))` holding the existing `Center(child: Loading())` (Loading
      is already re-skinned by 05-01 — keep the call, only fix the container color trap). GWLoadingState is an
      acceptable alternative wrapper if it maps cleanly.
    - Empty state: replace `Card(color: deepBlueCardColor) + AutoSizeText("No Coins Detected", ...)` with
      `GWEmptyState(icon: Icons.account_balance_wallet_outlined, title: 'No coins yet', message: 'Your holdings
      will appear here once you receive a token.')`. This is the ONE deliberate copy change ("No Coins Detected"
      -> "No coins yet", §6) — record it in the SUMMARY.
    - VERIFY (do NOT change) the refresh timer at :44 stays `Timer.periodic(const Duration(minutes: 1), ...)`
      (finding 14, criterion 4). Re-read the line and confirm before closing.
    `coin_card_row.dart` (§4.2): add the `gw` read; replace the ONE hardcoded `Colors.white` value text (~:70)
    with `gw.textPrimary`. Keep the `ListTile` structure (mechanical translation — NOT a GWTokenRow swap). Leave
    the gain/loss color logic (`cs.primary` / `cs.error`) unchanged — it already flips correctly through the wired
    theme (Phase 4). Preserve every `AutoSizeText` and any overflow guard exactly (criterion 5 — long values).
    Preserve the `buildTokenIcon()` null-safe fallback; do NOT add Alex's letter-avatar path (finding 30 binding
    rule — it reintroduces the empty-symbol crash). Do NOT touch the pull-to-refresh / getCoins wiring. Token
    discipline: zero raw hex/px; no deepBlueCardColor / Colors.white survives.
  </action>
  <verify>
    <automated>flutter analyze lib/components/coins/view/coins_screen.dart lib/components/coins/view/coin_card_row.dart && bash tool/verify_additive_boundary.sh && grep -q 'extension<GWColors>()' lib/components/coins/view/coin_card_row.dart && grep -q 'Duration(minutes: 1)' lib/components/coins/view/coins_screen.dart</automated>
  </verify>
  <done>
    coins_screen loading is a GWDecorations.surface container around the re-skinned Loading; empty state is a
    GWEmptyState reading "No coins yet" (recorded copy change); the refresh Timer is still Duration(minutes: 1)
    (finding 14, grep gate); coin_card_row's value text reads gw.textPrimary and its ListTile / gain-loss logic /
    AutoSizeText guards / buildTokenIcon fallback are unchanged with no letter-avatar added; no deepBlueCardColor
    / Colors.white survives; analyze 0 errors and additive guard passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Holdings walk — criterion 1 + criterion 4 (1-min refresh) + criterion 5 (long values); both modes</name>
  <action>Human verification checkpoint — no code changes. Run the recipe in <how-to-verify> in BOTH modes; record pass/fail per criterion in the SUMMARY.</action>
  <what-built>
    A re-skinned holdings list — GWDecorations.surface loading container, GWEmptyState "No coins yet", CoinCardRow
    value text on gw.textPrimary — with develop's ListTile structure, gain/loss logic, icon fallback, and the
    1-minute market-data refresh timer preserved.
  </what-built>
  <how-to-verify>
    Cold debug run (pinned SDK on PATH; close any Release exe): `CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release
    -DGENIUS_DEPENDENCY_BRANCH=develop -Dc-ares_DIR=..." flutter run -d windows --debug
    --dart-define=GW_DEV_TOOLS=true`.
    1. Criterion 1: confirm the holdings list renders in the redesign skin (rows, gain/loss colors, the loading
       and empty states) and matches the Release exe reference.
    2. Empty state: with a wallet holding no coins, confirm "No coins yet" + the explanatory body render via
       GWEmptyState (not "No Coins Detected").
    3. Criterion 4 (finding 14): confirm market data refreshes on the ~60s cadence, not every 20s. (The value is
       gated in the automated check as Duration(minutes: 1); the walk confirms no visible thrash/429 behaviour.)
    4. Criterion 5 (long values): view a coin with a long balance / small-decimal value and confirm the row does
       NOT RenderFlex-overflow (AutoSizeText holds); confirm a coin with a missing/empty icon shows the
       image_not_supported placeholder, not a crash.
    5. LIVE-FLIP: toggle appearance IN PLACE via the Dev header row and confirm the row value text and the
       loading/empty containers re-skin immediately.
    6. Repeat in BOTH light and dark mode; confirm WCAG AA contrast for the value text, gain/loss colors, and the
       empty-state copy in each mode.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per mode)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CoinGecko poll cadence -> rate limit | The refresh Timer must stay at 1 minute; a regression to 20s risks CoinGecko 429s (finding 14) |
| coin icon source -> row render | An empty/null iconPath must fall through to a fixed placeholder, never a letter-avatar that crashes on an empty symbol (finding 30) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-01 | Denial of Service | market-data refresh interval | high | mitigate | Do NOT change the Timer; verify it stays `Duration(minutes: 1)` (grep gate + walk); a 20s regression risks CoinGecko 429s |
| T-05-03-02 | Denial of Service | empty/null coin icon or long balance overflow | medium | mitigate | Preserve the buildTokenIcon Icons.image_not_supported fallback and every AutoSizeText/overflow guard; do NOT add Alex's letter-avatar (finding 30); confirm no overflow/crash at the walk |
| T-05-03-03 | Information Disclosure | Stale appearance color read (row value text wrong-mode on a live toggle) | medium | mitigate | Read the value text via `Theme.of(context).extension<GWColors>() ?? GWColors.dark()`; verified by the walk live-flip step and the grep gate |
| T-05-03-04 | Tampering | shadow-import allowlist (coins_screen is a Loading importer) | low | mitigate | Run `bash tool/verify_additive_boundary.sh`; keep the canonical Loading import, no repoint |
</threat_model>

<verification>
- `flutter analyze` 0 errors on both files; `bash tool/verify_additive_boundary.sh` passes.
- Refresh Timer confirmed `Duration(minutes: 1)` (grep gate + finding-14 re-read); coin_card_row value text
  reads `Theme.of(context).extension<GWColors>()` (grep gate).
- Task 2 walk (both modes): criterion 1 (holdings skin + empty state copy), criterion 4 (refresh cadence),
  criterion 5 (long value no overflow + icon fallback), live flip, WCAG AA all confirmed.
- SUMMARY records the "No Coins Detected" -> "No coins yet" deliberate copy change.
</verification>

<success_criteria>
The holdings list wears the redesign (surface loading container, GWEmptyState "No coins yet", value text on
gw.textPrimary) and flips LIVE on an in-place toggle; market data still refreshes once a minute (finding 14);
long values and empty icons produce no overflow/crash (criterion 5); develop's ListTile / gain-loss logic /
icon fallback are unchanged; WCAG AA holds in both modes.
</success_criteria>

<output>
Create `.planning/phases/05-dashboard/05-03-SUMMARY.md` when done. Record the "No coins yet" deliberate copy
change explicitly, the finding-14 refresh re-verification, and confirmation no letter-avatar was introduced.
</output>
</content>
