---
context: phase
phase: 05-dashboard
task: 3
total_tasks: 3
status: phase-complete-pending-verification
last_updated: 2026-07-20T18:58:42.636Z
---

# Phase 05 (Dashboard) — COMPLETE, paused for handoff

**All 6 dashboard plans (05-01 … 05-06) are implemented AND human-walked & APPROVED in both
light and dark mode.** The phase is implementation-complete. It has NOT yet been through GSD
verification. Progress: 28/28 plans (100%).

**Next action:** `/gsd-progress` (or directly `/gsd-verify-work 05`) → verify Phase 05 → then
Phase 06 (onboarding).

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **Stage explicit paths only — never `git add -A`/`git commit -a`.** `README.md`
  is dirty (pre-existing, unrelated) and must never be committed. Verify `git diff --cached
  --name-only` before every commit.
- [ ] CONSTRAINT: **Re-derive the platform; do NOT trust stale macOS framing.** This session ran
  entirely on **Windows** (win32, `C:\` paths, `genius_wallet.exe`). Earlier handoffs said macOS.
  Determine the actual machine before relaying any run recipe.
- [ ] CONSTRAINT: **`GWColors.dark()` / `.light()` are NOT fixed palettes.** Both factories copy
  the appearance-aware `GeniusWalletColors.*` getters, so `GWColors.dark().textPrimary` FLIPS with
  mode. For text that must be always-light (over an always-dark photo scrim), use raw
  `Colors.white`/`white70` (§4.4 exception). See memory `gwcolors-dark-not-fixed-palette`.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| `git add -A` in a dirty tree | README.md (and historically skip-worktree signing files) sit uncommitted | blocking | Stage explicit paths; `git diff --cached --name-only` before each commit |
| Trusting `GWColors.dark()` as a fixed light color | It flips with appearance; caused a black-on-black scrim title | blocking | For always-light-on-dark use raw white (§4.4); see memory |
| Claiming an unearned PASS | walks are the gate; automated analyze ≠ visual proof | advisory | Record walk criteria as pending until a human confirms in BOTH modes |
| Diagnosing a black window as a code bug | a 2nd running instance holds the Hive lock → silent black window | advisory | Quit every instance before launching; app was closed at pause |

<current_state>
Phase 05 dashboard is fully re-skinned and walked. All 6 plans + 9 same-day quick tasks
(bgl/cw8/dty/eu9/gzq/ipg/jvr/k81/lyn) are committed on `ui-redesign-port` and approved in both
modes. The app was closed cleanly at pause (no running instance). Working tree: only README.md
dirty (leave it).
</current_state>

<completed_work>
- 05-01 container + Loading; 05-02 hero balance; 05-03 holdings; 05-04 markets; 05-05 news;
  05-06 transactions — all walked & approved.
- Chrome/light-mode: eu9 made the theme's `btnFilter` appearance-aware (fixes ALL bare
  TextButtons in light mode app-wide) + migrated reown/sgnus/network/account hardcoded colors +
  branded Submit-Job/Buy-GNUS as GWButtons.
- Visual polish: gzq (un-clipped surface-card shadows), lyn (softened light-mode card/dialog
  shadows), ipg (shared `GWPageHeader` unifying Markets/News/Swap titles), k81 (transaction badge
  white, row spacing, drawer plain-data + un-clipped close button).
- Dev tooling: bgl (draggable dev bubble), dty (sectioned + branded), cw8 (mock-holdings), jvr
  (mock-transactions).
</completed_work>

<remaining_work>
- Phase 05 verification: `/gsd-verify-work 05` (the phase is impl-complete, not yet verified).
- Then Phase 06 (onboarding), per ROADMAP.
- Open follow-up todos (`.planning/todos/pending/`, not blocking the phase): unify-dashboard-
  section-card-titles (GWSectionHeader), expand-dev-mock-section-more-injectors, design-system-
  light-mode items, cta-hover-over-rounded, second-app-instance-black-window, mobile-nav-ia, etc.
</remaining_work>

## Infrastructure State
- **Branch:** `ui-redesign-port` (off develop), `branching_strategy: none`. GSD atomic commits
  authorized here (memory `gsd-commits-authorized`) despite AGENTS.md's general "do not commit".
- **Run (Windows, this machine):** Flutter at `C:/Users/User/Documents/Projects/GNUS/flutter/flutter/bin`
  (pinned 3.41.9, NOT on global PATH). Recipe:
  `CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release -DGENIUS_DEPENDENCY_BRANCH=develop -Dc-ares_DIR=C:/Users/User/Documents/Projects/GNUS/thirdparty/build/Windows/Release/cares/lib/cmake/c-ares" flutter run -d windows --debug --dart-define=GW_DEV_TOOLS=true`
  (`flutter analyze` is the CI gate; `flutter test` does NOT compile — do not use it).
- **Dev-tools bubble (GW_DEV_TOOLS=true):** draggable bug FAB, top-right below header. Sections:
  **Mock** (Populated / Long / No-icon / Clear holdings + **Mock txns**), **Test flows**,
  **Navigate** (Tokens/Gallery), **Appearance** (in-place light/dark toggle — use it to verify
  live-flip on any screen). This is how empty-wallet screens are populated OFFLINE for walks.
- **Deferred:** `deferred-items.md` notes a pre-existing `verify_additive_boundary.sh` Check-2
  hit on the dev files (`_Section` duplicate) — dev-only, out of scope.

<context>
This session took Phase 05 from "05-02 walk pending" to fully walked, building the dev-tooling
needed to walk offline along the way, and fixing the light-mode chrome at the root (btnFilter).
Everything is committed and approved. The natural resume is verification.
</context>

<next_action>
Start with: `/gsd-progress` — it will report Phase 05 as implementation-complete and route to
verification (`/gsd-verify-work 05`). After verification passes, proceed to Phase 06 (onboarding).
</next_action>
