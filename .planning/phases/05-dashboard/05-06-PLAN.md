---
phase: 05-dashboard
plan: 06
type: execute
wave: 6
depends_on: [05-05]
files_modified:
  - lib/dashboard/home/widgets/transaction_displays.dart
  - lib/dashboard/home/widgets/transactions_slim_view.dart
  - lib/dashboard/transactions/transactions_screen.dart
autonomous: false
requirements: [SCR-01, GAP-06]
must_haves:
  truths:
    - "The transaction rows and detail drawers wear the redesign: each item's outer Card becomes a GWDecorations.surface Container (GAP-06), the _buildRow label reads GeniusWalletColors.textPrimary70, the received-arrow badge is brandGreen with a textOnBrand icon, swap/purchase failures use statusError, amount/heading text reads gw.textPrimary and secondary text gw.textSecondary, and the 'View on Explorer' button is a GWButton — all via the UI-SPEC §3.3 token table (canonical transaction_displays.dart, GAP-06, edited in place; NOT split to Alex's four-file layout)"
    - "Overflow-safety survives the re-skin: every Flexible + overflow:TextOverflow.ellipsis wrap on the detail rows (findings 32/33) and every errorBuilder null-safety guard on _buildCoinIconWithBadge's Image.asset are preserved unchanged (criterion 5)"
    - "The transaction count footer is present and re-skinned: 'Transactions: ${txs.length}' stays verbatim on GeniusWalletTypography.labelMd + gw.textSecondary (finding 31, criterion 5)"
    - "The filter selector keeps develop's SegmentedButton<Filters> (NOT Alex's TransactionFilters chip widget), re-skinned so the selected segment uses brandPrimary; and TransactionsScreen still has a SINGLE @override on build() (finding 34) and its pull-to-refresh reloads (RefreshIndicator -> getCoins(), finding 10, criterion 2)"
    - "develop's behaviour and copy are preserved: the transaction detail labels (Date/Status/To/From/Network/Network Fee/Hash) and 'View on Explorer' are unchanged; the sent-arrow badge keeps its deliberate lightBlueAccent accent (Alex kept it too); appearance-aware colors read via Theme.of(context).extension<GWColors>() so open detail drawers flip LIVE on an in-place toggle"
  artifacts:
    - "lib/dashboard/home/widgets/transaction_displays.dart — item Card -> GWDecorations.surface Container, textPrimary70 labels, brandGreen/textOnBrand received badge, statusError failures, GWButton explorer, bodyMd/bodySm row typography; every Flexible/ellipsis + errorBuilder guard preserved (GAP-06)"
    - "lib/dashboard/home/widgets/transactions_slim_view.dart — SegmentedButton selected segment re-skinned to brandPrimary, count footer on labelMd + gw.textSecondary, optional GWEmptyState for the empty-filtered list"
    - "lib/dashboard/transactions/transactions_screen.dart — verify-only: single @override (finding 34) + RefreshIndicator->getCoins (finding 10) re-confirmed; re-skin only its children"
  key_links:
    - "transaction_displays.dart build()/_buildRow -> Theme.of(context).extension<GWColors>() ?? GWColors.dark() -> amount/secondary text + card surface flip live in an OPEN detail drawer; mode-invariant brandGreen/textOnBrand/statusError/lightBlueAccent stay static"
    - "TransactionsScreen RefreshIndicator(onRefresh: getCoins) (:16-19) -> reload (finding 10, criterion 2) — unchanged; single @override (:12-13, finding 34) — unchanged"
    - "TransactionsSlimView count footer 'Transactions: ${txs.length}' (:115-125) -> labelMd + gw.textSecondary (finding 31, criterion 5)"
---

<objective>
Re-skin develop's transactions area (UI-SPEC §4.5 / §3.3) — the shared transaction_displays.dart item widgets
and detail drawers (GAP-06), the TransactionsSlimView (filter + count footer), and verify TransactionsScreen —
in place. This is the last dashboard area and it owns most of criterion 5.

Purpose: criterion 1 (transactions render in the redesign skin), criterion 2's transactions clause (finding 10
pull-to-refresh reloads), and criterion 5 (long-value/filtered-list overflow-safety + the count footer + the
single @override — findings 31/32/33/34, plus finding 30's no-crash preserved by the icon guards). Verify, bind,
re-skin (§2): findings 10/31/32/33/34 are ALREADY correct on develop; this plan re-skins their visuals without
disturbing the logic and re-reads their line ranges to prove it.

Re-skin, never restructure (§1 / GAP-06): keep develop's SINGLE-FILE transaction_displays.dart — do NOT split it
to match Alex's four-file layout (the deferred structural question 03-GAP-INVENTORY.md recorded, answered by
GAP-06's own "re-skinned in place; structure unchanged"). Keep develop's SegmentedButton — do NOT adopt Alex's
separate TransactionFilters chip widget. Use Alex's four transaction files + his TransactionsSlimView as
READ-ONLY token references only.

Appearance-color access path (04-02/04-04 discipline): appearance-aware amount/secondary text + card surface
read via `final gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();` (so an OPEN detail drawer flips
live); mode-invariant brandGreen / textOnBrand / statusError / lightBlueAccent / brandPrimary stay on the
`GeniusWalletColors` static getters.

Output: token-correct, appearance-aware `transaction_displays.dart` + `transactions_slim_view.dart`, and a
re-confirmed `transactions_screen.dart`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin transaction_displays.dart item widgets + detail drawers (§3.3, GAP-06)</name>
  <files>lib/dashboard/home/widgets/transaction_displays.dart</files>
  <read_first>
    - lib/dashboard/home/widgets/transaction_displays.dart (565 lines): TransactionItem, TransactionPurchasedItem, TransactionSwappedItem, TransactionEscrowReleaseItem; shared _buildRow (label Colors.white70; value Text wrapped in Flexible + overflow:TextOverflow.ellipsis — findings 32/33), _buildDetailsCard (Card color deepBlueMenu), _buildCoinIconWithBadge (Image.asset with errorBuilder null-safety guard); sent-arrow badge Colors.lightBlueAccent; received-arrow badge Colors.greenAccent; badge icon Colors.black; swap/purchase failure Colors.redAccent; amount/heading Colors.white; fee/secondary Colors.white60/cs.onSurfaceVariant; "View on Explorer" ElevatedButton.icon with Colors.lightBlueAccent
    - lib/theme/gw_colors.dart (appearance-aware textPrimary/textSecondary/surface fields; fail-soft read)
    - lib/theme/genius_wallet_decorations.dart (GWDecorations.surface), lib/theme/genius_wallet_consts.dart (radiusMd), lib/components/cards/gw_card.dart (GWCard — if its default padding fits), lib/components/buttons/gw_button.dart (GWButton secondary variant)
    - lib/theme/genius_wallet_colors.dart (textPrimary70, brandGreen, textOnBrand, statusError, lightBlueAccent — which are static vs appearance-aware), lib/theme/genius_wallet_typography.dart (bodyMd, bodySm)
    - C:\Users\User\Documents\Projects\GNUS-compare\GeniusWallet-3514\lib\dashboard\transactions\transaction_item.dart (Alex's token choices — READ-ONLY reference: label textPrimary70 :119, received badge/icon :29/:103, etc.)
    - 05-UI-SPEC.md §3.3 (the per-element substitution table + the binding rule to preserve every Flexible/ellipsis + errorBuilder guard), §2 findings 32/33, §6 (detail labels + "View on Explorer" unchanged), §7 (deepBlueMenu / Colors.white trap)
  </read_first>
  <action>
    Edit the CANONICAL single-file transaction_displays.dart IN PLACE (GAP-06 — do NOT split to Alex's four
    files). Add `final gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();` at the top of each build()
    / shared helper that reads an appearance-aware color (thread `context` into the shared _buildRow /
    _buildDetailsCard helpers so they can resolve `gw`). Follow §3.3's table:
    - Card fill (item outer Card + _buildDetailsCard, currently deepBlueMenu / cs.surfaceContainerHigh) ->
      `GWDecorations.surface(radius: GeniusWalletConsts.radiusMd)` on a `Container` (drop `Card`), or `GWCard` if
      its default padding fits (appearance-aware surface).
    - _buildRow label (Colors.white70) -> `GeniusWalletColors.textPrimary70` (Alex's exact choice).
    - Sent-arrow badge bg (Colors.lightBlueAccent) -> KEEP as `Colors.lightBlueAccent` (Alex kept this exact
      deliberate accent too — it is not a token gap).
    - Received-arrow badge bg (Colors.greenAccent) -> `GeniusWalletColors.brandGreen`.
    - Badge icon color (Colors.black) -> `GeniusWalletColors.textOnBrand` (same WCAG rationale as Phase 4 §1.3).
    - Swap/purchase failure red (Colors.redAccent) -> `GeniusWalletColors.statusError`.
    - Amount/heading text (Colors.white) -> `gw.textPrimary` (context-resolved, live re-skin).
    - Fee/secondary text (Colors.white60 / cs.onSurfaceVariant) -> `gw.textSecondary` uniformly.
    - Row typography (_buildRow label/value) -> `GeniusWalletTypography.bodyMd`/`bodySm`.
    - "View on Explorer" ElevatedButton.icon -> `GWButton` secondary variant, SAME label/icon/handler.
    brandGreen / textOnBrand / statusError / lightBlueAccent are MODE-INVARIANT — keep them on the static getters,
    do NOT route through GWColors. PRESERVE every `Flexible` + `overflow: TextOverflow.ellipsis` wrap (findings
    32/33) and every `errorBuilder: (_, _, _) => const SizedBox.shrink()` null-safety guard on
    _buildCoinIconWithBadge's Image.asset — re-read those sites and confirm unchanged. Preserve develop's detail
    labels (Date/Status/To/From/Network/Network Fee/Hash) and "View on Explorer" copy verbatim (§6). Token
    discipline: zero raw hex/px; no deepBlueMenu / Colors.white / Colors.white60 / Colors.white70 / Colors.redAccent
    / Colors.greenAccent / Colors.black survives (the sent-arrow Colors.lightBlueAccent is the sole intentional
    exception).
  </action>
  <verify>
    <automated>flutter analyze lib/dashboard/home/widgets/transaction_displays.dart && bash tool/verify_additive_boundary.sh && grep -q 'extension<GWColors>()' lib/dashboard/home/widgets/transaction_displays.dart</automated>
  </verify>
  <done>
    Item cards + _buildDetailsCard use GWDecorations.surface/GWCard; _buildRow label = textPrimary70; received
    badge = brandGreen with a textOnBrand icon; failures = statusError; amount = gw.textPrimary; secondary =
    gw.textSecondary; row typography bodyMd/bodySm; explorer button = GWButton; the sent-arrow lightBlueAccent is
    intentionally kept; every Flexible/ellipsis + errorBuilder guard is unchanged; the file is NOT split; detail
    labels + explorer copy verbatim; no banned raw color survives; analyze 0 errors and additive guard passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin transactions_slim_view.dart (filter + count footer + optional empty state); verify transactions_screen.dart</name>
  <files>lib/dashboard/home/widgets/transactions_slim_view.dart, lib/dashboard/transactions/transactions_screen.dart</files>
  <read_first>
    - lib/dashboard/home/widgets/transactions_slim_view.dart (130 lines): "Transactions" heading via textTheme.headlineLarge (token-correct — leave); SegmentedButton<Filters> (Material-native) filter selector; row items (see Task 1); count footer Align(centerRight, AutoSizeText("Transactions: ${txs.length}", TextStyle(color: cs.onSurfaceVariant))) at :115-125 (finding 31); the empty-after-filter case currently renders a bare blank ListView above "Transactions: 0"
    - lib/dashboard/transactions/transactions_screen.dart (40 lines): single @override before Widget build() at :12-13 (finding 34 — VERIFY unchanged); RefreshIndicator(onRefresh: () async { context.read<WalletDetailsCubit>().getCoins(); }) at :16-19 (finding 10 — VERIFY unchanged)
    - lib/theme/gw_colors.dart (appearance-aware textSecondary; fail-soft read), lib/theme/genius_wallet_typography.dart (labelMd), lib/theme/genius_wallet_colors.dart (brandPrimary — mode-invariant), lib/components/feedback/gw_empty_state.dart (GWEmptyState)
    - 05-UI-SPEC.md §4.5 (SegmentedButton kept + re-skinned; count footer; optional empty-filtered state), §2 findings 10/31/34, §6 (count footer string + optional empty copy)
  </read_first>
  <action>
    `transactions_slim_view.dart` (§4.5): add the `gw` read where needed.
    - Heading: leave "Transactions" via textTheme.headlineLarge (already token-correct).
    - Filter selector: KEEP `SegmentedButton<Filters>` (do NOT adopt Alex's separate TransactionFilters chip
      widget). Apply a local `SegmentedButton.styleFrom(...)` (or `segmentedButtonTheme` if preferred) so the
      SELECTED segment uses `GeniusWalletColors.brandPrimary` (mode-invariant static accent) — matching every
      other selected-state token this phase.
    - Count footer: keep the string `"Transactions: ${txs.length}"` VERBATIM (§6); re-skin its style to
      `GeniusWalletTypography.labelMd.copyWith(color: gw.textSecondary)` (was cs.onSurfaceVariant). Keep its
      Align(centerRight) position (finding 31).
    - Empty-filtered list (RECOMMENDED, not mandated — §4.5): when `filteredTransactions.isEmpty`, render
      `GWEmptyState(icon: Icons.receipt_long_outlined, title: 'No transactions yet', message: 'Your sends,
      receives and swaps will appear here.')` in place of the bare blank ListView — same list, same position, same
      behaviour when non-empty. This is additive polish (matches Alex's own TransactionsSlimView), not a
      structural change. If skipped, criterion 5 is still met (the footer alone communicates the zero count) —
      record the decision either way in the SUMMARY.
    `transactions_screen.dart` (VERIFY-ONLY, §4.5): re-read :12-13 and confirm a SINGLE `@override` before
    `Widget build(BuildContext context)` (finding 34 — do NOT add a duplicate), and re-read :16-19 and confirm the
    `RefreshIndicator(onRefresh: ... getCoins())` wrap (finding 10) is unchanged. Re-skin ONLY its children (via
    Tasks 1-2) — the screen itself has no raw values to change beyond re-confirming these two findings. Token
    discipline: zero raw hex/px.
  </action>
  <verify>
    <automated>flutter analyze lib/dashboard/home/widgets/transactions_slim_view.dart lib/dashboard/transactions/transactions_screen.dart && bash tool/verify_additive_boundary.sh && grep -q 'Transactions: ' lib/dashboard/home/widgets/transactions_slim_view.dart</automated>
  </verify>
  <done>
    SegmentedButton kept with the selected segment re-skinned to brandPrimary (no TransactionFilters chip added);
    the count footer string is verbatim on labelMd + gw.textSecondary in its Align(centerRight) position (finding
    31); the empty-filtered decision (GWEmptyState added or footer-only) is recorded; transactions_screen.dart's
    single @override (finding 34) and RefreshIndicator->getCoins (finding 10) are re-confirmed unchanged; analyze
    0 errors and additive guard passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Transactions walk — criterion 1 + criterion 2 (pull-to-refresh) + criterion 5 (overflow/footer/filter); both modes</name>
  <action>Human verification checkpoint — no code changes. Run the recipe in <how-to-verify> in BOTH modes; record pass/fail per criterion in the SUMMARY.</action>
  <what-built>
    Re-skinned transaction rows + detail drawers (GWDecorations.surface cards, textPrimary70 labels, brandGreen
    received badge, statusError failures, GWButton explorer), a re-skinned TransactionsSlimView (brandPrimary
    selected filter, labelMd count footer, optional empty-filtered GWEmptyState), and a re-confirmed
    TransactionsScreen — with develop's overflow guards, count footer, @override, and pull-to-refresh intact.
  </what-built>
  <how-to-verify>
    Cold debug run (pinned SDK on PATH; close any Release exe): `CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release
    -DGENIUS_DEPENDENCY_BRANCH=develop -Dc-ares_DIR=..." flutter run -d windows --debug
    --dart-define=GW_DEV_TOOLS=true`. (Tip: with GW_DEV_TOOLS=true the onboarding Mock button injects a fake
    wallet + 20 fake transactions — use it to populate the list.)
    1. Criterion 1: open the transactions list and a transaction's detail drawer (send, receive, swap, purchase);
       confirm the rows and detail cards match the Release exe reference — GWDecorations.surface cards,
       textPrimary70 labels, brandGreen received badge with a legible icon, statusError failures, a GWButton
       "View on Explorer".
    2. Criterion 2 (finding 10): pull-to-refresh the transactions list and confirm it reloads (getCoins()).
    3. Criterion 5:
       - Count footer (finding 31): confirm "Transactions: N" is present beneath the list.
       - Overflow (findings 32/33): open a detail drawer for a transaction with a LONG value (address/hash/amount)
         and confirm NO RenderFlex overflow — the Flexible/ellipsis guards hold.
       - Filtered list: apply a filter that yields zero results and confirm no overflow/crash — either the
         GWEmptyState renders (if added) or the footer reads "Transactions: 0" over a clean blank list.
       - @override (finding 34): (covered by the automated single-@override re-read; no runtime action).
    4. Filter: confirm the SegmentedButton's selected segment reads as brandPrimary and switching filters works.
    5. LIVE-FLIP: with a detail drawer OPEN, toggle appearance IN PLACE via the Dev header row and confirm the
       card surface, amount/secondary text, and labels re-skin immediately (open-drawer stale = a static-getter
       regression, BLOCKS close).
    6. Repeat in BOTH light and dark mode; confirm WCAG AA contrast for the row/detail text, the received badge
       icon on brandGreen, the statusError failures, and the count footer in each mode.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per mode)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| long tx value (address/hash/amount) -> detail row render | A long value must ellipsis-truncate, never RenderFlex-overflow (findings 32/33) |
| tx fetch -> refresh surface | Pull-to-refresh must reload, not no-op (finding 10) |
| empty-after-filter list -> render | A zero-result filter must render cleanly (empty state or footer), never crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06-01 | Denial of Service | Flexible/ellipsis overflow guards + _buildCoinIconWithBadge errorBuilder | high | mitigate | Preserve every Flexible/overflow:ellipsis wrap (findings 32/33) and every errorBuilder guard during the token swap; confirm no overflow at the walk with long values |
| T-05-06-02 | Denial of Service | TransactionsScreen pull-to-refresh + single @override | medium | mitigate | Do NOT touch :16-19 (getCoins) or add a duplicate @override at :12-13 (finding 34); re-read + re-confirm; verify reload at the walk |
| T-05-06-03 | Information Disclosure | Stale appearance color read (open detail drawer wrong-mode on a live toggle) | medium | mitigate | Read amount/secondary text + card surface via `Theme.of(context).extension<GWColors>() ?? GWColors.dark()`; verified by the walk open-drawer live-flip step and the grep gate |
| T-05-06-04 | Tampering | GAP-06 single-file structure (do not split to Alex's 4 files) | low | accept | Edit transaction_displays.dart in place; keep SegmentedButton (not Alex's chip widget); the deferred split is a recorded product question, not this plan's to answer |
</threat_model>

<verification>
- `flutter analyze` 0 errors on all three files; `bash tool/verify_additive_boundary.sh` passes.
- transaction_displays.dart amount/secondary text reads `Theme.of(context).extension<GWColors>()` (grep gate);
  mode-invariant brandGreen/textOnBrand/statusError/lightBlueAccent/brandPrimary stay static; the count footer
  string is present (grep gate).
- Findings 10/31/32/33/34 re-read and confirmed unchanged.
- Task 3 walk (both modes): criterion 1 (tx skin + detail drawers), criterion 2 (pull-to-refresh reload),
  criterion 5 (count footer + long-value no overflow + filtered list + single @override), filter re-skin,
  open-drawer live flip, WCAG AA all confirmed; detail labels + explorer copy verbatim.
</verification>

<success_criteria>
The transaction rows, detail drawers, filter, and count footer wear the redesign and flip LIVE on an in-place
toggle (open detail drawer included); every overflow/icon guard is preserved (findings 32/33, criterion 5); the
count footer is present (finding 31); pull-to-refresh reloads (finding 10) and the single @override holds
(finding 34); transaction_displays.dart is edited in place (not split) and SegmentedButton is kept; WCAG AA
holds in both modes.
</success_criteria>

<output>
Create `.planning/phases/05-dashboard/05-06-SUMMARY.md` when done. Record the empty-filtered-list decision
(GWEmptyState added or footer-only), confirmation transaction_displays.dart was kept single-file (GAP-06, not
split), and the re-read confirmation that findings 10/31/32/33/34 stayed correct.
</output>
</content>
