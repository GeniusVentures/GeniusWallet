---
phase: 05-dashboard
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - lib/dashboard/chart/markets_screen.dart
  - lib/dashboard/chart/markets_search_bar.dart
  - lib/dashboard/chart/dashboard_markets.dart
autonomous: false
requirements: [SCR-01]
must_haves:
  truths:
    - "The markets surface wears the redesign: the 'Markets' heading is GeniusWalletTypography.headlineLg on gw.textPrimary, the search field is a GWTextField ('Search Coins...', search prefix), each spark-line grid card is a GWDecorations.surface Container (not a plain Material Card), and appearance-aware text/surface reads go through Theme.of(context).extension<GWColors>() so they flip LIVE on an in-place toggle (UI-SPEC §4.3)"
    - "The markets error state gives the user a working retry, never an endless spinner: FutureStateWidget's error slot renders GWErrorState(onRetry: _retryCoins) (or token-styled error text) and the ALREADY-CORRECT _retryCoins/_retryMarketData wiring (finding 8, :97/:120) is untouched (criterion 3)"
    - "develop's markets structure and behaviour are preserved: the header + search-drawer + FutureStateWidget-gated grid IA is unchanged, the cached _coinsFuture/_marketDataFuture in _MarketsScreenState is untouched, and the search suffix spinner (re-skinned Loading) is kept (UI-SPEC §4.3 / §1)"
    - "No overflow or crash on empty symbols: the missing-data placeholder is a statusError-tinted Container (a debug aid, copy unchanged), and no letter-avatar path is introduced — an empty/null symbol falls through develop's existing null-safe icon path (finding 30, criterion 5)"
    - "The market search hint copy 'Search Coins...' and the develop error/empty strings ('Failed to load market coins' / 'No market data available') are unchanged (UI-SPEC §6)"
  artifacts:
    - "lib/dashboard/chart/markets_screen.dart — headlineLg heading, GWDecorations.surface grid cards, GWErrorState retry in the error slot, statusError missing-data placeholder; cached futures + retry wiring untouched"
    - "lib/dashboard/chart/markets_search_bar.dart — raw TextField replaced by GWTextField ('Search Coins...', search prefix); the re-skinned Loading suffix spinner kept"
    - "lib/dashboard/chart/dashboard_markets.dart — dashboard-embedded markets list heading aligned to a typography token, hairline divider re-skinned to borderSubtle"
  key_links:
    - "markets_screen.dart / dashboard_markets.dart build() -> Theme.of(context).extension<GWColors>() ?? GWColors.dark() -> heading/surface/divider flip live; mode-invariant statusError/brandPrimary stay static"
    - "FutureStateWidget error slot -> GWErrorState(onRetry: _retryCoins) -> the finding-8 retry wiring (:97/:120) surfaces a real button, never an endless spinner (criterion 3)"
    - "GWTextField focus border -> brandPrimary accent (§7 reserved accent) -> the markets search field"
---

<objective>
Re-skin develop's markets surface (UI-SPEC §4.3) — the standalone MarketsScreen, its MarketSearchBar, and the
dashboard-embedded DashboardMarkets — in place. develop's markets and Alex's share the same structure (header +
search-drawer + FutureStateWidget-gated spark-line grid), so this is an unambiguous visual reference, not a
restructure risk.

Purpose: criterion 1 (markets render in the redesign skin) and criterion 3's markets clause (finding 8 — a
working retry, never an endless spinner). Verify, bind, re-skin (§2): finding 8's cached-futures + retry wiring
is ALREADY correct; this plan re-skins its chrome without touching the plumbing, and re-reads :97/:120 to prove
it. Criterion 5's empty-symbol clause is satisfied by develop's existing null-safe icon path — do NOT add Alex's
letter-avatar (finding 30 binding rule).

Appearance-color access path (04-02/04-04 discipline): appearance-aware heading/surface/divider read via `final
gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();`; mode-invariant statusError / brandPrimary stay
on the `GeniusWalletColors` static getters. markets_search_bar.dart is a Loading importer — its suffix spinner is
re-skinned automatically by 05-01; keep the call.

Output: token-correct, appearance-aware `markets_screen.dart`, `markets_search_bar.dart`, `dashboard_markets.dart`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin markets_screen.dart + markets_search_bar.dart + dashboard_markets.dart (§4.3)</name>
  <files>lib/dashboard/chart/markets_screen.dart, lib/dashboard/chart/markets_search_bar.dart, lib/dashboard/chart/dashboard_markets.dart</files>
  <read_first>
    - lib/dashboard/chart/markets_screen.dart (203 lines): _MarketsScreenState caches _coinsFuture/_marketDataFuture; _retryCoins/_retryMarketData wired to FutureStateWidget.onRetry at :97 and :120 (finding 8 — do NOT touch); "Markets" heading raw TextStyle(fontSize: 32, w500, Colors.white); grid Card(clipBehavior: hardEdge, child: CryptoSparkLineChart(...)); missing-data placeholder Container(color: Colors.red, child: Text(..., white)); FutureStateWidget error text Text(..., Colors.white)
    - lib/dashboard/chart/markets_search_bar.dart (185 lines): raw TextField — Colors.white text, Colors.grey[900] fill, Colors.white24/38 borders, lightGreenPrimary focus border; the Loading() suffix spinner in a Padding
    - lib/dashboard/chart/dashboard_markets.dart (97 lines): AutoSizeText(title, TextStyle(fontSize: GeniusWalletFontSize.sectionHeader, textPrimary)); Container(height: 2, color: deepBlueTertiary) divider
    - lib/theme/gw_colors.dart (appearance-aware textPrimary/textSecondary/surface/border fields; fail-soft read)
    - lib/components/inputs/gw_text_field.dart (GWTextField — hint / prefix API), lib/components/feedback/gw_error_state.dart (GWErrorState — onRetry API), lib/theme/genius_wallet_decorations.dart (GWDecorations.surface)
    - lib/theme/genius_wallet_typography.dart (headlineLg, titleLg, bodyMd), lib/theme/genius_wallet_consts.dart (radiusMd), lib/theme/genius_wallet_colors.dart (statusError, brandPrimary, borderSubtle)
    - 05-UI-SPEC.md §4.3 (the per-element substitution table), §2 finding-8 (retry wiring already correct), finding-30 (no letter-avatar), §6 (copy unchanged), §7 (zero-raw / deepBlue* / Colors.white / Colors.grey[N] trap + accent reservation)
  </read_first>
  <action>
    Add `final gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();` at the top of each build() that
    needs an appearance-aware color. Follow §4.3's table:
    `markets_screen.dart`:
    - "Markets" heading: raw `TextStyle(fontSize: 32, w500, Colors.white)` -> `GeniusWalletTypography.headlineLg
      .copyWith(color: gw.textPrimary)`.
    - Search icon button: keep the plain `IconButton` as-is (icon-only affordance is fine).
    - Grid card: `Card(clipBehavior: hardEdge, child: CryptoSparkLineChart(...))` ->
      `Container(decoration: GWDecorations.surface(radius: GeniusWalletConsts.radiusMd), child:
      CryptoSparkLineChart(...))` (preserve clip behaviour if the chart needs it via the Container's clipBehavior).
    - FutureStateWidget error slot: pass `GWErrorState(onRetry: _retryCoins)` (and _retryMarketData for the market
      grid) as the `error:` widget so the ALREADY-WIRED retry surfaces a real GWButton — or, if GWErrorState does
      not fit, token the error text to `GeniusWalletTypography.bodyMd.copyWith(color: gw.textSecondary)`. Do NOT
      touch the onRetry wiring itself (:97/:120, finding 8) — re-read those lines and confirm unchanged.
    - Missing-data placeholder: `Container(color: Colors.red, ...)` -> `Container(color:
      GeniusWalletColors.statusError.withAlpha(100), ...)` (a debug aid — copy unchanged, statusError is static).
    `markets_search_bar.dart`:
    - Replace the raw `TextField` with `GWTextField(hint: 'Search Coins...', prefix: Icon(Icons.search))` — a
      direct mechanical swap (GWTextField already carries the surface fill + brandPrimary 2px focus border). Keep
      the search hint copy verbatim (§6). Keep the `Loading()` suffix spinner (re-skinned by 05-01).
    `dashboard_markets.dart`:
    - Keep `GeniusWalletFontSize.sectionHeader` if it is used consistently elsewhere, else align the title to
      `GeniusWalletTypography.titleLg` on `gw.textPrimary`; the `Container(height: 2, color: deepBlueTertiary)`
      divider -> `Container(height: 1, color: gw.borderSubtle)` (hairline weight, matching §4.2's precedent).
    Do NOT add a letter-avatar for empty symbols (finding 30). Keep develop's cached futures and IA. Token
    discipline: zero raw hex/px; no Colors.white / Colors.grey[N] / deepBlueTertiary / lightGreenPrimary survives.
  </action>
  <verify>
    <automated>flutter analyze lib/dashboard/chart/markets_screen.dart lib/dashboard/chart/markets_search_bar.dart lib/dashboard/chart/dashboard_markets.dart && bash tool/verify_additive_boundary.sh && grep -q 'extension<GWColors>()' lib/dashboard/chart/markets_screen.dart</automated>
  </verify>
  <done>
    Markets heading is headlineLg on gw.textPrimary; the search field is a GWTextField ('Search Coins...',
    search prefix); grid cards are GWDecorations.surface Containers; the error slot uses GWErrorState(onRetry:...)
    (or token-styled error text) with the finding-8 _retryCoins/_retryMarketData wiring (:97/:120) unchanged; the
    missing-data placeholder is statusError-tinted; dashboard_markets divider is a borderSubtle hairline; no
    Colors.white/grey[N]/deepBlueTertiary/lightGreenPrimary survives; no letter-avatar added; copy verbatim;
    analyze 0 errors and additive guard passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Markets walk — criterion 1 + criterion 3 (retry) + criterion 5 (empty symbol); both modes</name>
  <action>Human verification checkpoint — no code changes. Run the recipe in <how-to-verify> in BOTH modes; record pass/fail per criterion in the SUMMARY.</action>
  <what-built>
    A re-skinned markets surface — headlineLg heading, GWTextField search, GWDecorations.surface spark-line grid
    cards, GWErrorState retry, statusError missing-data placeholder — with develop's cached futures, retry wiring,
    and IA intact, plus the re-skinned Loading suffix spinner.
  </what-built>
  <how-to-verify>
    Cold debug run (pinned SDK on PATH; close any Release exe): `CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release
    -DGENIUS_DEPENDENCY_BRANCH=develop -Dc-ares_DIR=..." flutter run -d windows --debug
    --dart-define=GW_DEV_TOOLS=true`.
    1. Criterion 1: open the markets surface (both the dashboard-embedded DashboardMarkets and the standalone
       MarketsScreen); confirm the heading, the spark-line grid cards, and the search field match the Release
       exe reference.
    2. Search: open the search field, confirm it reads "Search Coins..." and its focus border is the brand accent;
       type a query and confirm the suffix shows the re-skinned Loading spinner while resolving.
    3. Criterion 3 (finding 8): force a market-data fetch failure (e.g. offline) and confirm an error state with
       a WORKING retry button appears — never an endless spinner; press retry and confirm it re-issues the fetch.
    4. Criterion 5 (empty symbol): confirm a coin with an empty/missing symbol renders without a RenderFlex
       overflow or crash (develop's null-safe icon path; NO letter-avatar).
    5. LIVE-FLIP: toggle appearance IN PLACE via the Dev header row and confirm the heading, grid card surfaces,
       search field, and divider re-skin immediately.
    6. Repeat in BOTH light and dark mode; confirm WCAG AA contrast for the heading, card content, search text,
       and error state in each mode.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per mode)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Market-data fetch -> error/retry surface | A failed fetch must surface a working retry, not a stale/endless spinner (finding 8) |
| coin symbol -> grid card render | An empty/null symbol must not crash the card (finding 30) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04-01 | Denial of Service | FutureStateWidget retry wiring (_retryCoins/_retryMarketData) | high | mitigate | Do NOT touch the onRetry wiring at :97/:120; only re-skin the error slot chrome; confirm a working retry (never an endless spinner) at the walk |
| T-05-04-02 | Denial of Service | empty/null coin symbol on a grid card | medium | mitigate | Preserve develop's null-safe icon path; do NOT add Alex's letter-avatar (finding 30); confirm no overflow/crash at the walk |
| T-05-04-03 | Information Disclosure | Stale appearance color read (heading/card/search wrong-mode on a live toggle) | medium | mitigate | Read appearance-aware colors via `Theme.of(context).extension<GWColors>() ?? GWColors.dark()`; verified by the walk live-flip step and the grep gate |
| T-05-04-04 | Tampering | shadow-import allowlist (markets_search_bar is a Loading importer) | low | mitigate | Run `bash tool/verify_additive_boundary.sh`; keep the canonical Loading import, no repoint |
</threat_model>

<verification>
- `flutter analyze` 0 errors on all three files; `bash tool/verify_additive_boundary.sh` passes.
- markets_screen appearance-aware reads via `Theme.of(context).extension<GWColors>()` (grep gate); the finding-8
  retry wiring re-read and confirmed unchanged.
- Task 2 walk (both modes): criterion 1 (markets skin), criterion 3 (working retry), criterion 5 (empty symbol
  no crash), live flip, WCAG AA all confirmed; search hint and error copy verbatim.
</verification>

<success_criteria>
The markets surface wears the redesign (headlineLg heading, GWTextField search, GWDecorations.surface grid
cards, GWErrorState retry) and flips LIVE on an in-place toggle; a failed fetch surfaces a working retry never
an endless spinner (finding 8); an empty symbol produces no overflow/crash (finding 30); develop's cached
futures / retry wiring / IA are unchanged; WCAG AA holds in both modes.
</success_criteria>

<output>
Create `.planning/phases/05-dashboard/05-04-SUMMARY.md` when done. Record the GWErrorState-vs-token-text choice
for the error slot, the finding-8 retry re-verification, and confirmation no letter-avatar was introduced.
</output>
</content>
