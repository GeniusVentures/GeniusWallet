---
phase: 20-feedback-page-frame-153-b-focused-frame-left-title-receipt-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/logs/submit_logs_screen.dart
  - test/logs/submit_logs_page_frame_test.dart
  - .planning/sketches/MANIFEST.md
  - .planning/sketches/153-feedback-page/README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "At a window wider than the cap, the page content stops at GeniusBreakpoints.large (1024) - not xxl (D-01)."
    - "The page title 'Send Feedback' starts at the same X as the composer card's left edge, left-aligned in the frame's Column(stretch) (D-02)."
    - "At a frame content width of 1024 the composer card (640) and the receipt rail card sit side by side with level tops (D-03)."
    - "Below the two-column width the rail card sits under the composer card, both at the same left edge, and nothing overflows (D-03)."
    - "The rail shows at most two log rows plus SDK and Platform, and says why there are no log rows when the SDK is stopped (D-04)."
    - "The chip strip is gone from inside the composer - the same facts appear exactly once, in the rail (D-05)."
    - "The send action sits below the status line, never beside a multi-line status block (D-06)."
    - "All five states still render (Ready / Sending / Success / No-SDK / Failed) and the two Failed messages stay distinct."
  artifacts:
    - "lib/logs/submit_logs_screen.dart (modified - the only source file this phase touches)"
    - "test/logs/submit_logs_page_frame_test.dart (new - the phase's one runnable check)"
  key_links:
    - "LayoutBuilder sits INSIDE the ConstrainedBox, so constraints.maxWidth is the frame's content width and not the window width."
    - "_buildRail is mounted in BOTH LayoutBuilder branches - a rail mounted only in the wide branch disappears on a narrow window."
    - "GWPageHeader is a direct child of the frame's Column(stretch) with nothing between them - no Center, no second ConstrainedBox."
    - "GWPageHeader keeps its `centered` parameter and every other caller keeps passing it; only this one call site stops."
    - "_tailBadge survives the chip-strip deletion because the rail reuses it."
---

<objective>
Phase 19 built the Feedback **card**. This plan builds the **page** around it, per sketch 153 variant
**B - Focused frame**: narrow the page frame to `GeniusBreakpoints.large`, put the title back on the
frame's left edge like every other tab, and fill the width beside the composer with a receipt rail
made only of facts `_probes` already computes.

Purpose: today a 560px column floats inside a 1536px frame with roughly 430px of dead page on each
side, and the title was pushed inside that column to hide the mismatch. 153-B fixes the frame instead
of the content. No new feature, no new data, no new shared component.

Output: one modified screen (`lib/logs/submit_logs_screen.dart`), one new widget test, and two
one-line planning-doc updates.

**Decision IDs.** `20-CONTEXT.md` numbers its locked decisions 1-7 without a prefix. This plan cites
them as **D-01 .. D-07**, in that same order:
D-01 frame cap -> `GeniusBreakpoints.large`;
D-02 title returns to the left;
D-03 two columns above ~1020, stacked below;
D-04 new receipt rail;
D-05 the chip strip leaves the composer;
D-06 Failed-state footer fix is in scope;
D-07 copy is unchanged (the rail's own labels are the sanctioned exception).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/20-feedback-page-frame-153-b-focused-frame-left-title-receipt-r/20-CONTEXT.md
@.planning/sketches/153-feedback-page/README.md
@lib/logs/submit_logs_screen.dart
@lib/dashboard/transactions/transactions_screen.dart
@lib/components/scaffold/gw_page_header.dart
@test/dashboard/transactions_page_frame_test.dart
</context>

<source_audit>
## Multi-source coverage audit

Every item below is COVERED by a task in this plan. No item is deferred, simplified or split out.

| Source | Item | Covered by |
|---|---|---|
| GOAL | ROADMAP 20 item 1 - page frame `xxl` -> `GeniusBreakpoints.large` (1024), no new constant | Task 2 |
| GOAL | ROADMAP 20 item 2 - header leaves the 560 column, `centered: true` dropped | Task 2 |
| GOAL | ROADMAP 20 item 3 - `LayoutBuilder` two columns wide / stacked narrow | Task 2 |
| GOAL | ROADMAP 20 item 4 - new rail of probe rows + the note | Task 2 |
| GOAL | ROADMAP 20 item 5 - chip strip leaves the composer, content is not duplicated | Task 2 |
| GOAL | ROADMAP 20 item 6 - Failed-state footer fix | Task 3 |
| GOAL | ROADMAP 20 "Preserve, do not regress" - the whole Phase 19 mechanic | Tasks 2, 3 (anti-goals) + Task 3 verify (the 5 pure-logic tests pass unchanged) |
| REQ | none - `phase_req_ids` is "none new - this is a layout change to a shipped surface" | n/a, `requirements: []` by instruction |
| RESEARCH | none - deliberately skipped. The sketch README carries the code-grounded findings instead; both are covered below | n/a |
| RESEARCH (sketch finding 1) | `_candidateLogNames` is a two-element const, so the rail is 4 rows on a good day and 2 with the SDK stopped - size it for that | Task 2 (rail built from `_probes`, no growth affordance) |
| RESEARCH (sketch finding 2) | The status line and the CTA share a `Row` with the CTA fixed-width; the 130-char empty-event-ID copy parks the button mid-block | Task 3 |
| RESEARCH (sketch finding 3) | Nothing persists `_lastEventId`; no feedback history exists, so no "recent reports" surface may appear | Task 2 (anti-goal, explicit) |
| RESEARCH (sketch finding 4) | `GWCard` padding is `space12` (24) on this card | Task 2 (the rail card uses the same `space12`) |
| CONTEXT | D-01 frame cap -> `GeniusBreakpoints.large` | Task 2 |
| CONTEXT | D-02 title returns to the left | Task 2 |
| CONTEXT | D-03 two columns / stacked | Task 2 |
| CONTEXT | D-04 receipt rail incl. the two note strings and the No-SDK variant | Task 2 |
| CONTEXT | D-05 chip strip leaves the composer | Task 2 |
| CONTEXT | D-06 Failed-state footer fix | Task 3 |
| CONTEXT | D-07 copy unchanged; rail labels follow the short-hyphen rule | Tasks 2, 3 (every string is either already in the file or quoted verbatim from D-04/D-07) |
| CONTEXT | "One new runnable check" - widget test, geometry arithmetic, no goldens, no fixtures | Task 1 (RED) + Task 3 (GREEN) |

### One interpretation the planner had to make

D-07 says "copy is unchanged" and D-04 hands over four new strings. They are not in conflict: D-07's
own last sentence names the rail's labels ("What gets sent", "SDK", "Platform") as the rail's own,
and D-04 quotes the two note sentences verbatim. The casualties are the chip strip's two header
strings, "SDK logs attached automatically" and "last 1 MB of each, empty ones skipped" - they are
*replaced by* the rail's title and note, which is precisely what D-04/D-05 ask for. Every other
user-visible string in the file - subtitle, placeholders, the four status strings, button labels, the
whole Success block - is untouched. This is called out so the executor does not stall on the apparent
contradiction.
</source_audit>

<artifacts_this_phase_produces>
New symbols in `lib/logs/submit_logs_screen.dart` (all private to `_SubmitLogsScreenState`):

- `static const double _composerWidth` - `GeniusBreakpoints.small` (640), the composer column.
- `static const double _railMinWidth` - `360`, the narrowest the rail is allowed to be.
- `static const double _twoColumnMin` - `_composerWidth + GeniusWalletConsts.space10 + _railMinWidth`
  (1020), the `LayoutBuilder` threshold, derived so it cannot drift from the widths it is about.
- `Widget _buildRail(GWColors gw, bool sdkReady)` - the receipt rail.
- `Widget _railRow(GWColors gw, String label, String value, {bool strike = false, bool tail = false, Color? valueColor})`
  - one key/value line of the rail.

Deleted symbols: `_buildReceipt`, `_logChip`, `_metaChip`.
Kept and reused: `_tailBadge`, `_friendlySize`, `_AttachmentProbe`, `attachmentDispositionFor`,
`attachmentNameFor`, `FeedbackType`, `_probeAttachments`, `_submitFeedback`, `_buildSuccess`,
`_buildTypeChooser`, `_buildTypeSegment`, `_buildComposer`.

New file `test/logs/submit_logs_page_frame_test.dart`, containing test-local:
`_StoppedSdkApi` (GeniusApi stub), `_host()`, `_surface(WidgetTester, double, [double])`,
`_composerCard()`, `_railCard()`.

No new shared widget, no new token, no new constant class, no new package.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write the page-frame check first, and watch it fail</name>

  <read_first>
    - `test/dashboard/transactions_page_frame_test.dart` - the harness idiom this file copies: the
      `_surface` helper with BOTH teardowns, the `implements GeniusApi` + `noSuchMethod` stub, and the
      header comment that says why it pumps the REAL screen instead of a replica.
    - `test/components/gw_page_header_centered_test.dart` - the arithmetic-on-geometry idiom
      (`tester.getRect`, `moreOrLessEquals`, an epsilon, no golden).
    - `lib/logs/submit_logs_screen.dart` - the widget under test as it stands today.
    - `lib/utils/breakpoints.dart` - `GeniusBreakpoints.small` (640) and `.large` (1024) are the two
      public tokens the assertions use, so the test and the screen cannot drift apart.
  </read_first>

  <files>test/logs/submit_logs_page_frame_test.dart</files>

  <behavior>
    - Test 1 (window 2000): the frame content measures exactly `GeniusBreakpoints.large`; the title
      text's left edge equals the composer card's left edge; the composer card measures
      `GeniusBreakpoints.small` and its right edge is left of the rail card's left edge; the two cards'
      tops are level.
    - Test 2 (window 1000): the two cards share a left edge, the rail's top is at or below the
      composer's bottom, and both measure the viewport minus the two 12px gutters.
    - Test 3 (window 1000): the send button's top is at or below the bottom of the status line.
    - All three currently FAIL. That is the point of this task.
  </behavior>

  <action>
Create `test/logs/submit_logs_page_frame_test.dart`. It must COMPILE and RUN today and FAIL on its
geometry assertions - a test that fails to compile has proved nothing.

**Header comment**, in the voice of `transactions_page_frame_test.dart`, stating three things: (a) this
file pumps the real `SubmitLogsScreen`, not a hand-copied replica, because a replica cannot fail for
the reason this file exists - swapping the frame cap back would leave a replica green; (b) the
`flutter_test` surface is 800x600 logical and nothing in this repo changes it, so every pump goes
through `_surface` and its teardowns are not optional, since a leaked surface changes every file that
runs after this one; (c) what this file does NOT cover, for the phase walk to judge - the SDK-running
rail rows, the Failed status string, light mode, and the send path itself.

**Stub.** `class _StoppedSdkApi implements GeniusApi` with exactly two members: an override
`bool get isSdkInitialized => false;` and `dynamic noSuchMethod(Invocation i) => super.noSuchMethod(i);`.
Comment why: the single override keeps `_probeAttachments` off the disk (it returns early and sets an
empty probe list), and `noSuchMethod` forwarding to `super` throws loudly if the screen ever starts
calling something else rather than returning a silent null. Also state why SDK-stopped is the state
pumped: it is the only state reachable without a real SDK, a real base path and a live Sentry, and the
page geometry under test is identical in every state.

**Host.** `Provider<GeniusApi>.value(value: _StoppedSdkApi(), child: MaterialApp(theme: ThemeData(extensions: [GWColors.dark()]), home: const SubmitLogsScreen()))`.
Import `package:provider/provider.dart` - a direct dependency (`pubspec.yaml:38`). `RepositoryProvider.value`
from `flutter_bloc` works identically; `lib/main.dart` uses both forms.

**Surface helper.** Copy `_surface(WidgetTester tester, double width, [double height = 900])` verbatim
in shape from the transactions file: set `tester.view.physicalSize = Size(width * 3, height * 3)` and
`tester.view.devicePixelRatio = 3.0`, then `addTearDown` for `resetPhysicalSize` AND
`resetDevicePixelRatio`.

**Finders.** Anchor on content, not on index, so the assertions survive a reorder:
`_composerCard()` returns `find.ancestor(of: find.byType(TextField), matching: find.byType(GWCard)).first`;
`_railCard()` returns `find.ancestor(of: find.text('What gets sent'), matching: find.byType(GWCard)).first`.

**Test 1** - name it for the property, e.g. `'the frame caps at large, the title sits on its left edge, and the rail sits beside the composer'`.
`_surface(tester, 2000)`, pump the host, `pumpAndSettle`. A window WIDER than the cap, so the cap
actually binds - at the cap itself it would not bind and the test would silently measure the viewport.
Assert, each with a `reason:`:
`tester.getSize(find.byType(GWPageHeader)).width` is `closeTo(GeniusBreakpoints.large, 1)`;
`tester.getTopLeft(find.text('Send Feedback')).dx` is `moreOrLessEquals(tester.getTopLeft(_composerCard()).dx, epsilon: 0.5)`
- name this one as the assertion that fails if the 560 `Center` wrapper or a centred header comes back;
`tester.getSize(_composerCard()).width` is `closeTo(GeniusBreakpoints.small, 1)`;
`tester.getRect(_composerCard()).right` is `lessThan(tester.getRect(_railCard()).left)`;
the two cards' `.top` are `moreOrLessEquals(..., epsilon: 0.5)`.
Finish with `expect(tester.takeException(), isNull)`.

**Test 2** - `'below the two-column width the rail stacks under the composer'`. `_surface(tester, 1000)`
- content is 976, under the 1020 threshold, so the stacked branch binds. Assert the two cards' `.left`
are equal to within 0.5; `tester.getRect(_railCard()).top` is `greaterThanOrEqualTo(tester.getRect(_composerCard()).bottom)`;
both card widths are `closeTo(1000 - 24, 1)` - viewport minus the two 12px gutters, the same property
the transactions frame test measures. `expect(tester.takeException(), isNull)` catches a
`RenderFlex overflowed` report, which is the real failure mode of a narrow rail row.

**Test 3** - `'the send action sits below the status line, never beside it'`. `_surface(tester, 1000)`,
pump. Assert `tester.getRect(find.byType(GWButton)).top` is `greaterThanOrEqualTo(tester.getRect(find.text('Start the SDK first - feedback needs it running to attach logs and send.')).bottom)`.
Use that string verbatim - it is the No-SDK status line the stub puts the screen into, it is ~78
characters, and it wraps in the harness's wide fallback font, which is exactly the condition that
parks the button mid-block today. Comment that the arrangement is unconditional, so proving it in the
No-SDK state proves it for the Failed state whose 130-character message this is really about, and
which no test can reach without a live Sentry.

Do NOT add goldens, fixtures, mock packages, or a `setUpAll`. Do NOT touch any other test file.
  </action>

  <verify>
    <automated>flutter analyze test/logs/submit_logs_page_frame_test.dart && flutter test test/logs/submit_logs_page_frame_test.dart; echo "exit=$? (a NON-zero exit is the expected RED for this task)"</automated>
  </verify>

  <acceptance_criteria>
    - `test/logs/submit_logs_page_frame_test.dart` exists and `flutter analyze` on it reports no issues.
    - `flutter test test/logs/submit_logs_page_frame_test.dart` runs and reports 3 tests, ALL FAILING.
    - The failures are geometry-assertion failures (a `closeTo`/`moreOrLessEquals`/`lessThan` mismatch,
      or a "Finder found zero widgets" on the rail's 'What gets sent' text). They are NOT compile
      errors, NOT `ProviderNotFoundException`, and NOT a `NoSuchMethodError` from the stub. If any of
      those three appear, the harness is wrong and the task is not done.
    - No other file under `test/` is modified: `git status --porcelain test/` lists only the one new
      untracked file.
  </acceptance_criteria>

  <done>The check exists, compiles, runs, and fails for the right reason.</done>
</task>

<task type="auto">
  <name>Task 2: Focused frame - left title, two columns, receipt rail</name>

  <read_first>
    - `lib/logs/submit_logs_screen.dart` in full - line numbers below are from the tree as read on
      2026-07-26 and may have drifted; verify by reading, do not trust them blind.
    - `lib/dashboard/transactions/transactions_screen.dart:72-121` - the page-frame idiom this screen
      aligns to: `Padding` OUTSIDE the `ConstrainedBox` (the order is load-bearing in both
      directions), `Column(crossAxisAlignment: stretch, mainAxisSize: min)`, and `GWPageHeader` as its
      first child with NO spacer after it.
    - `lib/components/scaffold/gw_page_header.dart` - the default left-aligned branch this screen now
      renders, and the `centered` flag that stays.
    - `.planning/sketches/153-feedback-page/index.html` section `#variant-b`, plus its `rail()`
      builder - the rail's exact row order and note text.
    - `lib/theme/genius_wallet_colors.dart:173-194` and
      `lib/dashboard/home/widgets/transaction_displays.dart:37-48` - the `statusWarning` precedent and
      the light-mode ceiling already recorded for it.
  </read_first>

  <files>lib/logs/submit_logs_screen.dart</files>

  <behavior>
    - At a frame content width of 1024 the composer card is 640 wide with the rail card beside it.
    - Below 1020 of content width the rail card renders under the composer card, both full width.
    - The page title renders left-aligned at the frame's left edge, at the composer card's X.
    - The rail renders 'What gets sent', the SDK row, the Platform row and the note in every state; it
      adds at most two log rows, and none at all when the SDK is stopped.
    - The chip strip no longer renders anywhere: 'SDK logs attached automatically' and 'SDK Stopped'
      are gone from the tree, 'Stopped' appears exactly once, in the rail.
  </behavior>

  <action>
All of this is inside `lib/logs/submit_logs_screen.dart`. Nothing else in `lib/` may change - in
particular `lib/squid_router/swap_screen.dart` and `lib/components/scaffold/gw_page_header.dart` are
untouchable in this phase.

**1. Layout constants (D-01, D-03).** Add three `static const double` fields at the top of
`_SubmitLogsScreenState`, beside `_maxAttachmentBytes`: `_composerWidth` = `GeniusBreakpoints.small`,
`_railMinWidth` = `360`, and `_twoColumnMin` = `_composerWidth + GeniusWalletConsts.space10 + _railMinWidth`.
Comment that the threshold is DERIVED rather than typed so it cannot drift from the two widths it is
about, and that its value (1020) is why the frame cap is the existing `large` token with 4px to spare
- the sketch's 1040 is a mockup number and no new width constant is introduced for it.

**2. Frame cap (D-01).** In `build`, the page frame's `BoxConstraints` maxWidth changes from
`GeniusBreakpoints.xxl` to `GeniusBreakpoints.large`. Rewrite the comment above it: this frame is
deliberately NARROWER than Transactions / Markets / News because it is a focused form page, not a
content page - what is left over reads as page margin instead of as a hole. Leave the surrounding
`Align(topCenter)`, the `SingleChildScrollView` and its `EdgeInsets.fromLTRB(12, space32, 12, 8)`
exactly as they are: the 12/8 gutter and the 64px navbar-to-title gap are what keep this page in
vertical and horizontal agreement with the other tabs, and the `Padding`-outside-`ConstrainedBox`
order is load-bearing (a narrow window gets its gutter from the Padding alone).

**3. Title returns to the left (D-02).** Delete the `Center` wrapper, the `ConstrainedBox(maxWidth: 560)`
inside it and the inner `Column` entirely, and delete the `centered: true` argument from the
`GWPageHeader` call. `GWPageHeader(title: 'Send Feedback', subtitle: 'Bug reports, ideas and questions go straight to the team.')`
becomes the FIRST direct child of the frame's `Column(crossAxisAlignment: CrossAxisAlignment.stretch, mainAxisSize: MainAxisSize.min)`.
Do not add a `SizedBox` after it - `GWPageHeader` owns its own `space8` bottom gap and adding one is
how this page would drift out of vertical alignment with the other tabs. Title and subtitle strings
are unchanged (D-07). Note in the comment that the flag itself stays in `GWPageHeader` for Swap and is
covered by `test/components/gw_page_header_centered_test.dart`; only this call site stops passing it.

**4. Two columns (D-03).** The SECOND and last child of that `Column` is a `LayoutBuilder`. It must sit
INSIDE the `ConstrainedBox`, so `constraints.maxWidth` is the frame's content width and not the window
width - say so in a comment, it is the single easiest thing to get wrong here. In its builder, build
two locals first: `composer` = `GWCard(padding: EdgeInsets.all(GeniusWalletConsts.space12), child: isSuccess ? _buildSuccess(gw) : _buildComposer(gw, sdkReady))`
(the same card, the same padding, the same `isSuccess` branch as today), and `rail` =
`GWCard(padding: EdgeInsets.all(GeniusWalletConsts.space12), child: _buildRail(gw, sdkReady))`. Then
branch on `constraints.maxWidth >= _twoColumnMin`:
wide returns `Row(crossAxisAlignment: CrossAxisAlignment.start, children: [SizedBox(width: _composerWidth, child: composer), const SizedBox(width: GeniusWalletConsts.space10), Expanded(child: rail)])`;
narrow returns `Column(crossAxisAlignment: CrossAxisAlignment.stretch, mainAxisSize: MainAxisSize.min, children: [composer, const SizedBox(height: GeniusWalletConsts.space10), rail])`.
Both locals must be used in both branches - a rail built only in the wide branch vanishes on a narrow
window, and `crossAxisAlignment: start` is what stops the two cards being forced to a common height.

**5. The rail (D-04).** Add `Widget _buildRail(GWColors gw, bool sdkReady)` returning a
`Column(crossAxisAlignment: CrossAxisAlignment.start, mainAxisSize: MainAxisSize.min)`:
  - `Text('What gets sent', style: GeniusWalletTypography.labelMd.copyWith(color: gw.textPrimary))`,
    then `SizedBox(height: GeniusWalletConsts.space8)`.
  - When `sdkReady` is true, the log rows, driven by `_probes` exactly as the old chip strip was:
    if `_probes` is null, one line reading 'Checking for logs...' in `bodySm` / `gw.textSecondary`;
    otherwise, if every probe is `skipEmpty` or the list is empty, the line 'No logs found yet' in the
    same style, placed ABOVE the probe rows (the old code added that chip and then still listed the
    probes - keep that behaviour); then one `_railRow` per probe. Both strings already exist in this
    file today; reuse them verbatim (D-07).
  - When `sdkReady` is false, NO log rows at all - D-04 says the No-SDK rail keeps SDK + Platform and
    explains itself in the note.
  - Always, after the log rows: `_railRow(gw, 'SDK', sdkReady ? 'Running' : 'Stopped', valueColor: sdkReady ? gw.statusSuccess : gw.textSecondary)`
    then `_railRow(gw, 'Platform', Platform.operatingSystem)`.
  - Then `SizedBox(height: GeniusWalletConsts.space8)` and the note. When `sdkReady`, a single `Text`
    in `bodySm` / `gw.textSecondary`: 'Only the last 1 MB of each log is read, so large files stay light. Empty logs are skipped.'
    When not, a `Row(crossAxisAlignment: CrossAxisAlignment.start)` of
    `Icon(Icons.warning_amber_rounded, size: 16, color: GeniusWalletColors.statusWarning)`, a
    `SizedBox(width: GeniusWalletConsts.space3)`, and `Expanded(Text('Logs are collected by the SDK. Start it and they attach here automatically.', ...))`
    in the same `bodySm` / `gw.textSecondary`. Both sentences are quoted verbatim from D-04.
  - Add a `ponytail:` comment on the No-SDK note explaining that the warning tone is carried by the
    amber GLYPH and not by the sentence, because `GeniusWalletColors.statusWarning` (#FFC42E) is
    mode-invariant and measures roughly 1.8:1 as a label on the light canvas - the ceiling
    `lib/dashboard/home/widgets/transaction_displays.dart` already records for the pending pill.
    Name the remaining ceiling honestly: the 16px glyph itself is under 3:1 on the light canvas
    (WCAG 1.4.11), deferred with the rest of the light pass. Upgrade path: a `gw.statusWarning` getter
    with a darker light-mode amber (about #B26A00), mirroring `gw.statusSuccess` / `gw.statusError` -
    the same upgrade that file names.
  - Add a short comment recording the finding this rail is sized for: `_candidateLogNames` is a
    two-element const, so the rail is four rows with the SDK running and two with it stopped. It is
    not a list that grows, and it must not be built as if it does.

**6. The rail row.** Add ONE helper,
`Widget _railRow(GWColors gw, String label, String value, {bool strike = false, bool tail = false, Color? valueColor})`.
Wrap it in `Padding(EdgeInsets.symmetric(vertical: GeniusWalletConsts.space3))` and return a
`Row(crossAxisAlignment: CrossAxisAlignment.start)` whose label side is `Flexible` with
`TextOverflow.ellipsis` (in `bodySm`, `gw.textPrimary`, and `TextDecoration.lineThrough` +
`gw.textSecondary` when `strike`), followed by the existing `_tailBadge(gw)` after a
`SizedBox(width: GeniusWalletConsts.space3)` when `tail`, then a `SizedBox(width: GeniusWalletConsts.space4)`
and the value `Text` in `bodySm` / `valueColor ?? gw.textSecondary`. Do NOT use a bare `Spacer` or a
fixed-width label: the rail can be as narrow as 364, and `sgnslog2.log` + `TAIL` + `1.4 MB` has to
degrade by ellipsis rather than by overflowing. Call it for a probe with `label` = the file name,
`value` = `_friendlySize(probe.size)` normally or the existing word `skipped` when the disposition is
`skipEmpty`, `strike` and `tail` derived from the disposition exactly as `_logChip` derived them.

**7. The chip strip leaves the composer (D-05).** Delete the three chip helpers whose names begin
`_buildReceipt`, `_logChip` and `_metaChip`, and delete the call to the first of them in
`_buildComposer` together with the `SizedBox(height: GeniusWalletConsts.space8)` that preceded it. KEEP
`_tailBadge` - the rail reuses it. The content moves; it is not rendered in both places. `flutter
analyze` is the check here: a helper left behind and unused trips `unused_element`.

**Anti-goals, all hard.** Do not touch `_submitFeedback`, `_probeAttachments`, `_readTailBytes`,
`_normalizedBasePath`, `_isSuccessfulSentryId`, `_resetToReady`, `_copyEventId`, `_buildSuccess`,
`_buildTypeChooser`, `_buildTypeSegment`, `_friendlySize`, `attachmentDispositionFor`,
`attachmentNameFor`, `AttachmentDisposition` or `FeedbackType` - the last four are what
`test/logs/submit_logs_feedback_test.dart` tests and their signatures are frozen. Do not change the
Sentry scope, the tags, the contexts, the attachment logic, the 1 MiB cap, the empty-file skip, the
No-SDK guard, or either of the two distinct failure messages. Do not add a feedback-history, ticket,
reply or "recent reports" surface - the app has no data for any of them and nothing persists
`_lastEventId` past the session. Do not add a new width constant, a new colour token, a new shared
widget or a package. Do not commit.
  </action>

  <verify>
    <automated>flutter analyze lib/logs/submit_logs_screen.dart && flutter test test/logs/submit_logs_page_frame_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `flutter analyze lib/logs/submit_logs_screen.dart` reports no issues (this is also what proves no
      dead private helper was left behind).
    - Tests 1 and 2 of `test/logs/submit_logs_page_frame_test.dart` PASS. Test 3 (the footer) may still
      fail - Task 3 closes it.
    - `grep -c 'GeniusBreakpoints.large' lib/logs/submit_logs_screen.dart` is at least 1.
    - `grep -c 'What gets sent' lib/logs/submit_logs_screen.dart` is exactly 1.
    - `grep -c '_tailBadge' lib/logs/submit_logs_screen.dart` is at least 2 - still defined and still
      called from the rail.
    - `git diff --name-only` lists `lib/logs/submit_logs_screen.dart` and nothing else under `lib/`.
    - No commit is created.
  </acceptance_criteria>

  <done>The page renders as one frame: title on the left edge at the composer's X, composer and rail side by side above 1020 of content width, stacked below it, and the chip strip exists nowhere.</done>
</task>

<task type="auto">
  <name>Task 3: Failed-state footer, then prove the whole thing</name>

  <read_first>
    - `lib/logs/submit_logs_screen.dart` - the footer `Row` at the end of `_buildComposer`, and the
      empty-event-ID message inside `_submitFeedback` that is the reason this task exists.
    - `.planning/sketches/153-feedback-page/README.md` - the "MANIFEST row" section, which carries the
      row to append verbatim, and the frontmatter `winner:` key.
    - `.planning/sketches/MANIFEST.md` - the table's five columns are `# | Name | Design Question | Winner | Tags` (header at line 24).
    - `CLAUDE.md` - the parallel-session rules that govern the two planning-doc edits, and the standing
      instruction not to create commits.
  </read_first>

  <files>lib/logs/submit_logs_screen.dart, .planning/sketches/MANIFEST.md, .planning/sketches/153-feedback-page/README.md</files>

  <behavior>
    - The status line occupies its own full-width line; the send button renders below it, right-aligned.
    - The button keeps its exact size, variant, icon, `isLoading` and `onPressed` wiring.
    - Test 3 of the new check goes green, and the two pre-existing test files pass with a zero-line diff.
  </behavior>

  <action>
**1. The footer (D-06).** At the end of `_buildComposer`, replace the final
`Row(crossAxisAlignment: CrossAxisAlignment.center, ...)` that pairs the status `Text` with the
`GWButton`. The new arrangement is a `Column(crossAxisAlignment: CrossAxisAlignment.stretch, mainAxisSize: MainAxisSize.min)`
holding: the status `Text` at full width with its existing `statusText` / `statusColor` /
`GeniusWalletTypography.bodySm` untouched; a `SizedBox(height: GeniusWalletConsts.space8)`; and
`Align(alignment: Alignment.centerRight, child: GWButton(...))` with the `GWButton` call copied over
unchanged - same `label`, `leading`, `isLoading`, `onPressed: canSend ? _submitFeedback : null`.
Delete the `Expanded` around the status text and the `SizedBox(width: space8)` that separated them.

Do NOT make the button full-width. The sketch's A/C/D variants use a block CTA, but Phase 19 locked
the primary / gradientOutline button ladder and a full-bleed primary is a different visual weight;
right-aligning keeps the button byte-identical and moves only the status off its line, which is the
whole defect. Keep the arrangement UNCONDITIONAL - one shape for all five states, not a special case
for Failed, because a layout that only exists in an unreachable state is a layout nobody can verify.

Comment the defect being fixed, naming it precisely: the empty-event-ID message is about 130
characters, it wraps to several lines in a 640 column, and the old row's centre cross-alignment parked
the button in the middle of that block. No user-visible string changes anywhere in this step (D-07).

**2. Prove it.** Run, in this order, and record the actual output in the SUMMARY:
`flutter analyze lib/logs/submit_logs_screen.dart test/logs/submit_logs_page_frame_test.dart` - must
report no issues; then
`flutter test test/logs/submit_logs_page_frame_test.dart test/logs/submit_logs_feedback_test.dart test/components/gw_page_header_centered_test.dart`
- all must pass, 3 + 5 + 2 = 10 tests.
Then `git diff --stat test/logs/submit_logs_feedback_test.dart test/components/gw_page_header_centered_test.dart`
must print NOTHING. If either of those two files needed an edit to pass, stop and say so in the
SUMMARY as a design smell rather than editing it: the first tests four pure helpers this phase froze,
and the second pumps `GWPageHeader` in isolation, so neither can legitimately be affected by a
page-layout change.
Do NOT run the full suite and do NOT quote a whole-suite pass/fail number: the repo's baseline is
contested while sessions run in parallel (CLAUDE.md), and a neighbour's tests would be misread as this
phase's regression.

**3. Bookkeeping.** Two small planning-doc edits, both append/replace-one-value only:
  - `.planning/sketches/153-feedback-page/README.md` - set the frontmatter `winner:` from `null` to
    `B` (chosen by Jakub 2026-07-26).
  - `.planning/sketches/MANIFEST.md` - append the sketch-153 row. The row is drafted verbatim in that
    README's "MANIFEST row" section; paste it and replace the `_pending pick_` marker in the Winner
    column with the chosen variant and the phase that shipped it. APPEND ONLY, at the end of the
    table. Do not reorder, rewrite or reflow any existing row - the file is already dirty from a
    parallel session, and this is the exact class of collision `CLAUDE.md` documents. First check with
    `grep -c '^| 153 ' .planning/sketches/MANIFEST.md`: if it is not 0, a sibling session already added
    the row, so update only its Winner cell instead of adding a second one.

Do not create a commit. Do not stage anything.
  </action>

  <verify>
    <automated>flutter analyze lib/logs/submit_logs_screen.dart test/logs/submit_logs_page_frame_test.dart && flutter test test/logs/submit_logs_page_frame_test.dart test/logs/submit_logs_feedback_test.dart test/components/gw_page_header_centered_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `flutter analyze` on the two touched Dart files reports no issues.
    - All 10 tests pass across the three named files (3 new + 5 pure-logic + 2 header).
    - `git diff --stat test/logs/submit_logs_feedback_test.dart test/components/gw_page_header_centered_test.dart`
      produces empty output - both pre-existing test files are byte-identical.
    - `git diff --name-only` under `lib/` lists `lib/logs/submit_logs_screen.dart` and nothing else;
      `lib/squid_router/swap_screen.dart` and `lib/components/scaffold/gw_page_header.dart` are
      untouched.
    - `grep -c '^| 153 ' .planning/sketches/MANIFEST.md` is exactly 1.
    - The sketch README frontmatter reads `winner: B`.
    - `git log --oneline -1` is unchanged from before the phase - no commit was created.
  </acceptance_criteria>

  <done>The send action sits below the status line in every state, the new check is green, both pre-existing test files pass untouched, and the sketch's winner is recorded.</done>
</task>

</tasks>

<planner_discipline_allowlist>
These literals appear in `<action>` bodies because the executor has to be told which existing symbols
to delete or replace. None of them is negative-grepped by an acceptance criterion in this plan -
`flutter analyze` (via `unused_element`) and the widget test's `findsNothing`-equivalent geometry
assertions carry that duty instead - but the markers are recorded so intent is unambiguous.

<!-- planner-discipline-allow: _buildReceipt -->
<!-- planner-discipline-allow: _logChip -->
<!-- planner-discipline-allow: _metaChip -->
<!-- planner-discipline-allow: centered -->
<!-- planner-discipline-allow: GeniusBreakpoints.xxl -->
</planner_discipline_allowlist>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard -> `_feedbackController` -> Sentry | Untrusted free text leaves the machine. Unchanged by this phase; `_submitFeedback` is not touched. |
| local disk (`geniusApi.jsonFilePath`) -> Sentry attachment | SDK log bytes leave the machine. Unchanged by this phase; `_probeAttachments` and `_readTailBytes` are not touched. |
| local disk -> rendered rail rows | File NAMES and SIZES are rendered to the local user. This is the only boundary this phase moves, and it moves it sideways: the shipped chip strip already renders exactly these values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Information disclosure | `_buildRail` log rows | low | accept | The rail shows the same file names and `_friendlySize` values the shipped `_logChip` strip already shows, to the same local user on their own machine. No file CONTENT is ever rendered. Net-zero exposure change. |
| T-20-02 | Information disclosure | the rail's Platform row | low | accept | `Platform.operatingSystem` is already rendered by the shipped chip strip and already leaves the machine as the `platform` Sentry tag. Unchanged. |
| T-20-03 | Denial of service (local render) | `_railRow` at the 364px rail width | low | mitigate | Label side is `Flexible` with `TextOverflow.ellipsis` and no `Spacer`; Test 2 asserts `tester.takeException()` is null at the stacked width, which surfaces a `RenderFlex overflowed` report as a test failure. |
| T-20-04 | Tampering | dependency supply chain | n/a | accept | No package-manager install task exists in this phase. `pubspec.yaml` and `pubspec.lock` are not in `files_modified`, so the Package Legitimacy Gate does not apply. If any task ever needs a new package, stop and re-plan. |
| T-20-05 | Repudiation | the two distinct failure messages | medium | mitigate | The exception path and the empty-`SentryId` path must stay distinct so a dropped upload is not misread as a thrown error. Task 2's anti-goals freeze `_submitFeedback`; Task 3 changes only the footer's arrangement, never the strings. |
</threat_model>

<verification>
## Automated (run by Task 3, gating)

- `flutter analyze lib/logs/submit_logs_screen.dart test/logs/submit_logs_page_frame_test.dart` -> no issues.
- `flutter test test/logs/submit_logs_page_frame_test.dart test/logs/submit_logs_feedback_test.dart test/components/gw_page_header_centered_test.dart` -> 10/10 pass.
- `git diff --stat` on the two pre-existing test files -> empty.
- Deliberately NOT run: the full suite. The baseline is contested while sessions run in parallel
  (CLAUDE.md), so a whole-suite number is not evidence about this phase.

## Human check (for phase verification, not a mid-plan gate)

These are the success criteria in `20-CONTEXT.md` that geometry arithmetic cannot settle. Run the app
(`flutter run` - executor session only, per CLAUDE.md) and look:

<human-check>
1. **Maximised window.** The page reads as ONE frame: the title sits on the frame's left edge at the
   same X as the composer card, the rail sits beside the composer, and no leftover band is wider than
   it reads as a page margin. Compare against Transactions in the same window.
2. **Drag the window narrow.** The rail drops under the composer at the seam; nothing overflows,
   no red stripe, no clipped rail row. Check `sgnslog2.log` + TAIL + size on the narrowest rail.
3. **The rail is honest at both ends.** With the SDK running: at most two log rows, plus SDK Running
   and the platform, plus the 1 MB note. With the SDK stopped: no log rows at all, SDK Stopped, the
   platform, and the amber-glyph note explaining why there are none.
4. **The footer.** In any state the status sentence sits on its own line with the send button below it
   and to the right - never floating beside a multi-line block.
5. **The card did not regress.** Chooser (all three types swap the placeholder), message field, send,
   Success with the Reference number and Copy, Send another -> back to Ready. Phase 19's walk covered
   this on 2026-07-25; this phase must not have moved any of it.
</human-check>
</verification>

<success_criteria>
- The page frame caps at `GeniusBreakpoints.large`, proved by an assertion at a 2000px window.
- The title's left edge equals the composer card's left edge, proved by an assertion, so a returning
  `Center(ConstrainedBox(560))` or centred header fails the build.
- Two cards side by side at 1024 of content width, stacked below 1020, both proved by assertions.
- The receipt facts appear exactly once in the tree, in the rail.
- The send action never sits beside a multi-line status line.
- `test/logs/submit_logs_feedback_test.dart` and `test/components/gw_page_header_centered_test.dart`
  pass with a zero-line diff.
- `flutter analyze` clean on both touched Dart files.
- Exactly one source file under `lib/` changed; no commit created.
</success_criteria>

<output>
Create `.planning/phases/20-feedback-page-frame-153-b-focused-frame-left-title-receipt-r/20-01-SUMMARY.md` when done.

Record in it: the actual `flutter analyze` and `flutter test` output lines (not a paraphrase), the
final rail row order in each SDK state, the footer arrangement chosen, and anything the live walk
still owes. Do not create a commit.
</output>
