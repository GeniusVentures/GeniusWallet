---
phase: 21-drawer-language-rollout-the-four-decided-drawer-designs-appl
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/components/bottom_drawer/drawer_content.dart
  - lib/reown/swap_result_drawer.dart
  - lib/banxa/banxa_components/buy_success_drawer.dart
  - lib/banxa/banxa_components/buy_success_drawer_content.dart
  - lib/banxa/banxa_components/buy_cancelled_drawer.dart
  - lib/banxa/banxa_components/buy_cancelled_drawer_content.dart
  - tool/check_raw_colors.sh
  - test/components/drawer_content_test.dart
  - test/components/result_receipt_family_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "The three remaining result drawers render the same head as the transaction receipt - same gap rhythm, same centring, same pill geometry - so a swap result, a purchase result and a transaction receipt are one picture rather than three."
    - "Every status colour in these three drawers comes from txStatusColors, the same function the transaction receipt's pill and Status row already share. Cancelled is slate, not red."
    - "No amount, order number, payment method or fiat figure appears that the drawer's own API does not carry. A result with no amount shows no amount rather than a fabricated one."
    - "Every button in these three drawers is a GWButton - filled gradient for the primary action, gradient outline for a secondary one. No raw accent-coloured Material button survives."
    - "lib/banxa/banxa_components/ holds zero appearance-blind colour literals and is added to the standing CI gate, so it cannot regress."
  artifacts:
    - test/components/result_receipt_family_test.dart
  key_links:
    - "txStatusColors (transaction_displays.dart) -> GWDrawerStatusPill -> the three result receipts -> one status palette across all six receipts (D-02, D-03)"
    - "GWDrawerReceiptHead (21-01) -> its first three consumers -> the shared gap rhythm that makes the family read as one"
    - "tool/check_raw_colors.sh COVERED_DIRS -> lib/banxa/banxa_components -> a permanent gate replacing a one-off cleanup"
---

<objective>
Bring the three result receipts that are still on pre-redesign styling onto
031-B1 / archetype B.

D-02 names six receipts. Measured at HEAD `08f6df5`, three are already done and
in two different ways:

- `showTransactionDetails` (`transaction_displays.dart`) is **fully converted** —
  identity + amount centred, fiat line, exact-amount row, `_statusPill`,
  `GWKicker` + `GWDetailGrid` sections, 154-D copy rows, explorer button
  suppressed when the chain has no URL. Nothing to do.
- `squid_router/swap_success_drawer.dart` and `swap_fail_drawer.dart` **do not
  exist**. Commit `9ff7c04` deleted them a phase earlier and repointed the swap
  screen at the shared receipt (`swap_screen.dart:348-351`). D-02 is satisfied
  for those two by the strongest possible means: they became the transaction
  receipt.

That leaves `reown/swap_result_drawer.dart`, `banxa/.../buy_success_drawer.dart`
and `banxa/.../buy_cancelled_drawer.dart`. All three still ship raw Material
palette values, raw `OutlinedButton`/`ElevatedButton`, and a `deepBlueMenu` card
with fixed white text.

**The honest constraint on this plan.** Sketch 066's archetype B draws these
with an amount, a fiat line and a Purchase section (`Paid`, `Method`, `Order`).
None of those three drawers' APIs carry any of it: `BuySuccessDrawer.show` takes
only an optional `onClose`, `BuyCancelledDrawer.show` takes nothing, and
`SwapResultDrawer.show` takes `isSuccess`, `txHash` and `coinSymbol`. 21-CONTEXT's
scope fence says **do not invent data**, and wiring order data through is Phase 9
mechanics, not this phase's. So the head renders with the slots it actually has,
and the missing rows are recorded as a finding, not fabricated.

Purpose: three drawers, one receipt language, zero appearance-blind colours, and
a standing gate so the Banxa half cannot rot back.
Output: six converted files, one primitive made honest about optional amounts,
and one family check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/21-drawer-language-rollout-the-four-decided-drawer-designs-appl/21-CONTEXT.md
@.planning/sketches/drawers-final/README.md
@.planning/sketches/066-drawer-language/README.md
@lib/components/bottom_drawer/drawer_content.dart
@lib/components/cards/gw_detail_grid.dart
@lib/components/data/gw_copy_row.dart
</context>

<read_first>
Read these before editing anything:

1. `lib/components/bottom_drawer/drawer_content.dart` — 21-01's two primitives in
   full. `GWDrawerStatusPill(label:, foreground:, background:)` maps no enum and
   owns no palette. `GWDrawerReceiptHead(identity:, amount:, amountColor:, pill:,
   fiat:, exact:)` centres the head and omits every null slot entirely. Both have
   **zero consumers today**; this plan is their first three.
2. `lib/dashboard/home/widgets/transaction_displays.dart:53-75` — `txStatusColors`,
   a **public** top-level function returning `({Color fg, Color wash})` for each
   of the four `TransactionStatus` values. This is the single source of status
   colour the transaction receipt's pill AND its Status row already share. Read
   the doc comment: it exists precisely because two consumers had drifted apart.
   Note the recorded `ponytail:` ceiling — the completed/failed tones still miss
   AA as *label text* in light mode. Do not try to fix that here; it has its own
   todo (`2026-07-29-status-pill-success-error-fail-aa-in-light-mode.md`).
3. `lib/components/data/gw_copy_row.dart` — `GWCopyRow(label:, value:, shorten:)`.
   Already the shipped way to put a hash in a `GWDetailGrid`; see
   `lib/submit_job/view/widgets/job_steps.dart:355-392` for two live call sites.
   Its own doc fences it to public on-chain identifiers, which a tx hash is.
4. `lib/reown/swap_result_drawer.dart`, and the four
   `lib/banxa/banxa_components/buy_*` files. All six are under 110 lines; read
   them whole.
5. `tool/check_raw_colors.sh` — read the `COVERED_DIRS` array and the header
   paragraph explaining that a directory is appended **once its own count hits
   zero**, never speculatively.
</read_first>

<environment>
The Flutter SDK is **not** on `PATH` in this repo's environment. Every shell
step in this plan must start with:

```bash
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
```

**Entering baseline, measured at HEAD `08f6df5`:**

| Gate | Baseline |
|---|---|
| `flutter analyze` (root + `packages/genius_api`) | 0 issues, exit 0 |
| `flutter test` | **732/732** passing |
| `bash tool/check_brace_style.sh` | 0 |
| `bash tool/check_raw_colors.sh` | 0 |
| `bash tool/check_no_new_key_logging.sh` | PASSED |
| `bash tool/check_onboarding_seed_safety.sh` | PASSED |
| `dart format --set-exit-if-changed` | exit 0 |

**Measured offender counts inside this plan's fence** (run with `COVERED_DIRS`
temporarily pointed at these paths, 2026-07-30):

| Path | Appearance-blind literals today |
|---|---|
| `lib/banxa/banxa_components/buy_success_drawer.dart` | 2 |
| `lib/banxa/banxa_components/buy_success_drawer_content.dart` | 1 |
| `lib/banxa/banxa_components/buy_cancelled_drawer.dart` | 2 |
| `lib/banxa/banxa_components/buy_cancelled_drawer_content.dart` | 1 |
| `lib/banxa/banxa_components/` (everything else: order_card, order_details_card, order_status_style, quote_card) | **0** |
| `lib/reown/swap_result_drawer.dart` | 2 |
| `lib/reown/` (rest: handle_dapp_requests 1, reown_connect_button 2, send_transaction_details 2, approve drawers 2) | 7 |

That table is why Task 4 can add `lib/banxa/banxa_components` to the gate and
cannot add `lib/reown` — three of reown's seven live in files no plan in this
phase touches.

**`lib/banxa/` is NOT behind the AGENTS.md auto-generated fence.** That fence
names `/banxa` and `/squidrouter` — the **repo-root** directories, which exist
and are genuinely generated. `lib/banxa/` is ordinary app code with a full Phase 9
re-skin history (see `test/banxa/banxa_reskin_literals_test.dart`, which gates ten
`lib/banxa/` files). Sketch 066 recorded this as an open question for Braian; it
is answered, and the answer is that these four files are editable.
</environment>

<constraints>
- **Do not commit.** Leave every change in the working tree. Wave 1 runs four
  plans in parallel against one git index.
- **Do not invent data.** No order number, no payment method, no amount, no fiat
  figure, no timestamp, no "Try again" action. If a slot has no source in the
  drawer's own parameters, it does not render. This is 21-CONTEXT's scope fence
  and it is the reason this plan looks smaller than sketch 066's drawing.
- **Do not change any of the three drawers' signatures or navigation contracts.**
  `SwapResultDrawer.show`'s four named params, `BuySuccessDrawer.show(context,
  {onClose})` and `BuyCancelledDrawer.show(context)` keep their shapes;
  `handle_dapp_requests.dart`, `reown_connect_button.dart` and
  `dev_tools_bubble.dart` are OUT of `files_modified` and must not be edited.
- **D-03 is absolute:** colour rides on the icon, the pill and a status row only.
  No amount, headline or body sentence takes a status colour in any of the three.
- **D-07/D-08:** primary action = `GWButtonVariant.gradient`; secondary =
  `GWButtonVariant.gradientOutline`. No flat blue, no accent-coloured Material
  button, no radius-10 hand-rolled shape.
- Do not migrate `transaction_displays.dart`'s own inline head onto
  `GWDrawerReceiptHead`. It is shipped, walked, and out of this plan's file scope;
  its gap rhythm (`space8` top) differs from the primitive's (`space16`) and
  reconciling them is a separate, visual decision — record it, do not make it.
- Do not touch `test/banxa/banxa_reskin_literals_test.dart`. Its
  `_fencedPathSubstrings` list deliberately excludes these four files from Phase
  9's gate; leave the fence intact and let Task 4's gate cover them instead.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GWDrawerReceiptHead learns that a result may have no amount (D-02)</name>
  <files>lib/components/bottom_drawer/drawer_content.dart, test/components/drawer_content_test.dart</files>
  <behavior>
    - Test 1: a head built with no amount renders no amount text at all — no
      placeholder, no dash, no zero — exactly as the already-tested `fiat`,
      `exact` and `pill` slots behave when omitted.
    - Test 2: a head built with an amount still renders it in the caller's own
      colour, unchanged. The four existing cases in this file keep passing
      untouched.
  </behavior>
  <action>
Make `amount` and `amountColor` optional (`String?` / `Color?`) on
`GWDrawerReceiptHead`, and wrap the amount `Text` plus its preceding `SizedBox`
in the same `if (x != null) ...[ ]` form the widget already uses for `fiat`,
`exact` and `pill`. When the amount is absent the head is identity → gap → pill,
with the rhythm otherwise unchanged.

This is an optional SLOT, not a mode flag, and that distinction is the reason it
is allowed under AGENTS.md's "never add a boolean flag to make one component
serve two callers" — the widget already has three such slots and its own doc
comment already states the rule ("Every optional slot omitted entirely when null
— never a placeholder, never a fabricated zero").

Extend the class doc comment with the measured reason: three of the six receipts
D-02 names (both Banxa results and the Reown swap result) carry no amount in
their APIs at all, so an amount-required head would have forced either a
fabricated figure or three receipts outside the family. Name the alternative that
was rejected and why.

Keep `amountColor` required-when-amount-present by asserting it in the
constructor initialiser list, so a caller cannot pass an amount with no colour
and silently inherit a default — D-03 exists because a derived amount colour is
the failure mode.

Add the two new cases to `test/components/drawer_content_test.dart` alongside the
four already there. Do not restructure the existing four.
  </action>
  <verify>
    <automated>export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH" && cd "$(git rev-parse --show-toplevel)" && flutter test test/components/drawer_content_test.dart && flutter analyze lib/components/bottom_drawer/drawer_content.dart</automated>
  </verify>
  <done>
`drawer_content_test.dart` passes with 6 cases (4 pre-existing + 2 new).
`flutter analyze` is 0 for the file. No other file changed in this task.
  </done>
</task>

<task type="auto">
  <name>Task 2: The Reown swap result becomes a 031-B1 receipt (D-02, D-03, D-07, D-08)</name>
  <files>lib/reown/swap_result_drawer.dart</files>
  <action>
Rebuild the body and footer of `SwapResultDrawer.show` on the shared vocabulary.
The signature, the `message`/`icon` derivation, the `getExplorerUrl` call and the
`await`/`unawaited` behaviour at both call sites stay exactly as they are.

**Status colour, from one source.** Import `txStatusColors` from
`lib/dashboard/home/widgets/transaction_displays.dart` and resolve it once:
`isSuccess` maps to `TransactionStatus.completed`, otherwise
`TransactionStatus.failed`. Use the returned `fg` for the icon and for the pill's
foreground, and `wash` for the pill's background. Do not write a second switch
and do not read `gw.statusSuccess`/`gw.statusError` directly here — the whole
point of D-02 is that the pill in this drawer and the pill on the transactions
row cannot disagree.

**Head.** `GWDrawerReceiptHead` with:
`identity:` an `Icon(icon, size: 56, color: fg)` — the existing glyph choice
(`check_circle` / `error`) is kept; only its size and colour source change;
`amount:` **omitted** — this drawer's API carries no amount;
`pill:` `GWDrawerStatusPill(label: isSuccess ? 'Completed' : 'Failed',
foreground: fg, background: wash)`.
Delete the big coloured headline `Text` that currently repeats the drawer title
in the status colour: the title bar already says it, and a second copy in red or
green is the exact "colour everywhere" 031 round-2 removed.

**Body.** When `txHash` is non-empty, a `GWKicker('Transaction')`, a `space4` gap,
then `GWDetailGrid(rows: [GWCopyRow(label: 'Transaction hash', value: txHash)])`.
This replaces the `deepBlueMenu` container with its fixed-white `SelectableText`
and keeps the hash gettable — `GWCopyRow` copies the FULL value while displaying
a truncated form, which is strictly better than select-to-copy inside a drawer.
When `txHash` is empty (the failure branch always passes `""`), render neither the
kicker nor the grid — `GWDetailGrid` already renders nothing for an empty row list,
but do not emit a bare kicker over nothing.

**Footer.** Both buttons become `GWButton`, `size: GWButtonSize.lg`, `expand: true`:
`Go to Transactions` as `GWButtonVariant.gradient` (it is the panel's committing
action); `View on Explorer` as `GWButtonVariant.gradientOutline` (D-07 names this
button explicitly), still suppressed when `explorerUrl` is empty, with a `space6`
gap between them. Keep both `onPressed` closures verbatim.

Every colour in the finished file must come from a token or from `txStatusColors`.
Brace every `if` with the body on its own line.
  </action>
  <verify>
    <automated>export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH" && cd "$(git rev-parse --show-toplevel)" && flutter analyze lib/reown/swap_result_drawer.dart && grep -c 'GWDrawerReceiptHead\|GWDrawerStatusPill\|txStatusColors\|GWCopyRow\|GWButton' lib/reown/swap_result_drawer.dart</automated>
  </verify>
  <done>
`flutter analyze` is 0 for the file. The shared-vocabulary grep count is at least
8 (imports plus call sites). `SwapResultDrawer.show`'s four named parameters are
unchanged — verify with `git diff` that no line in `handle_dapp_requests.dart` or
`dev_tools_bubble.dart` moved.
  </done>
</task>

<task type="auto">
  <name>Task 3: The two Banxa result receipts (D-02, D-03, D-07, D-08)</name>
  <files>lib/banxa/banxa_components/buy_success_drawer.dart, lib/banxa/banxa_components/buy_success_drawer_content.dart, lib/banxa/banxa_components/buy_cancelled_drawer.dart, lib/banxa/banxa_components/buy_cancelled_drawer_content.dart</files>
  <action>
Four small files, one shape. `*_content.dart` holds the head; `*_drawer.dart`
holds the shell and footer.

**Titles.** Sketch 066 (accepted by Jakub 2026-07-27) records the current titles
as a defect — a bare `Success` with no noun. Use `Purchase complete` and
`Purchase cancelled`.

**Status colour, from one source.** Same rule as Task 2: `txStatusColors` with
`TransactionStatus.completed` for success and `TransactionStatus.cancelled` for
cancelled. **Cancelled takes slate, not red** — that is 066-B's explicit rule and
`txStatusColors` already implements it (`fg: textSecondary`, `wash: surfaceMenu`):
nothing broke, so nothing should read as an error. Today this drawer paints a red
cancel glyph, which is the thing being corrected.

**Head, in each `*_content.dart`.** `GWDrawerReceiptHead` with `identity:` an
`Icon` at size 56 in `fg` (`check_circle` for success; for cancelled use an
outline `close`/`cancel_outlined` glyph rather than the filled alarm-shaped
`Icons.cancel`), `amount:` omitted, and `pill:` a `GWDrawerStatusPill` labelled
`Completed` / `Cancelled` from `fg`/`wash`. Below the head, a `space6` gap and the
existing context sentence — "Crypto has been added to your wallet." and "You
exited the checkout process before completing the transaction." — kept verbatim,
centred, in `GeniusWalletTypography.bodyMd` / `gw.textSecondary`. Delete the
current bold 20px headline `Text`: the drawer title now carries that.

Both widgets must take a live `Theme.of(context).extension<GWColors>()` read
inside `build`. They are `const` today, which is exactly the "const widget does
not re-skin" defect class Phase 9's own gate was written for — they cannot stay
`const`.

**Footers.** `GWButton`, `size: GWButtonSize.lg`, `expand: true`.
Success: one `GWButtonVariant.gradient` labelled `Done`.
Cancelled: one `GWButtonVariant.gradientOutline` labelled `Close`. Do **not** add
066-B's "Try again" — no retry callback exists and inventing one is mechanics.

**One defect to fix while here, and to record.** Both footer buttons are dead
today: success calls `onClose` only if it was supplied (the sole caller supplies
nothing), and cancelled's handler is an empty block. Make each one pop the drawer
first, then invoke `onClose` if present. Note it in the summary as a fixed
defect, not as a silent improvement.

**A finding to record, not to act on.** These two drawers have **no production
caller** — the only references outside their own files are
`lib/dev/dev_tools_bubble.dart:683,688`. Sketch 066-B's Purchase section (`Paid`,
`Method`, `Order`) needs order data that neither the drawers' APIs nor their one
dev-only caller supply. State both facts plainly in the summary so the developer
can decide whether these drawers should be wired to a real purchase result or
deleted. Do not decide it here.

Colours from tokens only; brace every `if` with the body on its own line.
  </action>
  <verify>
    <automated>export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH" && cd "$(git rev-parse --show-toplevel)" && flutter analyze lib/banxa/banxa_components/ && flutter test test/banxa/</automated>
  </verify>
  <done>
`flutter analyze` is 0 across `lib/banxa/banxa_components/`. The whole existing
`test/banxa/` suite still passes, including
`banxa_reskin_literals_test.dart` — whose fence list must be untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: The family check, and a standing gate over the Banxa half</name>
  <files>test/components/result_receipt_family_test.dart, tool/check_raw_colors.sh</files>
  <behavior>
    - Test 1: a success result renders a pill whose foreground is the SAME colour
      `txStatusColors(TransactionStatus.completed, gw).fg` returns. Pinning the
      source, not the hex, is what stops the palette forking again.
    - Test 2: a cancelled result's pill reads slate, not the error colour — the
      one tone every earlier round of this design got wrong.
    - Test 3 (the D-03 guard): in a failed result, no text in the subtree is
      painted in the status colour except the pill's own label. This is the check
      that fails if someone re-reddens a headline or an amount.
    - Test 4: a result drawer with no amount renders no numeric headline at all.
  </behavior>
  <action>
Write `test/components/result_receipt_family_test.dart` against the widgets, not
the files — pump `GWDrawerReceiptHead` + `GWDrawerStatusPill` in the exact
composition Tasks 2 and 3 build, with `MaterialApp(theme: ThemeData(extensions:
[GWColors.dark()]))`. Model the style on `test/components/drawer_content_test.dart`
and `test/components/gw_select_row_test.dart`: no new package, no fixture
directory, no golden, no `integration_test`.

Open with a header comment naming what this test is FOR, in the house voice: the
six receipts were six columns that resembled each other, and the specific way
that comes back is a second status palette or a re-coloured headline — both of
which look plausible in a screenshot.

For test 3, walk the pumped subtree's `Text` widgets and assert that none of them
resolves to the status foreground except the pill's label. Read the resolved
style rather than trusting the widget's constructor argument.

**Then extend the standing gate.** In `tool/check_raw_colors.sh`, append
`"lib/banxa/banxa_components"` to `COVERED_DIRS`, keeping the array's existing
alphabetical-ish ordering and its one-entry-per-line shape. The header explains
the rule this obeys: a directory is appended once its own count is zero, never
speculatively — so run the gate BEFORE editing the array to confirm Task 3 really
took that directory to zero, and only then add it. Add a one-line comment next to
the new entry naming the plan that cleared it.

`lib/reown` is deliberately NOT added: three offenders remain in
`handle_dapp_requests.dart` and `reown_connect_button.dart`, neither of which any
plan in this phase touches. Record that in the same comment so the next person
does not have to re-measure it.
  </action>
  <verify>
    <automated>export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH" && cd "$(git rev-parse --show-toplevel)" && flutter test test/components/result_receipt_family_test.dart && bash tool/check_raw_colors.sh --self-test && bash tool/check_raw_colors.sh</automated>
  </verify>
  <done>
The new test passes with 4 cases. `check_raw_colors.sh --self-test` still exits 0
(the array edit did not break the script's own fixtures), and the gate itself
exits 0 with `lib/banxa/banxa_components` now inside its scope. Full
`flutter test` is at least **738** passing (732 baseline + 2 from Task 1 + 4 here)
with 0 failures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dApp session -> swap result | `txHash` and `coinSymbol` reach `SwapResultDrawer` from a remote dApp's session request via `handle_dapp_requests.dart`. |
| result drawer -> clipboard | `GWCopyRow` writes the full hash to the system clipboard on tap. |
| status enum -> displayed outcome | Whether a user believes a swap succeeded is carried entirely by this drawer's icon, pill and title. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-04 | Spoofing | success/failure indication | critical | mitigate | `isSuccess` continues to drive icon, pill and title from one resolved `txStatusColors` pair. Task 4 tests 1-2 pin the mapping to its source function, so a re-skin cannot make a failure read as a success. |
| T-21-05 | Tampering | truncated hash reaching the clipboard | high | mitigate | `GWCopyRow` copies `value`, never `_displayValue` — a property its own doc records as audited in Phase 23 and covered by `test/components/gw_copy_row_test.dart`. This plan adopts the audited widget rather than hand-rolling a second copy path. |
| T-21-06 | Denial of Service | unbounded `coinSymbol` / dApp-supplied strings in the head | medium | mitigate | `GWDrawerReceiptHead` ellipsises at `maxLines: 1`; `GWCopyRow` truncates by construction. No unbounded remote string reaches an unclipped `Text`. |
| T-21-07 | Information Disclosure | key material through `GWCopyRow` | high | accept | `GWCopyRow`'s doc fences it to public on-chain identifiers. A transaction hash is public by definition. No seed, key or mnemonic is in scope in any of these three files, and none is introduced. |
| T-21-08 | Repudiation | a cancelled purchase reading as a failure | low | mitigate | `txStatusColors(cancelled)` is slate, asserted by Task 4 test 2. Miscolouring a cancellation as an error trains users to discount real errors. |
| T-21-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package-manager install in this plan.** No new dependency, no new dev-dependency, no `pubspec.yaml` edit. If any step appears to need one, stop and raise it rather than installing. |

No package-manager installs in this plan. No new dependency.
</threat_model>

<verification>
```bash
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
cd "$(git rev-parse --show-toplevel)"
flutter analyze                        # expect 0 issues, exit 0
(cd packages/genius_api && flutter analyze)
flutter test                           # expect >= 738 passing, 0 failing
bash tool/check_brace_style.sh         # expect 0
bash tool/check_raw_colors.sh --self-test
bash tool/check_raw_colors.sh          # expect 0, now covering lib/banxa/banxa_components
bash tool/check_no_new_key_logging.sh
bash tool/check_onboarding_seed_safety.sh
dart format --set-exit-if-changed lib test
git diff --stat -- lib/reown/handle_dapp_requests.dart lib/reown/reown_connect_button.dart lib/dev/dev_tools_bubble.dart
#   ^ must print nothing: the three drawers' callers are outside this fence
```
</verification>

<success_criteria>
- All six D-02 receipts are on 031-B1 by one of two routes: converted here
  (3), already converted (`showTransactionDetails`), or deleted-and-repointed at
  the shared receipt by `9ff7c04` (the two swap drawers).
- One status palette across the whole app: every pill in every receipt resolves
  through `txStatusColors`.
- No amount, order figure or fiat line exists anywhere in the three converted
  drawers, because none exists in their data.
- `lib/banxa/banxa_components` is inside `check_raw_colors.sh` and the gate is green.
- The three drawers' `show()` signatures and their callers are byte-identical to
  HEAD.
</success_criteria>

<human_verification>
The executor cannot launch the app. Record these **OUTSTANDING**, verbatim, in
the summary — do not mark them PASS. The dev-tools bubble reaches all three
without a live dApp or a Banxa sandbox round trip:

- [ ] Dev bubble → **Buy OK**. Dark mode: title reads "Purchase complete"; one
      green check glyph; a "Completed" pill under it; the sentence below is quiet
      grey, NOT green; one filled-gradient "Done" that actually closes the drawer.
- [ ] Dev bubble → **Buy fail**. Title "Purchase cancelled"; the glyph and pill
      read **slate**, not red — a cancellation must not look like an error; the
      "Close" button is a gradient outline and actually closes the drawer.
- [ ] Dev bubble → the two swap-result buttons. Success shows a "Completed"
      pill and a TRANSACTION grid holding one copyable hash row; tapping the row
      shows the "Transaction hash copied" snackbar. Paste it somewhere and
      confirm the **full** 66-character hash arrived, not the truncated display
      form.
- [ ] The failure branch (empty hash) shows no empty grid and no bare kicker
      floating over nothing.
- [ ] Repeat all four in **light** mode. The slate cancelled pill is the one most
      likely to fail here.
- [ ] Put the three side by side with a real transaction receipt (Transactions
      tab → any row). Same icon size, same gap above the pill, same pill
      geometry. If they do not read as one family, that is a finding worth
      filing — it is the phase's headline claim.
</human_verification>

<artifacts>
| Symbol | Location | Consumed by |
|---|---|---|
| `GWDrawerReceiptHead` with optional `amount` | `lib/components/bottom_drawer/drawer_content.dart` | its first three consumers, this plan |
| `test/components/result_receipt_family_test.dart` (new) | test | the 031-B1 pattern's check |
| `COVERED_DIRS += lib/banxa/banxa_components` | `tool/check_raw_colors.sh` | CI, permanently |

Deleted: the `deepBlueMenu` hash card in `swap_result_drawer.dart`, the duplicated
status headline in all three drawers, and every raw Material button in the six files.
</artifacts>

<output>
Create `.planning/phases/21-drawer-language-rollout-the-four-decided-drawer-designs-appl/21-03-SUMMARY.md` when done.

Record in the summary, under a heading of its own, the two findings this plan
must not act on:
1. `BuySuccessDrawer` / `BuyCancelledDrawer` have **no production caller** — only
   `dev_tools_bubble.dart`. Archetype B's Purchase rows need order data no caller
   supplies.
2. `handle_dapp_requests.dart:174-184` builds a **complete `Transaction` model**
   two lines before it calls `SwapResultDrawer.show`. That model is exactly what
   `showTransactionDetails` consumes, which means the Reown swap result could
   take the same deletion-and-repoint `9ff7c04` applied to the two squid swap
   drawers, instead of being re-skinned. That is a Phase 10 mechanics call and it
   is outside this phase's fence — record the evidence, do not act on it.

Do NOT commit. Leave every change in the working tree.
</output>
