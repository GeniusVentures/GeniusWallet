---
context: phase
phase: 04-navigation-shell-chrome
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-07-17T21:11:59.033Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was discovered through actual failure on this project. Acknowledge before proceeding.

- [ ] CONSTRAINT: **Never run plan executors in parallel.** Phase 2 Wave 1 ran two concurrently; they share one git index and one agent's staged file was swept into the other's commit. Also, the worktree base is diverged (`worktree.base-check` → `shouldDegrade=true`), so isolation is off and executors run on the main tree. → One executor at a time.
- [ ] CONSTRAINT: **`flutter analyze` is NOT evidence.** `analysis_options.yaml:5` excludes `lib/**/*.g.dart`. It has passed clean on files with real compile errors. → Gate only; correctness comes from the human walk.
- [ ] CONSTRAINT: **Never adopt Alex's files wholesale — RE-DERIVE, never inherit.** His `theme.dart` silently drops six live develop `ThemeData` sections; a wholesale swap would regress them. This discipline caught 6+ wrong inherited facts across Phases 3–4 (section counts, importer counts, a physically-impossible walk instruction, the dropped theme sections, a task-ordering bug). → Read develop's file yourself, reconcile key-by-key, verify each preserved section is present.
- [ ] CONSTRAINT: **Never add to `tool/shadow-baseline.txt` to make the guard pass.** Guard trip = STOP and report.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Parallel executors | Two agents, one git index → cross-contaminated commits | blocking | One executor per wave, sequential; worktree base is diverged so isolation is off anyway |
| Analyze-as-evidence | `flutter analyze` = 0 errors while `.g.dart` holds real errors | blocking | Human walk for render/appearance correctness; analyze is a gate only |
| Wholesale-adopt Alex's file | His `theme.dart` drops 6 live develop `ThemeData` sections | blocking | Reconcile key-by-key; a must_have asserts each preserved section present |
| Baselining a shadow | Editing Check 2's census to make the guard green | blocking | Guard trip = STOP and report, never edit the baseline |

<current_state>
Phase 4, plan 1 of 6. Branch `ui-redesign-port`, HEAD `c3a80d3`, working tree clean.

**04-01 (theme-only linchpin) code is COMPLETE and orchestrator-verified.** develop's `theme.dart` is
now appearance-aware (light/dark `ColorScheme`, `textTheme: toMaterialTextTheme()`,
`scaffoldBackgroundColor: surfaceBase`) with all six develop-only `ThemeData` sections preserved and
Alex's WCAG `onPrimary: textOnBrand` kept. `main.dart` wraps `MaterialApp.router` in a
`ValueListenableBuilder<GWAppearanceMode>` (line 297) so a toggle rebuilds the app.
`gw_appearance.dart`'s `load()` now reads OS brightness on first launch (line 33) — D-03 follow-OS. The
gallery `Scaffold` was reverted from the 03-09 `surfaceBase` workaround back to `Colors.transparent`.
`flutter analyze lib` 0 errors (62-issue baseline); `tool/verify_additive_boundary.sh` PASSED.

**Waiting on the human: the D-02 gallery re-walk.** That is the next action, and it is the GATE before
any 04-02+ plan starts.
</current_state>

<completed_work>

- Task 1: theme.dart reconciled appearance-aware, key-by-key — Done (`953f1c7`, padding fix `029c089`). Six preserved sections verified present.
- Task 2: OS-first-launch default + MaterialApp ValueListenableBuilder wrap + gallery revert — Done (`2f3aa8a`).
- Docs/state/roadmap — Done (`c3a80d3`).
</completed_work>

<remaining_work>

- Task 3 (THIS PLAN): the D-02 gallery re-walk — HUMAN gate. Not started. See Next Action.
- 04-02 (wave 2): shell chrome re-skin (`responsive_overlay.dart`) — criteria 1, 2.
- 04-03 (wave 3): dashboard boot + wallet drawer (`account_dropdown_selector.dart`) — criteria 3, 4.
- 04-04 (wave 4): Settings re-skin (`settings_screen.dart`) — criterion 5, GAP-02.
- 04-05 (wave 5): SDK account manager re-skin (`sdk_account_manager.dart`) — criterion 5, GAP-03.
- 04-06 (wave 6): branded exception recovery screen (`main.dart`) — criterion 6, BEH-02.
</remaining_work>

<decisions_made>

- **04-01 is theme-only, ends at the D-02 gate** (D-01/D-02). Executed via `--wave 1` so the run halts here.
- **theme.dart reconciled key-by-key, not wholesale-adopted** — Alex drops 6 live develop sections; all 6 verified present.
- **GlobalSwapFabHost + the 7a63b4f !_dirty carry deferred to the swap-FAB phase** (D-08) — not built this phase; ROADMAP records it as a carry-move. Criterion 1's general clause stays, walked in 04-02.
- **Delete confirmation is a pure re-skin** (D-06 resolved) — develop already has `_confirmDeleteWallet`.
</decisions_made>

<blockers>
- None technical. Waiting on the human's D-02 gallery re-walk (see Next Action).
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured state, the pending human action, decisions
2. `.planning/phases/04-navigation-shell-chrome/04-01-SUMMARY.md` — what 04-01 did and the exact walk recipe in its "Outstanding" section
3. `.planning/phases/04-navigation-shell-chrome/04-CONTEXT.md` — D-01..D-08 (the authority)
4. `.planning/phases/04-navigation-shell-chrome/04-VALIDATION.md` — the observation map (no test harness; walk is the spine)
5. `.planning/todos/pending/2026-07-17-design-system-has-no-light-mode-treatment.md` — the light-mode grain question the re-walk finally answers

## Infrastructure State

- **Branch**: `ui-redesign-port` (off develop), HEAD `c3a80d3`, tree clean.
- **Worktree isolation is OFF** (`worktree.base-check` shouldDegrade=true — HEAD diverged from origin/HEAD). Executors run sequentially on the main tree.
- **Guard**: `bash tool/verify_additive_boundary.sh` → PASSED. Run before every commit.
- **Analyze baseline**: `flutter analyze lib` → 0 errors, 62 info/warnings.
- **Flutter SDK not on default PATH**: `export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"` (pinned 3.41.9).
- **Run recipe** (append `--dart-define=GW_DEV_TOOLS=true` for the Dev row):
  `CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release -DGENIUS_DEPENDENCY_BRANCH=develop -Dc-ares_DIR=C:/Users/User/Documents/Projects/GNUS/thirdparty/build/Windows/Release/cares/lib/cmake/c-ares" flutter run -d windows --debug`
- **Reference worktree** (READ-ONLY): `C:\Users\User\Documents\Projects\GNUS-compare\GeniusWallet-3514` — cannot run at the same time as the develop build (shared Hive dir, lock deadlock).
- **No test harness**: `flutter test` does not compile (APP-02). Verification = analyze gate + guard + human walk.

<context>
The re-walk is the payoff moment for the whole evening's light-mode thread. 04-01 wired the
appearance-aware theme that Phase 2 deferred and Phase 3's walk proved was the root cause of 5 of 8
findings. If the wiring is right, those five evaporate on the toggle and the dark-only count becomes
derivable for the first time — the number the user needs to decide whether light-mode grain is a
two-component patch or a design conversation. The two unexplained findings (screen wrappers blank in
dark; disabled checkbox invisible) may also clear, or may finally give a real repro.
</context>

<next_action>
**Ask the user for the D-02 gallery re-walk result.** Recipe (also in 04-01-SUMMARY's Outstanding section):

```
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release -DGENIUS_DEPENDENCY_BRANCH=develop -Dc-ares_DIR=C:/Users/User/Documents/Projects/GNUS/thirdparty/build/Windows/Release/cares/lib/cmake/c-ares" flutter run -d windows --debug --dart-define=GW_DEV_TOOLS=true
```

Dev → Gallery, toggle appearance, walk all 30 sections in BOTH modes. Report:
  1. Did the 5 theme-confound findings evaporate? (token-row/wallet-card/empty-error text, button font, icons now flip color)
  2. The dark-only COUNT across 30 sections — the light-mode-decision number.
  3. Do the two unexplained findings persist? (Screen wrappers blank in dark; disabled GWCheckbox invisible in dark)
  4. OS-follow sanity — clear the persisted appearance / launch fresh on a light-set OS → does it open light?

FULL stop-and-rerun (this plan changed main.dart's app root + touched a .g.dart), not hot reload.
Close the reference Release exe first.

Then: split findings bugs-vs-design (D-04), record the count in 04-01-SUMMARY, close the Task 3 gate,
and run `/gsd-execute-phase 4 --wave 2` (04-02 shell re-skin). One executor at a time.
</next_action>
