---
phase: 04-navigation-shell-chrome
plan: 07
type: execute
wave: 7
depends_on: [04-06]
files_modified:
  - lib/main.dart
autonomous: false
requirements: [BEH-02]
must_haves:
  truths:
    - "A build-time exception renders the branded recovery screen (GWErrorState/GWButton, surfaceBase) with a Go-to-Dashboard button — not the red error box — in BOTH appearance modes"
    - "Closing the window shuts the SDK down on every platform (finding 12 — shutdownSDK() runs unconditionally)"
    - "The exception-boundary wiring (ErrorWidget.builder, FlutterError.onError) is unchanged — only the rendered screen is re-skinned (finding 11)"
  artifacts:
    - "lib/main.dart — re-skinned ErrorWidget.builder recovery screen (visual only)"
  key_links:
    - "ErrorWidget.builder (wiring unchanged) -> re-skinned branded Material -> GoRouter.go('/dashboard') via navigatorKey"
    - "onWindowClose -> geniusApi.shutdownSDK() (unconditional, verify-only)"
---

<objective>
Re-skin the build-time exception recovery screen (BEH-02, findings 11/12) — the visual only. Replace
the hardcoded-dark `Material(color: deepBlueTertiary)` recovery screen with a branded, appearance-aware
one (`GWErrorState`/`GWIcon` + `GWButton`, `surfaceBase`), keeping the "Go to Dashboard" handler and the
`ErrorWidget.builder`/`FlutterError.onError` wiring untouched. Verify the window-close SDK shutdown.

Purpose: Criterion 6 — a build-time exception renders the branded recovery screen rather than the red
error box, and closing the window shuts the SDK down on every platform. This closes BEH-02 for Phase 4.

Scope note (D-08): BEH-02's `!_dirty`/`GlobalSwapFabHost` sub-clause is DEFERRED out of Phase 4 to the
swap-FAB phase — the crashing component is not built here. This plan MUST NOT scope any
`GlobalSwapFabHost` work. Criterion 1's general clause (boot, every route, no exception) is walked in
04-03; its `!_dirty`-via-`GlobalSwapFabHost` sub-clause is N/A this phase (D-08).

Output (artifacts this plan produces): a re-skinned recovery screen in `main.dart`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-navigation-shell-chrome/04-CONTEXT.md
@.planning/phases/04-navigation-shell-chrome/04-RESEARCH.md
@.planning/phases/04-navigation-shell-chrome/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin the ErrorWidget.builder recovery screen (visual only)</name>
  <files>lib/main.dart</files>
  <read_first>
    - lib/main.dart:223-258 (ErrorWidget.builder recovery Material — deepBlueTertiary, error icon, "Something went wrong", "Go to Dashboard")
    - lib/main.dart:150-171 (onWindowClose -> shutdownSDK; verify-only, do not modify)
    - 04-UI-SPEC.md §6 (element-by-element re-skin table), §7 (copy verbatim), §4.1 (wiring must not move)
    - lib/components/states/gw_error_state.dart (Phase 3 §2.4 — icon-in-circle/title/message/retry pattern), lib/components/gw_icon.dart, lib/components/buttons/gw_button.dart
  </read_first>
  <action>
    Re-skin the recovery screen per §6, VISUAL ONLY. Background `Material(color:
    GeniusWalletColors.deepBlueTertiary)` -> `GeniusWalletColors.surfaceBase` (appearance-aware — legible in
    light mode too, D-03). Icon `Icon(Icons.error_outline, color: Colors.redAccent, size: 64)` ->
    `GWIcon.material(Icons.error_outline, color: GeniusWalletColors.statusError, size: 64)`. Heading
    `Text('Something went wrong', ... Colors.white, fontSize: 18)` -> `GeniusWalletTypography.titleLg` +
    `GeniusWalletColors.textPrimary` (keep the string verbatim — §7, matches GWErrorState's own default).
    Action `ElevatedButton(child: Text('Go to Dashboard'))` -> `GWButton` primary variant, SAME label, SAME
    `GoRouter.of(navigatorKey.currentContext!).go('/dashboard')` handler. RECOMMENDED (not mandated, §6): if
    `GWErrorState` fits `ErrorWidget.builder`'s constrained, non-BuildContext-guaranteed call shape, build
    the screen on `GWErrorState` directly rather than hand-rolling the Material (its second real consumer).
    Do NOT move or alter the `ErrorWidget.builder`/`FlutterError.onError` wiring, and do NOT touch
    `onWindowClose`/`shutdownSDK` (findings 11/12 — verified in Task 2; §4.1). Do NOT add any
    `GlobalSwapFabHost` work (D-08). Token discipline: zero raw hex/px.
  </action>
  <verify>
    <automated>flutter analyze lib/main.dart && bash tool/verify_additive_boundary.sh</automated>
  </verify>
  <done>
    Recovery screen uses surfaceBase/statusError/titleLg/GWButton with develop's exact copy and the same
    /dashboard handler; ErrorWidget.builder/FlutterError.onError wiring is byte-unchanged; onWindowClose/
    shutdownSDK untouched; no GlobalSwapFabHost added; analyze 0 errors and additive guard passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Exception screen + SDK shutdown walk — criterion 6, both modes</name>
  <action>Human verification checkpoint — no code changes. Perform the deliberate fault-injection walk in <how-to-verify> in both appearance modes; record pass/fail and the SDK-shutdown log line in the SUMMARY.</action>
  <what-built>
    A re-skinned branded build-time-exception recovery screen (visual only), with the error-boundary and
    window-close SDK-shutdown wiring unchanged.
  </what-built>
  <how-to-verify>
    Debug run with deliberate fault injection: `flutter run -d windows --debug` (pinned SDK PATH +
    CMAKE_ARGUMENTS; close any Release exe first).
    1. Deliberately trigger a build-time exception (e.g., a thrown error inside a widget's build() during a
       debug session). Confirm the BRANDED recovery screen renders (icon-in-circle/statusError, "Something
       went wrong", "Go to Dashboard" GWButton) on a surfaceBase background — NOT the red Flutter error box.
       Confirm "Go to Dashboard" routes to /dashboard.
    2. Repeat the fault injection in BOTH light and dark mode (D-03) — the screen must be legible in light
       mode; a light-mode failure blocks close.
    3. Finding 12: close the app window and confirm the debug console logs the SDK shutdown (e.g., "Window
       closed. GeniusApi shutdown: ...") before the process exits, on this platform.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per mode)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Framework error boundary -> recovery screen | ErrorWidget.builder runs with no guaranteed BuildContext; the re-skin must not depend on context that may be absent, and must not move the wiring |
| Window-close lifecycle -> native SDK shutdown | shutdownSDK() must run unconditionally on close on every platform (finding 12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07-01 | Denial of Service | ErrorWidget.builder re-skin | medium | mitigate | Re-skin visual only; do not move ErrorWidget.builder/FlutterError.onError wiring; verify the branded screen renders on deliberate fault injection in both modes (Task 2) |
| T-04-07-02 | Tampering | onWindowClose -> shutdownSDK | high | mitigate | Do not touch onWindowClose; verify-only at the walk that shutdownSDK() still runs unconditionally on close (finding 12) |
| T-04-07-03 | Tampering | shadow-import allowlist (recovery screen mounts canonicals) | low | mitigate | Run `bash tool/verify_additive_boundary.sh` in Task 1 verify |
</threat_model>

<verification>
- `flutter analyze lib` 0 errors; `bash tool/verify_additive_boundary.sh` passes.
- Task 2 walk (both modes): branded recovery screen renders on fault injection; "Go to Dashboard" routes
  to /dashboard; window close logs SDK shutdown (finding 12).
- ErrorWidget.builder/FlutterError.onError/onWindowClose wiring confirmed unchanged (findings 11/12).
- No GlobalSwapFabHost work introduced (D-08).
</verification>

<success_criteria>
A build-time exception renders the branded recovery screen with its Go-to-Dashboard button in both
appearance modes; closing the window shuts the SDK down; the exception-boundary wiring is unchanged.
BEH-02 closes for Phase 4 (findings 11/12); the !_dirty/GlobalSwapFabHost sub-clause is deferred (D-08).
</success_criteria>

<output>
Create `.planning/phases/04-navigation-shell-chrome/04-07-SUMMARY.md` when done. Record whether the screen
was built on GWErrorState directly or hand-rolled, and confirm the BEH-02 !_dirty carry-move to the
swap-FAB phase (D-08) so the roadmap reflects a carry-move, not a drop.
</output>
