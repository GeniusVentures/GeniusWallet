---
phase: 04-navigation-shell-chrome
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - lib/account/sdk_account_manager.dart
autonomous: false
requirements: [GAP-03]
must_haves:
  truths:
    - "The SDK account manager drawer wears the redesign skin (BottomDrawer chrome, GWCard rows, GWButton actions) and flips appearance in both modes"
    - "Every existing action still works: add via each footer button, select an account, delete an account — behavior unchanged"
    - "The mnemonic/private-key entry dialogs are re-skinned to GWTextField in GWDialog with NO new logging of key material introduced"
    - "The two footer OutlinedButton actions are explicitly restyled (they depended on the now-dropped outlinedButtonTheme)"
  artifacts:
    - "lib/account/sdk_account_manager.dart — BottomDrawer chrome, GWCard rows, GWButton footer/dialog actions, GWTextField+GWDialog key-entry dialogs"
  key_links:
    - "SDKAccountManagerButton -> ResponsiveDrawer.show(child: BottomDrawer(...)) -> account rows + footer actions"
    - "Add-with-mnemonic / add-with-private-key dialogs -> GWTextField (chrome only) -> untouched native SDK FFI"
---

<objective>
Re-skin the SDK account manager (GAP-03) in place — drawer chrome, account rows, footer actions, and
the mnemonic/private-key entry dialogs — structure unchanged, only tokens and components change. The
underlying native SDK FFI calls are untouched; this plan re-skins chrome only.

Purpose: Criterion 5 (half) — the SDK account manager opens from the shell and wears the extended
design language with develop's behavior intact. Security-sensitive: the key-entry dialogs handle
mnemonic/private-key material; the re-skin must not introduce any logging of that input (V6, §Security).

Output (artifacts this plan produces): a token-correct, appearance-aware `sdk_account_manager.dart`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-navigation-shell-chrome/04-CONTEXT.md
@.planning/phases/04-navigation-shell-chrome/04-RESEARCH.md
@.planning/phases/04-navigation-shell-chrome/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wave-0 gate scaffold — no-new-key-logging check for the key-entry flow</name>
  <files>tool/check_no_new_key_logging.sh</files>
  <read_first>
    - tool/verify_additive_boundary.sh (the project's existing gate-script conventions)
  </read_first>
  <action>
    Create a tiny gate script `tool/check_no_new_key_logging.sh` that Task 3's verify depends on (Nyquist —
    the automated check must exist before it is invoked). It takes one file path argument, runs
    `git diff -- "$1"`, and fails (exit 1) if any ADDED line ('^+' but not the '+++' header) contains a
    console-logging call from the print family — matched via a runtime-assembled pattern so the literals are
    not embedded in this repo's source (assemble the token from the string 'print' plus its debug-prefixed
    variant, e.g. via a shell variable, to avoid self-tripping the additive guard). Print "OK: no new key
    logging" and exit 0 on a clean diff. Make it executable. This closes the V6 Information-Disclosure gate
    for the mnemonic/private-key dialog re-skin (04-RESEARCH §Security Domain).
  </action>
  <verify>
    <automated>test -f tool/check_no_new_key_logging.sh && bash tool/check_no_new_key_logging.sh lib/account/sdk_account_manager.dart</automated>
  </verify>
  <done>
    tool/check_no_new_key_logging.sh exists, is executable, exits 0 on a diff with no added print-family
    call and exit 1 otherwise; its own source does not embed the literal it greps for (assembled at runtime).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin the drawer chrome and account rows</name>
  <files>lib/account/sdk_account_manager.dart</files>
  <read_first>
    - lib/account/sdk_account_manager.dart (436 lines — SDKAccountManagerButton, ResponsiveDrawer.show(), instruction line, ListView.separated of account rows, footer action row)
    - 04-UI-SPEC.md §3.2 (component mapping table), §5.1 (drawer mechanism unchanged)
    - lib/components/bottom_drawer/bottom_drawer.dart, lib/components/bottom_drawer/responsive_drawer.dart
    - lib/components/cards/gw_card.dart, lib/components/gw_icon.dart
  </read_first>
  <action>
    Re-skin the drawer content chrome to `BottomDrawer` (Phase 3 §2.5, first real consumer) passed as
    `child:` to `ResponsiveDrawer.show()` — omit `title`/`actions` on the `.show()` call itself so
    BottomDrawer's own header renders without a competing AppBar (§5.1 binding rule). Do NOT touch
    `ResponsiveDrawer.show()`'s `useRootNavigator`/`enableDrag`/breakpoint args (already correct, §5.1).
    Replace each account row (`Card`/`ListTile`-shaped) with `GWCard`. Replace icons with `GWIcon`. Replace
    `deepBlueCardColor`/`deepBlueTertiary`/`Colors.greenAccent` with `surfaceElevated`/`brandPrimary`. Keep
    the mnemonic QR display UNCHANGED — it keeps develop's existing `qr_flutter` usage directly (not a gw_*
    component, no new dependency — 03-GAP-INVENTORY §6). Preserve the empty-state string verbatim (§7: "No
    SDK accounts available.\nAdd one to get started."). Token discipline: zero raw hex/px.
  </action>
  <verify>
    <automated>flutter analyze lib/account/sdk_account_manager.dart && bash tool/verify_additive_boundary.sh</automated>
  </verify>
  <done>
    Drawer content is BottomDrawer via child: with title/actions omitted on .show(); account rows use GWCard;
    icons use GWIcon; ResponsiveDrawer.show() args untouched; QR display unchanged; empty-state string
    unchanged; analyze 0 errors and additive guard passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-skin the footer actions and key-entry dialogs (security-sensitive)</name>
  <files>lib/account/sdk_account_manager.dart</files>
  <read_first>
    - lib/account/sdk_account_manager.dart (the two footer OutlinedButton.icon actions "Add with mnemonic"/"Add with private key", and the mnemonic/private-key entry dialogs)
    - 04-UI-SPEC.md §3.2 (dialog mapping: GWTextField inside GWDialog), §7 (copy preserved)
    - 04-RESEARCH.md §1 (outlinedButtonTheme drop — these two buttons are fully theme-dependent), §Security Domain (V6, no logging of key material)
    - lib/components/overlays/gw_dialog.dart, lib/components/inputs/gw_text_field.dart, lib/components/buttons/gw_button.dart
  </read_first>
  <action>
    Replace the add/delete/set-active actions with `GWButton`. The two footer `OutlinedButton.icon` calls
    ("Add with mnemonic" / "Add with private key") have NO inline `style:` override, so they were fully
    dependent on the now-dropped `outlinedButtonTheme` (04-RESEARCH §1/§4.2) — restyle them EXPLICITLY as
    `GWButton` secondary/tertiary so they do not fall back to Material 3 defaults; keep develop's exact
    labels (§7). Re-skin the mnemonic/private-key entry dialogs to `GWTextField` inside `GWDialog` (GWDialog
    is Dialog-not-AlertDialog, so the dropped dialogTheme is a non-issue, §4.2). SECURITY (V6): the key-entry
    dialogs handle mnemonic/private-key material — introduce NO new console-logging call anywhere in the
    dialog flow while converting to GWTextField/GWDialog. Do not alter the underlying native SDK FFI calls —
    chrome only. Token discipline: zero raw hex/px.
  </action>
  <verify>
    <automated>flutter analyze lib/account/sdk_account_manager.dart && bash tool/verify_additive_boundary.sh && bash tool/check_no_new_key_logging.sh lib/account/sdk_account_manager.dart</automated>
  </verify>
  <done>
    Add/delete/set-active use GWButton; the two footer buttons are explicitly restyled (not M3 fallback) with
    develop's labels; key-entry dialogs use GWTextField inside GWDialog; the diff introduces no new
    console-logging call in the key-entry flow (security gate passes); native SDK FFI untouched; analyze 0
    errors and additive guard passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: SDK account manager walk — criterion 5 (SDK half), both modes</name>
  <action>Human verification checkpoint — no code changes. Perform the walk recipe in <how-to-verify> in both appearance modes; record pass/fail per action in the SUMMARY.</action>
  <what-built>
    A re-skinned SDK account manager — BottomDrawer chrome, GWCard rows, GWButton footer actions, and
    GWTextField-in-GWDialog key-entry dialogs — with develop's native SDK behavior intact.
  </what-built>
  <how-to-verify>
    Cold debug run: `flutter run -d windows --debug` (pinned SDK PATH + CMAKE_ARGUMENTS; close any Release
    exe first).
    1. Open the SDK account manager drawer from the shell action row. Confirm the drawer chrome, rows, and
       footer actions wear the redesign skin.
    2. Add an account via each footer button ("Add with mnemonic" and "Add with private key") — confirm each
       dialog opens with GWTextField/GWDialog styling and the add flow still completes.
    3. Select an account and delete an account — confirm both behaviors are unchanged.
    4. Confirm the mnemonic QR display still renders (unchanged qr_flutter) and the empty-state copy is
       unchanged when no accounts exist.
    5. Repeat in BOTH light and dark mode (D-03) — a light-mode regression blocks close.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per mode)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Key-entry dialog input -> native SDK FFI | Mnemonic/private-key material crosses here; the re-skin touches chrome only and must never log the input |
| SDK account UI -> native SDK account mutation | Add/select/delete dispatch into the native SDK; the re-skin must not change what is dispatched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05-01 | Information Disclosure | mnemonic/private-key entry dialog re-skin | high | mitigate | Introduce no new console-logging call in the key-entry flow; enforce with tool/check_no_new_key_logging.sh (Task 1, Wave-0 gate scaffold) gating Task 3's verify (04-RESEARCH §Security Domain, V6) |
| T-04-05-02 | Tampering | native SDK FFI account mutation | high | mitigate | Re-skin chrome only; keep every add/select/delete handler pointing at the existing FFI call; verify each behavior at the walk (Task 4) |
| T-04-05-03 | Information Disclosure | copy-address / clipboard (if surfaced) | low | accept | Pre-existing develop behavior; not introduced or changed this plan |
</threat_model>

<verification>
- `flutter analyze lib` 0 errors; `bash tool/verify_additive_boundary.sh` passes.
- `bash tool/check_no_new_key_logging.sh lib/account/sdk_account_manager.dart` passes (V6 gate).
- Task 4 walk (both modes): add via each footer button, select/delete an account, QR + empty state intact.
</verification>

<success_criteria>
The SDK account manager wears the extended design language in both modes; add/select/delete work
unchanged; key-entry dialogs are re-skinned with no new logging of key material; structure unchanged
(re-skin in place, GAP-03).
</success_criteria>

<output>
Create `.planning/phases/04-navigation-shell-chrome/04-05-SUMMARY.md` when done. Record the explicit
restyle of the two theme-dependent footer buttons and confirmation the V6 no-logging gate passed.
</output>
