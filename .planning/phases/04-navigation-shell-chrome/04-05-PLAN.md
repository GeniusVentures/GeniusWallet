---
phase: 04-navigation-shell-chrome
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - lib/settings/settings_screen.dart
autonomous: false
requirements: [GAP-02]
must_haves:
  truths:
    - "The Settings screen's three sections (Log Config, Network Config, CRDT Config) wear the redesign skin and flip appearance in both modes"
    - "Every existing settings action still works: Apply/Save, log-level change, and each config toggle behave exactly as on develop"
    - "The section structure and rows are unchanged — this is a re-skin in place, not a restructure (GAP-02)"
  artifacts:
    - "lib/settings/settings_screen.dart — GWCard sections, GWSelect/GWTextField/GWSwitch rows, GWButton actions, token status colors"
  key_links:
    - "Config read/write logic (unchanged) -> re-skinned GWSelect/GWSwitch/GWTextField rows -> status line color mapping"
---

<objective>
Re-skin the Settings screen (GAP-02) in place — structure, rows, and behavior unchanged, only tokens
and components change. Map develop's `Card`/`DropdownButton`/`TextFormField`/`SwitchListTile`/
`FilledButton`/`OutlinedButton` widgets to Phase 3 `gw_*` primitives per UI-SPEC §3.1.

Purpose: Criterion 5 (half) — Settings opens from the shell and wears the extended design language
with develop's behavior intact. A mechanical re-skin; no structural question (IA-clean per GAP-01).

Output (artifacts this plan produces): a token-correct, appearance-aware `settings_screen.dart`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-navigation-shell-chrome/04-CONTEXT.md
@.planning/phases/04-navigation-shell-chrome/04-RESEARCH.md
@.planning/phases/04-navigation-shell-chrome/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin the three section cards, screen wrapper, and dividers</name>
  <files>lib/settings/settings_screen.dart</files>
  <read_first>
    - lib/settings/settings_screen.dart (407 lines — three Card sections, title rows, Dividers, status lines)
    - 04-UI-SPEC.md §3.1 (component mapping table), §3.1 screen-wrapper note (AppScreenView vs GWScreen)
    - 04-RESEARCH.md §1 (dividerTheme drop) and §4.1 (Divider contrast against GWCard surface)
    - lib/components/cards/gw_card.dart, lib/components/screens/app_screen_view.dart, lib/components/screens/gw_screen.dart
  </read_first>
  <action>
    Replace each `Card` section chrome with `GWCard` (space8 padding, radiusLg, elevationCard — Phase 3
    §2.2). Replace each section title's raw `Icon`+`Text` with `GWIcon.material` + `GeniusWalletTypography.
    titleMd` (keep bold-equivalent weight). Pick the screen wrapper — `AppScreenView` OR `GWScreen` (Phase
    3 §2.5 flags both exist with an undecided overlap; this is the first real caller) — and RECORD which and
    why in the SUMMARY; do not treat the choice as obvious. For the section `Divider()` calls: the reconciled
    theme (04-01) dropped `dividerTheme`, so Material 3's default divider color (outlineVariant) now applies
    against GWCard's surfaceElevated fill — visually check contrast at the walk and set an explicit
    `GeniusWalletColors` divider color if it reads wrong (04-RESEARCH §4.1). Do NOT change any config
    read/write logic. Token discipline: zero raw hex/px.
  </action>
  <verify>
    <automated>flutter analyze lib/settings/settings_screen.dart && bash tool/verify_additive_boundary.sh</automated>
  </verify>
  <done>
    Three sections use GWCard; title rows use GWIcon.material + titleMd; a screen wrapper is chosen and the
    choice/rationale is recorded in the SUMMARY; Dividers render with acceptable contrast against GWCard;
    no config logic changed; analyze 0 errors and additive guard passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin the config rows, action buttons, and status-line colors</name>
  <files>lib/settings/settings_screen.dart</files>
  <read_first>
    - lib/settings/settings_screen.dart (DropdownButton log-level rows, TextFormField/SwitchListTile config rows, FilledButton/OutlinedButton actions, status line ✅/Error logic)
    - 04-UI-SPEC.md §3.1 (row mapping table), §7 (status strings preserved verbatim — color-only change)
    - lib/components/inputs/gw_select.dart, gw_text_field.dart, gw_switch.dart, lib/components/buttons/gw_button.dart
  </read_first>
  <action>
    Map the rows per §3.1: `DropdownButton<String>` (log-level) -> `GWSelect<String>` (radiusPill, focus
    border brandPrimary 2px); `TextFormField` (numeric/text config) -> `GWTextField` (radiusLg,
    surfaceElevated fill); `SwitchListTile` (boolean config) -> `GWSwitch` + a plain `Text` label, dropping
    the `ListTile` wrapper (48px tap target); `FilledButton.icon`/`OutlinedButton.icon` (Apply/Save) ->
    `GWButton` primary/secondary variant — never invent a new label (§7, reuse develop's exact strings).
    Status line: KEEP the ✅/Error/neutral LOGIC exactly; swap only the colors —
    `Colors.greenAccent`/`Colors.redAccent`/`Colors.grey` -> `GeniusWalletColors.statusSuccess`/`statusError`/
    `textSecondary` (color-only substitution). Preserve every status string verbatim (§7: "{X} applied ✅",
    "Saved ✅ — Restart required for changes", "Error: {e}"). Do NOT change what any action does — only its
    skin. Token discipline: zero raw hex/px.
  </action>
  <verify>
    <automated>flutter analyze lib/settings/settings_screen.dart && bash tool/verify_additive_boundary.sh</automated>
  </verify>
  <done>
    Log-level rows use GWSelect; config rows use GWTextField/GWSwitch; Apply/Save use GWButton with
    develop's exact labels; status colors mapped to statusSuccess/statusError/textSecondary with the ✅/Error
    logic and strings unchanged; analyze 0 errors and additive guard passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Settings walk — criterion 5 (Settings half), both modes</name>
  <action>Human verification checkpoint — no code changes. Perform the walk recipe in <how-to-verify> in both appearance modes; record pass/fail per section in the SUMMARY.</action>
  <what-built>
    A re-skinned Settings screen — three GWCard sections with GWSelect/GWTextField/GWSwitch rows, GWButton
    actions, and token status colors — with develop's config logic intact.
  </what-built>
  <how-to-verify>
    Cold debug run: `flutter run -d windows --debug` (pinned SDK PATH + CMAKE_ARGUMENTS; close any Release
    exe first).
    1. Open Settings from the shell. Confirm all three sections (Log Config, Network Config, CRDT Config)
       wear the redesign skin.
    2. Exercise each section's primary action: change a log level (GWSelect), toggle a config switch
       (GWSwitch), edit a config field (GWTextField), and press Apply/Save (GWButton) — confirm each action
       still works and the status line shows the expected ✅/Error/neutral message in the right token color.
    3. Confirm dividers read cleanly against the GWCard surface.
    4. Repeat in BOTH light and dark mode (D-03) — a light-mode regression blocks close.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per mode)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI -> config persistence | Log/network/CRDT config read/write is develop's existing logic; the re-skin must not alter what any control writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05-01 | Tampering | config row controls (GWSelect/GWSwitch/GWTextField) | medium | mitigate | Re-skin the widget only; keep every onChanged/value binding pointing at develop's existing config logic; verify each action at the walk |
| T-04-05-02 | Tampering | shadow-import allowlist / duplicate-class census | low | mitigate | Run `bash tool/verify_additive_boundary.sh` in each task verify |
</threat_model>

<verification>
- `flutter analyze lib` 0 errors; `bash tool/verify_additive_boundary.sh` passes.
- Task 3 walk (both modes): each section's primary action works and shows the correct status color.
- Screen-wrapper choice recorded in the SUMMARY.
</verification>

<success_criteria>
Settings wears the extended design language in both modes; every existing action (Apply/Save, log-level,
config toggle) works unchanged; structure/rows unchanged (re-skin in place, GAP-02).
</success_criteria>

<output>
Create `.planning/phases/04-navigation-shell-chrome/04-05-SUMMARY.md` when done. Record the AppScreenView
vs GWScreen wrapper choice and rationale, and whether an explicit divider color was needed.
</output>
