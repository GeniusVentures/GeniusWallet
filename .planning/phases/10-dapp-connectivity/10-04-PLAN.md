---
phase: 10-dapp-connectivity
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified: []
autonomous: false
requirements: [SCR-06]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "On this Windows x64 machine the app pairs with react-app.walletconnect.com, approves the session, signs a personal_sign and disconnects (criterion 2, D-07 walk half)"
    - "The navbar chip, the pairing drawer and the approve drawers render in the redesign skin in dark and light (criterion 1, D-01)"
    - "The walk's console log contains WalletKit initialized, no MissingPluginException, and never a symKey (criterion 2, D-08, D-03)"
    - "format, analyze ($? 0), the brace check and the full flutter test suite pass on the finished tree"
  artifacts:
    - {path: .planning/phases/10-dapp-connectivity/10-04-SUMMARY.md, provides: "gate output quoted, walk result step by step, offline-retry outcome as observed"}
  key_links:
    - {from: "ReownConnectButton (navbar)", to: "PairDappDrawer, then ApproveDappConnectionDrawer, then handleDappRequests", via: "one live pairing on Windows x64"}
---

<objective>
Prove the phase on real hardware: one live pairing walk on this Windows x64 machine (D-07, criteria 1, 2 and 4), plus
the repo gates on the finished tree. This plan writes no code. A failure found here goes back as a gap, not a quick
patch inside the walk.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/10-dapp-connectivity/10-CONTEXT.md
@.planning/phases/10-dapp-connectivity/10-01-SUMMARY.md
@.planning/phases/10-dapp-connectivity/10-02-SUMMARY.md
@.planning/phases/10-dapp-connectivity/10-03-SUMMARY.md
Every command runs after `export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"`.
A running genius_wallet.exe locks the next Windows build's DLLs, which reads like a code error. Kill it first.
Only this session runs `flutter run`, since a second instance dies on the Hive lock.
Hand Braian one selectable choice per walk step, never a prose checklist. Run every command yourself.
Infrastructure errors, such as a relay 5xx or the dApp site being down, are infrastructure. Check the control case before calling a step a code failure.
</context>

<tasks>

<task type="auto">
  <name>The finished tree passes every gate, and the app is running with its log captured</name>
  <files>(none: verification only)</files>
  <read_first>.planning/phases/10-dapp-connectivity/10-01-SUMMARY.md (which option Braian chose), 10-02-SUMMARY.md, 10-03-SUMMARY.md</read_first>
  <action>Run each gate and quote its real output in the SUMMARY:
- `dart format --output=none --set-exit-if-changed lib/reown lib/web/web_view_windows.dart test/reown packages/local_secure_storage/lib`
- `flutter analyze; echo "exit=$?"`
- `bash tool/check_brace_style.sh` and `bash tool/check_raw_colors.sh`
- the full `flutter test`, quoting its pass and fail counts. If anything fails, run the same test on develop before blaming this phase.
- `git ls-files --eol` on the three new Dart files
- `echo $PROCESSOR_ARCHITECTURE`
Then kill any genius_wallet.exe (`taskkill //F //IM genius_wallet.exe`, which may report none). Set `SCRATCH` to your scratchpad directory (never inside the repo). Start `flutter run -d windows` in the background, tee'd to `$SCRATCH/walk.log`. Wait until the log shows the WalletKit-initialized line, which only appears on desktop because of plan 01's Pay stub.</action>
  <verify><automated>grep -c "WalletKit initialized" "$SCRATCH/walk.log"</automated></verify>
  <acceptance_criteria>
    - format exits 0; analyze prints exit=0; both tool scripts exit 0; flutter test reports 0 failures, with counts quoted
    - `git ls-files --eol` shows `i/lf` for pair_dapp_drawer.dart, pair_dapp_drawer_test.dart and walletkit_init_test.dart
    - PROCESSOR_ARCHITECTURE prints AMD64, and walk.log has at least one WalletKit-initialized line and no initialization-failed line
  </acceptance_criteria>
  <done>Every gate is green with its output quoted, and the app is running on x64 with WalletKit initialized.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Live dApp pairing walk on Windows x64</name>
  <precondition>Network is up and https://react-app.walletconnect.com loads in Chrome on this machine.</precondition>
  <read_first>.planning/phases/10-dapp-connectivity/10-CONTEXT.md (decisions D-02, D-06, D-07), `$SCRATCH/walk.log` (tail)</read_first>
  <what-built>A redesigned pairing drawer, a real init retry, a clipboard-pairing toast and symKey-safe logging, running from walk.log's session.</what-built>
  <how-to-verify>
    Offer each step as a pass/fail choice, one at a time. Do steps 1-5 before opening the Web tab in this run: its clipboard poller would auto-pair a copied link.
    1. First, `grep -E "WalletKit initialized|MissingPluginException|initialization failed" "$SCRATCH/walk.log"` shows `✅ WalletKit initialized` and neither failure (D-08). Then, in dark mode, the navbar chip reads Connect. Press it: a side drawer opens on the paste field (D-02). Toggle to the QR: it sits on white. Toggle back. Cancel: the drawer closes and the chip reads Connect again.
    2. In Chrome, open react-app.walletconnect.com, pick Ethereum, press Connect and copy the WalletConnect link. In the app, press Connect, use the paste button, then press Connect. The approve drawer appears. Press Allow. The chip reads Disconnect and the dApp shows the wallet address. If plan 01 took option-b, a relay failure here is the known Windows version-string bug: record it as the carried gap.
    3. In the dApp, run personal_sign. The signing drawer appears. Approve it, and the dApp shows a signature.
    4. Press Disconnect. The chip reads Connect and the dApp shows it is disconnected.
    5. Switch to Light mode and repeat step 1. The chip, the drawer, the QR and an error line (paste `hello`, press Connect) are all legible.
    6. Open the Web tab and copy `wc:00@2?relay-protocol=irn&symKey=00`. Exactly one error toast appears within a few seconds, and no second one within 10 s (D-06).
    7. Offline retry, best effort (criterion 4). Quit the app, disable the network, relaunch the same way, and press Connect.
       - If the restart toast appears and the chip reads Retry Connect: re-enable the network and press Retry Connect. The drawer should open.
       - If the drawer or some other error appears instead, record exactly what you saw. The SDK swallows relay errors at startup, so init may not fail offline. The plan-02 unit test then carries criterion 4.
    8. After quitting: `grep -c symKey "$SCRATCH/walk.log"` prints 0. Step 6 does not count here, because the clipboard path logs only the error type.
  </how-to-verify>
  <acceptance_criteria>
    - Steps 1-6 and 8 pass; step 7's outcome is recorded as observed, with no diagnosis beyond what was seen
  </acceptance_criteria>
  <resume-signal>Choose: "all passed", "step N failed: what you saw", or "step 7 not reproducible (init did not fail offline)"</resume-signal>
</task>

</tasks>

<threat_model>
Trust boundary: the live relay and a third-party test dApp. This plan changes no code.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-09 | Information disclosure | walk.log | high | mitigate | The log lives in the scratchpad and never in the repo, and step 8 greps it for symKey |
| T-10-10 | Spoofing | react-app.walletconnect.com | low | accept | Reown's official test dApp. It signs only a personal_sign message, never a transaction, so no funds move |
</threat_model>

<verification>
Task 1's gates are green, and every walk step is recorded in 10-04-SUMMARY.md with Braian's choice.
</verification>

<success_criteria>
ROADMAP criteria 1 and 2 are observed live on x64 Windows. Criterion 2 also rests on plan 01's stub test and walk step 1. Criterion 3 rests on plan 02's test, and criterion 4 on that test plus step 7 as observed. The gates are green on the finished tree.
</success_criteria>

## Artifacts this phase produces

This plan: `10-04-SUMMARY.md` only (gates quoted plus the walk record). Code artifacts: plan 01 (`stubPayOnDesktop`, the reown_walletkit bump), plan 02 (`WalletKitInstance.withInit`, `walletkit_init_test.dart`) and plan 03 (`PairDappDrawer`, `pair_dapp_drawer_test.dart`).

<output>
Create `.planning/phases/10-dapp-connectivity/10-04-SUMMARY.md` (at most 40 lines) when done.
</output>
