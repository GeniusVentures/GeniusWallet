---
phase: 10-dapp-connectivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [test/reown/walletkit_init_test.dart, lib/reown/reown_walletkit_instance.dart, pubspec.yaml, pubspec.lock, packages/local_secure_storage/pubspec.yaml, packages/local_secure_storage/lib/src/local_secure_storage_base.dart]
autonomous: false
requirements: [SCR-06]
estimate: {tokens: 55000, raw_tokens: 55000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "On Windows, macOS and Linux a no-op WalletConnect Pay platform is in place before WalletKit init, so init completes and the log reads WalletKit initialized (D-08, criterion 2)"
    - "On android and iOS the native Pay platform is left in place (D-08)"
    - "On desktop every Pay method except initialize throws UnsupportedError, so no Pay call can fake a success (D-08)"
    - "On option-a: the lock holds reown_walletkit 1.5.1 and reown_core 1.5.1, and the existing Windows wallet still opens after the key store moves to flutter_secure_storage v10 (D-08)"
  artifacts:
    - {path: lib/reown/reown_walletkit_instance.dart, provides: "stubPayOnDesktop() plus a private no-op WalletconnectPayPlatform, called first in WalletKitInstance._internal()"}
    - {path: test/reown/walletkit_init_test.dart, provides: "Pay stub group: installed on desktop, left alone on mobile"}
    - {path: pubspec.yaml, provides: "walletconnect_pay as a pinned direct dependency; reown_walletkit ^1.5.1 on option-a"}
  key_links:
    - {from: "WalletKitInstance._internal", to: "WalletconnectPayPlatform.instance", via: "stubPayOnDesktop() before ReownWalletKit is built"}
    - {from: "ReownWalletKit.init -> WalletConnectPay.init", to: "the no-op platform", via: "Pay reads WalletconnectPayPlatform.instance at call time"}
---

<objective>
WalletKit has not started on any desktop since reown_walletkit 1.4.0. Its `init()` ends in `pay.init()`, and walletconnect_pay
has no desktop plugin, so init throws MissingPluginException. No other plan in this phase can be walked until that is
fixed (D-08). Install a no-op Pay platform on desktop before init and pin it with one unit test. Then, after Braian's
decision, bump reown_walletkit to ^1.5.1 for reown_core 1.5.1's Windows relay fix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/10-dapp-connectivity/10-CONTEXT.md
Every command runs after `export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"`. Set `SCRATCH` to your scratchpad directory, never inside the repo.
Commits land on the phase feature branch, never on develop. `flutter analyze` exits 1 on infos: check `$?`.
Source comments: WHY only, doc comments at most 3 lines, never a plan/phase/decision id or a test file name.
Measured at planning (pub cache `C:\Users\User\AppData\Local\Pub\Cache\hosted\pub.dev\`):
- `WalletConnectPay.init()` reads `WalletconnectPayPlatform.instance` at call time, so any install before `walletKit.init()` works.
- The setter runs `PlatformInterface.verifyToken`. The stub must `extends WalletconnectPayPlatform`, because `implements` fails the token check.
- `depend_on_referenced_packages` is on (lints core.yaml), so importing walletconnect_pay from lib/ needs a direct dependency.
- The lock holds reown_walletkit 1.4.0, reown_core 1.3.8, reown_sign 1.3.9 and walletconnect_pay 1.0.0. Pay's platform interface is identical in 1.0.0 and 1.1.0.
- `flutter test` defaults the target platform to android, so the stub stays out of every existing test.
- Windows keeps the key store in `%APPDATA%\com.example\genius_wallet\flutter_secure_storage.dat` (`.planning/reference/FRESH-INSTALL-RECIPE.md`). Never touch Credential Manager. Quit any running genius_wallet.exe before each `flutter run -d windows`: a second instance dies on the Hive lock.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>WalletKit starts on desktop: a no-op Pay platform is in place before init</name>
  <files>test/reown/walletkit_init_test.dart, lib/reown/reown_walletkit_instance.dart, pubspec.yaml, pubspec.lock</files>
  <read_first>lib/reown/reown_walletkit_instance.dart (full); in the pub cache, walletconnect_pay-1.0.0/lib/walletconnect_pay_platform_interface.dart and walletconnect_pay_method_channel.dart; test/reown/utilities_test.dart (house style)</read_first>
  <behavior>
    - For windows, macOS and linux, set through debugDefaultTargetPlatformOverride: after stubPayOnDesktop() the instance is not a MethodChannelWalletconnectPay, and initialize() completes with true
    - On that stub, getPaymentOptions, getRequiredPaymentActions and confirmPayment each throw UnsupportedError
    - For android and iOS: after stubPayOnDesktop() the instance is still a MethodChannelWalletconnectPay
    - tearDown sets the platform override back to null and the instance to a new MethodChannelWalletconnectPay, so no case leaks into the next
  </behavior>
  <action>Per D-08. Write the test first, as a Pay stub group in the new test/reown/walletkit_init_test.dart. It cannot compile until stubPayOnDesktop exists.
In reown_walletkit_instance.dart:
- Add a private `_NoPayPlatform extends WalletconnectPayPlatform`. initialize returns true. The three payment methods throw `UnsupportedError('WalletConnect Pay is not available on desktop')`.
- Add a public top-level `void stubPayOnDesktop()`. When `!kIsWeb` and `defaultTargetPlatform` is windows, macOS or linux, it sets `WalletconnectPayPlatform.instance = _NoPayPlatform()`. kIsWeb goes first because on web defaultTargetPlatform reports the browser's OS. Use defaultTargetPlatform, not dart:io Platform, so the test can drive it.
- Call it as the first statement of `WalletKitInstance._internal()`. That constructor runs before either caller's initOnce().
- Add one comment of at most 3 lines, starting `ponytail:`. WalletKit runs Pay's init unconditionally and Pay ships no desktop plugin. The fix leans on a public but unexported platform-interface file. Remove it once Reown guards pay.init on unsupported platforms.
- Imports: `package:walletconnect_pay/walletconnect_pay_platform_interface.dart` and foundation. The test also imports `walletconnect_pay_method_channel.dart`.
In pubspec.yaml, add `walletconnect_pay: 1.0.0` beside reown_walletkit. Pin it exactly to the version the lock resolves. Add a one-line WHY comment: the desktop stub imports its platform interface.
Run `flutter pub get`. The lock diff must change only walletconnect_pay's dependency line, to "direct main".</action>
  <verify><automated>flutter test test/reown/walletkit_init_test.dart && flutter analyze lib/reown test/reown; echo "exit=$?"</automated></verify>
  <acceptance_criteria>
    - The test did not compile before stubPayOnDesktop existed and passes after it. analyze prints exit=0
    - `grep -c "ponytail:" lib/reown/reown_walletkit_instance.dart` prints 1, and `grep -c "stubPayOnDesktop();" lib/reown/reown_walletkit_instance.dart` prints 1
    - `git diff pubspec.lock` changes only walletconnect_pay's dependency line
    - End to end: `flutter run -d windows` in the background, tee'd to `$SCRATCH/init.log`. It logs `✅ WalletKit initialized` and no `MissingPluginException`. Then quit the app
  </acceptance_criteria>
  <done>A desktop start reaches "WalletKit initialized", and mobile keeps its native Pay plugin.</done>
  <reversibility rating="reversible">One function, one private class and one pinned dependency; a revert restores today's startup.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Braian decides whether the WalletKit bump may move the key store to flutter_secure_storage v10</name>
  <decision>Bump reown_walletkit to ^1.5.1 now, which moves the wallet key store to flutter_secure_storage v10?</decision>
  <context>D-08 locks the bump for reown_core 1.5.1's fix: relay connectivity fails when the Windows version string contains double quotes, and this machine's does (`"Windows 11 Pro" 10.0 (Build 26200)`). Without it, the Windows pairing walk cannot pass.
But reown_core 1.4.0 and later require flutter_secure_storage ^10, and `packages/local_secure_storage`, which holds the wallets' secrets, pins ^9.2.4. pub cannot resolve unless the key store moves too. On Android the move is one-way: v10 re-encrypts the data on first read, and by default erases all of it on a read error. macOS keeps the same keychain default, and Windows keeps its file store.</context>
  <options>
    <option id="option-a"><name>Bump now, guarded</name><pros>Criterion 2 can pass in this phase. The key store sets `resetOnError: false` and backs up before migrating. The Windows wallet is checked after the upgrade, with its file backed up first</pros><cons>Android's one-way migration is walked only after merge, on the CI develop APK in the emulator</cons></option>
    <option id="option-b"><name>Stub only; the storage upgrade becomes its own phase</name><pros>The key store is untouched here, and its migration gets its own Android walk</pros><cons>Windows pairing stays broken until then. Criterion 2 and walk steps 2-4 are carried as a gap</cons></option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
  <decided>option-a, by Braian on 2026-09-26. Skip this checkpoint and go straight to Task 3.</decided>
</task>

<task type="auto">
  <name>WalletKit 1.5.1 brings the Windows relay fix, and the key store survives the storage upgrade</name>
  <precondition>Braian chose option-a. On option-b, skip this task and record the choice in the SUMMARY.</precondition>
  <files>pubspec.yaml, pubspec.lock, packages/local_secure_storage/pubspec.yaml, packages/local_secure_storage/lib/src/local_secure_storage_base.dart</files>
  <read_first>packages/local_secure_storage/lib/src/local_secure_storage_base.dart (25-45); after pub get, flutter_secure_storage 10.x lib/options/android_options.dart in the pub cache; test/local_wallet_storage_test.dart (1-40)</read_first>
  <action>Per D-08.
- pubspec.yaml: set `reown_walletkit: ^1.5.1`, `walletconnect_pay: 1.1.0` (the version walletkit 1.5.1 resolves) and the dev dependency `flutter_secure_storage: ^10.0.0`.
- packages/local_secure_storage/pubspec.yaml: set `flutter_secure_storage: ^10.0.0`. Leave packages/genius_api's `reown_walletkit: ^1.3.2` alone; it already admits 1.5.1.
- local_secure_storage_base.dart: the AndroidOptions becomes `AndroidOptions(resetOnError: false, migrateWithBackup: true)`.
  - Drop the old Jetpack flag. v10 deprecates and ignores it, and migrates that data on first read by default.
  - Why resetOnError: its default of true would erase every stored wallet on a single read error. reown already turns it off for its own store.
  - Replace the old comment with one line giving that WHY.
- Run `flutter pub get`. If it does not resolve, stop and report the conflict. Never add dependency_overrides. If v10 changed an API that local_wallet_storage_test.dart uses, adapt the test, never the key store's logic.
- From the lock diff, quote these versions in the SUMMARY: reown_walletkit, reown_core, reown_sign, walletconnect_pay, flutter_secure_storage and flutter_secure_storage_windows. Also quote every other package that moved.
- Windows survival check: quit the app, copy `flutter_secure_storage.dat` to `flutter_secure_storage.dat.pre-v10` beside it, then run `flutter run -d windows` tee'd to `$SCRATCH/bump.log`.
  Pass: the dashboard opens with the same wallet (not /landing_screen) and the log shows `✅ WalletKit initialized`; delete the copy. Fail: quit, restore the copy, and stop.</action>
  <verify><automated>flutter test test/reown/walletkit_init_test.dart test/local_wallet_storage_test.dart && flutter analyze; echo "exit=$?"</automated></verify>
  <acceptance_criteria>
    - Both test files pass, and analyze prints exit=0 (no deprecated_member_use from the old flag)
    - `grep -c "resetOnError: false" packages/local_secure_storage/lib/src/local_secure_storage_base.dart` prints 1
    - pubspec.lock shows reown_walletkit 1.5.1, reown_core 1.5.1 and walletconnect_pay 1.1.0
    - The existing Windows wallet opened after the upgrade, and the `.pre-v10` copy is gone
  </acceptance_criteria>
  <done>reown_core 1.5.1 is in the build, and the key store moved to v10 without losing the Windows wallet.</done>
  <reversibility rating="one-way">On Android the first launch re-encrypts the key store, and a v9 build cannot read it back.</reversibility>
</task>

</tasks>

<threat_model>
Trust boundaries: the app to the WalletConnect relay, and the app to the OS secure store that holds the wallets' secrets.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-11 | Denial of service | desktop WalletKit startup | high | mitigate | The no-op Pay platform lets init finish. The unit test and a Windows launch pin it |
| T-10-12 | Tampering | the no-op Pay platform | medium | mitigate | Only initialize succeeds; every payment method throws UnsupportedError. It is installed on desktop only, and the test proves mobile keeps the native plugin |
| T-10-13 | Denial of service (wallet loss) | key store move to flutter_secure_storage v10 | critical | mitigate | A decision checkpoint comes first, then `resetOnError: false` and `migrateWithBackup: true`. The Windows file is backed up before the first v10 launch, and the wallet is checked |
| T-10-SC | Tampering | pub resolution | medium | mitigate | No new package names, only versions already in the lock's graph. No dependency_overrides. The lock diff is quoted |
</threat_model>

<verification>
`dart format --output=none --set-exit-if-changed` on the changed Dart files; `flutter analyze` with `$?` equal to 0; `bash tool/check_brace_style.sh`;
`flutter test test/reown/ test/local_wallet_storage_test.dart`. After `git add`, `git ls-files --eol test/reown/walletkit_init_test.dart` shows `i/lf`.
</verification>

<success_criteria>
Windows logs "WalletKit initialized", mobile keeps native Pay, and one test pins both. On option-a, reown_core 1.5.1 is in the lock and the Windows wallet is intact. On option-b, the choice is recorded.
</success_criteria>

<output>
Create `.planning/phases/10-dapp-connectivity/10-01-SUMMARY.md` (at most 40 lines) when done.
</output>
