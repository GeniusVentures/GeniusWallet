---
phase: 03-gw-component-library
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/components/gw_icon.dart
  - lib/components/buttons/gw_button.dart
  - lib/components/buttons/gw_swap_fab.dart
  - lib/components/cards/gw_card.dart
  - lib/components/cards/gw_gradient_border_card.dart
  - lib/components/cards/gw_token_row.dart
  - lib/components/cards/gw_wallet_card.dart
  - lib/components/inputs/gw_text_field.dart
  - lib/components/inputs/gw_select.dart
  - lib/components/inputs/gw_checkbox.dart
  - lib/components/inputs/gw_switch.dart
  - lib/components/data/gw_animated_number.dart
autonomous: true
requirements: [DS-02]
user_setup: []

must_haves:
  truths:
    - "All 12 core primitive files exist at their verified paths and `flutter analyze` reports 0 errors."
    - "No file that already exists on develop was modified — the port is purely additive."
    - "Nothing reachable from main.dart imports any of the 12, so every existing screen renders exactly as before."
  artifacts:
    - lib/components/gw_icon.dart
    - lib/components/buttons/gw_button.dart
    - lib/components/cards/gw_card.dart
    - lib/components/inputs/gw_text_field.dart
    - lib/components/data/gw_animated_number.dart
  key_links:
    - "`GWWalletCard` builds on `GWCard` and `GWTokenRow` is independent of it — port `gw_card.dart` before `gw_wallet_card.dart` so the import resolves within this plan."
    - "Every one of the 12 consumes ONLY Phase 2's token classes. If any file needs a token that does not exist on develop, STOP — that is a Phase 2 gap, not something to invent here."
    - "`DESIGN_SYSTEM.md` §5.2 lists gw_token_row.dart under `lib/components/data/`. That is stale. The verified path is `cards/gw_token_row.dart`. Follow the path in this plan, not the doc."
---

<objective>
Port the 12 core `gw_*` primitives — the icon API, buttons, cards, inputs and the animated number —
verbatim from the design branch. These are the primitives every later screen phase consumes, and the
ones the existing design gallery already demos, so they land first.

<!-- planner-discipline-allow: WIRE- -->

Purpose: DS-02. These are additive files with zero collision against develop, which is exactly why
this phase is the lowest-risk step in the whole port (ROADMAP "Why this order"). Nothing consumes
them yet; plan 03-07/03-09 makes them inspectable.
Output: 12 new files under `lib/components/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gw-component-library/03-UI-SPEC.md
@.planning/phases/03-gw-component-library/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the icon API and the two buttons</name>
  <files>lib/components/gw_icon.dart, lib/components/buttons/gw_button.dart, lib/components/buttons/gw_swap_fab.dart</files>
  <action>
**Standing port protocol for every task in plans 03-02 through 03-06.** Read it once, apply it to
every file:

1. Copy the file **verbatim** from the READ-ONLY reference worktree
   `C:\Users\User\Documents\Projects\GNUS-compare\GeniusWallet-3514\<same path>` to the same path in
   this repo. Never edit the reference worktree.
2. **Do not "improve" anything.** Not the formatting, not the deprecated API, not the `Key? key`
   super-param style, not a naming inconsistency you notice. This is a port of an adopted design
   system; fidelity to the source is the requirement. If something looks wrong, record it in the
   SUMMARY and leave it.
3. **Do not consolidate, rename, or merge** two primitives that look redundant. The UI-SPEC records
   the known overlaps deliberately (§2.5); picking a winner is a later phase's call.
4. Adapt ONLY what the environment forces: `hive` → `hive_ce` if a file touches storage. **Verified:
   none of the 50 in-scope files imports `hive` at all**, so no storage adaptation is expected in
   this phase. If you hit one, stop and report — it means the scope changed.
5. `grep -n "WIRE-"` each ported file. **Verified: zero WIRE- markers across all 50 in-scope files.**
   If one appears, stop — WIRE-01 forbids it reaching develop.
6. Confirm every `package:genius_wallet/...` import resolves against a file that exists on develop
   today or is landed by this same phase. A forward reference to a file nobody ports is a scope gap —
   report it, do not stub it.
7. Do not modify ANY file that already exists on develop. Not one line. The additive-only rule
   (UI-SPEC §1.1) is the whole reason this phase is low-risk.

**This task's three files:**

- `lib/components/gw_icon.dart` — `GWIcon`, three named constructors: `.material(IconData, {size = 20,
  color, package, semanticLabel})` → `Icon`; `.svg(String, {size = 20, color, package =
  'genius_wallet', semanticLabel})` → `SvgPicture.asset` with `color` applied via
  `ColorFilter.mode(color, BlendMode.srcIn)`; `.png(String, {size = 20, color, package =
  'genius_wallet', semanticLabel})` → `Image.asset`. Uses `flutter_svg` (already a dependency).
- `lib/components/buttons/gw_button.dart` — `GWButton`, 6 variants (`primary`, `secondary`,
  `tertiary`, `ghost`, `destructive`, `gradient`) + an `.icon` constructor; 3 sizes (`sm` 44 / `md` 48
  / `lg` 56, all >=44px); states default/hover/pressed/disabled/loading/expand. Filled brand variants
  render their label in `textOnBrand` (#000B18) — **never white**, that is the WCAG AA contract.
- `lib/components/buttons/gw_swap_fab.dart` — `GWSwapFab`, circular 56px, `brandCta` gradient fill.
  Port it **standalone and unwired**. Its only caller on the design branch is
  `overlay/global_swap_fab_host.dart`, which is Phase-4 nav-shell scope and explicitly excluded from
  this phase (UI-SPEC §1.2). Do not port the host, and do not wire the FAB into anything.

Do **not** port `buttons/gw_ai_fab.dart`, which sits in the same source directory. It imports
`package:genius_wallet/ai/ai_processing_status.dart`, and `lib/ai/` is WIRE-02 out-of-scope for this
milestone entirely.
  </action>
  <verify>
    <automated>
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
test -f lib/components/gw_icon.dart
test -f lib/components/buttons/gw_button.dart
test -f lib/components/buttons/gw_swap_fab.dart
# The excluded AI FAB must NOT have come along for the ride.
test ! -f lib/components/buttons/gw_ai_fab.dart
# No WIRE- stub reached develop (WIRE-01).
test "$(grep -rc 'WIRE-' lib/components/gw_icon.dart lib/components/buttons/ | grep -v ':0$' | wc -l)" -eq 0
flutter analyze
bash tool/verify_additive_boundary.sh
    </automated>
    <human-check>
Deferred to plan 03-09's gallery walk, where these render for the first time. Nothing reachable
imports them yet.
    </human-check>
  </verify>
  <done>
Three files exist at the verified paths, `gw_ai_fab.dart` does not, `flutter analyze` reports 0
errors, and the additive-boundary guard passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Port the four cards</name>
  <files>lib/components/cards/gw_card.dart, lib/components/cards/gw_gradient_border_card.dart, lib/components/cards/gw_token_row.dart, lib/components/cards/gw_wallet_card.dart</files>
  <action>
Apply the standing port protocol from Task 1. Port `gw_card.dart` first — `GWWalletCard` builds on it.

- `cards/gw_card.dart` — `GWCard`, the default container: `space8` padding, `radiusLg`,
  `elevationCard` shadow, optional `onTap`, optional custom `radius` override.
- `cards/gw_gradient_border_card.dart` — `GWGradientBorderCard`, the hero/featured card:
  `gradientBorder` stroke, `glow: bool` (dual cyan/mint glow shadow), `glass: bool` (frosted surface
  that the mesh shows through).
- `cards/gw_token_row.dart` — `GWTokenRow`: 40x40 icon (asset or widget, with a fallback dot on load
  error) + symbol/name + balance/subBalance + optional trailing widget; `InkWell` at `radiusMd`.
  **Path note:** `DESIGN_SYSTEM.md` §5.2's table lists this under `lib/components/data/`. That is doc
  drift — the verified source path is `cards/gw_token_row.dart`. Port to `cards/`.
- `cards/gw_wallet_card.dart` — `GWWalletCard`: icon (30x30, `Image.asset(package: 'genius_wallet')`)
  + name + optional trailing arrow, built on `GWCard` at `radiusPill` with `space12`/`space10`
  padding.
  </action>
  <verify>
    <automated>
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
test -f lib/components/cards/gw_card.dart
test -f lib/components/cards/gw_gradient_border_card.dart
test -f lib/components/cards/gw_token_row.dart
test -f lib/components/cards/gw_wallet_card.dart
# Doc drift not followed: the token row must NOT have landed under data/.
test ! -f lib/components/data/gw_token_row.dart
flutter analyze
bash tool/verify_additive_boundary.sh
    </automated>
    <human-check>
Deferred to plan 03-09's gallery walk.
    </human-check>
  </verify>
  <done>
Four card files exist under `cards/`, none under `data/`, `flutter analyze` reports 0 errors, guard
passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Port the four inputs and the animated number</name>
  <files>lib/components/inputs/gw_text_field.dart, lib/components/inputs/gw_select.dart, lib/components/inputs/gw_checkbox.dart, lib/components/inputs/gw_switch.dart, lib/components/data/gw_animated_number.dart</files>
  <action>
Apply the standing port protocol from Task 1.

- `inputs/gw_text_field.dart` — **three classes in one file**: `GWTextField` (`radiusLg`,
  `surfaceElevated` fill, focus border `brandPrimary` 2px), `GWPasswordField` (adds a show/hide
  toggle, 20px, `textSecondary`), `GWSearchField` (prefix icon + clear button, `radiusXs` on web).
  All three land together; do not split the file.
- `inputs/gw_select.dart` — `GWSelect<T>`, dropdown at `radiusPill` (48px), focus border
  `brandPrimary` 2px; `GWSelectItem` supports `enabled: false`.
- `inputs/gw_checkbox.dart` — `GWCheckbox`: selected = `brandPrimary` fill; unselected = transparent
  + `brandPrimary` 1px border; 48px tap target; supports `tristate`.
- `inputs/gw_switch.dart` — `GWSwitch`: on-state `brandPrimary`, 48px tap target.
- `data/gw_animated_number.dart` — `GWAnimatedNumber`, the animated tabular-figure counter used for
  balances.

The 48px tap targets and the >=44px button sizes are from the design branch's own touch-legibility
pass. They are the contract, not an accident to normalize.
  </action>
  <verify>
    <automated>
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
test -f lib/components/inputs/gw_text_field.dart
test -f lib/components/inputs/gw_select.dart
test -f lib/components/inputs/gw_checkbox.dart
test -f lib/components/inputs/gw_switch.dart
test -f lib/components/data/gw_animated_number.dart
# All three text-field classes came across in the one file.
grep -q 'class GWTextField' lib/components/inputs/gw_text_field.dart
grep -q 'class GWPasswordField' lib/components/inputs/gw_text_field.dart
grep -q 'class GWSearchField' lib/components/inputs/gw_text_field.dart
flutter analyze
bash tool/verify_additive_boundary.sh
    </automated>
    <human-check>
Deferred to plan 03-09's gallery walk.
    </human-check>
  </verify>
  <done>
All five files exist, `gw_text_field.dart` declares all three classes, `flutter analyze` reports 0
errors, guard passes. 12/12 of this plan's files landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference worktree → repo | Source files copied in from an external, untrusted-by-default tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | The 12 ported files | medium | mitigate | `grep -n "WIRE-"` per file (WIRE-01: no demo stub may reach develop). Verified zero markers across all 50 in-scope files at planning time; the check is a tripwire against drift, not a live finding. |
| T-03-05 | Elevation of Privilege | Additive-only rule | high | mitigate | `bash tool/verify_additive_boundary.sh` after every task; `flutter analyze` 0 errors. No collision file is opened. A modified collision file is how this phase would silently break an un-ported screen. |
| T-03-06 | Information Disclosure | `GWButton` label contrast | low | accept | Filled brand variants render `textOnBrand` (#000B18), never white — a WCAG AA contract carried verbatim from source. Not independently re-derived here; observed in plan 03-09's gallery. |
</threat_model>

<verification>
`flutter analyze` reports 0 errors (gate, never evidence). `bash tool/verify_additive_boundary.sh`
exits 0. All 12 files exist at their verified paths; `gw_ai_fab.dart` does not.

These are Dart-only additions with no pubspec change, so the human's running debug session picks them
up with hot reload (`r`) — but there is nothing to see, because nothing imports them. The visual
proof is plan 03-09's gallery. The only observation this plan owes is negative (nothing changed), and
that is folded into plan 03-10's phase walk.
</verification>

<success_criteria>
12 of the phase's 50 in-scope files are on develop, additively, analyze-clean, consuming only Phase 2
tokens, and consumed by nothing.
</success_criteria>

<output>
Create `.planning/phases/03-gw-component-library/03-02-SUMMARY.md` when done. Record any source-file
oddity you deliberately left alone.
</output>
</content>
