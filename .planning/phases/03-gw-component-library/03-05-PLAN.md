---
phase: 03-gw-component-library
plan: 05
type: execute
wave: 3
depends_on: ["03-04"]
files_modified:
  - lib/components/app_screen_view.dart
  - lib/components/app_screen_with_header_desktop.dart
  - lib/components/app_screen_with_header_mobile.dart
  - lib/components/desktop_body_container.dart
  - lib/components/responsive_grid.dart
  - lib/components/scaffold/gw_screen.dart
  - lib/components/bottom_drawer/bottom_drawer.dart
  - lib/components/overlays/gw_bottom_sheet.dart
  - lib/components/overlays/gw_dialog.dart
autonomous: true
requirements: [DS-02]
user_setup: []

must_haves:
  truths:
    - "All 9 layout and overlay files exist at their verified paths and `flutter analyze` reports 0 errors."
    - "`bottom_drawer/bottom_drawer.dart` lands as content chrome only — develop's `responsive_drawer.dart` is unmodified, retaining `useRootNavigator: true`, `enableDrag: true`, and `GeniusBreakpoints.medium` (768)."
    - "Alex's `responsive_drawer.dart` is NOT ported, this phase or any later one — it is the regression findings 13/25/26 describe, not the fix."
    - "None of the 9 nav-shell chrome files came along with the layout port."
  artifacts:
    - lib/components/bottom_drawer/bottom_drawer.dart
    - lib/components/app_screen_view.dart
    - lib/components/scaffold/gw_screen.dart
  key_links:
    - "`app_screen_with_header_desktop/mobile.dart` import `components/registration_header.g.dart`, landed by plan 03-04. That is this plan's only reason for being wave 3."
    - "`BottomDrawer` supplies its own header. It must be shown via develop's existing `ResponsiveDrawer.show()` with `title`/`actions` OMITTED, so `_ResponsiveDrawerScaffold` renders no competing AppBar. Plan 03-09 demos exactly that."
    - "`AppScreenView` and `GWScreen` overlap in intent. Port both verbatim. Consolidating them is a screen phase's call, not this phase's — the UI-SPEC (§2.5) records the ambiguity deliberately rather than picking a winner."
---

<objective>
Port the 9 layout, screen-wrapper and overlay primitives — including `BottomDrawer`, the second of the
two components this phase's findings bind to.

<!-- planner-discipline-allow: 800 -->

Purpose: DS-02, and the binding half of findings 13/25/26. Develop's `responsive_drawer.dart` is
already correct today on all three counts; the regressions exist only in Alex's fork and cannot land
under the additive-only rule. So there is nothing to fix — there is something to prove is already
true, and to bind so Phase 4 does not regress it when it finally wires a drawer into the shell
(UI-SPEC §4.1/§4.4).
Output: 9 new files under `lib/components/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gw-component-library/03-UI-SPEC.md
@.planning/phases/03-gw-component-library/03-04-SUMMARY.md
@.planning/reference/REVIEW_FINDINGS_REDESIGN.md

Target files as they exist today — the correct behavior this plan must NOT disturb:
@lib/components/bottom_drawer/responsive_drawer.dart
@lib/utils/breakpoints.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the six layout and screen-wrapper primitives</name>
  <files>lib/components/app_screen_view.dart, lib/components/app_screen_with_header_desktop.dart, lib/components/app_screen_with_header_mobile.dart, lib/components/desktop_body_container.dart, lib/components/responsive_grid.dart, lib/components/scaffold/gw_screen.dart</files>
  <action>
Apply the standing port protocol from plan 03-02 Task 1 (copy verbatim, improve nothing, grep for
WIRE-, confirm imports resolve, modify no collision file).

- `app_screen_view.dart` — `AppScreenView`: base screen container, `SafeArea` + `CustomScrollView`
  with a `body` sliver and a bottom-pinned `footer` sliver. **Note:** Phase 2's `02-UI-SPEC.md` §9
  described this as "an existing develop widget". That was imprecise and is verified false — the file
  does not exist on develop. It is additive, zero collision, and lands here.
- `app_screen_with_header_desktop.dart` / `app_screen_with_header_mobile.dart` —
  `AppScreenWithHeaderDesktop` / `AppScreenWithHeaderMobile`: screen-level header + body wrappers.
  Both import `components/registration_header.g.dart` (landed by plan 03-04).
  **These are NOT app-shell chrome**, despite the name. Verified by caller trace: on the design branch
  they are imported exclusively by onboarding screens (`legal_screen.dart`, `backup_phrase_screen.dart`,
  `recovery_phrase_screen.dart`, `verify_recovery_phrase_screen.dart`, `import_security_screen.dart`,
  `import_wallet_screen.dart`) and `pin_screen.dart` — all un-ported, Phase 4/6 territory. They land
  as inspectable, unconsumed primitives. Do not wire them into anything.
- `desktop_body_container.dart` — `DesktopBodyContainer`: desktop max-width/padding wrapper. Do NOT
  confuse with `DesktopContainer`, which is a collision file already on develop and is not touched.
- `responsive_grid.dart` — `ResponsiveGrid`: responsive column grid.
- `scaffold/gw_screen.dart` — `GWScreen`: a second, distinct screen wrapper (`SafeArea` + optional
  `AppBar` + max content width for wide viewports). **It overlaps with `AppScreenView` above.** Port
  both verbatim. Neither is ours to consolidate — the UI-SPEC (§2.5) records the overlap deliberately
  rather than silently picking a winner, and that call belongs to whichever screen phase adopts one.

**Do not port any of the 9 nav-shell files** from `components/overlay/`: `desktop_overlay.dart`,
`mobile_overlay.dart`, `desktop_tab_bar.dart`, `genius_tabbar.dart`, `gw_bottom_nav.dart`,
`destinations.dart`, `genius_destination.dart`, `selected_wallet_and_network.dart`,
`global_swap_fab_host.dart`. Verified by caller trace: every one is imported only from `main.dart`
and `bloc/app_bloc.dart` on the design branch — they collectively ARE the shell replacement, which is
Phase 4's scope (NAV-01/NAV-02). Landing them here would sit dead at best and tempt a wire-up at
worst.
  </action>
  <verify>
    <automated>
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
for f in \
  lib/components/app_screen_view.dart \
  lib/components/app_screen_with_header_desktop.dart \
  lib/components/app_screen_with_header_mobile.dart \
  lib/components/desktop_body_container.dart \
  lib/components/responsive_grid.dart \
  lib/components/scaffold/gw_screen.dart ; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done
# The registration header dependency resolves (proves 03-04 landed).
test -f lib/components/registration_header.g.dart
# None of the 9 Phase-4 nav-shell files came along.
for f in desktop_overlay mobile_overlay desktop_tab_bar genius_tabbar gw_bottom_nav destinations genius_destination selected_wallet_and_network global_swap_fab_host ; do
  test ! -f "lib/components/overlay/$f.dart" || { echo "PHASE 4 SCOPE LEAKED: $f.dart"; exit 1; }; done
flutter analyze
bash tool/verify_additive_boundary.sh
    </automated>
    <human-check>
Deferred to plan 03-09's gallery walk, where `AppScreenView` renders for the first time.
    </human-check>
  </verify>
  <done>
Six files exist at their verified paths. Zero nav-shell files leaked into this phase. `flutter
analyze` reports 0 errors; guard passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Port BottomDrawer as content chrome, and bind findings 13/25/26</name>
  <files>lib/components/bottom_drawer/bottom_drawer.dart, lib/components/overlays/gw_bottom_sheet.dart, lib/components/overlays/gw_dialog.dart</files>
  <action>
Apply the standing port protocol from plan 03-02 Task 1.

- `bottom_drawer/bottom_drawer.dart` — `BottomDrawer`: modal **content chrome only**. Centered title
  + left close-X + divider + scrollable `ListView.builder` body + optional footer, `deepBlueTertiary`
  background. It supplies its own header. It is landed unconsumed; plan 03-09 demos it.

- `overlays/gw_bottom_sheet.dart` — `GWBottomSheet`: generic modal bottom sheet helper (`.show()`
  static, title + scroll-controlled + optional handle). **Orphaned — zero callers anywhere on the
  design branch.** Port for completeness. It is NOT the canonical sheet pattern; `ResponsiveDrawer`
  is (`DESIGN_SYSTEM.md` §6.3). Do not treat it as a replacement for anything.
- `overlays/gw_dialog.dart` — `GWDialog`: generic modal dialog helper (title/message/content/actions/
  icon, `.show()` static). **Also orphaned on the source branch.** Same caveat.

**Findings 13, 25 and 26 — confirm, bind, change nothing.**

`lib/components/bottom_drawer/responsive_drawer.dart` is a **collision file this phase does not
touch.** Develop's copy is already correct on all three counts — verified line by line this planning
session, and re-verify it yourself before writing the SUMMARY:
- `showDialog(..., useRootNavigator: useRootNavigator)` and `showModalBottomSheet(...,
  useRootNavigator: useRootNavigator)`, both defaulting `useRootNavigator = true` (line 13) →
  **finding 13 already satisfied.**
- `enableDrag: enableDrag`, default `true` (line 15) → **finding 25 already satisfied.**
- `MediaQuery.sizeOf(context).width >= GeniusBreakpoints.medium` (line 18), where
  `GeniusBreakpoints.medium = 768` (`lib/utils/breakpoints.dart:9`) → **finding 26 already
  satisfied.** Not the hardcoded 800 Alex's fork introduced.

The regressions these findings describe exist only in Alex's fork and, under this phase's
additive-only rule, cannot land. So:

1. **Do NOT port Alex's `responsive_drawer.dart`** — not this phase, not any later phase. It IS the
   regression, not the fix. It is a collision file; leave develop's alone.
2. **Binding rule to record in the SUMMARY, for Phase 4 and every later phase:** whenever a phase
   wires `BottomDrawer` into a real screen's drawer, it must keep calling develop's
   `ResponsiveDrawer.show()` with its current defaults — `useRootNavigator: true`, `enableDrag: true`,
   `GeniusBreakpoints.medium` (768). Never reintroduce Alex's hardcoded `800` or `enableDrag: false`.
   And it must omit `title`/`actions` on the `ResponsiveDrawer.show()` call so
   `_ResponsiveDrawerScaffold` renders no AppBar competing with `BottomDrawer`'s own header.
3. Assert zero diff on `responsive_drawer.dart` and `breakpoints.dart` before finishing.
  </action>
  <verify>
    <automated>
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
test -f lib/components/bottom_drawer/bottom_drawer.dart
test -f lib/components/overlays/gw_bottom_sheet.dart
test -f lib/components/overlays/gw_dialog.dart
# Findings 13/25/26: develop's drawer and breakpoints are untouched.
test -z "$(git diff --name-only -- lib/components/bottom_drawer/responsive_drawer.dart lib/utils/breakpoints.dart)"
# ...and still hold the correct values.
grep -qE 'bool useRootNavigator = true' lib/components/bottom_drawer/responsive_drawer.dart
grep -qE 'bool enableDrag = true' lib/components/bottom_drawer/responsive_drawer.dart
grep -q 'GeniusBreakpoints.medium' lib/components/bottom_drawer/responsive_drawer.dart
grep -qE 'static const double medium = 768' lib/utils/breakpoints.dart
# Alex's regressed drawer did not come across: no hardcoded breakpoint comparison in the drawer dir.
# Scoped to the exact comparison pattern -- a bare grep for the number would match unrelated
# durations/widths and make this gate fire on innocent code.
test "$(grep -rEc 'width >= 800|width > 800' lib/components/bottom_drawer/ | grep -v ':0$' | wc -l)" -eq 0
# Only the expected files exist under bottom_drawer/ -- Alex's responsive_drawer.dart was not added.
test "$(ls lib/components/bottom_drawer/ | wc -l)" -eq 2
flutter analyze
bash tool/verify_additive_boundary.sh
    </automated>
    <human-check>
Deferred to plan 03-09, which opens a real drawer from the gallery via
`ResponsiveDrawer.show(context: context, child: BottomDrawer(...))` and confirms it mounts over the
whole app, swipes down to dismiss at mobile width, and flips to the desktop side-dialog at exactly
768px — ROADMAP criterion 4.
    </human-check>
  </verify>
  <done>
Three files exist. `responsive_drawer.dart` and `breakpoints.dart` show zero diff and still carry
`useRootNavigator = true`, `enableDrag = true` and `medium = 768`. No hardcoded 800 exists anywhere
under `bottom_drawer/`. `flutter analyze` reports 0 errors; guard passes. 9/9 of this plan's files
landed (running total 41/50). Findings 13/25/26's binding rule is recorded in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference worktree → repo | Source files copied in from an external tree |
| Alex's `responsive_drawer.dart` → develop's | A regressed version of a collision file sits one copy command away in the reference tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15 | Tampering | `lib/components/bottom_drawer/responsive_drawer.dart` | high | mitigate | The single most tempting wrong move in this plan: `bottom_drawer/` is the directory being ported into, and Alex's regressed `responsive_drawer.dart` sits right beside `bottom_drawer.dart` in the source tree. Porting it would regress findings 13, 25 and 26 in one commit. Mitigated by zero-diff assertion + positive greps re-asserting all three correct values + a negative grep for the hardcoded 800. |
| T-03-16 | Elevation of Privilege | Phase 4 nav-shell scope | medium | mitigate | The 9 `components/overlay/` shell files sit adjacent to the layout files being ported and carry similar names. Asserted absent by explicit per-file negative test. Landing them would tempt a `main.dart` wire-up, which is NAV-01/NAV-02 scope. |
| T-03-17 | Repudiation | Findings 13/25/26's binding rule | medium | mitigate | The rule is recorded in this plan's SUMMARY, not only in the UI-SPEC, so Phase 4 inherits it from the phase record. Without it, the phase that wires the drawer rediscovers three findings the hard way. |
| T-03-18 | Information Disclosure | `GWBottomSheet` / `GWDialog` orphan status | low | accept | Both are dead code on the source branch. Ported for inventory completeness under the additive rule; flagged as non-canonical so no screen phase adopts them by assuming they are the intended sheet/dialog pattern. |
</threat_model>

<verification>
`flutter analyze` reports 0 errors (gate, never evidence). `bash tool/verify_additive_boundary.sh`
exits 0. `responsive_drawer.dart`, `breakpoints.dart` and every collision file show zero diff. The
three finding values are re-asserted positively, not assumed from the UI-SPEC.

Dart-only additions — hot reload applies them, nothing imports them, nothing changes on screen. The
drawer's real behavioral proof is plan 03-09's gallery, which is where ROADMAP criterion 4 is
observed.
</verification>

<success_criteria>
9 more of the phase's 50 in-scope files are on develop (running total 41/50). `BottomDrawer` is landed
as content chrome with develop's correct `ResponsiveDrawer` untouched, and findings 13/25/26 are
proven already-true and bound for Phase 4.
</success_criteria>

<output>
Create `.planning/phases/03-gw-component-library/03-05-SUMMARY.md` when done. Record findings
13/25/26's binding rule verbatim for Phase 4, with the re-verified line references.
</output>
</content>
