---
phase: 03-gw-component-library
plan: 10
type: execute
wave: 6
depends_on: ["03-08", "03-09"]
files_modified:
  - .planning/phases/03-gw-component-library/03-VERIFICATION.md
autonomous: true
requirements: [DS-02, DS-03, DS-04, GAP-01]
user_setup: []

must_haves:
  truths:
    - "All 6 ROADMAP Phase 3 criteria carry a real observation, or are marked OUTSTANDING/DEFERRED with a reason — a criterion with no observation behind it is FAIL, not PASS."
    - "Criterion 5 is proven by a human walking every un-ported screen with GW_DEV_TOOLS unset and observing zero difference."
    - "The 9 .g.dart files' unverified RENDER status is recorded as an explicit accepted gap, not inferred to PASS from a clean analyzer run."
    - "All 50 in-scope files are reconciled against the authoritative diff, with the count stated."
  artifacts:
    - .planning/phases/03-gw-component-library/03-VERIFICATION.md
  key_links:
    - "`02-VERIFICATION.md`'s rule is inherited verbatim: the forward-port reached 0 analyze errors and still dropped 37 develop behaviors, 3 of them blockers. Recording an unearned PASS here recreates exactly that failure mode."
    - "Criterion 5's walk must run with GW_DEV_TOOLS UNSET — that is the build a user gets, and the one where the gallery and Dev row must be invisible."
    - "The three shadow classes make criterion 5's walk load-bearing: if a Loading, Splash or WalletsOverview import silently repointed, the walk is where it shows up as a visual difference."
---

<objective>
Close Phase 3: run the no-visual-change walk, reconcile the 50 files against the diff, and write the
phase's verification record against all 6 ROADMAP criteria — recording gaps honestly rather than
inferring passes.

Purpose: this is the BLD-02 loop Phase 2 established, applied to Phase 3. `02-VERIFICATION.md` exists
because the forward-port reached 0 `flutter analyze` errors and still dropped 37 develop behaviors.
This document is where that is prevented again — every row carries a real observation or says plainly
that it does not.
Output: `.planning/phases/03-gw-component-library/03-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gw-component-library/03-UI-SPEC.md
@.planning/phases/02-design-tokens-verification-loop/02-VERIFICATION.md
@.planning/phases/03-gw-component-library/03-07-SUMMARY.md
@.planning/phases/03-gw-component-library/03-08-SUMMARY.md
@.planning/phases/03-gw-component-library/03-09-SUMMARY.md
@.planning/phases/03-gw-component-library/03-SHADOW-NAMES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile all 50 files against the diff and run the full mechanical gate set</name>
  <files>.planning/phases/03-gw-component-library/03-VERIFICATION.md</files>
  <action>
Before any human walks anything, prove mechanically that the phase landed what it claimed.

**A. Reconcile the 50.** Re-run the authoritative diff and reconcile it against the tree:

    git diff --diff-filter=A --name-only develop..origin/ui-redesign-3.514 -- lib/components/

That returns **60**. Subtract the 9 nav-shell files (`overlay/desktop_overlay.dart`,
`mobile_overlay.dart`, `desktop_tab_bar.dart`, `genius_tabbar.dart`, `gw_bottom_nav.dart`,
`destinations.dart`, `genius_destination.dart`, `selected_wallet_and_network.dart`,
`global_swap_fab_host.dart` — Phase 4) and `buttons/gw_ai_fab.dart` (WIRE-02, `lib/ai/` out of scope)
= **50 in scope**. Assert each of the 50 exists in this tree, and assert each of the 10 excluded does
**not**. State the count in the document. A file missing from the tree is a file that never landed.

Cross-check the per-plan totals: 03-02 (12) + 03-03 (6) + 03-04 (14) + 03-05 (9) + 03-06 (9) = 50.

**B. Prove the additive rule held across the whole phase.** `git diff --name-only develop..HEAD --
lib/components/` must show only **new** files — no modified collision file. The only permitted
modifications anywhere are `pubspec.yaml`, `lib/navigation/router.dart` and
`lib/test/dev_tools_widget.dart` (the last two must show **0 deletions**), plus the new files under
`lib/dev/`, `tool/`, `assets/images/textures/` and `.planning/`. Record the full modified-file list.

**C. Run the gate set** and record each result:
- `flutter analyze` → 0 errors. **Record it as a gate, never as evidence** — and note explicitly that
  it is not even a gate for the 9 `.g.dart` files (`analysis_options.yaml:5` excludes them).
- `bash tool/verify_additive_boundary.sh` → exits 0. Record all three checks' output: the shadow
  import boundary, the duplicate-class-name census, and the WIRE- grep.
- Assert the three canonical importer counts directly: `components/loading.dart` = **18**,
  `screens/splash.dart` = **1** (`router.dart`), `components/wallet_overview.dart` = **1**
  (`dashboard_screen.dart`).

**Do not write a PASS for any criterion from this task.** Mechanical checks establish that the code is
shaped correctly; they say nothing about runtime behavior. That is the entire lesson of
`02-VERIFICATION.md` and the reason BLD-02 exists.
  </action>
  <verify>
    <automated>
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
# All 50 in-scope files exist.
git diff --diff-filter=A --name-only develop..origin/ui-redesign-3.514 -- lib/components/ \
 | grep -vE "overlay/(desktop_overlay|mobile_overlay|desktop_tab_bar|genius_tabbar|gw_bottom_nav|destinations|genius_destination|selected_wallet_and_network|global_swap_fab_host)\.dart" \
 | grep -v "buttons/gw_ai_fab.dart" > /tmp/inscope50.txt
test "$(wc -l < /tmp/inscope50.txt)" -eq 50
while read -r f; do test -f "$f" || { echo "NEVER LANDED: $f"; exit 1; }; done < /tmp/inscope50.txt
# All 10 excluded files stayed out.
test ! -f lib/components/buttons/gw_ai_fab.dart
for f in desktop_overlay mobile_overlay desktop_tab_bar genius_tabbar gw_bottom_nav destinations genius_destination selected_wallet_and_network global_swap_fab_host ; do
  test ! -f "lib/components/overlay/$f.dart" || { echo "PHASE 4 SCOPE LEAKED: $f"; exit 1; }; done
# Additive rule: no collision file under lib/components/ was modified all phase.
test -z "$(git diff --diff-filter=M --name-only develop..HEAD -- lib/components/)"
# The two wired files are insertion-only.
test "$(git diff --numstat develop..HEAD -- lib/navigation/router.dart | awk '{print $2}')" = "0"
test "$(git diff --numstat develop..HEAD -- lib/test/dev_tools_widget.dart | awk '{print $2}')" = "0"
# Canonical importer counts intact.
test "$(grep -rl 'package:genius_wallet/components/loading.dart' lib/ | wc -l)" -eq 18
test "$(grep -rl 'package:genius_wallet/screens/splash.dart' lib/ | wc -l)" -eq 1
test "$(grep -rl 'package:genius_wallet/components/wallet_overview.dart' lib/ | wc -l)" -eq 1
flutter analyze
bash tool/verify_additive_boundary.sh
    </automated>
    <human-check>
None for this task — it is mechanical by design, and mechanical results are explicitly not evidence
of runtime behavior. Task 2 is where the human observes.
    </human-check>
  </verify>
  <done>
All 50 in-scope files exist; all 10 excluded do not; zero collision files under `lib/components/` were
modified across the whole phase; `router.dart` and `dev_tools_widget.dart` are insertion-only; the
three canonical importer counts are 18/1/1; `flutter analyze` reports 0 errors; the guard's three
checks all pass. Every number is recorded in `03-VERIFICATION.md` with the command that produced it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Walk the app un-gated, then write the verification record against all 6 criteria</name>
  <files>.planning/phases/03-gw-component-library/03-VERIFICATION.md</files>
  <action>
**A. Run criterion 5's walk** (the human-check below), then **B. write the record.**

Structure `03-VERIFICATION.md` on `02-VERIFICATION.md` — read it and follow its shape and its
standards, which this phase inherits:
- The standing run recipe, and the note that `CMAKE_BUILD_TYPE=Release` is deliberate, not a bug.
- The standing environment facts (shared Hive lock with the reference exe; hot reload vs restart).
- **`flutter analyze` is a gate, never evidence** — plus this phase's addition: it is not even a gate
  for the 9 `.g.dart` files.
- **There is no working test harness** (`flutter test` does not compile — APP-02). No tests were
  written or run. Never offer a test as evidence.
- One section per ROADMAP criterion, each with its real observation, or **OUTSTANDING/DEFERRED with
  the reason**. A criterion with no observation behind it is **FAIL, not PASS**.

The 6 criteria and where their evidence comes from:

| # | Criterion | Evidence source |
|---|---|---|
| 1 | `/design_gallery` renders every ported primitive, matching the Release exe | 03-09's walk |
| 2 | `GWCanvasBackground` renders texture, not blank fill; no asset error | 03-07's observation |
| 3 | Every entry renders in light + dark; no QR dark-on-dark | 03-09's appearance walk |
| 4 | Drawer mounts over the app, swipe-dismiss, 768 not 800 | 03-09's drawer walk |
| 5 | Every un-ported screen still renders/behaves as before | **This plan's walk** |
| 6 | `.planning/` records the GAP-01 treatment decision; every primitive it calls for exists in the gallery | 03-08's inventory, **cross-checked against 03-09's built gallery here** |

For criterion 6, do the cross-check now rather than restating 03-08: take every primitive
`03-GAP-INVENTORY.md`'s "mechanically re-skinnable" surfaces call for, and confirm each has a section
in the built gallery. Note the ROADMAP's "12 develop features" is an unverified estimate — record
03-08's real evidenced count and explain the discrepancy rather than forcing the number.

**Record these gaps honestly. Do not let any of them be inferred into a PASS:**

1. **The 9 `.g.dart` files do not render this phase.** Nothing calls `build()` on any of them; every
   real caller is a screen in an un-ported phase (Phase 5/6/7). What this phase DID establish: their
   dependency closure resolves (03-04's mechanical check), the analyzer type-checks every reference
   into them via `lib/dev/generated_closure_canary.dart`, and importing that canary from the gallery
   makes the Dart front-end compile their bodies for real. What it did NOT establish: that any of them
   renders correctly. Record that as an **explicit accepted gap**, the way `02-VERIFICATION.md`
   recorded criterion 1 clause B — not as an inferred pass. Name the phase that closes each.
2. **The three shadow classes** (`Loading`, `Splash`, `WalletsOverview`/`WalletsOverviewState`) are
   landed and unconsumed. Record the guard's role, and that it is **run-on-demand, not CI-enforced** —
   this repo has no pre-commit hooks and its only CI workflow (`.github/workflows/build.yml`) is a
   build matrix that runs no analyze or check step. State that limitation plainly; wiring the guard
   into CI is a real option and a decision for a later phase, not a claim to make here.
3. **Binding rules for later phases**, restated in one place so they are inherited from the phase
   record rather than needing a re-read of the design contract:
   - Findings 13/25/26 → Phase 4: keep calling develop's `ResponsiveDrawer.show()` with
     `useRootNavigator: true`, `enableDrag: true`, `GeniusBreakpoints.medium` (768). Never port Alex's
     `responsive_drawer.dart`.
   - Finding 15 → Phase 5: when `custom_future_builder.dart` is rewritten to consume `GWErrorState`,
     do not collapse to `return error ?? GWErrorState(...)` — keep both the custom error content and
     the retry affordance.
   - Findings 16 / 6 → Phases 7 and 9: the QR quiet-zone background stays a theme-invariant light
     constant, never bound to `textPrimary*` or any appearance-flipping token.
   - WIRE-3 → Phase 7: develop's real recipient validation wins. Never adopt Alex's
     `recipient.length >= 6`.

Finally, update `.planning/REQUIREMENTS.md`'s traceability rows for DS-02, DS-03, DS-04 and GAP-01,
and `.planning/ROADMAP.md`'s Phase 3 status and progress row — **only if** every criterion genuinely
carries an observation. If any is OUTSTANDING, say so and leave the requirement open.
  </action>
  <verify>
    <automated>
test -f .planning/phases/03-gw-component-library/03-VERIFICATION.md
# All 6 criteria have a section.
for n in 1 2 3 4 5 6; do grep -qE "^## Criterion $n" .planning/phases/03-gw-component-library/03-VERIFICATION.md || { echo "MISSING criterion $n"; exit 1; }; done
# The .g.dart render gap is recorded as a gap, not inferred to a pass.
grep -qi 'accepted gap\|not established\|does not prove' .planning/phases/03-gw-component-library/03-VERIFICATION.md
# The analyze-is-not-evidence rule is carried forward.
grep -qi 'gate, never evidence' .planning/phases/03-gw-component-library/03-VERIFICATION.md
# The binding rules for later phases are recorded in one place.
for r in 768 'custom_future_builder' 'quiet' 'WIRE-3'; do grep -q "$r" .planning/phases/03-gw-component-library/03-VERIFICATION.md || { echo "MISSING binding rule: $r"; exit 1; }; done
# The 50-file reconciliation count is stated.
grep -q '50' .planning/phases/03-gw-component-library/03-VERIFICATION.md
    </automated>
    <human-check>
**Criterion 5 — the no-visual-change walk. This is the phase's most important observation.**

The whole phase rests on one claim: the library is additive and nothing consumes it, so **nothing may
change visually**. If landing 50 files altered any existing screen, something is wrong. This walk is
also where a silently repointed shadow import (`Loading`, `Splash`, `WalletsOverview`) would surface —
the compiler and the analyzer cannot see those, but your eyes can.

Close the reference Release exe first (shared Hive lock). Then run the standing recipe **with NO dev
define** — that is the build a user gets:

```
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release -DGENIUS_DEPENDENCY_BRANCH=develop -Dc-ares_DIR=C:/Users/User/Documents/Projects/GNUS/thirdparty/build/Windows/Release/cares/lib/cmake/c-ares" flutter run -d windows --debug
```

Plan 03-01 changed `pubspec.yaml`, so this needs a **full stop and re-run** — hot reload cannot pick
up a pubspec change.

Walk top to bottom, the same route Phase 2 walked, and confirm **zero observable difference** and no
startup exception:
1. **Cold start** → the splash appears and the app reaches the dashboard. (This is the `Splash` shadow
   check — develop's splash, not a different one.)
2. **Dashboard** → balances, holdings, transactions, markets, news all render as before. (This is the
   `WalletsOverview` shadow check.)
3. **Token screens** → token info, send, receive.
4. **Swap**, **Banxa buy**, **Settings**.
5. Any screen that shows a **loading state** — Banxa buy, swap, the news feed, onboarding import.
   Confirm the spinner looks exactly as it did. (This is the `Loading` shadow check: develop's
   `Loading` uses `lightGreenPrimary` and a raw 16 gap; the shadow uses `brandGreen` and
   `GeniusWalletConsts.space8` with `headlineLg` text. **They look different.** If any loading state
   changed appearance, an import repointed.)
6. Confirm the **`Dev` row is absent** and `/design_gallery` has no entry point.
7. Watch the debug console through startup: **no new exception, and no `Unable to load asset`.**

Report what you saw for each step. Anything that looks different, however small, is a finding — report
it rather than deciding it does not matter.
    </human-check>
  </verify>
  <done>
The human has walked every un-ported screen with `GW_DEV_TOOLS` unset and reported zero observable
difference, no startup exception, the `Dev` row absent, and every loading state unchanged.
`03-VERIFICATION.md` records all 6 criteria, each with a real observation or an explicit
OUTSTANDING/DEFERRED reason; the 9 `.g.dart` files' render gap is recorded as an accepted gap; the
guard's run-on-demand (not CI-enforced) limitation is stated plainly; all four binding rules are
recorded for Phases 4, 5, 7 and 9. REQUIREMENTS.md and ROADMAP.md are updated only for criteria that
genuinely passed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification record → the rest of the milestone | Six later phases inherit this document's claims and binding rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-35 | Repudiation | An unearned PASS in `03-VERIFICATION.md` | high | mitigate | The phase's most consequential threat, and the one `02-VERIFICATION.md` was written against: the forward-port reached 0 analyze errors and dropped 37 behaviors, 3 of them blockers. Mitigated by the inherited rule — a criterion with no observation behind it is FAIL, not PASS — plus the explicit requirement to record the `.g.dart` render gap and the guard's CI limitation rather than glossing them. |
| T-03-36 | Spoofing | A silently repointed shadow import | high | mitigate | Criterion 5's walk is the last line of defence and the only one that can see this class of bug at runtime. The walk names the specific screens where each of the three shadows would surface, and states what the visual difference would look like — a generic "walk the app" instruction would not catch it. |
| T-03-37 | Tampering | Premature requirement closure | medium | mitigate | REQUIREMENTS.md/ROADMAP.md are updated only for criteria that genuinely carry an observation. Closing DS-02/03/04 or GAP-01 on the strength of a clean analyzer run would hand Phase 4 a false foundation. |
| T-03-38 | Repudiation | The guard's enforcement reach | medium | accept | Accepted and recorded, not hidden: `tool/verify_additive_boundary.sh` is run-on-demand. This repo has no pre-commit hooks, and `.github/workflows/build.yml` is a build matrix with no analyze or check step. Wiring the guard into CI is a real follow-up option; claiming automatic enforcement it does not have would be worse than naming the limit. |
</threat_model>

<verification>
The phase's whole gate set, re-run one final time and recorded: `flutter analyze` 0 errors (gate,
never evidence — and not even a gate for the 9 `.g.dart` files); `bash tool/verify_additive_boundary.sh`
exits 0 on all three checks; 50/50 in-scope files present; 10/10 excluded absent; zero modified
collision files under `lib/components/`; `router.dart` and `dev_tools_widget.dart` insertion-only;
canonical importer counts 18/1/1.

And the observation that actually matters: a human walked every un-ported screen in a normal build and
saw no difference.
</verification>

<success_criteria>
Phase 3 closes with all 6 ROADMAP criteria carrying real observations — or with any shortfall named
plainly as OUTSTANDING rather than inferred into a pass. DS-02 (50 files), DS-03 (gallery), DS-04
(asset bundled and proven) and GAP-01 (inventory) are each either closed on evidence or left open with
a reason. Every binding rule Phases 4–9 need is recorded in the phase record, not only in the design
contract.
</success_criteria>

<output>
Create `.planning/phases/03-gw-component-library/03-10-SUMMARY.md` when done, alongside
`03-VERIFICATION.md`. If any criterion is OUTSTANDING, say so in the SUMMARY's first line — the
orchestrator needs to see it without reading the record.
</output>
</content>
