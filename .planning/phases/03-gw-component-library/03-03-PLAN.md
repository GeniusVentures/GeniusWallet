---
phase: 03-gw-component-library
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/components/feedback/gw_empty_state.dart
  - lib/components/feedback/gw_error_state.dart
  - lib/components/feedback/gw_loading_state.dart
  - lib/components/loading/gw_spinner.dart
  - lib/components/loading/loading.dart
  - lib/components/effects/gw_mesh_background.dart
autonomous: true
requirements: [DS-02]
user_setup: []

must_haves:
  truths:
    - "All 6 feedback/effect files exist at their verified paths and `flutter analyze` reports 0 errors."
    - "`GWErrorState` renders its retry button whenever `onRetry` is non-null, independent of whether the caller customizes title or message (finding 15's binding rule at the component level)."
    - "`lib/components/loading.dart` — develop's existing file — is byte-for-byte unmodified, and all 18 of its importers still resolve to it."
    - "Nothing outside the design gallery imports `lib/components/loading/loading.dart`."
  artifacts:
    - lib/components/feedback/gw_error_state.dart
    - lib/components/effects/gw_mesh_background.dart
    - lib/components/loading/loading.dart
  key_links:
    - "`loading/loading.dart` declares a SECOND class literally named `Loading`, shadowing develop's `components/loading.dart` (18 importers). Different paths mean no compile error and no analyzer warning. `tool/verify_additive_boundary.sh` (plan 03-01) is the gate; it must pass after this plan."
    - "`gw_loading_state.dart` imports `package:shimmer` — landed by plan 03-01. If it does not resolve, 03-01 did not complete."
    - "`GWMeshBackground` is procedural CustomPaint with NO asset dependency. It is NOT the noise.png consumer — `GWCanvasBackground` (in Phase 2's `theme/genius_wallet_decorations.dart`) is. Do not wire noise.png into this file."
---

<objective>
Port the 6 feedback and effect primitives, including the two the phase's findings bind to
(`GWErrorState`, finding 15) and the one that shadows an existing develop class (`loading/loading.dart`).

<!-- planner-discipline-allow: noise.png -->

Purpose: DS-02. `GWErrorState` and `BottomDrawer` (plan 03-05) are the two primitives that make
findings 13/15/16/25/26 possible for a later phase to regress — this phase's job is to land them with
the correct behavior and prove it, so the phase that eventually consumes them inherits a rule instead
of rediscovering the bug (UI-SPEC §4.4).
Output: 6 new files under `lib/components/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gw-component-library/03-UI-SPEC.md
@.planning/phases/03-gw-component-library/03-01-SUMMARY.md
@.planning/phases/03-gw-component-library/03-SHADOW-NAMES.md
@.planning/REVIEW_FINDINGS_REDESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the three feedback states and the spinner</name>
  <files>lib/components/feedback/gw_empty_state.dart, lib/components/feedback/gw_error_state.dart, lib/components/feedback/gw_loading_state.dart, lib/components/loading/gw_spinner.dart</files>
  <action>
Apply the standing port protocol from plan 03-02 Task 1 (copy verbatim, improve nothing, adapt only
what the environment forces, grep for WIRE-, confirm imports resolve, modify no collision file).

- `feedback/gw_empty_state.dart` — `GWEmptyState`: 72px icon-in-circle (`GWDecorations.surfaceSheen`
  background, `borderSubtle`), `titleLg` heading, optional `bodyMd`/`textSecondary` message, optional
  secondary `GWButton` action. Default icon `Icons.inbox_outlined`. No default title — it is a
  caller-supplied required param.
- `feedback/gw_error_state.dart` — **two classes in one file**: `GWErrorState` (the same 72px
  icon-in-circle pattern at `statusError` @12% background, `titleLg` heading defaulting to
  `'Something went wrong'`, optional message, optional primary `GWButton` retry via
  `onRetry`/`retryLabel` defaulting to `'Retry'` with a leading refresh icon) and `GWErrorBanner`
  (the inline dismissible variant). Both land together.
- `feedback/gw_loading_state.dart` — `GWLoadingState`, the full-screen/area loader. Imports
  `package:shimmer`, landed by plan 03-01.
- `loading/gw_spinner.dart` — `GWSpinner`: indeterminate, 18x18 default, 2px stroke, `brandPrimary`.

**Path note for all three feedback files:** `DESIGN_SYSTEM.md` §5.4 documents them under
`lib/components/loading/`. That is doc drift. The verified source path is `feedback/`. Port to
`feedback/`. `gw_spinner.dart` genuinely does live under `loading/`.

**Finding 15's component-level contract — confirm, do not change.** After porting
`gw_error_state.dart`, read it and confirm that `GWErrorState` renders its retry button whenever
`onRetry` is non-null, regardless of whether the caller also supplies a custom `title` or `message`.
This is already true in the source; you are confirming the port carried it, not fixing anything.
Record the confirmation in the SUMMARY.

**Do not touch `lib/components/custom_future_builder.dart`.** Finding 15 names that file, and
develop's copy is **already correct today** — it renders `error ?? Icon(...)` AND, independently, the
retry button (`if (onRetry != null) ... ElevatedButton ...`), both, not either/or. The regression the
finding describes exists only in Alex's fork and cannot land under this phase's additive-only rule.
The finding's binding rule — recorded for whichever later phase rewrites that file to consume
`GWErrorState`: it must not collapse to `return error ?? GWErrorState(...)`, which would drop
`onRetry` whenever a caller supplies a custom error widget. Keep both the custom error content and
the retry affordance visible together. Restate this in the SUMMARY so Phase 5 inherits it.
  </action>
  <verify>
    <automated>
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
test -f lib/components/feedback/gw_empty_state.dart
test -f lib/components/feedback/gw_error_state.dart
test -f lib/components/feedback/gw_loading_state.dart
test -f lib/components/loading/gw_spinner.dart
# Doc drift not followed: the feedback states must NOT have landed under loading/.
test ! -f lib/components/loading/gw_error_state.dart
test ! -f lib/components/loading/gw_empty_state.dart
test ! -f lib/components/loading/gw_loading_state.dart
# Both classes came across in the one error file.
grep -q 'class GWErrorState' lib/components/feedback/gw_error_state.dart
grep -q 'class GWErrorBanner' lib/components/feedback/gw_error_state.dart
# shimmer resolves (proves 03-01 landed).
grep -q "package:shimmer" lib/components/feedback/gw_loading_state.dart
# Finding 15's file is untouched on develop.
test -z "$(git diff --name-only -- lib/components/custom_future_builder.dart)"
flutter analyze
bash tool/verify_additive_boundary.sh
    </automated>
    <human-check>
Deferred to plan 03-09, which demos `GWErrorState` with both a `message` and an `onRetry` supplied
and confirms the Retry button renders.
    </human-check>
  </verify>
  <done>
Four files exist under `feedback/` and `loading/` at their verified (not documented) paths;
`gw_error_state.dart` declares both classes; `custom_future_builder.dart` has zero diff; `flutter
analyze` reports 0 errors; guard passes. Finding 15's binding rule is restated in the SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Port the mesh background and the shadowed Loading duplicate</name>
  <files>lib/components/effects/gw_mesh_background.dart, lib/components/loading/loading.dart</files>
  <action>
Apply the standing port protocol from plan 03-02 Task 1.

- `effects/gw_mesh_background.dart` — `GWMeshBackground`: an animated `CustomPaint` mesh of 3
  drifting radial blobs (`brandPrimary`/`brandSecondary`/`brandTertiary`) over `surfaceBase`, 36s
  loop, `intensity` 0–1, vignette overlay. **It is pure procedural paint with no asset dependency.**
  The ROADMAP's criterion-2 wording ("`gw_mesh_background` renders its texture") is misleading: the
  actual `noise.png` consumer is `GWCanvasBackground`, which lives in Phase 2's
  `lib/theme/genius_wallet_decorations.dart` and is proven in plan 03-07. Do not wire `noise.png`
  into this file, and do not "fix" the absence of a texture reference.

- `loading/loading.dart` — **the shadow file. Read `03-SHADOW-NAMES.md` before touching this.**

  This is not a new component. It is a re-skinned duplicate of one develop already has. Develop's
  `lib/components/loading.dart` declares `class Loading extends StatelessWidget` with an identical
  `{String? text}` constructor and is imported by **18 files** — verified, among them
  `custom_future_builder.dart`, `banxa_orders_history.dart`, `banxa_payment.dart`, `checkout_qr.dart`,
  `user_kyc/kyc_registration.dart`, `wallet_routes.dart`, `swap_screen.dart` and
  `screens/loading_screen.dart`. This additive file declares a **second class also literally named
  `Loading`** at a different path, using Phase 2 tokens (`GeniusWalletColors.brandGreen`,
  `GeniusWalletConsts.space8`, `GeniusWalletTypography.headlineLg`) where develop's uses raw values
  (`lightGreenPrimary`, raw `16`, default `TextStyle`).

  Different import paths mean **no compile error and no analyzer warning** — a repointed import
  compiles and silently re-skins a loading state. It would detonate in Phase 5/6/9, not here.

  Rules for this file:
  1. Port it verbatim to `lib/components/loading/loading.dart`. Do not rename the class. A later
     phase migrating callers expects `Loading` at this path.
  2. Do **not** delete, move, or edit `lib/components/loading.dart`. Alex's branch deleted it; this
     phase does not. Its 18 callers keep resolving to it.
  3. Do **not** repoint a single one of those 18 imports. Not one. This file is unconsumed until a
     later phase deliberately migrates callers to it.
  4. Add a head comment to the NEW file stating: it duplicates the class name of
     `lib/components/loading.dart`; the canonical file for all 18 existing callers is the old path;
     migrating a caller is a deliberate act that requires updating
     `.planning/phases/03-gw-component-library/03-SHADOW-NAMES.md` and re-running
     `tool/verify_additive_boundary.sh` in the same commit. Point at both by name.
  5. Run the guard. It pins the exact 18-file canonical importer set and asserts nothing outside the
     allowlist imports the shadow path. It must exit 0.
  </action>
  <verify>
    <automated>
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
test -f lib/components/effects/gw_mesh_background.dart
test -f lib/components/loading/loading.dart
# The mesh background is procedural: it must NOT reference the texture asset.
test "$(grep -c 'noise.png' lib/components/effects/gw_mesh_background.dart)" -eq 0
# Develop's canonical Loading is untouched and still exists.
test -f lib/components/loading.dart
test -z "$(git diff --name-only -- lib/components/loading.dart)"
# All 18 canonical importers still resolve to the old path.
test "$(grep -rl 'package:genius_wallet/components/loading.dart' lib/ | wc -l)" -eq 18
# Nothing imports the shadow path yet (the gallery lands in 03-09).
test "$(grep -rl 'package:genius_wallet/components/loading/loading.dart' lib/ | wc -l)" -eq 0
# The shadow file carries its warning head comment.
grep -q 'SHADOW-NAMES' lib/components/loading/loading.dart
flutter analyze
bash tool/verify_additive_boundary.sh
    </automated>
    <human-check>
Deferred to plan 03-10's phase walk, which confirms every screen that renders a loading state
(Banxa buy, swap, news, onboarding import) is visually unchanged — i.e. still resolving to develop's
`Loading`, not the shadow.
    </human-check>
  </verify>
  <done>
Both files exist. `lib/components/loading.dart` has zero diff and retains exactly 18 importers. The
shadow path has zero importers. The shadow file carries a head comment pointing at
`03-SHADOW-NAMES.md` and the guard. `flutter analyze` reports 0 errors; the guard exits 0. 6/6 of
this plan's files landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference worktree → repo | Source files copied in from an external tree |
| shadow class → 18 existing callers | A second `Loading` class enters a namespace 18 reachable files already depend on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Spoofing | `loading/loading.dart`'s `Loading` class | high | mitigate | The phase's headline hazard: a widget impersonating another, invisible to both compiler and analyzer, detonating three phases later. Mitigated by (a) `tool/verify_additive_boundary.sh` pinning the exact 18-file canonical importer set and allowlisting the shadow path, (b) a head comment on the shadow file naming the guard and the baseline doc, (c) an explicit zero-importer assertion in this plan's verify. |
| T-03-08 | Tampering | `lib/components/loading.dart` (collision file) | high | mitigate | `git diff --name-only` asserted empty. Deleting or editing it — as Alex's branch did — would break 18 reachable callers at once. |
| T-03-09 | Repudiation | Finding 15's binding rule | medium | mitigate | The rule is restated in this plan's SUMMARY, not only in the UI-SPEC, so the phase that rewrites `custom_future_builder.dart` inherits it from the phase record rather than needing to re-read a design contract. |
| T-03-10 | Denial of Service | `package:shimmer` in `gw_loading_state.dart` | low | accept | Unreachable from main.dart this phase; not compiled into any user-facing path. Legitimacy handled by T-03-SC in plan 03-01. |
</threat_model>

<verification>
`flutter analyze` reports 0 errors (gate, never evidence — and note it is structurally blind to the
`Loading` shadow, which is why the guard exists). `bash tool/verify_additive_boundary.sh` exits 0.
`lib/components/loading.dart` and `lib/components/custom_future_builder.dart` both show zero diff.

Dart-only additions, no pubspec change — hot reload (`r`) applies them, but nothing imports them so
nothing changes on screen. That negative observation is folded into plan 03-10's phase walk.
</verification>

<success_criteria>
6 more of the phase's 50 in-scope files are on develop. `GWErrorState` carries finding 15's contract
and it is confirmed by reading, not assumed. The `Loading` shadow is landed, unconsumed, gated, and
documented — and develop's 18 callers are provably untouched.
</success_criteria>

<output>
Create `.planning/phases/03-gw-component-library/03-03-SUMMARY.md` when done. Restate finding 15's
binding rule for Phase 5, and record the confirmed 18-importer count.
</output>
</content>
