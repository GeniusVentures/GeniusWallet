---
phase: 03-gw-component-library
plan: 09
type: execute
wave: 5
depends_on: ["03-07"]
files_modified:
  - lib/dev/design_gallery_screen.dart
autonomous: true
requirements: [DS-03]
user_setup: []

must_haves:
  truths:
    - "The gallery renders a section for every primitive ROADMAP criterion 1 names, plus CryptoAddressQR, GWSwapFab, GWDialog and GWBottomSheet."
    - "Every gallery entry renders correctly in both light and dark appearance, toggled live via the same GWAppearance mechanism Phase 2's token probe uses."
    - "The QR quiet zone stays light in BOTH appearance modes — no dark-on-dark (finding 16)."
    - "A drawer opened from the gallery mounts over the whole app, swipes down to dismiss at mobile width, and flips to the desktop side-dialog at exactly 768px — not 800 (findings 13, 25, 26)."
    - "GWErrorState renders its Retry button when both a message and an onRetry are supplied (finding 15)."
  artifacts:
    - lib/dev/design_gallery_screen.dart
  key_links:
    - "The drawer MUST be opened via develop's existing, unmodified `ResponsiveDrawer.show(context: context, child: BottomDrawer(...))`, with `title`/`actions` OMITTED so `_ResponsiveDrawerScaffold` renders no AppBar competing with BottomDrawer's own header. This is UI-SPEC §4.1's load-bearing decision."
    - "The appearance toggle is `GWAppearance.instance` + `ValueListenableBuilder<GWAppearanceMode>`, exactly as Phase 2's token probe does it. Without the ValueListenableBuilder, setMode() persists but nothing rebuilds and the flip looks broken — Phase 2 learned this."
    - "The gallery is the ONLY file allowed to import the `Loading` and `Splash` shadow paths. Adding those imports here is what makes plan 03-01's allowlist non-empty; the guard must still pass afterward."
---

<objective>
Extend the gallery with a section for every primitive ROADMAP criterion 1 names, add the appearance
toggle, and stage the three finding-bound demos (drawer, QR, error state) so criteria 1, 3 and 4 can
be observed rather than assumed.

Purpose: DS-03's whole point is that a human can confirm fidelity against the Release exe before any
screen depends on these primitives. The ported gallery covers 14 sections; criterion 1 explicitly
names several that have none. This plan closes that, and turns findings 13/15/16/25/26 from
paper rules into observed behavior.
Output: an extended `lib/dev/design_gallery_screen.dart`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gw-component-library/03-UI-SPEC.md
@.planning/phases/03-gw-component-library/03-07-SUMMARY.md
@.planning/phases/03-gw-component-library/03-05-SUMMARY.md
@.planning/REVIEW_FINDINGS_REDESIGN.md

The Phase 2 patterns to copy — appearance toggle and probe structure:
@lib/dev/token_probe_screen.dart
@lib/components/bottom_drawer/responsive_drawer.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the missing primitive sections and the appearance toggle</name>
  <files>lib/dev/design_gallery_screen.dart</files>
  <action>
Extend the ported gallery following **its own existing pattern** — `_Section(title:, child:)` inside
the top-level `ListView`, token-driven styling throughout. Do not restructure the file.

**A. Add the appearance toggle.** Wrap the gallery body in
`ValueListenableBuilder<GWAppearanceMode>` on `GWAppearance.instance` and add a toggle control in the
`AppBar` actions, exactly as Phase 2's `lib/dev/token_probe_screen.dart` does it — read that file and
copy the mechanism, do not invent a second one. Phase 2's plan 02-05 recorded the trap: without the
`ValueListenableBuilder`, `setMode()` persists but nothing rebuilds and the flip looks broken.

**B. Add a `_Section` for each primitive criterion 1 names that has none.** Section headings are plain
component names, matching the file's existing convention (UI-SPEC §9's copy contract):

| Section heading | Renders |
|---|---|
| `Icons` | `GWIcon.material`, `GWIcon.svg`, `GWIcon.png` — one of each, so all three constructors are exercised |
| `Token row` | `GWTokenRow` — with an icon, and a second instance with a deliberately broken asset path to show the fallback dot |
| `Wallet card` | `GWWalletCard` — with and without the trailing arrow |
| `Swap FAB` | `GWSwapFab` — standalone, unwired |
| `Empty / Error / Loading states` | `GWEmptyState` (default `Icons.inbox_outlined`); `GWErrorState`; `GWErrorBanner`; `GWLoadingState` (shimmer) |
| `Mesh background` | `GWMeshBackground` — procedural, no asset. Distinct from the `Canvas background` section plan 03-07 added |
| `Screen wrappers` | `AppScreenView` — in a constrained box so it does not swallow the page. Note in a comment that `GWScreen` overlaps it by design (UI-SPEC §2.5) |
| `Dialog / Bottom sheet` | Buttons invoking `GWDialog.show()` and `GWBottomSheet.show()`. Label both as orphaned/non-canonical — `ResponsiveDrawer` is the canonical pattern |
| `Loading (duplicate)` | The **shadow** `Loading` from `components/loading/loading.dart` |
| `Splash (duplicate)` | The **shadow** `Splash` from `components/splash.dart`, in a constrained box |

**The `Loading` and `Splash` sections need the shadow imports.** The gallery is the only file plan
03-01's guard allowlists for them. Add a comment above each import naming
`.planning/phases/03-gw-component-library/03-SHADOW-NAMES.md` and stating that this is the sole
permitted call site, that develop's canonical class at the other path is what every real caller uses,
and that these two sections exist so a human can **see that the duplicate is a different widget** —
which is precisely the evidence that makes the shadow hazard concrete rather than theoretical. Run the
guard afterward; it must still pass.

**Token discipline (UI-SPEC §8, checker-verified):** every addition uses `GeniusWalletConsts` for
spacing, `GeniusWalletTypography` for type, `GeniusWalletColors`/`GeniusWalletGradient`/
`GeniusWalletDecorations` for color. **Zero new token values, no raw hex, no ad hoc `Color(0x...)`, no
raw px typography literal.** If a section seems to need a token that does not exist, stop — that is a
Phase 2 gap, not something to invent here.
  </action>
  <verify>
    <automated>
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
# Every primitive criterion 1 names has a section.
for s in GWIcon GWTokenRow GWWalletCard GWSwapFab GWEmptyState GWErrorState GWLoadingState GWMeshBackground AppScreenView GWDialog GWBottomSheet ; do
  grep -q "$s" lib/dev/design_gallery_screen.dart || { echo "NO SECTION: $s"; exit 1; }; done
# The appearance toggle uses Phase 2's mechanism, not a new one.
grep -q 'ValueListenableBuilder' lib/dev/design_gallery_screen.dart
grep -q 'GWAppearance' lib/dev/design_gallery_screen.dart
# The shadow imports are present, commented, and the guard still passes with the allowlist.
grep -q 'package:genius_wallet/components/loading/loading.dart' lib/dev/design_gallery_screen.dart
grep -q 'package:genius_wallet/components/splash.dart' lib/dev/design_gallery_screen.dart
grep -q 'SHADOW-NAMES' lib/dev/design_gallery_screen.dart
# Token discipline: no raw color literals.
test "$(grep -cE 'Color\(0x' lib/dev/design_gallery_screen.dart)" -eq 0
flutter analyze
bash tool/verify_additive_boundary.sh
    </automated>
    <human-check>
Folded into Task 2's single run.
    </human-check>
  </verify>
  <done>
Every primitive ROADMAP criterion 1 names has a `_Section`; the appearance toggle uses Phase 2's
`GWAppearance` + `ValueListenableBuilder` mechanism; both shadow sections exist with comments naming
`03-SHADOW-NAMES.md`; no raw color literal; `flutter analyze` reports 0 errors; the guard passes with
the gallery on the shadow allowlist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Stage the three finding-bound demos, then walk the gallery against the Release exe</name>
  <files>lib/dev/design_gallery_screen.dart</files>
  <action>
Three sections that exist specifically to make findings 13/15/16/25/26 observable rather than asserted.

**A. `Drawer` (findings 13, 25, 26).** A button that opens:

    ResponsiveDrawer.show(
      context: context,
      child: BottomDrawer(title: 'Drawer demo', children: [ ...several rows, enough to scroll... ]),
    )

Use develop's **existing, unmodified** `ResponsiveDrawer.show()`. **Omit `title` and `actions`** on the
`ResponsiveDrawer.show()` call so `_ResponsiveDrawerScaffold` renders no AppBar competing with
`BottomDrawer`'s own header — UI-SPEC §4.1's load-bearing decision. Do **not** import or port Alex's
`responsive_drawer.dart`: it is the regression, not the fix.

**B. `QR` (finding 16).** Render develop's existing `CryptoAddressQR` (`lib/components/qr/crypto_address_qr.dart`)
with a sample address. That file is a collision file and stays untouched — its QR background is
`Colors.white.withValues(alpha: 0.8)`, theme-invariant and already correct. This section passes by
construction; the point is that it must be **observed in both modes, not assumed.**

**C. `Error state with retry` (finding 15).** A `GWErrorState` with **both** a custom `message` **and**
an `onRetry` supplied, so the Retry button's presence alongside custom content is visible. That is
the component-level contract finding 15 turns on.

Then walk it.

**Fidelity protocol (UI-SPEC §5.3).** For each section, compare against the reference Release exe at
`GeniusWallet-3514\build\windows\x64\runner\Release\genius_wallet.exe` — its equivalent gallery
section or the screen that uses the component. Confirm colors, spacing, radius, typography and
elevation match, in both appearance modes. **The two builds cannot run at the same time** — they share
a Hive data directory and deadlock on file locks. Close one before starting the other, every time you
switch.
  </action>
  <verify>
    <automated>
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
grep -q 'ResponsiveDrawer.show' lib/dev/design_gallery_screen.dart
grep -q 'BottomDrawer' lib/dev/design_gallery_screen.dart
grep -q 'CryptoAddressQR' lib/dev/design_gallery_screen.dart
grep -q 'onRetry' lib/dev/design_gallery_screen.dart
# Findings 13/25/26: develop's drawer, breakpoints and QR are still untouched.
test -z "$(git diff --name-only develop..HEAD -- lib/components/bottom_drawer/responsive_drawer.dart lib/utils/breakpoints.dart lib/components/qr/crypto_address_qr.dart)"
# Alex's regressed drawer was not imported.
test "$(grep -c 'bottom_drawer/responsive_drawer' lib/dev/design_gallery_screen.dart)" -eq 1
flutter analyze
bash tool/verify_additive_boundary.sh
    </automated>
    <human-check>
**Close the develop debug build before opening the reference exe, and vice versa — every switch.**
They share a Hive data directory and deadlock on file locks (`02-VERIFICATION.md`, environment fact 1).

Run the standing recipe with the dev define (a hot reload `r` suffices if a session from plan 03-07 is
still up — this plan changes only Dart, no pubspec):

```
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release -DGENIUS_DEPENDENCY_BRANCH=develop -Dc-ares_DIR=C:/Users/User/Documents/Projects/GNUS/thirdparty/build/Windows/Release/cares/lib/cmake/c-ares" flutter run -d windows --debug --dart-define=GW_DEV_TOOLS=true
```

Open `Dev` → `Gallery`, then confirm:

**Criterion 1 — every primitive renders and matches.** Walk every section top to bottom. Nothing
blank, no red error box, no missing-asset placeholder. Then open the reference Release exe (close the
develop build first) and compare section by section: same colors, spacing, radius, typography,
elevation.

**Criterion 3 — light and dark.** Toggle appearance in the AppBar. Confirm every section renders
correctly in **both** modes — then re-walk. Specifically:
- **The `QR` section's quiet zone stays LIGHT in both modes.** The QR modules are black; if the
  background ever goes dark you get dark-on-dark and the code stops scanning. This is finding 16, and
  finding 6 (Banxa, Phase 9) rides on the same rule.

**Criterion 4 — the drawer.** Press the `Drawer` section's button, then confirm all three:
- It mounts **over the whole app**, not clipped inside the gallery's own navigator (finding 13).
- At mobile width it can be **swiped down to dismiss** (finding 25).
- **Resize the window across 768px** and confirm it flips to the desktop side-dialog at **exactly 768,
  not 800** (finding 26). Drag slowly through the boundary — the whole point is the exact number.

**Finding 15.** In `Error state with retry`, confirm the **Retry button renders** alongside the custom
message.

Report what you saw for each. If any section does not match the reference, describe the difference —
do not judge whether it matters; that is the fidelity record's job.
    </human-check>
  </verify>
  <done>
Every gallery section renders and matches the Release exe in both appearance modes. The QR quiet zone
is observed light in both modes. The drawer mounts over the whole app, swipes to dismiss, and flips at
768. `GWErrorState` shows Retry alongside a custom message. `responsive_drawer.dart`,
`breakpoints.dart` and `crypto_address_qr.dart` all show zero diff against develop.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gallery → shadow classes | The gallery is the only permitted importer of the `Loading` and `Splash` shadow paths |
| dev-gated surface → normal build | The extended gallery must stay unreachable without the dev define |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-31 | Tampering | Alex's `responsive_drawer.dart` | high | mitigate | The drawer section is exactly where an executor is most tempted to reach for Alex's version. Mitigated by zero-diff assertion on develop's copy + a grep bounding the gallery to a single drawer import + a human observation of all three finding behaviors (root-navigator mount, swipe-dismiss, 768 flip). |
| T-03-32 | Spoofing | Shadow imports entering the gallery | medium | mitigate | This plan is what makes the allowlist non-empty. Both imports are commented with the baseline doc; the guard is re-run and must still pass, confirming the allowlist bounds them to this one file and no others drifted in. |
| T-03-33 | Information Disclosure | QR quiet-zone contrast | medium | mitigate | Observed light in both appearance modes. Passes by construction (the file is untouched and theme-invariant) but is observed anyway — UI-SPEC §4.3 requires it be seen, not assumed. **Binding rule for Phase 7 (SCR-03) and Phase 9 (finding 6): the QR background must stay a theme-invariant light constant, never bound to `textPrimary*` or any appearance-flipping token.** |
| T-03-34 | Repudiation | Fidelity claims vs the reference exe | medium | mitigate | Each section is compared side by side against the Release exe rather than judged from memory, and differences are reported as observations rather than adjudicated by the executor. |
</threat_model>

<verification>
`flutter analyze` reports 0 errors (gate, never evidence). `bash tool/verify_additive_boundary.sh`
exits 0 with the gallery now on the shadow allowlist. `responsive_drawer.dart`, `breakpoints.dart` and
`crypto_address_qr.dart` show zero diff against develop.

ROADMAP criteria 1, 3 and 4 are observed here. Criterion 5 (un-ported screens unchanged) and
criterion 6 (GAP-01) are plan 03-10's. Nothing in this plan is recorded as passed on the strength of a
clean analyzer run.
</verification>

<success_criteria>
DS-03 is complete: every primitive the ROADMAP names is inspectable in one place, in both appearance
modes, side by side against the design's own Release exe. Findings 13/15/16/25/26 are observed correct
rather than asserted correct.
</success_criteria>

<output>
Create `.planning/phases/03-gw-component-library/03-09-SUMMARY.md` when done. Record the human's
observation for each of criteria 1, 3 and 4 and for finding 15, plus any fidelity difference found —
verbatim, unadjudicated.
</output>
</content>
