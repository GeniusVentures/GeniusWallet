---
phase: 03-gw-component-library
plan: 06
type: execute
wave: 3
depends_on: ["03-02", "03-04"]
files_modified:
  - lib/components/qr_scanner/gw_qr_scanner.dart
  - lib/components/currency_dropdown.dart
  - lib/components/custom_drop_down.dart
  - lib/components/coins/view/coin_card_container.dart
  - lib/components/sgnus/sgnus_connected_dropdown.dart
  - lib/components/sgnus/sgnus_wallet.dart
  - lib/components/number_pad.dart
  - lib/components/splash.dart
  - lib/components/toast/ticker_provider.dart
autonomous: true
requirements: [DS-02]
user_setup: []

must_haves:
  truths:
    - "All 9 specialist files exist at their verified paths and `flutter analyze` reports 0 errors."
    - "`lib/screens/splash.dart` — develop's, and the app's actual boot screen — is unmodified, and `router.dart:30` still imports it."
    - "`GWQrScanner` lands parse-only: `extractWalletAddress` parses plain / EIP-681 / EIP-681-transfer / BIP-21 forms and does NOT validate. Validation is WIRE-3, out of scope."
    - "`currency_dropdown.dart` wraps `genius_api`'s `Currency`, NOT `lib/preferences/gw_currency.dart` (WIRE-7, out of scope)."
  artifacts:
    - lib/components/qr_scanner/gw_qr_scanner.dart
    - lib/components/splash.dart
    - lib/components/sgnus/sgnus_wallet.dart
  key_links:
    - "`currency_dropdown.dart` imports `components/inputs/gw_select.dart` (plan 03-02) and `sgnus/sgnus_wallet.dart` imports `components/wallet_preview.g.dart` (plan 03-04). Those two imports are this plan's entire reason for being wave 3."
    - "`components/splash.dart` declares a SECOND class named `Splash`, shadowing `lib/screens/splash.dart` — whose single importer is `router.dart:30`, the app's boot path. Alex DELETED `screens/splash.dart` on his branch; this phase does not."
    - "`gw_qr_scanner.dart` imports `package:mobile_scanner`, landed by plan 03-01. If it does not resolve, 03-01 did not complete."
---

<objective>
Port the last 9 in-scope files — the QR scanner, the two dropdowns, the SGNUS pair, and four
miscellaneous helpers including the third and final shadow file. This plan closes DS-02's 50-file set.

<!-- planner-discipline-allow: WIRE- -->

Purpose: DS-02. These are the specialist primitives whose eventual callers are collision files in
Phases 5/7/9/10. They land dormant and dependency-closed, so the phase that finally touches those
screens finds the primitive already reviewed.
Output: 9 new files under `lib/components/`, completing 50/50.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gw-component-library/03-UI-SPEC.md
@.planning/phases/03-gw-component-library/03-SHADOW-NAMES.md
@.planning/phases/03-gw-component-library/03-02-SUMMARY.md
@.planning/phases/03-gw-component-library/03-04-SUMMARY.md

Target file as it exists today — the boot path this plan must not disturb:
@lib/navigation/router.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the QR scanner and the two dropdowns</name>
  <files>lib/components/qr_scanner/gw_qr_scanner.dart, lib/components/currency_dropdown.dart, lib/components/custom_drop_down.dart</files>
  <action>
Apply the standing port protocol from plan 03-02 Task 1 (copy verbatim, improve nothing, grep for
WIRE-, confirm imports resolve, modify no collision file).

- `qr_scanner/gw_qr_scanner.dart` — `GWQrScanner` plus the `extractWalletAddress` payload parser
  (handles plain, EIP-681, EIP-681-transfer and BIP-21 forms). Imports `package:mobile_scanner`,
  landed by plan 03-01. **It is explicitly parse-only and does not validate** — recipient validation
  is WIRE-3 and out of scope for this milestone. Do not add validation, and do not "improve" the
  parser: Alex's branch validates with `recipient.length >= 6` (any 6+ characters passes), which is
  exactly the class of demo logic WIRE-01 forbids reaching develop. The parser as ported has no
  out-of-scope coupling. Its eventual callers (`reown/reown_connect_button.dart`,
  `tokens/send_screen.dart`) are collision files in Phases 7/10 — do not touch them.

- `currency_dropdown.dart` — a thin `GWSelect<Currency>` wrapper. **Critical distinction:** it wraps
  `genius_api`'s `Currency` (the fiat currency used for Banxa), **not** `lib/preferences/gw_currency.dart`,
  which is WIRE-7 (Alex's currency picker changes the symbol only while values stay USD) and is
  WIRE-02 out-of-scope. Confirm the import resolves to `genius_api` before finishing; if it points at
  `lib/preferences/`, stop — the scope changed. Imports `components/inputs/gw_select.dart` (plan
  03-02). Orphaned today; generally useful to a future Banxa screen phase.

- `custom_drop_down.dart` — a generic dropdown, distinct from `GWSelect`. Do not consolidate the two.
  Its caller, `lib/banxa/banaxa_buy_screen.dart`, is a collision file untouched until Phase 9.
  </action>
  <verify>
    <automated>
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
test -f lib/components/qr_scanner/gw_qr_scanner.dart
test -f lib/components/currency_dropdown.dart
test -f lib/components/custom_drop_down.dart
# mobile_scanner resolves (proves 03-01 landed).
grep -q 'package:mobile_scanner' lib/components/qr_scanner/gw_qr_scanner.dart
# WIRE-02: the currency dropdown must bind genius_api's Currency, never lib/preferences/.
test "$(grep -c 'genius_wallet/preferences' lib/components/currency_dropdown.dart)" -eq 0
grep -q 'genius_api' lib/components/currency_dropdown.dart
# WIRE-01: no demo stub reached develop.
test "$(grep -rc 'WIRE-' lib/components/qr_scanner/ lib/components/currency_dropdown.dart lib/components/custom_drop_down.dart | grep -v ':0$' | wc -l)" -eq 0
flutter analyze
bash tool/verify_additive_boundary.sh
    </automated>
    <human-check>
Deferred to plan 03-09's gallery walk.
    </human-check>
  </verify>
  <done>
Three files exist. `gw_qr_scanner.dart` resolves `mobile_scanner`. `currency_dropdown.dart` binds
`genius_api`'s `Currency` and has zero reference to `lib/preferences/`. No WIRE- marker present.
`flutter analyze` reports 0 errors; guard passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Port the SGNUS pair, the misc helpers, and the Splash shadow</name>
  <files>lib/components/sgnus/sgnus_connected_dropdown.dart, lib/components/sgnus/sgnus_wallet.dart, lib/components/coins/view/coin_card_container.dart, lib/components/number_pad.dart, lib/components/toast/ticker_provider.dart, lib/components/splash.dart</files>
  <action>
Apply the standing port protocol from plan 03-02 Task 1.

- `sgnus/sgnus_connected_dropdown.dart` and `sgnus/sgnus_wallet.dart` — SGNUS-specific display
  components, distinct from `sgnus_connection_widget.dart` (a collision file, untouched).
  `sgnus_wallet.dart` imports `components/wallet_preview.g.dart` (plan 03-04) and
  `wallets/view/genius_balance_display.dart` (already on develop). The UI-SPEC records these as "not
  independently verified — low-risk, additive, port as-is"; that remains true, so port as-is and note
  in the SUMMARY that their contract was not independently derived.
- `coins/view/coin_card_container.dart` — additive, but **orphaned: zero callers anywhere on the
  design branch itself.** Port for completeness; flag as unconsumed with no known future caller. Do
  not expect a screen phase to adopt it. Its sibling `coin_card_row.dart` is a collision file — do
  not touch it.
- `number_pad.dart` — `NumberPad`, 4-digit PIN entry. Uses `StringButton`, an existing collision
  component already on develop. Do not port Alex's `string_button.dart`.
- `toast/ticker_provider.dart` — a supporting class for the existing (collision) toast system. It is
  an additive addition to an existing feature; it changes nothing about the toast files themselves,
  which stay untouched.

- `splash.dart` — **the second shadow file. Read `03-SHADOW-NAMES.md` before touching this.**

  `lib/components/splash.dart` declares `class Splash extends StatefulWidget`. **Develop already has
  `class Splash`** — a `StatelessWidget` at `lib/screens/splash.dart`. Its single importer is
  `lib/navigation/router.dart:30`, which is the **splash route: the app's boot path.**

  What makes this live rather than theoretical: **Alex deleted `lib/screens/splash.dart` on his
  branch** — verified, the file does not exist there. So the source tree presents this as a completed
  move, and a later phase "finishing the move" by repointing `router.dart`'s import would swap the
  app's startup screen from a `StatelessWidget` to a `StatefulWidget` with no compile error and no
  analyzer warning.

  Rules for this file:
  1. Port it verbatim to `lib/components/splash.dart`. Do not rename the class.
  2. Do **not** delete, move, or edit `lib/screens/splash.dart`. This phase does not complete Alex's
     move.
  3. Do **not** repoint `router.dart:30`. `router.dart` is a collision file; it gains exactly one
     appended route in plan 03-07 and nothing else, all phase.
  4. Add a head comment to the NEW file stating: it duplicates the class name of
     `lib/screens/splash.dart`; the canonical boot splash is the old path, still routed from
     `router.dart`; Alex's branch deleted the old path but this port does not; migrating is a
     deliberate act requiring an update to
     `.planning/phases/03-gw-component-library/03-SHADOW-NAMES.md` and a re-run of
     `tool/verify_additive_boundary.sh` in the same commit.
  5. Run the guard. It pins `screens/splash.dart`'s single-importer set and allowlists the shadow to
     the gallery only.
  </action>
  <verify>
    <automated>
export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"
for f in \
  lib/components/sgnus/sgnus_connected_dropdown.dart \
  lib/components/sgnus/sgnus_wallet.dart \
  lib/components/coins/view/coin_card_container.dart \
  lib/components/number_pad.dart \
  lib/components/toast/ticker_provider.dart \
  lib/components/splash.dart ; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done
# Develop's boot splash is untouched and still routed.
test -f lib/screens/splash.dart
test -z "$(git diff --name-only -- lib/screens/splash.dart lib/navigation/router.dart)"
grep -q 'package:genius_wallet/screens/splash.dart' lib/navigation/router.dart
# The shadow has no importers yet (the gallery lands in 03-09).
test "$(grep -rl 'package:genius_wallet/components/splash.dart' lib/ | wc -l)" -eq 0
# The shadow carries its warning head comment.
grep -q 'SHADOW-NAMES' lib/components/splash.dart
# The collision siblings were not ported over.
test -z "$(git diff --name-only -- lib/components/string_button.dart lib/components/coins/view/coin_card_row.dart lib/components/sgnus/sgnus_connection_widget.dart)"
flutter analyze
bash tool/verify_additive_boundary.sh
    </automated>
    <human-check>
Deferred to plan 03-10's phase walk, whose FIRST observation is a cold start — which is exactly the
boot path this shadow threatens. If the app still shows develop's splash and reaches the dashboard,
`router.dart` is still resolving `Splash` to `screens/splash.dart`.
    </human-check>
  </verify>
  <done>
Six files exist. `lib/screens/splash.dart` and `lib/navigation/router.dart` show zero diff, and
`router.dart` still imports the canonical splash. The shadow has zero importers and carries its head
comment. `string_button.dart`, `coin_card_row.dart` and `sgnus_connection_widget.dart` are untouched.
`flutter analyze` reports 0 errors; guard passes. **50/50 in-scope files landed.**
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference worktree → repo | Source files copied in from an external tree |
| shadow class → boot path | A second `Splash` class enters a namespace the app's startup route depends on |
| QR payload → parser | `extractWalletAddress` parses untrusted scanned input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-19 | Spoofing | `components/splash.dart`'s `Splash` class | high | mitigate | Shadows the app's boot screen, and the source tree presents the swap as a finished move (Alex deleted the canonical file). A repointed `router.dart:30` import would change what the app boots into, silently. Mitigated by the guard's pinned single-importer set, a zero-diff assertion on both `screens/splash.dart` and `router.dart`, a head comment on the shadow, and a cold-start observation in plan 03-10. |
| T-03-20 | Tampering | `extractWalletAddress` (parse-only) | medium | accept | Ported parse-only by design; validation is WIRE-3, explicitly out of scope. Accepted for THIS phase because the parser is unreachable — nothing calls it until Phase 7/10 wires `send_screen.dart`. **Binding note for Phase 7:** develop's real validation wins; never adopt Alex's `recipient.length >= 6` check, which passes any 6+ character string. Recorded in the SUMMARY. |
| T-03-21 | Tampering | `currency_dropdown.dart`'s Currency binding | medium | mitigate | Asserted to bind `genius_api`'s `Currency` with zero reference to `lib/preferences/` (WIRE-7/WIRE-02 out of scope — Alex's picker changes the symbol while values stay USD, i.e. it shows users wrong numbers). |
| T-03-22 | Repudiation | SGNUS pair contract | low | accept | The UI-SPEC did not independently verify these two components' contracts. Ported as-is (additive, low-risk, unconsumed) with the non-derivation recorded in the SUMMARY rather than implied to be verified. |
</threat_model>

<verification>
`flutter analyze` reports 0 errors (gate, never evidence). `bash tool/verify_additive_boundary.sh`
exits 0. `lib/screens/splash.dart`, `lib/navigation/router.dart` and every collision sibling show
zero diff.

Dart-only additions — hot reload applies them, nothing imports them, nothing changes on screen. This
plan completes the 50-file set; plans 03-07/03-09 make it inspectable and 03-10 records the walk.
</verification>

<success_criteria>
**All 50 in-scope files are on develop** (12 + 6 + 14 + 9 + 9 = 50), additively, analyze-clean,
consuming only Phase 2 tokens and files that already exist. All three shadow classes are landed,
unconsumed, gated and documented. DS-02's port is complete.
</success_criteria>

<output>
Create `.planning/phases/03-gw-component-library/03-06-SUMMARY.md` when done. Record the running
50/50 file reconciliation against the diff, the WIRE-3 binding note for Phase 7, and the SGNUS
non-derivation.
</output>
</content>
