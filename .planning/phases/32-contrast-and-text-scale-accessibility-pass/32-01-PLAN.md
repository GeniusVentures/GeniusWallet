---
phase: 32-contrast-and-text-scale-accessibility-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [test/theme/enabled_control_contrast_test.dart, lib/theme/genius_wallet_colors.dart, lib/theme/gw_colors.dart, test/theme/gw_colors_parity_test.dart, lib/components/inputs/gw_switch.dart, test/theme/material_text_slots_test.dart, lib/theme/genius_wallet_typography.dart, lib/theme/theme.dart, lib/components/overlay/responsive_overlay.dart, test/components/mobile_nav_destinations_test.dart]
autonomous: true
requirements: []
estimate: {tokens: 70000, raw_tokens: 70000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "An enabled GWSwitch's outline clears 3:1 against its ON track (over surfaceBase, surfaceElevated, surfaceMenu, surfaceSunken) and its OFF track, in both appearance modes"
    - "All 15 Material TextTheme slots resolve to Inter, so a bare ActionChip label and a bare AlertDialog title render in Inter, not Roboto, with no size change"
    - "The phone bottom bar follows OS text scale up to 221/180 (about 1.228x) and holds there, so its 60px slot never overflows vertically"
  artifacts:
    - {path: lib/theme/genius_wallet_colors.dart, provides: "_borderControlOnBrand: ink 52% light, white 58% dark"}
    - {path: lib/theme/gw_colors.dart, provides: "GWColors.borderControlOnBrand, wired like borderControl"}
    - {path: lib/theme/genius_wallet_typography.dart, provides: "GeniusWalletTypography.sansFamily"}
    - {path: lib/components/overlay/responsive_overlay.dart, provides: "kMobileBarMaxTextScale and the clamp in _MobileTabBar.build"}
  key_links:
    - {from: lib/components/inputs/gw_switch.dart, to: GWColors.borderControlOnBrand, via: "trackOutlineColor, enabled branch"}
    - {from: lib/theme/theme.dart, to: GeniusWalletTypography.sansFamily, via: "ThemeData(fontFamily:)"}
    - {from: "_MobileTabBar.build", to: kMobileBarMaxTextScale, via: "MediaQuery.withClampedTextScaling"}
---

<objective>
Close three of the phase's four defects, each as its own slice with its own test: the enabled switch outline (goal 1),
the unmapped Material text slots (goal 4), and the phone-bar text-scale clamp (goal 3, locked remedy). Goal 2, the
status-colour text sweep, is plans 02-04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/32-contrast-and-text-scale-accessibility-pass/32-RESEARCH.md
@.planning/phases/32-contrast-and-text-scale-accessibility-pass/32-PATTERNS.md
Every command runs after `export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"`.
Commits land on the phase feature branch, never on develop. `flutter analyze` exits 1 on infos: check `$?`, not the tail.
Source comments: WHY only, doc comments at most 3 lines, never a plan/phase/todo id or a test file name.
Planning re-measured three research claims (pinned SDK 3.41.9 at ../flutter/flutter, token values from lib/theme/):
- Dark outline is 58%, not 56%. `Color.fromRGBO` stores opacity as-is, and 0.56 measures 2.99:1 on ON-over-surfaceMenu. Research's figure was 144/255.
- headlineSmall HAS live consumers: AlertDialog titles (SDK dialog.dart:1819) at disclaimer_dialogue.dart:19 and reown_connect_button.dart:292.
- AppBar's clamp is real (app_bar.dart:44, 1.34), and `MediaQuery.withClampedTextScaling` exists (widgets/media_query.dart:1368).
<assumption_delta_decision>no-change: the detector fired on "Remedy chosen 2026-09-25", which is prose about a decision date, not a derived value. False positive.</assumption_delta_decision>
Spec-less probe fallback: skipped on purpose, because there is no SPEC and no requirement IDs. The four ROADMAP goals are the acceptance criteria.
</context>

<tasks>

<task type="tracer">
  <name>An enabled switch's outline clears 3:1 on both tracks, measured from the widget's own resolvers</name>
  <files>test/theme/enabled_control_contrast_test.dart, lib/theme/genius_wallet_colors.dart, lib/theme/gw_colors.dart, test/theme/gw_colors_parity_test.dart, lib/components/inputs/gw_switch.dart</files>
  <read_first>lib/components/inputs/gw_switch.dart (full), lib/theme/genius_wallet_colors.dart (196-232), lib/theme/gw_colors.dart (55-65, 140-147, 316-322, 390-400, 487-493, 560-567, 650-656, 720-726), test/theme/disabled_control_contrast_test.dart (full), test/theme/gw_colors_parity_test.dart (58-100)</read_first>
  <action>Goal 1. Write the test first. enabled_control_contrast_test.dart imports `contrastRatio`/`themeFor` from theme_contrast_test.dart and never re-implements them. For each GWAppearanceMode, pump `GWSwitch(value: true, onChanged: (_) {})` under `MaterialApp(theme: themeFor(mode))`, then take the rendered Switch's own `trackColor` and `trackOutlineColor` resolvers. For states `{selected}` and `{}`, alphaBlend the track over each of surfaceBase, surfaceElevated, surfaceMenu and surfaceSunken, alphaBlend the outline over that composite (two stages, because computeLuminance ignores alpha), and expect `contrastRatio >= 3.0` with a reason naming the mode, state and surface. Run it: it must fail today, because the current hairline token measures about 1.3:1.
Then add `_borderControlOnBrand` directly after `_borderControl`: light `Color.fromRGBO(16, 19, 26, 0.52)`, dark `Color.fromRGBO(255, 255, 255, 0.58)`. Its doc comment is at most 3 lines and states the worst case it clears: 3.03:1 light, 3.10:1 dark, both on the ON track. Wire `GWColors.borderControlOnBrand` everywhere borderControl is wired: constructor, field (doc at most 3 lines), both factories, both factory asserts, and copyWith (param and body). Add `borderControlOnBrand: Colors.black` to the parity probe and change its "68 fields" title to 69. In gw_switch.dart, the enabled branch of trackOutlineColor (selected and unselected alike) returns `gw.borderControlOnBrand`. The disabled branch keeps `gw.borderControl`. Don't raise borderControl itself: its four other consumers are tuned to its margin.</action>
  <verify><automated>flutter test test/theme/enabled_control_contrast_test.dart test/theme/gw_colors_parity_test.dart test/theme/disabled_control_contrast_test.dart</automated>
    <human-check>/settings in Light and Dark: every switch, on and off, shows a visible outline.</human-check></verify>
  <acceptance_criteria>
    - The new test failed before the token existed and passes after (2 modes x 2 states x 4 surfaces)
    - `grep -c "borderSubtle" lib/components/inputs/gw_switch.dart` prints 0; `grep -c "gw.borderControl;" lib/components/inputs/gw_switch.dart` prints 1
    - `grep -c "borderControlOnBrand" lib/theme/gw_colors.dart` prints at least 8
  </acceptance_criteria>
  <done>The enabled outline reads the new token in both modes, and the parity probe and the factory asserts cover it.</done>
  <reversibility rating="reversible">Two alpha values and one resolver branch; a single revert undoes them.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Every Material text slot resolves to Inter, without changing any size</name>
  <files>test/theme/material_text_slots_test.dart, lib/theme/genius_wallet_typography.dart, lib/theme/theme.dart</files>
  <read_first>lib/theme/genius_wallet_typography.dart (full), lib/theme/theme.dart (55-85, 416-430), lib/components/disclaimer_dialogue.dart (15-22)</read_first>
  <behavior>
    - In both modes, all 15 slots of themeFor(mode).textTheme have fontFamily 'Inter'. This fails today: the five unmapped slots are Roboto
    - A bare ActionChip label and a bare AlertDialog title resolve fontFamily 'Inter' (read RichText.text.style under each)
    - Mapped slots keep their size: labelMedium is 13, bodySmall is 14
  </behavior>
  <action>Goal 4, done as "left unmapped on purpose with a comment" plus one ThemeData line, instead of research's new labelLg getter. ThemeData applies `fontFamily` to Material's defaults before it merges our textTheme (theme_data.dart:510-524), so one line gives all five slots Inter at Material's own sizes. That also covers the headlineSmall dialog titles research missed. Write the test first. It asserts the literal 'Inter', so a typo in the constant fails it. Then add `static const String sansFamily = 'Inter';` beside monoFamily (doc at most 3 lines), make `_inter` read it, and add `fontFamily: GeniusWalletTypography.sansFamily,` next to `textTheme:` in getThemeData(). In toMaterialTextTheme(), add a comment of at most 3 lines saying displaySmall, headlineSmall, titleSmall, labelLarge and labelSmall stay unmapped on purpose: they keep Material's sizes and take Inter from ThemeData.fontFamily. Leave menuButtonTheme alone: its bodySm w400 is not labelLarge's w500.</action>
  <verify><automated>flutter test test/theme/</automated>
    <human-check>The onboarding recovery-phrase word chips and the Banxa disclaimer title render in Inter.</human-check></verify>
  <acceptance_criteria>
    - The slot test failed before the theme.dart line and passes after it
    - `grep -c "fontFamily: GeniusWalletTypography.sansFamily" lib/theme/theme.dart` prints 1
    - `grep -c "'Inter'" lib/theme/genius_wallet_typography.dart` prints 1, the constant only
    - toMaterialTextTheme() still maps the same 10 slots to the same getters
  </acceptance_criteria>
  <done>No Material text slot falls back to Roboto, and no mapped style changed.</done>
</task>

<task type="auto">
  <name>The phone bar clamps text scale where its label line would overflow</name>
  <files>lib/components/overlay/responsive_overlay.dart, test/components/mobile_nav_destinations_test.dart</files>
  <read_first>lib/components/overlay/responsive_overlay.dart (90-106, 145-200), test/components/mobile_nav_destinations_test.dart (280-316)</read_first>
  <action>Goal 3, using the locked remedy: clamp only, with no icon-only fallback and no bar-height change. After kMobileBarHeight, add a public `const double kMobileBarMaxTextScale = 221 / 180;`. It is public for the same reason as its neighbours. Its doc comment is at most 3 lines: this is the scale at which the 13.85px label line has used up the bar's 3.15px of slack. In `_MobileTabBar.build`, wrap the returned SizedBox in `MediaQuery.withClampedTextScaling(maxScaleFactor: kMobileBarMaxTextScale, child: ...)`, the same helper AppBar uses. In the test file's group, add 'the text-scale clamp sits exactly where the label eats the slack'. Using the constants the slack test already uses, expect `padding + icon + gap + labelLine * kMobileBarMaxTextScale` to equal kMobileBarHeight (moreOrLessEquals, epsilon 1e-9), and expect kMobileBarMaxTextScale to be 1.2278 within 1e-4. Add a debugPrint of the ceiling in the file's style. In the slack test, replace the "This is PRE-EXISTING" paragraph (it names the pending todo and calls the clamp out of scope) with at most 2 lines saying the bar clamps at kMobileBarMaxTextScale. There is no widget pump: mounting `_MobileTabBar` needs AppBloc, WalletDetailsCubit and a GoRouter, and no test mounts MobileOverlay today. The grep below and the walk pin the one-line wiring.</action>
  <verify><automated>flutter test test/components/mobile_nav_destinations_test.dart</automated>
    <human-check>Debug build, window narrower than 1024px, OS text size 130% or more: the bar labels stop growing at about 1.23x and no overflow stripe shows. The ceiling has zero slack by construction. If a sub-pixel overflow appears, stop and report the amount; do not lower the locked ceiling.</human-check></verify>
  <acceptance_criteria>
    - `grep -c "withClampedTextScaling" lib/components/overlay/responsive_overlay.dart` prints 1, and that call passes `maxScaleFactor: kMobileBarMaxTextScale`
    - `grep -c "221 / 180" lib/components/overlay/responsive_overlay.dart` prints 1
    - `grep -c "mobile-bar-overflows" test/components/mobile_nav_destinations_test.dart` prints 0, and the slack test's 3.15 assertion still passes
  </acceptance_criteria>
  <done>The bar's label stops scaling at the exact point where it would overflow, and a geometry test pins that point.</done>
</task>

</tasks>

<threat_model>
No trust boundary is crossed. This plan changes a colour token, a theme font family and a layout clamp: no input, secret, key or network path.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering | lib/theme tokens | low | accept | Presentation only. The parity probe and factory asserts catch a token wired on one side only |
</threat_model>

<verification>
`dart format --output=none --set-exit-if-changed` on the touched files; `flutter analyze` with `$?` equal to 0; `bash tool/check_brace_style.sh`;
`flutter test test/theme/ test/components/mobile_nav_destinations_test.dart`. After `git add`, `git ls-files --eol` shows `i/lf` for both new test files.
</verification>

<success_criteria>
Goals 1, 3 and 4 hold in both appearance modes, each has a test that fails without its fix, and nothing outside these 10 files changed.
</success_criteria>

## Source audit (whole phase)

| Source | Item | Covered by |
|---|---|---|
| GOAL | 1 switch outline, 3 bar clamp, 4 text slots | 01 T1, T3, T2 |
| GOAL | 2 status colours as text, per real backdrop | 02 (price colours), 03 (account/settings), 04 (forms, banners, census) |
| REQ | none: backlog phase, `requirements: []` as in phases 20-21 | n/a |
| RESEARCH | open Q1 (swap_settings_drawer, reown) resolved at planning; Q2 token name kept generic | 04 T2; 01 T1 |
| CONTEXT | no CONTEXT.md; locked: all four in one phase, clamp at 221/180 | 01-04; 01 T3 |

## Artifacts this phase produces

This plan: `enabled_control_contrast_test.dart` and `material_text_slots_test.dart` (new), the token, `sansFamily`, `kMobileBarMaxTextScale`, and `32-01-SUMMARY.md`.

<output>
Create `.planning/phases/32-contrast-and-text-scale-accessibility-pass/32-01-SUMMARY.md` (at most 40 lines) when done.
</output>
