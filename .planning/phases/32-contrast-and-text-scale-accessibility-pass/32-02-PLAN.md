---
phase: 32-contrast-and-text-scale-accessibility-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [test/theme/status_text_contrast_test.dart, lib/components/coins/view/coin_card_row.dart, lib/chart/crypto_simple_chart.dart, lib/chart/crypto_live_chart.dart, lib/dashboard/chart/markets_table.dart, lib/dashboard/chart/markets_cards.dart, lib/dashboard/chart/markets_hero_card.dart, lib/tokens/token_info_screen.dart, lib/components/coins/assets_total_band.dart, lib/dashboard/news/view/crypto_news_screen.dart]
autonomous: true
requirements: []
estimate: {tokens: 60000, raw_tokens: 60000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "In light mode every green or red price-change label, pill label, stat value and news timestamp clears 4.5:1 on its real backdrop"
    - "Dark mode paints exactly the same pixels as before, because the partner tokens are byte-identical to the raw ones in dark"
    - "Sparklines, chart lines, gradients and pill washes keep the raw status colour"
    - "A test pins statusSuccessText/statusErrorText at 4.5:1 or better on every plain surface and on a 12-20% status wash, in both modes"
  artifacts:
    - {path: test/theme/status_text_contrast_test.dart, provides: "the backdrop matrix the whole sweep relies on"}
  key_links:
    - {from: "price-change Text widgets", to: "GWColors.statusSuccessText / statusErrorText", via: "a sibling text local next to each shared changeColor/trendColor/tone"}
---

<objective>
Goal 2, part one: price-change colours on the dashboard, markets, token and asset surfaces, plus the news timestamp.
These are the sites where one local colour feeds a wash or a line and a label at the same time.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/32-contrast-and-text-scale-accessibility-pass/32-RESEARCH.md
Every command runs after `export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"`. Commits land on the phase branch, never on develop.
Source comments: WHY only, at most 3 lines, never a plan/phase/todo id or a test file name.
THE RULE (plans 02-04): a foreground (Text, TextStyle, TextSpan, a label-painting parameter, or an Icon sitting beside such a
label) reads `statusSuccessText`/`statusErrorText`. A wash, fill, border, dot, chart line or sparkline keeps the raw token.
When one local feeds both, add a sibling local such as `changeTextColor = up ? gw.statusSuccessText : gw.statusErrorText`
and point only the foreground at it. This is the shipped fg/wash split in transaction_displays.dart:83-100.
Counting raw reads for a file: `grep -vE '^\s*//' FILE | grep -oE '\.status(Success|Error)\b' | wc -l`.
Contrast figures below were computed at planning from the token hex values with the WCAG formula.
</context>

<tasks>

<task type="tracer">
  <name>The partner tokens are pinned on every backdrop, and the assets row reads them</name>
  <files>test/theme/status_text_contrast_test.dart, lib/components/coins/view/coin_card_row.dart</files>
  <read_first>lib/components/coins/view/coin_card_row.dart (45-140), test/theme/control_track_contrast_test.dart (full), test/theme/theme_contrast_test.dart (16-36)</read_first>
  <action>Test first. status_text_contrast_test.dart imports `contrastRatio`/`themeFor` from theme_contrast_test.dart. For each mode and each tone pair (statusSuccess/statusSuccessText, statusError/statusErrorText), expect the text token to reach at least 4.5:1 on each of surfaceElevated, surfaceMenu, surfaceBase and surfaceSunken. Also expect it on the raw token alphaBlended at 0.12, 0.14, 0.15 and 0.20 over surfaceElevated and over surfaceBase. The expected worst cases are 4.61 in light (success, 0.20 wash on base) and 4.69 in dark (error, 0.20 wash on elevated). Add a "why this exists" test in the style of control_track_contrast_test.dart: in light mode, raw statusSuccess on surfaceMenu is below 4.5 (it measures 4.03).
Then, in coin_card_row.dart, add a sibling `changeTextColor` next to changeColor (:55). Give it to the percentage Text (:137). The pill wash (:131) stays on changeColor.
Executor check: trace which surface the row's pill sits on. In dark mode, statusErrorText on a 0.15 wash over surfaceMenu is 4.48:1. If this pill (or any 0.15/0.20 pill in T2) sits on surfaceMenu, record it in SUMMARY as a dark-mode shortfall that predates this plan, which this swap does not change. Don't widen the scope to fix it.</action>
  <verify><automated>flutter test test/theme/status_text_contrast_test.dart</automated></verify>
  <acceptance_criteria>
    - The matrix test passes: 2 modes x 2 tones x (4 plain + 8 wash) backdrops, plus the one "why" assertion
    - coin_card_row.dart has 2 raw reads (the changeColor definition) and `grep -c "statusSuccessText" lib/components/coins/view/coin_card_row.dart` prints 1
    - `flutter analyze lib/components/coins/view/coin_card_row.dart test/theme/status_text_contrast_test.dart` exits 0
  </acceptance_criteria>
  <done>The token pairing is pinned by a test, and one real label reads it end to end.</done>
  <reversibility rating="reversible">A colour lookup per label; nothing persists.</reversibility>
</task>

<task type="auto">
  <name>Chart and market labels read the partner; lines, sparklines and washes do not</name>
  <files>lib/chart/crypto_simple_chart.dart, lib/chart/crypto_live_chart.dart, lib/dashboard/chart/markets_table.dart, lib/dashboard/chart/markets_cards.dart, lib/dashboard/chart/markets_hero_card.dart</files>
  <read_first>lib/chart/crypto_simple_chart.dart (50-60, 170-225), lib/chart/crypto_live_chart.dart (318-325, 425-470, 551-620), lib/dashboard/chart/markets_table.dart (228-235, 315-368), lib/dashboard/chart/markets_cards.dart (250-335), lib/dashboard/chart/markets_hero_card.dart (235-245, 320-332, 578-600, 800-812)</read_first>
  <action>Apply THE RULE file by file:
- crypto_simple_chart: sibling for the pill text (:219). The sparkline (:179) and the wash (:213) stay raw.
- crypto_live_chart: add a sibling `trendTextColor` beside trendColor (:321). Use it for the pill text (:445), and pass it to `_ChartHeaderRow(trendColor: ...)` (:467), whose only use of the colour is its percentage Text (:616). The wash (:435), `_TradingFrameChart` and `_SparklineChart` keep trendColor.
- markets_table: sibling for the text (:325). The wash (:319) and the `_MiniSpark` (:365) stay raw.
- markets_cards: sibling for the text (:332). The wash (:326) stays raw.
- markets_hero_card: `_ChangePill` gets a `required Color textColor` for its label (:595) and keeps `color` for its wash (:589). The one caller (:326) passes a sibling of changeColor. The tooltip `pctColor` (:808-810) swaps both branches to the partners. `trend` (:663) feeds only the chart line and is untouched.</action>
  <verify><automated>flutter analyze lib/chart/ lib/dashboard/chart/</automated></verify>
  <acceptance_criteria>
    - Raw reads per file: crypto_simple_chart 2, crypto_live_chart 2, markets_table 2, markets_cards 2, markets_hero_card 4
    - Each of the five files contains at least one `statusSuccessText` and one `statusErrorText`
    - `flutter analyze lib/chart/ lib/dashboard/chart/` exits 0
  </acceptance_criteria>
  <done>Every market or chart label reads an AA-safe token, and every line, sparkline and wash is unchanged.</done>
</task>

<task type="auto">
  <name>Token detail, the assets band and the news timestamp read the partner</name>
  <files>lib/tokens/token_info_screen.dart, lib/components/coins/assets_total_band.dart, lib/dashboard/news/view/crypto_news_screen.dart</files>
  <read_first>lib/tokens/token_info_screen.dart (1270-1320, 1355-1382), lib/components/coins/assets_total_band.dart (100-115), lib/dashboard/news/view/crypto_news_screen.dart (650-668)</read_first>
  <action>token_info_screen: add a sibling `toneText` beside tone (:1274) for the Icon (:1310) and the Text (:1315) that sit together on the pill. The wash (:1301) stays on tone. The two `GWStatTile.valueColor` ternaries (:1362, :1377) swap to the partners. assets_total_band: swap the change Text (:109) inline. crypto_news_screen: swap the timestamp (:662) to statusSuccessText, and correct the comment above it, which names the raw token and its light hex, to name the partner (#065F46 light).</action>
  <verify><automated>flutter analyze lib/tokens/token_info_screen.dart lib/components/coins/assets_total_band.dart lib/dashboard/news/view/crypto_news_screen.dart</automated>
    <human-check>Light mode: dashboard, Markets, a token's detail page and News show a darker, legible green or red on every change label. Dark mode looks unchanged.</human-check></verify>
  <acceptance_criteria>
    - Raw reads per file: token_info_screen 2, assets_total_band 0, crypto_news_screen 0
    - `grep -c "#07875F" lib/dashboard/news/view/crypto_news_screen.dart` prints 0
    - `flutter test test/theme/` passes
  </acceptance_criteria>
  <done>No price-change or timestamp label in these surfaces reads a fill-tuned token.</done>
</task>

</tasks>

<threat_model>
No trust boundary is crossed. These are colour lookups on labels only.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-02 | Tampering | label colours | low | accept | Presentation only. Dark mode is byte-identical, so no repaint risk there |
</threat_model>

<verification>
`dart format --output=none --set-exit-if-changed` on the touched files; `flutter analyze` with `$?` equal to 0; `bash tool/check_brace_style.sh`;
`flutter test test/theme/`. After `git add`, `git ls-files --eol test/theme/status_text_contrast_test.dart` shows `i/lf`.
Tests that pin a raw colour in dark mode keep passing, because the partners are identical there. If a light-mode pin fails, update it to the partner; do not revert the swap.
</verification>

<success_criteria>
The nine surfaces' labels read the partner tokens. The matrix test passes. Every wash, line and sparkline keeps the raw token.
</success_criteria>

## Artifacts this phase produces

This plan: `test/theme/status_text_contrast_test.dart` (new), nine swapped call-site files, and `32-02-SUMMARY.md` recording any pill found on surfaceMenu.

<output>
Create `.planning/phases/32-contrast-and-text-scale-accessibility-pass/32-02-SUMMARY.md` (at most 40 lines) when done.
</output>
