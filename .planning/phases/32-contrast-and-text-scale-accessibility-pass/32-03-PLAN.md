---
phase: 32-contrast-and-text-scale-accessibility-pass
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [lib/screens/pin_screen.dart, lib/components/wallet_information.dart, lib/components/wallets_overview.dart, lib/account/account_drawer.dart, lib/account/sdk_account_manager.dart, lib/components/qr/crypto_address_qr.dart, lib/settings/settings_screen.dart, lib/logs/submit_logs_screen.dart, lib/screens/banxa_buy_screen.dart]
autonomous: true
requirements: []
estimate: {tokens: 45000, raw_tokens: 45000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "In light mode, 'Incorrect PIN', 'No funds available', 'Delete' and 'Delete Wallet', the Copied confirmation, the settings debug line, the logs status and rail value, and the Banxa form error each clear 4.5:1 on their real backdrop"
    - "An icon that sits beside one of those labels takes the same partner token, so icon and label stay one colour"
    - "Dark mode paints the same pixels as before"
  artifacts:
    - {path: lib/screens/pin_screen.dart, provides: "the 'Incorrect PIN' label on statusErrorText"}
  key_links:
    - {from: "account, wallet, settings and logs labels", to: "GWColors.statusSuccessText / statusErrorText", via: "direct token swap at each foreground"}
---

<objective>
Goal 2, part two: status-coloured labels in the account, wallet, PIN, receive, settings, logs and Banxa screens. Every
site here is a foreground read directly (no shared local feeding a wash), so each one is a straight token swap.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/32-contrast-and-text-scale-accessibility-pass/32-RESEARCH.md
Every command runs after `export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"`. Commits land on the phase branch, never on develop.
Source comments: WHY only, at most 3 lines, never a plan/phase/todo id or a test file name.
THE RULE is the one stated in 32-02-PLAN.md: foregrounds (text, a label-painting parameter, or an icon beside such a label)
read `statusSuccessText`/`statusErrorText`; washes, fills, borders, dots and lines keep the raw token. On the plain surfaces
these screens use, the partners measure 5.16-8.31:1 in light mode (`32-02`'s matrix test pins this). Keep each site's
existing accessor (`gw.` or `context.gw.`).
Counting raw reads for a file: `grep -vE '^\s*//' FILE | grep -oE '\.status(Success|Error)\b' | wc -l`.
</context>

<tasks>

<task type="tracer">
  <name>The PIN error label reads the AA-safe partner</name>
  <files>lib/screens/pin_screen.dart</files>
  <read_first>lib/screens/pin_screen.dart (90-110)</read_first>
  <action>Swap the "Incorrect PIN" Text colour (:103) from gw.statusError to gw.statusErrorText. The comment above it (:96-102) explains why this reads the theme extension rather than a static. Keep that reasoning, but make it name the token the line now reads. Change the colour only; the PIN-handling logic is untouched.</action>
  <verify><automated>flutter analyze lib/screens/pin_screen.dart</automated></verify>
  <acceptance_criteria>
    - pin_screen.dart has 0 raw reads and `grep -c "statusErrorText" lib/screens/pin_screen.dart` prints at least 1
    - `flutter analyze lib/screens/pin_screen.dart` exits 0
  </acceptance_criteria>
  <done>The PIN error label clears 4.5:1 in light mode and is unchanged in dark mode.</done>
  <reversibility rating="reversible">One colour lookup.</reversibility>
</task>

<task type="auto">
  <name>Wallet and account labels, with their icons, read the partner</name>
  <files>lib/components/wallet_information.dart, lib/components/wallets_overview.dart, lib/account/account_drawer.dart, lib/account/sdk_account_manager.dart</files>
  <read_first>lib/components/wallet_information.dart (86-96, 228-252), lib/components/wallets_overview.dart (84-94), lib/account/account_drawer.dart (452-468), lib/account/sdk_account_manager.dart (284-305), lib/components/sliding_drawer_button.dart (25-55)</read_first>
  <action>wallet_information: swap "No funds available" (:92). Also swap `SlidingDrawerButton(color: ...)` for "Delete Wallet" (:248); that one `color` paints both the icon and the label. wallets_overview: swap "No funds available" (:90). account_drawer: swap both the MenuItemButton icon (:459) and `Text('Delete')` (:464), so they stay one colour. sdk_account_manager: swap the `danger` branch of `fg` (:292). It feeds the leading icon, foregroundColor and the label together, on surfaceMenu. All swaps are statusError to statusErrorText.</action>
  <verify><automated>flutter analyze lib/components/wallet_information.dart lib/components/wallets_overview.dart lib/account/</automated></verify>
  <acceptance_criteria>
    - Raw reads per file: wallet_information 0, wallets_overview 0, account_drawer 0, sdk_account_manager 0
    - `flutter analyze lib/components/wallet_information.dart lib/components/wallets_overview.dart lib/account/` exits 0
  </acceptance_criteria>
  <done>Every destructive or empty-state label in the wallet and account surfaces reads the partner token.</done>
</task>

<task type="auto">
  <name>Receive, settings, logs and Banxa status labels read the partner</name>
  <files>lib/components/qr/crypto_address_qr.dart, lib/settings/settings_screen.dart, lib/logs/submit_logs_screen.dart, lib/screens/banxa_buy_screen.dart</files>
  <read_first>lib/components/qr/crypto_address_qr.dart (150-170), lib/settings/settings_screen.dart (356-374), lib/logs/submit_logs_screen.dart (518-532, 610-618, 790-798), lib/screens/banxa_buy_screen.dart (640-650)</read_first>
  <action>crypto_address_qr: swap both the check Icon (:156) and the "Copied" Text (:166), which sit together, to statusSuccessText. settings_screen: swap both branches (:368, :370). The comment above them claims the two status tokens never change with appearance. They do (they diverge in light mode), so correct that sentence in at most 1 line and keep the rest of the comment. submit_logs_screen: swap `statusColor` (:530), which only feeds the status Text at :615, and the rail `valueColor` (:795). banxa_buy_screen: swap the error Text (:647).</action>
  <verify><automated>flutter analyze lib/components/qr/crypto_address_qr.dart lib/settings/settings_screen.dart lib/logs/submit_logs_screen.dart lib/screens/banxa_buy_screen.dart</automated>
    <human-check>Light mode: the receive drawer's "Copied" state, the settings debug line, the Feedback screen status and a Banxa form error all read as a darker, legible green or red.</human-check></verify>
  <acceptance_criteria>
    - Raw reads per file: crypto_address_qr 0, settings_screen 0, submit_logs_screen 0, banxa_buy_screen 0
    - `grep -c "mode-invariant" lib/settings/settings_screen.dart` prints 0
    - `flutter analyze` on the four files exits 0, and `flutter test test/theme/` passes
  </acceptance_criteria>
  <done>No label in these four screens reads a fill-tuned status token.</done>
</task>

</tasks>

<threat_model>
No trust boundary is crossed. pin_screen.dart changes a label colour only; no PIN value, comparison or storage path is touched.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-03 | Tampering | label colours | low | accept | Presentation only. The action on pin_screen.dart is limited to the colour argument |
</threat_model>

<verification>
`dart format --output=none --set-exit-if-changed` on the touched files; `flutter analyze` with `$?` equal to 0; `bash tool/check_brace_style.sh`; `flutter test test/theme/`.
Tests that pin a raw colour in dark mode keep passing, because the partners are identical there. If a light-mode pin fails, update it to the partner; do not revert the swap.
</verification>

<success_criteria>
All nine files have 0 raw status reads, analyze is clean, and dark mode is unchanged.
</success_criteria>

## Artifacts this phase produces

This plan: nine swapped call-site files and `32-03-SUMMARY.md`. There is no new test here; plan 04's census test is what fails if any of these files regresses.

<output>
Create `.planning/phases/32-contrast-and-text-scale-accessibility-pass/32-03-SUMMARY.md` (at most 40 lines) when done.
</output>
