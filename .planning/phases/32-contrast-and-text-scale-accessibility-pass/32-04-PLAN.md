---
phase: 32-contrast-and-text-scale-accessibility-pass
plan: 04
type: execute
wave: 2
depends_on: [32-02, 32-03]
files_modified: [lib/components/inputs/gw_text_field.dart, lib/components/inputs/gw_select.dart, lib/components/feedback/gw_error_state.dart, lib/squid_router/swap_screen.dart, lib/dashboard/bridge/bridge_screen.dart, lib/squid_router/swap_settings_drawer.dart, lib/reown/reown_connect_button.dart, lib/squid_router/route_details_card.dart, lib/dashboard/home/widgets/transaction_displays.dart, test/theme/status_text_color_invariant_test.dart]
autonomous: true
requirements: []
estimate: {tokens: 60000, raw_tokens: 60000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "In light mode, form error text, error banners, the swap and bridge refusal CTAs, the slippage message, the WalletConnect error states, the route 'Price Impact' value and the receipt's 'Not charged' each clear 4.5:1 on their real backdrop"
    - "Input borders, the slippage field edge and every banner wash keep the raw token"
    - "A source-scan test fails the moment any file in lib/ gains or loses a raw statusSuccess/statusError read without updating a reasoned census"
  artifacts:
    - {path: test/theme/status_text_color_invariant_test.dart, provides: "census of the 25 files allowed to read the raw tokens, with a count and reason for each"}
  key_links:
    - {from: test/theme/status_text_color_invariant_test.dart, to: "every .dart file under lib/ except lib/theme/", via: "Directory('lib').listSync(recursive: true)"}
---

<objective>
Goal 2, part three, and the gate for the whole sweep: form errors, error banners, swap/bridge/WalletConnect foregrounds,
the route and receipt values. Then a census test that locks every remaining raw read to a named non-text reason.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/32-contrast-and-text-scale-accessibility-pass/32-RESEARCH.md
@.planning/phases/32-contrast-and-text-scale-accessibility-pass/32-PATTERNS.md
Every command runs after `export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"`. Commits land on the phase branch, never on develop.
Source comments: WHY only, at most 3 lines, never a plan/phase/todo id or a test file name.
THE RULE is the one stated in 32-02-PLAN.md. Every wash in this plan is 0.12 (or `withAlpha(31)`, 0.122), where the partners measure 4.53:1 or better on all four surfaces in both modes (computed at planning).
Counting raw reads for a file: `grep -vE '^\s*//' FILE | grep -oE '\.status(Success|Error)\b' | wc -l`.
</context>

<tasks>

<task type="tracer">
  <name>Form field error text reads the partner while the error border stays raw</name>
  <files>lib/components/inputs/gw_text_field.dart, lib/components/inputs/gw_select.dart</files>
  <read_first>lib/components/inputs/gw_text_field.dart (218-265), lib/components/inputs/gw_select.dart (96-108)</read_first>
  <action>Swap `errorStyle`'s colour in gw_text_field (:232) and gw_select (:105) to `context.gw.statusErrorText`. The error borders (gw_text_field :223, :226, :261; gw_select :100, :101) stay on statusError. They are edges, and 1.4.11's 3:1 floor is already met.</action>
  <verify><automated>flutter analyze lib/components/inputs/</automated></verify>
  <acceptance_criteria>
    - Raw reads: gw_text_field 3, gw_select 2 (all border sites); `grep -c "statusErrorText" lib/components/inputs/gw_text_field.dart` prints 1
    - `flutter analyze lib/components/inputs/` exits 0
  </acceptance_criteria>
  <done>Every form's inline error message clears 4.5:1 in light mode, and borders are unchanged.</done>
  <reversibility rating="reversible">Two colour lookups.</reversibility>
</task>

<task type="auto">
  <name>Error banners, swap/bridge CTAs, the slippage message and WalletConnect states read the partner</name>
  <files>lib/components/feedback/gw_error_state.dart, lib/squid_router/swap_screen.dart, lib/dashboard/bridge/bridge_screen.dart, lib/squid_router/swap_settings_drawer.dart, lib/reown/reown_connect_button.dart</files>
  <read_first>lib/components/feedback/gw_error_state.dart (30-112), lib/squid_router/swap_screen.dart (684-718, 784-830), lib/dashboard/bridge/bridge_screen.dart (730-766), lib/squid_router/swap_settings_drawer.dart (145-160, 236-242, 310-335), lib/reown/reown_connect_button.dart (540-650)</read_first>
  <action>These are research's open question 1, resolved at planning; re-read each site before editing.
- gw_error_state: in the inline banner row, swap the icon (:94), the message (:100) and the close icon (:107). The washes (:37, :88), the border (:90) and the standalone 32px icon in its own circle (:40) stay raw.
- swap_screen: swap the error-row icon (:693) and both Texts (:704, :714), and `foreground` (:789), which feeds the spinner and the CTA label on the 0.12 wash. The washes (:687, :787) stay raw.
- bridge_screen: swap `foreground` (:735), the same shape as swap's. The wash (:733) stays raw.
- swap_settings_drawer: swap the error branch of the message `color` (:318), which feeds the Icon and the message Text. The error branch of `edge` (:149) stays raw: it becomes `restingColor: edge` (:241), the field border.
- reown_connect_button: swap both error assignments of `stateColor` (:547, :559) to statusErrorText. stateColor feeds the icon, the dot, the 0.16 hover overlay and the label (:648, on the control track), and they stay one colour. The statusWarning branches are not in this phase's four defects; leave them.</action>
  <verify><automated>flutter analyze lib/components/feedback/gw_error_state.dart lib/squid_router/ lib/dashboard/bridge/bridge_screen.dart lib/reown/reown_connect_button.dart</automated></verify>
  <acceptance_criteria>
    - Raw reads per file: gw_error_state 4, swap_screen 2, bridge_screen 1, swap_settings_drawer 1, reown_connect_button 0
    - `sed -n 148,150p lib/squid_router/swap_settings_drawer.dart` still shows `SlippageLevel.error => gw.statusError`
    - analyze on these files exits 0
  </acceptance_criteria>
  <done>Every error foreground on a banner, CTA or chip reads the partner, and every wash and edge is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>The last two values swap, and a census test locks the sweep</name>
  <files>lib/squid_router/route_details_card.dart, lib/dashboard/home/widgets/transaction_displays.dart, test/theme/status_text_color_invariant_test.dart</files>
  <read_first>lib/squid_router/route_details_card.dart (45-72), lib/dashboard/home/widgets/transaction_displays.dart (376-384), test/components/drawer_padding_invariant_test.dart (full)</read_first>
  <behavior>
    - One test walks every .dart file under lib/ except lib/theme/ (token definitions), drops lines whose trimmed text starts with `//`, counts `\.status(Success|Error)\b` matches, and expects the whole path-to-count map to equal the census. A new raw read, a vanished one, and a stale entry all fail it
    - Its failure message states the rule: a foreground reads statusSuccessText/statusErrorText; only a wash, fill, border, dot, line or standalone icon may read the raw token, and it goes in the census with its reason
  </behavior>
  <action>Swap route_details_card's "Price Impact" `valueColor` (:69) to gw.statusSuccessText, and transaction_displays' "Not charged" branch (:381) to gw.statusErrorText. The doc at :67 describes the old code, so leave it. Write status_text_color_invariant_test.dart on drawer_padding_invariant_test.dart's shape: its `_strippedSource` walk, and a hand-written `_census` of `path: (count, reason)`. The expected census, 25 files and 50 reads, is: lib/chart crypto_simple_chart 2 sparkline+wash, crypto_live_chart 2 lines+wash; lib/components/coins/view/coin_card_row 2 wash; lib/dashboard/chart markets_table 2 wash+sparkline, markets_cards 2 wash, markets_hero_card 4 wash+chart line; lib/tokens/token_info_screen 2 wash; lib/dev/design_gallery_screen 2 swatch; lib/web web_view_windows 1 and web_view_mobile 1 lock icon; lib/components custom_future_builder 1 icon, feedback/gw_error_state 4 wash+border+standalone icon, toast/toast_widget 2 accent icon, incorrect_pin 1 fill, inputs/gw_text_field 3 border, inputs/gw_select 2 border; lib/banxa/banxa_components/order_status_style 2 wash; lib/dashboard/home/widgets transaction_displays 2 wash, transaction_badge 3 fill; lib/squid_router swap_settings_drawer 1 field edge, swap_screen 2 wash; lib/dashboard/bridge/bridge_screen 1 wash; lib/dashboard/compute/compute_panel 2 dot; lib/submit_job/view/widgets job_step_list 1 dot, job_steps 3 dot. If the scan disagrees, read the site and classify it by THE RULE; never just copy the number.</action>
  <verify><automated>flutter test test/theme/status_text_color_invariant_test.dart test/theme/</automated>
    <human-check>Light mode: a swap refusal CTA, a form error, a WalletConnect "Retry Connect" state and a route's Price Impact read as a legible darker red or green. Dark mode looks unchanged.</human-check></verify>
  <acceptance_criteria>
    - The census test passes with 25 entries summing to 50. Adding `color: gw.statusError` to any lib/ file makes it fail (try once, then revert)
    - Raw reads: route_details_card 0, transaction_displays 2
    - `flutter test` (full suite) passes, with the count quoted in SUMMARY
  </acceptance_criteria>
  <done>Goal 2 holds across lib/, and any future raw text read fails a test that names the rule.</done>
</task>

</tasks>

<threat_model>
No trust boundary is crossed. These are colour lookups plus a read-only source-scan test.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-04 | Tampering | label colours | low | accept | Presentation only. reown_connect_button's connect/disconnect logic is not touched |
</threat_model>

<verification>
`dart format --output=none --set-exit-if-changed` on the touched files; `flutter analyze` with `$?` equal to 0; `bash tool/check_brace_style.sh`;
full `flutter test`. After `git add`, `git ls-files --eol test/theme/status_text_color_invariant_test.dart` shows `i/lf`.
Tests that pin a raw colour in dark mode keep passing, because the partners are identical there. If a light-mode pin fails, update it to the partner; do not revert the swap.
</verification>

<success_criteria>
The census test and the full suite pass, and no foreground anywhere in lib/ reads the raw status tokens.
</success_criteria>

## Artifacts this phase produces

This plan: `test/theme/status_text_color_invariant_test.dart` (new), nine swapped files, and `32-04-SUMMARY.md` with the full-suite count.
Phase close: move the four source todos from `.planning/todos/pending/` to `.planning/todos/completed/`.

<output>
Create `.planning/phases/32-contrast-and-text-scale-accessibility-pass/32-04-SUMMARY.md` (at most 40 lines) when done.
</output>
