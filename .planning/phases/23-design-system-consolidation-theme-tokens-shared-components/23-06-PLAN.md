---
phase: 23-design-system-consolidation-theme-tokens-shared-components
plan: 06
type: execute
wave: 6
depends_on: ["23-05"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/23-design-system-consolidation-theme-tokens-shared-components/23-06-CLOSEOUT.md
autonomous: false
requirements: [ORG-04, ORG-05]

must_haves:
  truths:
    - "The phase's claims are proven by re-run output quoted verbatim, not asserted from memory."
    - "The whole app has been walked in BOTH appearance modes against a specific checkable list, and every item has a verdict."
    - "ORG-01 through ORG-05 carry traceability rows, with ORG-05 recorded honestly as partial."
    - "Everything this phase found and deliberately did not fix is collected in one handover list naming its source document."
  artifacts:
    - ".planning/phases/23-design-system-consolidation-theme-tokens-shared-components/23-06-CLOSEOUT.md"
  key_links:
    - "WHAT PROVES THIS PLAN: re-run gate output from a clean tree, plus a human walk that this phase made LOAD-BEARING rather than confirmatory. With no golden baseline, the walk is not a nice-to-have -- it is the only check that sees layout and interaction at all. That is why the checklist below is numbered, screen-by-screen and states what a failure looks like, instead of saying 'look at it'."
    - "This project has a standing no-unearned-PASS rule. Anything not walked is recorded OUTSTANDING, never assumed passing."
    - "ORG-01, ORG-02 and ORG-03 belong to Phase 22 and were never written into REQUIREMENTS.md -- the plan that was going to do it was superseded. This plan closes that gap as well as its own."
---

<objective>
Close the phase honestly.

Five plans changed the colour layer from three competing sources to one compiler-enforced semantic
layer, fixed three mode-breaking offenders with measured evidence, closed one accessibility
regression, and collapsed one duplicated pattern while refusing four others on measured grounds.

This plan proves those claims rather than repeating them, walks the app in both appearance modes
against a specific list, writes the requirement traceability that two phases now owe, and hands the
next phase everything this one found and chose not to fix.

Purpose: end the phase with numbers that were re-run and a handover that was written down.
Output: the closeout record, the requirement rows, and the handover list.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/23-design-system-consolidation-theme-tokens-shared-components/23-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<environment>
**The Flutter SDK is NOT on `PATH`.** Prefix every session, before ANY flutter/dart command:
`export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"`

`flutter analyze` exits non-zero on infos (`fatal-infos` defaults true).

A running `genius_wallet.exe` locks the DLL and fails the next Windows build. Kill it first — and note
you will be launching the app for the walk, so kill it again before any later build.

**Entering baseline:** analyzer **0 / exit 0**, format clean, brace gate 0, raw-colour gate 0 over its
covered scope, all four `tool/*.sh` gates green, full suite green above 512.

**The 2026-07-28 phase-entry baseline, for the before/after table:** `flutter analyze` 0 issues /
exit 0 · `flutter test` 512 pass / 0 fail · `tool/check_brace_style.sh --count` 0 ·
`dart format --set-exit-if-changed lib test` exit 0 · CI `quality` job wired and blocking but never
executed · `GeniusWalletColors` ~46 public members with ~288 call sites outside `lib/theme/` ·
raw colour references outside `lib/theme/` 525, of which 83 the mode-breaking white/black/grey subset ·
`GWColors` 21 fields.

**There is NO golden baseline and none will be created.** Do not write `matchesGoldenFile`, do not
create `test/goldens/`, do not install a snapshot or visual-diff package.
</environment>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: The phase walk — both appearance modes, two widths</name>
  <files>(none — verification gate, no files change)</files>
  <read_first>
    - Every `23-0N-SUMMARY.md` in the phase directory, so the walk checks claims that were actually
      made rather than a generic checklist. In particular: which screens 23-02's per-directory
      migration touched, which pairs 23-03 measured, and which surfaces 23-05 migrated in each batch.
    - `23-03-CONTRAST.md` — so you can say which items below are expected to look *different* from
      before, and which must look identical.
  </read_first>
  <action>
    Build and run the app, then halt and hand the checklist below to the developer. Do not start Task
    2 until it returns. Anything not walked is recorded OUTSTANDING, never assumed passing.

    Before handing it over, state plainly in the handover message which three items are expected to
    look **different** (the toast, the button foreground, the swap drawer warning — all deliberate
    fixes with measured ratios in `23-03-CONTRAST.md`) and that **everything else must look
    identical** to the pre-phase build. A walker who does not know which changes are intended cannot
    report a regression.
  </action>
  <acceptance_criteria>
    - All twelve checklist items were exercised, each in BOTH appearance modes.
    - Every item has an explicit verdict recorded in the closeout — pass, fail, or not reached.
      "Should be fine" is not a verdict and neither is silence.
    - Items 10 and 11 were checked at a NARROW window width as well as desktop width.
    - Anything reported is either fixed before the phase closes or recorded with a status and a named
      follow-up, never absorbed.
  </acceptance_criteria>
  <what-built>
    Phase 23 consolidated the design system's colour layer and collapsed one duplicated pattern:

    - `GWColors` extended from 21 fields to full name parity with the legacy palette, seeded from the
      same primitives, with a parity test asserting every token resolves identically in both modes.
    - Roughly 260 colour reads across ~80 files moved onto `context.gw` by an AST rewriter — no
      package installed, `pubspec` untouched.
    - The toast widget's inverted light-mode palette, the button's hardcoded white foreground, and the
      raw colours throughout the dApp connection surface all fixed, each with a WCAG ratio measured
      and then asserted in `test/theme/theme_contrast_test.dart`.
    - The swap settings drawer's forked warning widget deleted; `GWWarningNote` and its documented
      light-mode amber contrast fix restored.
    - The raw palette demoted to a genuinely private primitive layer — reachability is now a compile
      error, not a convention. A scoped `tool/check_raw_colors.sh` gate wired into CI.
    - Twelve hand-rolled hover implementations replaced by one `GWHoverable`, with each call site's
      visual response moved verbatim.

    **What this phase deliberately did NOT do,** and why it matters to the walk: there is no golden
    baseline. Four other extractions were refused or deferred on measured grounds. So this walk is the
    only check in the whole phase that sees layout and interaction. It is load-bearing.
  </what-built>
  <how-to-verify>
    Start in **dark** mode. Walk items 1-9, noting how each looks. Then open Settings and switch to
    **light** mode, and walk them all again. Items 10-12 have their own instructions.

    Three things are *expected* to look different from before the phase: the toast (item 7), the
    button labels (item 8), and the swap drawer's warning note in light mode (item 4). **Everything
    else must look the same as it did before.**

    1. **Dashboard** — balance header, holdings list, transactions list, markets table, news cards.
       Hover a card and a markets row: the reaction must be exactly what it was (cards lift and their
       hairline strengthens; rows highlight). A hover that no longer reacts, or reacts differently, is
       a regression from the hover extraction.
    2. **Dashboard timeframe tabs** — click through them. The selected tab's treatment and the hover
       on unselected tabs must be unchanged.
    3. **A token detail screen** — tap a coin. Chart, price header, the address row, the stat rows,
       the timeframe tabs. Hover the copy affordance. Copy the address and paste it somewhere: the
       pasted value must be the FULL address, not the shortened form shown on screen.
    4. **Swap** — the two amount fields (hover them), the token selectors, then open the settings
       drawer. In **light** mode the warning note must be clearly legible. This is the accessibility
       fix; it is expected to look different and better.
    5. **Settings** — toggle appearance dark → light → dark. Every visible surface must flip *under
       you*, not only after leaving and returning. A surface that stays dark in light mode is a
       frozen colour.
    6. **Submit logs** (Settings → logs) — the monospace text must still be monospace, with characters
       in even columns. If it looks proportional, the font token lost the family name.
    7. **A toast** — trigger a success and an error toast in **light** mode. Both must be readable.
       This is expected to look different from before; before, it was unreadable.
    8. **Buttons** — find each button variant (primary, secondary, and any tertiary/destructive) and
       check enabled, hovered, pressed and **disabled** in both modes. A disabled button must still
       read as clearly disabled. This is expected to look different and must not be ambiguous.
    9. **A dApp connect flow**, if reachable — the connect prompt and any approval text must be
       readable in both modes.
    10. **An onboarding screen at a NARROW window width** — drag the window narrow. Nothing may run
        edge to edge with no gutter. This repo has shipped that bug once.
    11. **The dashboard at a NARROW window width** — same check.
    12. **Recovery phrase screen** — the phrase must render in the monospace family, and no copy
        affordance may have appeared that was not there before.

    Then confirm the numbers in the closeout document match what you see: analyze 0, tests green, all
    gates green, and the CI `quality` job green on GitHub.

    **What to report:** anything that looks different from before that is not items 4, 7 or 8;
    anything illegible in light mode; anything that stops responding to the appearance toggle; any
    hover that changed or stopped; any layout that runs to the window edge at narrow width.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or list the item numbers that failed and what you saw.</resume-signal>

  <done>All twelve walk items were exercised in both appearance modes, items 10 and 11 also at narrow width, and every item has an explicit verdict recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run every gate from a clean tree and record the numbers</name>

  <read_first>
    - Every `23-0N-SUMMARY.md` in the phase directory — the claims you are about to test.
    - `.planning/phases/23-.../23-04-GATE-SCOPE.md` — the raw-colour gate's covered scope, so the
      count you record is labelled with what it covers.
  </read_first>

  <files>.planning/phases/23-design-system-consolidation-theme-tokens-shared-components/23-06-CLOSEOUT.md</files>

  <action>
    From a clean tree, run every gate one final time and record the **real output verbatim**, never a
    paraphrase:

      - `flutter analyze --no-pub` at the repo root and in `packages/genius_api`
      - `dart format --output=none --set-exit-if-changed lib test`
      - `bash tool/check_brace_style.sh --count` and `--self-test`
      - `bash tool/check_raw_colors.sh --count` and `--self-test`
      - `bash tool/check_no_new_key_logging.sh`
      - `bash tool/check_onboarding_seed_safety.sh`
      - `bash tool/verify_additive_boundary.sh`
      - `flutter test --no-pub` (full)
      - the CI `quality` job on GitHub — and state whether it has now actually executed, since it was
        wired and blocking but never run as of 2026-07-28

    Then write the before/after table against the 2026-07-28 baseline in this plan's `<environment>`:
    test count, analyzer count, brace violations, raw-colour count over the gate's covered scope (say
    what it covers), legacy-palette call sites outside `lib/theme/`, `GWColors` field count, and net
    LOC change across `lib/`.

    Include a short, honest section titled **"What proved this phase, and what did not."** State
    plainly: value equality proved the token migration; the compiler proved the primitive demotion;
    measured contrast ratios proved the accessibility fixes; the builder shape proved the hover
    extraction preserved paint; and **nothing automated proved layout or spacing anywhere**, because
    this phase has no visual baseline. Name that as the phase's residual risk and name the walk as
    what stood in for it. A closeout that claims more coverage than it had is worse than one that
    admits a gap — the next phase plans against this document.
  </action>

  <verify>
    <automated>export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH" && flutter analyze --no-pub && dart format --output=none --set-exit-if-changed lib test && bash tool/check_brace_style.sh && bash tool/check_raw_colors.sh && bash tool/verify_additive_boundary.sh && bash tool/check_no_new_key_logging.sh && bash tool/check_onboarding_seed_safety.sh && flutter test --no-pub 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - Every gate above was run from a clean tree and its real output is quoted in
      `23-06-CLOSEOUT.md`. Quoted means copied, not summarised.
    - The before/after table is complete, with the 2026-07-28 baseline beside each final number.
    - The raw-colour count is labelled with the directories the gate actually covers.
    - The CI `quality` job's status is reported, including whether this phase was its first execution.
    - The "What proved this phase, and what did not" section exists and names the layout/spacing gap
      explicitly.
    - The walk's twelve verdicts from Task 1 are transcribed into the closeout.
  </acceptance_criteria>

  <done>Every gate was re-run from a clean tree with its real output quoted in 23-06-CLOSEOUT.md, the before/after table is complete against the 2026-07-28 baseline, and the what-proved-this-phase section names the layout gap explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Requirement traceability and the handover list</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` in full — its table structure and its existing ID conventions.
      Note that ORG-01 through ORG-05 are **absent**: the Phase 22 plan that was going to add them was
      superseded along with the golden baseline, so two phases' traceability is outstanding.
    - `.planning/ROADMAP.md` § Phase 23 — the requirement IDs and the stale seven-plan list, and the
      blocked-on-a-verification-decision banner that this re-plan resolves.
    - `23-05-EXTRACTION-AUDIT.md` — the refusals and deferrals that feed the handover.
    - `23-04-GATE-SCOPE.md` — the uncovered directories that feed the handover.
  </read_first>

  <files>
.planning/REQUIREMENTS.md
.planning/phases/23-design-system-consolidation-theme-tokens-shared-components/23-06-CLOSEOUT.md
  </files>

  <action>
    **Traceability.** Add ORG-01 through ORG-05 to `.planning/REQUIREMENTS.md` with a row each,
    mapping to the phase that satisfied them and its status. Use a **scoped edit**, never a whole-file
    rewrite:
      - ORG-01 (rules mechanically enforced in CI) → Phase 22, plus this phase's raw-colour gate
      - ORG-02 (dead code removed) → Phase 22
      - ORG-03 (analyzer clean) → Phase 22
      - ORG-04 (one colour source of truth) → Phase 23, plans 01-04
      - ORG-05 (duplication collapsed at 3+ call sites) → Phase 23, plan 05 — **mark this PARTIAL, not
        complete.** One family was collapsed; four candidates were refused or deferred on measured
        grounds. Point the row at `23-05-EXTRACTION-AUDIT.md` as the evidence. A row claiming ORG-05
        complete would be the unearned PASS this project has a standing rule against.

    **Handover.** Collect into one list in `23-06-CLOSEOUT.md` everything this phase found and
    deliberately did not fix, each item naming its source document so a future reader can check the
    reasoning rather than re-derive it:
      - the raw-colour gate's uncovered directories with their counts, and the statement that a full
        de-hex of the remaining raw references across `lib/` is larger than one phase
        (`23-04-GATE-SCOPE.md`)
      - the four refused or deferred extractions with their measured reasons, including the timeframe
        selector's failed character-identical premise and the control-track colour divergence between
        its two copies (`23-05-EXTRACTION-AUDIT.md`, plus the existing
        `.planning/todos/pending/2026-07-24-unify-timeframe-segment-component.md`)
      - the `GWScreen` scaffold sweep, deferred whole, with the reason (it is a layout change with no
        automated proof) and the note that it needs a functional test net first
      - the platform-generic monospace reference in the Banxa order-details card (`23-04-SUMMARY.md`)
      - any clipboard site that failed the full-value or no-logging check (`23-05-EXTRACTION-AUDIT.md`)
      - any deliberate context-free colour consumer served by a narrow named accessor
        (`23-03-SUMMARY.md`, `23-04-SUMMARY.md`)
      - the known x64 WalletConnect architectural finding in `lib/reown/`, untouched by design
      - **the golden/visual-regression gap itself.** State it as a standing decision with its
        consequence: layout and spacing changes ship unverified, which is why `GWScreen` and three
        extractions are deferred. If a future phase wants those, it needs a functional test net first.
        Name `integration_test` and `patrol` as the Flutter-native candidates the developer already
        mentioned as a someday item, and note that a browser driver is technically wrong for Flutter
        because it renders to a single canvas with no DOM to select.

    **ROADMAP.** Update the Phase 23 entry with a scoped edit: remove the blocked-on-a-verification-
    decision banner (it is resolved — this re-plan took option 2, human-walk and test-based
    verification, narrowed per option 3), correct the plan count to 6, and replace the seven-item plan
    list with the six that shipped. Do not rewrite the file.
  </action>

  <verify>
    <automated>grep -c 'ORG-0' .planning/REQUIREMENTS.md && grep -q 'ORG-05' .planning/REQUIREMENTS.md && grep -c '23-0[1-6]-PLAN.md' .planning/ROADMAP.md && test -f .planning/phases/23-design-system-consolidation-theme-tokens-shared-components/23-06-CLOSEOUT.md</automated>
  </verify>

  <acceptance_criteria>
    - ORG-01 through ORG-05 appear in `.planning/REQUIREMENTS.md` with traceability rows and statuses.
    - ORG-05's row says PARTIAL and points at the extraction audit.
    - The edits to `REQUIREMENTS.md` and `ROADMAP.md` were scoped replacements; `git diff` on each
      shows only the intended section changed.
    - The ROADMAP Phase 23 entry no longer carries the blocked banner, lists 6 plans, and its plan
      list matches the files on disk.
    - The handover list is complete and every item names its source document.
    - The golden/visual-regression gap is stated as a standing decision with its consequence, not
      omitted and not apologised for.
  </acceptance_criteria>

  <done>ORG-01 through ORG-05 carry traceability rows with ORG-05 marked PARTIAL, ROADMAP Phase 23 is de-blocked and lists the 6 plans on disk, both edits were scoped, and the handover list names a source document per item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase closeout → project record | An unearned PASS is this project's documented failure mode; every later phase plans against this document |
| walk coverage → shipped quality | With no visual baseline, an unwalked screen is an unverified screen, not a probably-fine one |
| handover list → future work | A finding discovered and not written down is thrown away, and the next planner re-derives it from a worse position |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-33 | Repudiation | closing the phase on unverified claims | high | mitigate | Task 2 re-runs every gate from a clean tree and quotes real output; the walk is a blocking checkpoint before it. |
| T-23-34 | Spoofing | a walk that misses the frozen-colour and hover failure modes | high | mitigate | The checklist is numbered per screen, states what a failure looks like for each, names which three items are expected to differ, and mandates both modes plus two widths on the two layout-sensitive items. |
| T-23-35 | Repudiation | ORG-05 recorded complete when four candidates were refused | high | mitigate | ORG-05's row is explicitly PARTIAL and points at the extraction audit; the closeout's "what proved this phase, and what did not" section names the layout gap. |
| T-23-36 | Repudiation | findings discovered but not carried forward | medium | mitigate | Task 3 collects the handover list from named source documents, including the visual-regression gap itself as a standing decision with its consequence. |
| T-23-37 | Tampering | a whole-file rewrite of REQUIREMENTS.md or ROADMAP.md destroying sibling entries | high | mitigate | Scoped edits only, with `git diff` on each file as an acceptance criterion. |
| T-23-38 | Information Disclosure | a copy affordance appearing on a seed-phrase surface | high | mitigate | Walk item 12 checks explicitly that no clipboard affordance appeared where one did not exist before. |
</threat_model>

<verification>
1. The twelve-item walk is complete, in both appearance modes, with items 10 and 11 also at narrow width, and every item carries a verdict.
2. `flutter analyze --no-pub` exits 0 at the repo root and in `packages/genius_api`.
3. `dart format --output=none --set-exit-if-changed lib test` exits 0.
4. Both style gates report 0 and both self-tests pass.
5. All three security gates exit 0.
6. Full `flutter test --no-pub` green, above the 512 entry baseline.
7. The CI `quality` job status is reported, including whether it has now executed at all.
8. `23-06-CLOSEOUT.md` carries the before/after table with verbatim output, the walk verdicts, and the "what proved this phase, and what did not" section.
9. `.planning/REQUIREMENTS.md` carries ORG-01..ORG-05, with ORG-05 marked PARTIAL.
10. `.planning/ROADMAP.md` Phase 23 is de-blocked, lists 6 plans, and matches disk.
11. The handover list is written and every item names its source.
</verification>

<success_criteria>
- Every gate is green and every number was re-run and quoted, not remembered.
- The app was walked in both appearance modes against a specific list, and the walk's load-bearing
  role in this phase is stated rather than implied.
- Requirement traceability exists for both Phase 22 and Phase 23, with ORG-05 honestly partial.
- What this phase found and did not fix — including the visual-regression gap itself — is handed
  forward rather than lost.
</success_criteria>

<artifacts_this_plan_produces>
| Artifact | Kind | Note |
|----------|------|------|
| `.planning/phases/23-.../23-06-CLOSEOUT.md` | new file | verbatim gate output, the before/after table against the 2026-07-28 baseline, the twelve walk verdicts, the "what proved this phase, and what did not" section, and the handover list |
| `.planning/REQUIREMENTS.md` | modified (scoped) | ORG-01..ORG-05 traceability rows; ORG-05 PARTIAL |
| `.planning/ROADMAP.md` | modified (scoped) | Phase 23 de-blocked, plan count 6, plan list matching disk |

New public symbols: none. This plan produces records.
</artifacts_this_plan_produces>

<output>
Create `.planning/phases/23-design-system-consolidation-theme-tokens-shared-components/23-06-SUMMARY.md` when done.
Record: the twelve walk verdicts, the full before/after table, the CI `quality` job status and whether
this was its first run, the requirement rows added with ORG-05's partial status and its reason, and
the handover list with its source documents.
</output>
