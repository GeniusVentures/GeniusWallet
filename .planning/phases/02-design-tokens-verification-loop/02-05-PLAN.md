---
phase: 02-design-tokens-verification-loop
plan: 05
type: execute
wave: 4
depends_on: ["02-02", "02-04"]
files_modified:
  - lib/dev/token_probe_screen.dart
  - lib/navigation/router.dart
  - lib/test/dev_tools_widget.dart
autonomous: true
requirements: [DS-01, BLD-02]
user_setup: []

must_haves:
  truths:
    - "With GW_DEV_TOOLS=true, a `Tokens` button in the Dev row opens `/dev/token-probe`."
    - "The probe renders the new typography, surface decoration, brand CTA gradient, spacing and radius tokens."
    - "Tapping the probe's appearance toggle flips its colors between the dark and light values live, with no restart."
    - "In a normal build the probe is unreachable — no entry point, no nav link."
    - "Editing a token value and hot-reloading applies it without a full restart; a breakpoint in the probe's build() is hit by the Dart debugger."
    - "Every existing screen still renders exactly as before."
  artifacts:
    - lib/dev/token_probe_screen.dart
    - .planning/phases/02-design-tokens-verification-loop/02-VERIFICATION.md
  key_links:
    - "The `Tokens` button lives INSIDE DevToolsWidget, so it inherits the `kDebugMode && kShowDevTools` gate plan 02-02 put on that widget's single call site. Do not add a second flag and do not add a second gated call site."
    - "The route is appended to router.dart's top-level routes list — outside the ShellRoute, additive only, no existing entry reordered or modified."
    - "The probe wraps its body in ValueListenableBuilder<GWAppearanceMode> on GWAppearance.instance — without it, setMode() persists but nothing rebuilds and the flip looks broken."
---

<objective>
Build the probe surface and close the phase by running the BLD-02 verification protocol end to end.
The probe is a dev-gated screen whose only job is to prove the token layer resolves and flips at
runtime, without any un-ported screen depending on it (UI-SPEC §9). Then execute UI-SPEC §7's five
checks and write them up as the phase's verification record.

Purpose: ROADMAP criterion 3 asks for the token set to be demonstrated live "without touching an
un-ported screen" — that is exactly what a probe is for. And BLD-02 is not a document: the
verification loop is established by running it once, here, and the write-up is what every later phase
inherits. The forward-port reached 0 analyze errors and dropped 37 behaviors; this plan is the
antidote to that being possible again.
Output: `lib/dev/token_probe_screen.dart`, one route, one gated button, and `02-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-tokens-verification-loop/02-UI-SPEC.md
@.planning/phases/02-design-tokens-verification-loop/02-02-SUMMARY.md
@.planning/phases/02-design-tokens-verification-loop/02-04-SUMMARY.md

Target files as they exist today:
@lib/navigation/router.dart
@lib/test/dev_tools_widget.dart
@lib/theme/gw_appearance.dart
</context>

<standing_run_recipe>
This is the standing command for the rest of the milestone (UI-SPEC §7). Task 3 records it verbatim
in `02-VERIFICATION.md`.

```
CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release -DGENIUS_DEPENDENCY_BRANCH=develop -Dc-ares_DIR=C:/Users/User/Documents/Projects/GNUS/thirdparty/build/Windows/Release/cares/lib/cmake/c-ares" flutter run -d windows --debug
```
Append `--dart-define=GW_DEV_TOOLS=true` for dev tools. `CMAKE_BUILD_TYPE=Release` is deliberate and
correct: the multi-config VS generator takes the build type from `--config`, while the dependency
downloader keys off `CMAKE_BUILD_TYPE`. Do not "fix" it. Bash/Git-Bash syntax, not PowerShell.
Cold build: several minutes. Hot reload (`r`): ~1s.

There is NO working test harness (`flutter test` does not compile — APP-02). Never add or run tests.
`flutter analyze` is a gate, never evidence.
</standing_run_recipe>

<tasks>

<task type="auto">
  <name>Task 1: Build the token probe screen</name>
  <files>lib/dev/token_probe_screen.dart</files>
  <action>
    Create a new `TokenProbeScreen` widget at lib/dev/token_probe_screen.dart, per UI-SPEC §9. It is
    additive: nothing existing imports it, and it imports no existing screen.

    Use a plain `Scaffold` — NOT `AppScreenView`, which is an existing develop widget with its own
    styling contract and is out of scope. Wrap the body in a `ValueListenableBuilder<GWAppearanceMode>`
    listening to `GWAppearance.instance`, mirroring the pattern main.dart will eventually use for the
    whole app in Phase 4. Without that builder the toggle persists the mode but nothing rebuilds, and
    the flip will look broken. Paint the Scaffold's background from `GeniusWalletColors.surfaceBase`
    so the canvas itself demonstrably flips.

    Body content, using ONLY the new additive tokens (this is the point of the screen — it must fail
    visibly if a token does not resolve):
    - A heading reading `Design tokens` in `GeniusWalletTypography.headlineLg`.
    - Body text in `GeniusWalletTypography.bodyMd`.
    - A card built with `GWDecorations.surface()` — its radius, elevation and hairline border all
      resolve from tokens.
    - A button filled with `GeniusWalletGradient.brandCta`, labelled with
      `GeniusWalletColors.textOnBrand`. Per `DESIGN_SYSTEM.md` §5.1 brand fills never use white text —
      use the on-brand token, not `Colors.white`.
    - A visible use of at least one `space*` token for layout padding, and one `radius*` token beyond
      the card default.
    - An appearance toggle button whose label reads the mode it would switch TO — `Dark` when
      currently light, `Light` when currently dark (UI-SPEC §6) — calling
      `GWAppearance.instance.setMode(...)` with the opposite of `GWAppearance.isLight`.

    In `initState`, call `GWAppearance.instance.load()` once. This exercises the preferences box plan
    02-01 opened, and makes the persisted mode restore when you re-enter the probe — turning that
    box-open from dead code into something the human check actually proves.

    **Do not instantiate `GWCanvasBackground`** (UI-SPEC §1.3 note 3 / §9). Its noise texture is not
    bundled until Phase 3 (DS-04 / `d8db88c`); instantiating it here would throw or render a
    missing-asset placeholder.

    Keep it to one screen's worth of widget code. This is a probe, not a gallery — the full primitive
    gallery is DS-03 in Phase 3.
  </action>
  <verify>
    <automated>test -f lib/dev/token_probe_screen.dart &amp;&amp; grep -c 'ValueListenableBuilder&lt;GWAppearanceMode&gt;' lib/dev/token_probe_screen.dart | awk '{exit $1 < 1}' &amp;&amp; grep -c 'GWAppearance.instance.setMode' lib/dev/token_probe_screen.dart | awk '{exit $1 < 1}' &amp;&amp; grep -c 'GWAppearance.instance.load()' lib/dev/token_probe_screen.dart | grep -qx 1 &amp;&amp; grep -c 'GeniusWalletTypography.headlineLg' lib/dev/token_probe_screen.dart | awk '{exit $1 < 1}' &amp;&amp; grep -c 'GWDecorations.surface(' lib/dev/token_probe_screen.dart | awk '{exit $1 < 1}' &amp;&amp; grep -c 'GeniusWalletGradient.brandCta' lib/dev/token_probe_screen.dart | awk '{exit $1 < 1}' &amp;&amp; grep -c 'GeniusWalletColors.textOnBrand' lib/dev/token_probe_screen.dart | awk '{exit $1 < 1}' &amp;&amp; grep -Ec 'GWCanvasBackground|AppScreenView|Colors.white' lib/dev/token_probe_screen.dart | grep -qx 0 &amp;&amp; flutter analyze lib</automated>
    <human-check>Deferred to Task 2 — the screen has no entry point yet.</human-check>
  </verify>
  <done>`TokenProbeScreen` exists, listens to `GWAppearance.instance`, calls `load()` once at init, renders heading/body/card/gradient-button from the new tokens, and instantiates neither `GWCanvasBackground` nor `AppScreenView`. `flutter analyze lib` reports 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Register the route and add the gated entry point</name>
  <files>lib/navigation/router.dart, lib/test/dev_tools_widget.dart</files>
  <action>
    Two additive edits.

    In lib/navigation/router.dart: append one new `GoRoute` for path `/dev/token-probe`, building
    `TokenProbeScreen`, to the TOP-LEVEL `routes:` list of `geniusWalletRouter` — the same list that
    holds `/`, `/buy`, `/createOrder`. Put it outside the `ShellRoute`, so the probe does not mount
    into the nav shell (the shell is Phase 4's subject and must stay out of this). Add the import for
    the probe screen alongside the existing imports. This is additive registration ONLY: do not
    reorder, modify, or re-path any existing route entry, and do not touch the `redirect:` callback —
    that callback drives SDK init, wallet loading and account fetch on every navigation, and is not
    this phase's business.

    In lib/test/dev_tools_widget.dart: add a `Tokens` button to the existing `Row`, after the three
    test buttons, that pushes `/dev/token-probe`. Putting it inside `DevToolsWidget` is deliberate —
    the widget already sits behind the `kDebugMode && kShowDevTools` gate plan 02-02 applied to its
    single call site in `responsive_overlay.dart`, so the probe entry point inherits that gate for
    free. Reuse `kShowDevTools`; do not invent a second flag (UI-SPEC §8), do not add a second gated
    call site, and do not re-gate inside the widget. Leave the existing `Dev` label and the three test
    buttons exactly as they are. The `Row` may need to become non-const to hold the new button — that
    is expected and is the only structural change permitted here.

    Do not touch `responsive_overlay.dart`; plan 02-02 owns it and its work is done.
  </action>
  <verify>
    <automated>grep -c "path: '/dev/token-probe'" lib/navigation/router.dart | grep -qx 1 &amp;&amp; grep -c 'token_probe_screen.dart' lib/navigation/router.dart | grep -qx 1 &amp;&amp; grep -c '/dev/token-probe' lib/test/dev_tools_widget.dart | grep -qx 1 &amp;&amp; grep -Ec 'kShowDevTools|kDebugMode' lib/test/dev_tools_widget.dart | grep -qx 0 &amp;&amp; git diff --numstat -- lib/components/overlay/responsive_overlay.dart lib/theme/theme.dart lib/main.dart | wc -l | grep -qx 0 &amp;&amp; flutter analyze lib</automated>
    <human-check>
      1. Close the reference Release exe at `GeniusWallet-3514\build\windows\x64\runner\Release\genius_wallet.exe` if running — shared Hive data dir, the two builds deadlock on lock files.
      2. Run the standing recipe WITH `--dart-define=GW_DEV_TOOLS=true`. Confirm the `Dev` row now carries a `Tokens` button alongside the three test buttons.
      3. Press `Tokens`. Confirm `/dev/token-probe` opens and renders: heading, body text, a card with a visibly rounded corner and hairline border, and a button filled with the green→blue horizontal CTA gradient with dark (not white) label text.
      4. Press the appearance toggle. Confirm the whole probe flips live, with no restart: background goes from near-black `#0B0D12` to light gray `#DCE0E6`, card from near-black to white, text from white to dark ink, hairline border flipping polarity. Press it again and confirm it flips back. **This is ROADMAP criterion 3.**
      5. Navigate back out of the probe and re-enter it. Confirm it reopens in the mode you left it in — this proves `load()` and the preferences box round-trip.
      6. Stop the app. Re-run the standing recipe with NO define. Confirm the `Dev` row and its `Tokens` button are both absent, and that `/dev/token-probe` is reachable from nowhere in the UI. **This is ROADMAP criterion 4 for the probe.**
    </human-check>
  </verify>
  <done>The route is registered top-level and additively; the `Tokens` button inherits `DevToolsWidget`'s existing gate with no second flag; `responsive_overlay.dart`, `theme.dart` and `main.dart` have zero diff; the probe opens, renders every token, flips live both directions, restores its persisted mode, and is absent from a normal build.</done>
</task>

<task type="auto">
  <name>Task 3: Run the BLD-02 protocol and record it</name>
  <files>.planning/phases/02-design-tokens-verification-loop/02-VERIFICATION.md</files>
  <action>
    Execute UI-SPEC §7's five checks against the finished phase and write the results to
    `02-VERIFICATION.md`. This document IS the BLD-02 deliverable: it is the loop every later phase
    inherits, so write the run recipe verbatim at the top (including the note that
    `CMAKE_BUILD_TYPE=Release` is deliberate) and state plainly that `flutter analyze` is a gate and
    never evidence, because analyze-clean is exactly what masked the forward-port's 37 regressions.

    Structure it as one section per ROADMAP Phase 2 success criterion (1–5), each recording: what was
    run, what was observed, and PASS/FAIL. Record real observations, not restatements of intent — a
    criterion with no observation behind it is FAIL, not PASS. Note the two standing environment
    facts for future phases: the reference Release exe and the develop build cannot run simultaneously
    (shared Hive data dir, they deadlock on lock files), and hot reload applies Dart edits in ~1s
    while a cold build takes several minutes.

    Two carry-forward items belong in the write-up as deferrals, not as passes:
    - ROADMAP criterion 4 mentions the onboarding `Mock` button. It does not exist on develop today —
      it is redesign-only and lands in Phase 6, which ports `wallet_creation_screen.dart` and
      `lib/dev/mock_mode.dart` together and carries `kShowDevTools` forward (UI-SPEC §8). Record it as
      out of scope with that reasoning; do not mark it verified.
    - The shell header height stays at develop's value this phase; the design's taller value is a
      real visual change owned by Phase 4, which re-skins the header that consumes it (UI-SPEC §2).

    Also tick the six checker sign-off boxes at the bottom of `02-UI-SPEC.md` and set its
    `**Approval:**` line, since the contract has now been executed and observed. Update its
    frontmatter `status` from `draft` accordingly.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-design-tokens-verification-loop/02-VERIFICATION.md &amp;&amp; grep -c 'GENIUS_DEPENDENCY_BRANCH=develop' .planning/phases/02-design-tokens-verification-loop/02-VERIFICATION.md | awk '{exit $1 < 1}' &amp;&amp; grep -Eic 'criterion' .planning/phases/02-design-tokens-verification-loop/02-VERIFICATION.md | awk '{exit $1 < 5}' &amp;&amp; grep -c '\- \[ \] Dimension' .planning/phases/02-design-tokens-verification-loop/02-UI-SPEC.md | grep -qx 0 &amp;&amp; flutter analyze lib</automated>
    <human-check>
      Run the five UI-SPEC §7 checks and record each observation as you go. Close the reference
      Release exe first.

      1. **Hot reload + debugger** (criterion 1). With the app running from the standing recipe, edit `GeniusWalletMotion.base`'s duration, press `r`, and confirm it applies with no full restart. Set a breakpoint in `TokenProbeScreen.build()`, open the probe, and confirm the Dart debugger attaches and hits it. Revert the token edit.
      2. **No-visual-change walk** (criterion 2). Normal build, no define. Walk every reachable route: splash → onboarding → dashboard → transactions → token detail → send → swap → bridge → markets → news → buy → settings → logs. Confirm no color shift, no spacing shift, no font change, no startup exception, no crash on any route transition. There should be literally zero observable difference — `appBarHeight` did not change, so even the header is identical.
      3. **Token + appearance probe** (criterion 3). Re-confirm Task 2's step 4: the probe flips dark↔light live.
      4. **Dev gating** (criterion 4). Re-confirm both directions: `Dev` row and `Tokens` button absent in a normal build, present with `--dart-define=GW_DEV_TOOLS=true`.
      5. **floatingLabelBehavior** (criterion 5 / finding 36). Open an existing screen with a `TextFormField` and confirm the label stays pinned above the field rather than sitting inside it as placeholder text. This holds automatically because `theme.dart` is untouched; the check catches a regression, it does not expect a change.
    </human-check>
  </verify>
  <done>`02-VERIFICATION.md` exists, records the standing run recipe verbatim, and carries a real observation plus a PASS/FAIL for each of ROADMAP Phase 2's five criteria — with the onboarding `Mock` button and the header height recorded as Phase 6 / Phase 4 deferrals rather than passes. `02-UI-SPEC.md`'s six sign-off boxes are ticked and its approval line is set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-gated route → shipped app | A new route is registered unconditionally in the router of a self-custody wallet; only its entry point is gated. |
| probe screen → app state | The probe runs inside the live app with a real router and a real Hive box. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-15 | Elevation of Privilege | `/dev/token-probe` route present in a release build | medium | mitigate | The route registers unconditionally (gating a `GoRoute` on a const would complicate the routes list for no gain), but it is reachable only through the `Tokens` button inside `DevToolsWidget`, which sits behind `kDebugMode && kShowDevTools` — so it is unreachable in release regardless of the define. Task 2's human-check step 6 confirms it is reachable from nowhere in a normal build. Residual exposure is nil: the screen reads no wallet state, holds no key material, takes no input, and performs no action beyond flipping a display enum. |
| T-02-16 | Tampering | `router.dart` `redirect:` callback | high | mitigate | The callback drives SDK init, wallet loading, account fetch and user-exists checks on every navigation — the exact surface whose breakage made wallets vanish at startup on the forward-port (finding 2). Task 2 forbids touching it and forbids reordering any existing route; the criterion-2 walk exercises every route to confirm. |
| T-02-17 | Information Disclosure | Probe screen contents | low | accept | Renders static token swatches and literal copy only. Reads no wallet, no balance, no address, no seed. |
| T-02-18 | Repudiation | `02-VERIFICATION.md` recording unearned passes | high | mitigate | This is the phase's headline risk given the project's history: the forward-port's "0 analyze errors" was recorded as success and masked 37 regressions. Task 3 requires a real observation behind every PASS, demotes `flutter analyze` to a gate in the document itself, and forces the two known gaps (`Mock` button, header height) to be recorded as deferrals rather than passes. |
</threat_model>

<verification>
Gate: `flutter analyze lib` reports 0 errors.

Mechanical proof of additivity: `git diff --stat -- lib/components/overlay/responsive_overlay.dart
lib/theme/theme.dart lib/main.dart` returns empty output; the router diff contains no deletions
outside the appended route's own insertion.

Evidence: `02-VERIFICATION.md`, carrying an observation and a PASS/FAIL for each of ROADMAP Phase 2's
five criteria, produced by actually running the app.
</verification>

<success_criteria>
- **ROADMAP criterion 1** — debug build launches, hot reload applies a token edit without restart, debugger hits a breakpoint. Recorded.
- **ROADMAP criterion 2** — every existing screen renders exactly as before; full walk, no exception. Recorded.
- **ROADMAP criterion 3** — the token set resolves at runtime and flips with light/dark on the probe, without touching an un-ported screen. Recorded.
- **ROADMAP criterion 4** — `Dev` row absent by default, present with the flag. Recorded, with the `Mock` button deferred to Phase 6 in writing.
- **ROADMAP criterion 5** — form-field labels stay pinned above the field (finding 36). Recorded.
- **BLD-02** is established as a loop later phases inherit, not as a claim.
- **DS-01** closes: the token vocabulary lives on develop and coexists with develop's theme without breaking un-ported screens.
</success_criteria>

<output>
Create `.planning/phases/02-design-tokens-verification-loop/02-05-SUMMARY.md` when done.
Also produced by this plan: `02-VERIFICATION.md` (the BLD-02 deliverable).
</output>
