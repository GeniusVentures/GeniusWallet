---
phase: 02-design-tokens-verification-loop
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - lib/theme/genius_wallet_motion.dart
  - lib/theme/genius_wallet_font_size.dart
  - lib/theme/genius_wallet_text.dart
  - lib/theme/genius_wallet_elevation.dart
  - lib/theme/genius_wallet_typography.dart
  - lib/theme/genius_wallet_decorations.dart
autonomous: true
requirements: [DS-01]
user_setup: []

must_haves:
  truths:
    - "All 6 remaining redesign theme files exist on develop and compile."
    - "Every existing screen still renders exactly as before — nothing consumes these classes yet."
  artifacts:
    - lib/theme/genius_wallet_motion.dart
    - lib/theme/genius_wallet_font_size.dart
    - lib/theme/genius_wallet_text.dart
    - lib/theme/genius_wallet_elevation.dart
    - lib/theme/genius_wallet_typography.dart
    - lib/theme/genius_wallet_decorations.dart
  key_links:
    - "GeniusWalletTypography resolves textPrimary/textSecondary from plan 02-03's color block and calls GoogleFonts.inter from plan 02-01's dependency."
    - "GeniusWalletElevation resolves brandPrimary/brandSecondary from plan 02-03."
    - "GWDecorations resolves radiusLg/radiusPill from plan 02-03's consts, borderSubtle from its colors, GeniusWalletElevation.card, and GWAppearance from plan 02-01."
    - "toMaterialTextTheme() exists but is NOT wired into theme.dart this phase — theme.dart is the file §1.1 excludes wholesale."
---

<objective>
Port the six remaining redesign theme files. All six are brand-new file paths with brand-new class
names and no develop equivalent — `GeniusWalletTypography`, `GeniusWalletElevation`,
`GeniusWalletMotion`, `GeniusWalletFontSize`, `GeniusWalletText`, `GWDecorations`/`GWCanvasBackground`.
Confirmed by grep: develop references none of these symbols anywhere in `lib/` or `packages/`. That
makes this the zero-collision half of DS-01 — the opposite risk profile to plan 02-03.

Purpose: these classes are what Phase 3's `gw_*` primitives will be built out of, and what plan
02-05's probe surface demonstrates. They land now so the vocabulary is complete before anything is
skinned.
Output: six new files under `lib/theme/`, consumed by nothing, changing nothing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-tokens-verification-loop/02-UI-SPEC.md
@.planning/phases/02-design-tokens-verification-loop/02-03-SUMMARY.md

Source to port FROM (reference worktree, read-only — do not edit):
@C:/Users/User/Documents/Projects/GNUS-compare/GeniusWallet-3514/lib/theme/genius_wallet_motion.dart
@C:/Users/User/Documents/Projects/GNUS-compare/GeniusWallet-3514/lib/theme/genius_wallet_font_size.dart
@C:/Users/User/Documents/Projects/GNUS-compare/GeniusWallet-3514/lib/theme/genius_wallet_text.dart
@C:/Users/User/Documents/Projects/GNUS-compare/GeniusWallet-3514/lib/theme/genius_wallet_elevation.dart
@C:/Users/User/Documents/Projects/GNUS-compare/GeniusWallet-3514/lib/theme/genius_wallet_typography.dart
@C:/Users/User/Documents/Projects/GNUS-compare/GeniusWallet-3514/lib/theme/genius_wallet_decorations.dart
</context>

<standing_run_recipe>
```
CMAKE_ARGUMENTS="-DCMAKE_BUILD_TYPE=Release -DGENIUS_DEPENDENCY_BRANCH=develop -Dc-ares_DIR=C:/Users/User/Documents/Projects/GNUS/thirdparty/build/Windows/Release/cares/lib/cmake/c-ares" flutter run -d windows --debug
```
`CMAKE_BUILD_TYPE=Release` is deliberate and correct — do not "fix" it. Bash/Git-Bash syntax.

There is NO working test harness (`flutter test` does not compile — APP-02). Never add or run tests.
`flutter analyze` is a gate, never evidence.
</standing_run_recipe>

<tasks>

<task type="auto">
  <name>Task 1: Port motion, font sizes, copy constants, and elevation</name>
  <files>lib/theme/genius_wallet_motion.dart, lib/theme/genius_wallet_font_size.dart, lib/theme/genius_wallet_text.dart, lib/theme/genius_wallet_elevation.dart</files>
  <action>
    Copy all four files from the reference worktree to `lib/theme/` verbatim — no adaptation needed,
    they carry no Hive import and no asset reference. Keep every value, comment and doc comment as
    written.

    - `genius_wallet_motion.dart` — `GeniusWalletMotion`: the three durations and two curves per
      UI-SPEC §5. The rule the class encodes (decelerative easing only; never ease-in or linear for
      interactive transitions, per `DESIGN_SYSTEM.md` §3.7) is carried by the token set itself; do
      not add a linear or ease-in curve.
    - `genius_wallet_font_size.dart` — `GeniusWalletFontSize`: five raw size constants.
    - `genius_wallet_text.dart` — `GeniusWalletText`: the onboarding/wallet copy string constants.
      Note this is copy for screens that land in later phases; nothing displays these strings this
      phase, which is consistent with UI-SPEC §6 ("no user-facing copy changes this phase").
    - `genius_wallet_elevation.dart` — `GeniusWalletElevation`: `card`, `dialog`, `glowBrand`,
      `glowGradient` per UI-SPEC §5. This one resolves `brandPrimary` and `brandSecondary` from plan
      02-03's color block, so it only compiles once 02-03 has landed.

    All four class names are new to develop — grep confirms zero existing references — so there is
    nothing to collide with. Do not import any of them from any existing screen.
  </action>
  <verify>
    <automated>test -f lib/theme/genius_wallet_motion.dart &amp;&amp; test -f lib/theme/genius_wallet_font_size.dart &amp;&amp; test -f lib/theme/genius_wallet_text.dart &amp;&amp; test -f lib/theme/genius_wallet_elevation.dart &amp;&amp; for f in genius_wallet_motion genius_wallet_font_size genius_wallet_text genius_wallet_elevation; do diff -q /c/Users/User/Documents/Projects/GNUS-compare/GeniusWallet-3514/lib/theme/$f.dart lib/theme/$f.dart || exit 1; done &amp;&amp; grep -rEc 'Curves.linear|Curves.easeIn[,)]' lib/theme/genius_wallet_motion.dart | grep -qx 0 &amp;&amp; flutter analyze lib</automated>
    <human-check>Deferred to Task 3's walk — all six files ship together.</human-check>
  </verify>
  <done>All four files exist and are byte-identical to their reference-worktree originals; `flutter analyze lib` reports 0 errors (proving `GeniusWalletElevation` resolves 02-03's brand colors).</done>
</task>

<task type="auto">
  <name>Task 2: Port the typography scale</name>
  <files>lib/theme/genius_wallet_typography.dart</files>
  <action>
    Copy `genius_wallet_typography.dart` from the reference worktree to `lib/theme/` verbatim. It
    needs no adaptation: `google_fonts` landed in plan 02-01 and `textPrimary`/`textSecondary` landed
    in 02-03.

    Preserve exactly, per UI-SPEC §3: all 13 styles as **getters, not cached finals** — they resolve
    `textPrimary`'s appearance-aware color at call time, so caching them would freeze the color at
    first access and silently break the light/dark flip that ROADMAP criterion 3 turns on. Preserve
    the tabular-figures font feature on the three numeric variants: `DESIGN_SYSTEM.md` §6.5 requires
    balances, addresses and percentages to align column-wise, and dropping it makes crypto amounts
    render with proportional digits — a real usability regression in a wallet, not a cosmetic one.
    Preserve the private `_inter` helper, the tight-tracking constant, and every size/height/weight
    exactly as written.

    Do not consolidate the scale. UI-SPEC §3's justification section records that its 13 styles /
    8 sizes / 4 weights are a verbatim port of `DESIGN_SYSTEM.md` §3.2 and its `gnus-tokens.json`
    mirror, shared with the gnus.ai marketing site, and were reviewed and accepted as-is against the
    checker's stricter default heuristic. Reducing them would fabricate a scale that is neither the
    site's nor the designer's.

    `toMaterialTextTheme()` comes across with the file and must compile — but **do not wire it into
    `theme.dart`**. That wiring lives in the design branch's `theme.dart`, which UI-SPEC §1.1
    excludes from this phase wholesale: it is a 100%-collision file that re-themes every Material
    widget in the app at once. `theme.dart` stays byte-identical to develop's version for the whole
    of Phase 2. Nothing consumes `GeniusWalletTypography` until plan 02-05's probe surface.
  </action>
  <verify>
    <automated>diff -q /c/Users/User/Documents/Projects/GNUS-compare/GeniusWallet-3514/lib/theme/genius_wallet_typography.dart lib/theme/genius_wallet_typography.dart &amp;&amp; grep -c 'FontFeature.tabularFigures()' lib/theme/genius_wallet_typography.dart | awk '{exit $1 < 1}' &amp;&amp; grep -c 'static TextStyle get ' lib/theme/genius_wallet_typography.dart | awk '{exit $1 != 13}' &amp;&amp; grep -c 'GoogleFonts.inter(' lib/theme/genius_wallet_typography.dart | awk '{exit $1 < 1}' &amp;&amp; git diff --numstat -- lib/theme/theme.dart | wc -l | grep -qx 0 &amp;&amp; grep -rc 'GeniusWalletTypography' lib/theme/theme.dart | grep -qx 0 &amp;&amp; flutter analyze lib</automated>
    <human-check>Deferred to Task 3's walk.</human-check>
  </verify>
  <done>The file is byte-identical to the reference original; all 13 styles are getters; tabular figures survive; `theme.dart` has zero diff and does not reference `GeniusWalletTypography`; `flutter analyze lib` reports 0 errors.</done>
</task>

<task type="auto">
  <name>Task 3: Port the decorations, then confirm nothing moved</name>
  <files>lib/theme/genius_wallet_decorations.dart</files>
  <action>
    Copy `genius_wallet_decorations.dart` from the reference worktree to `lib/theme/` verbatim,
    including both classes. Per UI-SPEC §1.3 note 3:

    - `GWDecorations` — the `BoxDecoration` helpers `surface()`, `pill()`, `actionCircle()` and the
      `heroGlow` field, plus the `canvas` / `canvasTopLight` / `surfaceSheen` gradients. None of
      these touch a bundled asset. They resolve `radiusLg`/`radiusPill` from plan 02-03's consts,
      `borderSubtle`/`brandPrimary` from its colors, `GeniusWalletElevation.card`/`.glowGradient`
      from Task 1, and `GWAppearance` from plan 02-01.
    - `GWCanvasBackground` — port it as a class. It compiles fine unreferenced, and leaving it out
      would mean re-porting the file in Phase 3. But it references a noise texture under
      `assets/images/textures/` that this phase does not bundle: that directory is owned by DS-04 /
      commit `d8db88c` and lands in Phase 3. **Do not instantiate `GWCanvasBackground` anywhere in
      Phase 2** — not here, and not in plan 02-05's probe surface. Instantiating it without the
      bundled asset either throws or renders a missing-asset placeholder, which is a Phase-3-owned
      bug, not a Phase 2 concern. Do not add the asset, do not touch the `flutter:` assets block in
      pubspec.yaml, and do not stub or delete the image reference — port the class as the design
      branch wrote it and leave it dormant.

    Then run the app and confirm the full set of six files changed nothing (see `<human-check>`).
  </action>
  <verify>
    <automated>diff -q /c/Users/User/Documents/Projects/GNUS-compare/GeniusWallet-3514/lib/theme/genius_wallet_decorations.dart lib/theme/genius_wallet_decorations.dart &amp;&amp; grep -rEc 'GWCanvasBackground\(' --include=*.dart lib | grep -v 'genius_wallet_decorations.dart' | grep -Ev ':0$' | wc -l | grep -qx 0 &amp;&amp; git diff --numstat -- pubspec.yaml lib/theme/theme.dart lib/main.dart | wc -l | grep -qx 0 &amp;&amp; flutter analyze lib</automated>
    <human-check>
      1. Close the reference Release exe at `GeniusWallet-3514\build\windows\x64\runner\Release\genius_wallet.exe` if running — shared Hive data dir, the two builds deadlock on lock files.
      2. Run the standing recipe with NO define.
      3. Walk the app top to bottom again — splash → onboarding → dashboard → transactions → token detail → send → swap → bridge → markets → news → buy → settings. Confirm no visual change from the 02-03 walk and no startup exception.
      4. Confirm no missing-asset placeholder (a grey box or broken-image icon) appears anywhere. Its absence is the evidence that `GWCanvasBackground` is dormant.
      Expected result: zero observable difference. These six classes are consumed by nothing, so anything you see here is a real defect.
    </human-check>
  </verify>
  <done>The file is byte-identical to the reference original; `GWCanvasBackground` is defined but instantiated nowhere in `lib/`; pubspec.yaml, `theme.dart` and `main.dart` have zero diff; `flutter analyze lib` reports 0 errors; the walk shows no visual change and no missing-asset placeholder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference worktree → develop | Six files copied wholesale from a 127-commit-divergent fork into a self-custody wallet's build. |
| asset reference → runtime | `GWCanvasBackground` names an asset path that does not resolve in this phase's bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering | Six new theme classes entering `lib/theme/` | low | mitigate | Verified additive by grep before planning: develop references none of these six class names anywhere in `lib/` or `packages/`. Each file is gated by `diff -q` against its reference original, so no unreviewed edit slips in, and by `flutter analyze lib`. No file touches key material, FFI, network or Hive. |
| T-02-12 | Denial of Service | `GWCanvasBackground` rendering an unbundled asset | medium | mitigate | Class ported but never instantiated — enforced by a negative grep across `lib/` and by the walk's missing-asset check. The asset lands with DS-04 / `d8db88c` in Phase 3. |
| T-02-13 | Tampering | `theme.dart` re-themed via `toMaterialTextTheme()` wiring | high | mitigate | `toMaterialTextTheme()` ships dormant. Gated by a zero-diff check on `theme.dart` plus a negative grep for `GeniusWalletTypography` inside it — wiring it would re-theme every Material widget at once and break ROADMAP criteria 2 and 5. |
| T-02-14 | Information Disclosure | `GeniusWalletText` copy constants | low | accept | Static UI copy for onboarding screens. Contains no secret; displays nothing this phase. |
</threat_model>

<verification>
Gate: `flutter analyze lib` reports 0 errors — which here does carry real weight for one narrow
claim: it proves the new classes resolve 02-01's and 02-03's symbols. It still proves nothing about
criterion 2.

Mechanical proof of additivity: all six files `diff -q` clean against their reference originals;
`git diff --stat -- lib/theme/theme.dart lib/main.dart pubspec.yaml` returns empty output; no
`GWCanvasBackground` instantiation exists in `lib/`.

Evidence: the app runs and every route looks exactly as it did after plan 02-03, with no
missing-asset placeholder anywhere.
</verification>

<success_criteria>
- DS-01's vocabulary is complete on develop: colors, typography, spacing, radius, elevation,
  gradients, motion and decorations all exist.
- ROADMAP criterion 2 still holds — six new classes, zero consumers, zero visual change.
- ROADMAP criterion 5 still holds — `theme.dart` remains untouched, so `floatingLabelBehavior: always`
  survives.
- Plan 02-05's probe surface has everything it needs: `GeniusWalletTypography.headlineLg`/`bodyMd`,
  `GWDecorations.surface()`, `GeniusWalletGradient.brandCta`, `GeniusWalletColors.textOnBrand`.
</success_criteria>

<output>
Create `.planning/phases/02-design-tokens-verification-loop/02-04-SUMMARY.md` when done.
Record that `GWCanvasBackground` is dormant pending DS-04 (Phase 3), and that
`toMaterialTextTheme()` is unwired pending the shell re-skin (Phase 4).
</output>
