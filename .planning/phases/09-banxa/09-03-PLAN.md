---
phase: 09-banxa
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - lib/screens/banxa_buy_screen.dart
  - lib/banxa/banxa_components/quote_card.dart
  - test/banxa/quote_card_test.dart
  - test/banxa/banxa_buy_screen_test.dart
  - .planning/todos/pending/2026-07-27-quote-card-duplicated-by-the-buy-screens-inline-block.md
autonomous: true
requirements: [SCR-05, GAP-05]
must_haves:
  truths:
    - "The buy screen's Create Order CTA is a real `GWButton` gradient, not a hand-rolled InkWell over an Ink BoxDecoration."
    - "The disabled Create Order rung uses `GWButton`'s own disabled treatment, and its label is still Create Order."
    - "Get Quote reads as the secondary step it is, not as a second primary."
    - "The inline quote block sits on a `GWCard` and its two lines carry token typography."
    - "`quote_card.dart` renders through `GWCard` with token typography — and produces zero user-visible change, because nothing calls it (D-08)."
    - "The buy screen shows no raw colour literal except the boot-overlay scrim, which is mode-invariant by construction."
  artifacts:
    - lib/screens/banxa_buy_screen.dart
    - lib/banxa/banxa_components/quote_card.dart
    - test/banxa/quote_card_test.dart
    - test/banxa/banxa_buy_screen_test.dart
    - .planning/todos/pending/2026-07-27-quote-card-duplicated-by-the-buy-screens-inline-block.md
  key_links:
    - "`state.canCreateOrder` still gates the CTA's `onPressed`; passing null is what produces the disabled rung, so the ladder is driven by the same expression it is today."
    - "The disclaimer dialog is still awaited before any order is created, and its `accepted` false path still short-circuits with the same snackbar."
    - "`showCheckoutOptionsSheet` is still called from both of this screen's existing call sites with the same three arguments."
  prohibitions:
    - "Do NOT repoint the buy screen at `QuoteCard`. D-08 chose re-skin-and-leave-dead over consolidation; repointing is restructuring, fenced by PROJECT.md §65. 09-UI-SPEC's Component Inventory recommends the consolidation — D-08 is dated after it and supersedes it."
    - "Do not delete `quote_card.dart` — D-08 rejected deletion too."
    - "Do not change `MakeOrderCubit` calls, `_amountLabel`, the controller-sync block, the boot-loading condition, the disclaimer copy, or the `redirectUrl ?? ''` / `?? BanxaApiService.redirectUrl` fallbacks (D-01/D-02). The two competing redirect definitions are finding 1's territory and are deferred."
    - "Do not convert the boot overlay's black-alpha scrim to a theme token — it is a temporary full-screen block, mode-invariant by construction, same class as the QR's white backing (09-UI-SPEC Screen Contract)."
    - "Do not touch `lib/banxa/banxa_order/*`, `banxa_api_services.dart`, `banxa_model.dart`, `banxa_helpers/*` (D-06), the four Phase-21 drawers (D-05), or the repository-root `banxa/` directory (AGENTS.md)."
  human_judgment:
    - "The ENABLED Create Order rung cannot be reached in a test without a live quote from the sandbox, which D-03 forbids. Only the disabled rung is automatable here; the enabled rung's paint stays a walk item and is OUTSTANDING."
    - "Whether the re-skinned form reads as a sibling of the Swap tab is a walk judgement. No walk is authorised (D-03)."
---

<objective>
Re-skin the buy form — the screen GAP-05 names as `screens/banxa_buy_screen.dart` — and re-skin
the dead `quote_card.dart` alongside it under D-08's explicit terms.

The buy screen carries the phase's marquee anti-pattern: a Create Order CTA hand-rolled from an
`InkWell` over an `Ink` `BoxDecoration` carrying the brand gradient, with a grey-shade gradient and
a black-alpha text colour standing in for a disabled state. Phase 8 already removed exactly this
construct from Swap; this is the same fix on the same class of file.

Purpose: close `banxa_buy_screen.dart`'s share of GAP-05 and settle D-08 on the record so nobody
later reports the invisible quote-card re-skin as a broken one.
Output: both files re-skinned in place, a widget test pinning the disabled CTA rung and the retry
affordance offline, a `QuoteCard` render test, and a filed todo owning the duplication D-08 accepts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-banxa/09-CONTEXT.md
@.planning/phases/09-banxa/09-UI-SPEC.md
@.planning/phases/09-banxa/09-PATTERNS.md
@.planning/phases/09-banxa/09-RESEARCH.md
@.planning/phases/09-banxa/09-01-SUMMARY.md

# Shipped precedents to copy
@lib/squid_router/swap_settings_drawer.dart
@lib/dashboard/news/view/crypto_news_screen.dart
@lib/tokens/token_info_screen.dart

# Primitives
@lib/components/buttons/gw_button.dart
@lib/components/cards/gw_card.dart
@lib/components/disclaimer_dialogue.dart
</context>

<execution_notes>
**D-08 in one line, for the executor who lands here first.** `quote_card.dart` is dead code.
`QuoteCard(` has exactly one match in all of `lib/` — its own constructor. The buy screen
hand-rolls an equivalent card inline instead. Braian chose to re-skin it and leave it dead.
Task 2 therefore produces **zero user-visible change on purpose**. That is the specification,
not a defect, and it must not be "fixed" by wiring the component in.

Sampling per 09-VALIDATION.md: `flutter analyze lib` after every task commit, against the count
pinned in `09-01-SUMMARY.md`'s `## Pinned baseline`.

<!-- planner-discipline-allow: Colors.red -->
<!-- planner-discipline-allow: Colors.blue -->
<!-- planner-discipline-allow: Colors.grey -->
<!-- planner-discipline-allow: GeniusWalletColors.deepBlue -->
</execution_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-skin the buy screen — back-arrow AppBar, the GWButton CTA ladder, the quote card wrap</name>
  <read_first>
    - `lib/screens/banxa_buy_screen.dart` in full (373 lines). The regions that change: the
      snackbar background at 66-70, the plain `AppBar` at 105, the Get Quote `ElevatedButton` at
      204-211, the retry `IconButton` at 215-233, the inline quote `Card` at 236-262, the
      disclaimer's active-colour argument at 284, and the whole hand-rolled CTA at 272-344.
    - `lib/tokens/token_info_screen.dart:92-129` — the back-arrow AppBar recipe. This screen is
      pushed at `/createOrder`, so it always has a back target; no conditional chrome is needed.
    - `lib/squid_router/swap_settings_drawer.dart:96-105` — the shipped
      `GWButton(variant: gradient, size: lg, expand: true)` usage, including how a null
      `onPressed` produces the disabled paint.
    - `lib/components/buttons/gw_button.dart` — confirm `GWButtonVariant.gradient` and
      `.secondary`, and that `expand: true` plus `GWButtonSize.lg` gives the 56px hero height.
    - `lib/theme/genius_wallet_colors.dart` — `statusError` for the snackbar, and
      `brandPrimaryOnSurface` (a static GETTER, so it cannot appear inside a `const` expression)
      for the disclaimer's active colour.
  </read_first>
  <files>lib/screens/banxa_buy_screen.dart, test/banxa/banxa_buy_screen_test.dart</files>
  <behavior>
    - Pumped offline (the sandbox is unreachable under `flutter_test`, so `loadCurrencies` lands
      in its caught-error arm), the screen renders without throwing.
    - In that state the Create Order CTA is present, labelled Create Order, and disabled.
    - In that state the currency-load retry affordance is present with its Retry tooltip.
    - The Get Quote control is present and disabled.
    - No hand-rolled gradient container renders the CTA.
  </behavior>
  <action>
Re-skin in place. Column order, copy, controller wiring, cubit calls and the disclaimer flow are
frozen (D-01/D-02); only paint changes.

Take the live `final gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();` read inside
the `builder`, where `context` is below the `BlocProvider`.

Swap the plain `AppBar(title: Text('Buy Crypto'))` for the `token_info_screen.dart` back-arrow
recipe with the title Buy Crypto. This screen is always pushed, so use the recipe unconditionally
— no `canGoBack` branch is needed here.

Replace the entire `InkWell` / `Ink` / `BoxDecoration` / `Container` CTA tree at 272-344 with a
single `GWButton(variant: GWButtonVariant.gradient, size: GWButtonSize.lg, expand: true,
label: 'Create Order', onPressed: state.canCreateOrder ? <the existing async body, moved
verbatim> : null)`. Move the async body — disclaimer, the not-accepted snackbar short-circuit, the
checkout-sheet-or-createOrder branch — across unchanged, character for character. Deleting the
hand-rolled tree removes four literals at once: the grey-shade disabled gradient, the deep-blue
enabled label colour and the black-alpha disabled label colour all vanish, because `GWButton`
owns both rungs. 09-UI-SPEC's CTA ladder has exactly two rows for this screen, and passing null
for `onPressed` is what selects the disabled one — the ladder is driven by the same
`state.canCreateOrder` expression it is today. Keep the label Create Order in both states; do not
invent a different disabled label.

Replace the Get Quote `ElevatedButton` with `GWButton(variant: GWButtonVariant.secondary,
expand: true, label: 'Get Quote', onPressed: state.canGetQuote ? <existing body> : null)`. It is
not the money-moving step, so it does not take the brand accent. It stays inside the `Expanded` of
its existing `Row` beside the retry `IconButton`, which keeps its icon, its Retry tooltip and its
whole `loadCurrencies` argument list (09-UI-SPEC's Copywriting table preserves the icon-only
retry).

Wrap the inline quote block's `Card` in `GWCard`, dropping the manual `Padding(EdgeInsets.all(12))`
— `GWCard`'s own default padding covers it. Retype the receive line as
`GeniusWalletTypography.bodyLg` in `gw.textPrimary` and both fee lines in
`GeniusWalletTypography.bodySm` in `gw.textSecondary`. The three interpolated strings themselves
are unchanged.

Swap the two remaining literals: the error snackbar's background becomes
`GeniusWalletColors.statusError`, and the disclaimer dialog's active-colour argument becomes
`GeniusWalletColors.brandPrimaryOnSurface` (a getter, so the surrounding expression cannot be
`const`). The disclaimer's copy is preserved verbatim — it is a third-party disclosure.

Leave the boot/loading overlay's black-alpha scrim exactly as it is. 09-UI-SPEC's Screen Contract
is explicit that it is a temporary full-screen block, mode-invariant by construction, and needs no
change. Leave the three `DropdownMenu`s and both `TextField`s structurally alone — they already
inherit Phase 4's app-wide input and dropdown themes, and a before/after with nothing changed on
them is the correct outcome.

Then write `test/banxa/banxa_buy_screen_test.dart` asserting every line of `<behavior>`, using
`gwHost` from 09-01. Pump `BanxaBuyScreen` directly: it builds its own `BlocProvider`, and
`MakeOrderCubit.loadCurrencies` wraps its service calls in a try/catch, so the unreachable sandbox
under `flutter_test` deterministically lands the screen in its error step with empty currency
lists — which is exactly the disabled CTA rung plus the visible retry affordance. Note in a
comment on the test that this is the offline state by construction and that the ENABLED rung is
unreachable without a live quote (D-03), so it is deliberately not asserted here. Pump inside a
generous `SizedBox` and settle with `pump()` rather than `pumpAndSettle()` — a spinner or a
scrim in the tree can make `pumpAndSettle` hang.
  </action>
  <verify>
    <automated>flutter test test/banxa/banxa_buy_screen_test.dart</automated>
    <automated>flutter analyze lib</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/banxa/` passes with all five `<behavior>` lines asserted.
    - `grep -c 'greenBlueGreenGradient' lib/screens/banxa_buy_screen.dart` returns 0.
    - `grep -c 'ElevatedButton' lib/screens/banxa_buy_screen.dart` returns 0.
    - `grep -vE '^\s*//' lib/screens/banxa_buy_screen.dart | grep -cE 'Colors\.(red|blue|grey)|GeniusWalletColors\.deepBlue'` returns 0.
    - `grep -c 'Colors.black45' lib/screens/banxa_buy_screen.dart` still returns 1 — the scrim was
      deliberately preserved.
    - `grep -c "label: 'Create Order'" lib/screens/banxa_buy_screen.dart` is at least 1 and the
      label appears in no state-dependent ternary.
    - `git diff` shows `showCheckoutOptionsSheet` still called at both existing sites with the same
      three arguments, and `BanxaApiService.redirectUrl` untouched.
    - `flutter analyze lib` at or below the pinned baseline.
  </acceptance_criteria>
  <done>The buy form wears the redesign, the hand-rolled gradient CTA is gone, and the disabled rung plus the retry affordance are pinned by an offline test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-skin quote_card.dart under D-08 — knowingly invisible, and file the todo that owns the duplication</name>
  <read_first>
    - `lib/banxa/banxa_components/quote_card.dart` in full — all 39 lines. A `Card` holding a
      receive line and a two-`Text` `Wrap` of fees, short-circuiting to a `SizedBox` when
      `state.hasQuote` is false.
    - `.planning/phases/09-banxa/09-CONTEXT.md` D-08 — the decision and its accepted cost.
    - `.planning/phases/09-banxa/09-RESEARCH.md` Pitfall 1 — the trap this task exists to
      neutralise: closing a task as done when a before/after screenshot shows nothing changed.
    - `lib/components/cards/gw_card.dart` — `GWCard`'s default padding and radius.
    - An existing file under `.planning/todos/pending/` — copy its front-matter/heading shape so
      the new todo matches the queue's convention (AGENTS.md: one file per item, never a shared list).
  </read_first>
  <files>lib/banxa/banxa_components/quote_card.dart, test/banxa/quote_card_test.dart, .planning/todos/pending/2026-07-27-quote-card-duplicated-by-the-buy-screens-inline-block.md</files>
  <behavior>
    - With a quote-bearing state, `QuoteCard(compact: false)` renders the full receive wording and
      both fee lines.
    - With `compact: true`, it renders the short receive wording.
    - With a state carrying no quote, it renders nothing.
    - Its primary text colour differs between a dark host and a light host (live appearance read).
  </behavior>
  <action>
Re-skin `quote_card.dart` in place: `Card` becomes `GWCard`, the manual
`Padding(EdgeInsets.all(12))` is dropped in favour of `GWCard`'s own default, the receive line
becomes `GeniusWalletTypography.bodyLg` in `gw.textPrimary`, and both `Wrap` fee lines become
`GeniusWalletTypography.bodySm` in `gw.textSecondary`. Add the live
`Theme.of(context).extension<GWColors>() ?? GWColors.dark()` read — this widget has none today.
The `compact` ternary, the `hasQuote` short-circuit, the `Wrap` spacing and all four interpolated
strings are unchanged (D-01).

Put a short comment at the top of the file, in your own words, recording three things: that this
widget currently has no callers; that `banxa_buy_screen.dart` renders an equivalent block inline;
and that Phase 9 re-skinned this file under 09-CONTEXT.md D-08 without wiring it in, deliberately.
Write it as prose about the situation — do not paste a constructor-call sample or a "before" code
snippet into the comment, because 09-07's literal gate scans this file's source and a sample can
trip its own check.

**Do not repoint the buy screen at this widget.** 09-UI-SPEC's Component Inventory row recommends
that consolidation and calls it Claude's discretion; D-08 is dated after the UI-SPEC and chose the
opposite. Consolidation is restructuring under PROJECT.md §65. The accepted cost — two divergent
copies of the same card, one styled and dead, one live — is recorded, not hidden.

Write `test/banxa/quote_card_test.dart` covering `<behavior>` using `gwHost`, `gwBothModes` and
09-01's `testQuoteState()` factory. This test is the only thing that will ever exercise this
widget, which is precisely why it is worth its four assertions.

Then file `.planning/todos/pending/2026-07-27-quote-card-duplicated-by-the-buy-screens-inline-block.md`.
It must state: that the two copies exist and where; that Phase 9 chose this deliberately under
D-08; that the risk is a future edit landing on the dead copy and appearing to do nothing; and
that the resolution is a product decision — consolidate the screen onto `QuoteCard`, or delete
`quote_card.dart` — not a re-skin decision. Give the duplication an owner; that is what D-08 asks
for.
  </action>
  <verify>
    <automated>flutter test test/banxa/quote_card_test.dart</automated>
    <automated>flutter analyze lib</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/banxa/` passes with all four `<behavior>` lines asserted.
    - `grep -c 'GWCard' lib/banxa/banxa_components/quote_card.dart` is at least 1 and
      `grep -c 'GWColors.dark()'` is at least 1.
    - `grep -rc 'QuoteCard(' lib/ --include=*.dart | grep -v ':0' | wc -l` returns 1 — the only
      file matching is `quote_card.dart` itself, i.e. the widget is STILL dead, as D-08 requires.
    - `.planning/todos/pending/2026-07-27-quote-card-duplicated-by-the-buy-screens-inline-block.md`
      exists and names both copies by path.
    - `flutter analyze lib` at or below the pinned baseline.
  </acceptance_criteria>
  <done>`quote_card.dart` wears the redesign, is still unreferenced exactly as D-08 specifies, is covered by the only test it will ever have, and its duplication has a named owner in the todo queue.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
No new `lib/` symbol — both `lib/` files are edits in place. New files:
`test/banxa/banxa_buy_screen_test.dart`, `test/banxa/quote_card_test.dart`, and the pending todo
`.planning/todos/pending/2026-07-27-quote-card-duplicated-by-the-buy-screens-inline-block.md`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → buy form | Amount and wallet-address `TextField`s. Their `onChanged` handlers, validation and the cubit calls behind them are untouched (D-01) — this plan changes decoration only, and the decoration is already theme-wired app-wide. |
| app → Banxa checkout hand-off | `showCheckoutOptionsSheet` and the redirect-URL fallbacks. Untouched; finding 1 is deferred (D-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08 | Tampering | Create Order CTA gating | high | mitigate | The CTA's enablement must stay bound to `state.canCreateOrder`, and the disclaimer must still be awaited and its rejection still short-circuit, before any order is created. Moving the async body into `GWButton.onPressed` is a verbatim move; the acceptance criteria diff-check the disclaimer flow and both `showCheckoutOptionsSheet` call sites. A CTA that became tappable in a state where it is disabled today would let a user start a payment from an incomplete form. |
| T-09-09 | Spoofing | redirect-URL fallbacks (`state.redirectUrl ?? ''` and `?? BanxaApiService.redirectUrl`) | high | transfer | Two competing redirect definitions exist and are what finding 1 points at (09-CONTEXT `<code_context>`). D-02 fences the fix out of this phase. Transferred to the deferred behaviour-fix phase; this plan must leave both expressions byte-identical, which the diff check enforces. |
| T-09-10 | Information Disclosure | wallet-address `TextField` | low | mitigate | The field's decoration changes only through the app-wide theme; no obscuring, masking or autofill behaviour is added or removed, and no new value is logged. |
| T-09-11 | Repudiation | third-party disclaimer copy | medium | mitigate | The Banxa disclosure text is preserved verbatim; only the checkbox's active colour changes. Altering that copy in a re-skin commit would silently change what the user agreed to. |
| T-09-SC | Tampering | package installs | n/a | accept | Zero new packages; `pubspec.yaml` untouched (09-RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `flutter test test/banxa/` — green including the two new files.
- `flutter analyze lib` — at or below `09-01-SUMMARY.md`'s pinned baseline.
- `git diff --name-only` for this plan lists exactly the five entries in `files_modified`.
- Diff read confirms: the disclaimer copy, both redirect fallbacks, `_amountLabel`, the controller
  sync block and every `MakeOrderCubit` call are unchanged (D-01/D-02).
- `QuoteCard` still has exactly one match in `lib/` (D-08 held).
</verification>

<success_criteria>
- `banxa_buy_screen.dart` renders its CTA ladder through `GWButton` and carries no raw colour
  literal other than the deliberately-preserved boot scrim — its share of GAP-05 is closed.
- `quote_card.dart` wears the redesign and is still dead, exactly as D-08 specifies.
- The disabled CTA rung and the retry affordance are pinned by an offline test; the enabled rung
  is recorded as OUTSTANDING rather than claimed.
- The duplication D-08 accepts has an owner in `.planning/todos/pending/`.
</success_criteria>

<output>
Create `.planning/phases/09-banxa/09-03-SUMMARY.md` when done. It MUST state plainly that Task 2
produced zero user-visible change by design (D-08), and that the enabled Create Order rung was not
verified because D-03 forbids the live quote needed to reach it.
</output>
