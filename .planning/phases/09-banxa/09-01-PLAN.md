---
phase: 09-banxa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/banxa/fixtures.dart
  - test/banxa/gw_pump.dart
  - test/banxa/banxa_test_harness_test.dart
  - lib/banxa/banxa_components/order_status_style.dart
  - test/banxa/order_status_style_test.dart
autonomous: true
requirements: [SCR-05]
must_haves:
  truths:
    - "`flutter test test/banxa/` runs and passes — the directory exists and holds at least one real assertion."
    - "A manufactured Banxa `Order` can be built in a test with one call, for any status string, with zero network access (D-03)."
    - "A Banxa widget can be pumped under both `GWColors.dark()` and `GWColors.light()` from a single shared helper."
    - "One 4-bucket status→semantic-token ladder exists, and it is the only place the mapping is written down."
    - "The pending/warning bucket compiles — it reads `GeniusWalletColors.statusWarning`, not a `GWColors` field that does not exist."
  artifacts:
    - test/banxa/fixtures.dart
    - test/banxa/gw_pump.dart
    - test/banxa/banxa_test_harness_test.dart
    - lib/banxa/banxa_components/order_status_style.dart
    - test/banxa/order_status_style_test.dart
  key_links:
    - "`order_status_style.dart` is imported by 09-02 (order_card), 09-04 (order_details_card + order_details_page). If its API shape changes after this plan, three files break."
    - "`test/banxa/fixtures.dart`'s `testOrder()` factory is consumed by 09-02 and 09-04's widget tests."
    - "`test/banxa/gw_pump.dart`'s `gwHost()` is consumed by every later test file in this phase."
  prohibitions:
    - "Do not edit any file under `lib/banxa/banxa_order/`, `lib/banxa/banxa_helpers/`, `banxa_api_services.dart` or `banxa_model.dart` (D-06). The fixture factory CONSTRUCTS an `Order`; it does not modify the model."
    - "Do not edit the four Phase-21 drawers (D-05): buy_success_drawer.dart, buy_cancelled_drawer.dart, or their two `_content` files."
    - "Do not touch the repository-root `banxa/` or `squidrouter/` directories — AGENTS.md declares those auto-generated. This phase works only under `lib/banxa/`, `lib/screens/` and `test/banxa/`."
    - "Do not add a `statusWarning` field to `GWColors` — `lib/theme/gw_colors.dart` is out of scope."
  human_judgment:
    - "WCAG AA of `GeniusWalletColors.statusWarning` (#FFC42E) used as pill TEXT on a tinted background has never been measured (09-RESEARCH Assumption A2, 09-UI-SPEC Color note). This plan adopts the already-shipped `transaction_displays.dart` recipe rather than inventing one, so the risk is inherited, not created — but it stays OUTSTANDING until a walk measures it."
---

<objective>
Create the Wave 0 test floor for Phase 9 and the single shared order-status paint ladder that
three later files depend on.

Phase 9 is a re-skin (09-CONTEXT D-01/D-02), and `test/banxa/` does not exist — zero Banxa test
coverage of any kind. 09-VALIDATION.md makes that a **Wave 0 requirement, not a Wave 2 backfill**:
without this plan every later task in the phase has nowhere to put an assertion, and no
end-to-end walk is authorised (D-03) to compensate.

Purpose: give the other six plans a runnable gate, and stop the 4-bucket status ladder from being
written three times in three files that must agree.
Output: `test/banxa/` with a fixture factory, a two-mode pump helper and one runnable harness
check; plus `lib/banxa/banxa_components/order_status_style.dart` and its test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-banxa/09-CONTEXT.md
@.planning/phases/09-banxa/09-UI-SPEC.md
@.planning/phases/09-banxa/09-VALIDATION.md
@.planning/phases/09-banxa/09-PATTERNS.md

# The shipped precedents this plan copies rather than invents
@lib/dashboard/home/widgets/transaction_displays.dart
@test/squid_router/route_details_card_test.dart

# The model the fixture factory constructs (READ ONLY — D-06 fences edits)
@lib/banxa/banxa_model.dart
@lib/banxa/banxa_order/create_order_state.dart
@lib/banxa/banxa_helpers/banxa_helpers.dart

# Tokens
@lib/theme/gw_colors.dart
@lib/theme/genius_wallet_colors.dart
@lib/theme/genius_wallet_typography.dart
@lib/theme/genius_wallet_consts.dart
</context>

<execution_notes>
**Flutter binary on this host:** `C:\Users\User\Documents\Projects\GNUS\flutter\flutter\bin\flutter.bat`
(09-VALIDATION.md). Use `flutter` if it resolves on PATH, that absolute path otherwise.

**Baseline re-pin (09-RESEARCH Assumption A1 — do this FIRST, before Task 1's edit).**
The figures quoted downstream (`flutter analyze lib` = 59 issues; `flutter test` = 376 pass / 1
known pre-existing failure in `test/local_wallet_storage_test.dart`, "Missing definition of `main`
method") were supplied by an orchestrator and never re-run. Run both once, record the ACTUAL two
numbers in `09-01-SUMMARY.md` under a heading `## Pinned baseline`, and treat those recorded
numbers — not the quoted ones — as the no-regression bar for every plan in this phase.

**Sampling (09-VALIDATION.md):** `flutter analyze lib` after every task commit;
`flutter test` (full suite) at the wave boundary. `--watch` is banned; it never exits.

<!-- planner-discipline-allow: Colors.green -->
<!-- planner-discipline-allow: Colors.orange -->
<!-- planner-discipline-allow: Colors.red -->
<!-- planner-discipline-allow: Colors.grey -->
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Pin the baseline, then create the test/banxa/ floor — fixtures, two-mode pump helper, one runnable check</name>
  <read_first>
    - `test/squid_router/route_details_card_test.dart` — the exact shape to copy: a file-local
      `_token()` factory function (not a class) plus a `_host(Widget, {GWColors? gw})` MaterialApp
      wrapper. 09-PATTERNS.md rates this an "exact" analog.
    - `lib/banxa/banxa_model.dart` — `Order` (required: id, externalId, externalCustomerId,
      orderStatusUrl, orderType, crypto, fiat, fiatAmount, cryptoAmount, paymentMethodId,
      paymentMethodName, processingFee, networkFee, walletAddress, status, createdAt, updatedAt)
      and `Crypto` (id, blockchain, network required; address nullable). Note `fiatAmount`,
      `cryptoAmount`, `processingFee`, `networkFee` are `String`, not numbers.
    - `lib/banxa/banxa_order/create_order_state.dart` — `MakeOrderState.initial()` plus
      `copyWith` is the only sane way to build a quote-bearing state; `hasQuote` is
      `quote != null`, `fiatCode`/`cryptoCode` derive from the selected currencies.
    - `lib/theme/gw_colors.dart` — confirm `GWColors.dark()` and `GWColors.light()` factories.
  </read_first>
  <files>test/banxa/fixtures.dart, test/banxa/gw_pump.dart, test/banxa/banxa_test_harness_test.dart</files>
  <action>
Run the baseline re-pin described in `<execution_notes>` before editing anything, and record both
numbers for the SUMMARY.

Create `test/banxa/gw_pump.dart` with two top-level members and nothing else:
a `gwHost(Widget child, {GWColors? gw})` function returning
`MaterialApp(theme: ThemeData(extensions: [gw ?? GWColors.dark()]), home: Scaffold(body: child))`
— copied from `route_details_card_test.dart`'s `_host`, promoted to a shared file because six later
test files in this phase need it — and a `const gwBothModes` list holding the two appearance
values, so a test can loop dark then light in one line. That loop is the phase's only automated
defence against the const-widget-does-not-re-skin defect class named as a hard rule in
09-UI-SPEC Interaction rule 3.

Create `test/banxa/fixtures.dart` with factory FUNCTIONS (not classes — mirrors
`route_details_card_test.dart:15-25`'s `_token()` idiom, and AGENTS.md's no-unrequested-
abstractions rule):
- `testOrder({String status = 'completed', String id, String fiat, String fiatAmount,
  String cryptoAmount, String cryptoId, String paymentMethodName, String walletAddress,
  DateTime? createdAt, DateTime? updatedAt})` returning a fully-populated `Order`. Give every
  parameter a deterministic default — a fixed `DateTime.utc(...)` for the two dates, NOT
  `DateTime.now()`, so date-formatted assertions do not drift between runs. Use a wallet address
  longer than 10 characters so `OrderDetailCard`'s masking branch is exercisable.
- `testQuoteState({String cryptoAmount, String processingFee, String networkFee,
  String fiatCode, String cryptoCode})` returning a `MakeOrderState` built from
  `MakeOrderState.initial().copyWith(...)` carrying a `Quote` plus the selected `FiatCurrency`
  and `CryptoCurrency` needed for `fiatCode`/`cryptoCode` to resolve — 09-03's `QuoteCard` test
  needs `hasQuote` to be true without touching a cubit or the network (D-03).

This is fixture CONSTRUCTION only. It reads `banxa_model.dart` and `create_order_state.dart`; it
must not modify either (D-06).

Create `test/banxa/banxa_test_harness_test.dart` — the one runnable check AGENTS.md requires of
non-trivial logic. Assert: `testOrder()` returns the requested status and its derived date fields
are stable across two calls; `testOrder(status: 'declined')` carries that status; and
`gwHost` renders a trivial child successfully under both entries of `gwBothModes`. Keep it small
— its job is to prove the floor exists and runs, not to test Flutter.
  </action>
  <verify>
    <automated>flutter test test/banxa/banxa_test_harness_test.dart</automated>
    <automated>flutter analyze lib</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/banxa/` exits 0 and reports at least one passing test.
    - `test/banxa/fixtures.dart`, `test/banxa/gw_pump.dart` and
      `test/banxa/banxa_test_harness_test.dart` all exist.
    - `grep -c 'DateTime.now()' test/banxa/fixtures.dart` returns 0 — fixtures are deterministic.
    - `flutter analyze lib` issue count is at or below the number pinned at the start of this task.
    - `git diff --name-only` for this task shows no file under `lib/banxa/banxa_order/`,
      `lib/banxa/banxa_helpers/`, and neither `banxa_api_services.dart` nor `banxa_model.dart`.
  </acceptance_criteria>
  <done>`test/banxa/` exists, runs green, and any later plan can build an `Order` for any status and pump a widget in both appearances with two imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The one shared 4-bucket order-status ladder (per D-01, replacing three divergent copies-to-be)</name>
  <read_first>
    - `lib/dashboard/home/widgets/transaction_displays.dart:49-95` — `_statusPill`. This is the
      shipped Phase-12 solution to exactly this problem shape (a 4-bucket semantic ladder on a
      per-item card) and 09-UI-SPEC's Color section describes it in prose. Copy the shape, swap
      the switch subject from `TransactionStatus` to a lowercased status string.
    - `lib/banxa/banxa_components/order_card.dart:21-34` — today's `_getStatusColor()`: the case
      labels to preserve. The branch SHAPE is fixed by D-01; only the returned paint changes.
    - `lib/banxa/banxa_helpers/banxa_helpers.dart:15-34` — `getBannerInfo()`. READ ONLY (D-06).
      Its three status strings (`cancel`; `failure`/`failed`; `success`/`completed`) are what the
      banner classifier must switch on.
    - `lib/components/feedback/gw_error_state.dart` (`GWErrorBanner`) — the shipped tinted-banner
      recipe: an alpha-31 fill, `radiusMd`, an alpha-100 border, a coloured 18px icon and coloured
      `bodySm` text. Generalise that container to three severities; do not hand-roll a new one.
    - `lib/theme/gw_colors.dart` — confirm for yourself that there is NO `statusWarning` field on
      `GWColors` (09-RESEARCH Pitfall 4). Success and error are appearance-aware; warning is not.
  </read_first>
  <files>lib/banxa/banxa_components/order_status_style.dart, test/banxa/order_status_style_test.dart</files>
  <behavior>
    - `orderStatusTone('completed')` and `orderStatusTone('COMPLETED')` → success.
    - `orderStatusTone` of 'pendingpayment' / 'pending' / 'inprogress' → warning.
    - `orderStatusTone` of 'declined' / 'cancelled' / 'expired' / 'failed' → error.
    - `orderStatusTone('wat')` and `orderStatusTone('')` → neutral (never a grey literal fallback).
    - `bannerTone('cancel')` → warning; `bannerTone('failure')` and `bannerTone('failed')` → error;
      `bannerTone('success')` and `bannerTone('completed')` → success; `bannerTone(null)` → null.
    - The success and error foreground colours differ between a `GWColors.dark()` host and a
      `GWColors.light()` host (proves the live appearance read, 09-UI-SPEC rule 3).
    - The warning foreground is the same in both hosts — it is a mode-invariant static today, and
      the test asserts that as a recorded fact rather than a defect.
  </behavior>
  <action>
Create `lib/banxa/banxa_components/order_status_style.dart`. 09-UI-SPEC's Color section requires
"one ladder reused across order_card.dart, order_details_card.dart and order_details_page.dart's
banner" — this file is that one ladder. Writing it three times would guarantee the divergence
that D-08 already records as an accepted cost elsewhere in this phase; writing it once is also
the smaller diff (AGENTS.md rung 5).

Expose exactly four things and no more:
- `enum OrderStatusTone { success, warning, error, neutral }`.
- `OrderStatusTone orderStatusTone(String status)` — the 4-bucket switch on
  `status.toLowerCase()`, case labels exactly as listed in `<behavior>`. Per 09-UI-SPEC
  Interaction rule 7 this EXTENDS and never inverts: every string that has a colour today keeps
  the same semantic bucket. The `default` arm returns neutral.
- `({Color fg, Color bg}) orderStatusPaint(OrderStatusTone tone, GWColors gw)` — success maps to
  `gw.statusSuccess` over `gw.statusSuccess.withValues(alpha: 0.14)`; warning to
  `GeniusWalletColors.statusWarning` over the same at alpha 0.16; error to `gw.statusError` over
  the same at alpha 0.14; neutral to `gw.textSecondary` over `gw.surfaceMenu`. These four rows are
  copied verbatim from the shipped `_statusPill` — do not re-derive them and do not substitute
  `gw.statusWarning`, which does not compile (09-RESEARCH Pitfall 4).
- `class OrderStatusPill extends StatelessWidget` taking a raw `status` String, doing its own
  live `Theme.of(context).extension<GWColors>() ?? GWColors.dark()` read, and rendering the
  shipped pill shape: `space4` horizontal / 3px vertical padding, a 999 radius, a 6px dot in the
  foreground colour, a 5px gap, then the status text in `GeniusWalletTypography.labelMd` at
  weight 600 in the foreground colour. Preserve today's uppercase presentation of the status
  string from `order_card.dart` — the copy is unchanged, only the paint.

Add two banner members in the same file, for 09-04:
- `OrderStatusTone? bannerTone(String? initialStatus)` — classifies the SAME three strings
  `BanxaHelpers.getBannerInfo()` already switches on, returning null when that helper would
  return null. This exists so `order_details_page.dart` can hand a semantic tone downstream
  instead of the helper's raw pastel `Color`. `banxa_helpers.dart` itself stays byte-for-byte
  (D-06) — only its consumer's render recipe changes.
- `class OrderStatusBanner extends StatelessWidget` taking `text` and `tone`, rendering the
  `GWErrorBanner` container recipe generalised to three severities: an alpha-31 tint of the tone's
  foreground, `radiusMd`, an alpha-100 border of the same, a coloured leading icon
  (check-circle / info / error outline by tone) and `bodySm` text in the tone's foreground.
  Today's `order_details_card.dart:39-47` paints the helper's pastel as an OPAQUE fill; that is
  the anti-pattern this replaces.

Then write `test/banxa/order_status_style_test.dart` covering every line of `<behavior>`, using
`gwHost` and `gwBothModes` from Task 1. Assert the tone functions directly (pure functions, no
pump needed) and pump `OrderStatusPill` once per appearance to compare the rendered text colour.
  </action>
  <verify>
    <automated>flutter test test/banxa/order_status_style_test.dart</automated>
    <automated>flutter analyze lib</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/banxa/` passes with the four tone buckets, the banner classifier and the
      dark-vs-light colour difference all asserted.
    - `grep -v '^\s*//' lib/banxa/banxa_components/order_status_style.dart | grep -cE 'Colors\.(green|orange|red|grey|amber)'` returns 0.
    - `grep -c 'gw.statusWarning' lib/banxa/banxa_components/order_status_style.dart` returns 0 —
      the field does not exist on `GWColors`.
    - `grep -c 'GWColors.dark()' lib/banxa/banxa_components/order_status_style.dart` is at least 1
      — the fail-soft live read is present.
    - `git diff --name-only` shows `lib/banxa/banxa_helpers/banxa_helpers.dart` UNCHANGED.
  </acceptance_criteria>
  <done>One file holds the whole 4-bucket ladder and the three-severity banner container; 09-02 and 09-04 can import it instead of each re-deriving the mapping.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New in this plan (nothing else in Phase 9 introduces a new `lib/` symbol):

| Artifact | Kind | Consumed by |
|---|---|---|
| `test/banxa/gw_pump.dart` → `gwHost()`, `gwBothModes` | test helper | every later Phase 9 test file |
| `test/banxa/fixtures.dart` → `testOrder()`, `testQuoteState()` | test helper | 09-02, 09-03, 09-04 |
| `test/banxa/banxa_test_harness_test.dart` | test | — |
| `lib/banxa/banxa_components/order_status_style.dart` → `OrderStatusTone`, `orderStatusTone()`, `orderStatusPaint()`, `OrderStatusPill`, `bannerTone()`, `OrderStatusBanner` | lib | 09-02 (order_card), 09-04 (order_details_card + order_details_page) |
| `test/banxa/order_status_style_test.dart` | test | — |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan adds a test-only directory and one presentation-only `lib/` file. It crosses no trust boundary: no input is parsed, no network call is made, no credential or key is read, no persisted state is written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Information Disclosure | `test/banxa/fixtures.dart` | low | mitigate | Fixtures are synthetic constants. No real order id, wallet address, customer id or sandbox credential is committed — the wallet address is an obviously-fake padded string. |
| T-09-02 | Tampering | `order_status_style.dart` status switch | low | mitigate | The ladder EXTENDS and never inverts (09-UI-SPEC rule 7): every status string that had a colour keeps its semantic bucket, so a declined order can never render as success. Asserted in `order_status_style_test.dart`, not assumed. |
| T-09-SC | Tampering | npm/pip/cargo/pub installs | n/a | accept | Zero packages are introduced by this phase — 09-RESEARCH's Package Legitimacy Audit records "not applicable", `pubspec.yaml` is untouched, and `flutter_test` already ships with the SDK. No legitimacy checkpoint is warranted; manufacturing one would be noise. |
| T-09-03 | Spoofing | webview redirect URL matching | medium | transfer | The substring `request.url.contains(...)` match in `kyc_registration.dart` / `banxa_payment.dart` is a real weakness (09-RESEARCH Security Domain), and D-02 fences it out of this phase. Transferred to the deferred behaviour-fix phase; recorded so it is inherited, not rediscovered. Not touched by this plan. |
</threat_model>

<verification>
- `flutter test test/banxa/` — green.
- `flutter analyze lib` — at or below the count pinned at the start of Task 1.
- `flutter test` (full suite) — pass count strictly greater than the pinned baseline, failure count
  unchanged at exactly the one known `test/local_wallet_storage_test.dart` failure. Any executor
  reporting "1 failure" must name that file (09-VALIDATION.md).
- `git diff --name-only $(git merge-base HEAD main)..HEAD` for this plan touches only the five
  files in `files_modified`.
</verification>

<success_criteria>
- `test/banxa/` exists and `flutter test test/banxa/` passes.
- A Banxa `Order` in any of the four status buckets can be constructed in one call, offline.
- Any Banxa widget can be pumped in both appearances from one shared helper.
- The 4-bucket ladder and the three-severity banner exist in exactly one file, with a test that
  pins all four buckets, the banner classifier, and the live appearance read.
- The actual `flutter analyze lib` and `flutter test` baseline is recorded in the SUMMARY under
  `## Pinned baseline` (09-RESEARCH Assumption A1) — subsequent plans compare to that, not to the
  quoted figures.
</success_criteria>

<output>
Create `.planning/phases/09-banxa/09-01-SUMMARY.md` when done. It MUST include the
`## Pinned baseline` heading with the two measured numbers.
</output>
