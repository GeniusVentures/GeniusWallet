---
phase: 09-banxa
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - lib/banxa/banxa_components/order_card.dart
  - lib/banxa/banxa_orders_history.dart
  - test/banxa/order_card_test.dart
  - test/banxa/orders_history_states_test.dart
autonomous: true
requirements: [SCR-05, GAP-05]
must_haves:
  truths:
    - "An order card renders the redesign: a `GWCard` surface, a semantic status pill, token typography on its label/value rows, and `GWButton` actions."
    - "A declined order's status pill reads error-red and its Retry action does NOT read destructive-red."
    - "The orders list's failure branch offers a retry, instead of a bare red line of text with no way forward."
    - "A wallet that has never bought anything sees a real empty state naming the way forward, instead of the words 'No orders found.'"
    - "A filter that narrows to zero says so in its own words, and the filter controls that caused it are still on screen directly above."
    - "No raw colour literal survives in either file."
  artifacts:
    - lib/banxa/banxa_components/order_card.dart
    - lib/banxa/banxa_orders_history.dart
    - test/banxa/order_card_test.dart
    - test/banxa/orders_history_states_test.dart
  key_links:
    - "`order_card.dart` imports `OrderStatusPill` from 09-01's `order_status_style.dart` — it must not re-derive the ladder."
    - "`banxa_orders_history.dart`'s `GridView.builder` keeps calling `OrderCard(order:, onSeeDetails:, onCompletePayment:, onRetryOrder:)` with all four arguments unchanged."
    - "`GWEmptyState`'s `onAction` for the never-purchased case pushes `/createOrder` — the SAME destination the AppBar's existing New Order action already uses, so it surfaces an existing affordance rather than adding one."
  prohibitions:
    - "Do not change `OrderCard`'s constructor signature, `_shortId`, `formatDate`, or which status strings gate which action button (D-01)."
    - "Do not change `_onSeeDetails`, `_onCompletePayment`, `_onRetryOrder`, `_pickDateRange`, `_onStatusChanged`, `initState`'s `fetchOrders('your-cust-id')` call, the `applyFilters` calls, the grid delegate maths, or the `294.0`/`300.0` sizing constants (D-01)."
    - "Do not add a Clear Filter action — see the recorded deviation in Task 3. Resetting filter state is behaviour, and the filter controls remain visible directly above the empty state, so no dead end exists."
    - "Do not touch `lib/banxa/banxa_order/*`, `banxa_helpers/*`, `banxa_api_services.dart`, `banxa_model.dart` (D-06), the four Phase-21 drawers (D-05), or the repository-root `banxa/` directory (AGENTS.md)."
    - "Do not create a live Banxa sandbox order to see a status colour (D-03) — use 09-01's `testOrder()` factory."
  human_judgment:
    - "Whether the re-skinned card and list LOOK right is a walk judgement. No walk is authorised this phase (D-03); record as OUTSTANDING, never as met."
    - "`GWEmptyState` switches to a compact layout below a 192px slot height (09-RESEARCH). The orders empty state renders inside a `Column` whose height is not fixed here; if a walk ever happens, check it is not squeezed."
---

<objective>
Re-skin the orders list and its card — the sibling pair 09-RESEARCH groups together because they
share a host/child edge and nothing else.

`order_card.dart` carries the densest concentration of pre-redesign literals in the phase: a
hand-set card border in `GeniusWalletColors.lightGreenSecondary`, a four-branch opaque colour
switch, a grey label colour, an orange primary button and a red outline button. `banxa_orders_history.dart`
is one of the three files GAP-05 names by name, and its error and empty branches are bare `Text`
widgets with no affordance at all.

Purpose: close the largest single block of GAP-05's literal debt and give the list the error/empty
vocabulary every other re-skinned list in the app already uses.
Output: both files re-skinned in place, structure and wiring untouched (D-01), with two widget
tests pinning the four status buckets and the two new state branches.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-banxa/09-CONTEXT.md
@.planning/phases/09-banxa/09-UI-SPEC.md
@.planning/phases/09-banxa/09-PATTERNS.md
@.planning/phases/09-banxa/09-VALIDATION.md
@.planning/phases/09-banxa/09-01-SUMMARY.md

# Shipped precedents to copy
@lib/dashboard/news/view/crypto_news_screen.dart
@lib/dashboard/home/widgets/transaction_displays.dart
@lib/tokens/token_info_screen.dart

# Primitives
@lib/components/cards/gw_card.dart
@lib/components/buttons/gw_button.dart
@lib/components/feedback/gw_error_state.dart
@lib/components/feedback/gw_empty_state.dart
@lib/banxa/banxa_components/order_status_style.dart
</context>

<execution_notes>
Sampling per 09-VALIDATION.md: `flutter analyze lib` after every task commit, against the count
pinned in `09-01-SUMMARY.md`'s `## Pinned baseline`.

<!-- planner-discipline-allow: Colors.green -->
<!-- planner-discipline-allow: Colors.orange -->
<!-- planner-discipline-allow: Colors.red -->
<!-- planner-discipline-allow: Colors.grey -->
<!-- planner-discipline-allow: GeniusWalletColors.lightGreenSecondary -->
</execution_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-skin OrderCard — GWCard surface, the shared status pill, token rows, the GWButton ladder</name>
  <read_first>
    - `lib/banxa/banxa_components/order_card.dart` in full (170 lines) — note `_getStatusColor`
      at 21-34, the hand-set `shape`/`BorderSide` at 55-61, the status pill container at 79-95,
      the three action buttons at 109-144, and `OrderInfoRow` at 154-169.
    - `lib/banxa/banxa_components/order_status_style.dart` (from 09-01) — `OrderStatusPill` is a
      drop-in for the whole status container; `_getStatusColor` disappears entirely.
    - `lib/dashboard/home/widgets/transaction_displays.dart:389-419` — `_buildRow`: label in
      `bodySm`/`textPrimary70`, value in `bodyMd`/`textPrimary`, value wrapped in `Flexible` with
      `maxLines: 1` and ellipsis. This is the row shape `OrderInfoRow` adopts.
    - `lib/components/cards/gw_card.dart` — default `padding` is `EdgeInsets.all(space8)` and
      default `radius` is `radiusLg`, so both the manual `Padding` and the explicit radius
      argument are dropped rather than translated.
    - `lib/components/buttons/gw_button.dart` — `GWButtonVariant` enum and the
      `label`/`variant`/`size`/`expand`/`onPressed` parameters.
  </read_first>
  <files>lib/banxa/banxa_components/order_card.dart, test/banxa/order_card_test.dart</files>
  <behavior>
    - A card built from `testOrder(status: 'completed')` shows a success-toned status pill.
    - `'pendingpayment'` shows a warning-toned pill AND a Complete Payment button.
    - `'declined'` shows an error-toned pill AND a Retry Order button whose label colour is NOT
      the error token — retrying is not destructive (09-UI-SPEC Component Inventory).
    - An unrecognised status shows a neutral-toned pill and neither of those two buttons.
    - Every card shows a See Details button.
    - The five row labels and the derived `fiat`/`crypto` strings are byte-identical to what the
      pre-re-skin card printed for the same fixture — the paint changed, the values did not.
    - The status pill's text colour differs between a dark host and a light host for the success
      bucket (live appearance read).
  </behavior>
  <action>
Re-skin `order_card.dart` in place. Structure, order, copy and callbacks are frozen (D-01); only
paint changes.

Take a live `final gw = Theme.of(context).extension<GWColors>() ?? GWColors.dark();` read at the
top of `build` — this widget has no `GWColors` read at all today, and adding one is part of the
re-skin, not an incidental change (09-UI-SPEC Interaction rule 3).

Delete `_getStatusColor` outright and replace the whole status `Container` in the top row with
`OrderStatusPill(status: order.status)` from 09-01. That single substitution removes four raw
colour literals and gives this card the same status vocabulary as the Transactions list. The card
gains the tinted-pill treatment it lacked; the status STRING is unchanged (D-01).

Replace the outer `Card` with `GWCard`. Drop the `shape:` argument — its
`GeniusWalletColors.lightGreenSecondary` border is precisely the pre-redesign token this phase
exists to remove, and `GWCard`'s default hairline replaces it. Drop the explicit
`BorderRadius.circular(12)` — `GWCard`'s default `radiusLg` supersedes it per 09-UI-SPEC's Radius
note. Drop the inner `Padding(EdgeInsets.all(16))`; `GWCard`'s default padding already is
`space8`. Keep the `margin` behaviour by wrapping in a `Padding` with the equivalent token values
rather than inventing a new outer geometry — `GWCard` has no `margin` parameter.

Retype the headline: the Order # text moves from an ad-hoc 16px bold `TextStyle` to
`GeniusWalletTypography.titleLg` in `gw.textPrimary`. Keep `_shortId` and its output exactly.

Retype `OrderInfoRow` after `transaction_displays.dart:389-419`: label in
`GeniusWalletTypography.labelMd` at `gw.textSecondary` (replacing the grey-700 literal), value in
`GeniusWalletTypography.bodyMd` at `gw.textPrimary`, value wrapped in `Flexible` with
`maxLines: 1` and ellipsis so a long payment-method name cannot overflow the 300px grid tile.
`OrderInfoRow` needs its own live `GWColors` read since it is a separate `StatelessWidget`.

Swap all three action buttons to `GWButton`, per 09-UI-SPEC: Complete Payment becomes
`GWButtonVariant.gradient` (it is the money-moving action, so it earns the brand accent — its
current orange fill is the status colour bleeding onto a CTA); Retry Order becomes
`GWButtonVariant.secondary` (its current red foreground and red border read as if retrying were
destructive, which it is not); See Details becomes `GWButtonVariant.tertiary`. Labels, ordering,
the `SizedBox(width: 8)` gap and the `if/else if/else` gating on the status string are all
unchanged. Give the buttons `GWButtonSize.sm` if the default `md` height makes the 300px tile
overflow — verify by running the test, not by guessing.

Then write `test/banxa/order_card_test.dart` covering every line of `<behavior>`, importing
`gwHost`/`gwBothModes` and `testOrder` from 09-01. Wrap the pumped card in a `SizedBox` of the
grid's real tile size (width from the `294.0` divisor, height `300.0`) so an overflow introduced
by the re-skin fails the test rather than passing silently.
  </action>
  <verify>
    <automated>flutter test test/banxa/order_card_test.dart</automated>
    <automated>flutter analyze lib</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/banxa/` passes, including all four status buckets and the dark-vs-light
      pill colour difference.
    - `grep -v '^\s*//' lib/banxa/banxa_components/order_card.dart | grep -cE 'Colors\.(green|orange|red|grey)|lightGreenSecondary'` returns 0.
    - `grep -c '_getStatusColor' lib/banxa/banxa_components/order_card.dart` returns 0.
    - `grep -c 'ElevatedButton\|OutlinedButton' lib/banxa/banxa_components/order_card.dart` returns 0.
    - `grep -c 'GWColors.dark()' lib/banxa/banxa_components/order_card.dart` is at least 2 —
      `OrderCard` and `OrderInfoRow` each take their own live read.
    - No RenderFlex overflow is reported by the test at the real grid tile size.
    - `flutter analyze lib` is at or below the pinned baseline.
  </acceptance_criteria>
  <done>The order card wears the redesign, carries zero raw colour literals, and its four status buckets and three action variants are pinned by a test that never touches the network.</done>
</task>

<task type="auto">
  <name>Task 2: Re-skin the orders-history chrome — back-arrow AppBar when it can go back, token typography on the filter row</name>
  <read_first>
    - `lib/banxa/banxa_orders_history.dart` in full (241 lines) — note the `canGoBack` guard at
      107-110, the three AppBar actions at 112-136, the filter `DropdownMenu` at 160-174, the
      date-range `OutlinedButton` at 175-181, the selected-range caption at 182-189 and the
      Total Orders text at 190-196.
    - `lib/tokens/token_info_screen.dart:92-129` — the shipped back-arrow AppBar recipe:
      `toolbarHeight: 48`, `backgroundColor: gw.surfaceSunken`, `elevation: 0`, `titleSpacing: 0`,
      `automaticallyImplyLeading: false`, `centerTitle: false`, and a title `Padding` whose
      horizontal inset is `space10` above `GeniusBreakpoints.medium` and `space8` below, holding a
      30x30 `InkWell` chevron calling `Navigator.of(context).maybePop()` beside a left-aligned
      title. Copy it; do not invent a second back-arrow style.
    - `lib/dashboard/news/view/crypto_news_screen.dart` — the shipped screen-shell shape this
      list mirrors.
  </read_first>
  <files>lib/banxa/banxa_orders_history.dart</files>
  <action>
Re-skin this screen's chrome only; the state branches are Task 3.

Take the live `GWColors` read at the top of `build`, beside the existing `canGoBack` computation.

Apply the back-arrow AppBar convention from `token_info_screen.dart` — but ONLY on the
`canGoBack` true branch. When `canGoBack` is false this screen is a root/tab-like entry with no
back target, and 09-UI-SPEC's Screen Contract is explicit that it then keeps a plain `AppBar`
carrying its three action icons. The existing `automaticallyImplyLeading: canGoBack` guard already
encodes that distinction — preserve the guard, restyle around it. The three actions (KYC, Refresh,
New Order) keep their icons, tooltips, order and `onPressed` bodies exactly (D-01); they are
navigation affordances, not the money-moving CTA, so none of them takes the brand accent
(09-UI-SPEC's "Accent reserved for" rule).

Retype the two ad-hoc text styles. The selected-date-range caption drops its grey literal and
becomes `GeniusWalletTypography.bodySm` in `gw.textSecondary`. The Total Orders line drops its
16px-bold literal and becomes `GeniusWalletTypography.labelMd` in `gw.textSecondary` per
09-UI-SPEC's Component Inventory.

Leave the `DropdownMenu<String>` and the date-range `OutlinedButton` structurally alone. Both
already inherit `theme.dart`'s app-wide `inputDecorationTheme` and `dropdownMenuTheme`, wired in
Phase 4 — 09-UI-SPEC is explicit that a before/after with nothing else changed on these two is the
CORRECT outcome, and "fixing" what the theme already fixes is the mistake here. Confirm they read
`surfaceElevated`/`borderSubtle` through the live theme; do not add a local decoration.

Do not touch `initState`, `_pickDateRange`, `_onStatusChanged`, `_onSeeDetails`,
`_onCompletePayment`, `_onRetryOrder`, the `applyFilters` calls, the `ConstrainedBox` max widths,
or the `GridView` delegate maths (D-01).
  </action>
  <verify>
    <automated>flutter analyze lib</automated>
    <automated>flutter test test/banxa/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib` is at or below the pinned baseline.
    - `grep -c 'toolbarHeight: 48' lib/banxa/banxa_orders_history.dart` is at least 1.
    - `grep -c 'automaticallyImplyLeading: canGoBack' lib/banxa/banxa_orders_history.dart` is at
      least 1 OR an equivalent branch on `canGoBack` is present — the two-chrome distinction
      survives.
    - `grep -vE '^\s*//' lib/banxa/banxa_orders_history.dart | grep -c 'fetchOrders' ` still
      returns 2 — the `initState` call and the Refresh action are both intact.
    - `git diff` on this task shows no change inside `_pickDateRange`, `_onStatusChanged`,
      `_onSeeDetails`, `_onCompletePayment` or `_onRetryOrder`.
  </acceptance_criteria>
  <done>The orders list wears the shared back-arrow AppBar where it has a back target, keeps its root chrome where it does not, and its two ad-hoc text styles are tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace the bare error and empty branches with GWErrorState and GWEmptyState</name>
  <read_first>
    - `lib/banxa/banxa_orders_history.dart:138-198` — the `BlocBuilder` branches: loading at
      140-142, the bare red error `Text` at 143-150, and the `orders.isEmpty` ternary at 197-198.
    - `lib/dashboard/news/view/crypto_news_screen.dart:149-166` — the cleanest shipped example of
      `GWErrorState` in an error branch and `GWEmptyState` in a data branch, in one file.
    - `lib/components/feedback/gw_error_state.dart` — `title`, `message`, `onRetry`, `retryLabel`.
    - `lib/components/feedback/gw_empty_state.dart` — `icon`, `title`, `message`, `actionLabel`,
      `onAction`; note the 192px compact/full threshold.
  </read_first>
  <files>lib/banxa/banxa_orders_history.dart, test/banxa/orders_history_states_test.dart</files>
  <behavior>
    - In the error branch, the exact `state.error` string still appears on screen, alongside a
      retry affordance.
    - Tapping retry re-dispatches the same `fetchOrders('your-cust-id')` call the Refresh action
      and `initState` already use — no new call, no new argument.
    - With zero orders and no filter applied, the empty state reads "No orders yet" and offers
      New Order.
    - With zero orders and a filter applied, the empty state reads "No orders match this filter."
      and offers no action.
    - Neither empty state renders the words "No orders found."
  </behavior>
  <action>
Replace the error branch's `Center(child: Text("..."))` with `GWErrorState(title: "Couldn't load
your orders", message: state.error, onRetry: () => context.read<OrdersCubit>().fetchOrders(
'your-cust-id'))`. Copy is fixed by 09-UI-SPEC's Copywriting table. `state.error` is preserved
verbatim as the detail line — the cross-mark prefix and the red `TextStyle` go away, the operator's
actual message does not. `onRetry` re-dispatches the EXISTING call with the EXISTING argument;
this is not a new capability, it is the affordance the shipped error component provides for a call
the screen already makes twice.

Replace the `orders.isEmpty ? Text("No orders found.") : Expanded(...)` ternary's true arm with
`GWEmptyState`, in two situations, distinguished only by state the screen already holds:

- No filter is active (`selectedStatus` is empty AND both dates are null): title "No orders yet",
  message "Your Banxa purchases will show up here once you create one.",
  `icon: Icons.receipt_long_outlined`, `actionLabel: "New Order"`, `onAction` pushing
  `/createOrder`. That destination is byte-identical to the AppBar's existing New Order action —
  the same affordance surfaced where the user is looking, not a new one.
- A filter IS active: same shell and icon, title "No orders match this filter.", no `actionLabel`
  and no `onAction`.

**Recorded deviation from 09-UI-SPEC's Screen Contract, with reason.** The UI-SPEC's filtered-to-
nothing variant also specifies a "Clear Filter" action resetting `selectedStatus`/`startDate`/
`endDate`. That action does not exist anywhere on this screen today, and adding it is behaviour,
which D-01 and D-02 fence out of this phase. It is also unnecessary: the empty state renders
inside the same `Column`, directly BELOW the status dropdown and the date-range button, so those
controls stay on screen and there is no dead end. The copy split is presentation over state the
screen already holds and is kept; the reset action is deferred. Note this deviation explicitly in
`09-02-SUMMARY.md` so the next reader sees a decision, not an omission.

Keep the `Expanded(child: LayoutBuilder(...))` false arm and everything inside it untouched (D-01).

Then write `test/banxa/orders_history_states_test.dart`. Do NOT pump `OrdersPage` itself — its
`initState` calls a cubit that hits the network, which D-03 forbids and a test cannot rely on.
Instead pump the two state widgets directly with the exact arguments this screen passes them,
asserting the contracted copy renders and that `GWErrorState`'s retry callback fires. If proving
the wiring needs the screen, provide the `OrdersCubit` through a hand-rolled fake that returns a
fixed `OrdersState` and records whether `fetchOrders` was called — a fake, never a live call.
  </action>
  <verify>
    <automated>flutter test test/banxa/orders_history_states_test.dart</automated>
    <automated>flutter analyze lib</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/banxa/` passes with all five `<behavior>` lines asserted.
    - `grep -c 'No orders found' lib/banxa/banxa_orders_history.dart` returns 0.
    - `grep -vE '^\s*//' lib/banxa/banxa_orders_history.dart | grep -cE 'Colors\.(red|grey)'` returns 0.
    - `grep -c 'GWErrorState' lib/banxa/banxa_orders_history.dart` is at least 1 and
      `grep -c 'GWEmptyState'` is at least 1.
    - `grep -c 'Clear Filter' lib/banxa/banxa_orders_history.dart` returns 0 — the deviation held.
    - `flutter analyze lib` is at or below the pinned baseline.
  </acceptance_criteria>
  <done>Both dead-end branches now offer a way forward using the shipped components, and the deliberate Clear-Filter deferral is on the record.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
No new `lib/` symbol. `test/banxa/order_card_test.dart` and
`test/banxa/orders_history_states_test.dart` are the two new files; both consume 09-01's
`gwHost`, `gwBothModes` and `testOrder`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Banxa API → orders list | `state.error` is operator-supplied text now rendered inside `GWErrorState`'s message slot instead of an interpolated `Text`. Still rendered as plain text by a Flutter `Text` widget — no markup, no HTML, no link parsing. |
| user → navigation | `GWEmptyState`'s action pushes a fixed route literal. No user input reaches the router. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Information Disclosure | `OrderCard` row values | medium | mitigate | The card must not begin displaying a field it does not display today. `_shortId` still truncates the order id; the five row labels and their sources are unchanged; no new `Order` field is rendered. Asserted by the byte-identical-values line in Task 1's `<behavior>`. |
| T-09-05 | Information Disclosure | `GWErrorState(message: state.error)` | low | mitigate | The error text shown is exactly what the screen already prints today — same string, different container. No stack trace, request URL, header or customer id is newly surfaced. |
| T-09-06 | Tampering | status → colour mapping | medium | mitigate | The pill comes from 09-01's single tested ladder, so a declined order cannot render as success. The action-button gating still switches on the same two status strings it does today; the `if/else if/else` shape is frozen by D-01. |
| T-09-07 | Elevation of Privilege | `GWEmptyState.onAction` route push | low | accept | The action pushes the same `/createOrder` route the AppBar already exposes on this screen to the same user in the same state. No authorisation boundary is crossed and none exists on this route today. |
| T-09-SC | Tampering | package installs | n/a | accept | Zero new packages; `pubspec.yaml` untouched (09-RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `flutter test test/banxa/` — green, including the two new files.
- `flutter analyze lib` — at or below `09-01-SUMMARY.md`'s pinned baseline.
- `git diff --name-only` for this plan lists exactly the four files in `files_modified`.
- Manual read of the diff confirms every callback body, route literal and cubit call in
  `banxa_orders_history.dart` is unchanged (D-01).
</verification>

<success_criteria>
- Both files render through `GWCard`/`GWButton`/`GWErrorState`/`GWEmptyState` and the shared
  status pill.
- Zero raw colour literals remain in either file.
- The four status buckets, the two action-button variants, and both new state branches are pinned
  by tests that never touch the network (D-03).
- `banxa_orders_history.dart`'s share of GAP-05 is closed at the code level.
- The Clear-Filter deferral is recorded in the SUMMARY as a decision with its reason.
</success_criteria>

<output>
Create `.planning/phases/09-banxa/09-02-SUMMARY.md` when done, including the recorded Clear-Filter
deviation and confirmation that the visual-fidelity judgement stays OUTSTANDING (D-03).
</output>
