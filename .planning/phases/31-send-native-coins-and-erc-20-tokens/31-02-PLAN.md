---
phase: 31-send-native-coins-and-erc-20-tokens
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified: [packages/genius_api/lib/web3/send_service.dart, packages/genius_api/lib/src/genius_api.dart, lib/send/send_cubit.dart, lib/send/send_screen.dart, lib/navigation/router.dart, test/components/drawer_padding_invariant_test.dart, test/send/send_screen_test.dart, test/send/send_service_test.dart, test/send/send_cubit_test.dart]
autonomous: true
requirements: [SEND-02, SEND-04, SEND-06, SEND-07]
estimate: {tokens: 95000, raw_tokens: 95000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "On a signable chain a user types an address and an amount of the native coin, sees the EIP-1559 fee in the gas coin before approving, approves, and the coin moves"
    - "The send appears in history as pending the moment it is broadcast and becomes completed or failed once a receipt arrives: one row, never two"
    - "A fee estimate never reaches the signer as zero; an RPC without EIP-1559 falls back to the legacy gas price"
    - "A failed signature, a failed estimate or a balance short of amount plus fee writes nothing and says why"
  artifacts:
    - {path: packages/genius_api/lib/web3/send_service.dart, provides: "SendFee, chooseFeePerGas, buildSendTx, pollReceipt, settledStatus, feePaid, chain reads"}
    - {path: lib/send/send_cubit.dart, provides: "SendCubit, SendState, SendReview, sendRow"}
    - {path: lib/send/send_screen.dart, provides: "SendScreen, the /send route target and its review drawer"}
  key_links:
    - {from: lib/send/send_cubit.dart, to: GeniusApi.signAndSendTransaction, via: "the only signing call"}
    - {from: lib/send/send_cubit.dart, to: TransactionsCubit.addTransaction, via: "resolved row, exactly once"}
    - {from: lib/navigation/router.dart, to: SendScreen, via: "/send GoRoute extra symbol + chainId"}
---

<objective>
Native send end to end (D-01 native half, D-03 route shape, D-04 address format): /send screen, SendCubit (form
state only), fee estimate, review drawer, sign through the existing path, pending then resolved history row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31-send-native-coins-and-erc-20-tokens/31-RESEARCH.md
@.planning/phases/31-send-native-coins-and-erc-20-tokens/31-PATTERNS.md
@.planning/phases/31-send-native-coins-and-erc-20-tokens/31-01-SUMMARY.md
Every command runs after `export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"`.
Tests live in the app's `test/send/`: genius_api declares no test dependency and CI runs only the root suite.
genius_api works in BigInt wei only; decimal strings convert app-side with `toBaseUnits`/`formatTokenAmount`.
Source comments: WHY only, doc comments at most 3 lines, never a plan/phase/decision/requirement id or test file name.
</context>

<tasks>

<task type="tracer">
  <name>Send 0.5 of the native coin on Amoy: form, fee, review, sign, pending, completed</name>
  <files>packages/genius_api/lib/web3/send_service.dart, packages/genius_api/lib/src/genius_api.dart, lib/send/send_cubit.dart, lib/send/send_screen.dart, lib/navigation/router.dart, test/components/drawer_padding_invariant_test.dart, test/send/send_screen_test.dart</files>
  <read_first>packages/genius_api/lib/web3/web3.dart (rawBalanceOf 297-322, signAndSendTransaction 681-738), packages/genius_api/lib/src/genius_api.dart (1205-1257), lib/squid_router/swap_execution.dart (_poll 307-334), lib/squid_router/swap_screen.dart (_submitSwap 439-545, _applyOutcome 551-605, _swapRow 1120-1151), lib/squid_router/squid_util.dart, lib/reown/send_transaction_details.dart, lib/reown/utilities.dart, lib/navigation/router.dart (252-265), test/squid_router/swap_submit_test.dart (harness)</read_first>
  <action>send_service.dart: `SendFee` (maxFeePerGas, maxPriorityFeePerGas, gasLimit BigInts; `maxCost` getter), record typedef `FeePerGas`, `SendFeeUnavailable` exception, `chooseFeePerGas({eip1559, gasPrice})` (EIP-1559 reading first; on throw or a non-positive max fee use the legacy gas price for both fields; priority clamped to max; non-positive or failing legacy throws SendFeeUnavailable), `buildSendTx({from, recipient, amount, fee})` returning the hex map signAndSendTransaction reads (from, to, value, gas, maxFeePerGas, maxPriorityFeePerGas; no data key), `pollReceipt({hash, read, wait, attempts = 20, interval = 3s})` mirroring swap's bounded loop (null or a throw is "not yet", exhausted returns null), `settledStatus(TransactionReceipt?)` (null pending, status true completed, false failed), `feePaid(receipt)` (gasUsed times effectiveGasPrice or null), and three reads that own a Web3Client and dispose it: `readNativeBalance`, `readSendFee` (the 50th-percentile entry, index 1, of `getGasInEIP1559()` via chooseFeePerGas, gas limit from `estimateGas(sender, to, data)`, no buffer), `readReceipt`. GeniusApi gets thin wrappers `nativeBalance`, `estimateSendFee`, `transactionReceipt` in the rawBalanceOf style; no print calls anywhere in the package. send_cubit.dart: `SendState` (coin, recipient, amount, busy, error, review, recorded) with copyWith and no key material; `SendReview` (tx map, fee, raw amount, recipient); `SendCubit({api, walletAddress, network, transactions, storage, wait, initialCoin})` with setRecipient, setAmount, cancelReview, `review()` (isEvmAddress, toBaseUnits with decimals 18 for a coin without address, fee, raw native balance, refuse when amount plus maxCost exceeds it, build, then top-level `builtTxMatches(tx, {recipient, amount})` must hold: `to` equals the recipient ignoring case, value equals the amount, no data) and `submit()` (sign via `api.signAndSendTransaction`; on a hash write the pending row, poll, write the resolved row under the same hash, then `transactions.addTransaction(resolved)` once, then clear recipient, amount and review so a second Review cannot resend; emits after an await skip when `isClosed`, the writes do not). Top-level `sendRow(...)`: type transfer, direction sent, coinSymbol the gas coin `(nativeSymbol ?? symbol).toUpperCase()`, assetSymbol the coin symbol, chainId, amount and fee via formatTokenAmount (fee from feePaid, else maxCost). send_screen.dart: `SendScreen({preselectSymbol, preselectChainId, storage})` reads WalletDetailsCubit/TransactionsCubit, seats the held coin whose symbol equals `preselectSymbol` ignoring case, only when `preselectChainId` is null or equals the selected network's chainId (never a guess), BlocProvider keyed on wallet address plus chainId, GWPageHeader 'Send', GWTextField recipient and amount, GWButton 'Review'; on a new review opens `ResponsiveDrawer.show<bool>` titled 'Review send' with `SendTransactionDetails` (amountSymbol coin, feeSymbol gas coin) and a Cancel/Send footer StatelessWidget; on `recorded` shows a toast and `showTransactionDetails`. Router: `/send` GoRoute beside `/swap`, same extra-map shape. Census: `'lib/send/send_screen.dart': _Inset.shellInset`. Screen test pumps a seeded WalletDetailsCubit (Amoy: chainId 80002, symbol matic, so the gas coin reads MATIC), a fake GeniusApi, a recording storage and a real TransactionsCubit through Review then Send.</action>
  <verify><automated>flutter test test/send/ test/components/drawer_padding_invariant_test.dart</automated></verify>
  <acceptance_criteria>
    - Screen test: drawer shows 'Gas Fee' ending in the gas coin; storage writes are [pending, completed] with one hash; TransactionsCubit state has length 1 and it is the completed row with assetSymbol MATIC and chainId 80002
    - The signed map has `to` equal to the recipient, `value` equal to 0.5e18 in hex, no `data`, non-zero `maxFeePerGas`
    - `grep -n "path: '/send'" lib/navigation/router.dart` matches once; `grep -rniE "privatekey|mnemonic" lib/send/` exits 1
    - `flutter analyze` "No issues found!" at root and in packages/genius_api
  </acceptance_criteria>
  <done>D-03's /send screen sends a native coin on a D-01 chain and history records it pending then resolved.</done>
</task>

<task type="auto" tdd="true">
  <name>Fee fallback and receipt polling hold under a misbehaving RPC</name>
  <files>packages/genius_api/lib/web3/send_service.dart, test/send/send_service_test.dart</files>
  <read_first>packages/genius_api/lib/web3/send_service.dart, test/squid_router/swap_execution_test.dart (injected wait style)</read_first>
  <behavior>
    - chooseFeePerGas: EIP-1559 answer used as given; a throw or a zero max fee uses the legacy price for both fields; both failing, or a zero legacy price, throws SendFeeUnavailable; priority above max is clamped
    - buildSendTx native map has exactly from/to/value/gas/maxFeePerGas/maxPriorityFeePerGas, lowercase hex with 0x
    - pollReceipt: null then success returns on read 2 after one wait; a throwing read is not terminal; exhaustion is exactly `attempts` reads, `attempts - 1` waits, returns null; a status-false receipt returns at once
    - settledStatus null/true/false maps to pending/completed/failed; feePaid is gasUsed times effectiveGasPrice, null if either is missing
  </behavior>
  <action>Write the cases first, run them red where behaviour is missing, then fix send_service.dart until green. Construct receipts with web3dart's `TransactionReceipt` directly.</action>
  <verify><automated>flutter test test/send/send_service_test.dart</automated></verify>
  <acceptance_criteria>
    - Every behaviour line above is one named test; the file passes; no case touches the network
  </acceptance_criteria>
  <done>The fee path never hands the D-01 signer a zero fee and a lost receipt leaves a row pending, not lost.</done>
</task>

<task type="auto">
  <name>Every failure says why and stores nothing; a bare /send offers the held coins</name>
  <files>lib/send/send_cubit.dart, lib/send/send_screen.dart, test/send/send_cubit_test.dart, test/send/send_screen_test.dart</files>
  <read_first>lib/send/send_cubit.dart, lib/send/send_screen.dart, lib/components/coins/view/coins_screen.dart (onCoinSelected picker mode, 37-50 and 244-250), lib/reown/utilities.dart (canSignOn)</read_first>
  <action>Cubit cases: a failed signature writes nothing, keeps the form and shows the returned reason; a thrown estimate or SendFeeUnavailable shows "Couldn't estimate the network fee." and opens no review; amount plus fee above balance names the gas coin; cancelReview then submit signs nothing; two overlapping submits sign once; closing the cubit mid-poll still produces both writes and the single TransactionsCubit add. Screen: no seated coin renders `CoinsScreen(onCoinSelected: cubit.selectCoin)` (add `selectCoin`, which clears amount and review); a network failing `canSignOn`, or no wallet, renders a GWEmptyState and no form; switching the selected network rebuilds a fresh form.</action>
  <verify><automated>flutter test test/send/</automated></verify>
  <acceptance_criteria>
    - Each case above is a named test and passes; recording storage stays empty in every failure case
    - `bash tool/check_brace_style.sh`, `bash tool/check_raw_colors.sh`, `bash tool/check_no_new_key_logging.sh --scan-tree` exit 0
  </acceptance_criteria>
  <done>D-01 scope holds (unsignable chains get no form) and no failure path creates a history row.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| user input -> SendCubit | recipient and amount strings |
| RPC -> send_service | fee, balance and receipt answers from a possibly hostile endpoint |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-04 | Information disclosure | SendState | high | mitigate | No key field on state; signing only via GeniusApi.signAndSendTransaction; key-logging gate + negative grep |
| T-31-05 | Tampering | submit() | high | mitigate | Early return while busy; overlapping-submit test proves one signature (no reused nonce) |
| T-31-06 | Denial of service | chooseFeePerGas | high | mitigate | A zero fee never reaches the signer; SendFeeUnavailable blocks the review |
| T-31-07 | Tampering | fee estimate | medium | mitigate | Fee shown in the gas coin before approval; amount plus max fee must fit the raw balance |
| T-31-08 | Tampering | history rows | medium | mitigate | Pending and resolved share the hash key; cubit receives the resolved row once |
| T-31-09 | Spoofing | chain of the send | medium | mitigate | Form keyed on address plus chainId; the review captures the chain it was built for |
</threat_model>

## Artifacts this phase produces
- `send_service.dart`: `SendFee`, `FeePerGas`, `SendFeeUnavailable`, `chooseFeePerGas`, `buildSendTx`, `pollReceipt`, `settledStatus`, `feePaid`, `readNativeBalance`, `readSendFee`, `readReceipt`
- `GeniusApi.nativeBalance`, `GeniusApi.estimateSendFee`, `GeniusApi.transactionReceipt`
- `lib/send/send_cubit.dart`: `SendCubit`, `SendState`, `SendReview`, `sendRow`, `builtTxMatches`; `lib/send/send_screen.dart`: `SendScreen`; `/send` route
- `test/send/send_screen_test.dart`, `test/send/send_service_test.dart`, `test/send/send_cubit_test.dart`

<verification>
Full `flutter test` exit 0; `flutter analyze` "No issues found!" at root and in packages/genius_api; `dart format
--set-exit-if-changed lib test packages/genius_api/lib/web3/send_service.dart packages/genius_api/lib/src/genius_api.dart`
exit 0 (never the whole package lib); `git ls-files --eol` shows lf on every new Dart file; `bash tool/check_onboarding_seed_safety.sh` exit 0.
</verification>

<success_criteria>
A native send goes from form to signed to a resolved history row with the fee seen first, and no failure stores a row.
</success_criteria>

<output>
Create `.planning/phases/31-send-native-coins-and-erc-20-tokens/31-02-SUMMARY.md` (at most 40 lines).
</output>
