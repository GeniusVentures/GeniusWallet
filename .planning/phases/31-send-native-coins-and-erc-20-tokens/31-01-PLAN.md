---
phase: 31-send-native-coins-and-erc-20-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/genius_api/lib/models/transaction.dart, packages/genius_api/lib/models/transaction.g.dart, lib/dashboard/home/widgets/transaction_utils.dart, lib/dashboard/home/widgets/transaction_displays.dart, lib/reown/handle_dapp_requests.dart, test/dashboard/transaction_asset_chain_test.dart, test/reown/handle_dapp_requests_test.dart, .planning/phases/30-dapp-calldata-decoding-end-blind-signing/deferred-items.md]
autonomous: true
requirements: [SEND-01]
estimate: {tokens: 60000, raw_tokens: 60000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "A token transfer row's headline, amount and icon name the token; its Network Fee row names the chain's gas coin"
    - "A row carrying a chain id links to that chain's explorer (8453 basescan, 80002 amoy.polygonscan); an unlisted chain shows no link"
    - "A row written by the previous 18-field adapter still reads back, both new fields null, every old field intact"
    - "A dApp-approved ERC-20 transfer is recorded with the token as asset, the decoded recipient and amount, the gas coin as coinSymbol, and the chain id"
  artifacts:
    - {path: packages/genius_api/lib/models/transaction.dart, provides: "assetSymbol @HiveField(18), chainId @HiveField(19), assetUnit getter"}
    - {path: lib/dashboard/home/widgets/transaction_utils.dart, provides: "kExplorerTxBase, explorerUrlFor(Transaction)"}
    - {path: test/dashboard/transaction_asset_chain_test.dart, provides: "reader, explorer and legacy round-trip cases"}
  key_links:
    - {from: lib/dashboard/home/widgets/transaction_displays.dart, to: explorerUrlFor, via: "receipt drawer explorer footer"}
    - {from: lib/reown/handle_dapp_requests.dart, to: "Transaction.assetSymbol / chainId", via: "record written after approval"}
---

<objective>
D-05, first: split what moved (the asset) from where it moved (the chain) in the history model, keep old rows
readable, key the explorer link off the chain, and close phase 30's deferred receipt-unit item.
Output: two nullable Hive fields, chain-keyed explorer, asset-aware history readers, an honest dApp token row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31-send-native-coins-and-erc-20-tokens/31-CONTEXT.md
@.planning/phases/31-send-native-coins-and-erc-20-tokens/31-RESEARCH.md
@.planning/phases/30-dapp-calldata-decoding-end-blind-signing/deferred-items.md
Every command runs after `export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"`.
Source comments: WHY only, doc comments at most 3 lines, never a plan/phase/decision/requirement id or a test file name.
</context>

<tasks>

<!-- One-way decision resolved by Braian 2026-09-23: asset-and-chain.
     assetSymbol String? @18 + chainId int? @19; coinSymbol stays the gas coin. No checkpoint needed. -->

<task type="tracer">
  <name>A token row reads its asset and links its chain: model to receipt, one path</name>
  <reversibility rating="one-way">Index 18/19 and their types are persisted on user devices; changing them later breaks every history read.</reversibility>
  <files>packages/genius_api/lib/models/transaction.dart, packages/genius_api/lib/models/transaction.g.dart, lib/dashboard/home/widgets/transaction_utils.dart, lib/dashboard/home/widgets/transaction_displays.dart, test/dashboard/transaction_asset_chain_test.dart</files>
  <read_first>packages/genius_api/lib/models/transaction.dart (field 17 is the precedent), packages/genius_api/lib/models/transaction.g.dart, lib/dashboard/home/widgets/transaction_utils.dart (getExplorerUrl lines 11-38; txRowContent title ~493, amount ~684-687, icon ~728), lib/dashboard/home/widgets/transaction_displays.dart (987-1008), test/dashboard/transaction_recovery_states_test.dart (receipt harness)</read_first>
  <action>Append to `Transaction`: `@HiveField(18) final String? assetSymbol;` and `@HiveField(19) final int? chainId;`, both optional constructor params; never renumber 0-17. Add getter `String get assetUnit => assetSymbol ?? coinSymbol;` with a one-line WHY doc. Regenerate the adapter, never hand-edit: from `packages/genius_api` run `dart run build_runner build --delete-conflicting-outputs`, then `git checkout --` any file under packages/genius_api other than the two transaction files that the generator touched. In transaction_utils.dart add `const Map<int, String> kExplorerTxBase` = {1: https://etherscan.io/tx/, 11155111: https://sepolia.etherscan.io/tx/, 137: https://polygonscan.com/tx/, 80002: https://amoy.polygonscan.com/tx/, 56: https://bscscan.com/tx/, 97: https://testnet.bscscan.com/tx/, 8453: https://basescan.org/tx/} (84531 deliberately absent: it is the retired Base Goerli id) and `String explorerUrlFor(Transaction tx)`: with a chainId, the table entry plus hash, or '' when unlisted or hash empty; without one, fall through to `getExplorerUrl(tx.coinSymbol, tx.hash)` unchanged. In `txRowContent` switch the non-swap title, the transfer/escrow amount and fiat unit, and the non-swap icon symbol to `tx.assetUnit`; the process-fee branch and swap branch keep what they read. In transaction_displays.dart call `explorerUrlFor(tx)` for the explorer footer; the Network and Network Fee rows keep `tx.coinSymbol`, which is now the gas coin. New test file covers: token row (coinSymbol ETH, assetSymbol USDC, chainId 8453) title/amount say USDC; explorer basescan; 80002 amoy; 84531 empty; a row with neither field behaves exactly as today; the receipt drawer shows the Network Fee value ending in ETH and no fee text with USDC.</action>
  <verify><automated>flutter test test/dashboard/</automated></verify>
  <acceptance_criteria>
    - `grep -A1 "@HiveField(18)" packages/genius_api/lib/models/transaction.dart` shows `final String? assetSymbol;`; `@HiveField(19)` shows `final int? chainId;`
    - `grep -c "writeByte(20)" packages/genius_api/lib/models/transaction.g.dart` = 1 and it contains `fields[18] as String?` and `fields[19] as int?`
    - `git diff --name-only -- packages/genius_api` lists only transaction.dart and transaction.g.dart
    - `flutter test test/dashboard/` exits 0; `flutter analyze` prints "No issues found!" at the root and in packages/genius_api
  </acceptance_criteria>
  <done>D-05's split exists on disk and the history reads it; old-format rows render exactly as before.</done>
</task>

<task type="auto">
  <name>The dApp token receipt tells the truth, and an 18-field row survives the new adapter</name>
  <files>lib/reown/handle_dapp_requests.dart, test/reown/handle_dapp_requests_test.dart, test/dashboard/transaction_asset_chain_test.dart, .planning/phases/30-dapp-calldata-decoding-end-blind-signing/deferred-items.md</files>
  <read_first>lib/reown/handle_dapp_requests.dart (nativeUnit ~79, record ~220-290), lib/reown/calldata_decoder.dart (DappCallSummary, receiptSymbol), test/reown/handle_dapp_requests_test.dart (~729-760), test/dashboard/transaction_row_test.dart (~386-420, Hive.init temp-dir pattern)</read_first>
  <action>In the record written after approval: `coinSymbol: nativeUnit`, `assetSymbol: receiptSymbol(summary, nativeSymbol: nativeUnit)`, `chainId: chainId`; for `DappCallKind.tokenTransfer` and `unverifiedToken` the single recipient is `summary.recipient` with `summary.amount` (the decoded pair, not the contract and native value). The signed `tx` map stays untouched. `SwapResultDrawer.show` keeps its current symbol. Amend the handler case asserting `coinSymbol` 'USDC' to assert `assetSymbol` 'USDC', `coinSymbol` 'ETH', `chainId` 8453 and the decoded recipient. Add a plain `test()` group (not testWidgets: real Hive I/O hangs there) to the new test file: `Hive.init` on a temp dir, register the enum and recipient adapters if absent, register a test-local legacy adapter for typeId 8 with `override: true` that writes exactly the old layout (writeByte(18), fields 0-17), put a row, close the box, re-register `TransactionAdapter()` with `override: true`, reopen and read. Append a closing note under the receipt-unit and explorer items in phase 30's deferred-items.md.</action>
  <verify><automated>flutter test test/reown/handle_dapp_requests_test.dart test/dashboard/transaction_asset_chain_test.dart</automated></verify>
  <acceptance_criteria>
    - Legacy case asserts `assetSymbol` and `chainId` null and hash, coinSymbol, recipients, status equal to what the legacy writer wrote
    - Handler case asserts `assetSymbol == 'USDC'`, `coinSymbol == 'ETH'`, `chainId == 8453`
    - `bash tool/check_brace_style.sh` and `bash tool/check_raw_colors.sh` exit 0; `dart format --set-exit-if-changed lib test` exits 0
  </acceptance_criteria>
  <done>Phase 30's receipt-unit and explorer items are closed by D-05's split; the signing payload is byte-identical.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| device Hive box -> app | rows from older builds and dApp-supplied symbols are untrusted input to the history UI |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering | Transaction adapter | high | mitigate | Append-only indices, generator-written adapter, legacy round-trip test |
| T-31-02 | Spoofing | explorer link | medium | mitigate | URL from a const chainId table, never composed from a stored symbol; unlisted chain shows no link |
| T-31-03 | Information disclosure | assetSymbol render | low | mitigate | Icon path still passes through sanitizeCoinAsset |
</threat_model>

## Artifacts this phase produces
- `Transaction.assetSymbol` (String?, @HiveField 18), `Transaction.chainId` (int?, @HiveField 19), `Transaction.assetUnit` getter
- `kExplorerTxBase` (const Map<int, String>), `explorerUrlFor(Transaction)` in transaction_utils.dart
- `test/dashboard/transaction_asset_chain_test.dart`

<verification>
`flutter test` full suite exit 0 (baseline 1590 pass / 5 skip); `flutter analyze` "No issues found!" at root and in
packages/genius_api; `dart format --set-exit-if-changed packages/genius_api/lib/models/transaction.dart` exits 0
(never format packages/genius_api/lib wholesale: it reflows a generated FFI binding); `git ls-files --eol` on touched
Dart files shows `i/lf w/lf`.
</verification>

<success_criteria>
History shows the token as the asset and the gas coin as the fee unit; explorer is chain-correct; old rows read.
</success_criteria>

<output>
Create `.planning/phases/31-send-native-coins-and-erc-20-tokens/31-01-SUMMARY.md` (at most 40 lines).
</output>
