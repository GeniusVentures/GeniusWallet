---
phase: 31-send-native-coins-and-erc-20-tokens
plan: 05
type: execute
wave: 5
depends_on: [31-04]
files_modified: [lib/tokens/token_info_screen.dart, test/tokens/coin_page_stat_rail_test.dart, lib/components/coins/view/coins_screen.dart, test/dashboard/dashboard_send_entry_test.dart]
autonomous: true
requirements: [SEND-06, SEND-07]
estimate: {tokens: 45000, raw_tokens: 45000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "On a coin page for a signable network, a Send button next to Swap and Receive opens /send with that coin seated"
    - "The dashboard's Assets section offers Send when the wallet holds something on a signable network; it opens /send with the coin picker"
    - "Neither entry appears on a network the wallet cannot sign on, and the coin-page test states that rule instead of claiming Send does not exist"
    - "A real native send and a real ERC-20 send land on a testnet and resolve in history without a duplicate row"
  artifacts:
    - {path: lib/tokens/token_info_screen.dart, provides: "Send GWButton in _CoinActionRow"}
    - {path: lib/components/coins/view/coins_screen.dart, provides: "dashboard Send footer"}
    - {path: test/dashboard/dashboard_send_entry_test.dart, provides: "dashboard entry cases"}
  key_links:
    - {from: lib/tokens/token_info_screen.dart, to: "/send", via: "push with extra symbol + chainId"}
    - {from: lib/components/coins/view/coins_screen.dart, to: "/send", via: "push with no extra, so SendScreen shows its picker"}
---

<objective>
D-02's two entry points: a Send CTA on the coin page with the coin preselected, and a dashboard Send that opens the
picker; correct the coin-page test that asserted Send does not exist; close the phase with the live testnet walk.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31-send-native-coins-and-erc-20-tokens/31-VALIDATION.md
Every command runs after `export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"`.
Source comments: WHY only, doc comments at most 3 lines, never a plan/phase/decision/requirement id or test file name.
</context>

<tasks>

<task type="tracer">
  <name>From a coin page, Send opens /send with that coin seated</name>
  <files>lib/tokens/token_info_screen.dart, test/tokens/coin_page_stat_rail_test.dart</files>
  <read_first>lib/tokens/token_info_screen.dart (_CoinActionRow 719-820, the Swap button's comment on marketData-first and push-not-go), lib/reown/utilities.dart (canSignOn), test/tokens/coin_page_stat_rail_test.dart (131-158), test/squid_router/swap_submit_test.dart (_SeededCubit)</read_first>
  <action>In `_CoinActionRow` add a `GWButton` (gradientOutline, sm, label 'Send', leading `Icon(Icons.send)`) after Swap, present only when `selectedNetwork` is non-null and `canSignOn` holds for it (absent rather than greyed, like Bridge). It calls `GoRouter.of(context).push('/send', extra: {'symbol': marketData?.symbol ?? selectedCoin?.symbol, 'chainId': selectedNetwork?.chainId})`, same symbol order and push reasoning as Swap; do not repeat Swap's long comment, one line pointing at the shared reason is enough. In the test, the no-network case keeps `findsNothing` for Send but its comment and name now say Send is absent because no signable network is selected; the old claim that Send has no screen or route is deleted (SGNUS transfers stay unwired, which is a separate fact). Add a case with a seeded cubit (Amoy, chainId 80002, rpcUrl set, selected coin USDC) inside a GoRouter host whose `/send` route records `state.extra`: Send is found once, and a tap records `{'symbol': 'USDC', 'chainId': 80002}`.</action>
  <verify><automated>flutter test test/tokens/coin_page_stat_rail_test.dart test/send/</automated></verify>
  <acceptance_criteria>
    - Seeded case: `find.widgetWithText(GWButton, 'Send')` findsOneWidget and the recorded extra equals the map above
    - No-network case: Send findsNothing, Swap findsOneWidget, Bridge findsNothing
    - `grep -c "no screen and no route" test/tokens/coin_page_stat_rail_test.dart` prints 0 (it prints 1 today)
  </acceptance_criteria>
  <done>D-02's coin-page entry opens D-03's /send with the coin preselected.</done>
</task>

<task type="auto">
  <name>A dashboard Send opens the coin picker; walk both sends on a testnet</name>
  <files>lib/components/coins/view/coins_screen.dart, test/dashboard/dashboard_send_entry_test.dart</files>
  <read_first>lib/components/coins/view/coins_screen.dart (isDashboard ~248, value-empty footer 477-510), test/dashboard/dashboard_section_caps_test.dart (how CoinsScreen is pumped), lib/send/send_screen.dart (picker when nothing is seated)</read_first>
  <action>In the dashboard branch add a footer after the existing value-empty footer: `Padding(top: space8)` with one expanded `GWButton` (gradientOutline, sm, label 'Send') calling `context.push('/send')` with no extra, shown only when `isDashboard`, some coin has a balance above zero, and the selected network passes `canSignOn`. It keys off holdings, not the fiat total, because testnet coins carry no price and the total reads zero. The picker path (`CoinsScreen(onCoinSelected: ...)`) never shows it. New test: a seeded Amoy wallet holding one coin shows 'Send' and a tap reaches `/send` with a null extra; a network without an rpcUrl shows none; all-zero balances show none; the picker mode shows none.</action>
  <verify>
    <automated>flutter test test/dashboard/</automated>
    <human-check>End of phase, funded testnet wallet on Polygon Amoy (80002) or Ethereum Sepolia (11155111). Not "Base - Sepolia": its catalogue chainId 84531 is the retired Base Goerli id. Send a small native amount from the dashboard picker and a small ERC-20 amount from its coin page. Check: fee in the review vs fee charged on the explorer; the self-send and contract warnings against two prepared addresses; pending becomes completed with one row; the explorer link opens the testnet explorer. Record both hashes and the wallet address in the SUMMARY.</human-check>
  </verify>
  <acceptance_criteria>
    - The four dashboard cases pass; `flutter test test/dashboard/` exits 0 including the section-caps suite
    - `git diff develop -U0 -- lib packages/genius_api/lib | grep '^+' | grep -cE 'D-0[1-5]|SEND-0[1-7]|31-0[1-5]|_test\.dart'` prints 0
  </acceptance_criteria>
  <done>D-02's dashboard entry opens the picker; the phase's native and ERC-20 sends are proven on a live chain.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| route extra -> SendScreen | symbol and chainId from a page the user was browsing |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-18 | Spoofing | preselection | medium | mitigate | SendScreen seats a coin only on symbol plus chainId match against the selected network; else the picker |
| T-31-19 | Elevation of privilege | entry points | low | mitigate | Both entries absent unless canSignOn; the screen refuses unsignable networks on its own too |
</threat_model>

## Artifacts this phase produces
- Send `GWButton` in `_CoinActionRow` (token_info_screen.dart); dashboard Send footer in coins_screen.dart
- Amended coin-page cases in coin_page_stat_rail_test.dart; `test/dashboard/dashboard_send_entry_test.dart`

<verification>
Phase gate: full `flutter test` exit 0 against the 1590 pass / 5 skip develop baseline plus this phase's cases;
`flutter analyze` "No issues found!" at root and in packages/genius_api; `dart format --set-exit-if-changed lib test`
exit 0; `bash tool/check_brace_style.sh`, `check_raw_colors.sh`, `check_no_new_key_logging.sh --scan-tree`,
`check_onboarding_seed_safety.sh`, `check_agent_rules_sync.sh` all exit 0; `git ls-files --eol` lf on touched Dart files.
</verification>

<success_criteria>
Both entry points reach Send, the coin-page test tells the truth, and the testnet walk records two resolved hashes.
</success_criteria>

<output>
Create `.planning/phases/31-send-native-coins-and-erc-20-tokens/31-05-SUMMARY.md` (at most 40 lines).
</output>
