---
phase: 31-send-native-coins-and-erc-20-tokens
plan: 04
type: execute
wave: 4
depends_on: [31-03]
files_modified: [lib/send/send_cubit.dart, lib/send/recipient_field.dart, lib/send/send_screen.dart, packages/genius_api/lib/web3/send_service.dart, packages/genius_api/lib/src/genius_api.dart, test/send/recipient_field_test.dart, ios/Runner/Info.plist, macos/Runner/Info.plist, macos/Runner/DebugProfile.entitlements, macos/Runner/Release.entitlements]
autonomous: true
requirements: [SEND-05]
estimate: {tokens: 60000, raw_tokens: 60000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Pasting or typing the wallet's own address shows a self-send warning under the recipient field"
    - "A recipient with code on-chain shows a contract warning; a stale or failed check never shows a wrong one"
    - "Paste works on every platform; a Scan button fills the field from a QR code on Android, iOS and macOS and is absent on Windows and Linux"
    - "A scanned EIP-681 payment link yields only its 0x address, which then goes through the same validation as typing"
  artifacts:
    - {path: lib/send/recipient_field.dart, provides: "RecipientField, addressFromScan, defaultCanScanQr, the scan page"}
    - {path: packages/genius_api/lib/web3/send_service.dart, provides: "readHasCode"}
    - {path: test/send/recipient_field_test.dart, provides: "paste, warnings, scan gating and parsing cases"}
  key_links:
    - {from: lib/send/send_cubit.dart, to: GeniusApi.hasCode, via: "eth_getCode on a valid, non-self recipient"}
    - {from: lib/send/recipient_field.dart, to: mobile_scanner MobileScanner, via: "scan page, only where supported"}
---

<objective>
D-04's recipient safety: address validation already gates the review; add paste, platform-gated QR scan, a
self-send warning and a contract-code warning. No address-poisoning check and no PIN re-entry (both deferred).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31-send-native-coins-and-erc-20-tokens/31-RESEARCH.md
Every command runs after `export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"`.
mobile_scanner 5.2.3 is already a dependency and has no Windows or Linux build. Colours from GWColors tokens only.
Source comments: WHY only, doc comments at most 3 lines, never a plan/phase/decision/requirement id or test file name.
</context>

<tasks>

<task type="tracer">
  <name>Paste the wallet's own address: the field fills and a self-send warning appears</name>
  <files>lib/send/send_cubit.dart, lib/send/recipient_field.dart, lib/send/send_screen.dart, test/send/recipient_field_test.dart</files>
  <read_first>lib/send/send_cubit.dart, lib/send/send_screen.dart, lib/components/inputs/gw_text_field.dart, lib/components/feedback/gw_warning_note.dart, lib/bloc/app_bloc.dart (~565, the lowercase address comparison)</read_first>
  <action>Add `selfSend` to SendState, set in `setRecipient` when the trimmed value is a valid address equal to the wallet address ignoring case. Extract the recipient input from send_screen.dart into a `RecipientField` StatelessWidget (reads the SendCubit, owns no state beyond a passed controller): GWTextField labelled 'To' with an error line "Enter a 0x address of 40 hex characters." only when non-empty and invalid, a suffix 'Paste' GWButton (ghost, sm) that reads `Clipboard.getData(Clipboard.kTextPlain)`, trims it and calls `setRecipient`, and under it a `GWWarningNote('This is your own address. Sending here moves nothing and still costs a fee.')` while `selfSend`. The screen's controller stays in step with the cubit's recipient. Test mocks the platform clipboard channel to return the seeded wallet address, taps Paste and finds the note; another address shows none.</action>
  <verify><automated>flutter test test/send/</automated></verify>
  <acceptance_criteria>
    - Pasting the wallet address fills the field and shows the self-send note; a different valid address shows no note
    - `grep -c "class RecipientField extends StatelessWidget" lib/send/recipient_field.dart` = 1
    - `bash tool/check_raw_colors.sh` and `bash tool/check_brace_style.sh` exit 0
  </acceptance_criteria>
  <done>D-04's self-send warning reaches the screen through the cubit, paste included.</done>
</task>

<task type="auto">
  <name>A recipient with code on-chain is flagged as a contract</name>
  <files>packages/genius_api/lib/web3/send_service.dart, packages/genius_api/lib/src/genius_api.dart, lib/send/send_cubit.dart, lib/send/recipient_field.dart, test/send/recipient_field_test.dart</files>
  <read_first>packages/genius_api/lib/web3/send_service.dart (readNativeBalance for the client shape), packages/genius_api/lib/src/genius_api.dart (rawBalanceOf wrapper), lib/send/send_cubit.dart</read_first>
  <action>send_service.dart: `readHasCode({rpcUrl, address})` returns whether `getCode` is non-empty, disposing its client; GeniusApi wrapper `hasCode` in the same style. SendState gains `contractRecipient`. After a valid recipient that is not the wallet's own, the cubit calls `api.hasCode` once and applies the answer only if `state.recipient` still equals the checked address; a throw leaves the flag false (the review's standing caution still applies). Any recipient edit clears the flag first. RecipientField shows `GWWarningNote('This address is a contract. Coins or tokens sent to a contract that cannot handle them are usually lost for good.')` while set. Cases: code gives the note; empty code none; throw none; an answer arriving after the text changed is ignored; the own address is never queried.</action>
  <verify><automated>flutter test test/send/recipient_field_test.dart</automated></verify>
  <acceptance_criteria>
    - The five cases above are named tests and pass; the fake's hasCode call count proves the own address is not queried
    - `flutter analyze` "No issues found!" at root and in packages/genius_api
  </acceptance_criteria>
  <done>D-04's contract warning fires from eth_getCode and never from a stale answer.</done>
</task>

<task type="auto">
  <name>Scan a QR code where the platform has a camera plugin</name>
  <files>lib/send/recipient_field.dart, test/send/recipient_field_test.dart, ios/Runner/Info.plist, macos/Runner/Info.plist, macos/Runner/DebugProfile.entitlements, macos/Runner/Release.entitlements</files>
  <read_first>lib/send/recipient_field.dart, lib/navigation/router.dart (~267, the Platform.isLinux gate), mobile_scanner 5.2.3 README "Platform Support" and iOS/macOS setup in the local pub cache, macos/Runner/Release.entitlements</read_first>
  <action>Top-level `bool get defaultCanScanQr => !(Platform.isWindows || Platform.isLinux);` and a `canScan` parameter on RecipientField defaulting to it. When true a 'Scan' GWButton (ghost, sm) pushes a private StatelessWidget page hosting `MobileScanner(onDetect: ...)` that pops exactly once (onDetect fires repeatedly) with the first detection's `rawValue`; the field passes it through top-level `addressFromScan(String raw)` (first `0x` plus 40 hex match, else null) and calls `setRecipient` on a match. Anything else a payment link carries (amount, chain) is ignored: the form's own chain and typed amount decide. Add `NSCameraUsageDescription` ("GeniusWallet uses the camera to scan a recipient's QR code.") to ios and macos Info.plist and `com.apple.security.device.camera` true to both macOS entitlements files; Android gets CAMERA from the plugin manifest. Tests: addressFromScan on a bare address, `ethereum:0xABC...@137?value=1e18`, upper-case hex and garbage (null); canScan false shows no 'Scan', true shows it (never tapped: no camera in tests).</action>
  <verify><automated>flutter test test/send/recipient_field_test.dart</automated></verify>
  <acceptance_criteria>
    - `grep -c NSCameraUsageDescription ios/Runner/Info.plist macos/Runner/Info.plist` reports 1 each; `grep -c com.apple.security.device.camera macos/Runner/*.entitlements` reports 1 each
    - Parsing and gating cases pass; `flutter analyze` "No issues found!"
  </acceptance_criteria>
  <done>D-04's recipient field takes a scanned address where supported and hides the button elsewhere.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| clipboard / camera -> recipient | pasted and scanned text is untrusted |
| RPC -> contract check | eth_getCode answer may be wrong or late |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-14 | Spoofing | recipient entry | high | mitigate | isEvmAddress gate, self and contract warnings, full address in the review; poisoning detection deferred by decision |
| T-31-15 | Tampering | QR payload | medium | mitigate | Only a 0x address is extracted; amount and chain in the link are ignored; same validation as typing |
| T-31-16 | Spoofing | contract check | low | accept | A hostile RPC can hide code; the warning is advisory and the review caution stays |
| T-31-17 | Information disclosure | camera | low | mitigate | Camera opens only on an explicit Scan tap, with a usage string; hidden where unsupported |
</threat_model>

## Artifacts this phase produces
- `lib/send/recipient_field.dart`: `RecipientField`, `addressFromScan`, `defaultCanScanQr`, private scan page
- `readHasCode` (send_service.dart), `GeniusApi.hasCode`; `SendState.selfSend`, `SendState.contractRecipient`
- Camera usage strings (iOS, macOS) and the macOS camera entitlement; `test/send/recipient_field_test.dart`

<verification>
Full `flutter test` exit 0; `flutter analyze` "No issues found!" at root and in packages/genius_api; `dart format
--set-exit-if-changed lib test packages/genius_api/lib/web3/send_service.dart packages/genius_api/lib/src/genius_api.dart`
exit 0; `bash tool/check_no_new_key_logging.sh --scan-tree` exit 0; lf endings on new Dart files.
</verification>

<success_criteria>
Recipient entry offers paste and, where possible, scan, and warns on self and contract recipients.
</success_criteria>

<output>
Create `.planning/phases/31-send-native-coins-and-erc-20-tokens/31-04-SUMMARY.md` (at most 40 lines).
</output>
