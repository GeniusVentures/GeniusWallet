---
phase: 31-send-native-coins-and-erc-20-tokens
plan: 03
type: execute
wave: 3
depends_on: [31-02]
files_modified: [packages/genius_api/lib/web3/send_service.dart, lib/send/send_cubit.dart, lib/send/send_screen.dart, test/send/send_token_test.dart, test/send/send_service_test.dart, test/send/send_cubit_test.dart]
autonomous: true
requirements: [SEND-02, SEND-03, SEND-05]
estimate: {tokens: 65000, raw_tokens: 65000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "A user can send a held ERC-20 token; the review shows the real recipient (decoded from the calldata, not the token contract) and the fee in the gas coin"
    - "The built transfer decodes back to exactly the recipient and base-unit amount the form holds, or the review refuses"
    - "A token send is refused before review when the native balance cannot pay the fee, or the token's decimals are unknown"
    - "MAX fills the exact token balance for a token, and the native balance minus the maximum fee for the native coin, never below zero"
  artifacts:
    - {path: packages/genius_api/lib/web3/send_service.dart, provides: "buildSendTx token branch, maxNativeSendable"}
    - {path: lib/send/send_cubit.dart, provides: "token review path, builtTxMatches token branch, useMax"}
    - {path: test/send/send_token_test.dart, provides: "calldata self-check against the app decoder"}
  key_links:
    - {from: lib/send/send_cubit.dart, to: tryDecodeErc20Transfer, via: "review refuses a built map that decodes to anything else"}
    - {from: lib/send/send_cubit.dart, to: GeniusApi.rawBalanceOf, via: "exact token balance for the check and for MAX"}
---

<objective>
D-01's ERC-20 half plus MAX: build `transfer` calldata with the existing ABI, prove it decodes back to the form,
gate on gas-coin balance, label the row with the token, and give the amount field a MAX that subtracts the fee.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31-send-native-coins-and-erc-20-tokens/31-RESEARCH.md
Every command runs after `export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"`.
The decoder lives in the app (`lib/reown/calldata_decoder.dart`), so the self-check test is an app test.
Source comments: WHY only, doc comments at most 3 lines, never a plan/phase/decision/requirement id or test file name.
</context>

<tasks>

<task type="tracer">
  <name>Send 5 USDC on Amoy: transfer calldata, gas-coin check, decoded review, token-labelled row</name>
  <files>packages/genius_api/lib/web3/send_service.dart, lib/send/send_cubit.dart, test/send/send_token_test.dart</files>
  <read_first>packages/genius_api/lib/web3/web3.dart (abi 48-56, approve 553-601), packages/genius_api/lib/web3/send_service.dart, lib/send/send_cubit.dart, lib/reown/calldata_decoder.dart (tryDecodeErc20Transfer 237, summarizeTransaction 368), test/reown/calldata_decoder_test.dart (fixture style)</read_first>
  <action>`buildSendTx` gains optional `tokenContract`: when set, `to` is the contract, `value` is 0x0 and `data` is `Web3.abi`'s `transfer` encoded via `DeployedContract(...).function('transfer').encodeCall([recipient address, amount])` as 0x hex; the native branch is unchanged. `readSendFee` passes that data to `estimateGas`. In the cubit a coin with an address is a token: decimals from `int.tryParse(coin.decimals)`, refused with "This token's decimals are unknown." when null or outside 0-36 (never assume 18); `api.rawBalanceOf` must cover the amount ("Your SYMBOL balance doesn't cover this amount."); the raw native balance must cover `fee.maxCost` ("Not enough GAS for the network fee.", GAS the gas coin). Extend `builtTxMatches` with the token branch: `to` equals the contract ignoring case, value is zero, and `tryDecodeErc20Transfer(tx['data'])` returns the form's recipient (case-insensitive) and exact raw amount; on mismatch the review refuses with "Couldn't build this transfer.". The review's recipient is the decoded counterparty; `sendRow` records that recipient, the token amount, assetSymbol the token symbol, coinSymbol the gas coin. New test file: build a USDC (6 decimals) transfer, assert `tryDecodeErc20Transfer` recovers recipient and 5000000, and `summarizeTransaction(tx, coins: [usdc], chainId: 80002, nativeSymbol: 'MATIC')` is `tokenTransfer` with that recipient and amount '5'; then a cubit run with a fake GeniusApi through review and submit.</action>
  <verify><automated>flutter test test/send/</automated></verify>
  <acceptance_criteria>
    - Signed map: `to` is the USDC contract, `value` is 0x0, `data` starts with `0xa9059cbb`
    - Resolved row: assetSymbol 'USDC', coinSymbol 'MATIC', chainId 80002, recipients.single.toAddr the recipient (not the contract), amount '5'
    - `builtTxMatches` returns false for a map whose data encodes another recipient, another amount, or whose `to` is not the contract
    - `flutter analyze` "No issues found!" at root and in packages/genius_api
  </acceptance_criteria>
  <done>D-01's ERC-20 send works on a signable chain and history names the token, per D-05's split.</done>
</task>

<task type="auto">
  <name>MAX fills what can actually be sent</name>
  <files>packages/genius_api/lib/web3/send_service.dart, lib/send/send_cubit.dart, lib/send/send_screen.dart, test/send/send_service_test.dart, test/send/send_cubit_test.dart</files>
  <read_first>lib/squid_router/swap_screen.dart (nativeCoinBaseUnits 50-78, for what not to copy), lib/components/inputs/gw_text_field.dart (suffix), lib/components/buttons/gw_button.dart, lib/send/send_screen.dart</read_first>
  <action>Add `maxNativeSendable({balance, fee})` (balance minus `fee.maxCost`, clamped at zero). Cubit `useMax()`: for a token the amount becomes `formatTokenAmount(rawBalanceOf, decimals)`; for the native coin estimate the fee to the recipient when it is a valid address, else to the wallet's own address, read the raw native balance, and write `formatTokenAmount(maxNativeSendable(...), 18)`; a zero result sets "Not enough GAS to cover the network fee." (GAS the gas coin). The raw reads are why no double balance is involved. Busy while it runs; estimate failures reuse the existing fee message. Screen: the amount GWTextField takes a TextEditingController kept in step with `state.amount` and a suffix `GWButton` (ghost, sm, label 'MAX') calling `useMax`. The review re-estimates, so a fee that grew after MAX fails with the existing amount-plus-fee message rather than signing.</action>
  <verify><automated>flutter test test/send/</automated></verify>
  <acceptance_criteria>
    - Service tests: balance above, equal to and below maxCost give the difference, zero and zero
    - Cubit tests: native MAX equals balance minus maxCost to the wei; token MAX equals the raw balance including a 6-decimal dust digit; zero sets the message; a grown fee at review refuses
    - Screen test: tapping 'MAX' puts the cubit's amount into the field
    - `bash tool/check_brace_style.sh` and `bash tool/check_raw_colors.sh` exit 0
  </acceptance_criteria>
  <done>MAX on a native coin subtracts the fee, and on a token spends the exact balance (D-01 both halves).</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| form -> built transaction | what the user typed vs what gets signed |
| RPC -> balances | token and native balances read before review |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-10 | Tampering | buildSendTx token branch | high | mitigate | Review refuses unless the calldata decodes back to the form's recipient and exact amount |
| T-31-11 | Tampering | amount conversion | high | mitigate | toBaseUnits string to BigInt, raw balances, never a double; unknown decimals refused |
| T-31-12 | Denial of service | token send without gas | medium | mitigate | Native balance must cover the maximum fee before the review opens |
| T-31-13 | Spoofing | review recipient | medium | mitigate | The review shows the decoded counterparty, never the token contract |
</threat_model>

## Artifacts this phase produces
- `buildSendTx(..., tokenContract)` token branch and `maxNativeSendable` in send_service.dart
- `SendCubit.useMax`, the token review path, `builtTxMatches` token branch in send_cubit.dart
- MAX suffix on the amount field in send_screen.dart; `test/send/send_token_test.dart`

<verification>
Full `flutter test` exit 0; `flutter analyze` "No issues found!" at root and in packages/genius_api; `dart format
--set-exit-if-changed lib test packages/genius_api/lib/web3/send_service.dart` exit 0; `bash tool/check_no_new_key_logging.sh --scan-tree` exit 0.
</verification>

<success_criteria>
A token send and a MAX send each reach a correct review, and a mismatched build never reaches the signer.
</success_criteria>

<output>
Create `.planning/phases/31-send-native-coins-and-erc-20-tokens/31-03-SUMMARY.md` (at most 40 lines).
</output>
