---
phase: 15-transactions-tab
plan: 06
type: execute
wave: 4
depends_on: ["15-01", "15-02", "15-03", "15-04", "15-05"]
files_modified: []
autonomous: false
requirements: [TT-01, TT-02, TT-03, TT-04, TT-05, TT-06]

must_haves:
  truths:
    - "Every claim this phase makes has been seen on a real screen, in DARK, at a wide window and a narrow one."
    - "The rail's active mark has been judged for loudness in situ — the one thing sketch 022 said only the live app could settle."
    - "The empty state has been seen in the tall page slot AND in the short dashboard card, so the shared-component change is judged on both."
    - "Assets and Markets have been opened deliberately, because 15-02 changed a component they consume."
    - "The failed row and the job row have been read as sentences, not as fields."
    - "The walk is DARK MODE ONLY. Light-mode findings are RECORDED as notes in the backlog todo, never as blockers."
  artifacts:
    - ".planning/phases/15-transactions-tab/15-06-SUMMARY.md with a per-criterion verdict"
  key_links:
    - "dev-tools bubble MOCK 'Mock txns' -> the extended batch from 12-05, the only way to see all seven types plus a failed and a pending row offline."
    - "dev-tools bubble MOCK 'Clear' -> the never-transacted state, which is the only way to see the hidden filter control and the sync_alt icon."
---

<objective>
Walk the Transactions tab on a real screen and decide, per criterion, whether it ships.

Purpose: this project's own record is unambiguous that the walk is a real gate, not a formality.
06-01's walk found a Rule-1 defect in its own deliverable. 05-06's walk drove quick task `k81`,
whose badge fix then became 05-VERIFICATION's Gap 1. And `flutter analyze` has shipped 37
regressions clean here, so "analyze is a gate, never evidence" is doctrine. Everything in this
phase a test could assert has been asserted by 15-01..15-05 — including the contrast numbers, which
are computed, not eyeballed. What is left is exactly what only eyes can judge: whether the rail's
underline is loud enough to answer "is a filter on?" at a glance (sketch 022 explicitly deferred
that to the live app), whether the empty state now lands where a person looks, whether the page
reads as a page, and whether the failed row's three facts add up to one honest sentence.

Output: a per-criterion verdict. `flutter analyze` output is a precondition for starting the walk,
never evidence that a criterion passed.

NOTE: per `./CLAUDE.md` this repo's commit gate is held by the user. Do NOT create commits — not
even for walk-driven fixes. Leave fixes in the working tree and report them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/sketches/020-transactions-tab/README.md
@.planning/sketches/021-transactions-tab-final/README.md
@.planning/sketches/022-rail-active-state/README.md
@.planning/todos/pending/2026-07-22-light-mode-verification-backlog.md
@.planning/phases/15-transactions-tab/15-01-SUMMARY.md
@.planning/phases/15-transactions-tab/15-02-SUMMARY.md
@.planning/phases/15-transactions-tab/15-03-SUMMARY.md
@.planning/phases/15-transactions-tab/15-04-SUMMARY.md
@.planning/phases/15-transactions-tab/15-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Precondition — the tree is green before anyone looks at it</name>
  <files></files>
  <action>
Run the gates and capture the raw output into the summary. Do not start the walk if either is worse
than the known baseline; fix or report instead.

  - `flutter analyze lib` — compare against the recorded baseline in STATE.md and report the DELTA,
    not just the absolute number.
  - `flutter analyze lib/dashboard/transactions lib/dashboard/home/widgets lib/components/feedback`
    — this scoped run should be clean ("No issues found!"): every file this phase touched lives
    under those paths.
  - `flutter test` — **the baseline is 187 passing, 1 failing.** The one failure is
    `test/local_wallet_storage_test.dart`, which is entirely commented out and fails at load. It is
    PRE-EXISTING: do not fix it and do not record it as a regression. Any OTHER red is a blocker.
    Report the new passing total, which should be the baseline plus roughly two dozen cases from
    15-01..15-05.

Then read each of the five plan summaries and assemble the list of items the executors explicitly
flagged for this walk. At minimum, expect these named items and carry them into the checkpoints
below rather than rediscovering them live:
  - 15-01: the exact strings the four representative rows now produce.
  - 15-02: which of Assets / Markets / gallery were covered by test rather than by eye.
  - 15-03: whether the 419px test fixture needed shortening, and confirmation the bar's 40×183
    geometry did not move.
  - 15-04: whether the `IntrinsicWidth` underline reads ragged across rows, and whether the rail's
    `SingleChildScrollView` ever actually scrolls at a realistic window height.
  - 15-05: whether `RefreshIndicator`'s pull still works and which scrollable captures it, and the
    fact that the SGNUS branch almost certainly went unexercised.
  </action>
  <verify>
    <automated>flutter test</automated>
    <automated>flutter analyze lib/dashboard/transactions lib/dashboard/home/widgets lib/components/feedback</automated>
  </verify>
  <done>
Scoped analyze is clean, `flutter analyze lib` is at or below the STATE.md baseline, the suite's
only failure is the known commented-out file, and the flagged-items list is assembled.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint A: Walk the page frame and the filter rail (dark)</name>
  <action>
Hand the running app to the human and walk the numbered recipe below. Do not proceed on your own
judgement — every item here is something only eyes can settle. Capture the verdict per numbered
item.
  </action>
  <what-built>
The page frame (15-05) and the filter rail (15-04): `/transactions` at `GWPageHeader` 24px, capped
at 1280, rail and list each in a `DashboardScrollContainer`; the rail is the `⋯` menu unrolled —
All with the total count, a Type group of seven, a Status group of two, all with live counts — with
`_menuItem`'s rest geometry, the sketch-008 lift-chip hover, and 022-B2's active mark (w700
`textPrimary` label that is never recoloured, a 2px gradient rule beneath it, glyph untouched).
  </what-built>
  <how-to-verify>
**This walk is DARK MODE ONLY.** Light mode is backlog per
`.planning/todos/pending/2026-07-22-light-mode-verification-backlog.md` — a light-mode finding is a
NOTE appended to that file, never a blocker on this phase and never a reason to stop the walk. Do
not flip the appearance toggle during this checkpoint.

Launch on this machine (macOS — `flutter run -d windows` is wrong for this box):

    flutter run -d macos --dart-define=GW_DEV_TOOLS=true

1. Dev-tools bubble, MOCK section, press **Mock txns**. Navigate to the **Transactions** tab.
2. Maximise the window (or go as wide as the display allows). Confirm the page frame:
   - **one** title, "Transactions", at page weight (24px) — visibly larger than the "Transactions"
     header on the dashboard panel. If you can see TWO titles, the page flag did not reach the
     slim view; stop and report.
   - the rail and the list are two separate cards with the surrounding space reading as margin.
     Rows sit ON a card, not on the raw page background.
   - the content stops at 1280px. On a 2000px window there is dead space, but roughly 360px per
     side rather than the ~630px it was. If it looks like it still stops at ~740px, defect #1
     survived.
3. The rail, top to bottom: **All**, a **Type** header, then Sent · Received · Mint · Computing ·
   Escrow · Swapped · Purchased, a hairline, a **Status** header, then Pending · Failed. Confirm
   the order and that every row shows a count on the right.
4. **All's count must equal the total number of transactions**, and there must be **no footer
   count** anywhere on the page. Two live totals on one screen is what this absorbed.
5. Hover an inactive row. It must lift onto a slightly lighter fill over ~120ms — the same gesture
   the dashboard's chart timeframe tabs and the panel's filter chips use. Hover several in
   sequence: it should feel like one control, not ten.
6. Click **Mint**. The active mark is the item sketch 022 deferred to this moment:
   - the label goes **bold and stays white** — it must NOT turn blue, green or gradient. A
     recoloured label is variant B, which was rejected.
   - a **2px gradient rule** sits directly beneath the label, the width of the WORD (so it is
     shorter under "Mint" than under "Purchased"). Ragged edges across rows are a known, accepted
     cost — say whether it reads as deliberate or as a bug.
   - **the glyph does not change at all** — same icon, same grey. If the icon changed colour, that
     is a defect, not a nicety.
   - **THE JUDGEMENT CALL, and the reason this checkpoint exists.** Look away, look back: can you
     tell at a glance that a filter is on? Sketch 022 chose B2 knowing a single quiet mark might
     not be enough in a permanent rail, and recorded variant D (add the gradient label back) as
     the fallback *only if B2 proves too quiet in the live app*. This is that test. Answer it
     explicitly.
7. Hover the ACTIVE row. It must NOT take the hover fill — hover and active are two different
   states and collapsing them is what killed variant E.
8. Click through several filters and watch the **counts**: they must NOT change as you filter. They
   are computed over the unfiltered list on purpose. If they move, the counts are being computed
   over the filtered list and every inactive filter will eventually read 0.
9. Click **All** to clear. Then click an already-active row a second time — it must STAY active
   (the rail deliberately does not toggle off; All is the way back). Confirm that reads as
   intentional rather than unresponsive.
10. **Narrow the window** until the page is under ~768px. The rail must disappear and the panel's
    chip bar must come back in its place. Nothing may clip or overflow at any width on the way
    down, and the console must stay clean through the whole drag. Widen again and confirm the rail
    returns.
11. **Drag the window shorter**, to a laptop-ish height and below. The rail's rows must scroll
    rather than compress, and — the freeze rule — the drag must stay smooth. Commit `37639d5` froze
    this app permanently on a drag-resize; that failure presents as a frame that never commits, and
    a clean console plus a smooth drag is the only signal available.
12. Go back to the **dashboard** and confirm the Transactions panel is UNCHANGED: still the four
    chips and the `⋯` menu, still its 18px section title, still its footer count. The rail is
    page-only and the panel was explicitly not to move.

STOP and report if: two titles appear; the content still caps near 768; the counts change when
filtering; the glyph changes with selection; the label is recoloured; hover and active look the
same; anything clips at any width; the dashboard panel changed; or the console shows any
`RenderFlex overflowed`, exception or frame stall.
  </how-to-verify>
  <resume-signal>Type "approved" or describe, per numbered item, what is wrong.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint B: Walk the empty states — on this page AND on the two screens that share the component (dark)</name>
  <action>
Walk the numbered recipe below with the human. Steps 5-7 are the shared-component blast radius and
must not be summarised — `GWEmptyState` has five call sites and this phase changed all of them at
once. Capture the verdict per numbered item.
  </action>
  <what-built>
The anchored empty state (15-02): `Center` replaced by `Alignment.topCenter` over a
`ConstrainedBox(maxHeight: 480)`, guarded so an unbounded slot still sizes to its content and the
compact tier still fires below 192 (256 with an action). Plus (15-03) `Icons.sync_alt` on the
never-transacted state, and the filter control hidden entirely when there is nothing to filter.
  </what-built>
  <how-to-verify>
Still DARK only. Light-mode observations go in the backlog todo as notes.

1. Dev bubble MOCK -> **Clear**. On the **Transactions** tab, the never-transacted state appears.
   Confirm:
   - the icon and title sit in the **upper portion** of the page, roughly a fifth to a quarter of
     the way down — not at the vertical midpoint, not below the fold. This is the whole of sketch
     021 §3; if you have to scroll or hunt for it, it failed.
   - the icon is **two horizontal opposed arrows** (`Icons.sync_alt`). Recorded, not
     re-litigated: this is close to the Swap tab's navbar glyph and was picked knowing that
     (sketch 021 §4, logged in 022). Do NOT report the resemblance as a surprise — but DO say if it
     actually reads as "go to Swap" at 32px, because that is new information.
   - **there is no filter control on screen at all** — no rail, no chips, no `⋯`. Five controls
     offering to filter nothing was the behavioural bug this closes.
2. Re-load **Mock txns**. Pick a filter with a count of **0** in the rail. Confirm the
   FILTERED-empty state: a filter icon, a title naming that filter, a message stating how many
   transactions you DO have, and a **"Show all"** button. The rail must STILL be there — that
   wallet is not empty and you must be able to get back. Press "Show all" and confirm it returns.
3. Hold steps 1 and 2 side by side in memory: they must read as **different situations**. If they
   read the same, the requirement is not met.
4. Narrow the window under 768 with an empty wallet: the panel presentation appears, and the chips
   must be absent there too — the rule is page rail *and* panel chips.
5. **Dashboard, Transactions panel, empty wallet.** The panel slot is short. The empty block must
   behave exactly as it did before this phase — the 480 cap is a rule that only binds in a TALL
   slot, so nothing should have moved here. If it moved, say by how much.
6. **Assets panel** (dashboard) with no coins — dev bubble, whichever MOCK control clears holdings.
   `coins_screen.dart:214` consumes the same component. Confirm nothing regressed: the block is
   positioned sensibly and nothing overflows.
7. **Markets**, both the dashboard card and the `/markets` tab. The dashboard's Markets card is the
   SHORT-slot case that quick task `260721-e3r` made adaptive: the empty/error state there must
   still use the **compact** layout (smaller icon circle, single-line title). If it grew back to
   the full 72px circle, the compact tier broke and that is a blocker, not a note.
8. If the dev design gallery is reachable (`design_gallery_screen.dart`, "Empty / Error / Loading
   states"), open it: the empty state there sits in an unbounded slot and must still hug its own
   content rather than inflating to a tall block.

STOP and report if: the empty block still lands at the vertical midpoint of the page; a filter
control appears on an empty wallet; the two empty states read the same; the Markets dashboard card
lost its compact layout; or anything overflows in the console.
  </how-to-verify>
  <resume-signal>Type "approved" or describe, per numbered item, what is wrong.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint C: Read the amounts as sentences (dark)</name>
  <action>
Walk the numbered recipe below with the human. This checkpoint is short and it is the one where the
phase can be wrong in a way no test catches: the numbers can all be correct and the row can still
tell a lie. Capture the verdict per numbered item.
  </action>
  <what-built>
Amount honesty (15-01): a processing job prints `− <fee> <symbol>` at full weight with a `<fiat>
fee` value line; a failed or cancelled transaction prints the real signed amount its completed twin
would print, quietly (`textSecondary`), with the load-bearing `Not charged` line kept beneath it.
No row anywhere prints an em dash where the number belongs.
  </what-built>
  <how-to-verify>
Still DARK only. Mock txns loaded. Walk BOTH surfaces — the page and the dashboard panel — because
the panel has no Status column and the design contract turns on that.

1. Find the **Computing / processing job** row. It must read a real negative fee — a minus sign, a
   number, the symbol — at the SAME visual weight as a successful send, with a dollar value line
   ending in the word `fee`. It must NOT show a dash. Ask yourself the design question directly:
   does it now read as "this job spent money", which it did?
2. Find the **failed** row. It must read the real signed amount, but **quietly** — grey, not the
   full-weight ink a successful spend gets. Beneath it, **`Not charged`**. That line is
   load-bearing (sketch 022): read the row as a sentence and confirm it says *this much was
   attempted, nothing was charged, it failed* — not *this much left the wallet*. If a person
   scanning the column could double-count that money, the phase failed its own argument.
3. The **cancelled** row, same check.
4. If the mock batch has a failed RECEIVE, confirm its amount is **not green**. Green is this app's
   success colour and the transaction failed. (If there is no such row, say so — it is covered by
   test, and this is a nice-to-have observation, not a gate.)
5. Scan the whole list: **no row anywhere shows an em dash in the amount column.** That is the
   headline claim of 15-01.
6. Now the **dashboard panel**, same rows. It has no Status column, so confirm the state is still
   carried: the red `failed` badge on the coin icon, and `· Failed` in the subtitle. Exactly once —
   not twice, not absent.
7. Tap a failed row and a job row to open the detail drawer. Confirm the drawer does not contradict
   the row it was opened from.

STOP and report if: any amount column still shows a dash; a failed row reads at full weight or in
green; `Not charged` is missing from any dead row; or the panel's failed row no longer states its
status.
  </how-to-verify>
  <resume-signal>Type "approved" or describe, per numbered item, what is wrong.</resume-signal>
</task>

</tasks>

<risk>
**The walk is DARK MODE ONLY, and that is a policy, not an omission.**
`.planning/todos/pending/2026-07-22-light-mode-verification-backlog.md` records the confirmed
policy: dark is what ships and what gets walked; light gets one dedicated pass across the whole
app. Every colour decision in this phase is still required to clear AA in both appearances and that
is already enforced by unit test — 15-04's rule-contrast test measures light explicitly. So if
anything light-mode-related is noticed in passing, **append it as a note to that todo file and keep
going.** A light-mode finding is never a blocker on this phase, never a reason to stop a
checkpoint, and never a reason to withhold approval.

**Walk-driven fixes follow Rule 1**: if the walk finds a defect in this phase's own deliverable,
fix it and re-walk that item — but do NOT commit. `./CLAUDE.md` holds the commit gate for the user
and this phase has no exemption. Report the fix in the summary with the file and the reasoning.

**Do not accept an unearned PASS.** 05-VERIFICATION recorded `gaps_found` with 1/5 must-haves
rather than rounding up, and Phase 05 was closed with three EXPLICIT overrides rather than pretend
passes. Any criterion not actually observed is OUTSTANDING, with the exact recipe to observe it.
The SGNUS branch is the most likely honest OUTSTANDING here — it needs an SGNUS wallet selected and
has no automated coverage at all.

**The console is part of the walk.** Zero `RenderFlex overflowed`, zero exceptions, zero
`LateInitializationError` across every relaunch is the evidence standard 06-01 used. Release builds
clip overflows SILENTLY (that is how the chart overflow hid), so a clean debug console is the only
signal available. Watch it hardest during Checkpoint A steps 10 and 11 — a window drag is where
`37639d5`'s freeze lived.

**Another session owns files this walk must not touch:** `lib/screens/boot_sequence.dart`,
`tool/boot_sequence_check.dart`, `test/boot_sequence_test.dart`, `.planning/phases/13-*`,
`.planning/phases/14-*`, `.planning/sketches/015-018`, `.planning/spikes/`. If a walk-driven fix
would land in any of them, STOP and report rather than editing.
</risk>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Automated gates -> "it works" | `flutter analyze` has passed clean on 37 shipped regressions in this project; it is a precondition, not evidence |
| Dev fixture -> the walked UI | Everything walked here is mock data behind `GW_DEV_TOOLS=true`; a real wallet may still differ |
| One phase's change -> four other screens | `GWEmptyState` is shared by Assets, Markets, the gallery and both transactions branches |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-11 | Repudiation | rail active mark loudness | high | mitigate | Checkpoint A step 6 puts the one question sketch 022 deferred — is B2 loud enough in a permanent rail — in front of human eyes, with variant D named as the recorded fallback |
| T-15-01 | Repudiation | failed-row amount | high | mitigate | Checkpoint C step 2 asks the human to read the row as a sentence rather than check fields; the double-counting failure mode is exactly what a field-by-field check misses |
| T-15-04 | Denial of service | shared `GWEmptyState` / freeze | high | mitigate | Checkpoint B steps 5-8 walk all four non-transactions call sites; Checkpoint A step 11 drags the window through the range `37639d5` froze on, with the console watched |
| T-15-16 | Repudiation | SGNUS branch | medium | mitigate | Named explicitly as the likeliest OUTSTANDING; recorded with its recipe rather than marked PASS by association |
| T-15-18 | Repudiation | unearned PASS in the phase record | high | mitigate | Every criterion not observed is recorded OUTSTANDING with its recipe; light-mode findings are recorded as notes in the backlog todo, never as silent skips |
| T-15-15 | Spoofing | dev mock data | low | accept | Walked deliberately as mock; the injector is behind compile-time const bools and folds out of release |
</threat_model>

<verification>
- All three checkpoints returned "approved", or their defects were fixed under Rule 1 and the
  affected items re-walked.
- Console evidence across every relaunch: zero `RenderFlex overflowed`, zero exceptions.
- Every criterion has a verdict: PASS (observed), OUTSTANDING (with recipe), or an explicit, named
  override.
- Any light-mode observation is appended to
  `.planning/todos/pending/2026-07-22-light-mode-verification-backlog.md` — and its absence from
  this phase's verdicts is correct, not an omission.
</verification>

<success_criteria>
- Every one of TT-01..TT-06 has a recorded, observed dark-mode verdict.
- No criterion is marked PASS on the strength of `flutter analyze` or a unit test alone.
- Checkpoint A step 6's judgement call (is B2 loud enough?) has an explicit answer, and if the
  answer is no, variant D is recorded as the proposed follow-up rather than applied silently.
- The four non-transactions `GWEmptyState` call sites each have a verdict.
- Light-mode notes, if any, live in the backlog todo — and no light-mode finding blocked anything.
- No commit created.
</success_criteria>

<output>
Create `.planning/phases/15-transactions-tab/15-06-SUMMARY.md` when done, with:
- a per-criterion table (TT-01..TT-06) with verdict and evidence;
- the answer to Checkpoint A step 6, in the human's own words;
- the shared-component verdicts for Assets, dashboard Markets, `/markets` and the gallery;
- any walk-driven fix, with file, reasoning and the fact that it is UNCOMMITTED;
- anything that should become a todo rather than a fix in this phase — including the SGNUS branch
  if it went unexercised.
</output>
