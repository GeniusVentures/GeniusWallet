---
phase: 18-web-tab-chrome-in-app-browser-address-bar-and-tab-strip-rede
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - lib/web/web_view_mobile.dart
  - pubspec.yaml
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "An always-visible horizontal tab strip sits between the app navbar and the omnibox (~46px)."
    - "Each tab shows favicon + title (getTitle() with URL fallback) + a close x; the + at the end adds a DuckDuckGo tab."
    - "The active tab wears the navbar's mark: surfaceElevated fill + a 2px brandCta gradient underline."
    - "The last remaining tab cannot be closed (its x reads disabled/absent)."
    - "The old full-screen tab manager, the upside-down thumbnails, and the now-dead Screenshot machinery are gone; flutter analyze is clean."
  artifacts:
    - lib/web/web_view_mobile.dart
    - pubspec.yaml
  key_links:
    - "_buildTabStrip -> _switchTab / _closeTab / _addNewTab (preserved verbatim)"
    - "x close enable state -> webTabCanClose(_controllers.length) from web_chrome_helpers.dart"
    - "active underline -> GeniusWalletGradient.brandCta (light AA via the transactions_slim_view shader precedent or a recorded deferred finding)"
---

<objective>
Replace the counter-button + full-screen `_buildTabManager` with the 036-A always-visible horizontal
tab strip (D-04/05/06) inserted above the omnibox in `web_view_mobile.dart`, and delete the dead tab
manager + `Screenshot`/`ScreenshotController` machinery (D-07). Chrome only — every tab mechanic
(`_addNewTab`, `_switchTab`, `_closeTab`, last-tab-locked) is preserved verbatim (D-09).

Purpose: tabs become zero-click and legible, using the navbar's own active mark for consistency;
removes the upside-down-thumbnail bug and its screenshot plumbing entirely.
Output: the tab strip UI plus a net code deletion (manager + screenshot code + unused dependency).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-web-tab-chrome-in-app-browser-address-bar-and-tab-strip-rede/18-CONTEXT.md
@.planning/sketches/036-web-tab-strip/README.md
@.planning/sketches/037-web-chrome-combined/README.md
@lib/web/web_view_mobile.dart
@lib/web/web_chrome_helpers.dart
@lib/theme/genius_wallet_colors.dart
@lib/theme/genius_wallet_consts.dart
@lib/theme/genius_wallet_gradient.dart

# Active-mark precedent (sketch 022 B2): 2px brandCta rule routed for light AA
@lib/dashboard/home/widgets/transactions_slim_view.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 036-A horizontal tab strip</name>
  <files>lib/web/web_view_mobile.dart</files>
  <action>
    Add a _buildTabStrip() widget and insert it into the build() Column (web_view_mobile.dart:268-273)
    as the FIRST child, above _buildSearchBar — order becomes [_buildTabStrip(), _buildSearchBar(),
    Expanded(webview)] (D-04/D-08, ~46px band).
    Strip = a horizontal SingleChildScrollView / Row of tab chips plus a trailing `+`:
      - Each tab i: favicon (Image.network(_getFaviconUrl(_tabUrls[i])) with the existing Icons.language
        errorBuilder) + title from FutureBuilder<String?>(_controllers[i].getTitle()) falling back to
        _tabUrls[i] (the existing pattern at :446-471; "Loading..." while waiting), maxLines 1 ellipsis,
        + a close `x` (Icons.close).
      - Active tab (i == _currentTabIndex) mark (D-05): fill GeniusWalletColors.surfaceElevated + a 2px
        GeniusWalletGradient.brandCta underline drawn under the tab. Reuse the active-underline language
        from sketch 022 B2 — look at transactions_slim_view.dart's active-row rule (the `_activeLabelShader`
        / brandCta routing, ~:547-575) and mirror it so the underline clears WCAG 1.4.11 3:1 in BOTH
        appearances. Do NOT invent a new active language.
        NOTE (plan-checker warning, resolved): `_activeLabelShader(GWColors gw)` takes a `GWColors`
        ThemeExtension instance, which `web_view_mobile.dart` does NOT use (it uses the static
        `GeniusWalletColors` class). Do NOT introduce a `GWColors`/`Theme.of(context)` dependency —
        the same light/dark branch is available directly on `GeniusWalletColors` (backed by
        `GWAppearance.isLight`, genius_wallet_colors.dart:45). Read `brandCta` from
        `GeniusWalletGradient` as the file already imports it, and branch the light-AA variant via
        `GeniusWalletColors`'s own getters — no new import beyond what the file already has.
      - Inactive tabs: muted (textPrimary60 label, transparent fill); hover = GeniusWalletColors.borderSubtle
        fill / subtle lift.
      - Tap a tab -> _switchTab(i) (:246, verbatim). `x` -> _closeTab(i) (:235, verbatim). The `x` is
        shown enabled only when webTabCanClose(_controllers.length) is true (from web_chrome_helpers.dart,
        18-01); on a lone tab the `x` reads disabled/absent (D-06 last-tab-locked; do not change the guard
        inside _closeTab).
      - Trailing `+` (Icons.add) -> _addNewTab("https://www.duckduckgo.com") (:500 default URL, verbatim, D-06).
    All colours/spaces/radii come from GeniusWalletColors / GeniusWalletConsts / GeniusWalletGradient;
    record any token deviation as a finding. If the light-mode gradient underline cannot clear 3:1 within
    this plan's budget, ship the dark-correct version and record a deferred light finding for the app-wide
    light pass (per CONTEXT: light-only bugs may defer).
  </action>
  <verify>
    <automated>flutter analyze lib/web/web_view_mobile.dart</automated>
  </verify>
  <done>flutter analyze clean; the strip renders above the omnibox with favicon+title+x per tab, the active tab wears surfaceElevated + a 2px brandCta underline, + adds a DuckDuckGo tab, and the lone-tab x is disabled/absent. _switchTab/_closeTab/_addNewTab unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the full-screen tab manager and dead Screenshot machinery</name>
  <files>lib/web/web_view_mobile.dart, pubspec.yaml</files>
  <action>
    Delete the obsolete full-screen tab model (D-07). Remove:
      - _buildTabManager() (web_view_mobile.dart:374-513) including the Matrix4.rotationX(pi) upside-down
        thumbnails.
      - the _showTabManager field (:30) and the `if (_showTabManager) Positioned.fill(...)` overlay branch
        in build() (:274-282), leaving build() = SafeArea > Column[_buildTabStrip, _buildSearchBar,
        Expanded(_buildWebView)]. The outer Stack can collapse to just the Column.
      - the Screenshot/ScreenshotController machinery that ONLY fed those thumbnails: the
        `import 'package:screenshot/screenshot.dart';` (:6), `import 'dart:math';` (:2, only used by
        Matrix4.rotationX(pi)), the screenshotController field (:27), captureScreenshot() (:195-205),
        the `_tabImages` list (:26) and every add/removeAt/index write to it, the `captureScreenshot()`
        call in onPageFinished (:170-173), and the Screenshot(...) wrapper in _buildWebView (:254-259)
        so it returns WebViewWidget(controller: _controllers[index]) directly.
      - the `_showTabManager = false` line inside _switchTab (:250) once the field is gone.
    Grounding: grep confirms `package:screenshot` / ScreenshotController / Screenshot( are used ONLY in
    web_view_mobile.dart, so removal is self-contained. After deleting all references, remove the
    `screenshot: ^3.0.0` line from pubspec.yaml (dependencies) and run `flutter pub get` so pubspec.lock
    updates (deletion over addition — the dep is now unused). Do NOT touch _addNewTab, the navigation
    delegates, the Uniswap JS, or the banner MutationObserver (D-09).
  </action>
  <verify>
    <automated>flutter analyze lib/web/web_view_mobile.dart</automated>
  </verify>
  <done>_buildTabManager, _showTabManager, _tabImages, screenshotController, captureScreenshot, the Screenshot wrapper, and the screenshot + dart:math imports are gone; screenshot removed from pubspec; `grep -n "screenshot\|_tabImages\|Matrix4\|_showTabManager" lib/web/web_view_mobile.dart` returns nothing; flutter analyze is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote page title/favicon -> tab chip | getTitle()/favicon rendered as tab identity (display only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-03 | Spoofing | tab title/favicon | low | accept | Titles are display-only chip labels (maxLines 1, ellipsis), not a security signal; identical to prior behavior, no new trust surface. |
| T-18-04 | Tampering | removing the `screenshot` dependency | low | mitigate | Removal is gated on grep proving web_view_mobile.dart is the sole consumer; `flutter analyze` + `flutter pub get` confirm no dangling references before the walk. No package is INSTALLED (removal only), so no legitimacy gate applies. |
</threat_model>

<verification>
- `flutter analyze` clean; `grep -n "screenshot\|_tabImages\|Matrix4\|_showTabManager" lib/web/web_view_mobile.dart` is empty.
- The 18-01 helper test still passes (`flutter test test/web/web_chrome_helpers_test.dart`).
- Human walk (macOS dark, then light AA spot-check): tab strip is always visible above the omnibox.
  Open 3-4 tabs with `+` (each a DuckDuckGo tab), click between them (active tab wears surfaceElevated +
  brandCta underline), close tabs; the final tab's x is disabled/absent. No full-screen manager appears,
  no upside-down thumbnails. Real browsing (navigate, search-fallback, favicon+title populate) is
  unregressed. Light mode: active underline holds 3:1 or is recorded as a deferred light finding.
</verification>

<success_criteria>
Tabs are the 036-A always-visible strip with favicon+title+close and a + that adds a DuckDuckGo tab;
the active tab wears the navbar's surfaceElevated + 2px brandCta underline; last-tab-locked holds. The
full-screen manager, upside-down thumbnails, and all Screenshot code/dependency are deleted; analyze clean.
</success_criteria>

<output>
Create `.planning/phases/18-web-tab-chrome-in-app-browser-address-bar-and-tab-strip-rede/18-02-SUMMARY.md` when done.
</output>
