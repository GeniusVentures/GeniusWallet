---
phase: 18-web-tab-chrome-in-app-browser-address-bar-and-tab-strip-rede
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - lib/web/web_view_windows.dart
autonomous: true
requirements: [D-01, D-02, D-03, D-09]
must_haves:
  truths:
    - "The Windows address bar reads as the same 035-B omnibox as macOS: one field with nested back/forward, favicon + https lock + host, refresh, and an outside menu affordance."
    - "Back/forward states trace to canGoBack()/canGoForward(); refresh calls _controller.reload; submit routes through _loadUrl."
    - "The WalletConnect clipboard poller and history-based tab switcher are untouched."
  artifacts:
    - lib/web/web_view_windows.dart
  key_links:
    - "_buildSearchBar (windows) -> omnibox using webDisplayHost/webIsSecure from web_chrome_helpers.dart"
    - "back/forward/refresh -> goBack / goForward / _controller.reload (existing sync API)"
---

<objective>
Port the 035-B omnibox re-skin to the Windows path (`_buildSearchBar`, `web_view_windows.dart:230`) so
the two platforms read as one design (D-01/02/03). Windows is secondary: the omnibox re-skin is the
deliverable; the always-visible tab strip is an explicit stretch goal NOT attempted here, because the
Windows tab model is a history list (`_showTabSwitcher`), not real tabs — that dialog stays as-is.
Chrome only; the WalletConnect clipboard poller and all engine wiring are untouched (D-09).

Purpose: Windows shouldn't visibly lag the macOS chrome; the omnibox ports cleanly.
Output: the rewritten Windows omnibox reusing the shared helpers from 18-01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-web-tab-chrome-in-app-browser-address-bar-and-tab-strip-rede/18-CONTEXT.md
@.planning/sketches/035-web-address-bar/README.md
@lib/web/web_view_windows.dart
@lib/web/web_chrome_helpers.dart
@lib/theme/genius_wallet_colors.dart
@lib/theme/genius_wallet_consts.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-skin the Windows _buildSearchBar into the 035-B omnibox</name>
  <files>lib/web/web_view_windows.dart</files>
  <action>
    Rewrite _buildSearchBar (web_view_windows.dart:230-269) to the same omnibox language as macOS
    (18-01, D-01/02/03), adapting to the Windows controller API (note: canGoBack()/canGoForward() here
    are SYNCHRONOUS bools at :113-114 — no FutureBuilder needed, unlike the mobile path). Deliberately
    do NOT extract a shared omnibox widget across the two files: the controllers and their APIs diverge
    (WebViewController async vs WebviewController sync) and a shared widget would need generic callbacks —
    more code than it saves. Reuse is limited to the pure helpers (webDisplayHost/webIsSecure from
    web_chrome_helpers.dart); mark this decision with a ponytail comment.
    Build ONE field container + outside `⋯`:
      - Field fill GeniusWalletColors.surfaceSunken, radius GeniusWalletConsts.radiusBase, rest border
        GeniusWalletColors.borderSubtle. LEFT edge nests back (Icons.arrow_back, enabled/brand when
        canGoBack(), else textPrimary38, onPressed goBack) and forward (Icons.arrow_forward, same rule on
        canGoForward(), onPressed goForward) — reuse the existing _buildIconButton but retint per state.
      - Middle: unfocused -> favicon (Image.network(_getFaviconUrl(current url))... add a small
        _getFaviconUrl(host) helper mirroring the mobile one, OR inline google.com/s2/favicons — favicon
        service is fenced by D-09, keep it identical) + https lock (Icons.lock, statusSuccess) only when
        webIsSecure(current url) + webDisplayHost(current url) as textPrimary. Focused -> the editable
        TextField bound to _urlController (full URL). Use a FocusNode (dispose it) for the swap, same as
        18-01.
      - Focus ring: 2px GeniusWalletColors.brandPrimary + brandPrimarySubtle glow on focus; borderSubtle at rest.
      - RIGHT edge inside the field: refresh (Icons.refresh) -> _controller.reload (already wired at :246).
      - Outside the field on the right: replace the current tab icon (Icons.tab -> _showTabSwitcher) with
        a `⋯` overflow affordance that keeps opening _showTabSwitcher (the existing history dialog) — this
        preserves the only tab affordance Windows has without adding a real tab strip. Do not change
        _showTabSwitcher itself.
    Submit stays onSubmitted -> _loadUrl (:262, unchanged). All colours/spaces/radii are tokens; record
    deviations as findings. Do NOT touch the clipboard poller, _initializeWebView, history wiring, or
    dispose logic (D-09).
  </action>
  <verify>
    <automated>flutter analyze lib/web/web_view_windows.dart</automated>
  </verify>
  <done>flutter analyze clean; the Windows omnibox renders as one field with nested state-correct back/forward, favicon+lock+host at rest, editable URL + brand focus ring on focus, refresh, and an outside menu affordance that still opens the history dialog. _loadUrl, the clipboard poller, and _showTabSwitcher are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote page identity -> chrome display | host string + https lock rendered as trust signals (display only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-05 | Spoofing | Windows omnibox lock + host | low | mitigate | Lock shows only when webIsSecure()==true; host derives from Uri parse of the real current URL (the `_controller.url` stream value), not user free-text. |
</threat_model>

<verification>
- `flutter analyze` clean for web_view_windows.dart.
- Note: no macOS/iOS runtime walk is possible for the Windows path on Jakub's dev machine; verification
  here is analyze-clean + code review against 18-01's omnibox. Windows runtime confirmation is deferred
  to whoever next runs the Windows build. Full Windows tab strip remains a stretch goal (out of scope).
</verification>

<success_criteria>
The Windows address bar wears the 035-B omnibox (nested state-correct back/forward, favicon + https lock
+ host, focus ring, refresh, outside menu), reusing the shared helpers. Clipboard poller, history switcher,
and engine wiring untouched. analyze clean.
</success_criteria>

<output>
Create `.planning/phases/18-web-tab-chrome-in-app-browser-address-bar-and-tab-strip-rede/18-03-SUMMARY.md` when done.
</output>
