---
phase: 33-app-and-sdk-data-out-of-the-documents-folder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/genius_api/lib/src/app_data_directory.dart, packages/genius_api/lib/genius_api.dart, test/app_data_directory_test.dart, lib/hive/init.dart, packages/genius_api/lib/src/genius_api.dart, lib/main.dart, .planning/reference/FRESH-INSTALL-RECIPE.md]
autonomous: true
requirements: []
estimate: {tokens: 40000, raw_tokens: 40000, tasks: 3, confidence: low}
must_haves:
  truths:
    - 'Windows/Linux, Documents holding neither wallet.hive nor secure_storage_id: Hive boxes, the SDK tree and the Sentry log read all use %LOCALAPPDATA%\GeniusVentures\GeniusWallet (Linux: $XDG_DATA_HOME/GeniusWallet, else ~/.local/share/GeniusWallet), created on first use (SC1, D-02, D-04)'
    - "Documents holding either marker, even a 0-byte wallet.hive: all three consumers stay on Documents, nothing is moved, copied or deleted, and the new folder is never created (SC2 as reshaped by D-01)"
    - "Android, iOS and macOS resolve to the same Documents directory as before (SC4)"
    - "Hive, the SDK base path and the Sentry log read take the folder from one memoized resolver, first awaited before any Hive box opens (D-03)"
    - "FRESH-INSTALL-RECIPE.md resets both locations, including secure_storage_id"
  artifacts:
    - {path: packages/genius_api/lib/src/app_data_directory.dart, provides: "pickAppDataDirectory (pure) + appDataDirectory (memoized, creates the chosen folder)"}
    - {path: test/app_data_directory_test.dart, provides: "existing-install, new-install and non-desktop cases on temp dirs"}
  key_links:
    - {from: lib/hive/init.dart initHive, to: appDataDirectory, via: "Hive.init(path) before the first openBox"}
    - {from: "genius_api.dart prepareConfigFiles", to: appDataDirectory, via: "SDK base path"}
    - {from: "main.dart _attachSdkLogsToHint", to: appDataDirectory, via: "sgnslog*.log read"}
---

<objective>
New Windows/Linux installs keep app and SDK data in their own local data folder; existing installs stay in Documents
untouched (D-01..D-04). SC3 (a move failing part-way) is moot: D-01 removed the move, so there is nothing to fail.
Output: one resolver, its test, three consumers rewired, and a reset recipe that covers both locations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/33-app-and-sdk-data-out-of-the-documents-folder/33-CONTEXT.md
Every command runs after `export PATH="/c/Users/User/Documents/Projects/GNUS/flutter/flutter/bin:$PATH"`. Commits land on
`phase-33-app-data-location`. `flutter analyze` exits 1 on infos: judge it by `$?`, not by the tail of the output.
Source comments: WHY only, at most 3 lines, never a plan/phase/decision id or a test file name.
This machine is an EXISTING install (Documents holds wallet.hive, secure_storage_id and a node dir). Nothing in this plan
may move, rename or delete anything in the user's Documents or AppData.
<!-- planner-discipline-allow: getApplicationDocumentsDirectory -->
<!-- planner-discipline-allow: initFlutter -->
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Resolver decides the folder and Hive opens its boxes there</name>
  <reversibility rating="one-way">Installs that start after this ships write into the D-04 folder; moving it later needs a migration. Chosen explicitly in D-04, so no re-ask.</reversibility>
  <files>packages/genius_api/lib/src/app_data_directory.dart, packages/genius_api/lib/genius_api.dart, test/app_data_directory_test.dart, lib/hive/init.dart</files>
  <read_first>lib/hive/init.dart (26-43), packages/genius_api/lib/genius_api.dart, lib/hive/constants/cache.dart (13), test/hive/hive_lock_test.dart</read_first>
  <behavior>
    - windows, empty temp Documents, LOCALAPPDATA=T: returns T\GeniusVentures\GeniusWallet, and that folder does not exist afterwards (pick is side-effect free)
    - windows, Documents with a 0-byte wallet.hive: returns Documents. Same with only secure_storage_id. Only an unrelated notes.txt: returns the local folder
    - windows with LOCALAPPDATA missing or empty: returns Documents
    - linux: XDG_DATA_HOME=/x/data gives /x/data/GeniusWallet; empty or relative XDG_DATA_HOME with HOME=/home/u gives /home/u/.local/share/GeniusWallet; wallet.hive in Documents gives Documents
    - macos, android, ios with empty Documents: return Documents
    - walletBoxName is 'wallet' (reason string: existing installs are detected by wallet.hive)
  </behavior>
  <action>Per D-02, D-03, D-04. Write the test first (root suite, it imports package:genius_api and the app's hive cache constants) and watch it fail. Then create app_data_directory.dart, exported from the genius_api barrel, with two public top-level functions and no class.
pickAppDataDirectory({required Directory documents, required String operatingSystem, required Map of String to String environment}) is synchronous and creates nothing. (a) Local folder: 'windows' is LOCALAPPDATA joined with GeniusVentures and GeniusWallet using backslashes; 'linux' is XDG_DATA_HOME when non-empty and starting with '/', else HOME plus /.local/share, then /GeniusWallet. Any other OS, or a missing or empty variable, means no local folder. (b) No local folder: return documents. (c) documents already holds wallet.hive or secure_storage_id: return documents. Test with FileSystemEntity.typeSync not equal to notFound, so a 0-byte file or a directory counts. (d) Otherwise return the local folder.
appDataDirectory() returns a Future of Directory, memoized in a private top-level nullable Future so every caller in the process gets one answer. It awaits getApplicationDocumentsDirectory, picks with Platform.operatingSystem and Platform.environment, awaits create(recursive: true) on the result (a no-op for Documents) and returns it. The doc comment says why in at most 3 lines: an install with data already in Documents stays there so its wallets never look missing, and wallet.hive is the app's walletBoxName box.
In initHive, replace the Hive Flutter helper call with Hive.init on appDataDirectory's path, before the first openBox. That drops the helper's Color and TimeOfDay adapters; no box stores either, so add nothing back. Leave the rest of initHive and its doc comment alone.</action>
  <verify><automated>flutter test test/app_data_directory_test.dart test/hive/ && flutter analyze lib/hive/init.dart packages/genius_api/lib test/app_data_directory_test.dart; echo "analyze exit $?"</automated></verify>
  <acceptance_criteria>
    - The test file was run red before the resolver existed, then passes, together with test/hive/
    - `grep -c "appDataDirectory" lib/hive/init.dart` prints 1 and analyze exits 0
    - `grep -cE "\.(delete|rename)" packages/genius_api/lib/src/app_data_directory.dart` prints 0 (the resolver never moves or removes data)
  </acceptance_criteria>
  <done>On a new Windows/Linux install every Hive box lands in the local folder; an existing install still opens its boxes from Documents.</done>
</task>

<task type="auto">
  <name>The SDK base path and the Sentry log read follow the same folder</name>
  <reversibility rating="reversible">Two call sites swap one path source for another behind the same return value.</reversibility>
  <files>packages/genius_api/lib/src/genius_api.dart, lib/main.dart</files>
  <read_first>packages/genius_api/lib/src/genius_api.dart (30-36, 372-423), lib/main.dart (35-46)</read_first>
  <action>Per D-03. prepareConfigFiles: take `directory` from appDataDirectory() instead of path_provider. Then drop this file's now-unused path_provider import; the resolver file keeps its own. Everything downstream derives from this return value (jsonFilePath, overridesDirPath, the settings screen's reload, the logs screen), so nothing else changes. main.dart _attachSdkLogsToHint: make the same swap, rename docsDir to dataDir, and drop main.dart's path_provider import. Keep its ignore comment and the commented-out call site as they are. The root pubspec stays untouched.</action>
  <verify><automated>! grep -rn --include='*.dart' -e getApplicationDocumentsDirectory -e initFlutter lib packages/genius_api/lib packages/local_secure_storage/lib | grep -v app_data_directory.dart && flutter analyze lib/main.dart packages/genius_api/lib; echo "analyze exit $?"</automated></verify>
  <acceptance_criteria>
    - No Documents-directory call and no Hive Flutter helper remains in app code outside app_data_directory.dart (the grep above prints nothing)
    - `grep -c "appDataDirectory" lib/main.dart packages/genius_api/lib/src/genius_api.dart` prints at least 1 for each file
    - analyze exits 0 (in particular no unused_import)
  </acceptance_criteria>
  <done>Hive, the SDK and the Sentry log read cannot disagree about the folder, because all three ask one memoized resolver.</done>
</task>

<task type="auto">
  <name>Reset recipe covers both folders, and the existing install still loads from Documents</name>
  <precondition>`Test-Path "$env:USERPROFILE\Documents\wallet.hive"` prints True (this machine is an existing install).</precondition>
  <files>.planning/reference/FRESH-INSTALL-RECIPE.md</files>
  <read_first>.planning/reference/FRESH-INSTALL-RECIPE.md</read_first>
  <action>Docs only (D-01, D-02; recipe update is in Claude's discretion). Add a short "Which folder?" note near the top: an install whose Documents held wallet.hive or secure_storage_id keeps using Documents, and any other Windows install keeps layers 1-2 in %LOCALAPPDATA%\GeniusVentures\GeniusWallet. So deleting the Documents markers turns the next launch into a new install. Step 0 counts SuperGNUSNode.Node.* in both folders. Layer rows 1-2 name both folders. Recipe targets gain Documents' secure_storage_id, sgnslog*.log, my_tasks.json and ipfs_cache as exact names, never a broad glob, because the user keeps unrelated files in Documents. They also gain the whole %LOCALAPPDATA%\GeniusVentures\GeniusWallet folder, but not its GeniusVentures parent. Keep the DryRun default of true, the Credential Manager warning and the *.hive glob section.</action>
  <verify><automated>test "$(grep -c 'GeniusVentures\\GeniusWallet' .planning/reference/FRESH-INSTALL-RECIPE.md)" -ge 2 && grep -q 'secure_storage_id' .planning/reference/FRESH-INSTALL-RECIPE.md && grep -qF '$DryRun = $true' .planning/reference/FRESH-INSTALL-RECIPE.md</automated>
    <human-check>Existing-install walk on this Windows machine; the executor runs every step itself. Before: `Test-Path "$env:LOCALAPPDATA\GeniusVentures"` is False; Documents holds wallet.hive and secure_storage_id; note the SuperGNUSNode.Node.* count. Kill any genius_wallet.exe, then run the recipe's `flutter run -d windows` command. If the SDK starts, the console's "Base path directory:" line names the Documents path. Braian confirms that the dashboard shows his existing wallet, its transaction history, and the same SDK accounts as before. After quitting: LOCALAPPDATA\GeniusVentures is still absent, and both markers and the node-dir count are unchanged. Never set $DryRun to $false, never touch Documents. The new-install path is covered by the unit test only. Documents is a Windows known folder that one process cannot redirect, a data-dir override was rejected, and the test pins every branch.</human-check></verify>
  <acceptance_criteria>
    - The recipe names both folders and secure_storage_id, and still defaults to a dry run
    - The walk above passes with no new folder under %LOCALAPPDATA% and nothing changed in Documents
  </acceptance_criteria>
  <done>An existing install behaves exactly as before, and the reset recipe produces a genuinely fresh profile under the new rule.</done>
</task>

</tasks>

<threat_model>
Trust boundary: local filesystem and per-user environment variables (same trust as the user running the app).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Denial of Service | pickAppDataDirectory on an existing install | high | mitigate | Wrong pick makes wallets look gone. Detection is by existence (a 0-byte wallet.hive counts). The test pins both markers and walletBoxName; the walk confirms on a real existing install |
| T-33-02 | Tampering | resolver data loss | high | mitigate | The resolver only ever creates the chosen folder; task 1 greps it for delete/rename; the recipe keeps DryRun on by default |
| T-33-03 | Tampering | LOCALAPPDATA / XDG_DATA_HOME | low | accept | User-controlled at user trust. A relative XDG value is ignored per the spec; a missing value falls back to Documents |
| T-33-04 | Denial of Service | new install, then an older build writes Documents | low | accept | The next start picks Documents (D-02). The new-folder data stays on disk untouched and recoverable |
| T-33-05 | Information Disclosure | data location | low | accept | Net improvement: %LOCALAPPDATA% is per-user and never synced, unlike a OneDrive-backed Documents, which also exposed wallet addresses as filenames |
</threat_model>

<verification>
`dart format --output=none --set-exit-if-changed` on the touched Dart files; `flutter analyze` with `$?` 0; `bash tool/check_brace_style.sh`;
full `flutter test` run before task 1 and after task 3 shows no new failure; `git ls-files --eol` shows `i/lf` for the two new Dart files.
</verification>

<success_criteria>
The resolver test passes; no Documents-directory lookup remains outside the resolver; the existing-install walk passes on Windows.
</success_criteria>

<output>
Create `.planning/phases/33-app-and-sdk-data-out-of-the-documents-folder/33-01-SUMMARY.md` (at most 40 lines), stating that the
new-install path and Linux were verified by the unit test only.
</output>
