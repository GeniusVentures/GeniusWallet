# This file configures the static analysis results for your project (errors,
# warnings, and lints).
#
# This enables the 'recommended' set of lints from `package:lints`.
# This set helps identify many issues that may lead to problems when running
# or consuming Dart code, and enforces writing Dart using a single, idiomatic
# style and format.
#
# If you want a smaller set of lints you can change this to specify
# 'package:lints/core.yaml'. These are just the most critical lints
# (the recommended set includes the core lints).
# The core lints are also what is used by pub.dev for scoring packages.

include: package:lints/recommended.yaml

analyzer:
  # Machine-generated bindings. These are not hand-edited, and linting them is
  # pure noise: on 2026-07-28 `lib/ffi/trust_wallet_api_ffi.dart` alone produced
  # 214 of the repo's 408 analyzer issues.
  #
  # These excludes MUST live in this file, not the repo-root analysis_options.yaml.
  # The analyzer resolves the *nearest* options file to each source file, so the
  # root file's excludes never applied to anything under packages/genius_api/ —
  # which is why the root's `packages/genius_api/lib/proto` entry did nothing.
  exclude:
    - lib/ffi/**
    - lib/proto/**

# Uncomment the following section to specify additional rules.

# linter:
#   rules:
#     - camel_case_types

# For more information about the core and recommended set of lints, see
# https://dart.dev/go/core-lints

# For additional information about configuring this file, see
# https://dart.dev/guides/language/analysis-options

formatter:
  page_width: 80
