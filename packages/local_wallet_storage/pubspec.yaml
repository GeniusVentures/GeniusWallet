name: local_wallet_storage
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.18.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^7.0.1
  flutter_test:
    sdk: flutter
    version: ^0.0.0
  wallet_storage_template:
    path: "../wallet_storage_template"
  genius_api:
    path: "../genius_api"

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
